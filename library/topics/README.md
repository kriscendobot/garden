# Topics

Concept-keyed taxonomy of the library's section files. An agent looking for material on a concept arrives here, scans the abstracts, and follows the matching topic page to its section table. Each topic page lists the sections currently filed under it with a one-line abstract per row.

The taxonomy below is the **seed** from the pilot ingestion (2026-05-13). It will grow and refactor as the corpus expands. The `conventions.md` document on `journal/library/` describes when to add a new topic, when to split, and when to merge.

## Index

| Topic | Abstract | Sections |
|-------|----------|----------|
| [llm-agent-frameworks](llm-agent-frameworks.md) | External LLM-application / agent frameworks ingested as cross-cutting reference: LangChain (the agent framework: create_agent harness, standard cross-provider model interface, tool calling, retrieval/RAG, memory) and LangGraph (the low-level orchestration runtime: nodes/edges over a reducer-merged State, Pregel super-steps, checkpointer persistence, durability modes, time travel, human-in-the-loop). | 72 |
| [agent-fleet-durability](agent-fleet-durability.md) | The durability and crash-safety layer beneath an autonomous agent fleet — how coordination state stays correct across concurrent writers, process death, reboots, and self-restart with no human watching: atomic multi-place claim state (board+ledger+worktree), liveness proven from a held flock never inferred from occupancy, branch-landed≠task-done, git-ref RMW-CAS and journal-then-untrack cutover, crash-safe write ordering, source-gated killswitch clears, model-guarded resume, and context-exhaustion roll-forward. The state-integrity layer under agent-fleet-orchestration; canonical corpus is unum's LORE, mapping onto the garden's own CAS job board and stale-claim reaper. | 3 |
| [agent-fleet-orchestration](agent-fleet-orchestration.md) | Orchestrating a fleet of autonomous coding agents against a shared work queue — an issue tracker / job board as the control plane: every open task gets a dedicated agent in an isolated per-task workspace, continuous poll–dispatch–supervise–retry, a dependency DAG, agents filing their own follow-up work, per-task safety invariants, a headless coding-agent app-server protocol, and objectives-over-state-machine framing. Canonical example: OpenAI's Symphony (Linear + Codex App Server). The multi-agent management layer above llm-agent-frameworks; the closest in-corpus analogue is the garden's own job-board/orchestrator model. | 26 |
| [agent-conventions](agent-conventions.md) | Repository-specific instructions written for AI agents working in a project; agent-security threat-class definitions; business-agent + reputation-system foundations. | 68 |
| [chat-ui](chat-ui.md) | Familiar Chat — the web-based keyboard-first UI for the Endo daemon; UI invariants, principles, and component designs. | 70 |
| [repository-governance](repository-governance.md) | Contribution rules, security policy, commit conventions, repository structure. | 61 |
| [typescript-conventions](typescript-conventions.md) | TypeScript rules in a `.js`-runtime + `.ts`-consumer repository. | 19 |
| [exo](exo.md) | The Exo class API: `makeExo`, `defineExoClass`, `defineExoClassKit`. | 50 |
| [testing](testing.md) | How to run and write tests in endo. | 24 |
| [security-disclosure](security-disclosure.md) | Vulnerability reporting channels and timelines. | 10 |
| [errors](errors.md) | SES's tamed `Error` + `assert` + causal `console` system. | 45 |
| [hardened-javascript](hardened-javascript.md) | SES substrate: frozen intrinsics, lockdown, taming. | 96 |
| [engine-implementation](engine-implementation.md) | Engine-level (interpreter/C/native) implementation of SES/ocap primitives — how `harden`, `lockdown`, intrinsics isolation, and Compartment/realm semantics are built into a small JIT-free bytecode machine (XS, quickjs-ng), with the performance-vs-safety tradeoffs; the home for sibling-engine comparisons read for the XS→Rust (Endor) program. | 5 |
| [capability-security](capability-security.md) | Object-capability discipline as practiced in Endo / Agoric; agent-runtime applications of capability discipline. | 305 |
| [identity](identity.md) | How a user's (or agent's) identity is **decomposed into separable identifiers**, each with a distinct role, rather than conflated into one overloaded handle. Keystone: Randy Farmer's **tripartite identity pattern** (Habitat Chronicles, 2008) — account identifier (permanent, inert, random, capability-free DB key), login identifier(s) (name/password or federated capability-based logins from OpenID / OAuth / Facebook Connect), and public identifier(s) (the non-unique, compound, mutable, context-plural social face). The separation is the identity-side statement of POLA: a leaked or shared handle never doubles as a credential or a spam target. Distinct from `capability-security` (ocap practice) and `capability-theory` (the papers). | 18 |
| [decentralized-identifiers](decentralized-identifiers.md) | The W3C DID family and the DID methods that matter in practice, as one row in a broader addressing taxonomy: an identifier grounded in the **authority to update** a document, resolved indirectly, whose document carries mutable key material and a set of `service` / `serviceEndpoint` **location hints**. Collects the method split that decides whether that framing even holds (self-certifying / verifiable-history methods such as `did:plc`, `did:webvh`, and the degenerate `did:key`, versus the DNS-and-TLS-grounded `did:web`, which collapses back into the URL row), the parsing contract a consumer applies to a resolved document, and the deployed worked example, ATProto, whose `#atproto_pds` entry moves on account migration while the DID does not. Distinct from `identity` (identity decomposition, the tripartite pattern) and `ucan-authorization` (DIDs as *principals*, not as resolvable *subjects*). | 27 |
| [capability-theory](capability-theory.md) | Theoretical foundations of object-capability security: four models, seven properties, POLA, confused deputies, eventual-send / vat / promise-pipelining / partial-failure / when-catch lineage, structure-of-authority + multiplicative-attack-surface arguments; sleeper-channel taxonomy + provenance-gate soundness theorem; permission-vs-authority + abstraction-as-protection + arena framework; Granovetter Operator + capability-based money + subjective aggregation + rights taxonomy + smart contracts; agoric-systems vision + competence-vs-performance modularity + marketplace-of-mind; **`obeys` + `MayAccess` + `MayAffect` as hypothetical trust + risk predicates, `Focal` + `Chainmail` formal model, Hoare four-tuple logic with code-agnostic inference rules**; **access-matrix-terminology formalization of the Confused Deputy attack (Tyler Close ~2009) — CSRF + clickjacking + click-fraud as worked Web examples, web-key fix, no-infrastructure-change migration claim**; **the runtime sending-event-causes-receiving-events causal-event-DAG (Mark S. Miller, `track-turns.js`) — the operational counterpart to *only-connectivity-begets-connectivity***; **SES_light formal foundation with Datalog points-to + soundness theorem + ENCAP tool finding Yahoo! ADSafe vulnerability + verifying conservation-of-currency Mint (Taly-Erlingsson-Mitchell-Miller-Nagra 2011)**; **OCPL formal foundation with Iris concurrent separation logic + low-integrity values + RobustSafety meta-theorem + Coq-mechanized verification of dynamic sealing + caretaker + membrane patterns (Swasey-Garg-Dreyer OOPSLA 2017)**. Distinct from `capability-security` (which catalogs Endo/Agoric *practice*); this topic catalogs the *papers* arguing for and naming the discipline. | 68 |
| [compartments](compartments.md) | SES compartments: isolated guest-code subtrees in a realm. | 43 |
| [module-harmony](module-harmony.md) | The TC39 module-proposal landscape a minimal Compartments spec must intersect — source phase imports (`import source`, `ModuleSource`, `%AbstractModuleSource%`), import defer (deferred synchronous evaluation), module expressions (`module {}`), module declarations (named inline modules), and the layered Compartments proposal (first-class `Module`/`ModuleSource`, virtual sources, Evaluators, Compartment). Unified by "phases of one module load"; concept [[module-harmony-intersection-surface]] synthesizes the adopt/defer-to/stay-compatible-with intersection. Distinct from `compartments` (SES implementation, evidence not spec) and `bundles` (compartment-mapper packaging). | 38 |
| [eventual-send](eventual-send.md) | `E()` and `E.when` for messaging local or remote objects. | 110 |
| [captp](captp.md) | Capability Transport Protocol: cross-process eventual-send. | 64 |
| [ocapn](ocapn.md) | The OCapN protocol family: CapTP + marshal + transports. | 78 |
| [marshal](marshal.md) | Pass-style serialization layer; smallcaps wire format; encodePassable rank-order-preserving format; rankOrder in-memory comparator. | 86 |
| [streams](streams.md) | Async-iterator-based stream abstraction; transport substrate. | 40 |
| [pass-style](pass-style.md) | Marshal's classification system for how values cross a serialization boundary. | 71 |
| [e-language](e-language.md) | Mark Miller's capability-secure distributed language E (sameness, selfless/selfish objects, the doc-tree map) as documented on erights.org; the language-level ancestor of Hardened JavaScript / Endo. Distinct from `capability-theory` (the papers arguing the discipline). | 44 |
| [daemon](daemon.md) | The Endo daemon: per-user persistent host for HardenedJS workers. | 113 |
| [persistence](persistence.md) | How values, state, and capabilities survive vat incarnations, upgrades, daemon restarts; the heap/virtual/durable zones. | 66 |
| [async-flow](async-flow.md) | The `@agoric/async-flow` durable-replay async-function infrastructure; closed-function discipline. | 8 |
| [patterns](patterns.md) | The @endo/patterns shape-matching language; method guards. | 73 |
| [getting-started](getting-started.md) | The on-ramp into Endo: install, first encounters, confinement walk-through. | 25 |
| [tooling](tooling.md) | Endo's developer-facing tooling and assorted single-purpose packages. | 78 |
| [bundles](bundles.md) | Module bundling, Compartment module loading, bundle-source / compartment-mapper family. | 33 |
| [spec-to-implementation](spec-to-implementation.md) | Cross-cutting concordance: OCapN spec sections ↔ Endo realizations. | (meta) |
| [references](references.md) | The cross-reference axis: external prior-art and specification citations the library quotes, and internal cross-cycle observations (design-evolution-record family, multi-cycle pattern threads) the scholar attaches while ingesting. A meta tag, not a section catalog; the routing target for `keywords.md`'s `\| references` lines. | (meta) |
| [cloud-marketplace](cloud-marketplace.md) | AWS Marketplace and equivalent cloud vendor distribution channels: seller requirements, AMI and container product technical constraints, pricing models, and listing lifecycle. | 3 |
| [agent-payments](agent-payments.md) | Machine and agent-initiated payment over open rails: the x402 protocol (paying over HTTP via the 402 status code), gateway-mediated pay-per-request monetization of pages/datasets/APIs/MCP-tools, stablecoin micropayments, and the payment-as-credential property. | 5 |
| [tls-provisioning](tls-provisioning.md) | TLS certificate acquisition and first-boot provisioning patterns for self-custodial nodes: ACME challenge types, vendor-delegated subdomains, TOFU self-signed, renewal automation. | 2 |
| [signed-updates](signed-updates.md) | Cryptographically signed software update channels for always-online nodes: TUF role hierarchy, online/offline key discipline, rollback-attack defense, deployment pattern. | 1 |
| [node-packaging](node-packaging.md) | OS packaging and marketplace distribution of the Endo gateway: aggregated external constraints on Phase 11 covering marketplace requirements, TLS provisioning, and signed update channels. | 7 |
| [package-manifest](package-manifest.md) | The `package.json` manifest: the schema and semantics of every known property and how each is consumed by package managers, module systems/runtimes, and bundlers. Primary sources: the Node.js "Modules: Packages" runtime spec and npm's field-by-field property catalog. Backbone for the `package-json` project's property-by-consumer matrix, inconsistencies, and platform-specific-publishing strategies. | 58 |
| [oauth-credentials](oauth-credentials.md) | OAuth 2.0 application credential models: client-credentials scoped service clients (durable client mints short-lived scope/tag-bounded tokens) vs authorization-code user-delegated apps; scope-to-capability mapping, token lifecycle, tag-owned capabilities. Tailscale as exemplar. | 6 |
| [data-structures](data-structures.md) | General-purpose data structures and the interfaces over them: kriskowal/collections (idiomatic uniform JS collection interfaces) and cask's columnar parallel-array pattern (deepened from cask's parallel-arrays design doc, with the on-disk allocator and Robin-Hood hash table from caskdbstore, and the array/columnar machinery cluster: caskarray + operational transform, the Rabin-chunked sortedarray with SDIF/SOPS sync, allocator/hashtreetouint64/indexheap/sessiontable, and the adaptive-width BigIntArray, and the membership member table reusing the session-table parallel-array shape). Cross-cutting, independent of the capability-security corpus. | 83 |
| [reactive-bindings](reactive-bindings.md) | Synchronous, incremental binding of object properties and collection contents (kriskowal/frb): one- and two-way bindings, incremental query chains, the small binding query language, and the grammar/compiler source behind it. | 22 |
| [html-modules](html-modules.md) | HTML/XML files as reactive component-declaration modules (Kris Kowal's Guten Tag framework): a tag is a component invocation, each instance has its own lexical scope (root/parent/this/components/caller/argument), the structural tags (repeat/reveal/choose) create child scopes and react incrementally to `value`, and components govern container-free body nodes over a virtual document (Koerper). Distinct from `reactive-bindings` (frb, a binding query language) and `web-frontend` (CSS technique). | 15 |
| [change-propagation](change-propagation.md) | How a change in one place is incrementally and reliably reflected elsewhere, between data structures and between agents: the cross-cutting *theory* (gtor's reactivity taxonomy) that FRB observers, @endo/pubsub topics (#513), and propagators instantiate. The changes-vs-latest duality and ordered-collection-windows-as-derived-topics threads. Distinct from `reactive-bindings` (FRB specifically) and `streams` (the @endo/stream transport). | 60 |
| [distributed-objects](distributed-objects.md) | Design patterns for objects that are *themselves distributed entities* — a single logical world-object with per-machine representations kept coherent — as distinct from ordinary "distributed objects" (whole objects at remote addresses). The keystone is Chip Morningstar's **unum pattern** (Habitat → Elko → Electric Communities): unum/presence, division of labor (not master/replica), the four messaging patterns, behavioral/anti-REST protocols, per-unum authority, the containership problem — the MMO/actor lineage behind the E-vat model and Endo's ocap presences. Distinct from `change-propagation` (delta propagation) and `capability-theory` (ocap security theory). | 13 |
| [content-addressed-storage](content-addressed-storage.md) | Storing data as content-addressed Merkle trees of fixed-size 1KB blocks (kriskowal/cask): block format, blobs/dirs/arrays/maps as block trees, content-agnostic GC (the pinned/deadline retention regimes, snapshot+quarantine concurrent GC, store-wrapper mark/sweep, and caskdbstore's WAL-quarantine GC), cells, reducer-style persistent structures, schema hashes, the caskdir directory format (v1 compact, v2 Rabin-chunked/table, the benchmark-driven adaptive hybrid), and the array/columnar machinery (caskarray + operational transform, the Rabin-chunked sortedarray with SDIF/SOPS sync, the swap-to-end allocator + hashtreetouint64 + indexheap + sessiontable, the adaptive-width BigIntArray, the membership member table under the caskhead root, the blob CAT content-defined chunked tree, and the integrating caskhead1+ system root). | 142 |
| [networking](networking.md) | Transport-layer design (kriskowal/cask): UDP block transfer reacting against TCP's limits, the layered casknet protocol stack, Noise-IK/PSK encryption (with the cryptography.md design proposal behind it), 256-bit priority load shedding, casktel telemetry; the current plaintext casksock local protocol and the never-implemented v2 UDP proposal; the SDIF/SOPS sorted-array diff-and-sync verbs; the membership-gating member table, the three-gate (membership/session/capability) access model, the deferred cluster-provisioning design, and the caskhead1+ system root's sessions/membership/Raft-consensus components; the proposed nursery staging area and its cask/verb packet commands; the net/crypto.go implementation-side command vocabulary, wire layouts, counter-nonce replay protection, and traffic-class clamping; the net/peer.go Peer/Server machinery (the span-tracked request lifecycle, the RFC 6298 / Karn's-algorithm retransmission timer, the traffic-class send-queue drain prioritization, the single-flight session-renewal handshake, the Server receive loop and encrypted-dispatch fork with mobility address update, the responder-side handshake consistency check and member-table authorization, and the average-holdback encrypted-acknowledge batching); and the net/noise.go Noise IK handshake implementation (the symmetric-state token-ladder machine, the HKDF/HMAC-BLAKE2b/ChaCha20-Poly1305 primitives, and the ed25519↔x25519 key conversion). | 92 |
| [web-frontend](web-frontend.md) | Web frontend CSS/HTML techniques for the garden's web surfaces and web-designer/web-builder work: customizable form-control (`<select>` picker) styling, CSS intrinsic & content-based sizing (`fit-content`/`stretch`/`min-content`/`max-content` and `calc-size()` arithmetic over them), `@supports` progressive enhancement, and CSS anchor positioning (`anchor-size()`/`position-area`/`position-try-fallbacks`/`flip-*`). Distinct from `chat-ui` (a product); this collects cross-cutting CSS technique. | 21 |
| [multi-tenant-platform](multi-tenant-platform.md) | Serverless multi-tenant platform hosting: running many end-customers' untrusted or AI-generated code on shared infrastructure with per-tenant isolation, a programmable dispatch/router layer, per-tenant resource limits, egress control, and fleet tagging. The exemplar is Cloudflare Workers for Platforms (dispatch namespaces, dynamic dispatch Workers, user Workers, outbound Workers, custom limits, tags, untrusted/trusted isolation). Vendor-agnostic in name. | 28 |
| [local-model-serving](local-model-serving.md) | Local LLM inference-runtime management on a single machine: discovering MLX and llama.cpp (GGUF) models, assigning each a stable port, supervising detached runtime child processes, exposing OpenAI-compatible HTTP endpoints, and publishing the resulting providers into a downstream agent's catalog (pi-agent). Canonical example: **athanor** (Myles Borins), a TUI/CLI for Apple Silicon. The *serving/provisioning* layer beneath an agent harness; distinct from `llm-agent-frameworks` (the consumer layer) and from Endo's `endopi` design (the least-authority re-implementation of pi-agent's shape). Relevant to the garden as a possible self-hosted-model backend for the fleet. | 8 |
| [virtual-dom](virtual-dom.md) | The light-weight virtual-DOM substrate beneath Kris Kowal's Guten Tag framework: Wizdom (a minimal mutable DOM subset — element/text/comment nodes, parse5 parser, inner/outer serializers) and Koerper (adds a container-free "body" node type proxying to the actual document, so components can encapsulate a document region without a wrapper element). The substrate behind html-modules' container-free `repeat`/`reveal` tags. Distinct from `html-modules` (the consuming framework) and `web-frontend` (CSS technique). | 4 |
| [module-loader](module-loader.md) | System (`gutentags/system`), the CommonJS/npm module + resource loader beneath Guten Tag: client+server, refresh-to-reload and a `sysjs` bundle build, cross-package module+resource resolution, and per-package `analyze`/`translate` extensions (how `gutentag/extension` turns HTML modules into JavaScript). Descends from C.js → Montage Require → System. Distinct from `node-packaging` (npm layout) and `bundles` (Endo's Compartment loading). | 6 |
| [animation-coordination](animation-coordination.md) | Blick (`gutentags/blick`), Guten Tag's component animation controller: a five-phase draw cycle (measure/transition/animate/draw/redraw) over the animation-frame handler that batches document reads apart from writes to avoid forced reflow, with a reusable per-component controller and a design rationale covering perceptual throttling and the CSS-transition two-frame problem. Distinct from `html-modules` (the consuming framework) and `change-propagation` (data-change theory). | 3 |
| [focus-management](focus-management.md) | Kamera (`gutentags/kamera`), Guten Tag's single-focus enforcer: a singleton injected at the root scope (`scope.attention`) so at most one component holds focus at a time — a widget calls `takeFocus(this)` from its `focus` method and the kamera blurs the prior focal component. The focus-coordination peer of Blick (animation) and System (loading) in the injected-singleton pattern. Distinct from `html-modules` (the consuming framework). | 2 |
| [spatial-geometry](spatial-geometry.md) | ndim (`gutentags/ndim`), the small multi-dimensional geometry library beneath the Guten Tag ecosystem: point/region/box types across dimensions with a pure/mutable operator split (`op` clones, `opThis` mutates in place and returns `this`) — elementwise vectors, region containment, box↔region conversion, and quadkey tile naming. The vectors Blick reaches for to avoid GC churn. Distinct from `html-modules` and `animation-coordination` (its consumers). | 1 |
| [datalog-query](datalog-query.md) | Datalog-style querying over an unschematized `{the, of, is, cause}` fact-triple store, plus the attribute/concept semantic layer that reads typed shapes back out (schema-on-read). Seeded 2026-07-06 from `dialog-db/dialog-db` (Irakli Gozalishvili's embeddable local-first database): the associative claim model, attribute/concept derive macros, assert/retract/query, deductive rules as disjunction, the JSON `match`/`when`/`case` Datalog surface, and the EAV/AEV/VAE covering indexes. Distinct from `capability-security` and `local-first-sync` (Dialog's other faces). | 113 |
| [local-first-sync](local-first-sync.md) | Local-first data ownership and efficient replica synchronization over an immutable, content-addressed, append-only store. Seeded 2026-07-06 from `dialog-db/dialog-db`: the design goals, the causal (B-theory) temporal model, the Merkle-CRDT query-time merge, the deterministic Probabilistic-B-Tree + segment layer, the blob-store + DID:key compare-and-swap mutable-pointer decoupling, and the user-chosen privacy-efficiency tradeoff. Lineage: Ink & Switch local-first, Merkle-CRDTs, Prolly Trees, RhizomeDB. Distinct from `datalog-query` and `content-addressed-storage`. | 54 |
| [ucan-authorization](ucan-authorization.md) | UCAN (User Controlled Authorization Network) capability-based authorization: offline-verifiable delegation tokens over a `subject x command x policy` structure keyed to did:key principals, delegable with monotone attenuation. Seeded 2026-07-06 from `dialog-db/dialog-db`, which uses UCANs for both effect authorization (the capability-system sketch's Archive/Memory/Acquire set) and data privacy (the L0-L3 tiered-encryption RFC). Distinct from `capability-security` (ocap practice) and `oauth-credentials` (the OAuth token model). | 55 |
| [query-languages](query-languages.md) | The design of languages and interfaces for querying data — SQL and its critics, datalog, GraphQL, dataframe/expression-embedded approaches — and their tradeoffs (expressiveness, compressibility, portability, porousness, application-layer pressure). Seeded by Jamie Brandon's outsider-opinion *Against SQL* essay; the language/interface counterpoint to the `persistence` topic's SQLite storage material. | 7 |
| [endpoint-security](endpoint-security.md) | Apple's EndpointSecurity (ES) framework: the macOS system-extension API for observing and authorizing security-relevant events via an ES client that subscribes a handler to auth (allow/deny) and notify (observe) events. Seeded 2026-07-07 from the `es_new_descendants_client` reference — the constructor for a client scoped to the calling process and its recursively-spawned descendant tree, requiring the client entitlement but no root and no TCC. | 7 |
| [process-monitoring](process-monitoring.md) | OS-level observation and bounding of a running process and its recursively-spawned descendant tree — the observe/authorize half of sandboxing a spawned program, complementary to (and contrasted with) in-process object-capability confinement. Seeded 2026-07-07 from Apple's `es_new_descendants_client`; the cross-cutting home for descendant-scoped supervision mechanisms. | 4 |
| [coding-agent-economics](coding-agent-economics.md) | The cost and economics of running LLM coding agents: spend as the multiplicand of token cost and token count, the two levers (cheaper tokens via model routing, fewer tokens via context pruning / agent-ready codebases / shift-left verification), and why cloud coding is where spend runs away. Seeded 2026-07-08 from Allen Pike's practitioner essay *How To (Not) Spend $10k/wk on Coding Agents*; cross-references the garden's own fleet-spend meter (`scripts/jobs/usage-meter.sh`). Distinct from `agent-payments` (payment rails) and `local-model-serving` (the serving layer). | 10 |
| [context-engineering](context-engineering.md) | The discipline of curating and maintaining the optimal set of tokens available to an LLM during inference — the iterative progression of prompt engineering. Organizing principle: context is a finite resource (context rot, the attention budget rooted in transformer n² attention), so find the smallest set of high-signal tokens that maximize the desired outcome. Collects the anatomy of effective context (system prompts / tools / examples), just-in-time context retrieval (hold lightweight identifiers, load at runtime; the hybrid CLAUDE.md + glob/grep model), and the long-horizon techniques (compaction, structured note-taking, sub-agent architectures). Seeded 2026-07-25 from Anthropic's *Effective context engineering for AI agents*. Distinct from `coding-agent-economics` (the cost of tokens; pruning as a spend lever) and `agent-fleet-orchestration` (coordinating many agents against a queue). | 10 |
| [frontier-model-apis](frontier-model-apis.md) | Hosted frontier LLM model APIs evaluated as agent/worker backends: their agent-relevant capabilities (tool/function calling, context window, multimodality, structured output, reasoning-token budgets), access (endpoint, auth, OpenAI-compatibility), cost/rate shape, and safety/robustness posture. The hosted-backend sibling of `local-model-serving` and distinct from `llm-agent-frameworks` (the harness layer). Seeded 2026-07-10 from Meta's **Muse Spark 1.1** (multimodal agentic reasoning model, 1M context, OpenAI-compatible preview API), researched via Simon Willison's `llm-meta-ai` plugin; carries the "can it back a garden worker?" assessment. | 5 |
| [financial-forecasting](financial-forecasting.md) | Forecasting financial / market time series — classical time-series (ARIMA, exponential smoothing, state-space), volatility models (ARCH/GARCH/GJR/EGARCH, realized-vol), econometric/factor models (Fama-French, VAR/VECM), and ML forecasters — plus the efficient-market limits of predictability. Load-bearing fact: conditional volatility is strongly forecastable while return direction at tradeable horizons largely is not, which grounds risk-forecasting-and-sizing systems like the garden's finbot. Seeded 2026-07-16 from the finbot literature survey; the how-to-judge axis is split into `forecast-evaluation`. | 77 |
| [forecast-evaluation](forecast-evaluation.md) | How to judge whether a forecast is any good — the anti-overfitting methodology: genuine out-of-sample evaluation (train/test, walk-forward / rolling-forecast-origin / time-series cross-validation), error metrics and their traps (MAE/RMSE/MAPE/MASE/RMSSE, QLIKE for volatility), formal forecast-comparison tests (Diebold–Mariano, Model Confidence Set, White Reality Check, Hansen SPA), and the biases that make a backtest lie (look-ahead, survivorship, data-snooping, unmodeled costs). The highest-value, most method-independent axis for any trading/forecasting system. Seeded 2026-07-16 from the finbot literature survey. | 87 |
| [decision-graph-authoring](decision-graph-authoring.md) | Authoring, compiling, and walking **decision graphs** — encoding a branching, stateful flow as a graph an engine traces. Seeded 2026-07-21 from **kriskowal/kni**, a whitespace-significant language that reads as interactive-fiction tooling but is a decision-graph language: text interleaved with options/conditions/mutations/labels/loops/procedures, compiled by a four-stage parser into a flat JSON instruction graph and walked by a small pluggable engine (program counter + call stack). Collects kni's grammar, compiled state machine, runtime/engine, handler hooks, and input model. Distinct from `automatic-agentic-loop` (kni evaluated as the deterministic half of an agent-context loop) and `llm-agent-frameworks` (LangGraph's sibling node/edge/checkpointer runtime). | 45 |
| [ai-writing-and-authorship](ai-writing-and-authorship.md) | Observable LLM-prose patterning, the limits of detector and stylometric inference, and revision guidance that improves specificity without evasion or cargo-culted degradation. | 4 |
| [automatic-agentic-loop](automatic-agentic-loop.md) | The line of thinking pairing a **deterministic, automatic half** (routes, elicits, records structured state) with an **open-ended, agentic half** (the reasoning the deterministic side cannot encode) into a mutually-reinforcing loop. Seeded 2026-07-21 from @kriskowal's evaluation of **kni** decision graphs as a candidate substrate for the deterministic half — a graph that walks an agent through a bounded context-gathering interview whose transcript becomes durable agent context. Collects the evaluation (the concept pages testing the thesis) and the kni sections supplying the mechanism (input capture, the handler/`get`/`set`/`ask`/`answer` seam, waypoint/resume, transcript/verify). Distinct from `decision-graph-authoring` (the language, framing-neutral) and `agent-fleet-orchestration` (orchestrating many agents against a work queue). | 11 |

| [open-ended-agent-ecologies](open-ended-agent-ecologies.md) | Persistent multi-agent environments that seek cumulative social and cultural development through the interaction of ecological constraints, limited agent cognition, shared editable artifacts, and post-hoc interpretation. Distinct from `llm-agent-frameworks` (agent construction/runtime) and `agent-fleet-orchestration` (operational task coordination). | 6 |

| [fireworks-ai-integration](fireworks-ai-integration.md) | Practical Fireworks AI provider integration: OpenAI-compatible auth/endpoints, model and deployment selection, serverless operations, streaming/metrics, capacity/error behavior, and retention boundaries. | 15 |

## Seed-but-not-yet-populated topics

The taxonomy in `conventions.md` lists additional topics expected to fill in as more of the endo corpus is ingested:

(no remaining seed-but-empty topics — streams is now populated by the stream + stream-node ingestion)

These will appear when the next ingestion batches (the flagship package READMEs queued in the inbox) create sections that file under them.
