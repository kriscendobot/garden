---
source: endo--changeset-base64-harden-md
cycle: 380
lane: chat
ingested: 2026-06-18
package: changeset
cluster: pivot
shape: cross-package-changeset-entry
shape_subtype: base64-hardening-with-shim-exemption
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 28
---

The 13-line `.changeset/base64-harden.md` is a concrete instance of the modern changeset system that cycle 379's meta-observation contrasted against the legacy per-package `changelogs/$ISSUENUMBER.txt` discipline. Reading it answers the question cycle 379 raised: what does a changeset entry actually look like, and what does its structure reveal about the project's release discipline? The single most structurally interesting move is the YAML frontmatter on lines 1-4. Two packages are declared simultaneously: `'@endo/base64': minor` and `'@endo/bundle-source': patch`. A single changeset can span MULTIPLE packages in the monorepo, each with its own semver-level bump declared in one place. §the-named-changeset-as-cross-package-version-bump-declaration names the move; §the-named-single-changeset-multiple-packages-different-bumps names the tier-3 meta-pattern. The author declares the impact shape (which packages, what level); the changesets tool later aggregates these declarations across PRs to determine the next release versions. The two-audience structure is itself worth marking. §the-named-frontmatter-as-version-bump-declaration marks the structured metadata in YAML; the body is human-readable prose. §the-named-yaml-frontmatter-as-tool-input-prose-as-human-output names the tier-3 meta-pattern. Tools read the frontmatter; humans read the body; the two halves serve different audiences in one file. The breaking change is named with precision. §the-named-named-exports-now-frozen-as-breaking-shape marks lines 6-7: "`@endo/base64`'s named exports (`encodeBase64`, `decodeBase64`, `atob`, `btoa`) are now frozen." Four named exports listed explicitly; the change is naming-level. §the-named-exhaustive-enumeration-in-breaking-change-note names the tier-3 meta-pattern; the reader can scan for their own usage rather than parsing prose. §the-named-error-mode-named-in-changeset marks lines 8-9: "Consumers that previously assigned to or extended these exports will see a `TypeError` under SES; read-only consumers are unaffected." The break manifests as a named runtime error and is bounded to consumers who were mutating the exports. §the-named-break-bounded-to-mutating-consumers names the tier-3 meta-pattern; the author tells the reader exactly which consumers are affected and how. The contrast with read-only consumers makes the impact precise. §the-named-read-only-consumers-unaffected names the explicit safety statement; §the-named-precise-impact-bounded-by-consumer-shape names the tier-3 meta-pattern. The discipline of telling the reader which side they're on (mutating versus read-only) is part of what makes the change communication useful. The shim exemption is the cycle's deepest structural move. Lines 11-13 read: "The shim entry point `@endo/base64/shim.js` (which `@endo/init/pre.js` uses to install `globalThis.atob` / `globalThis.btoa` before `lockdown()`) is unchanged and continues to be safe to load pre-lockdown." The shim layer is explicitly exempt from the break. §the-named-pre-lockdown-shim-entry-point-unchanged names the move; §the-named-shim-subpath-exempt-from-breaking-change names the tier-3 meta-pattern. The parenthetical names the @endo/init/pre.js pattern: install globalThis.atob and globalThis.btoa before lockdown freezes the realm. After lockdown the globals cannot be added. §the-named-globalThis-installation-before-lockdown names the move; §the-named-pre-lockdown-global-installation-as-bootstrap-discipline names the tier-3 meta-pattern. The order-of-operations discipline makes pre-lockdown a structural concept, not just a point in time. The compactness is the discipline. §the-named-thirteen-line-changeset-with-rich-structure marks the file's terseness — 3-line frontmatter, 1-line blank, 4-line prose paragraph describing breaks, 1-line blank, 3-line prose paragraph describing exemption — fits the entire change communication in 13 lines. Sibling shape to cycle 369's @endo/daemon (14-line README for substantial system) and cycle 363's @endo/benchmark (8-line README). §the-named-changeset-as-minimal-but-rich names the tier-3 meta-pattern; the system rewards terse authors but still allows enumeration where precision matters. The cascading-bump structure deserves its own naming. §the-named-bundle-source-patched-because-it-depends marks the bundle-source `patch` bump alongside the base64 `minor` bump. bundle-source's behavior doesn't change but it pulls in a new minor version of base64; the patch bump signals "no API change, just dependency refresh." §the-named-cascading-bump-via-dependency-update names the tier-3 meta-pattern. The changesets tool encodes the dependency graph implicitly via the bump declarations; the author writes the declaration once, the tool propagates it through the release. Seven citation arcs close on this cycle. Cycle 379 was the prior designs-lane changelog-system meta-doc; cycle 380 gives the meta observation a concrete example (arc 1). Cycle 359's @endo/eslint-plugin discipline composes with breaking-change-via-freeze; the lint config probably catches mutation-of-exports before this TypeError fires (arc 1). Cycle 339's @endo/lockdown is named as the moment after which globals cannot be added (arc 58). Cycle 344's @endo/init/pre.js bootstrap discipline is named explicitly in the parenthetical (arc 18). Cycle 364's @endo/base64 was the cluster's first data-format-package; cycle 380 shows the package being further hardened years later (arc 2). Cycle 326's pure-naming-as-discipline reaches one more sibling; named-exports-now-frozen is pure naming applied to the export surface (arc 54). Cycle 322's @endo/errors TypeError is the specific error type named (arc 54). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-THIRTY-NINE (332 + 7 net new). §seventy-cycles-with-named-pivot-domain-stay (310-380). §twenty-eighth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across twenty-eight consecutive cycles (353-380).
