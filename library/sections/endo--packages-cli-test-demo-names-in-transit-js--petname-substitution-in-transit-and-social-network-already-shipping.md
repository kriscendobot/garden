---
source: endo--packages-cli-test-demo-names-in-transit-js
cycle: 374
lane: chat
ingested: 2026-06-18
package: cli
cluster: pivot
shape: capability-passing-demo
shape_subtype: petname-substitution-with-adopt-dismiss-flow
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 22
---

The 27-line `test/demo/names-in-transit.js` is the chat-lane counterpart to cycle 373's cli README, and it operationally demonstrates the social-network capability-passing model in 27 lines of test/demo code. The single most structurally interesting move is the substitution that happens between line 7 and line 10. Line 7 sends a message: `endo send alice "Please enjoy this @counter:doubler."` The embedded capability reference is `@counter:doubler`, naming the doubler exposed under the host's local name "counter." Line 10's regex for alice-agent's inbox view expects: `"HOST" sent "Please enjoy this @counter."` The reference has been rewritten. The colon-suffixed sender-intent ("doubler") is gone; alice sees only the receiver-local name ("counter"). §the-named-petname-substitution-in-transit names the move. §the-named-petname-as-receiver-local-name-not-sender-global-name names the tier-3 meta-pattern. This is the Miller-Stiegler petname model from capability-security literature (public knowledge) made operational in user-facing CLI behavior: each principal names the same underlying object differently, no global directory of names exists, and the wire layer substitutes between local namespaces as messages cross principal boundaries. The Granovetter operator appears in concrete form. §the-named-Granovetter-operator-as-message-payload names it. The HOST holds a capability called "doubler" (in its own namespace, exposed as "counter" in the message); the HOST sends alice a message containing that capability reference embedded in human-readable text; alice now holds the capability after the next step. Mark Miller's introduction model — Alice can pass Bob to Carol via a reference in a message — realized in a concrete CLI flow. §the-named-introduction-by-reference-in-message names the tier-3 meta-pattern. Adoption is an explicit naming action. Line 12 reads `endo adopt --as alice-agent 1 counter --name redoubler`. Alice ADOPTS the capability from inbox message 1, naming it "redoubler" in her local namespace. The capability she now holds has THREE names: HOST's "doubler" (the host's local name), the wire's "counter" (the host's exposed-in-message name), and alice's "redoubler" (her chosen local name). §the-named-adopt-as-explicit-naming-action names the move; §the-named-three-names-per-capability-in-distributed-naming names the tier-3 meta-pattern. Each principal names the same underlying object differently for their own purposes; no global directory exists. Line 16 names another deep structural choice. `endo dismiss --as alice-agent 1` dismisses inbox message 1, but the capability alice adopted persists (line 14 confirms `redoubler` is still listed after dismissal). The message is the envelope; the capability is the cargo; they have separate lifecycles. §the-named-message-and-capability-separate-lifecycles names the move; §the-named-envelope-vs-cargo-distinction names the tier-3 meta-pattern. A user can clean up their inbox without losing the capabilities they accepted from those messages. The `--as` flag selects the principal. §the-named-named-recipient-via-as-flag names the CLI surface. The same physical CLI session acts on behalf of HOST (default, when unqualified) and alice-agent (via `--as alice-agent`). §the-named-multi-principal-from-single-CLI names the tier-3 meta-pattern. The CLI is a thin shell over the underlying multi-vat daemon; identity-switching is per-command, not per-session. Line 10's regex confirms `"HOST" sent` — HOST is the default identity, alice-agent is a separate principal. §the-named-host-as-default-identity names the convention; §the-named-default-principal-as-named-HOST names the tier-3 meta-pattern. The session-level observation that follows from this demo is the most important one for the competitive analysis. §the-named-twenty-seven-line-demo-of-social-network names the structural fact: the demo shows the *entire* peer-to-peer capability-passing flow (HOST → message → alice-agent → adopt → list → dismiss) in 27 lines of code. §the-named-social-network-already-shipping is the session-level observation: the social-network model is not speculative; it ships in test/demo as a tested user-facing workflow today. The 2026-06-18 competitive analysis identified peer-symmetric capability holding between humans and agents as endo-but-for-bots' strongest differentiator across both the substrate competitor set (Arcade, NanoClaw, Claw Patrol, MintMCP, Peta) and the orchestration competitor set (LangGraph, AutoGen, CrewAI, Microsoft Agent Framework). No competitor in either set delivers this end. Cycle 374's demo directly substantiates that claim with a tested shipping flow: HOST and alice-agent are peer principals; the message is the introduction; the capability persists under each principal's chosen local name. The example capabilities deserve their own naming. §the-named-counter-and-doubler-as-named-capabilities marks that the demo uses generic capabilities — counter, doubler — rather than Slack-or-Gmail integrations. The capability surface is whatever the host has exposed by name; arbitrary user-facing capabilities can be passed by the same mechanism. Seven citation arcs close on this cycle. Cycle 373 was the prior README; the demo operationally realizes the README's user-interface-as-thin-controller framing (arc 1). Cycle 369's daemon README named bootstrap-provides-user-agent-API and facets-for-other-agents; cycle 374 demo makes both concrete — the agent IS alice-agent, the facet IS the doubler reference passed in the message (arc 1). Cycle 368's exo taxonomy named bad-message-not-bad-input vocabulary; cycle 374 demo lives in that vocabulary — these are MESSAGES between principals, not API calls (arc 1). Cycle 367's exo README named mint-purse-payment-as-canonical-OCAP-example; counter and doubler are degenerate single-facet exos for demo purposes, same pattern at smaller scale (arc 1). Cycle 321's @endo/eventual-send promise-pipelining is what enables async-send between principals; the competition.txt social-network angle finds its substrate citation arc here (arc 9). Cycle 326's pure-naming-as-discipline reaches one more sibling; the entire demo IS naming-as-discipline (arc 47). Cycle 322's @endo/errors is not used; the social flow is too small to need error decoration in this demo (arc 48). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-NINETY (283 + 7 net new). §sixty-four-cycles-with-named-pivot-domain-stay (310-374). §twenty-second-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across twenty-two consecutive cycles (353-374). This cycle's ingest is also flagged as evidence supporting the competitive-analysis-private.md working note's social-network hypothesis: the 2026-06-18 analysis identified this differentiator from competitor research; cycle 374 finds the shipping demo that demonstrates it; the two are now linked in the library's record.
