---
source: endo-but-for-bots--packages-lal-test-simulator-mock-powers-js
cycle: 404
lane: chat
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: mock-implementation
shape_subtype: guest-power-surface-vs-tool-surface
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 52
---

The 296-line mock-powers.js is the mock-internals implementation cycle 403's simulator README pointed at by name. Sixth lal-package artifact in the cluster (after the lal README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, and the simulator README). Cycle 404's most structurally interesting move surfaces a previously-unnamed distinction between two surfaces. §the-named-guest-power-surface-larger-than-LLM-tool-surface names the observation. The mock implements TWENTY-TWO methods: help, has, list, lookup, remove, move, copy, makeDirectory, listMessages, resolve, reject, adopt, dismiss, request, send, reply, storeValue, identify, locate, followMessages, evaluate, and form. Cycle 401's LAL-ARCHITECTURE.md table named 18 tools (and cycle 402 noted the design doc's own 16-vs-18 inconsistency). The mock provides FIVE methods that are NOT in the LLM tool catalog: reply, storeValue, locate, followMessages, and form. These are GUEST POWERS the agent consumes programmatically — followMessages drives the main loop; locate, identify, and storeValue support introspection; reply is the message-back-to-sender variant; form is the form-submission flow that cycle 402 named via WorkerConfig. The agent uses these methods internally without exposing them to the LLM. §the-named-LLM-exposed-tool-surface-as-subset-of-guest-power-surface names the tier-3 meta-pattern. The LLM-facing tool catalog is a FILTERED VIEW of the underlying guest-power capability surface. The mock also has one omission. §the-named-mock-incomplete-relative-to-tool-catalog marks the missing method: inspectCapability is in the LAL-ARCHITECTURE catalog but not in the mock. The canonical reply+dismiss test path doesn't exercise it, so the omission is either deliberate or a small bug. §the-named-mock-omits-one-tool-from-design-catalog names the tier-3 meta-pattern. The mock has reduced semantics in another direction. §the-named-mock-with-finite-iterator-vs-real-infinite-iterator marks lines 87-91. The mock's followMessages yields each message in the messages array exactly ONCE and then returns — the generator completes. The REAL Endo daemon's followMessages is INFINITE — it blocks indefinitely waiting for new messages. The mock has SEMANTICALLY REDUCED behavior. The test path ends after the agent processes the single canned message because the iterator completes. §the-named-finite-mock-infinite-real names the tier-3 meta-pattern. This refines cycle 403's mock-internals-real-externals framing: the mock internals are not just MOCKED but SEMANTICALLY REDUCED. The cluster's topology now distinguishes mock surface from mock semantics. The observation hooks are asymmetric. §the-named-asymmetric-observation-hooks marks lines 42-57: whenDismissed is per-message-number (Map-keyed promise-kits, each dismissed message resolves its own promise); whenSend is a single global one-shot promise. The semantics differ — multiple sends can occur but only one wait resolves; multiple dismissed messages each have their own promise. §the-named-per-message-vs-global-observation-hook names the tier-3 meta-pattern. The canonical test prompt is baked in. §the-named-test-prompt-hardcoded-in-mock marks line 79: "Hello from the simulator. Please reply with a short greeting and then dismiss this message (dismiss message 1)." The simulator's canonical test prompt is HARDCODED inside the mock — not parameterized in, not configured from env. §the-named-canonical-test-prompt-hardcoded names the tier-3 meta-pattern. The mock uses discriminator fields. §the-named-mock-marker-field marks the pattern used in makeDirectory (line 161: `{ __mockDirectory: true, path: key }`), request (lines 209-213: `{ __mockRequest: true, recipientName, description }`), and evaluate (lines 279-282: `{ __mockEval: true, message: 'Mock would run: ...' }`). §the-named-mock-discriminator-as-test-marker names the tier-3 meta-pattern; the mock returns objects with `__mockX: true` discriminator fields so the test can identify them. The directory is a flat Map with synthetic compound keys. §the-named-flat-map-as-directory marks lines 22-23 and 102-157. §the-named-path-joined-string-as-flat-key names the tier-3 meta-pattern; conceptually hierarchical but stored as a flat map with synthetic compound keys joined by slash. The most powerful tool is stubbed completely. §the-named-evaluate-stubbed-completely marks lines 278-283: the mock returns a fake result with no actual code execution. The simulator's purpose (provider debugging) doesn't need real evaluate. §the-named-most-powerful-tool-stubbed-in-mock names the tier-3 meta-pattern; cycle 401 named evaluate as Lal's most powerful tool — the mock cannot exercise it. The form method is a no-op stub. §the-named-form-method-stubbed-with-no-op marks lines 285-287. This is the FORM-SUBMISSION flow cycle 402 found in the WorkerConfig type and in lal/CLAUDE.md but NOT in LAL-ARCHITECTURE.md. The mock STUBS the surface so the agent can call form() without crashing, but no form behavior is exercised. §the-named-stub-anticipates-unused-surface names the tier-3 meta-pattern; sibling INVERSION of cycle 402's vestigial-types-from-abandoned-design — here the mock anticipates a future-or-unused surface where the types anticipated a past-or-abandoned surface. Both are forms of latency between layers: the mock leads, the types lag, the design doc sits between. The Endo idiom recurs. §the-named-makePromiseKit-from-endo-promise-kit marks line 9. Standard Endo idiom for promise/resolver bundling. The mock uses two promise-kit instances: dismissWaiters (per-message Map of kits) and nextSendPromise (single global). §the-named-promise-kit-as-Endo-idiom names the tier-3 meta-pattern. The endo URL scheme appears. §the-named-endo-localhost-URL-shape marks line 269: `endo://localhost/?id=${ID}&type=handle`. The endo:// URL scheme is a deep Endo convention; the mock synthesizes URLs of this shape for the locate method. §the-named-endo-URL-scheme-as-deep-Endo-convention names the tier-3 meta-pattern. The SELF_ID is a hardcoded constant. §the-named-SELF_ID-as-hardcoded-string-constant marks line 11. The mock uses string IDs in place of opaque handles. §the-named-hardcoded-string-ID-in-mock names the tier-3 meta-pattern. The reply method is structurally distinct from send. §the-named-reply-vs-send-as-distinct-methods marks lines 216-228 (send) vs 230-248 (reply). Reply finds the parent message's number, determines the other party (the from or to depending on direction), records the recipient, and includes a replyTo field. Send is one-way. §the-named-reply-as-send-with-direction-inference names the tier-3 meta-pattern. The adopt method flattens capability semantics. §the-named-adopt-as-marker-not-actual-capability marks lines 188-195: the mock stores a string `'adopted-from-msg-${N}'` as the value — a placeholder marker, not the real adopted capability. §the-named-mock-flattens-capability-semantics names the tier-3 meta-pattern. Dismiss is three operations in sequence. §the-named-dismiss-as-three-operations marks lines 197-206: splice the message out of the array, resolve the waiter promise, delete the waiter entry. §the-named-dismiss-triggers-wait-resolution names the tier-3 meta-pattern; the mock records the canonical end-of-conversation as the resolution of a wait promise. The cycle is a session-level continuation. §the-named-fifty-two-conformant-cycles-and-counting marks the session-level observation: fifty-second AUTHORED conformant single-body section doc in post-refactor era (cycles 353-404). §ninety-four-cycles-with-named-pivot-domain-stay (310-404). Eight citation arcs close on this cycle. Cycle 403 (1, adjacent forward; the simulator README pointed at this mock file by name; the §mock-internals-real-externals topology is now refined to §mock-internals-at-reduced-semantics + §real-externals). Cycle 402 (2, .d.ts WorkerConfig type the mock's form() stub anticipates; sibling-inversion of vestigial-types). Cycle 401 (3, LAL-ARCHITECTURE.md tool catalog the mock is incomplete relative to; mock omits inspectCapability). Cycle 400 (3, the cluster's drift framings are now refined by mock-vs-real semantic-reduction direction). Cycle 318 (3, makePromiseKit Endo idiom sibling). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 387 (3, branded-types discipline the mock simplifies away with hardcoded string IDs). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-THIRTEEN (505 + 8 net new). The library has crossed 916 sections; the post-refactor era continues at fifty-two consecutive conformant cycles; the cluster's vocabulary distinguishes mock surface (which methods) from mock semantics (finite vs infinite, marker vs real capability), a refinement that names the simulator's testing topology more precisely than cycle 403's framing alone could.
