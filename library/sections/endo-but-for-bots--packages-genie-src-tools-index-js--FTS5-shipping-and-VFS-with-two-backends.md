---
source: endo-but-for-bots--packages-genie-src-tools-index-js
cycle: 384
lane: chat
ingested: 2026-06-18
package: genie
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: barrel-export-of-tool-factories
shape_subtype: make-prefixed-factories-with-named-validators
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 32
---

The 25-line `packages/genie/src/tools/index.js` is the genie package's tool-layer barrel export. Reading it reveals the actual surface, which differs from both the cycle 381 README and the cycle 382 DESIGN.md in interesting ways. The single most structurally interesting move sits on line 22: `export { makeFTS5Backend } from './fts5-backend.js';`. The cycle 382 DESIGN.md listed "Full Text Search (sqlite fts5)" as the FIRST item on the seven-named-future-enhancements list (line 181: "1. Full Text Search - use sqlite fts5"). Cycle 384 shows the FTS5 backend has already shipped. The future feature exists in source ahead of where the DESIGN doc placed it. §the-named-FTS5-backend-already-shipping-future-feature-from-DESIGN names the move; §the-named-design-doc-trails-code names the tier-3 meta-pattern. This is sibling shape to cycle 382's §the-named-readme-aspirational-design-current, but pointing the other direction: in cycle 382, the README was aspirational and the DESIGN was current; in cycle 384, the DESIGN is stale and the CODE is current. Both directions exist among the three documents (README/DESIGN/CODE). The reader cannot rely on any single document being authoritative; the truth is in whichever of the three was most recently edited for a given feature. Several smaller framings deserve naming. §the-named-VFS-abstraction-with-memory-and-node-variants marks lines 18-19: `makeMemoryVFS` and `makeNodeVFS`. The agent has a Virtual File System abstraction with two backends — in-memory (for testing or pure-substrate execution) and Node.js-filesystem (for production). The README and DESIGN both treated filesystem access as a single capability; cycle 384 reveals the abstraction underneath. §the-named-two-backend-VFS-via-make-prefix names the tier-3 meta-pattern. §the-named-make-prefix-as-factory-naming-convention marks the six `make*` factory exports: makeCommandTool + makeFileTools + makeMemoryVFS + makeNodeVFS + makeMemoryTools + makeFTS5Backend. Sibling shape to cycle 367's §the-named-three-shapes-of-make-and-define-and-define-kit; genie's tools all follow the `make` pattern (single-instance with closure state) exclusively, never `define` (factory of multiple instances). §the-named-make-only-no-define-in-tool-layer names the tier-3 meta-pattern; the tools are stateless services rather than class-instances. §the-named-rejectPatterns-and-rejectFlags-and-enforcePath-as-named-security-primitives marks lines 12-14: three named functions exported alongside `bash`. rejectPatterns blocks regex-matching commands; rejectFlags blocks specific CLI flags; enforcePath validates paths don't escape. These are the implementation of cycle 381's README security claims about "Path traversal prevention" and "Dangerous command detection." §the-named-validator-as-exportable-primitive-not-private-helper names the tier-3 meta-pattern; the security functions are usable independently by consumers who want to compose them with their own custom commands. §the-named-bash-as-tool-plus-three-named-validators marks the architecture: `bash` is exported alongside the more general `makeCommandTool` factory plus the three rejection/enforcement primitives. The bash tool is one preset of makeCommandTool using the three validators. §the-named-named-preset-via-factory-and-named-validators names the tier-3 meta-pattern. The counting mismatch deserves explicit naming. §the-named-eight-tool-README-vs-six-make-export-mismatch marks the discrepancy: cycle 381 README counted EIGHT tools (memory_get + memory_search + readFile + writeFile + editFile + webFetch + webSearch + bash); cycle 384 source exports SIX factories plus TWO direct functions. The naming level is different. The README counts logical tools the agent can call; the source exports composable factories where one factory often produces multiple logical tools. §the-named-logical-tool-count-not-equal-export-count names the tier-3 meta-pattern. Two specific bundlings are named. §the-named-makeFileTools-bundles-three-file-tools marks the README's readFile + writeFile + editFile trio bundled under a single `makeFileTools` factory. §the-named-makeMemoryTools-bundles-memory-get-and-search marks the parallel: memory_get + memory_search bundle under `makeMemoryTools` with `makeFTS5Backend` as the pluggable storage layer. §the-named-related-tools-bundled-by-factory and §the-named-tool-factory-with-pluggable-backend name the tier-3 meta-patterns; the memory tools are parameterized by which backend (FTS5 or other unnamed alternatives) they use. The discipline conventions from the cluster recur. §the-named-ts-check-pragma-as-opt-in-checking marks line 1 `// @ts-check`. §the-named-twenty-five-line-barrel-export marks the file's size; sibling shape to cycle 376's hidden.js (20-line constants-only module). Both are single-purpose module-surface files in substantial packages. Seven citation arcs close on this cycle. Cycle 383 was the prior designs-lane SOUL.md; cycle 384's tools index is the catalog of capabilities the SOUL-disciplined agent uses (arc 1). Cycle 382's DESIGN.md future-enhancements list is contradicted by the source; FTS5 has shipped; the new design-doc-trails-code framing extends the readme-aspirational-design-current shape with a third direction (arc 2). Cycle 381's README eight-tool catalog versus source six-factory exports reveals the counting-level discrepancy (arc 2). Cycle 367's @endo/exo make/define/define-kit naming convention; genie uses make-only at the tool layer (arc 5). Cycle 374's social-network capability-passing demo; tools are the capabilities passed in the network (arc 3). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 58). Cycle 322's @endo/errors not directly used in barrel exports (arc 58). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-SIXTY-SEVEN (360 + 7 net new). §seventy-four-cycles-with-named-pivot-domain-stay (310-384). §thirty-second-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-two consecutive cycles (353-384). §three-directions-of-document-drift-now-named as session-level observation: readme-aspirational-design-current (cycle 382), readme-undercounts-the-implementation (cycle 360), and now design-doc-trails-code (cycle 384) — three named directions of document-versus-code drift across the cluster's documentation discipline.
