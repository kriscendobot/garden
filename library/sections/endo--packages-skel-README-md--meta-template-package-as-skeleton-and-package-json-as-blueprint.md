---
source: endo--packages-skel-README-md
cycle: 365
lane: designs
ingested: 2026-06-16
package: skel
cluster: pivot
cluster_position: twenty-fifth
shape: meta-template-package-as-skeleton
shape_subtype: skeleton-with-blueprint-package-json
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 13
---

The 3-line README for @endo/skel reads literally `# [name]` then a blank line then `This `[package]` package is a skeleton package.` That is the entire document. The brackets are intentional. They are markup-placeholders that humans recognize as "fill in here" when they copy the skel directory to start a new @endo/* package. The package.json next to the README tells the complementary half of the story. Where the README is placeholder text, the package.json is a blueprint: full real values that the derivative inherits unchanged. The lint config extends `plugin:@endo/internal`. The test script runs `ava`. The `test:c8` script invokes the cycle 361 ses-ava CLI with c8 coverage. The `test:xs` script is `exit 0`, a no-op the derivative will replace. The devDependencies hard-code `@endo/lockdown` and `@endo/ses-ava`. The prepack and postpack hooks clean and build via TypeScript. The files allowlist names what gets published. The publishConfig declares public access. §the-named-template-package-where-package-json-is-blueprint-and-README-is-placeholder names the single most structurally interesting move; §the-named-readme-has-placeholders-package-json-has-blueprint names the tier-3 meta-pattern. The two files have dual nature, and the dual nature is the design: README is for humans to customize per-package, package.json is for the build and test infrastructure to consume unchanged. The shape opens a tier-3 meta-pattern peer to the seven prior cluster shapes. §the-named-meta-template-package-as-skeleton marks it as a NEW SHAPE: a package that exists to be COPIED rather than IMPORTED. §the-named-package-meant-to-be-copied-not-imported names the tier-3 meta-pattern; this is the twenty-fifth cluster member, and the eighth shape in the cluster's taxonomy. The cycle establishes a quartet. Cycle 359 introduced @endo/eslint-plugin (discipline-as-package). Cycle 361 introduced @endo/ses-ava (testing-substrate-bridge-as-package). Cycle 363 introduced @endo/benchmark (private-cross-engine-benchmark-tool). Cycle 365 introduces @endo/skel (meta-template-package-as-skeleton). §the-named-the-eslint-plugin-ses-ava-benchmark-skel-quartet names the four peer development-affordance packages; §the-named-four-development-affordance-package-shapes names the tier-3 framing. The package.json carries a structural contradiction that is the design's deepest revelation. It marks `"private": true` AND `"publishConfig": { "access": "public" }`. A package cannot be both private and publicly published; the contradiction resolves only by recognizing the package's dual nature. Skel itself is private and is never published; the publishConfig is BLUEPRINT for derivatives. Copies of skel that remove the `private: true` flag will publish to npm using the publishConfig that travels unchanged from the skeleton. §the-named-private-with-publish-config-for-derivatives names the design move; the publishConfig is for the derivative's eventual publication, not for the template's. The cycle 359 eslint-plugin and the cycle 365 skel interact at the eslintConfig level. Skel's eslintConfig extends `plugin:@endo/internal`, which is cycle 359's §the-named-internal-rules-only-for-source-repo — the tier of rules ONLY applicable to packages within the Endo source repo. Every @endo/* package starts as a copy of skel, so the internal config is the right baseline for the template. §the-named-skel-extends-internal-config names the move. The discipline package and the template package compose: the template hard-codes the internal config so every derivative starts with the discipline already wired up. §the-named-skel-uses-ses-ava-CLI names the test:c8 script's use of the cycle 361 ses-ava CLI. §the-named-skel-devDependencies-on-lockdown-and-ses-ava names the hard-coded test scaffolding inherited by every derivative. §the-named-test-xs-yields-trivially-in-skeleton names the no-op test:xs that derivatives are expected to replace. Several smaller moves deserve naming. The description field is `null` rather than empty string. §the-named-explicit-null-description-as-placeholder names the move; §the-named-typed-null-as-placeholder-not-empty-string names the tier-3 meta-pattern. The JSON shape declares "this field exists but has no value yet" via the null literal, which a JSON-aware tool can detect distinctly from a present-but-empty string. The README's bracket convention deserves its own name. §the-named-readme-with-explicit-placeholder-markup describes the brackets `[name]` and `[package]`; §the-named-placeholder-as-bracketed-name names the tier-3 meta-pattern. There is no templating language (no `{{ name }}` syntax); just human-readable markup. The convention assumes the reader recognizes brackets as "fill in here" rather than as part of the text. Finally, the template is a living one. §the-named-living-template names the structural reality that because skel is a workspace package, it gets dependency updates, lint runs, and CHANGELOG entries. The version is 1.1.13 as of ingest, meaning the template has been updated thirteen times within the 1.x major series. The derivative copied from skel today gets the present-tense template, not the past-tense template. Eight citation arcs close on this cycle. Cycle 364 was the prior chat-lane benchmark source; both private-package shape but distinct sub-shapes (benchmark is private-internal-tool; skel is private-template) — adjacent forward pair (arc 1). Cycle 363's benchmark introduced the private-package-as-internal-tooling shape; cycle 365 extends to meta-template-package-as-skeleton, establishing the quartet (arc 1). Cycle 362's ses-ava CLI is consumed by skel's test:c8 script (arc 1). Cycle 361's @endo/ses-ava is a hard-coded devDependency (arc 1). Cycle 360's eslint-plugin internal config is the skel's eslintConfig.extends value (arc 1). Cycle 359's eslint-plugin package itself ships the internal config skel uses (arc 1). Cycle 339's @endo/lockdown is a hard-coded devDependency (arc 50). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 38). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-TWENTY-SEVEN (219 + 8 net new). §fifty-five-cycles-with-named-pivot-domain-stay (310-365). §twenty-fifth-named-package-in-the-pivot-cluster. §thirteenth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirteen consecutive cycles (353-365).
