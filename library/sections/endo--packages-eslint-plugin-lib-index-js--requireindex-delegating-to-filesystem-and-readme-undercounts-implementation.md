---
source: endo--packages-eslint-plugin-lib-index-js
cycle: 360
lane: chat
ingested: 2026-06-15
package: eslint-plugin
cluster: pivot
shape: tiny-entry-point
shape_subtype: requireindex-delegating-to-filesystem
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 8
---

The 20-line `lib/index.js` for @endo/eslint-plugin contains only two lines of business logic. Both call `requireIndex`, the npm helper that enumerates every JavaScript file in a directory and returns an object keyed by filename. Line 19 reads `module.exports.rules = requireIndex(`${__dirname}/rules`)`; line 20 reads `module.exports.configs = requireIndex(`${__dirname}/configs`)`. The package's surface area is whatever happens to exist in `lib/rules/` and `lib/configs/`. §the-named-requireindex-delegating-to-filesystem is the single most structurally interesting move; §the-named-filesystem-as-source-of-truth-not-documentation is the tier-3 meta-pattern it instantiates. The implication is sharp once you compare against the cycle 359 README. The README named five configs (recommended, imports, style, strict, internal); `lib/configs/` actually contains eight (the five named plus daemon, ses, and recommended-requiring-type-checking). The README's "Supported Rules" section reads literally `* Fill in provided rules here`; `lib/rules/` actually contains six (assert-fail-as-throw, harden-exports, no-assign-to-exported-let-var-or-function, no-multi-name-local-export, no-polymorphic-call, restrict-comparison-operands). The README undercounts both surfaces, and the source ships those extras anyway because requireIndex does not consult the README. §the-named-readme-undercounts-the-implementation names the specific direction of drift, and it pairs with cycle 358's §the-named-readme-abstracts-the-implementation as a sibling shape. The cycle 357→358 zip pair was the abstracting direction: the README named the attack via magic numbers in comment text; the source named the exact byte sequences as named exports. The cycle 359→360 eslint-plugin pair is the undercounting direction: the README named a tiered policy taxonomy; the source ships more than the taxonomy describes. §the-named-two-shapes-of-design-vs-implementation-arc-from-README-to-source as the new tier-3 framing — ABSTRACTING and UNDERCOUNTING are both forms of the README not being the source of truth, but they differ in directionality. The honest-placeholder shape from cycle 359 plays a structural role here too. §the-named-honest-placeholder-in-README-as-license-to-under-document — the placeholder `* Fill in provided rules here` is an EXPLICIT acknowledgment that documentation lags implementation; the six functional rules are shipped without README entries because the placeholder states the gap. The placeholder is not merely honest about incompleteness — it pre-authorizes under-documentation. One more fossil deserves naming. The JSDoc preamble at the top of `lib/index.js` reads `@module Agoric-specific plugin` and `@author Agoric`. The package is now `@endo/eslint-plugin`, but the comment carries the upstream historical attribution from before the rename. §the-named-attribution-fossil-from-pre-rename is the tier-3 meta-pattern for such residue; cleaning it would have been a sweep of pure annotation churn, so the fossil remains, which is the same kind of honest-residue-in-place stance the cycle 357 zip README took with its "for expedience" pairings. Six citation arcs close on this cycle. The forward pair cycle 359 → cycle 360 README → source (arc 1) completes adjacently. Cycle 358's instance of the design-vs-implementation-arc-from-README-to-source pattern was the first known instance; this is the second, and two instances establish the meta-pattern (arc 1 to cycle 358). Cycle 326's §the-named-pure-naming-as-discipline reaches one more sibling (arc 34). Cycle 345's lockdown + Hardened JS naming trace finds its lint-plugin entry-point peer (arc 15). Cycle 188's Endo policy doc references resolve here (arc 173). Cycle 320's earlier eslint trace from garden design ingests reconnects (arc 46). The cycle pushes citation-arc-closures-in-pivot to ONE-HUNDRED-NINETY-ONE (185 + 6 net new). §fifty-cycles-with-named-pivot-domain-stay (310-360). §eighth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across eight consecutive cycles (353-360).
