---
source: endo-but-for-bots--packages-genie-README-md
cycle: 381
lane: designs
ingested: 2026-06-18
package: genie
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: AI-agent-framework-package
shape_subtype: prompt-builder-tools-memory-heartbeat
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 29
---

The 109-line `packages/genie/README.md` on endo-but-for-bots' `llm` branch is the cluster's first AI-agent-framework package — the bot-fork's product. It's structurally distinct from every endojs/endo package the librarian has surveyed because its purpose is not substrate (harden, lockdown, ses) and not tooling-for-substrate (eslint-plugin, ses-ava, benchmark, skel) but APPLICATION: building autonomous agents that use the substrate. The single most structurally interesting move is the package's positioning. Line 3 reads "A Claw-like AI Agent framework for the Endo hardened JavaScript project." The tagline positions the package by analogy to Claude (called "Claw" obliquely). The package's identity is given as a comparison rather than reconstructed from first principles. §the-named-Claw-like-as-positioning-reference names the move; §the-named-analogy-as-positioning-shorthand names the tier-3 meta-pattern. Sibling shape to cycle 363's @endo/benchmark "minimalistic ava-like interface" delegation; both READMEs define the package by what it RESEMBLES rather than by spec. The four-component architecture is named at the top of the Overview. §the-named-four-component-AI-agent-framework marks Modular Tool System + Memory Integration + Heartbeat Execution + System Prompt Builder. The product is a four-part architecture, each part packaged separately for composition. §the-named-AI-agent-as-four-component-architecture names the tier-3 meta-pattern. The most structurally surprising component is the System Prompt Builder. §the-named-system-prompt-builder-as-first-class-feature marks the top-level placement; building the prompt that goes to the LLM is not an implementation detail, it's an API surface. §the-named-prompt-construction-as-API-surface names the tier-3 meta-pattern. Most agent frameworks treat prompt construction as the application developer's problem; genie treats it as a packaged primitive. The systemBuilder's parameter shape is structurally revealing. §the-named-identity-soul-memory-tools-heartbeat-five-field-prompt-shape marks lines 19-25 + 84-94: identity (user-facing string), soul (internal truths string), memory (path to MEMORY.md), tools (path to tools directory), heartbeatPath (path to HEARTBEAT.md). Five fields are the named decomposition of "what an agent is." §the-named-five-named-fields-as-agent-decomposition names the tier-3 meta-pattern. The decomposition is sharp: identity is outward presentation; soul is inward belief; memory is fact recall; tools are verbs; heartbeat is the task loop. The soul field deserves its own naming. §the-named-soul-as-internal-truths marks lines 21-22 + 87 where soul is glossed as "Internal truths." Distinct from identity (how the agent presents) and memory (what it remembers). §the-named-soul-vs-identity-distinction names the tier-3 meta-pattern; the agent has both an outward identity and an inward soul. The parallel to `evoke/SOUL.md` at the repo root (29 lines, not yet ingested) suggests a project-wide pattern that future cycles can probe. The tool catalog is named explicitly. §the-named-eight-tool-catalog-with-named-actions marks lines 67-75: memory_get + memory_search + readFile + writeFile + editFile + webFetch + webSearch + bash. Each named explicitly with a one-line description. §the-named-tool-as-named-action names the tier-3 meta-pattern; the tool catalog is the agent's verbs, and the catalog itself is part of the API surface. The bash tool's framing is worth marking separately. §the-named-bash-as-named-tool-with-safe-execution marks line 75: "`bash` | Execute shell commands safely". Shell execution is in the catalog, marked "safely" — the implementation contains the safety layer. §the-named-dangerous-capability-named-with-safety-qualifier names the tier-3 meta-pattern; the README doesn't hide the dangerous capability behind euphemism, it names it and asserts the safety. The autonomy loop is named. §the-named-heartbeat-as-autonomous-task-loop marks the heartbeat execution component on line 9 + 46-48 + 97-103: HeartbeatRunner loads tasks from HEARTBEAT.md, parses them, executes, updates task status. The agent runs autonomously by reading its own task file and ticking through items. §the-named-self-read-task-file-as-autonomy-loop names the tier-3 meta-pattern. Two file conventions are named together. §the-named-MEMORY-md-as-persistent-knowledge-file and §the-named-HEARTBEAT-md-as-task-list-file mark the Markdown files (not databases, not JSON) the agent reads and writes. §the-named-markdown-as-agent-state-format names the tier-3 meta-pattern; the agent's knowledge and todo list are human-readable files in the same repo as the code. This is consistent with the broader cluster's discipline of human-readable state surfaces (cycle 366's @endo/skel test, cycle 374's @endo/cli inbox demo). The security discipline is named exhaustively. §the-named-security-as-cross-cutting-tool-requirement marks lines 56-62 and 30 + 41-43: every tool implements Input validation + Path traversal prevention + Code injection prevention + Dangerous operation detection + Content validation. §the-named-five-security-properties-per-tool names the tier-3 meta-pattern; every tool must implement all five, no exceptions. Configuration knobs are named. §the-named-policy-flags-as-discipline-knobs marks lines 90-93: disableSuffix, disablePolicy, strictPolicy, securityNotes. Four flags let consumers tune the prompt's policy section per-deployment. §the-named-policy-as-configurable-not-baked-in names the tier-3 meta-pattern; the SAFETY discipline is the default, but consumers can dial it up (strictPolicy) or down (disablePolicy/disableSuffix) or override (securityNotes) per their context. §the-named-DESIGN-md-pointer marks line 108's link to packages/genie/DESIGN.md (203 lines, ready for cycle 382 chat-lane). This is the second instance of the DESIGN.md-at-package-root convention after cycle 375's @endo/module-source DESIGN.md. Seven citation arcs close on this cycle. Cycle 380 was the prior chat-lane changeset entry; both cycles are about packaging-and-release shapes at different scales (arc 1). Cycle 374's @endo/cli names-in-transit demo on the master branch revealed the social-network capability-passing model; cycle 381 reveals the llm-branch fork has its own agent framework atop the same substrate (arc 1). Cycle 369's @endo/daemon; genie likely composes with the daemon for capability messaging though the README doesn't yet name it explicitly (arc 1). Cycle 363's @endo/benchmark README's analogy-as-positioning shape is the sibling (arc 1). Cycle 377's @endo/ses secure-coding-guide named POLA; genie's five-security-properties-per-tool is an application of POLA at the tool layer (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling; tool names and field names are pure naming (arc 55). Cycle 322's @endo/errors is not explicitly named in the README but presumably used in the tool layer (arc 55). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-FORTY-SIX (339 + 7 net new). §seventy-one-cycles-with-named-pivot-domain-stay (310-381). §twenty-ninth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across twenty-nine consecutive cycles (353-381). §first-endo-but-for-bots-source-in-session as session-level observation: previous cycles drew from endojs/endo; cycle 381 enters the bot-fork's `llm` branch for the first time, opening a fresh source domain.
