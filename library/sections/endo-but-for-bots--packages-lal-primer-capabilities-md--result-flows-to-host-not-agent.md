---
source: endo-but-for-bots--packages-lal-primer-capabilities-md
cycle: 411
lane: designs
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: agent-facing-capability-reference
shape_subtype: result-direction-asymmetry-in-define-endow
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 59
---

The 148-line capabilities.md is the agent-facing reference for working with capabilities inside @endo/lal's primer corpus and the thirteenth lal-package artifact in the cluster. Cycle 411 follows the one-hundredth-cycle milestone (cycle 410) and refines the cluster's attenuation framing in a structurally significant way. §the-named-result-flows-to-host-not-agent-in-define-endow names the observation. Lines 32-33 articulate what cycle 409 left implicit: "You receive a receipt, not the result. The host sees the result in their inbox and may share it with you via reply()." Cycle 409 named define-endow as the attenuation pattern but did not specify WHERE the result lives or HOW the agent learns about it. Cycle 411 clarifies: the agent's proposal yields no automatic output to the agent — the agent receives only a RECEIPT (acknowledgment that the proposal was processed); the RESULT lives in the host's inbox/inventory; the host MAY share back via reply(). The authority flow is asymmetric in BOTH directions: the host controls the input capability AND the host controls whether the agent sees the result. §the-named-receipt-not-result-for-define names the tier-3 meta-pattern. The cluster's attenuation framing now has a more precise shape: define-endow is a HOST-CENTRIC flow where the agent contributes only the code logic; both the input capability and the result reside in the host's authority. The asymmetry between evaluate and define now has two dimensions. §the-named-evaluate-result-stored-in-own-directory-vs-define-result-flows-to-host names the deeper structure. Evaluate's resultName stores the result IN THE AGENT's directory (line 43: "stores the result under resultName"); define's result flows to the HOST. Evaluate is AGENT-CENTRIC; define is HOST-CENTRIC. The two patterns differ on (1) WHO PROVIDES THE CAPABILITY (already known from cycle 409) and (2) WHO RECEIVES THE RESULT (newly named here). §the-named-result-direction-as-second-asymmetry-between-evaluate-and-define names the tier-3 meta-pattern. Cycle 407's framing of "define is PREFERRED" is itself refined to be conditional. §the-named-define-preferred-CONDITIONALLY marks lines 22-24: "Prefer `define()` when the user asks for code but you don't have all the required capabilities in your directory." If the agent DOES have the cap, evaluate is appropriate. §the-named-conditional-preference-on-inventory-state names the tier-3 meta-pattern; cycle 407's absolute preference is refined to a conditional one. The naming vocabulary extends. §the-named-two-name-kinds-special-and-pet marks lines 5-18. Special names (@-prefixed, read-only, indelible); pet names (lowercase alphanumeric with hyphens, 1-128 chars, mutable). §the-named-special-vs-pet-names-as-name-discipline names the tier-3 meta-pattern; sibling to cycle 386's petname-edgename framing — the cluster's naming vocabulary now has THREE kinds: special names, pet names, edge names. The canonical special names are four. §the-named-four-canonical-special-names marks lines 10-13: @self, @host, @agent, @main. §the-named-canonical-special-name-set names the tier-3 meta-pattern. The @agent meaning is ambiguous between docs. §the-named-at-agent-meaning-ambiguous marks line 12's "@agent — The own agent reference." Cycle 410's setup.js had `introducedNames: harden({ '@agent': 'host-agent' })` — introducing @agent as the HOST agent. The primer's wording "own agent reference" is ambiguous: own = self-own (own=mine) or own = owning (own = the one who granted you powers)? In setup.js's context, @agent points to the host-agent. The ambiguity may be intentional (the same special name means "your agent" from each holder's perspective — for a guest, that's its host's agent) or yet another doc-drift instance. §the-named-at-agent-disambiguation-needed names the tier-3 meta-pattern. The globals count drifts between primer documents. §the-named-globals-count-disagrees-between-primer-documents names the structural fact. Capabilities.md (lines 96-122) lists THREE globals: E, M, makeExo. Cycle 409's howto-code.md listed FOUR (E + M + makeExo + harden). Cycle 401's design doc said three (E + M + makeExo). Two primer documents — adjacent, both inside the same agent-facing corpus — disagree on the globals list. §the-named-globals-three-vs-four-across-primer-documents names the tier-3 meta-pattern; yet another count-drift instance to add to the cluster's already-rich drift vocabulary. The evaluate signature is five-argument. §the-named-evaluate-five-arguments marks line 40: `evaluate('@main', "E(counter).increment()", ["counter"], ["my-counter"], "increment-result")`. Five positional arguments: workerName + source + codeNames + edgeNames + resultName. §the-named-evaluate-positional-signature names the tier-3 meta-pattern. The codeNames and edgeNames have a positional relationship. §the-named-codeNames-vs-edgeNames-distinction marks lines 47-49: codeNames are LEXICAL (in source); edgeNames are INVENTORY (in your directory); mapping is positional: codeNames[i] ↔ edgeNames[i]. §the-named-lexical-name-to-inventory-name-positional-mapping names the tier-3 meta-pattern. The evaluate can run in a specific worker. §the-named-evaluate-runs-in-named-worker marks line 40 plus line 146 (`evaluate(undefined, ...)` for default). §the-named-workerName-as-isolation-selector names the tier-3 meta-pattern. The primer's pedagogy includes anti-patterns. §the-named-good-vs-bad-patterns-as-pedagogical-shape marks lines 63-76. §the-named-explicit-bad-example-in-pedagogy names the tier-3 meta-pattern; the primer teaches by contrasting good and bad. The expression-style trap is named. §the-named-assignment-returns-undefined-anti-pattern marks line 75: `const result = E(db).get("users")` is BAD because the last expression is an assignment, and assignments return undefined. §the-named-expression-style-trap names the tier-3 meta-pattern; expression-only evaluation contexts have a familiar pitfall — the same trap applies in shell command-substitution, in Lisp's interactive read, in Ruby's irb. The define workflow has four steps with host mediation. §the-named-define-flow-includes-wait-for-host-reply marks lines 132-138. Step 4 explicitly: "Wait for the host to share the result via a reply message, then reply() to the original sender." §the-named-async-multi-party-flow-with-host-mediated-result names the tier-3 meta-pattern. The evaluate workflow requires prior capability acquisition. §the-named-evaluate-requires-capability-in-message marks lines 143-144. The requester must already have sent the capability; the agent adopts it before evaluating. §the-named-evaluate-requires-prior-capability-acquisition names the tier-3 meta-pattern. The full define flow involves three parties. §the-named-define-result-flows-asymmetrically-three-parties marks the structural fact: requester (sends initial request) → agent (proposes via define) → host (endows capability AND receives result). The result goes to the HOST, not the requester. The agent then has to ask the host to share back, then forward to the requester. §the-named-three-party-define-flow-with-two-mediations names the tier-3 meta-pattern. The pet name format is constrained. §the-named-pet-name-format-1-to-128-characters marks lines 17-18. Same constraint cycle 401's inner CLAUDE.md mentioned for @-prefixed special names; here it's stated for pet names too. §the-named-128-char-name-budget names the tier-3 meta-pattern; both name kinds share the 128-character limit. The makeExo example shows the instance-state convention. §the-named-makeExo-with-this-state-pattern marks lines 115-122: `increment() { return ++this.state.count; }`. §the-named-makeExo-this-state-as-instance-state-pattern names the tier-3 meta-pattern; makeExo methods use `this.state` for internal mutable state, complementing the immutable interface guard. The three purposes for globals are named. §the-named-three-purposes-for-globals marks lines 124-127. §the-named-three-canonical-uses-of-eval-globals names the tier-3 meta-pattern. The cycle's session-level observation continues. §the-named-fifty-nine-conformant-cycles-and-counting marks the session-level observation: fifty-ninth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-411). §one-hundred-and-one-cycles-with-named-pivot-domain-stay (310-411). Ten citation arcs close on this cycle. Cycle 410 (1, adjacent forward; @agent meaning ambiguity surfaced via setup.js context). Cycle 409 (5, MAJOR refinement — attenuation framing now specifies result-flows-to-host, not back to agent; cycle 409 didn't address result destination). Cycle 407 (3, define-preferred is CONDITIONALLY preferred — refines the absolute framing). Cycle 401 (3, design doc said three globals — primer/capabilities matches design; primer/howto-code disagrees). Cycle 386 (3, special-vs-pet sibling to petname-edgename framing — naming vocabulary now THREE-KINDED). Cycle 402 (3, evaluate signature matches the types in agent.types.d.ts). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 364 (4, shapes count keeps growing). Cycle 346 (3, name-aliasing for special names). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-SEVENTY-NINE (569 + 10 net new). The library has crossed 923 sections; the post-refactor era continues at fifty-nine consecutive conformant cycles; the cluster has crossed the one-hundred-and-first post-pivot cycle. The cluster's attenuation framing is now MORE PRECISE than it was at cycle 410: the asymmetry between evaluate and define spans TWO dimensions, not just one — who provides the capability AND who receives the result. The cluster's drift framings continue to refine and deepen rather than just multiply; each cycle adds resolution to previously-named patterns.
