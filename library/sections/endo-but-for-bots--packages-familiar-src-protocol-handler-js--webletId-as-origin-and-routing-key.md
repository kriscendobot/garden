---
source: endo-but-for-bots--packages-familiar-src-protocol-handler-js
cycle: 440
lane: chat
ingested: 2026-06-22
repo: endo-but-for-bots
branch: llm
package: familiar
cluster: pivot
shape: protocol-handler-implementation
shape_subtype: webletId-as-dual-use-origin-and-routing-key
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 88
---

The 91-line protocol-handler.js from @endo/familiar/src is Layer 1 (CSP) of cycle 436's six-layer exfiltration defense — the final piece of the defense-distributed-across-files framing. Cycle 440's most structurally interesting move surfaces how the custom protocol scheme is simultaneously an Electron security primitive and a daemon routing mechanism. §the-named-webletId-as-origin-hostname-AND-daemon-routing-key names the observation. Lines 65-71 extract `url.hostname` as `webletId` and use it for two purposes: (1) as the origin boundary for CSP's `connect-src 'self'` — each weblet gets an isolated origin because the hostname is the weblet's identity; and (2) as the routing key forwarded to the daemon via the overridden `Host` header (`Host: webletId`). The protocol handler is the hinge point where Electron's security model (origin isolation via custom scheme) and the daemon's routing model (identify which weblet is calling) converge on the same datum. §the-named-webletId-as-dual-use-origin-and-routing-key names the tier-3 meta-pattern. The CSP itself is injected at response time, not configured statically. §the-named-layer-1-CSP-injected-at-response-time names the structural fact. Lines 79-87 show the handler adding `Content-Security-Policy` to every proxied response by overriding the headers. The daemon cannot circumvent the CSP because the handler always overwrites it. §the-named-CSP-injection-by-protocol-handler-not-daemon names the tier-3 meta-pattern. The scheme is registered with four privileges. §the-named-four-privileges-each-justified marks lines 44-52: standard (full origin), secure (treated as HTTPS), supportFetchAPI (allows fetch/XHR from weblets), corsEnabled (allows CORS for assets). Cycle 220's localhttp-protocol design named these four privileges with their purposes; cycle 440 confirms the implementation matches. §the-named-four-privileges-match-design names the tier-3 meta-pattern. The `connect-src 'self'` directive is load-bearing. §the-named-connect-src-self-as-load-bearing-network-confinement marks line 27. The directive works only because each weblet has a unique origin; `'self'` resolves to that weblet's specific localhttp:// origin. Without unique hostnames, `connect-src 'self'` would allow cross-weblet traffic. §the-named-connect-src-self-requires-unique-per-weblet-origin names the tier-3 meta-pattern. The `unsafe-eval` allowance connects to SES. §the-named-unsafe-eval-for-SES-lockdown marks the source comment at lines 19-21: `script-src 'unsafe-eval'` is required for SES lockdown's eval-based loader. Cycle 433 named the eval-based loader pattern from lockdown.js; here the consequence shows: the confinement layer (CSP) must relax the eval surface to allow SES to function. Security through eval and security against eval coexist: `'unsafe-eval'` opens the eval surface while `connect-src 'self'` closes the network surface. §the-named-unsafe-eval-opened-for-SES-while-network-closed names the tier-3 meta-pattern. The two-phase lifecycle pattern persists. §the-named-pre-ready-registration-post-ready-install marks the two exported functions: `registerLocalhttpScheme` (must be called before `app.whenReady()`) and `installLocalhttpHandler` (must be called after `app.whenReady()`). Cycle 436 named this Electron lifecycle pattern; cycle 440 confirms it is architectural rather than incidental — it appears in both Layer 1 (this file) and layers 2, 3, 5 (exfiltration-defense.js). §the-named-pre-ready-post-ready-persists-in-layer-1 names the tier-3 meta-pattern. The protocol handler is a localhost reverse proxy. §the-named-proxy-as-protocol-handler-core marks lines 63-77. The handler rewrites `localhttp://webletId/path?q` to `http://127.0.0.1:gatewayPort/path?q`, forwards method/headers/body with `Host: webletId`, and returns the proxied response with CSP injected. The daemon sees standard HTTP with a Host header; the localhttp:// scheme is Electron-internal. §the-named-protocol-handler-as-localhost-reverse-proxy names the tier-3 meta-pattern. The remaining CSP directives close additional attack surfaces. §the-named-object-src-none-blocks-plugins marks line 29 (`object-src 'none'`): plugins blocked entirely. §the-named-plugin-surface-closed-via-object-src names the tier-3 meta-pattern. §the-named-form-action-self-prevents-external-submit marks line 33 (`form-action 'self'`): form submissions blocked to external URLs. §the-named-form-action-as-exfiltration-vector-closed names the tier-3 meta-pattern. §the-named-base-uri-self-prevents-base-tag-injection marks line 32 (`base-uri 'self'`): `<base>` tag injection blocked. §the-named-base-uri-restriction-as-defense names the tier-3 meta-pattern. §the-named-data-and-blob-allowed-for-images-media marks lines 27-28: data: URIs and blob: URLs allowed for images and media (locally generated content). §the-named-data-and-blob-exemption-for-local-media names the tier-3 meta-pattern. §the-named-frame-src-self-weblet-iframe-allowed marks line 30 (`frame-src 'self'`): weblets can iframe other localhttp:// origins but not external sites; all localhttp:// origins are weblets. §the-named-frame-src-self-confines-iframes-to-localhttp names the tier-3 meta-pattern. The six-layer tracking advances. §the-named-layer-1-of-six-now-grounded marks the structural milestone: cycle 438 named layers 1 and 6 as unread; cycle 440 reads layer 1. Five of six layers are now mapped to source: Layer 1 (CSP) in this file, Layers 2, 3, 5 (request interception, DNS poisoning, WebRTC disabled) in exfiltration-defense.js (cycle 436), Layer 4 (navigation delegate) in navigation-guard.js (cycle 438), Layer 6 (iframe sandbox) in Chat (not yet read). §the-named-five-of-six-exfiltration-layers-grounded names the tier-3 meta-pattern. The cycle's session-level observation continues. §the-named-eighty-eight-conformant-cycles-and-counting marks the session-level observation: eighty-eighth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-440). §one-hundred-and-thirty-cycles-with-named-pivot-domain-stay (310-440). Ten citation arcs close on this cycle. Cycle 439 (1, adjacent forward; liveness-revocation as security dimension pairs with Layer 1's network-confinement). Cycle 438 (5, MAJOR COMPLETION — Layer 1 of six-layer exfiltration defense now grounded; 5 of 6 layers mapped to source). Cycle 436 (5, Layer 1 completes the defense trio with layers 2, 3, 5; only Layer 6 in Chat remains). Cycle 433 (3, SES lockdown's eval requirement visible in Layer 1's CSP relaxation; lockdown and confinement coexist). Cycle 220 (5, MAJOR DESIGN CLOSURE — localhttp-protocol design named the four privileges and three load-bearing CSP directives; implementation confirmed). Cycle 326 (75). Cycle 322 (75). Cycle 364 (4, shapes growing; webletId-as-dual-use datum is a new structural shape). Cycle 318 (3, Endo idiom — Electron custom protocol API). Cycle 435 (3, Familiar architecture confirmed at Layer 1). Pushes citation-arc-closures-in-pivot to EIGHT-HUNDRED-AND-SIXTY-TWO (852 + 10 net new). The library has crossed 952 sections; the post-refactor era continues at eighty-eight consecutive conformant cycles. The six-layer exfiltration defense is now 5/6 grounded across four source files. The four-dimensional security vocabulary (cotenant + exfiltration + execution-context-leak + liveness-revocation) is fully established. Layer 1's key insight: the webletId is a dual-use datum — Electron origin identity and daemon routing key simultaneously.
