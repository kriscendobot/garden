---
source: endo-but-for-bots--packages-chat-message-parse-js
cycle: 386
lane: chat
ingested: 2026-06-18
package: chat
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: single-function-parser-with-regex
shape_subtype: petname-syntax-validator-and-extractor
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 34
---

The 30-line `packages/chat/message-parse.js` is the literal implementation of the petname parser used by the chat web UI ingested in cycle 385. Reading it reveals concrete constraints the README and DESIGN sketched only loosely. The single most structurally interesting move is on line 3: `/@([a-z][a-z0-9-]{0,127})(?::([a-z][a-z0-9-]{0,127}))?/g`. One regex does two jobs at once. It validates the petname syntax (constrains which strings ARE petnames) and extracts the components for further processing (which sub-strings ARE the parts). §the-named-petname-regex-as-validation-and-extraction names the move; §the-named-single-regex-validates-and-extracts names the tier-3 meta-pattern. One source-of-truth for both syntax legality and component extraction; changing the constraint changes both behaviors at once. The regex specifies numeric constraints invisible in the documentation. §the-named-128-character-petname-length-limit marks the `{0,127}` quantifier — up to 128 characters per identifier when including the required first character. Concrete numeric constraint not visible in cycle 385's README or DESIGN. §the-named-numeric-constraint-in-code-not-in-docs names the tier-3 meta-pattern; the implementation specifies precisely what the documentation only sketched. §the-named-strict-lowercase-alpha-start-petname-rule marks the first character constraint `[a-z]`. No digits, no caps, no underscores. §the-named-lowercase-alpha-start-as-namespace-discipline names the tier-3 meta-pattern; the constraint excludes ambiguous starts (digits could look like numbers; caps could conflict with proper-name conventions). §the-named-hyphens-but-not-underscores-in-petnames marks the character class `[a-z0-9-]`. Hyphens allowed; underscores not. §the-named-kebab-case-only-for-petnames names the tier-3 meta-pattern. The cycle's most striking finding is the documentation-vs-code mismatch. The code names capture group 1 `edgeName` (the pre-colon identifier) and capture group 2 `petName` (the optional post-colon identifier). Cycle 385's README said the format is `@pet-name:edge-name` — pet-name FIRST, edge-name SECOND. The CODE's variable naming reads as `@edge-name:pet-name` — edge-name first. Either the README's documentation is reversed from the code's variable names, or the code's variable names mislead about which is which. The OUTPUT field `petNames` is filled by `petName ?? edgeName` (line 21), so the OUTPUT pet-name is the LAST identifier (post-colon if present, pre-colon otherwise). §the-named-petname-vs-edgename-variable-naming-question names the finding; §the-named-petname-edgename-naming-inversion-between-README-and-CODE names the tier-3 meta-pattern. This documentation drift is sibling shape to cycle 384's §the-named-design-doc-trails-code — except this one points the opposite direction (README's vocabulary inverts code's variable naming rather than design lagging code's feature shipping). The reader must consult both to understand the convention; neither is independently authoritative. The output shape is named explicitly. §the-named-three-parallel-arrays-strings-petNames-edgeNames marks lines 25-29's return `{ strings, petNames, edgeNames }`. Three arrays the consumer can interleave: strings[0] + petname-ref[0] + strings[1] + petname-ref[1] + ... + strings[n]. §the-named-text-and-references-as-parallel-arrays-not-tagged-union names the tier-3 meta-pattern; the output is a flat-arrays shape, not an array of variant-tagged tokens. The implementation uses two smaller patterns worth naming. §the-named-fallback-via-nullish-coalescing marks line 21's `petNames.push(petName ?? edgeName)`. The `??` (nullish coalescing) falls back from petName to edgeName when petName is undefined. §the-named-nullish-coalescing-for-optional-capture-group names the tier-3 meta-pattern; concise fallback when an optional regex capture might be undefined. §the-named-replace-with-side-effect-callback marks the use of `message.replace(pattern, (...) => '')` with a callback that pushes into arrays (side effects), returning an empty string each time. The replace's return value is discarded. §the-named-replace-as-iteration-with-side-effects names the tier-3 meta-pattern; using `replace` not for replacement but for iterating matches with side effects on captured groups. The cursor-tracking discipline is named. §the-named-start-tracking-for-text-between-references marks line 15's `let start = 0` and lines 17-18's tracking of where each text-between-references slice begins and ends. The text-before-first-reference, text-between-references, and text-after-last-reference are all captured in `strings[]`. §the-named-cursor-pattern-for-tokenization names the tier-3 meta-pattern. The file's size carries a cluster-recurring observation. §the-named-thirty-line-single-function-parser marks the entire parser as one exported function plus a module-scope regex constant. Sibling shape to cycle 372's @endo/compartment-mapper extension.js (22-line URL extension parser) — both are small single-purpose parsing utilities. §the-named-parsing-utility-as-tiny-module names the tier-3 meta-pattern. §the-named-ts-check-pragma-as-opt-in-checking marks line 1 continuing the cluster convention. Seven citation arcs close on this cycle. Cycle 385 was the prior designs-lane chat README; cycle 386 source is its petname-parser implementation (arc 1). Cycle 384's design-doc-trails-code framing finds a sibling here: cycle 386 reveals documentation-trails-code via the variable-naming inversion. The new framing extends the three-directions observation from cycle 384 (arc 1). Cycle 374's @endo/cli names-in-transit demonstrated the petname syntax in CLI; cycle 386 shows the literal regex parsing it for both surfaces (arc 5). Cycle 367's @endo/exo validated-OCAP composes with petname-as-capability-reference; the parser is the gate that turns a string into a capability reference candidate (arc 7). Cycle 326's pure-naming-as-discipline reaches one more sibling; petname syntax is pure naming made into a regex constraint (arc 60). Cycle 372's parsing-utility-as-tiny-module is the sibling shape (arc 2). Cycle 322's @endo/errors is not used in this small parser (arc 60). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-EIGHTY-ONE (374 + 7 net new). §seventy-six-cycles-with-named-pivot-domain-stay (310-386). §thirty-fourth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-four consecutive cycles (353-386).
