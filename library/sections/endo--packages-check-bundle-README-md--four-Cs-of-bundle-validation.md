---
source: endo--packages-check-bundle-README-md
cycle: 397
lane: designs
ingested: 2026-06-18
repo: endo
package: check-bundle
cluster: pivot
shape: bundle-validation-package
shape_subtype: four-Cs-of-bundle-validation
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 45
---

The 13-line README for @endo/check-bundle describes a small package whose entire purpose is bundle integrity verification. The single most structurally interesting move is on line 4. The README names four alliterative criteria for bundle integrity: "consistency, completeness, coherence, and conciseness (no extra files)." Four C-words with a parenthetical clarification on the fourth. §the-named-four-Cs-of-bundle-validation names the move; §the-named-alliterative-criteria-as-mnemonic-set names the tier-3 meta-pattern. The four are intentionally a SET, not a list — the alliteration signals that the four are co-equal aspects of the same concept rather than four arbitrary checks. The parenthetical on conciseness reveals its sharp meaning. §the-named-conciseness-as-no-extra-files marks the gloss. Bundle validation REJECTS bundles that include files beyond what the bundle declares. §the-named-strict-set-not-superset names the tier-3 meta-pattern; the bundle is the EXACT set declared, not a superset. The hash structure is named explicitly. §the-named-internal-hashes-plus-one-external-hash marks line 3's "inspects all of its internal hashes and its one external hash." The bundle has MANY internal hashes (one per included file or per content-addressed slot) and ONE external hash (presumably the bundle's own hash as known by the caller). §the-named-many-internal-one-external-hash-structure names the tier-3 meta-pattern; the bundle is content-addressed at two levels — its contents and itself. The function name is named for what it is. §the-named-checkBundle-as-bundle-integrity-verifier marks the function's purpose as encoded in its name. §the-named-self-documenting-function-name-purpose names the tier-3 meta-pattern. The contract shape is asymmetric. §the-named-rejected-promise-as-failure-signal marks line 5: "returns a rejected promise if the bundle fails the check." Success resolves (with nothing meaningful); failure rejects. §the-named-binary-promise-contract names the tier-3 meta-pattern. Cycle 392's setup-ws-relay.js returned a result string on success; cycle 397 checkBundle returns nothing on success, just resolves. Different conventions for different package shapes. The bundle's data shape is named. §the-named-bundle-is-JSON-serializable marks line 9's comment "'bundle' is JSON-serializable." The bundle is a plain data structure (no live capabilities) and can be passed through any JSON-compatible boundary. §the-named-bundle-as-plain-data-not-capability names the tier-3 meta-pattern. The runtime precondition is named. §the-named-must-run-in-Endo-environment marks line 13: "This must run in an Endo environment. To run on Node.js, import `@endo/init` before importing `@endo/import-bundle`." §the-named-Endo-environment-as-runtime-precondition names the tier-3 meta-pattern; the package depends on the Endo runtime (SES + lockdown). The init pattern recurs from cycle 344. §the-named-init-import-as-environment-precondition marks the README's reference to `@endo/init` as the way to prepare the Endo environment on Node.js. Cycle 344 ingested @endo/init; cycle 397 reveals one of its consumer-facing roles. §the-named-init-as-import-side-effect-environment-setup names the tier-3 meta-pattern. The minimal-README pattern recurs. §the-named-thirteen-line-README-for-validation-package marks the file's size. Sibling shape to cycle 363 (8-line @endo/benchmark README), cycle 365 (3-line @endo/skel README), cycle 369 (14-line @endo/daemon README), cycle 380 (13-line changeset). The §the-named-minimal-README-for-substantial-system shape recurs across the cluster. The companion-package pattern is named. §the-named-import-bundle-named-as-runtime-companion marks line 13's mention of @endo/import-bundle alongside @endo/init. The check-bundle README POINTS to import-bundle as the package consumer would use. §the-named-validation-paired-with-runtime-consumer names the tier-3 meta-pattern; the validator and the loader are explicitly companion packages — check before load. Seven citation arcs close on this cycle. Cycle 396 was the prior chat-lane daemon types.d.ts; both 396 and 397 ingest package public APIs (21-line and 13-line respectively); both reveal compact validation/lifecycle surfaces (arc 1). Cycle 371's @endo/compartment-mapper builds bundles; @endo/check-bundle validates them; complementary packages (arc 3). Cycle 369's @endo/daemon README named application runner; bundles are what the application runner runs (arc 7). Cycle 344's @endo/init named as the runtime precondition import; cycle 397 shows the consumer's usage (arc 19). Cycle 363's @endo/benchmark README is the minimal-README-for-substantial-system sibling (arc 3). Cycle 326's pure-naming-as-discipline reaches one more sibling; four-Cs is pure naming applied to the validation criteria set (arc 71). Cycle 322's @endo/errors not directly invoked in this small README (arc 71). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-FIFTY-EIGHT (451 + 7 net new). §eighty-seven-cycles-with-named-pivot-domain-stay (310-397). §forty-fifth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty-five consecutive cycles (353-397).
