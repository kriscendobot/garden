---
source: endo-but-for-bots--packages-captp-src-loopback-js
cycle: 430
lane: chat
ingested: 2026-06-22
repo: endo-but-for-bots
branch: llm
package: captp
cluster: pivot
shape: in-process-CapTP-pair
shape_subtype: real-CapTP-in-degenerate-configuration
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 78
session_milestone: 120-post-pivot-cycles-and-cycle-430
---

The 117-line loopback.js from @endo/captp/src is the in-process CapTP cycle 429 framed as "async barrier between near and far objects." Cycle 430 is the ONE-HUNDRED-AND-TWENTIETH consecutive non-garden source after the pivot AND the numeric 430-cycle mark — double milestone. Cycle 430's most structurally interesting move REFINES cycle 429's framing of Loopback in a structurally significant way. §the-named-loopback-is-real-CapTP-in-degenerate-configuration names the observation. Lines 54 and 83 each call makeCapTP. The Loopback creates TWO CapTP instances: one named `near-${ourId}`, one named `far-${ourId}`. Each side's send function is the OTHER side's dispatch — line 54 passes `o => farDispatch(o)`; line 83 passes `nearDispatch` directly. The two instances exchange JSON-serialized messages through real CapTP serialization. Cycle 429 framed Loopback as "mock CapTP with real in-process objects" (the inverse of cycle 403's mock-internals-real-externals). Cycle 430 refines: Loopback isn't a MOCK at all — it's REAL CapTP in a DEGENERATE configuration where both "remote" sides happen to live in the same address space. Every byte of serialization, every CapTP protocol message, every ref-resolution still goes through the production code. §the-named-degenerate-configuration-vs-mock names the tier-3 meta-pattern; the cluster's framing now distinguishes "mock" (different code paths for testing) from "degenerate configuration" (same code paths, simpler input). The async barrier is created by serialization. §the-named-nonce-based-round-trip-creates-async-barrier marks lines 29-39 and 92-105. To pass an object as "far," the loopback assigns it a fresh nonce, stores `{nonce -> object}` in a finalizing Map, and asks the OTHER side via E(refGetter).getRef(nonce). The other side fetches and deletes the entry. The object passes through the JSON serialization layer even though it's the same address space. §the-named-serialization-round-trip-as-async-barrier names the tier-3 meta-pattern; the async barrier is mechanical (serialization), not protocol-level. The finalizing Map prevents leaks. §the-named-makeFinalizingMap-for-no-leak-nonce-storage marks line 30. The nonce-to-ref Map releases entries when not referenced. Connects to cycle 423's rights-amplification framing about WeakMap. §the-named-finalizing-map-as-anti-leak-storage names the tier-3 meta-pattern. The symmetric pair is named. §the-named-makeFar-and-makeNear-as-symmetric-pair marks lines 108-109. §the-named-symmetric-far-near-makers names the tier-3 meta-pattern. The bootstrap is shared. §the-named-shared-bootstrap-for-loopback marks lines 32-39 + 54 + 83. The bootstrap (a refGetter Far object) is created ONCE and passed to BOTH makeCapTP calls. Both sides expose the SAME refGetter. Unusual — normally each side has its own bootstrap. §the-named-shared-bootstrap-as-loopback-trick names the tier-3 meta-pattern. Loopback uses original encoding. §the-named-loopback-uses-original-qclass-encoding-not-smallcaps marks lines 41-44: JSON.stringify({'@qclass': 'slot', index: 0}). The original marshal encoding (with @qclass) is used, not smallcaps. §the-named-qclass-encoding-as-loopback-choice names the tier-3 meta-pattern; cycle 423's two-marshal-encodings framing now has a specific use-case justification — Loopback intentionally uses original. The side-classification predicates are exposed. §the-named-isOnlyNear-and-isOnlyFar-predicates marks lines 21-22 and 110-111. §the-named-side-classification-predicates names the tier-3 meta-pattern. Per-side stats are available. §the-named-per-side-stats-via-getNearStats-and-getFarStats marks lines 23-24 and 112-113. §the-named-per-CapTP-monitoring names the tier-3 meta-pattern. The TrapCaps implementation is built in. §the-named-loopback-trapGuest-via-nearTrapImpl marks lines 55-70. The trapGuest handles cross-boundary traps synchronously via the in-process nature. Connects to cycle 429's TrapCaps framing — Loopback has a built-in TrapCaps implementation. §the-named-loopback-built-in-trap-implementation names the tier-3 meta-pattern. The circular definitions need explicit disables. §the-named-mutual-CapTP-references-require-use-before-define-disable marks lines 53, 60, 68. The two CapTP instances reference each other's dispatch + serialize/unserialize, creating circular definition. §the-named-circular-definition-via-eslint-disable names the tier-3 meta-pattern. Far is used directly. §the-named-Far-without-exo-for-minimal-bootstrap marks line 32. Uses Far from @endo/marshal directly, NOT through exo/makeExo. The bootstrap is minimal — no need for interface guards. Cycle 425 noted exos add validation; here that overhead is skipped. §the-named-Far-skipping-exo-when-no-guard-needed names the tier-3 meta-pattern. The single-use nonce is a security property. §the-named-single-use-nonce-for-generative-security marks line 36: after getRef returns the value, nonceToRef.delete(nonce). Each ref pass uses a FRESH NONCE consumed on fetch. §the-named-single-use-nonce-as-generative-capability names the tier-3 meta-pattern. The ts-expect-error acknowledges imperfection. §the-named-ts-expect-error-as-known-type-imperfection marks line 103. The type system can't easily express the EResult<T> coercion. §the-named-known-type-imperfection-as-explicit-acknowledgment names the tier-3 meta-pattern; sibling to cycle 424's acknowledged-stale-error-message — both are in-source acknowledgments of imperfection. The harden discipline is followed before storage. §the-named-harden-on-ref-before-storage marks line 102. §the-named-harden-before-storage-discipline names the tier-3 meta-pattern; consistent with cycle 423's marshal-requires-frozen-input. The practical purpose is conformance testing. §the-named-loopback-as-the-test-double-for-CapTP names the use case: enable testing of cap-based code in-process without an actual network. The serialization round-trip catches bugs (forgotten Far, unhardened values) that production CapTP would also catch. §the-named-loopback-as-CapTP-conformance-tester names the tier-3 meta-pattern; Loopback's serialization is identical to production, so test-time failures predict production failures. The cycle's session-level observation is a milestone. §the-named-seventy-eight-conformant-cycles-and-counting marks the session-level observation: seventy-eighth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-430). §the-named-cycle-430-as-numeric-milestone marks the double milestone: numeric 430-cycle mark AND one-hundred-and-twentieth post-pivot cycle. §one-hundred-and-twenty-cycles-with-named-pivot-domain-stay (310-430). Ten citation arcs close on this cycle. Cycle 429 (5, MAJOR REFINEMENT — Loopback isn't mock; it's real CapTP in degenerate configuration; cycle 429's framing sharpened). Cycle 428 (3, harden-before-storage parallels the harden-required-by-marshal framing). Cycle 425 (3, Far-without-exo as a skip of the defensive-remotable discipline when guards aren't needed). Cycle 423 (3, qclass-original-encoding vs smallcaps now has a use-case justification — Loopback uses original). Cycle 403 (5, mock-vs-degenerate-configuration framing refines the mock-internals-real-externals topology). Cycle 408 (3, role-cardinality-reduction parallels the two-CapTP-back-to-back shape). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75). Cycle 387 (3, branded-types via Far('refGetter', ...) label). Cycle 318 (3, E and eventual send underlie both CapTPs in Loopback). Pushes citation-arc-closures-in-pivot to SEVEN-HUNDRED-AND-SIXTY-TWO (752 + 10 net new). The library has crossed 942 sections; the post-refactor era continues at seventy-eight consecutive conformant cycles; the cluster has crossed the one-hundred-and-twentieth post-pivot cycle. The cluster's framing of CapTP is now sharper: production CapTP for cross-vat capability transfer; Loopback for in-process testing using the same code paths with different inputs; TrapCaps as the explicit opt-out for trusted relationships. Three configurations of one protocol.
