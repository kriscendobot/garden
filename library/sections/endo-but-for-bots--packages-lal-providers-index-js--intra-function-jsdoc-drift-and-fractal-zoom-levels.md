---
source: endo-but-for-bots--packages-lal-providers-index-js
cycle: 406
lane: chat
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: provider-factory
shape_subtype: dispatch-with-intra-function-jsdoc-drift
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 54
---

The 113-line providers/index.js is the LLM provider factory that ties together the four providers (anthropic, gemini, llamacpp, ollama) and uses the config.js helpers cycle 400 ingested. Eighth lal-package artifact in the cluster (after the README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, the simulator README, mock-powers.js, and primer/README.md). Cycle 406's most structurally interesting move pushes the cluster's drift framings down to the finest possible scale. §the-named-JSDoc-comment-undercounts-implementation-within-same-source-file names the observation. Lines 21-24's JSDoc comment on createProvider lists THREE providers: '"anthropic.com" → Anthropic provider; "/v1" suffix → llama.cpp; Otherwise → Native Ollama provider.' But the function IMPLEMENTS four providers — Gemini is imported (line 8), dispatched (lines 52-75), and constructed via makeGeminiProvider. The JSDoc comment is missing the ENTIRE Gemini branch. The doc-vs-code drift now occurs at the SMALLEST POSSIBLE SCALE: within a single function's own JSDoc comment. §the-named-intra-function-JSDoc-drift names the tier-3 meta-pattern. The cluster's drift vocabulary now reveals a fractal structure. §the-named-fractal-drift-across-zoom-levels names a refinement. The cluster has identified drift at: multi-document scale (cycle 401: README + design doc + source disagree); document-internal scale (cycle 401 LAL-ARCHITECTURE.md sixteen-vs-eighteen tools comment-vs-table); function-comment scale (cycle 406, here — JSDoc comment vs the very function below it). The SAME PATTERN appears at multiple zoom levels. §the-named-comment-lags-implementation-within-one-file names the tier-3 meta-pattern at the function-comment scale. The dummy auth token pattern is novel. §the-named-hardcoded-fallback-auth-token-as-dummy-key marks line 79: `const apiKey = env.LAL_AUTH_TOKEN || 'ollama'`. The openai-compatible provider DEFAULTS apiKey to the literal string 'ollama' when no LAL_AUTH_TOKEN is provided. This lets the OpenAI-compatible provider work against a local llama.cpp instance that doesn't actually need a key — the 'ollama' string is a dummy stand-in. §the-named-dummy-string-where-real-secret-not-needed names the tier-3 meta-pattern. A novel direction of drift is named. §the-named-library-uses-console-log-against-its-own-convention marks four console.log calls at lines 48, 65, 86, 100: every provider creation logs `[LAL] Using X provider at BASEURL with model: MODEL`. The outer CLAUDE.md convention says: "Libraries should be silent by default. No console.log from library code." But this code uses console.log for provider initialization diagnostics. §the-named-code-violates-its-own-stated-convention names the tier-3 meta-pattern. This is a NOVEL eighth direction of document-vs-code drift in the cluster: not document lagging code (cycle 400), not types lagging design (cycle 402), not documents agreeing against source (cycle 401), but CODE VIOLATING the convention the design doc states. The harden discipline reveals another instance. §the-named-no-harden-on-createProvider marks the function declaration. The file declares `export const createProvider = env => { ... }` at line 36 but no `harden(createProvider)` call appears in the file. The CLAUDE.md convention says: "Every named export MUST have a corresponding `harden(exportName)` call immediately after the declaration." Cycle 400's config.js followed this strictly. This file does not. §the-named-harden-discipline-not-uniform-across-package names the tier-3 meta-pattern — another code-violates-convention instance within the same package. The Provider interface is uniform. §the-named-uniform-Provider-interface-across-four-implementations marks lines 13-16's @typedef. All four providers expose a single `chat: (messages, tools) => Promise<{message}>` method. §the-named-single-method-interface-as-provider-contract names the tier-3 meta-pattern. The dispatch style is named. §the-named-if-with-early-return-as-dispatch-style marks lines 41-107: four `if (providerKind === '...')` branches plus a default block; each branch has its own early return; the default block is the file's final lines (98-107). §the-named-early-return-instead-of-else-chain names the tier-3 meta-pattern; compared to cycle 400's substring-match-cascade inside detectProviderKind, this is the PROVIDER CONSTRUCTION dispatch (different layer of the same configuration flow). The env-var parsing is duplicated. §the-named-duplicated-env-parsing-across-providers marks lines 59-64 (gemini max-tokens) and lines 80-85 (openai-compatible max-tokens): identical code, duplicated between two provider branches. §the-named-env-var-parsing-duplicated-across-branches names the tier-3 meta-pattern. The auth handling is asymmetric across providers. §the-named-asymmetric-auth-requirement marks the four different handlings: Anthropic throws (lines 43-47); Gemini throws (lines 54-58); openai-compatible defaults to dummy 'ollama' (line 79); Ollama passes through (line 99, no check). §the-named-auth-required-vs-optional-vs-dummy-vs-passthrough names the tier-3 meta-pattern — four providers, four different auth-handling shapes. The factory naming convention is uniform. §the-named-providers-imported-via-named-factory-functions marks lines 7-10's imports of makeAnthropicProvider, makeGeminiProvider, makeLlamaCppProvider, makeOllamaProvider. §the-named-makeXProvider-as-factory-naming-convention names the tier-3 meta-pattern; sibling to the Endo idiom for factory-function-returning-objects. The re-exports add an alternative API surface. §the-named-re-exports-of-all-four-factories marks lines 110-113. §the-named-re-export-as-alternative-API-surface names the tier-3 meta-pattern; the re-exports allow direct use of factories outside the createProvider dispatch. The default host is Ollama. §the-named-default-LAL_HOST-localhost-11434 marks line 37: `const baseURL = env.LAL_HOST || 'http://localhost:11434'`. The default LAL_HOST points at the Ollama default port. So out-of-the-box, no env config, you get Ollama as the provider. §the-named-ollama-as-default-out-of-the-box-provider names the tier-3 meta-pattern. The config helper is called per-provider-construction. §the-named-resolveModelForHost-applied-to-LAL_MODEL marks line 39: `const model = resolveModelForHost(baseURL, env.LAL_MODEL)`. This is the function cycle 400 named §the-named-resolveModelForHost-upgrades-qwen3-default. §the-named-config-helper-called-per-provider-construction names the tier-3 meta-pattern; the provider factory calls the config helper on every createProvider invocation. The modular separation is named. §the-named-imports-from-sibling-config-module marks line 11's imports of detectProviderKind and resolveModelForHost from ./config.js. §the-named-modular-config-separated-from-construction names the tier-3 meta-pattern; the configuration logic lives in a sibling module rather than inline. The cycle's session-level observation continues. §the-named-fifty-four-conformant-cycles-and-counting marks the session-level observation: fifty-fourth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-406). §ninety-six-cycles-with-named-pivot-domain-stay (310-406). Nine citation arcs close on this cycle. Cycle 405 (1, adjacent forward; primer-tool-name-drift sibling now visible at JSDoc-within-function scale; same drift shape, finer scale). Cycle 400 (3, config.js helpers this file uses; the JSDoc undercount recurs at the same shape cycle 400 named at the README-vs-source scale). Cycle 402 (3, types lag design vs comments lag implementation; both intra-package drift instances; bidirectional-drift framing now extends to comment-vs-code direction). Cycle 401 (3, the "no console.log from library code" convention this file violates). Cycle 386 (3, README-vs-CODE-inversion sibling for JSDoc drift). Cycle 384 (3, design-doc-trails-code sibling). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 364 (4, shapes count keeps growing — now EIGHT-PLUS-and-counting drift directions). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-THIRTY-ONE (522 + 9 net new). The library has crossed 918 sections; the post-refactor era continues at fifty-four consecutive conformant cycles; the cluster's drift vocabulary now distinguishes FRACTAL scales of drift — multi-document, document-internal, function-comment — at all of which the same lag-and-disagreement pattern appears.
