---
source: endo--packages-daemon-types-d-ts
cycle: 396
lane: chat
ingested: 2026-06-18
repo: endo
package: daemon
cluster: pivot
shape: package-root-types-d-ts
shape_subtype: lifecycle-verbs-and-client-factory
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 44
---

The 21-line `packages/daemon/types.d.ts` is the daemon package's top-level public TypeScript declarations — the surface a package consumer sees. The single most structurally interesting move sits on lines 3-4. Four functions are exported in two paired name inversions: `makeRefReader` + `makeReaderRef` and `makeRefIterator` + `makeIteratorRef`. The NAME INVERSION encodes the FUNCTIONAL INVERSION. `makeRefReader(ref)` takes a ref and gives you a reader; `makeReaderRef(reader)` takes a reader and gives you a ref. The two are dual operations across the local/remote boundary. §the-named-ref-X-and-X-ref-as-paired-name-inversions names the move; §the-named-name-inversion-encodes-function-inversion names the tier-3 meta-pattern. The naming convention is self-documenting about which direction of the boundary the function operates. A reader of the type names can predict the function's directionality without consulting the docs. The lifecycle section is named explicitly. §the-named-six-lifecycle-verbs marks lines 7-12: start + stop + restart + terminate + clean + purge. Each takes an optional Config and returns `Promise<void>`. §the-named-six-named-lifecycle-operations names the tier-3 meta-pattern. The verbs cover the full lifecycle from cold-start to cleanup. Two distinct-by-degree pairs sit inside the six. §the-named-terminate-vs-stop-distinction marks the pair: stop is presumably graceful, terminate forceful. §the-named-graceful-vs-forceful-halt-distinct-verbs names the tier-3 meta-pattern. §the-named-clean-vs-purge-distinction marks the parallel pair: clean is presumably partial state removal, purge full. §the-named-partial-vs-full-cleanup-distinct-verbs names the tier-3 meta-pattern. The daemon API has two pairs of distinct-by-degree verbs — the vocabulary surface encodes a graduated severity for both halt and cleanup. The client factory is named precisely. §the-named-makeEndoClient-as-generic-bootstrap-factory marks lines 13-21: `makeEndoClient<TBootstrap>(name, sockPath, cancelled, bootstrap?)`. Four parameters plus a generic for the bootstrap type. The function returns `Promise<{ getBootstrap, closed }>`. §the-named-generic-client-factory-with-bootstrap-type names the tier-3 meta-pattern. The cancellation pattern is named. §the-named-cancelled-promise-as-deadline-parameter marks line 16's `cancelled: Promise<void>`. The third parameter is a Promise that resolves when the operation should abort. §the-named-cancellation-via-promise-resolution names the tier-3 meta-pattern; cancellation is signaled by fulfillment of the cancelled promise, not by an explicit cancel() call. The pattern is dual to the shutdown signal. §the-named-closed-promise-as-shutdown-signal marks line 20's `closed: Promise<void>` in the return shape. The connection's shutdown is itself a promise the caller can await. §the-named-shutdown-via-promise-resolution names the tier-3 meta-pattern; both cancellation (input) and shutdown (output) use promise-resolution to signal lifecycle transitions. The asymmetric thunk is named. §the-named-getBootstrap-as-named-thunk marks line 19: `getBootstrap: () => Promise<EndoBootstrap>`. The bootstrap is accessed via a function call, not a property access. §the-named-getX-as-named-thunk-pattern names the tier-3 meta-pattern; the function-call shape signals that the result might require work (network call, decryption, etc.) rather than being a stored value. The Config type's reach is named. §the-named-Config-as-shared-config-shape marks lines 1, 6, and 7-12. Config is imported from src/types.js, re-exported, and used as the optional parameter type for all six lifecycle functions. One Config type, six users. §the-named-shared-config-type-across-lifecycle names the tier-3 meta-pattern. The file's location is structurally meaningful. §the-named-types-d-ts-as-package-public-API marks the file sitting at the package root (not under src/) — this IS the daemon package's public TypeScript declarations as seen by consumers. §the-named-package-root-types-d-ts-as-consumer-surface names the tier-3 meta-pattern. The compactness observation recurs. §the-named-twenty-one-line-public-API-surface marks the 21 lines summarizing the daemon's entire public API. Sibling shape to cycle 363/365/369/380's minimal-but-rich documents; this is the type-declarations-as-API-surface version. Seven citation arcs close on this cycle. Cycle 395 was the prior changeset that modified the daemon's types; cycle 396 shows the surface that change touches (arc 1). Cycle 369's @endo/daemon README named lifecycle management as the CLI's primary job; cycle 396 reveals the six lifecycle verbs in the type signature (arc 6). Cycle 387's AGENTS.md TypeScript conventions; types.d.ts pure-re-export discipline partially applies here — this file has function declarations, not pure re-exports, but it's the package-root file, not the types-index pair where the convention applies most strictly (arc 4). Cycle 367's exo ERef used elsewhere in daemon types (arc 14). Cycle 321's eventual-send substrate (arc 14). Cycle 326's pure-naming-as-discipline reaches one more sibling; the ref-X / X-ref name inversions are pure naming applied to encode direction (arc 70). Cycle 322's @endo/errors not directly invoked in this declarations file (arc 70). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-FIFTY-ONE (444 + 7 net new). §eighty-six-cycles-with-named-pivot-domain-stay (310-396). §forty-fourth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty-four consecutive cycles (353-396).
