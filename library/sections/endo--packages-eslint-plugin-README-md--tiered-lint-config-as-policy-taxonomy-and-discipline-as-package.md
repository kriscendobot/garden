---
source: endo--packages-eslint-plugin-README-md
cycle: 359
lane: designs
ingested: 2026-06-15
package: eslint-plugin
cluster: pivot
cluster_position: twenty-second
shape: substrate-policy-minimal
shape_subtype: discipline-as-package
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 7
---

The 54-line README for @endo/eslint-plugin is the **twenty-second package** in the pivot cluster, and it is the first one whose shape is **discipline-as-package** rather than runtime substrate or data format. The twenty-one prior members were either runtime substrate (harden, errors, lockdown, init, ses, far, immutable-arraybuffer, nat, check), data formats (base64, hex, lp32, netstring, zip), control flow (eventual-send → promise-kit, trampoline), or bundling/transport (bundle, import-bundle, cache-map, memoize, path-compare, stream). The twenty-second opens a new shape: a package that **enforces substrate discipline at lint time** rather than providing substrate at runtime. §the-named-discipline-as-package marks this new tier-3 meta-pattern as a peer to §the-named-runtime-substrate-package and §the-named-data-format-package, the two shapes that dominated the prior twenty-one. The README's central move is §the-named-tiered-lint-config-as-policy-taxonomy: it offers five named configs and explains what each is for. `recommended` is rules for code compatible with Hardened JS — the lint plugin reinforces at compile time the discipline the lockdown package enforces at runtime; §the-named-recommended-tier-paired-with-Hardened-JS names this lockstep relationship. `imports` carries opinions on how packages should use imports — a separate concern from compatibility, named separately. `style` carries opinions on JS coding style — again separate. `strict` is presented as "all of the above" — §the-named-strict-as-union-of-all-non-internal: composition by inclusion, not by additional rules. `internal` is "rules only for packages within the Endo source repository" — §the-named-internal-rules-only-for-source-repo: an explicit acknowledgment that some rules only apply when you ARE the project, not when you consume it; the discipline package is itself meta-recursive about who it disciplines and when. Honesty about incompleteness appears on the final line. The "Supported Rules" section reads literally `* Fill in provided rules here` — a placeholder shipped to npm rather than hidden. §the-named-unfilled-supported-rules-section names the specific placeholder; §the-named-honest-placeholder-not-hidden-gap names the broader tier-3 meta-pattern. This pairs directly with cycle 357's §the-named-for-expedience-as-honest-acknowledgment-with-named-recovery-path in @endo/zip: both READMEs choose to surface their gaps in-place rather than smoothing them over. Two adjacent shapes of honest-incompleteness-named-in-place: cycle 357's intentional-omission-with-named-recovery (five "for expedience" pairings, each with a recovery path) and cycle 359's placeholder-text-as-acknowledgment-of-future-work (one literal `Fill in provided rules here`). Five citation arcs close on this cycle: cycle 357's honest-incompleteness shape pairs with cycle 359's adjacent forward (arc 1); cycle 326's §the-named-pure-naming-as-discipline reaches one more discipline-shaped sibling (arc 33); cycle 345's lockdown + Hardened JS naming trace finds its lint-time peer here (arc 14); cycle 320's earlier eslint trace from garden design ingests reconnects (arc 45); and cycle 188's broader Endo policy doc that referenced ESLint customizations now resolves to the package itself (arc 172). The cycle pushes citation-arc-closures-in-pivot to ONE-HUNDRED-EIGHTY-FIVE (180 + 5 net new). §forty-nine-cycles-with-named-pivot-domain-stay (310-359). §twenty-second-named-package-in-the-pivot-cluster. §seventh-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across seven consecutive cycles (353/354/355/356/357/358/359), confirming the steady state.
