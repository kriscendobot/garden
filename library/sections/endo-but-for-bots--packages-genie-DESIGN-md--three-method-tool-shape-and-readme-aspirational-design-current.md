---
source: endo-but-for-bots--packages-genie-DESIGN-md
cycle: 382
lane: chat
ingested: 2026-06-18
package: genie
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: design-document-for-AI-agent-framework
shape_subtype: tool-interface-and-architecture-spec
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 30
---

The 203-line `packages/genie/DESIGN.md` on endo-but-for-bots' `llm` branch is the design counterpart to cycle 381's README. Reading it answers the questions the README raised about the tool interface, the memory layout, and what the package promises versus what it currently ships. The single most structurally interesting move is the tool interface specification on lines 124 through 145. Every tool follows a uniform three-method shape: `schema` (an `M.interface(...)` from @endo/patterns), `help` (a parameterless function returning a description string), `execute` (an async function returning `{ success: true, result: ... }`). The interface is uniform across the eight tools; the implementation varies. §the-named-tool-as-schema-help-execute-three-method-shape names the move; §the-named-three-method-tool-shape names the tier-3 meta-pattern. Schema validates inputs; help describes the tool; execute does the work. Three responsibilities, three named methods, one shape. The schema mechanism is named explicitly. §the-named-schema-via-M-interface marks line 129's import of `M` from `@endo/patterns`. Tool schemas use `M.interface(...)`, `M.string()`, `M.number().optional()`, `M.boolean().optional()` patterns. §the-named-patterns-M-namespace-applied-to-tool-validation names the tier-3 meta-pattern. The bot-fork's tool layer composes with the endojs/endo substrate's pattern-builder DSL — cycle 327's @endo/patterns README named `M.callWhen`, cycle 367's @endo/exo used `M.interface` for method guards, cycle 382's @endo/genie uses `M.interface` for tool-call schemas. The same DSL serves three different layers. The help convention is named. §the-named-help-method-as-convention marks every tool's `help: () => 'Tool description...'`. The function is parameterless and returns a description; §the-named-self-describing-tool-via-help-method names the tier-3 meta-pattern. The agent can call `help()` on each available tool to generate documentation, system prompts, or LLM-visible catalogs without hard-coding the strings. The result shape is standardized. §the-named-success-result-shape marks line 142's `return { success: true, result: ... }`. The caller branches on success without parsing the result body. §the-named-success-flag-as-uniform-result names the tier-3 meta-pattern. The DESIGN extends the README's security feature list. §the-named-six-security-features-now-includes-rate-limiting marks lines 50-55, where the README's five properties (Input validation + Path traversal prevention + Code injection prevention + Dangerous operation detection + Content validation) gain a sixth on the DESIGN: Rate limiting support. §the-named-design-doc-adds-feature-beyond-README names the tier-3 meta-pattern; the README is a subset, the DESIGN is the full surface. Four design principles are named. §the-named-four-design-principles marks lines 73-98: Security-First + Modular & Extensible + Autonomous Execution + Developer-Friendly. Each gets a sub-section. §the-named-four-named-design-principles names the tier-3 meta-pattern; the principles are the design's vocabulary. The architecture diagram is structural. §the-named-file-structure-as-architecture-diagram marks lines 28-34's ASCII tree of `src/system/` with `index.js` plus three siblings. §the-named-ascii-tree-as-component-decomposition names the tier-3 meta-pattern; the diagram IS the documentation for that subsystem. The memory layout is named. §the-named-memory-directory-layout marks lines 149-157: `MEMORY.md` as the main file + `memory/topic.md` for topic-specific memory + `memory/YYYY-MM-DD-summary.md` for dated summaries of prior session history. §the-named-dated-summary-files-as-session-history names the tier-3 meta-pattern; the agent's history is partitioned by date in human-readable files. Sibling shape to cycle 381's §the-named-markdown-as-agent-state-format. The current implementation's limit is named honestly. §the-named-simple-substring-search-upgrade-path marks line 161: "Simple substring search (can be upgraded to vector embeddings)." Honest acknowledgment of the current implementation and the future direction. §the-named-honest-current-implementation-with-named-upgrade names the tier-3 meta-pattern; sibling honest-acknowledgment shape from cycles 357 (zip for-expedience), 359 (eslint-plugin honest-placeholder), 372 (exported-for-tests), 375 (TODO-as-future-removal-candidate), 377 (bikeshedding acknowledged), 378 (honesty-via-shared-lie), 379 (empty-file-as-no-change). The Endo project (and its bot fork) consistently surfaces in-flight states in source. The heartbeat format is specified concretely. §the-named-heartbeat-markdown-checkbox-format marks lines 167-177: two sections (`## Current Tasks` with `- [ ]` checkboxes and `## Done Tasks` with `- [x]` checkboxes). Standard GitHub-flavored markdown. §the-named-task-list-as-checkbox-markdown names the tier-3 meta-pattern. The future roadmap is enumerated. §the-named-seven-future-enhancements-list marks lines 179-187: Full Text Search via sqlite fts5 + Semantic Search via RAG + Tool Chaining + Conversation Memory + Rate Limiting + Webhooks + Caching. The DESIGN names exactly which features are NOT yet implemented. §the-named-seven-named-future-enhancements names the tier-3 meta-pattern. The most cross-cutting framing this cycle adds is the README-vs-DESIGN comparison. §the-named-design-doc-acknowledges-shortcuts-the-README-hides marks the observation: the README implies a complete agent system; the DESIGN names the seven enhancements not yet shipped. The cycle 363→364 ANALOGY-VS-LITERAL arc finds a new variant here. The README ABSTRACTS the implementation's current state to its aspirational complete form; the DESIGN reveals the gap between current and aspirational. §the-named-readme-aspirational-design-current names the tier-3 meta-pattern; the DESIGN is the authentic accounting, the README is the marketed form. The contribution surface is named. §the-named-four-step-tool-contribution-checklist marks lines 198-203: define schema using `@endo/patterns` + implement `help()` documentation + add security validations + update `tools/index.js` exports. Four steps to add a new tool. §the-named-checklist-as-tool-extension-contract names the tier-3 meta-pattern. §the-named-testing-strategy-four-layer marks lines 189-194: Unit + Integration + End-to-end + Security validation as the four test layers. §the-named-four-layer-test-strategy names the tier-3 meta-pattern. Seven citation arcs close on this cycle. Cycle 381 was the prior README; cycle 382 DESIGN reveals what the README hid, opening the new readme-aspirational-design-current framing (arc 1). Cycle 367's @endo/exo M.interface composed with tool-as-schema-help-execute three-method shape; the bot-fork's tools are validated-OCAP objects via patterns (arc 3). Cycle 327's @endo/patterns M namespace applied here to tool validation (arc 6). Cycle 374's @endo/cli names-in-transit social-network demo on master; cycle 382 reveals tools are how that network's capabilities are parameterized in the bot-fork (arc 2). Cycle 359's honest-placeholder shape sibling — "can be upgraded to vector embeddings" is the same shape as "Fill in provided rules here" (arc 2). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 56). Cycle 322's @endo/errors is not directly used in DESIGN code examples (arc 56). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-FIFTY-THREE (346 + 7 net new). §seventy-two-cycles-with-named-pivot-domain-stay (310-382). §thirtieth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty consecutive cycles (353-382). The thirtieth-conformant milestone is itself worth marking; thirty consecutive cycles maintain the single-body section discipline established by the post-refactor era's refactor.
