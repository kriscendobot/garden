---
source: endo-but-for-bots--packages-lal-primer-tools-md
cycle: 407
lane: designs
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: agent-facing-tool-reference
shape_subtype: cluster-self-revision-with-fourth-tool-count
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 55
---

The 89-line primer/tools.md is the agent-facing tool reference inside @endo/lal's primer corpus and the ninth lal-package artifact in the cluster (after the README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, the simulator README, mock-powers.js, primer/README.md, and providers/index.js). Cycle 407's single most structurally interesting move forces a REVISION of a prior cycle's framing. §the-named-cycle-402-vestigial-framing-needs-revision names the observation. Cycle 402 read agent.types.d.ts and found PendingProposal and ProposalNotification types still defined. Cycle 401's LAL-ARCHITECTURE.md had said "No proposal/grant workflow is needed" and "There is no proposal/grant workflow." Cycle 402 reasoned: the design moved on but the types remained — vestigial. Cycle 407's primer/tools.md reveals the truth at lines 61-62: "define(source, slots) — Propose code with named slots for the host to fill (PREFERRED)". The primer tells the LLM that `define()` is PREFERRED for code evaluation. The types are NOT vestigial — they implement the PREFERRED code-evaluation mechanism. The DESIGN DOC is wrong. §the-named-design-doc-claims-workflow-absent-but-primer-prefers-it names the tier-3 meta-pattern; the design doc trails the implementation in a NEW direction — claiming a workflow doesn't exist when it not only exists but is the recommended path. The librarian must revise its own prior framing. §the-named-librarian-self-revision-of-prior-cycle-framing names the structural fact: cycle 402's vestigial-types-from-abandoned-design framing is now revised. The cluster's accumulated framings can themselves be wrong; new evidence updates the picture. §the-named-revision-of-prior-framing-as-cluster-discipline names the tier-3 meta-pattern; the cluster is not just building up framings — it must also revise them as the corpus widens. The librarian's drift framings have themselves been subject to drift. The cycle adds a fourth tool count to the cluster's framings. §the-named-four-different-tool-counts-across-four-documents extends cycle 405's three-models-across-four-documents framing to the tool-count axis. Cycle 407 counts fifteen tools in primer/tools.md: help (self-doc) + 7 directory tools (list, has, lookup, remove, move, copy, makeDirectory) + 8 mail tools (listMessages, adopt, dismiss, request, resolve, reject, reply, send) + locate (identity) + 3 capability tools (inspect, readText, writeText) + 2 code-evaluation tools (define, evaluate) = 15. Compare to: LAL-ARCHITECTURE.md's comment ("16 tool definitions total" per cycle 401); LAL-ARCHITECTURE.md's table (18 tools per cycle 401); mock-powers.js (22 methods per cycle 404); primer/tools.md (15 tools, this cycle). FOUR DIFFERENT TOOL COUNTS across four sibling documents about the same agent. §the-named-four-tool-counts-across-four-documents names the tier-3 meta-pattern; the drift cluster's vocabulary now spans count-disagreement at the same numeric level as the cluster's model-string disagreements. The code-evaluation tier is paired. §the-named-define-and-evaluate-as-paired-code-tools names the structural pairing at lines 61-64. §the-named-propose-vs-execute-as-two-code-tools names the tier-3 meta-pattern. The preference is three-tiered. §the-named-three-tier-tool-preference-direct-define-evaluate marks lines 66-69: "IMPORTANT: Always prefer direct tool calls over `evaluate()` or `define()`. Many tasks can be accomplished without code execution." Three priority tiers: direct tools > define > evaluate. §the-named-priority-ordered-tool-preference names the tier-3 meta-pattern. The reply tool is preferred over send. §the-named-reply-as-preferred-over-send marks lines 31-34. §the-named-message-reply-preferred-over-fresh-send names the tier-3 meta-pattern. The primer formatting includes a priority marker. §the-named-PREFERRED-marker-on-recommended-tools marks the "(PREFERRED)" parenthetical at lines 32 and 62. §the-named-PREFERRED-as-priority-marker-within-tool-pair names the tier-3 meta-pattern. Defensive tool ordering is explicit. §the-named-inspect-before-evaluate-on-unfamiliar marks lines 50-53: "IMPORTANT: Always call `inspect()` before using `evaluate()` on an unfamiliar capability... Do NOT guess method names or argument shapes — read the help text first." §the-named-defensive-tool-ordering names the tier-3 meta-pattern. The locate tool has a defensive prerequisite. §the-named-locate-defensive-prerequisite marks lines 43-45: "IMPORTANT: Only call `locate()` with names you know exist. Call `list()` first to see your pet names." §the-named-list-before-locate-as-defense-against-unknown-names names the tier-3 meta-pattern. The uppercase IMPORTANT marker is used as priority signal. §the-named-IMPORTANT-uppercase-as-LLM-priority-signal marks lines 43, 50, 68. §the-named-uppercase-marker-as-priority-signal names the tier-3 meta-pattern; the primer's formatting includes affordances for LLM attention allocation. The inbox is bidirectional. §the-named-listMessages-includes-sent-and-received marks lines 20-23: "List inbox messages (includes BOTH sent and received messages)." §the-named-inbox-as-bidirectional-journal-not-incoming-queue names the tier-3 meta-pattern; the inbox is the message JOURNAL, not the received-only queue. The threading shape is consistent. §the-named-messageId-and-replyTo-for-threading marks lines 21-23: messageId is unique identifier; replyTo is the messageId of the parent message. Same shape as cycle 402's TranscriptNode (messageId + parentMessageId). §the-named-message-threading-shape-shared-between-inbox-and-transcript names the tier-3 meta-pattern; the threading pattern is consistent between the inbox messages and the transcript structure — a structural alignment cycle 402 noted in the types and cycle 407 finds in the LLM-facing protocol. Shortcut tools avoid code evaluation. §the-named-readText-and-writeText-as-shortcuts marks lines 54-57. §the-named-shortcut-tools-for-common-capability-operations names the tier-3 meta-pattern. The naming-drift framing recurs. §the-named-inspect-vs-inspectCapability-naming-drift confirms cycle 405's finding. §the-named-locate-vs-identify-distinct-methods marks an even more interesting distinction: the primer's `locate` returns an `endo://...` URL (line 38-39); LAL-ARCHITECTURE.md's `identify` returns a raw formula ID. Different methods, different return shapes — and the primer doesn't expose `identify` to the LLM at all. The LLM gets the URL-shaped result, not the raw ID. §the-named-locate-for-URL-identify-for-raw-ID names the tier-3 meta-pattern; URL vs raw-ID is a designed boundary for LLM consumption — the LLM works with URLs rather than internal identifiers. The primer omits four guest-power methods. §the-named-primer-omits-form-storeValue-followMessages-identify-from-LLM-tools cross-references cycle 404's mock-powers.js (22 methods). The primer's 15 tools OMIT storeValue, followMessages, form, and identify. This confirms cycle 404's §the-named-LLM-exposed-tool-surface-as-subset-of-guest-power-surface framing — and now we have NUMERICAL confirmation: 22 mock methods, 15 LLM-exposed tools, ~7 methods agent-internal-only. The LLM gets a FILTERED view of the guest power surface. The second-person addressing recurs. §the-named-second-person-document-addressing-LLM confirms cycle 405's framing; the entire primer addresses the LLM in second person ("your capabilities", "your directory", "your @self locator"). The cycle's session-level observation continues. §the-named-fifty-five-conformant-cycles-and-counting marks the session-level observation: fifty-fifth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-407). §ninety-seven-cycles-with-named-pivot-domain-stay (310-407). Nine citation arcs close on this cycle. Cycle 406 (1, adjacent forward; tool-name-drift visible in JSDoc continues here at tool-catalog level). Cycle 402 (5, MAJOR realignment — vestigial-types framing REVISED; types are current implementation of PREFERRED define mechanism). Cycle 405 (3, primer-tool-name-drift confirmed with inspect vs inspectCapability). Cycle 404 (3, LLM-tool-surface as subset of guest-power-surface now NUMERICALLY confirmed: 15 vs 22). Cycle 401 (3, design doc's "no proposal/grant workflow" claim is WRONG given define() is PREFERRED). Cycle 400 (3, four-tool-count framing extends three-model-string framing). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 364 (4, shapes count keeps growing). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-FORTY (531 + 9 net new). The library has crossed 919 sections; the post-refactor era continues at fifty-five consecutive conformant cycles; the cluster's discipline now includes the practice of REVISING prior framings when new evidence overturns them. Cycle 407's most important contribution may not be the new framings it introduces but the prior framing it corrects — the cluster's accumulated knowledge is not just a record of what was found but a record of what was BELIEVED, and beliefs can be revised.
