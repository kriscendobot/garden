---
source: endo-but-for-bots--packages-lal-agent-types-d-ts
cycle: 402
lane: chat
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: typescript-type-definitions
shape_subtype: type-shape-vs-design-decision-drift
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 50
session_milestone: fifty-conformant-cycles
---

The 127-line agent.types.d.ts is the TypeScript type-definition file for @endo/lal — the fourth lal-package artifact in the cluster after cycle 399's README, cycle 400's source, and cycle 401's design doc. Cycle 402's most structurally interesting move surfaces an INVERSION of cycle 401's drift directionality. §the-named-vestigial-types-from-abandoned-design names the observation. Lines 81-96 still define two types: PendingProposal (lines 81-88) carries proposalId, source, codeNames, edgeNames, workerName, and a `promise: Promise<unknown>` field; ProposalNotification (lines 90-96) carries `status: 'granted' | 'rejected'`, proposalId, source, result, and error. But cycle 401's LAL-ARCHITECTURE.md (lines 41 and 233-235) explicitly states: "No proposal/grant workflow is needed" and "There is no proposal/grant workflow — the evaluate tool call blocks until the code finishes." The design moved on; the types remained. The types file LAGS BEHIND the design decision. §the-named-types-lag-design-decision names the tier-3 meta-pattern. The directionality matters. §the-named-drift-direction-can-invert-between-document-pairs names a refinement of the cluster's drift vocabulary. Cycle 401's finding: documents (README + design doc) LAG the source on the Anthropic default model. Cycle 402's finding: types LAG the design decision on proposal/grant workflow. The drift direction INVERTS — in one pair documents trail code, in another pair code trails documents. §the-named-bidirectional-document-code-drift names the tier-3 meta-pattern. The cluster's vocabulary for drift must now distinguish KIND and DIRECTION. The TranscriptNode definition adds another framing. §the-named-richer-types-than-design-doc-describes marks lines 118-127. TranscriptNode is a linked-chain transcript where each node holds a messageId, parentMessageId (null at root), the messages appended at that step, and an optional lastInboxNumber. The structure forms a TREE. Cycle 401's LAL-ARCHITECTURE.md (line 340) said the transcript is "an in-memory array that grows indefinitely" — a flat array. The types define a more sophisticated linked-chain TREE structure. §the-named-types-richer-than-design-doc and §the-named-tree-rooted-at-null-parent name the tier-3 meta-patterns. Either the types are aspirational (planned future structure) or the design doc undercounts the actual implementation. The WorkerConfig type extends the drift framing. §the-named-WorkerConfig-for-form-submission-flow-not-in-architecture-doc marks lines 99-104. WorkerConfig is "Configuration for a worker spawned from a form submission" — but cycle 401's LAL-ARCHITECTURE.md describes Lal as a single-agent message-following loop, and the form-submission flow is mentioned in the lal/CLAUDE.md (line 5) but NOT in the architecture document. The .d.ts knows about a feature the .md doesn't document. §the-named-types-document-features-architecture-doc-omits names the tier-3 meta-pattern; an inversion of the cycle 360 README-undercounts-implementation pattern — here the ARCHITECTURE doc undercounts the implementation that the types document. The flat-union shape of ToolCallArgs is named. §the-named-ToolCallArgs-as-flat-union-of-all-tool-arguments marks lines 56-76. A single record type with 19 optional fields, every possible argument across all 18 tools. No discriminated union per tool kind. §the-named-flat-optional-union-as-tool-argument-shape names the tier-3 meta-pattern; sibling to cycle 387 AGENTS.md branded-types discipline at a simpler level (flat optional record rather than tagged union). The optionality on ToolCall.id aligns with cycle 401's synthetic-ID observation. §the-named-id-optional-on-ToolCall marks line 36's `id?: string`. The TypeScript schema accommodates the upstream LLM behavior — Ollama doesn't issue tool call IDs, so the field must be optional. §the-named-type-shape-accommodates-protocol-divergence names the tier-3 meta-pattern; sibling to cycle 401's synthetic-tool-call-IDs-for-Ollama. The same optionality applies on ChatMessage. §the-named-tool-call-id-optional-on-ChatMessage marks line 47's `tool_call_id?: string`. The optionality is consistent across the schema where Ollama-style synthesis is needed. The role discrimination shape recurs. §the-named-ChatMessage-role-as-string-literal-union marks line 44's `role: 'system' | 'user' | 'assistant' | 'tool'`. Sibling to cycle 400's literal-union-as-enum-shape and cycle 378's mode-selector shape. The Tool is discriminated by type. §the-named-Tool-as-discriminated-by-type-function marks lines 30-33. The `type: 'function'` literal is the discriminator; the schema leaves room for other future type values without committing to them now. §the-named-discriminator-as-extension-point names the tier-3 meta-pattern. The tool parameters mirror JSON Schema. §the-named-Tool-Parameters-as-nested-typed-record marks lines 11-22. ToolParameterProperty has type + description + items + oneOf. The oneOf field supports OpenAI's anyOf/oneOf type variants in JSON Schema. ToolParameters has type: 'object' + properties: Record + required: string[]. §the-named-JSON-schema-subset-as-tool-parameter-format names the tier-3 meta-pattern; reflects OpenAI's JSON-Schema-subset tool parameter format. The aliases confirm the design doc. §the-named-InboxMessage-as-alias-for-StampedMessage marks line 78; §the-named-GuestPowers-as-alias-for-EndoGuest marks line 79. The design doc names both aliases; the types file confirms. §the-named-type-alias-as-renaming-for-clarity names the tier-3 meta-pattern. The vestigial types carry interesting shape vocabulary. §the-named-PendingProposal-with-promise-field marks lines 81-88: the promise field suggests the proposal mechanism was async and held a pending Promise. §the-named-async-grant-workflow-as-vestigial-shape names the tier-3 meta-pattern. §the-named-ProposalNotification-status-granted-or-rejected marks line 91's `status: 'granted' | 'rejected'`. The vestigial type carries the vocabulary of an explicit grant/reject decision — a more elaborate workflow than what the current design uses. The LalContext shape is novel. §the-named-LalContext-with-whenCancelled-and-cancelled marks lines 107-110. TWO cancellation fields, both optional. Either or both can be present. Suggests two parallel cancellation idioms in the daemon. §the-named-dual-cancellation-shape names the tier-3 meta-pattern. The cycle is a session-level milestone. §the-named-fifty-conformant-cycles-and-counting names the session-level observation: fiftieth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-402). §ninety-two-cycles-with-named-pivot-domain-stay extends. The cluster's vocabulary for document-vs-code drift has accumulated SEVEN named directions across the session — ABSTRACTING (cycle 357→358) + UNDERCOUNTING (cycles 359→360, 399→400) + WHAT-VS-HOW (cycle 361→362) + ANALOGY-VS-LITERAL (cycle 363→364) + ONE-DOCUMENT-VS-SOURCE (cycle 399→400) + TWO-DOCUMENTS-AGREE-AGAINST-SOURCE (cycle 400→401) + INVERTED-DIRECTION-CODE-LAGS-DOCUMENT (cycle 401→402, the newest direction; types lag the design decision rather than documents lagging the source). Nine citation arcs close on this cycle. Cycle 401 (1, adjacent forward; this is the types file that lags behind cycle 401's design decision). Cycle 400 (2, source-vs-document framing extends to types-vs-design-decision). Cycle 399 (3, the README is the document tier that cycle 401 named). Cycle 387 (3, branded-types discipline at simpler level — flat optional union rather than tagged branded union). Cycle 378 (2, string-literal-union sibling for ChatMessage role). Cycle 346 (3, name-aliasing-for-domain-vocabulary sibling for InboxMessage + GuestPowers aliases). Cycle 364 (4, four-shapes-of-design-vs-implementation-arc — now seven-shapes-and-counting). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-NINETY-EIGHT (489 + 9 net new). The cluster has crossed 914 sections; the post-refactor era has crossed the fifty-cycle mark — a session-level milestone in its own right; the citation-arc-closures-in-pivot is now within sight of 500. The cluster's drift vocabulary now recognizes drift KIND and drift DIRECTION as separate axes; the bidirectional framing makes the cluster's interconnection more precise.
