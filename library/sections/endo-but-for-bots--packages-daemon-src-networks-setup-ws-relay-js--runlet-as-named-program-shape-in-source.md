---
source: endo-but-for-bots--packages-daemon-src-networks-setup-ws-relay-js
cycle: 392
lane: chat
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: daemon
cluster: pivot
shape: runlet-instance-in-source
shape_subtype: network-service-installer
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 40
---

The 46-line `packages/daemon/src/networks/setup-ws-relay.js` is a concrete instance of the runlet shape cycle 391's daemon-lore named. Reading it answers what a runlet actually looks like in source. The single most structurally interesting move is the direct correspondence between the lore vocabulary and the source structure. §the-named-runlet-as-named-program-shape-in-source names the move. The file exports `main(powers)`, returns a result string, and is transient (not intended to exceed the life of main). The lore vocabulary maps directly to the source shape. §the-named-lore-vocabulary-realized-in-source names the tier-3 meta-pattern; cycle 391 named the abstract shape; cycle 392 reveals the concrete instance. The runlet's purpose is named in its header comment. §the-named-Usage-comment-as-self-documenting-command marks lines 2-3: "Usage (installs the ws-relay network caplet at NETS/ws-relay): endo run --UNCONFINED packages/daemon/src/networks/setup-ws-relay.js --powers @agent". The file's first comment IS its operating manual. §the-named-usage-as-header-comment names the tier-3 meta-pattern. The authority requirement is named with its reason. §the-named-requires-powers-agent-explicit-comment marks line 5: "Requires --powers @agent because the script calls makeUnconfined()." The script declares its own authority requirement and the reason. §the-named-authority-requirement-with-reason names the tier-3 meta-pattern; sibling shape to cycle 357's for-expedience-as-honest-acknowledgment — both surface a constraint with explicit reasoning. The header guides customization. §the-named-edit-defaults-below-comment marks line 6: "Edit the defaults below to target a different relay server." The header tells the reader where to customize. §the-named-customization-pointer-in-header names the tier-3 meta-pattern. The runlet-caplet relationship is named explicitly. §the-named-runlet-installs-caplet marks line 2's "ws-relay network caplet" plus line 3's `endo run`: a runlet installs a caplet. The runlet has main() that bootstraps the daemon's caplet registry. §the-named-runlet-as-caplet-installer names the tier-3 meta-pattern. The env-var default pattern is named. §the-named-env-var-with-or-default-pattern marks lines 30-31's `env.WS_RELAY_URL || 'wss://endo-relay.fly.dev'`. JavaScript's `||` short-circuit provides default fallback. §the-named-env-var-with-or-fallback names the tier-3 meta-pattern. The public default service is acknowledged in source. §the-named-public-default-relay-wss-endo-relay-fly-dev marks the default `wss://endo-relay.fly.dev` — a public Fly.io-hosted service. §the-named-public-default-service-acknowledged-in-source names the tier-3 meta-pattern. The persistence claim is named in JSDoc. §the-named-formula-env-persists-across-reincarnation marks lines 19-21's "The resolved values are persisted in the formula env so they survive reincarnation without a pet-store lookup at boot time." §the-named-formula-env-as-persistent-config names the tier-3 meta-pattern; the runlet's resolved env values become part of the formula's persisted state. The install operation is named explicitly. §the-named-makeUnconfined-as-network-service-installer marks lines 33-40: `E(powers).makeUnconfined(undefined, wsRelaySpecifier, { powersName: '@agent', resultName: 'network-service-ws-relay', env: { ... } })`. The call creates the network service formula. §the-named-makeUnconfined-as-formula-factory names the tier-3 meta-pattern. The post-install move is structurally meaningful. §the-named-move-from-arbitrary-name-to-nets-namespace marks line 42: `await E(powers).move(['network-service-ws-relay'], ['@nets', 'ws-relay']);`. After install, the formula is relocated from the arbitrary install name to the canonical namespace path. §the-named-install-then-move-to-canonical-path names the tier-3 meta-pattern; the install creates a formula under any name, then a move places it at the well-known address. The namespace prefix is named. §the-named-NETS-prefix-as-network-namespace marks `@nets` as the daemon's network-service namespace. §the-named-at-prefixed-namespaces-in-daemon-registry names the tier-3 meta-pattern; the daemon has multiple @-prefixed namespaces accessible via path syntax. The result-string convention is named. §the-named-result-string-as-success-confirmation marks line 44's return string. Cycle 391 lore said runlets are "not expected to return anything"; this runlet does return a string. §the-named-runlet-may-return-message-anyway names the tier-3 meta-pattern; the lore is loose, not strict. The harden discipline applies. §the-named-harden-main-as-export-discipline marks line 46's `harden(main);`. Every named export gets a harden() call per the bot-fork CLAUDE.md. §the-named-harden-after-named-export names the tier-3 meta-pattern. Two import-related observations: §the-named-import-from-eventual-send-for-E marks line 8's `import { E } from '@endo/eventual-send';`. §the-named-JSDoc-ERef-type-import marks line 10's `/** @import { ERef } from '@endo/eventual-send' */` — the JSDoc @import syntax from cycle 376 and cycle 387 AGENTS.md applied in concrete source. Seven citation arcs close on this cycle. Cycle 391 was the prior designs-lane daemon-lore; cycle 392 source realizes the runlet vocabulary in concrete code; lore-vocabulary-realized-in-source new framing (arc 1). Cycle 369's @endo/daemon README named the daemon as application runner; cycle 392 shows the runlet installs a network service formula (arc 4). Cycle 385's @endo/chat README named CapTP-over-WebSocket; the ws-relay network module presumably provides the WebSocket transport that powers chat (arc 2). Cycle 367's exo powers pattern is the parameter shape used by makeUnconfined (arc 12). Cycle 321's @endo/eventual-send E() is the call mechanism (arc 12). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 66). Cycle 322's @endo/errors not directly invoked in this runlet (arc 66). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-TWENTY-THREE (416 + 7 net new). §eighty-two-cycles-with-named-pivot-domain-stay (310-392). §fortieth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty consecutive cycles (353-392). §forty-conformant-milestone as session-level observation: the post-refactor discipline now spans forty consecutive cycles, demonstrating sustained adherence to the single-body section convention.
