---
source: endo-but-for-bots--packages-patterns-types-index-d-ts
cycle: 388
lane: chat
ingested: 2026-06-18
package: patterns
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: canonical-types-index-d-ts
shape_subtype: pure-re-export-with-pointer-to-AGENTS-md
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 36
---

The 9-line `packages/patterns/types-index.d.ts` is the canonical example of AGENTS.md's types-index convention named in cycle 387. The single most structurally interesting move is the comparison that emerges from looking at TWO concrete instances of the convention in the same monorepo. §the-named-canonical-and-violating-instances-of-types-index-convention names the move. patterns/types-index.d.ts is 9 lines of pure re-exports with a self-documenting header pointing to AGENTS.md. exo/types-index.d.ts is 176 lines containing 6 `export declare function` declarations (overloaded signatures for makeExo + defineExoClass + defineExoClassKit) plus imports plus extensive JSDoc — which AGENTS.md line 48 says should live in `.ts` files for tsc checking, not `.d.ts` where they silently pass. The convention is documented in cycle 387's AGENTS.md but not uniformly applied. §the-named-uniform-convention-unevenly-applied names the tier-3 meta-pattern. The file's self-documenting header is structurally interesting. §the-named-comment-naming-convention-and-pointing-to-source marks lines 1-2: "Pure re-export index — type definitions belong in .ts files. See AGENTS.md for the types-index convention." The file's own header DOCUMENTS its purpose AND pulls the reader to AGENTS.md. §the-named-self-documenting-header-with-pointer-to-authority names the tier-3 meta-pattern; the file knows what it is AND tells the reader where the rule lives. The TypeScript syntax used is worth naming. §the-named-export-type-star-from-as-type-only-star-re-export marks lines 3-4's use of `export type * from` (TypeScript 5.0+ syntax for type-only star re-export). Two source files: `src/types.js` (canonical types) and `src/type-from-pattern.js` (computed types). The syntax cleanly separates type-only re-exports from runtime re-exports. §the-named-export-type-star-as-bulk-type-re-export names the tier-3 meta-pattern. The cycle reveals a three-file dance. §the-named-three-file-dance-for-value-namespace-merge marks lines 6-9 explaining the THREE files involved in re-exporting M + matches + mustMatch. `src/type-from-pattern.ts` defines the value+namespace merge (TypeScript requires both in one module per AGENTS.md). `types-index.d.ts` re-exports the TYPES (line 9 `export { M, matches, mustMatch }` is TypeScript-only because it's in .d.ts; the JavaScript runtime doesn't see this file). `types-index.js` re-exports the RUNTIME (a sibling file that imports from patternMatchers.js). §the-named-three-file-dance-for-typed-runtime-export names the tier-3 meta-pattern; types and runtime are decoupled but coordinated via shared name surface. The forward pointer is named. §the-named-AGENTS-md-convention-cited-in-source-comment marks line 2's explicit citation of AGENTS.md as the authority. §the-named-source-to-doc-pointer-as-convention-reinforcement names the tier-3 meta-pattern; the rule lives in AGENTS.md, but the source code at the rule-application site has a comment saying "see AGENTS.md for the rule." A future developer touching the file gets pointed to the convention they should follow. The file's compactness deserves observation. §the-named-nine-line-as-AGENTS-conformant-shape marks patterns/types-index.d.ts at 9 lines: header comment + two `export type * from` lines + body comment + one `export { ... }` line. The convention's canonical realization is necessarily small. §the-named-canonical-convention-realization-is-small names the tier-3 meta-pattern; the convention's rule (no type definitions in .d.ts) means a conformant .d.ts file has nowhere to grow LARGE except by adding more re-export lines for more source files. The violation is structurally meaningful. §the-named-exo-types-index-d-ts-violates-AGENTS-md-convention marks the violating instance at 176 lines with 6 `export declare function` declarations. AGENTS.md line 48 specifies `declare function` overrides belong in `.ts` files for tsc checking; in `.d.ts` they silently pass. The exo package puts them in the .d.ts file. The reason could be (1) historical, predating the AGENTS convention; (2) deliberate, choosing silent-pass over checking trade-off; (3) oversight, pending migration. The librarian cannot tell which from the source alone. §the-named-convention-violation-may-be-historical-or-deliberate-or-pending names the tier-3 meta-pattern. The bidirectional coupling deserves its own framing. §the-named-AGENTS-md-and-source-converge-via-pointer marks the structure: cycle 387 AGENTS.md describes the types-index convention (one direction: doc-to-rule); cycle 388 patterns/types-index.d.ts applies the convention AND cites AGENTS.md (the other direction: source-to-doc). The two artifacts reinforce each other. §the-named-bidirectional-doc-source-coupling names the tier-3 meta-pattern; documents reference source examples and source files reference document conventions, creating a graph of references. The librarian and the developer can navigate from either direction. Seven citation arcs close on this cycle. Cycle 387 was the prior designs-lane AGENTS.md; cycle 388 finds both a canonical example AND a violation instance, opening the uniform-convention-unevenly-applied new framing (arc 1). Cycle 386's documentation-trails-code framing extends here: the documentation describes a convention; code BOTH follows AND violates it; doc and code drift in TWO directions within ONE convention (arc 1). Cycle 384's design-doc-trails-code framing finds yet another direction: source-violates-doc is a fifth-shape complement to the four-shapes-of-design-vs-implementation-arc and the dissolves-the-distinction case (arc 1). Cycle 367's @endo/exo this-context triple; cycle 387 named the 3×3 matrix; cycle 388 finds that the exo package's types-index VIOLATES the convention AROUND those same exo APIs (arc 9). Cycle 327's @endo/patterns README; cycle 388 reveals patterns is the canonical-application example of the types-index convention (arc 8). Cycle 326's pure-naming-as-discipline reaches one more sibling; the convention is pure naming applied to file extensions (arc 62). Cycle 322's @endo/errors is not directly invoked (arc 62). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-NINETY-FIVE (388 + 7 net new). §seventy-eight-cycles-with-named-pivot-domain-stay (310-388). §thirty-sixth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-six consecutive cycles (353-388).
