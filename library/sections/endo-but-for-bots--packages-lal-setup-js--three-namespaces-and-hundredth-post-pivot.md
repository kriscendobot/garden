---
source: endo-but-for-bots--packages-lal-setup-js
cycle: 410
lane: chat
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: provisioning-entry-point
shape_subtype: three-namespace-config-flow
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 58
session_milestone: one-hundredth-post-pivot-cycle
---

The 105-line setup.js is the provisioning entry point that launches the @endo/lal agent inside the Endo daemon — the genesis of every running lal agent. Twelfth lal-package artifact in the cluster (after the README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, the simulator README, mock-powers.js, primer/README.md, providers/index.js, primer/tools.md, providers/anthropic.js, and primer/howto-code.md). Cycle 410 is the ONE-HUNDREDTH consecutive non-garden source ingest after the pivot (310-410) — a session-level milestone. The cycle's most structurally interesting move surfaces a three-tier naming asymmetry at the config flow's heart. §the-named-three-env-var-namespaces-for-same-config names the observation. The lal configuration flows through THREE distinct naming systems at THREE layers of the setup architecture: (1) OUTER — maintainer-facing: ENDO_LLM_HOST, ENDO_LLM_MODEL, ENDO_LLM_AUTH_TOKEN, ENDO_LLM_NAME (lines 65, 67-70). The setup script reads these from process.env. (2) MIDDLE — form-submission inter-component: `{ name, host, model, authToken }` (line 98). The setup script submits these to the form. No "ENDO_LLM_" or "LAL_" prefix; just the field names. (3) INNER — agent-internal: LAL_HOST, LAL_MODEL, LAL_AUTH_TOKEN, LAL_MAX_TOKENS, LAL_MAX_MESSAGES (cycles 400 + 406). The provider factory reads these from its own env parameter. §the-named-three-namespaces-for-one-config-flow names the tier-3 meta-pattern. Three setup layers; three prefix conventions; the same conceptual config. The structural parallel to cycle 405 is named. §the-named-three-namespaces-as-parallel-to-three-surfaces names the deeper structure. Cycle 405 named three SURFACES for three AUDIENCES (LLM tool catalog ⊆ guest-power method surface ⊆ user-facing Chat slash commands). Cycle 410 names three NAMESPACES for three LAYERS of the same CONFIG. Layered systems routinely have one naming convention per layer; the parallel is structural, not coincidental. §the-named-per-layer-naming-convention names the tier-3 meta-pattern. The fourth document-drift faction is now visible. §the-named-fourth-document-drift-faction-confirmed marks line 7's example: `ENDO_LLM_MODEL=claude-sonnet-4-6-20250514`. This AGREES with cycle 400's source default in providers/config.js (line 31). The Anthropic-default model strings across the lal-package documents now fall into THREE FACTIONS: Faction A (sonnet-4-6) — providers/config.js source + setup.js example agree; Faction B (opus-4-5) — README + LAL-ARCHITECTURE.md agree; Faction C (3-5-sonnet-20241022) — simulator README stands alone. §the-named-document-drift-factions names the tier-3 meta-pattern. Refines cycle 403's three-model-strings framing: not just three strings but three FACTIONS of mutually-agreeing documents. The document tier has stratified into factions, each frozen at a different historical moment. The canonical introducedNames idiom recurs. §the-named-introducedNames-with-at-agent-pointing-to-host-agent marks line 53: `introducedNames: harden({ '@agent': 'host-agent' })`. §the-named-canonical-introducedNames-idiom names the tier-3 meta-pattern. The makeUnconfined daemon API equals the CLI flag. §the-named-makeUnconfined-as-daemon-API-equivalent-to-CLI-flag marks line 58: `await E(agent).makeUnconfined('@main', lalSpecifier, { ... })`. UNCONFINED is available both via the CLI `--UNCONFINED` flag (cycle 409) AND via the daemon's makeUnconfined method. §the-named-privilege-mode-via-CLI-flag-or-daemon-method names the tier-3 meta-pattern; two surfaces for the same privilege escalation. Idempotency is checked. §the-named-idempotent-provisioning-via-has-check marks lines 45-48: `if (await E(agent).has('controller-for-lal')) { return; }`. §the-named-existence-check-as-idempotency-guard names the tier-3 meta-pattern. The provisioning has three steps. §the-named-three-step-provisioning-flow marks the structure: (1) provideGuest 'setup-lal' if not present (lines 52-56); (2) makeUnconfined to launch agent.js (lines 58-61); (3) optionally submit form with env-derived config (lines 84-102). §the-named-provision-launch-configure names the tier-3 meta-pattern. The submission decision has three branches. §the-named-explicit-config-vs-reachability-probe-as-decision marks lines 72-82. The setup decides whether to auto-submit based on: explicit env-var ANY-of-three-set OR Ollama reachability probe. Three branches: explicit-config → submit; no-config-and-reachable → submit defaults; no-config-and-unreachable → leave for manual. §the-named-three-branch-config-decision names the tier-3 meta-pattern. The reachability probe uses a specific endpoint. §the-named-isOllamaReachable-probe-via-fetch-v1-models marks lines 21-32. The probe fetches `${host}/v1/models` with a 3-second AbortSignal timeout. The /v1/models endpoint is lightweight specifically for liveness check. §the-named-lightweight-endpoint-as-liveness-probe names the tier-3 meta-pattern. The inbox-watch pattern is canonical. §the-named-followMessages-with-filter-and-reverseLocate marks lines 89-102: followMessages + type filter + sender check via reverseLocate. §the-named-canonical-inbox-watch-pattern names the tier-3 meta-pattern. The reverseLocate pairs with locate. §the-named-reverseLocate-as-inverse-of-locate marks line 94: reverseLocate takes a locator URL and returns a pet-name array. Cycle 407 named locate as the URL-returning function. §the-named-locate-and-reverseLocate-as-inverse-pair names the tier-3 meta-pattern. The dummy auth token recurs. §the-named-ollama-as-dummy-auth-token-recurs marks line 69. SAME pattern as cycle 406's providers/index.js. The literal string 'ollama' appears as a dummy auth token in TWO files. §the-named-ollama-string-as-dummy-auth-across-files names the tier-3 meta-pattern. The Ollama default is consistent across files. §the-named-qwen3-as-default-model-consistent-across-files marks line 68's `model = env.ENDO_LLM_MODEL || 'qwen3'`. Cycle 400's config.js had `defaultModels.ollama = 'qwen3'`. Setup.js and config.js AGREE on qwen3. §the-named-ollama-default-stable-vs-anthropic-default-fragmented names the tier-3 meta-pattern; the Ollama default is consistent across files while the Anthropic default fragments into three factions. The harden discipline is partial. §the-named-harden-on-main-export marks line 104: `harden(main)`. Setup.js DOES follow the harden-every-export convention. Cycle 406's providers/index.js and cycle 408's providers/anthropic.js do NOT. §the-named-harden-discipline-inconsistent-across-package confirms cycle 406's framing with another data point. The file opens with a CLI invocation comment. §the-named-setup-as-CLI-invocation-via-comment marks line 4. §the-named-CLI-invocation-as-file-header-comment names the tier-3 meta-pattern. The lint rule is silenced for the await-in-loop. §the-named-no-await-in-loop-eslint-disable marks lines 93 and 97. §the-named-sequential-await-in-loop-as-intentional names the tier-3 meta-pattern. The module URL anchors agent.js resolution. §the-named-lalSpecifier-as-resolved-URL marks line 13. §the-named-import-meta-url-as-module-anchor names the tier-3 meta-pattern. The agent gets config via form, not via env. §the-named-config-via-form-not-env-after-provisioning marks the absence of an env parameter to makeUnconfined (lines 58-61). The agent reads config from the form submission. Per the inner CLAUDE.md: "LLM provider configuration (host, model, auth token) is received via forms at runtime, not environment variables." §the-named-form-as-runtime-config-channel names the tier-3 meta-pattern. The cycle is a session-level milestone. §the-named-cycle-410-as-one-hundredth-post-pivot names the structural fact: cycle 410 is the ONE-HUNDREDTH consecutive non-garden source after the pivot (cycles 310 through 410, inclusive). §the-named-one-hundred-post-pivot-cycles-reached names the session-level observation. The cluster has sustained domain-stay across one hundred consecutive cycles — a discipline that has accumulated 569+ citation arcs, 922 sections, 58 conformant cycles, and a refined drift vocabulary spanning multiple zoom levels and directions. §the-named-fifty-eight-conformant-cycles-and-counting marks the post-refactor authoring discipline session-level observation. Ten citation arcs close on this cycle. Cycle 409 (1, adjacent forward; the define-endow attenuation pattern this agent will use is launched by setup.js). Cycle 408 (3, providers/anthropic.js dispatched eventually from form-config submitted here). Cycle 407 (3, primer-tool catalog the agent uses is loaded after setup.js launches it). Cycle 405 (5, three-namespaces parallels three-surfaces structural framing — major framing-parallel). Cycle 403 (5, three-Claude-model-strings now refined to three-factions with setup.js agreeing with config.js source). Cycle 401 (3, design doc described form-based config — setup.js confirms). Cycle 400 (3, qwen3-as-ollama-default consistent between config.js and setup.js; Anthropic default disagrees). Cycle 406 (3, harden-discipline-inconsistent-across-package confirmed — setup.js follows, providers/ don't). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-SIXTY-NINE (559 + 10 net new). The library has crossed 922 sections; the post-refactor era continues at fifty-eight consecutive conformant cycles; the cluster has crossed the ONE-HUNDREDTH POST-PIVOT CYCLE — a milestone that marks one hundred consecutive ingests within a coherent domain. The cluster's vocabulary at the hundredth-cycle milestone now spans: drift directions (eight named + a fractal-zoom-level framing), surface tiers (three: LLM tool ⊆ guest power ⊆ user-facing), configuration namespaces (three: outer ENDO_LLM_* + middle form-fields + inner LAL_*), drift factions (three: sonnet-4-6 + opus-4-5 + 3-5-sonnet), and design intents (capability attenuation as named purpose of the define-endow flow). The hundredth cycle is structurally fitting because setup.js itself is the genesis point that ties every other artifact together: it provisions the guest, launches the agent that uses the tool catalog, dispatches to the provider that translates to the LLM API, and submits the form whose namespace differs from both the outer env and the inner agent env. Every cluster framing the cycle 310-410 corpus has accumulated meets in this one file's structure.
