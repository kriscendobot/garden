---
source: endo-but-for-bots--packages-lal-primer-smallcaps-md
cycle: 449
lane: designs
ingested: 2026-06-22
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: agent-facing-encoding-reference
shape_subtype: filtered-subset-of-full-smallcaps-vocabulary
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 97
---

The 25-line primer/smallcaps.md is the LLM-facing specification for SmallCaps encoding of tool call arguments inside @endo/lal, and the eleventh lal-package artifact in the cluster (after the README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, the simulator README, mock-powers.js, primer/README.md, providers/index.js, primer/tools.md, and primer/capabilities.md via howto-code.md). Cycle 449's single most structurally interesting move: §the-named-escape-set-covers-all-seven-prefixes-including-undisclosed-three names the observation. The primer's encoding vocabulary explicitly explains only five of SmallCaps' seven active special characters — BigInt (`+N`, `-N`), four manifest constants (`#undefined`, `#Infinity`, `#-Infinity`, `#NaN`), and the escape prefix (`!`). But the escape table (lines 20-24) lists SEVEN characters requiring the `!`-escape: `!, #, $, %, &, +, -`. The `$` (remotable), `%` (symbol), and `&` (promise) prefixes are never explained in the primer, yet they appear in the escape rule. §the-named-escape-set-covers-all-seven-prefixes-including-undisclosed-three as tier-3 meta-pattern: the escape rule is complete across all seven active SmallCaps characters; the encoding vocabulary description is filtered to the five types the LLM would encounter as direct argument values. The same subset-filter pattern found at the tool catalog level (15 of 22 methods exposed in tools.md, cycle 407) now appears at the encoding layer: the LLM sees the subset of the encoding vocabulary it needs for its role. §the-named-primer-smallcaps-as-filtered-subset-of-full-smallcaps-vocabulary names the tier-3 meta-pattern; the filtering-for-LLM-role discipline runs from the method surface down to the wire-format spec. §the-named-escape-hatch-discloses-undisclosed-types names a second structural observation: a reader who understands SmallCaps can infer from the escape table that `$`, `%`, and `&` must encode something meaningful (remotables, symbols, promises), even though the primer never says so. The escape rule is a window into the full vocabulary via its negative space. §the-named-negative-space-reveals-full-alphabet names the tier-3 meta-pattern; the silence about three prefix types teaches through what is required to avoid rather than what is defined. §the-named-bigint-as-primary-smallcaps-use-in-lal marks the observation that BigInt messageNumber is the dominant smallcaps use case for the LLM (lines 7-8: `"+N"` or `"-N"` for BigInt; lines 29-30 example: `{"messageNumber": "+5"}`). Every tool that takes a messageNumber argument requires SmallCaps BigInt encoding, which is why the primer leads with `+`/`-` before the `#`-prefixed manifest constants. §the-named-bigint-as-dominant-encoding-in-lal-tools names the tier-3 meta-pattern; BigInt prevalence is a structural consequence of the messageNumber-as-BigInt design choice from cycle 402's agent.types.d.ts. §the-named-manifest-constants-cover-four-special-float-values marks lines 10-13: the four `#`-prefixed encodings cover the four JSON-inexpressible numeric or absent values: undefined, Infinity, -Infinity, NaN. The set is exhaustive for the JSON gap. §the-named-four-manifest-constants-as-json-gap-closure names the tier-3 meta-pattern; SmallCaps closes a known JSON limitation (no infinity, no undefined, no NaN) with exactly four named constants — no more, no less. §the-named-escape-prefix-applies-to-any-special-first-character marks lines 16-24: the escape is not specific to `!`-prefixed strings; ANY string whose first character is one of the seven special characters must be escaped by prepending `!`. This is a uniform rule, not a per-type exception. §the-named-uniform-escape-rule-over-entire-special-character-set names the tier-3 meta-pattern. §the-named-most-tool-arguments-regular-json marks lines 25-26: "Most tool arguments are regular JSON values and don't need special encoding." The SmallCaps encoding is the exception, not the rule; the LLM mostly operates in ordinary JSON. §the-named-smallcaps-as-exceptional-case-not-default names the tier-3 meta-pattern; the primer's design philosophy positions SmallCaps as an extension to JSON rather than a replacement. The table structure is structurally compact. §the-named-five-row-encoding-table marks lines 4-14: Type / SmallCaps Format / Example — three columns, five rows, all non-JSON values the LLM might encounter in tool results or need to produce in arguments. The table covers BigInt (two rows for sign), undefined, Infinity, -Infinity, and NaN. §the-named-five-row-table-covers-five-non-json-types names the structural observation. §the-named-ninety-seven-conformant-cycles-and-counting marks the session-level observation: ninety-seventh AUTHORED conformant single-body section doc in post-refactor era (cycles 353-449). §one-hundred-thirty-nine-cycles-with-named-pivot-domain-stay (310-449). Closes seven citation arcs: cycle 448 (1, adjacent forward; ws-relay.js uses SmallCaps encoding for CapTP message framing; smallcaps.md is the LLM-facing spec for the same encoding layer) + cycle 407 (3, primer/tools.md listed tool signatures requiring SmallCaps BigInt messageNumber arguments; smallcaps.md is the wire-format spec that makes those arguments work) + cycle 423 (3, marshal README named the two-encoding-systems: smallcaps string-prefixes vs original qclass objects; smallcaps.md shows the filtered LLM-facing subset of the full seven-prefix vocabulary) + cycle 405 (3, primer-existence-vs-no-dynamic-context-claim raised but unresolved; smallcaps.md confirms the primer corpus as the agent's encoding-reference component) + cycle 402 (5, agent.types.d.ts revealed messageNumber as BigInt in TranscriptNode; smallcaps.md explains the `+N` encoding that delivers BigInt across the tool-call boundary) + cycle 326 (75, pure-naming-as-discipline) + cycle 322 (75, errors framing — smallcaps.md is the wire-format discipline for avoiding encoding errors in tool arguments). Pushes citation-arc-closures-in-pivot to NINE-HUNDRED-AND-TWENTY-EIGHT (921 + 7 net new). The library has crossed 961 sections; the post-refactor era continues at ninety-seven consecutive conformant cycles; the cluster's filtering-for-LLM-role discipline now spans three layers: tool method surface (15 of 22 methods) + encoding vocabulary description (5 of 7 prefixes explained) + escape rule (7 of 7 characters covered). The encode-vocabulary description is filtered; the escape rule is complete.
