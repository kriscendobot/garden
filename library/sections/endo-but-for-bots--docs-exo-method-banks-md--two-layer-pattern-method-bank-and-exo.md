---
source: endo-but-for-bots--docs-exo-method-banks-md
cycle: 389
lane: designs
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: docs
cluster: pivot
shape: design-doc-for-exo-architecture-pattern
shape_subtype: two-layer-method-bank-and-exo
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 37
---

The 135-line `docs/exo-method-banks.md` design document on endo-but-for-bots' `llm` branch explains a daemon-wide architectural pattern that sits structurally below cycle 367's exo APIs, cycle 368's exo taxonomy, and cycle 387's 3×3 this-context matrix. The single most structurally interesting move is the document's central thesis: the daemon uses a TWO-LAYER pattern. The implementation layer is a "method bank" — a plain object whose values are functions. The surface layer is an exo — a hardened remotable created with `makeExo` and validated against an `M.interface` guard. §the-named-two-layer-pattern-method-bank-and-exo names the move; §the-named-implementation-vs-surface-as-two-layers names the tier-3 meta-pattern. The method bank composes internally without guard overhead; the exo enforces the guard at the external boundary. The motivations are named explicitly. §the-named-three-reasons-for-two-layer marks lines 19-27's three reasons: Internal composition (methods call each other without guard overhead), Signature adaptation (the exo can rename or wrap methods), and Method inheritance (higher-level agents destructure methods from lower-level objects). §the-named-three-named-reasons-for-architectural-split names the tier-3 meta-pattern; the document does not just present the pattern, it names why the pattern exists. The renaming capability is structurally significant. §the-named-exo-can-rename-methods marks line 51's example: the exo's `write` is the implementation's `writeLocator`. The surface name can differ from the implementation name. §the-named-public-name-can-differ-from-private-name names the tier-3 meta-pattern; the exo is a translation layer between external naming and internal naming. Method inheritance via destructuring is named with concrete examples. §the-named-method-inheritance-via-destructuring marks lines 63-89 showing Host and Guest agents destructuring methods from Directory and Mailbox method banks. The Host's method bank is composed from the destructured methods plus agent-specific ones. §the-named-method-bank-composition-via-destructuring names the tier-3 meta-pattern. The discipline it enables is named in line 91-92: "This pattern ensures each method is defined once, at the layer that owns the logic, and carried up by reference." §the-named-each-method-defined-once-at-owning-layer marks the rule; §the-named-single-source-of-truth-via-reference names the tier-3 meta-pattern. Single source of truth via reference, not re-implementation. The async-iterator special handling is named. §the-named-makeIteratorRef-wraps-async-iterators-at-exo-boundary marks lines 94-110: async iterator methods require special handling at the exo boundary. The method bank returns a raw async iterator; the exo must wrap it in `makeIteratorRef` to make it passable over `E()`. §the-named-special-wrapping-at-boundary-for-passable-iterators names the tier-3 meta-pattern; the substrate's passable shape requires explicit wrapping of certain return types. The garbage-collection wrapping is named with its layer placement. §the-named-withCollection-wraps-guest-methods-for-GC marks lines 112-134: Guest methods are wrapped with `withCollection` to trigger GC after each call. The wrapping applies to the METHOD BANK functions, not the exo methods. §the-named-method-wrapping-at-bank-level-not-exo-level names the tier-3 meta-pattern; some wrappers belong on the inner layer, others on the outer boundary. The exemption mechanism is named. §the-named-unwrappedMethods-set-for-collection-exemption marks line 122's `unwrappedMethods.has(name) ? fn : withCollection(fn)`. A set of method names is exempt from the collection wrapper. §the-named-exemption-set-for-cross-cutting-concern names the tier-3 meta-pattern; cross-cutting concerns sometimes need targeted exemptions. The JS idiom for wrapping is named. §the-named-Object-fromEntries-Object-entries-pattern-for-wrapping marks lines 119-124's use of `Object.fromEntries(Object.entries(obj).map(...))` — the standard JS idiom for transforming object values. §the-named-object-fromEntries-entries-as-functional-transform names the tier-3 meta-pattern. The override-after-spread pattern is structurally meaningful. §the-named-iterator-method-override-after-spread marks lines 126-134: the exo spreads the wrapped guest methods AND THEN overrides the iterator methods with the iteratorRef-wrapped versions. Order matters: spread first, override second. §the-named-spread-then-override-for-selective-replacement names the tier-3 meta-pattern. The explicit GC trigger is named. §the-named-collectIfDirty-as-named-GC-trigger marks line 131's `await collectIfDirty()` before returning the iteratorRef. Explicit GC trigger named with the dirty-flag pattern. §the-named-explicit-GC-with-dirty-flag names the tier-3 meta-pattern. Seven citation arcs close on this cycle. Cycle 388 was the prior chat-lane patterns/types-index.d.ts; AGENTS.md's types-index convention extends here with the method-bank/exo two-layer pattern (arc 1). Cycle 387's AGENTS.md 3×3 exo this-context matrix gets its underlying architecture named: the two-layer method-bank-and-exo (arc 1). Cycle 368's exo taxonomy "this is how exos compose" gains the method-bank intermediary (arc 10). Cycle 367's exo README three-shapes-of-make-and-define gain the two-layer decomposition below them (arc 10). Cycle 327's @endo/patterns M.interface is the guard the exo wraps with (arc 9). Cycle 321's @endo/eventual-send E() is the call mechanism that makeIteratorRef enables for async iterators (arc 11). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 63). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-TWO (395 + 7 net new). The four-hundred threshold is crossed this cycle. §citation-arc-closures-cross-four-hundred as session-level observation: from cycle 376's 304 closures to cycle 389's 402 closures across thirteen cycles, averaging ~7.5 closures per cycle. The cluster's interconnection continues to thicken. §seventy-nine-cycles-with-named-pivot-domain-stay (310-389). §thirty-seventh-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-seven consecutive cycles (353-389).
