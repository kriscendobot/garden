---
source: endo--packages-benchmark-README-md
cycle: 363
lane: designs
ingested: 2026-06-15
package: benchmark
cluster: pivot
cluster_position: twenty-fourth
shape: private-cross-engine-benchmark-tool
shape_subtype: private-package-as-internal-tooling
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 11
---

The 8-line README for @endo/benchmark is the twenty-fourth package in the pivot cluster, and the entire package is structured by a single design choice that the README mentions almost in passing. The "Pre-reqs" section reads "Run the command `yarn install-engines` to ensure installation of V8 and XS engines in $HOME/.esvu for this package's `eshost`." That sentence is the reveal. The package exists because Endo's substrate must perform acceptably on BOTH V8, the mainstream JavaScript engine, AND XS, Moddable's embedded JavaScript engine, and verifying that requires running JavaScript code on both engines uniformly. The mechanism is eshost (the cross-engine runner) plus esvu (the JavaScript engine version manager). §the-named-cross-engine-substrate-benchmarking-explains-the-package names the single most structurally interesting move; §the-named-V8-and-XS-as-the-two-target-engines names the engine pair Endo cares about most. The eshost tool is what makes the benchmark code uniform across engines; §the-named-eshost-as-cross-engine-runner. The esvu tool is what installs the engines; §the-named-V8-and-XS-engines-via-esvu names the install path. Several other structural features fall out of the same choice. The package.json marks `"private": true`, the version is pre-1.0 (0.1.4), the index.js is empty (0 lines), the implementation is 39 lines in src/benchmark.js, and the README is 8 lines. The shape is internal-only, never-published, no-external-API. §the-named-private-package-as-internal-tooling marks the shape and is a NEW SHAPE in the cluster's tier-3 taxonomy, peer to runtime-substrate-package, data-format-package, control-flow-package, bundling-and-transport-package, discipline-as-package (cycle 359 eslint-plugin), and testing-substrate-bridge-as-package (cycle 361 ses-ava). §the-named-private-true-not-for-npm-publication names the package.json marker that anchors the shape. The package's exports point at an empty index.js, which means the package exposes literally nothing via the package boundary; its only interface is `yarn test`. §the-named-empty-index-js-private-package-pattern names this; §the-named-script-as-interface-not-imports names the tier-3 meta-pattern. The 8-line README also exhibits a structural move worth naming separately. It says "minimalistic ava-like interface to run benchmark tests" instead of defining the interface. The design specification is DELEGATED to an external reference: AVA. The reader is presumed to know AVA, and the package's surface area is "AVA but for benchmarks." §the-named-readme-defines-by-analogy-to-external-package names the move; §the-named-readme-as-pointer-to-external-conventions names the tier-3 meta-pattern. The minimal documentation effort is consistent with the private-package shape: no need for documentation polish when there is no external audience. §the-named-documentation-effort-proportional-to-public-audience names this tier-3 meta-pattern. Three cycles now form a trilogy of development-affordance packages: cycle 359 introduced @endo/eslint-plugin (discipline-as-package); cycle 361 introduced @endo/ses-ava (testing-substrate-bridge-as-package); cycle 363 introduces @endo/benchmark (private-package-as-internal-tooling, specialized to cross-engine substrate benchmarking). §the-named-the-eslint-plugin-ses-ava-benchmark-trilogy names the trilogy. The three are peer development-affordance packages — they support development of OTHER packages rather than appearing at runtime. The relationship to the three-shapes-of-design-vs-implementation-arc framing established in cycle 362 deserves its own name. The benchmark README does not fit any of the three shapes cleanly. It is not ABSTRACTING because there is no concrete-vs-abstract pairing between README and source. It is not UNDERCOUNTING because the README does not attempt enumeration at all. It is not WHAT-VS-HOW because the README does not state the WHAT clearly enough to compare to source HOW. The three-shapes framing applies to packages where the README ATTEMPTS to describe the implementation; the benchmark README DELEGATES the description to AVA-conventions. §the-named-three-shapes-framing-presumes-readme-attempts-description names the boundary. The benchmark cycle reveals the boundary without breaking the framing; the framing remains valid for the packages it covers, and the cycle 363 case is named as outside-the-framing rather than as a fourth shape. Seven citation arcs close on this cycle. Cycle 362 was the prior chat-lane (arc 1, adjacent forward pair). Cycle 361 was the prior testing-package ingest; both ses-ava and benchmark are "ava-related" — ses-ava IS AVA-wrapping, benchmark is "ava-like" (arc 1). Cycle 360's three-shapes framing finds its bounding case here (arc 1). Cycle 359's discipline-as-package completes the development-affordance-trilogy with this one (arc 1). Cycle 339's lockdown README named the cross-engine concerns this package measures (arc 48). Cycle 337's harden README named the cross-engine performance that this benchmark verifies (arc 48). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 36). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-TWELVE (205 + 7 net new). §fifty-three-cycles-with-named-pivot-domain-stay (310-363). §twenty-fourth-named-package-in-the-pivot-cluster. §eleventh-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across eleven consecutive cycles (353-363).
