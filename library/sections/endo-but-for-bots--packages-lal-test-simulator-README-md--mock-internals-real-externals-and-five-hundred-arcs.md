---
source: endo-but-for-bots--packages-lal-test-simulator-README-md
cycle: 403
lane: designs
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: testing-harness-design-doc
shape_subtype: mock-internals-real-externals
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 51
session_milestone: citation-arc-closures-crosses-500
---

The 54-line README.md for @endo/lal's simulator is the fifth lal-package artifact ingested in the cluster (after cycle 399's lal README, cycle 400's providers/config.js source, cycle 401's LAL-ARCHITECTURE.md design doc, and cycle 402's agent.types.d.ts). Cycle 403's single most structurally interesting move surfaces a testing topology that INVERTS the usual mock arrangement. §the-named-mock-internals-real-externals names the observation. Lines 3-5 articulate the design choice: "Run the Lal agent against real LLM providers (Anthropic, OpenAI, llama.cpp) using mock guest powers instead of a full Endo daemon. Use this to debug provider configuration, auth, and behavior without starting the daemon." Most simulators mock the EXTERNAL service and exercise the REAL internal code path. This simulator does the opposite: it MOCKS the internal daemon (the mock-powers.js file provides fake EndoGuest implementations) but uses the REAL external LLM provider. The inversion is purposeful — the surface the tester wants to exercise is the provider boundary, so the mock is positioned to isolate THAT surface from the daemon's complexity. §the-named-inverted-mock-surface-real-external-mock-internal names the tier-3 meta-pattern. The cluster's drift framings now extend further. §the-named-three-different-Claude-models-across-four-documents names a confirming-and-extending observation. Line 21 shows the example invocation: `LAL_MODEL=claude-3-5-sonnet-20241022`. Cycle 399's README listed the Anthropic default as `claude-opus-4-5-20251101`. Cycle 400's source defined the Anthropic default as `claude-sonnet-4-6-20250514`. The simulator README's example uses `claude-3-5-sonnet-20241022` — a THIRD distinct Claude model string, dated 2024-10-22 (much older than either of the other two; Claude 3.5 sonnet was superseded over a year before this ingest). The lal package now contains THREE different default Claude model strings across FOUR documents (README + LAL-ARCHITECTURE + source-default + simulator-example). §the-named-three-model-strings-across-four-documents-in-one-package names the tier-3 meta-pattern. This extends cycle 401's TWO-documents-agree-against-source framing — here it's THREE-documents-disagree-with-each-other and the source. The document tier has further stratified: it is not merely "documents lag source" or "two documents agree against source"; it is "three documents disagree with each other AND the source, each frozen at a different historical moment." §the-named-document-tier-as-temporal-stratification names the refinement. The mock is minimal. §the-named-minimal-mock-only-enough-for-one-message marks line 28: "Builds mock guest powers (in-memory directory, single inbox message)." Only as much as needed to deliver ONE message and observe ONE reply + dismiss. §the-named-minimal-test-setup-only-for-canonical-flow names the tier-3 meta-pattern. The flow has two entry points. §the-named-dual-entry-point-script-and-test marks line 14's "yarn simulator" and line 43's "yarn test test/simulator/run-simulator.test.js" — the same flow exposed as a standalone debugging script AND as an Ava test in CI. §the-named-same-flow-via-script-and-test names the tier-3 meta-pattern. The test gracefully degrades. §the-named-credential-gated-test-skip marks line 46: "If LAL_AUTH_TOKEN is not set for Anthropic, the test skips." Missing credentials produce a skip, not a failure. §the-named-graceful-skip-on-missing-credentials names the tier-3 meta-pattern. The mock exposes observation hooks. §the-named-mock-exposes-observation-hooks marks lines 51-52: mock-powers.js exposes `whenDismissed(n)` and `sent` for the runner to wait and inspect. §the-named-test-mock-as-assertion-surface names the tier-3 meta-pattern — the mock is also where the test-side observation hooks live. The timeout is bounded. §the-named-two-minute-timeout-on-canonical-flow marks line 31: "Waits until the agent dismisses that message (or 2-minute timeout)." Two minutes is the upper bound on the canonical reply+dismiss flow. §the-named-bounded-test-timeout-for-non-deterministic-LLM names the tier-3 meta-pattern; standard AVA test timeouts calibrated specifically for non-deterministic LLM response time. The canonical agent loop iteration is named. §the-named-canonical-conversation-as-reply-and-dismiss marks line 30: "asking the agent to reply and dismiss it." The smallest meaningful conversation in Lal is reply + dismiss — two tool calls. §the-named-minimum-agent-loop-iteration names the tier-3 meta-pattern. The simulator is positioned as a debugging tool first. §the-named-yarn-simulator-as-debugging-entry-point names the framing — line 5 says the simulator is for "debug provider configuration, auth, and behavior." The same flow run as a test in CI is secondary; the script is positioned primarily as a debugging tool. §the-named-debugging-script-first-test-second names the tier-3 meta-pattern. The stale model in the example deserves its own framing. §the-named-claude-3-5-sonnet-20241022-in-simulator marks the specific model string. The Claude 3.5 sonnet (October 2024) has been superseded for over a year but is still the example. §the-named-stale-model-example-in-debugging-doc names the tier-3 meta-pattern; document drift in DEEP form — an example file references a model that has been deprecated for over a year. The cycle's session-level milestone matters. §the-named-citation-arc-closures-crosses-500 names the structural fact: cycle 403 pushes the citation arc count to FIVE-HUNDRED-AND-FIVE, crossing the 500 threshold for the first time in the post-pivot era. §the-named-fifty-one-conformant-cycles-and-counting names the post-refactor authoring discipline session-level observation. §ninety-three-cycles-with-named-pivot-domain-stay (310-403). Seven citation arcs close on this cycle. Cycle 402 (1, adjacent forward; types file in the same package; the simulator README is the fifth artifact in a coherent ingestion of one package). Cycle 401 (2, the design doc names the simulator briefly in the project layout but doesn't describe it; cycle 403 reveals what the design doc omits — another instance of the architecture-doc-omits framing from cycle 402). Cycle 400 (3, source default is one of three model strings in the cluster; cycle 403 adds a third). Cycle 399 (3, README is another of three model strings). Cycle 346 (3, name-aliasing patterns; the canonical reply/dismiss naming). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-FIVE (498 + 7 net new). The cluster crosses 500 citation-arc-closures — a session-level milestone in its own right. The cluster's drift vocabulary continues to refine; cycle 403 adds §the-named-document-tier-as-temporal-stratification as a refinement of cycle 401's TWO-documents-agree-against-source framing. Each document is frozen at a different historical moment in the package's evolution; the document tier is stratified by time rather than by content.
