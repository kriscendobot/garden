---
source: endo--packages-ses-ava-prepare-endo-js
cycle: 362
lane: chat
ingested: 2026-06-15
package: ses-ava
cluster: pivot
shape: canonical-user-entry-point
shape_subtype: env-mutating-init-and-wrap
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 10
---

The 27-line `prepare-endo.js` is the file the cycle 361 README told users to import. The README said: replace your `import 'ses'` and `import test from 'ava'` with `import test from '@endo/ses-ava/prepare-endo.js'`. This file is the right-hand side of that recommendation. Reading it line by line reveals how the three named debugging affordances are actually delivered. Lines 3 and 4 import two distinct init files from @endo/init, in order: pre-remoting first, debug second. §the-named-two-init-imports-in-order names the sequence; the order matters because pre-remoting must establish the eventual-send turn boundary before debug instrumentation reads it. §the-named-imports-as-ordered-side-effect-sequence is the tier-3 meta-pattern, sibling to cycle 344's @endo/init rung-as-entry-point shape. Lines 12 through 21 mutate `globalThis.process.env`. Line 13 sets `env.TRACK_TURNS = 'enabled'`. Lines 15-21 add `track-turns` to the `DEBUG` env var if not already present, preserving any existing DEBUG value by appending rather than overwriting. §the-named-additive-env-modification names the stance; §the-named-augment-not-clobber names the tier-3 meta-pattern, which respects existing user configuration while adding the package's own needs. The structural insight that justifies most of this cycle's naming is that env-var mutation is the cross-package coordination mechanism. The README named three debugging affordances (deep stacks of prior turns, unredacted stack traces, unredacted error messages); the implementation reveals that they are delivered NOT by patching SES, NOT by patching AVA, NOT by passing arguments through wrapTest, but by SETTING ENV VARS that other modules consult at their own initialization. @endo/init/debug.js looks at DEBUG; @endo/env-options has the `environmentOptionsListHas` function imported on line 5; the SES Assert machinery consults its own env-var configuration. The env IS the configuration bus, even though all participants are JavaScript modules in the same process. §the-named-env-vars-as-cross-package-config-channel names the single most structurally interesting move. This is the third instance of the design-vs-implementation-arc-from-README-to-source pattern, and it is a third shape, not a third example of the first two. Cycle 357→358 zip was the ABSTRACTING shape: README named the attack abstractly via "magic numbers in a comment", source named the exact byte sequences as named exports. Cycle 359→360 eslint-plugin was the UNDERCOUNTING shape: README named five configs and zero rules, source had eight configs and six rules. Cycle 361→362 ses-ava is the WHAT-VS-HOW shape: README named the three debugging affordances; source reveals the env-var mutation that delivers them. §the-named-three-shapes-of-design-vs-implementation-arc-from-README-to-source — ABSTRACTING and UNDERCOUNTING and WHAT-VS-HOW — names the tier-3 framing. Three instances establish the framing as a meta-pattern rather than a coincidence. Line 26 carries one of the smaller-but-named moves: `// eslint-disable-next-line no-restricted-exports`. The file uses `export { test as default }` on line 27, which the project's base ESLint configuration disallows by default. The disable comment is explicit, single-line, and located at the README-canonical-entry-point. §the-named-eslint-disable-as-deliberate-rule-carveout names the move; §the-named-carveout-located-at-readme-canonical-entry-point names the tier-3 meta-pattern, which observes that the carve-out must happen here because the README's recommended import pattern is `import test from '@endo/ses-ava/prepare-endo.js'`, and that requires a default export. The discipline package from cycles 359-360 (@endo/eslint-plugin) and this user-facing entry point interact: the discipline-package enforces no-restricted-exports across the project; this file is the single, named, located carve-out for the documented public API. Lines 10 and 11 are two TODOs. The first reads "TODO consider adding env option setting APIs to @endo/env-options" — the file mutates env directly because env-options does not yet have a setter API. The second reads "TODO should set up globalThis.process.env if absent" — the code assumes `globalThis.process` exists. §the-named-TODO-in-source-as-known-gap names both moves; §the-named-honest-gap-named-as-TODO is the tier-3 meta-pattern, sibling to cycle 359's §the-named-unfilled-supported-rules-section as another shape of in-source-honest-incompleteness. The mechanism behind the first README-named affordance deserves its own name: line 13's `env.TRACK_TURNS = 'enabled'` is the env-var that turns on the eventual-send turn-tracking machinery, which is the substrate behind "deep stacks of prior turns." §the-named-deep-stacks-via-track-turns-env-var ties the README's named affordance to its underlying mechanism, and demonstrates the WHAT-VS-HOW shape concretely. Seven citation arcs close on this cycle. Cycle 361 was the prior README; the forward pair completes (arc 1). Cycle 360 was the cycle that established the design-vs-implementation-arc meta-pattern with two instances; this cycle's third instance promotes the meta-pattern to a three-shapes framing (arc 1). Cycle 358 was the first instance (zip); the framing now has it as one of three shapes (arc 1). Cycle 344's @endo/init introduced the two init files this source imports in order (arc 17). Cycle 207's @endo/env-options ingest introduced `environmentOptionsListHas`, which this file imports and uses (arc 33). Cycle 322's errors README defined the redaction story; this cycle reveals the named channel through which the redaction can be opened explicitly (arc 41). Cycle 359's @endo/eslint-plugin discipline rules are explicitly carved out in this file at line 26 (arc 1, the prior-cycle-of-the-discipline-package paired with the prior-cycle-of-the-testing-substrate-bridge in their adjacent forward sources). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-FIVE (198 + 7 net new). §fifty-two-cycles-with-named-pivot-domain-stay (310-362). §tenth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across ten consecutive cycles (353-362).
