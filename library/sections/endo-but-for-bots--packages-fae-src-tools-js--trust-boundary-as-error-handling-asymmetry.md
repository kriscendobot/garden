---
source: endo-but-for-bots--packages-fae-src-tools-js
cycle: 416
lane: chat
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: fae
cluster: pivot
shape: tool-discovery-module
shape_subtype: asymmetric-trust-boundary-handling
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 64
---

The 95-line tools.js from @endo/fae/src is the tool-discovery module the cycle 415 COMPARISON document referenced. Cycle 416's most structurally interesting move surfaces a structural property of the code itself: the source-level expression of a security model. §the-named-asymmetric-error-handling-local-trusted-remote-defensive names the observation. The file treats LOCAL tools and REMOTE tools differently for error handling. Lines 32-34 (local): `for (const tool of localTools.values()) { schemas.push(tool.schema()); }` — NO try/catch. If a local tool's schema() throws, the discovery FAILS. Lines 57-67 (remote): try/catch around the schema() call, console.warn on failure, SKIP the tool but continue discovery. The trust boundary mirrors the cap-security model: local code is THIS AGENT's code (trusted to follow the FaeTool contract); remote code is SOMEONE ELSE's code (validated defensively because the contract may not hold). §the-named-trust-boundary-as-error-handling-asymmetry names the tier-3 meta-pattern; the source code's structure reflects the security model. The cluster's accumulated framings about provider boundaries, surface tiers, and authority flows now reach the code level: the code itself ENCODES the trust topology in its error-handling discipline. Failure on local code is treated as a programming error (let it propagate); failure on remote code is treated as misbehavior (handle gracefully). Other findings layer onto established framings. §the-named-graceful-tool-discovery-failure-via-warning marks lines 63-67. §the-named-partial-discovery-over-total-failure names the tier-3 meta-pattern; partial discovery is preferred over total failure when the failing tool is external. §the-named-CONFIRMS-cycle-415-runtime-discovery marks lines 22-23: "Called at the start of each agent turn so that tools adopted between turns (e.g., received via mail) are immediately available." Verifies cycle 415's COMPARISON-FAE-LAL.md claim. §the-named-comparison-claim-confirmed-by-source names the tier-3 meta-pattern; another counterexample to drift framings — cycle 412 named this for Anthropic-ID synthesis; cycle 416 adds another instance for runtime tool discovery. The local-precedence rule is named. §the-named-local-tools-take-precedence-over-daemon-tools marks line 55: `.filter(name => !toolMap.has(name))`. The daemon's tools/ directory is added only for names not already in the local toolMap. Local tools shadow daemon tools by name. §the-named-shadowing-by-local-precedence names the tier-3 meta-pattern. The parallel discovery is named. §the-named-parallel-tool-discovery-via-allSettled marks line 53. Tool schemas are fetched in PARALLEL using allSettled — one tool's failure doesn't fail the others. §the-named-parallel-with-isolation-via-allSettled names the tier-3 meta-pattern; sibling to cycle 408's role-cardinality framings — both reflect Endo's preference for capability-based composition. The error message is diagnostic. §the-named-unknown-tool-error-lists-available-tools marks lines 89-90. §the-named-helpful-error-with-available-options names the tier-3 meta-pattern. The defensive directory-fallback is named. §the-named-missing-directory-equals-empty-directory marks lines 41-46. §the-named-defensive-fallback-to-empty names the tier-3 meta-pattern. The JSDoc type predicate is named. §the-named-type-narrowing-via-JSDoc-predicate marks lines 47-52. The .filter callback has a JSDoc annotation declaring its predicate as `x is string`. §the-named-JSDoc-type-predicate-for-runtime-filter names the tier-3 meta-pattern; the Endo/SES discipline reaches into runtime-type-narrowing via JSDoc rather than pure TypeScript syntax. The eslint-disable is potentially stale. §the-named-stale-eslint-disable-no-active-violations marks line 3's file-level disable for no-await-in-loop. But discoverTools uses Promise.allSettled (not await-in-loop); executeTool doesn't loop. The disable is unnecessary in the current code. §the-named-defensive-or-stale-eslint-disable names the tier-3 meta-pattern; the cluster's growing inventory of documentation-vs-code drift now extends to LINT-CONFIG-vs-CODE drift — even the linting metadata drifts from the code it modifies. The harden discipline is followed. §the-named-harden-on-both-exports marks lines 73 and 95. Both discoverTools and executeTool are explicitly hardened. Counterexample to providers/ directory's absence (cycles 406, 408, 412). §the-named-fae-src-follows-harden-convention names the tier-3 meta-pattern. Cluster's harden-discipline-inconsistent-across-package framing now refined: the inconsistency is PER-SUBDIRECTORY (lal/providers don't harden; lal/setup does; conversation-tree does; fae/src does). The eventual-send is locality-transparent. §the-named-eventual-send-uniform-across-local-and-remote marks line 92: `await E(tool).execute(args)`. The E() invocation works for both local FaeTool exo objects AND remote far-references. §the-named-E-send-as-locality-transparent names the tier-3 meta-pattern; one of the deepest Endo idioms — the same call syntax for in-process and cross-process invocation. The toolMap type union reflects the trust boundary. §the-named-toolMap-as-typed-union-by-locality marks line 36: `Map<string, FaeTool | object>`. Local tools are typed FaeTool; remote tools are typed `object` because the CapTP boundary erases interface information. §the-named-type-union-reflects-trust-boundary names the tier-3 meta-pattern; another shape where the SECURITY model influences the CODE structure. The DiscoveredTools type has two parallel shapes. §the-named-DiscoveredTools-two-field-shape marks lines 13-16. DiscoveredTools is `{ schemas: ToolSchema[], toolMap: Map<string, FaeTool | object> }`. Two fields: the LLM-facing schemas array AND the dispatch-facing toolMap. Same data, two organizations. §the-named-LLM-facing-vs-dispatch-facing-shapes names the tier-3 meta-pattern; sibling to cycle 414's storage-vs-presentation framing — the same data appears in two shapes for two consumers. The discovery is stateless. §the-named-no-tool-version-tracking marks the structural fact: discoverTools doesn't track tool versions. Each turn re-fetches the schema fresh. If a tool's interface changes between turns, the LLM sees the new shape. §the-named-stateless-discovery names the tier-3 meta-pattern; per-turn freshness over caching. The cycle's session-level observation continues. §the-named-sixty-four-conformant-cycles-and-counting marks the session-level observation: sixty-fourth AUTHORED conformant single-body section doc in post-refactor era (cycles 353-416). §one-hundred-and-six-cycles-with-named-pivot-domain-stay (310-416). Ten citation arcs close on this cycle. Cycle 415 (1, adjacent forward; COMPARISON-FAE-LAL claimed runtime tool discovery; cycle 416 verifies in source). Cycle 414 (3, two-field DiscoveredTools shape echoes storage-vs-presentation framing). Cycle 412 (3, comparison-claim-confirmed-by-source recurs — design-doc-prediction-matches-code third instance). Cycle 406 (3, harden-discipline subdirectory-pattern extends). Cycle 387 (5, FaeTool branded-interface via M.interface — confirms cycle 415's faetool-as-exo framing). Cycle 401 (3, eventual-send framing applied uniformly here). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing — error messages should help debug). Cycle 364 (4, shapes count keeps growing). Cycle 318 (3, Endo idiom — E() locality-transparency). Pushes citation-arc-closures-in-pivot to SIX-HUNDRED-AND-TWENTY-SIX (617 + 9 net new). The library has crossed 928 sections; the post-refactor era continues at sixty-four consecutive conformant cycles. Cycle 416's most distinctive contribution is the framing that SOURCE CODE STRUCTURE ENCODES SECURITY TOPOLOGY — the trust boundary between local and remote shows up in the asymmetric error handling, in the type unions, in the helper conventions. The code itself is a partial expression of the cap-security model — not just a runtime expression but a structural one.
