---
source: endo-but-for-bots--packages-pass-style-src-copyRecord-js
cycle: 424
lane: chat
ingested: 2026-06-21
repo: endo-but-for-bots
branch: llm
package: pass-style
cluster: pivot
shape: pass-style-helper-implementation
shape_subtype: two-phase-validation
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 72
session_milestone: citation-arcs-cross-700
---

The 70-line copyRecord.js from @endo/pass-style/src is the canonical implementation of the pass-by-copy/Record classification cycle 423 named in @endo/marshal. Cycle 424's most structurally interesting move surfaces a thoughtful validator design choice. §the-named-two-phase-validation-can-be-valid-then-rest-valid names the observation. The CopyRecordHelper exports two validation methods (lines 50-69): confirmCanBeValid (lines 50-59) checks the CANDIDATE itself — prototype must be Object.prototype, all keys must be string, no method-like values. assertRestValid (lines 61-69) RECURSES into the values, calling passStyleOfRecur on each. The split separates SHALLOW STRUCTURAL VALIDITY (can this object possibly be a record at all?) from DEEP RECURSIVE VALIDITY (do all its members validly pass-style?). §the-named-shallow-structural-vs-deep-recursive-validation names the tier-3 meta-pattern; the validator design is thoughtful — fast shallow check rules out most rejects before the recursive walk. The pass-style helpers share this pattern across categories. The cluster encounters infrastructure-level naming drift. §the-named-three-names-for-same-marshal-classification names the observation: marshal README (cycle 423) called it "pass-by-copy"; copyRecord.js's styleName (line 48) calls it "copyRecord"; the error messages (lines 21, 35, 39) call it "Records". §the-named-pass-by-copy-copyRecord-Records-three-names names the tier-3 meta-pattern; the cluster's naming drift framings now extend into marshal infrastructure itself — even the core capability infrastructure has terminology drift. The three constraints for record validity are articulated. §the-named-record-constraints-three-conditions names them: (1) Records must inherit from Object.prototype (line 21); (2) Records can only have string-named properties (line 35); (3) Records cannot contain non-far functions because they may be methods of an implicit Remotable (line 39). §the-named-three-conditions-for-record-validity names the tier-3 meta-pattern. The Rejector pattern is named. §the-named-Rejector-as-tagged-template-error-pattern marks lines 18-21 and 31-40. The validation functions take a `Rejector` parameter that can be called with a tagged template literal: `reject\`Records must inherit from Object.prototype: ${candidate}\``. §the-named-Rejector-tagged-template-for-error-threading names the tier-3 meta-pattern; a clean way to thread error reporting through validation without raw throws. The short-circuit validation idiom is named. §the-named-short-circuit-with-reject-template-as-validation-idiom marks lines 32-40. The whole expression returns truthy iff validation passes; otherwise the reject template contributes the error message. §the-named-short-circuit-validation-with-inline-reject names the tier-3 meta-pattern. The stale error message is acknowledged. §the-named-stale-error-message-with-TODO-comment marks line 38's TODO. The error message refers to "implicit Remotable," a concept that no longer exists. The TODO acknowledges the drift. §the-named-acknowledged-stale-error-message names the tier-3 meta-pattern; contrast cycle 422's intra-file convention-drift without acknowledgment — here the drift is marked but not yet fixed. The cluster's drift framings now distinguish acknowledged from unacknowledged drift. The canBeMethod predicate is named. §the-named-canBeMethod-as-method-shape-predicate marks lines 4 and 36. The validation rejects records with method-like values. §the-named-method-shape-vs-data-shape-discrimination names the tier-3 meta-pattern. The Reflect.ownKeys enumeration is named. §the-named-Reflect-ownKeys-for-validation-enumeration marks line 11. ownKeys enumerates ALL properties (including symbol-keyed); symbol-keyed properties are then rejected via the typeof string check. §the-named-enumerate-all-keys-but-reject-symbols names the tier-3 meta-pattern. The prototype-identity check is strict. §the-named-prototype-identity-check-for-plain-object marks lines 11-12 and 20. §the-named-strict-reference-equality-for-prototype-identity names the tier-3 meta-pattern. The PassStyleHelper interface is shared. §the-named-PassStyleHelper-interface-per-style marks line 45's `@type {PassStyleHelper}`. §the-named-helper-per-pass-style-category names the tier-3 meta-pattern; the pass-style package has a HELPER per pass-style category (copyArray, copyRecord, error, etc.). The safety helper is named. §the-named-confirmOwnDataDescriptor-as-safety-helper marks lines 3 and 66. §the-named-own-data-descriptor-access-for-getter-safety names the tier-3 meta-pattern; the helper accesses values safely without triggering getters. The harden discipline is followed. §the-named-CopyRecordHelper-as-hardened-object marks line 47. §the-named-pass-style-helpers-follow-harden-convention names the tier-3 meta-pattern. The string tag identifies the style. §the-named-styleName-as-string-tag marks line 48. §the-named-string-tag-as-style-discriminator names the tier-3 meta-pattern. The mutual recursion is via callback. §the-named-passStyleOfRecur-as-mutual-recursion-callback marks lines 61 and 67. §the-named-mutual-recursion-via-callback names the tier-3 meta-pattern; the helpers don't directly call each other; they go through the dispatcher. The Fail helper is named. §the-named-Fail-from-endo-errors-as-throw-helper marks line 2. §the-named-Fail-tagged-template-as-unconditional-rejector names the tier-3 meta-pattern. The two error modes are paired. §the-named-reject-vs-Fail-asymmetry names the structural fact: confirmCanBeValid takes a Rejector parameter (conditional); assertRestValid uses Fail directly (unconditional). §the-named-conditional-reject-vs-unconditional-Fail names the tier-3 meta-pattern; two error-reporting modes for two phases of validation. The cycle's session-level observation is a milestone. §the-named-seventy-two-conformant-cycles-and-counting marks the session-level observation: seventy-second AUTHORED conformant single-body section doc in post-refactor era (cycles 353-424). §one-hundred-and-fourteen-cycles-with-named-pivot-domain-stay (310-424). §the-named-citation-arcs-cross-700 marks the structural fact: cycle 424 pushes citation-arc-closures-in-pivot to SEVEN-HUNDRED-AND-THREE — crossing 700 for the first time. Ten citation arcs close on this cycle. Cycle 423 (1, adjacent forward; marshal-depends-on-pass-style framing now confirmed by reading pass-style source; the classification cycle 423 named in marshal is IMPLEMENTED here). Cycle 422 (3, intra-file drift comparison: cycle 422 had unacknowledged drift; cycle 424 has TODO-acknowledged drift). Cycle 421 (3, three-name-drift like cycle 421's ChatMessage cross-package duplication — same classification, multiple names). Cycle 416 (3, trust-boundary asymmetric error handling parallels conditional-reject-vs-unconditional-Fail asymmetry). Cycle 408 (3, role-cardinality-reduction is a kind of validation filtering, sibling to pass-style classification). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing reaches the Rejector pattern). Cycle 387 (5, branded-types via PassStyleHelper interface). Cycle 364 (4, shapes count growing). Cycle 318 (3, Endo idiom — Fail and tagged templates). Pushes citation-arc-closures-in-pivot to SEVEN-HUNDRED-AND-THREE (693 + 10 net new). The library has crossed 936 sections; the post-refactor era continues at seventy-two consecutive conformant cycles; the citation-arc-closures count has crossed 700 — a session-level milestone. The cluster's vocabulary at this point connects agent-level framings (cycles 399-422) through marshal's classification (cycle 423) into the validators that ENFORCE the classification (cycle 424). The infrastructure layers come into focus.
