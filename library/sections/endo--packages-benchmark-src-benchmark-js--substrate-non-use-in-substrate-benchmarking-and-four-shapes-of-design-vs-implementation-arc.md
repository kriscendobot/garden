---
source: endo--packages-benchmark-src-benchmark-js
cycle: 364
lane: chat
ingested: 2026-06-15
package: benchmark
cluster: pivot
shape: hand-rolled-minimal-substrate-benchmark
shape_subtype: private-package-implementation
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 12
---

The 39-line `src/benchmark.js` is the implementation behind cycle 363's "minimalistic ava-like interface" claim, and reading it line by line reveals a structural insight that none of the prior 23 cluster members exhibit. Line 1 reads `const getTime = () => Date.now() * 1_000_000`, which converts milliseconds to nanoseconds by multiplication. Lines 21 and 25 throw native `Error` objects, not `@endo/errors`. Line 13's `console.log` does the reporting. There is no SES Assert machinery, no `@endo/check`, no `@endo/eventual-send`, no @endo anything. The two hand-rolled assertion helpers `assert` and `truthy` (lines 20-26) total six lines and throw native Error directly. §the-named-substrate-non-use-in-substrate-benchmarking names the single most structurally interesting move. The benchmark MEASURES substrate; using substrate would corrupt the measurement. The third-level reasoning is §the-named-circular-measurement-avoidance-explains-substrate-non-use: cross-engine + measuring substrate + can't use substrate → minimal native-only implementation. The cycle 363 README's "minimalistic" descriptor turns out to mean "minimal-substrate-use" not "minimal-code-effort." The 39 lines are exactly as much as needed to measure substrate primitives without measuring through them. This cycle revises the framing established by cycle 362 (and recapitulated by cycle 363). Cycle 362 said three shapes of design-vs-implementation-arc-from-README-to-source (ABSTRACTING, UNDERCOUNTING, WHAT-VS-HOW). Cycle 363 said benchmark sits outside the three-shapes framing because its README delegates to AVA-conventions rather than attempting description. Cycle 364 reveals the source doesn't use AVA at all; the source self-defines a literal 39-line subset of AVA's surface API. The README WAS attempting description, just by analogy. §the-named-analogy-vs-literal-arc names the fourth shape: README points to an external reference loosely; source provides the literal self-contained definition that approximates the reference. §the-named-four-shapes-of-design-vs-implementation-arc-from-README-to-source is the revised tier-3 framing: ABSTRACTING (cycle 357→358 zip) + UNDERCOUNTING (cycle 359→360 eslint-plugin) + WHAT-VS-HOW (cycle 361→362 ses-ava) + ANALOGY-VS-LITERAL (cycle 363→364 benchmark). Four instances establish a four-shapes taxonomy where two adjacent cycles previously suggested only three. Honest revision in light of new evidence is part of the discipline. Several smaller moves deserve naming. The Date.now-times-million pattern is one. `Date.now()` returns milliseconds; multiplying by a million yields nanosecond units with FAKE precision (the trailing six digits are always zero). §the-named-Date-now-times-million-for-nanoseconds names the move; §the-named-fake-nanosecond-precision-for-cross-engine-compat names the tier-3 meta-pattern. The deliberate compromise is because XS may not have `performance.now()` or `process.hrtime`, and the cross-engine concern from cycle 363 forces fall-back to the universally-available `Date.now`. The trade-off is named, not hidden. The typo `expedtedTime` appears on lines 3, 15, and 16. §the-named-typo-fossil-in-private-package names the residue. Pairs with cycle 363's NEW SHAPE §the-named-private-package-as-internal-tooling: a public package would have caught the typo through peer review; a private package consumed only via `yarn test` has no audience that sees the parameter name. §the-named-typo-as-evidence-of-missing-audience-effect names the tier-3 meta-pattern. The private-package shape has observable consequences in source. Line 7 carries `// eslint-disable-next-line no-await-in-loop` for line 8's `await fn();`. This is the SECOND eslint-disable-as-deliberate-rule-carveout in two cycles, after cycle 362's ses-ava `no-restricted-exports` carve-out. The recurrence promotes the pattern. §the-named-await-in-loop-deliberate-for-sequential-iteration names the local justification (the function under measurement is async and must be awaited sequentially, not concurrently, to measure cumulative time). §the-named-eslint-disable-as-recurring-carve-out-pattern-across-packages establishes the pattern as a tier-3 meta-pattern that recurs across the development-affordance trilogy. Lines 4 and 29 both contain `await null;`. The pattern yields to the microtask queue without waiting for anything specific, forcing the function body to start in a clean async tick before measurement begins. §the-named-await-null-as-microtask-yield names the idiom; §the-named-microtask-yield-before-measurement names the purpose. The `test` function on lines 28-37 is 10 lines: log + invoke fn with injected helpers + log pass-or-fail with emoji. There is no AVA at all. §the-named-ava-like-is-just-try-catch names the structural surprise; §the-named-ava-like-as-surface-similarity-not-implementation-derivation names the broader pattern. AVA-like in cycle 363's README is approximate accuracy: the function is named `test`, takes a name and a function, and reports pass/fail. The surface mimics AVA; the implementation derives from nothing. The injected helpers pattern deserves its own naming. The `test` function passes `{ assert, truthy }` to the test fn on line 32; the test fn then calls `benchmark(name, t, fn, expedtedTime)` where `t` is the same helpers object. §the-named-helper-injection-via-t-object names the move; §the-named-injected-helpers-as-the-t-pattern names the analogue to AVA's `t` argument. The package's entire exported API is on line 39: `export { benchmark, test }`. §the-named-two-function-API names the minimalism; this pairs with cycle 363's §the-named-empty-index-js-private-package-pattern as a sibling shape of minimal-API-when-no-external-audience. Lines 33 and 35 contain ✅ and ❌ emoji. §the-named-emoji-in-private-package-output names the move; §the-named-emoji-consequence-of-private-shape names the tier-3 meta-pattern. Private packages can use emoji without worrying about cross-environment rendering or terminal compatibility because the audience is the developers themselves. Seven citation arcs close on this cycle. Cycle 363 was the prior README; the forward pair completes and promotes the three-shapes framing to four-shapes framing (arc 1). Cycle 362 was the first eslint-disable-as-deliberate-rule-carveout instance; this cycle's second instance establishes the recurring-carve-out-pattern-across-packages (arc 1). Cycle 361 was the testing-substrate-bridge README; the ava-like claim that flows through to cycle 363 finds its source-level resolution here (arc 1). Cycle 358 was the first design-vs-implementation-arc instance; the four-shapes framing now positions it as one of four shapes (arc 1). Cycle 322's @endo/errors README is the substrate this file explicitly does not use (arc 42). Cycle 339's lockdown README names Date constructor as absent in locked-down environments; the benchmark uses `Date.now()` anyway via cross-engine-compatibility need (arc 49). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 37). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-NINETEEN (212 + 7 net new). §fifty-four-cycles-with-named-pivot-domain-stay (310-364). §twelfth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across twelve consecutive cycles (353-364).
