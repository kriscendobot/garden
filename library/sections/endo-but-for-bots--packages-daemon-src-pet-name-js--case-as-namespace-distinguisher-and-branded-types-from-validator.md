---
source: endo-but-for-bots--packages-daemon-src-pet-name-js
cycle: 394
lane: chat
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: daemon
cluster: pivot
shape: name-validator-and-branded-type-module
shape_subtype: pet-name-and-special-name-validators
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 42
---

The 126-line `packages/daemon/src/pet-name.js` is the canonical implementation of the bot-fork's pet-name validation discipline. Reading it answers questions raised by cycle 393's mailbox-durability plan (which used naming-as-schema), cycle 386's chat petname-parser (which set the 128-char limit), and cycle 387's AGENTS.md (which named the branded-types-from-validators discipline). The single most structurally interesting move sits in lines 9-10. Two regex patterns distinguish two namespaces by FIRST-CHARACTER CASE. `validPetNamePattern = /^[a-z0-9][a-z0-9-]{0,127}$/` accepts names that start with a lowercase letter or digit and continue with lowercase alphanumerics or hyphens, up to 128 characters. `validSpecialNamePattern = /^[A-Z][A-Z0-9-]{0,127}$/` accepts names that start with an uppercase letter and continue with uppercase alphanumerics or hyphens, also 128 characters. Same shape, different namespace via case. §the-named-two-patterns-petName-and-specialName names the move; §the-named-case-as-namespace-distinguisher names the tier-3 meta-pattern. Case is the namespace flag — simple, visual, unambiguous. The length limit deserves its cross-package consistency observation. §the-named-128-char-limit-matches-cycle-386 marks the `{0,127}` quantifier matching cycle 386's @endo/chat petname-parser limit. The daemon's pet-name validator and the chat's message parser enforce the same character limit. §the-named-cross-package-name-length-consistency names the tier-3 meta-pattern. The branded-types discipline is canonical here. §the-named-PetName-and-SpecialName-as-branded-types marks lines 14, 20, and 26 using TypeScript type predicates with branded types (`PetName`, `SpecialName`, `Name`). Cycle 387's AGENTS.md said: "prefer a branded return type from the validator over raw string." pet-name.js is a canonical application of that discipline. §the-named-branded-types-from-validator names the tier-3 meta-pattern. The trio structure recurs in three different forms. §the-named-three-isX-predicates marks the three predicates (isPetName, isSpecialName, isName). §the-named-is-predicate-trio-mirrors-branded-type-trio names the tier-3 meta-pattern. §the-named-three-assertX-functions marks the three assertion functions (assertPetName, assertSpecialName, assertName) each using `asserts X is Y` JSDoc to convert a string to a branded type via runtime check. §the-named-assert-trio-mirrors-predicate-trio names the tier-3 meta-pattern; the predicates and the asserts are parallel trios over the same branded-type trio. The path shape extends the naming structure. §the-named-NamePath-as-array-of-Name marks the NamePath type as an array of names. §the-named-name-path-as-array-of-names names the tier-3 meta-pattern. The path decomposer is sibling shape to cycle 386's parser. §the-named-assertPetNamePath-returns-decomposition marks lines 100-115: returns `{ namePath, prefixPath, petName }`. The path is decomposed into three named parts: full path + prefix (all-but-last) + final pet name. §the-named-three-named-parts-of-decomposed-path names the tier-3 meta-pattern; sibling shape to cycle 386's three-parallel-arrays-strings-petNames-edgeNames. Both patterns decompose into THREE named outputs to give the caller pre-sliced parts. The normalizer accepts two input shapes. §the-named-namePathFrom-normalizes-string-or-array marks lines 122-126: a string becomes a single-element array; an array passes through; both are validated before return. §the-named-flexible-input-strict-output names the tier-3 meta-pattern; the caller can pass either form, but the output is always a validated array. Two small disciplinary observations. §the-named-q-from-endo-errors-in-error-messages marks line 7's import of `q` from `@endo/errors` and its use in error messages. Cycle 392's CLAUDE.md sidebar named the convention: "use q() to safely quote values in error messages." §the-named-q-from-errors-as-quoting-discipline names the tier-3 meta-pattern. §the-named-references-types-d-ts-via-triple-slash marks line 3's `/// <reference types="./types.d.ts" />` — TypeScript triple-slash directive. Sibling shape to cycle 376's JSDoc @import; here using triple-slash. §the-named-triple-slash-vs-jsdoc-import-as-two-shapes names the tier-3 meta-pattern; same purpose (referencing types), different syntax. The cycle's compactness observation. §the-named-126-line-canonical-name-validator marks the entire pet-name validation discipline fitting in 126 lines: two patterns + three predicates + three asserts + an array assert + a path assert + a decomposing path assert + a normalizer. The vocabulary surface is compact. §the-named-compact-vocabulary-via-trio-pattern names the tier-3 meta-pattern. The cycle's most cross-cluster observation. §the-named-special-name-vs-at-prefixed-name-distinction marks the difference between this file's `validSpecialNamePattern` (UPPERCASE, no prefix) and cycle 387/392 CLAUDE.md's `@`-prefixed names (`@agent`, `@self`, etc., matching `/^@[a-z][a-z0-9-]{0,127}$/`). The bot-fork has THREE coexisting naming conventions: lowercase-pet-name, UPPERCASE-special-name (no prefix), and @-prefixed-lowercase. §the-named-three-name-namespaces-pet-uppercase-at-prefixed names the tier-3 meta-pattern. The three namespaces serve different purposes: pet-names are user-chosen mutable identifiers; UPPERCASE-special-names are system-reserved identifiers used in user-facing contexts (HOST from cycle 374); @-prefixed names are daemon-internal special references (@agent, @self, etc.). Seven citation arcs close on this cycle. Cycle 393 was the prior designs-lane mailbox-durability plan; cycle 394 reveals the canonical name validator that underlies the mailbox's naming convention overlay (arc 1). Cycle 386's @endo/chat petname-parser used the same 128-char limit; cycle 394 names the cross-package consistency (arc 7). Cycle 387's AGENTS.md named the branded-types-from-validators discipline; pet-name.js is its canonical application (arc 2). Cycle 374's @endo/cli names-in-transit demo used HOST vs alice-agent as case-distinguished namespace; cycle 394 names the case-as-namespace mechanism behind it (arc 7). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 68). Cycle 322's @endo/errors q() is used here for error-message quoting (arc 68). Cycle 376's JSDoc @import is sibling shape to triple-slash directive used here (arc 3). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-THIRTY-SEVEN (430 + 7 net new). §eighty-four-cycles-with-named-pivot-domain-stay (310-394). §forty-second-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty-two consecutive cycles (353-394).
