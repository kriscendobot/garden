---
source: endo--packages-daemon-README-md
cycle: 369
lane: designs
ingested: 2026-06-16
package: daemon
cluster: pivot
cluster_position: twenty-sixth
shape: persistent-daemon-process-as-package
shape_subtype: host-for-guest-programs
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 17
---

The 14-line README for @endo/daemon names a package that is unlike every prior member of the pivot cluster. Every prior cluster member is a library — functions and classes imported by other code. The daemon SHIPS A PROCESS that runs continuously, accepts connections from clients on a Unix domain socket or named pipe, and manages guest programs in hardened JavaScript worker processes. §the-named-persistent-process-as-package marks the single most structurally interesting move; §the-named-process-not-library names the tier-3 meta-pattern. The cluster's twenty-sixth member opens the FIFTH new shape introduced post-pivot, joining discipline-as-package (cycle 359 eslint-plugin), testing-substrate-bridge-as-package (cycle 361 ses-ava), private-package-as-internal-tooling (cycle 363 benchmark), and meta-template-package-as-skeleton (cycle 365 skel). The four development-affordance packages were all libraries that support development of OTHER libraries; the daemon is the first cluster member that ships an entirely different kind of artifact: a process. The package has a two-part structure that the README names explicitly. Line 3-4 reads "This package provides the Endo daemon and controller. The controller manages the Endo daemon lifecycle." §the-named-controller-manages-daemon-lifecycle names the move; §the-named-two-part-package-daemon-and-controller names the tier-3 meta-pattern. The daemon is the long-running process; the controller is the lifecycle manager that starts, stops, and supervises the daemon. Cycle 365's skel had a dual nature (README is placeholder, package.json is blueprint); cycle 369's daemon has its own dual nature (the running daemon is the runtime, the controller is the meta-level lifecycle management). The communication channel layers four named protocol elements. Line 9: "Unix domain socket or named pipe associated with the user." §the-named-Unix-domain-socket-or-named-pipe-channel names the platform-conditional transport; §the-named-platform-specific-transport-as-named-pair names the tier-3 meta-pattern. Lines 11-12: "Over that channel, the daemon communicates in CapTP over netstring message envelopes." §the-named-CapTP-over-netstring-message-envelopes names the layered protocol. CapTP is Mark Miller's Capability Transport Protocol, the canonical protocol for object-capability message passing across machines (public knowledge in capability-style systems literature). Netstring is the wire format from cycle 318. The daemon stack reads from bottom to top: Unix-socket (cycle 318's netstring layered atop becomes complete here) → netstring framing → CapTP protocol → user-agent capabilities. §the-named-four-layer-protocol-stack names the tier-3 meta-pattern; the daemon makes the netstring citation arc forward into its production use. The isolation story is named in two complementary ways. Line 9-10: "manages per-user storage and compute access." §the-named-per-user-storage-and-compute-access names the user-level isolation boundary; §the-named-user-as-isolation-boundary names the tier-3 meta-pattern. Lines 6-7: "guest programs in hardened JavaScript worker processes." §the-named-hardened-JavaScript-worker-processes names the per-guest isolation; §the-named-double-isolation-lockdown-plus-process-boundary names the tier-3 meta-pattern that combines per-vat lockdown isolation with per-guest process isolation. The OCAP discipline carries across the library/process boundary. Lines 13-14: "The bootstrap provides the user agent API from which one can derive facets for other agents." Bootstrap is the root capability handed to the connecting client; the user agent is the entry point; facets are sub-capabilities derived from the user agent and delegated to other agents. §the-named-bootstrap-provides-user-agent-API names the move; §the-named-user-agent-as-root-bootstrap-capability names the tier-3 meta-pattern. The composition with cycle 367's exo least-authority-via-facets discipline is direct: the daemon level operates under the same capability-style discipline as the language level. A guest program receives a facet of the user agent and can call only the methods that the facet permits. Two vocabulary moves deserve naming. §the-named-guest-programs-vocabulary marks line 6's use of "guest programs" rather than "applications" or "modules." The vocabulary treats hosted code as guests in the daemon's house; §the-named-host-guest-relationship-as-vocabulary names the tier-3 meta-pattern. An interesting absence is §the-named-vat-vocabulary-implicit-not-named: the README does NOT use the word "vat" even though the Endo ecosystem uses vat as the canonical term for a message-passing isolation boundary. The README says "hardened JavaScript worker processes" instead. A possible reason is that vat is Agoric-specific vocabulary; the daemon README aims at a broader audience that may not know vat. §the-named-vocabulary-choice-for-broader-audience names the tier-3 meta-pattern. The README's terseness is structurally meaningful. The daemon is a complex multi-package subsystem (daemon process + controller + worker process management + CapTP + storage), but the README is 14 lines. §the-named-fourteen-line-README-for-substantial-system names the move; this pairs with cycle 363's benchmark (8-line README, substantial cross-engine system) and cycle 365's skel (3-line README, blueprint package.json). §the-named-minimal-README-for-substantial-system names the tier-3 meta-pattern; the package's design content lives elsewhere (source, package.json, examples), and the README is a pointer rather than a description. Nine citation arcs close on this cycle. Cycle 368 was the prior chat-lane (exo taxonomy); both deal with vat/process boundaries at different shapes — exo at the language level, daemon at the process level (arc 1). Cycle 367's exo least-authority-via-facets composes with daemon's facets-for-other-agents; the OCAP discipline carries across library/process boundary (arc 1). Cycle 365's skel and cycle 369's daemon both exhibit minimal-README-for-substantial-system; skel is template-with-blueprint, daemon is process-with-controller (arc 1). Cycle 363's benchmark minimal-README shape is a sibling (arc 1). Cycle 339's @endo/lockdown provides the per-vat isolation; cycle 369's daemon composes it with per-process isolation (arc 51). Cycle 337's @endo/harden defended interfaces compose with process isolation in worker boundaries (arc 50). Cycle 321's @endo/eventual-send named §the-named-vat-as-undefined-but-used-vocabulary; cycle 369 names the vat-vocabulary's interesting absence in the daemon README, completing the vocabulary arc (arc 7). Cycle 318's netstring is the wire-format for CapTP messages here; the daemon closes the netstring citation arc forward into production use (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 42). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-FIFTY-THREE (244 + 9 net new). §fifty-nine-cycles-with-named-pivot-domain-stay (310-369). §twenty-sixth-named-package-in-the-pivot-cluster. §seventeenth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across seventeen consecutive cycles (353-369).
