---
source: endo-but-for-bots--packages-daemon-src-networks-tcp-netstring-js
cycle: 446
lane: chat
ingested: 2026-06-22
repo: endo-but-for-bots
branch: llm
package: daemon
cluster: pivot
shape: network-module-in-source
shape_subtype: tcp-network-backend
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 94
---

The 191-line `packages/daemon/src/networks/tcp-netstring.js` is the TCP network backend for the Endo daemon. It represents the lowest rung of the three-backend connectivity ladder cycle 445 named: direct TCP, requiring an open port. Reading it answers what a daemon network MODULE (as distinct from a setup runlet) looks like in source. The single most structurally interesting move is the `make(powers, context)` signature: network modules export `make`, not `main`. §the-named-make-powers-context-as-network-module-shape names the pattern. The `context` parameter is new compared to the runlet shape; it carries the module's lifecycle. `context.whenCancelled()` is a promise that resolves when the daemon cancels the network service; `context.addDisposalHook()` ties `stopped` (the server's connection-drain future) into the daemon's teardown sequence. The module shape is richer than the runlet shape: it has structured startup, an active accept loop, outbound connection support, and a lifecycle contract. §the-named-context-as-lifecycle-carrier names the tier-3 meta-pattern; the context parameter distinguishes a long-lived module from a transient runlet. The greeter/gateway asymmetry is the second structurally interesting observation. The server side passes `localGreeter` to `makeMessageCapTP` on each accepted connection (line 104); the client side passes `localGateway` to `makeMessageCapTP` on each outbound connection (line 162). Both call `remoteGreeter.hello(...)` after connecting (line 176), but with different local capabilities. §the-named-greeter-vs-gateway-as-server-client-role-discriminator names the tier-3 meta-pattern; the greeter is the identity-introduction endpoint (what the server exposes to incoming connections) while the gateway is the capability-provision endpoint (what the client offers to the remote it calls). These are not the same object: the server-side CapTP bootstrap is a greeter, the client-side CapTP bootstrap is a gateway. The protocol constant is named at the top. Line 18: `const protocol = 'tcp+netstring+json+captp0';`. This string appears in log messages, in `addresses.push(...)`, and in the `supports` predicate: `new URL(address).protocol === \`${protocol}:\``. The protocol string IS the namespace for this backend. §the-named-protocol-constant-as-namespace-and-discriminator names the tier-3 meta-pattern; a single string serves as the address scheme, the log prefix, and the routing filter. The makeSocketPowers adapter pattern appears on line 21: `const { servePort, connectPort } = makeSocketPowers({ net, fsp });`. Node's `net` and `fsp` (file-system promises) are passed into `makeSocketPowers` as capability objects, returning `servePort` and `connectPort` as tested, composable operations. §the-named-makeSocketPowers-as-node-capability-adapter names the tier-3 meta-pattern; the Node-specific capabilities are wrapped at the boundary, keeping the rest of the module testable without real sockets. The lazy startup pattern is named. Lines 49-73 wrap the async bind-and-listen operation in a promise-returning IIFE assigned to `started`. The module does not bind the port until `started` is awaited; the `await started` call on line 184 gates the return of `make`. §the-named-lazy-server-start-via-started-promise names the tier-3 meta-pattern; the module starts asynchronously but the `make` function waits for the server to be ready before returning the service object. The operator-requested configuration is named. Lines 53-57: `E(powers).request('SELF', 'Please select a host:port like 127.0.0.1:8920', 'tcp-netstring-json-captp0-host-port')`. The module asks the operator for its own bind address at startup time. §the-named-operator-requested-bind-address names the tier-3 meta-pattern; the network backend is not configured at install time but at activation time, matching the daemon's request-form flow named in earlier cycles. The connection generator pattern appears on lines 38-44: an immediately-invoked generator `(function* generateNumbers() { ... })()` yields a monotonically increasing integer for each connection. Lines 83-84 and 127-128 both call `connectionNumbers.next()` to assign a sequential connection number to each accepted or initiated connection. §the-named-generator-as-connection-counter names the tier-3 meta-pattern; the generator keeps the counter state inside a closure, not in a module-level `let`. The `connectionClosedPromises` set pattern appears on lines 47 and 108-113. Each accepted connection's close promise is added to the set on creation and removed (via `finally`) when closed. Line 118 awaits `Promise.all(Array.from(connectionClosedPromises))` to drain all open connections on shutdown. §the-named-promise-set-for-graceful-drain names the tier-3 meta-pattern; the set accumulates in-flight work and the drain awaits the whole set. The Far service return is named. Lines 186-190: `return Far('TcpNetstringService', { addresses: () => harden(addresses), supports: address => ..., connect })`. The network module returns a `Far` object with three methods: `addresses` (the bound address list), `supports` (address-scheme predicate), and `connect` (outbound initiator). §the-named-network-service-interface-contract names the tier-3 meta-pattern; three methods are the network service contract that the daemon's network registry expects. The `harden(addresses)` call inside `addresses()` hardens the snapshot array before returning. §the-named-harden-on-return-not-at-creation names a subtle discipline: the `addresses` array is a mutable local variable, but harden is called on the snapshot at read time. The TCP framing stack is named explicitly. Lines 90-96 and 149-156 both apply the same two-layer encoding: `makeNetstringWriter(bytesWriter)` wraps raw bytes in netstring framing, then `mapWriter(... messageToBytes)` encodes messages as bytes. `makeNetstringReader` plus `mapReader(... bytesToMessage)` is the symmetric decode stack. §the-named-two-layer-framing-stack names the tier-3 meta-pattern; netstring framing over TCP bytes, then message encoding over netstrings: two composable stream adapters. The TODO comments are named as honest gaps. Three distinct TODO blocks appear: lines 34-36 (TODO make requesting configuration easier), line 69 (TODO log assigned port), line 84 and line 145 (TODO log listen and connect addresses). §the-named-todo-comments-as-honest-gaps-in-source recurs (sibling shape to cycle 391 daemon-lore WIP acknowledgment); the source names its own rough edges explicitly. Six citation arcs close on this cycle. Cycle 445 (1, adjacent forward; chat-reference named the three-backend ladder; tcp-netstring closes the TCP rung at the source level). Cycle 392 (2, setup-ws-relay.js is a sibling network backend in the same directory; setup-ws-relay was a runlet that installs a caplet; tcp-netstring is the caplet; the two together show the install/run separation). Cycle 391 (3, daemon-lore named the program-shape vocabulary; make(powers, context) is the module shape the lore did not explicitly name; cycle 446 extends the vocabulary). Cycle 367 (N, Far() for remotable objects; network service is returned as Far). Cycle 321 (N, E() eventual-send recurs). Cycle 326 (N, pure-naming-as-discipline). Pushes citation-arc-closures-in-pivot to NINE-HUNDRED-AND-EIGHT (902 + 6 net new). §one-hundred-thirty-six-cycles-with-named-pivot-domain-stay (310-446). §ninety-fourth-authored-conformant-single-body-section-doc as session-level observation.
