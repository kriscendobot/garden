---
source: endo-but-for-bots--packages-chat-README-md
cycle: 385
lane: designs
ingested: 2026-06-18
package: chat
repo: endo-but-for-bots
branch: llm
cluster: pivot
shape: web-chat-application-on-endo-daemon
shape_subtype: Vite-based-web-GUI-over-CapTP
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 33
---

The 98-line `packages/chat/README.md` on endo-but-for-bots' `llm` branch is the bot-fork's web-based chat application — the GUI sibling of cycle 374's `@endo/cli/test/demo/names-in-transit.js` CLI demo. The single most structurally interesting move is on lines 57-58: "Browser opens WebSocket to daemon's built-in gateway. CapTP Handshake." Cycle 369's @endo/daemon README named "CapTP over netstring message envelopes" as the daemon's communication channel; cycle 385 reveals there is ALSO a CapTP-over-WebSocket layer for browser clients. Same protocol, different transports. §the-named-CapTP-over-WebSocket-extends-CapTP-over-netstring names the move; §the-named-CapTP-as-transport-agnostic-protocol names the tier-3 meta-pattern. Cycle 369's four-layer protocol stack (Unix socket + netstring framing + CapTP + user-agent capabilities) gains a parallel: WebSocket (already self-framing) + CapTP + agent capabilities. The wire format is platform-independent above the transport. The chat application surface has five named tabs. §the-named-five-features-list-inbox-inventory-chat-eval-peers marks lines 11-15: Inbox + Inventory + Chat + Eval + Known Peers. Each a different agent-interaction primitive. §the-named-five-tab-application-surface names the tier-3 meta-pattern. The vocabulary equation deserves its own name. §the-named-inventory-as-pet-names-aka-capabilities marks line 12's parenthetical alias: "Browse and manage pet names (capabilities)". The equation is named openly. §the-named-petname-equals-capability-alias-discipline names the tier-3 meta-pattern; the design treats the petname (the user-facing local name) and the capability (the unforgeable reference) as the same thing at the UI layer, with the petname being the surface form. The petname syntax recurs. §the-named-pet-name-syntax-at-prefix-recurs-from-cli-demo marks lines 86-87's `@pet-name` and `@pet-name:edge-name` syntax — the same exact syntax cycle 374's `names-in-transit.js` used (`@counter:doubler`). The convention spans CLI and web surfaces. §the-named-pet-name-syntax-spans-CLI-and-web names the tier-3 meta-pattern; same wire format, different UIs. §the-named-edge-name-as-named-second-part-of-petname marks the colon-separated form's conceptual structure. The petname is the WHOLE rooted reference; the edge-name is the specific sub-reference along the way. §the-named-edge-name-vs-pet-name-distinction names the tier-3 meta-pattern. The Eval feature is named explicitly. §the-named-Eval-as-named-feature marks line 14: "Evaluate JavaScript expressions with pet name endowments." The chat UI lets users RUN JAVASCRIPT against the daemon's capability graph. §the-named-eval-with-petname-endowments-as-power-user-surface names the tier-3 meta-pattern; the eval feature gives users direct access to compose capabilities via JavaScript rather than only through the predefined UI flows. The dependency list reveals a strong claim. §the-named-ses-as-browser-dependency marks line 98's `ses - Secure ECMAScript (HardenedJS)` dependency. The chat app runs hardened JS in the BROWSER, not just in the daemon. §the-named-hardened-JS-in-browser-too names the tier-3 meta-pattern; the bot-fork's web client inherits the same hardening discipline as the server. The development setup is structurally meaningful. §the-named-Vite-Endo-plugin-as-bootstrap-helper marks lines 27-31's three plugin jobs: ensure daemon running + read gateway address and agent ID + inject connection parameters. §the-named-bundler-plugin-as-daemon-aware-bootstrap names the tier-3 meta-pattern. §the-named-five-step-connection-flow marks lines 56-60: Vite Plugin → WebSocket → CapTP Handshake → AGENT Fetch → UI Initialization. §the-named-bootstrap-as-five-phase-pipeline names the tier-3 meta-pattern. The fifth tab is structurally meaningful. §the-named-known-peers-as-fifth-tab marks line 15: "List all known remote Endo peers and their connection hints." The chat app shows OTHER daemons it knows about. The §the-named-social-network-already-shipping observation from cycle 374 (where the CLI demo showed alice and HOST as peer principals) reaches a GUI counterpart here — the network of peers is a first-class browseable surface in the chat app. §the-named-peer-list-as-named-UI-surface names the tier-3 meta-pattern. The keyboard shortcuts deserve naming. §the-named-three-keyboard-shortcuts marks lines 62-66's three keys: `"` or `'` for chat dialog + `.` for eval dialog + `Escape` to close. §the-named-quote-dot-escape-three-key-ui names the tier-3 meta-pattern; three keys cover the application's primary modes. The development discipline is named. §the-named-development-server-uses-system-daemon marks lines 34-38: the dev server uses your system's Endo daemon at `~/.local/state/endo/`. State persists across dev server restarts; CLI changes reflect in the UI. §the-named-shared-daemon-state-between-CLI-and-web names the tier-3 meta-pattern. The chat app is not a separate runtime, it's a different UI on the same underlying daemon. CLI and web are alternative surfaces over one persistent state. The package's origin is named. §the-named-extracted-from-cli-demo-cat-js marks line 7: "extracts the chat functionality from `packages/cli/demo/cat.js`". The chat package's origin is a CLI demo file. §the-named-package-extracted-from-demo-file names the tier-3 meta-pattern; ad-hoc demo code grew into a packaged surface. Cycle 374's @endo/cli demo directory is precisely where this lineage starts — the same directory whose names-in-transit demo cycle 374 ingested as the social-network-already-shipping evidence. The chat package lifts that demo from CLI-form into a real product. Seven citation arcs close on this cycle. Cycle 384 was the prior chat-lane source; both this cycle and 384 are llm-branch bot-fork surfaces (arc 1). Cycle 374's @endo/cli names-in-transit demo used the same pet-name syntax; cycle 385's chat is the web-GUI sibling to that CLI demo; the social-network-already-shipping observation reaches a GUI counterpart (arc 4). Cycle 369's @endo/daemon CapTP-over-netstring is one transport; cycle 385's CapTP-over-WebSocket is a parallel transport; the new transport-agnostic-protocol framing extends the cycle 369 four-layer stack observation (arc 2). Cycle 367's @endo/exo dependency (arc 6). Cycle 321's @endo/eventual-send / E dependency (arc 10). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 59). Cycle 325's @endo/pass-style dependency (arc 7). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-SEVENTY-FOUR (367 + 7 net new). §seventy-five-cycles-with-named-pivot-domain-stay (310-385). §thirty-third-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-three consecutive cycles (353-385).
