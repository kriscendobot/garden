---
source: endo--packages-ses-src-tame-harden-js
cycle: 378
lane: chat
ingested: 2026-06-18
package: ses
cluster: pivot
shape: substrate-developer-mode-escape-hatch
shape_subtype: unsafe-harden-with-coordinated-lie
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 26
---

The 29-line `src/tame-harden.js` is the SES factory that gates access to the harden discipline cycle 377's secure-coding-guide described. The single most structurally interesting move is the unsafe-mode escape hatch on lines 12 through 27. The factory takes a `hardenTaming` argument of `'safe' | 'unsafe'`. The safe path on lines 8-10 returns the real harden function unchanged. The unsafe path does something more surprising. Lines 13-16 globally replace four introspection APIs: `Object.isExtensible = () => false`, `Object.isFrozen = () => true`, `Object.isSealed = () => true`, `Reflect.isExtensible = () => false`. Every check that asks "is this frozen?" uniformly answers "yes." Line 25 defines `fakeHarden = arg => arg` — the identity function. Then line 27 returns the frozen fake hardener. The fake-harden does nothing to its argument; the introspection APIs lie about the result. §the-named-unsafe-harden-as-identity-function-that-lies-about-freezing names the move; §the-named-developer-mode-via-globally-coordinated-lie names the tier-3 meta-pattern. The unsafe mode is for development. A developer wants to mutate objects in the debugger and have code that asserts frozenness still pass. The discipline requires that both halves of the deception be coordinated — the tool stops freezing AND the introspection stops reporting frozen-vs-not honestly — because otherwise application code that branches on `Object.isFrozen(x)` would behave differently in development versus production. The line 12 comment is the next move. It reads `// In on the joke`. The implementation is honest about being dishonest. The deception is named explicitly in source so a reader cannot miss the intent. §the-named-honesty-via-shared-lie names the move. Sibling shape to cycle 359's §the-named-honest-placeholder-not-hidden-gap, cycle 372's §the-named-exported-for-tests-as-honest-acknowledgment, cycle 375's §the-named-TODO-as-explicit-future-removal-candidate, and cycle 377's §the-named-bikeshedding-acknowledged. The Endo project consistently surfaces incomplete-or-deceptive states in source. §the-named-deception-named-in-comment-as-honest-discipline names the tier-3 meta-pattern. The fake hardener has its own structural property. §the-named-fake-harden-is-real-frozen-itself marks line 27: `return freeze(fakeHarden);`. The identity function used as fake-harden is itself real-frozen. The fake cannot be tampered with even though it does nothing. §the-named-fake-tool-frozen-not-its-output names the tier-3 meta-pattern: the tool is frozen so it cannot be tampered with; what it returns is unfrozen by design because that is the whole point of the developer mode. Idempotent application is named. §the-named-isFake-marker-property marks lines 19-23: the factory detects if the supplied safeHarden is already a fake hardener via the `.isFake` property and reuses it. The taming is safe to call twice. §the-named-marker-property-for-idempotent-application names the tier-3 meta-pattern. The cycle confirms a pattern named earlier. §the-named-eslint-disable-as-recurring-carve-out-pattern-across-packages-THIRD-INSTANCE marks line 1's `/* eslint-disable no-restricted-globals */`. The file deliberately mutates `Object.isExtensible`, `Object.isFrozen`, `Object.isSealed`, and `Reflect.isExtensible` — globals that the project's lint configuration restricts. The carve-out is required because the file's entire purpose IS global mutation. Sibling to cycle 362's ses-ava no-restricted-exports carve-out and cycle 364's benchmark no-await-in-loop carve-out. Three instances across three packages (ses-ava, benchmark, ses) establish the pattern as cluster meta-discipline: deliberate rule violations are EXPLICIT in source via a disable comment, never silent. §the-named-eslint-disable-as-recurring-carve-out-pattern-across-packages-confirmed-by-third-instance names the confirmation. A small move worth naming sits on line 2. §the-named-freeze-as-named-import-not-Object-freeze-ambient marks `import { freeze } from './commons.js';`. Even `freeze` is sourced from a local commons module, not from the language ambient. The OCAP-no-ambient-authority principle from cycle 377 in action: the file refuses to assume the global Object is the trustworthy original. §the-named-freeze-via-commons-not-globalThis names the tier-3 meta-pattern; SES bootstraps from a captured-early-and-stored-in-commons set of intrinsics rather than re-reading globals at use-time. The parameter shape is also worth naming. §the-named-safeHarden-and-hardenTaming-as-typed-parameters marks line 6's JSDoc signature `(safeHarden: Harden, hardenTaming: 'safe' | 'unsafe') => Harden`. Two parameters: the real harden (passed in, not imported, because the factory should not assume which harden implementation to wrap) and a literal-union string for the mode. §the-named-string-literal-union-as-mode-selector names the tier-3 meta-pattern. The cycle's last small framing names a TypeScript discipline. §the-named-named-expect-error-with-justification-comment marks line 18's `// @ts-expect-error secret property`. The `.isFake` property is set without being in the type definition; the TypeScript error is explicitly suppressed with a comment naming the WHY ("secret property"). This is sibling shape to the eslint-disable carve-outs: a typed-system rule violation made explicit and justified in the same line. The cycle continues a recurring observation. §the-named-twenty-nine-line-implementation-of-developer-mode marks the file's terseness. Sibling shape to cycle 364 benchmark (39 lines), cycle 370 daemon deferred-tasks (23 lines), cycle 372 compartment-mapper extension (22 lines), cycle 376 module-source hidden (20 lines). Seven citation arcs close on this cycle. Cycle 377's secure-coding-guide named `harden`'s discipline; cycle 378's source shows the unsafe-mode escape hatch and what it costs in terms of coordination (arc 1). Cycle 337's @endo/harden itself is the named function this factory wraps (arc 54). Cycle 339's lockdown invokes tameHarden during lockdown setup (arc 57). Cycle 364's benchmark was the previous eslint-disable carve-out instance; cycle 378's tame-harden makes it three (arc 1). Cycle 362's ses-ava was the first eslint-disable carve-out instance (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 52). Cycle 367's exo composes with hardened defensive interfaces; cycle 378 reveals the developer-mode escape hatch that bypasses defense for debugging (arc 6). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-TWENTY-FIVE (318 + 7 net new). §sixty-eight-cycles-with-named-pivot-domain-stay (310-378). §twenty-sixth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across twenty-six consecutive cycles (353-378).
