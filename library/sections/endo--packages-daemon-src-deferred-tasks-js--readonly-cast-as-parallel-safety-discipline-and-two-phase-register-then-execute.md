---
source: endo--packages-daemon-src-deferred-tasks-js
cycle: 370
lane: chat
ingested: 2026-06-18
package: daemon
cluster: pivot
shape: tiny-internal-utility
shape_subtype: two-phase-register-then-execute
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 18
---

The 23-line `deferred-tasks.js` is one small utility inside the @endo/daemon package's roughly eight-thousand-line source tree. The file defines `makeDeferredTasks` — a factory that returns an object with two methods, `push` and `execute`. Push records a task; execute runs all recorded tasks in parallel via Promise.all and awaits their joint completion. The structurally interesting move sits on line 16: `tasks.map(task => task(/** @type {Readonly<T>} */ (param)))`. The Readonly cast is on the SAME parameter that every task receives. The cast does not freeze the parameter at runtime; the cast tells the type checker that no task should mutate the value. §the-named-readonly-cast-as-parallel-safety-discipline names the single most structurally interesting move; §the-named-type-level-defense-against-parallel-mutation names the tier-3 meta-pattern. The defense is at the type-system level rather than the runtime level. The runtime allows mutation; the type system rejects it. This is consistent with the broader @endo discipline of treating the type checker as a source of authority alongside the runtime. The two-method API names a staged composition discipline. §the-named-two-phase-register-then-execute names the push-then-execute pattern; §the-named-batch-execution-at-commit-time names the tier-3 meta-pattern. Tasks accumulate during one phase (registration); all tasks run together at the commit point (execute). The pattern recurs through the @endo cluster at different scales. Cycle 368's exo taxonomy named prepare* as a fourth-verb-pattern that restores durable objects from baggage on the first crank — register-then-execute scaled up to the vat-incarnation timescale. Cycle 366's skel test injected helpers as prepared state passed via the test function's `t` argument — register-then-execute scaled to the test-fixture timescale. Cycle 362's ses-ava prepare-endo.js mutated env vars additively during module load — register-then-execute at the cross-package-config timescale. Cycle 370's deferred-tasks is the smallest known instance: register-then-execute as a 23-line utility. The fan-out via Promise.all deserves its own naming. §the-named-parallel-isolated-task-execution names the parallelism; §the-named-fan-out-via-Promise-all names the tier-3 meta-pattern. Each task receives the same parameter but cannot read other tasks' state because Promise.all runs them concurrently. The Readonly cast and the parallel execution are linked: the cast is necessary because parallel execution makes any mutation a data race. Several smaller framings deserve naming. §the-named-ts-check-pragma-as-opt-in-checking marks line 1's `// @ts-check`, sibling to cycle 369's observation in check-bundle/json.js. The JavaScript file opts into TypeScript checking for this file alone, without converting to TypeScript syntax. §the-named-JSDoc-import-syntax marks line 3's `@import { DeferredTasks, DeferredTask } from './types.js'`. This is TypeScript 5.5+ JSDoc syntax for type-only imports without runtime import statements; the types come from `./types.js` but no runtime dependency on that file is introduced. §the-named-type-only-import-without-runtime-import names the tier-3 meta-pattern. §the-named-template-generic-T-constrained-to-Record marks the `@template {Record<string, string | string[]>} T` constraint, which preserves the parameter shape across the push/execute boundary while constraining the universe of allowed shapes. §the-named-constrained-generic-parameter names the tier-3 meta-pattern. §the-named-factory-returning-execute-and-push-pair marks the tiny two-method API; §the-named-tiny-two-method-API names the tier-3 meta-pattern, sibling to cycle 364's two-function-API in benchmark. The relationship between cycle 369's README and cycle 370's source deserves its own framing. The daemon README is 14 lines and describes the daemon at the operational layer (it is a daemon, it manages workers, CapTP protocol, user agent API). The deferred-tasks.js source is 23 lines and operates at the engineering-utility layer. The two files are at different LAYERS of abstraction, and the source is not on the README's narrative path. §the-named-twenty-three-line-utility-from-thousands-of-lines-of-daemon names the move; §the-named-fragmentary-HOW-from-minimal-README-shape names the tier-3 meta-pattern. The WHAT-VS-HOW shape (established for cycle 361→362 ses-ava) has a variant when the README is minimal and the implementation is vast: the source is one fragment of the HOW rather than a complete realization. The four-shapes framing applies, but with this nuance about fragmentary realization. Seven citation arcs close on this cycle. Cycle 369 was the prior README; the source is orthogonal to the README's operational narrative but is fragmentary HOW from minimal README (arc 1). Cycle 368's exo taxonomy named the prepare-as-fourth-verb-pattern; deferred-tasks is the same staged-composition idea scaled to a 23-line utility (arc 1). Cycle 366's skel test injected helpers via dependency-as-prepared-state; cycle 370's deferred-tasks is push-then-execute at the runtime layer, sibling shape of staged-composition (arc 1). Cycle 364's benchmark had a two-function-API; cycle 370's deferred-tasks is also a two-method-API (arc 1). Cycle 362's ses-ava env-mutation via additive-augment was another staged-composition; deferred-tasks generalizes the idea into a reusable utility (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 43). Cycle 322's @endo/errors README is not used here because the utility is so small it does not need error decoration (arc 44). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-SIXTY (253 + 7 net new). §sixty-cycles-with-named-pivot-domain-stay (310-370). §eighteenth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across eighteen consecutive cycles (353-370). The §the-named-staged-composition-recurs-across-scales is a session-level observation: the same idea (register then execute) appears at five different scales across the cluster, from cross-package config (cycle 362) to vat incarnation (cycle 368) to runtime utility (cycle 370).
