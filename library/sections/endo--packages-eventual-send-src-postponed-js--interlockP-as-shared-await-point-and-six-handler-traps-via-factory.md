---
title: "@endo/eventual-send src/postponed.js — interlockP as shared await point; six-handler-traps via factory; async-bootstrap-via-queue; return-tuple-not-object"
source: endo--packages-eventual-send-src-postponed-js
url: https://github.com/endojs/endo/blob/master/packages/eventual-send/src/postponed.js
authors: [Mark S. Miller, Turadg Aleahmad, Endo project (collective)]
repo: endojs/endo
path: packages/eventual-send/src/postponed.js
total-lines: 46
ingest-cycle: 354
ingest-date: 2026-06-16
lane: chat
section-tags:
  - the-named-interlockP-as-shared-await-point
  - the-named-six-handler-traps-via-factory
  - the-named-three-operations-times-two-modes-equals-six-traps
  - the-named-async-bootstrap-via-queue
  - the-named-donePostponing-callback-resolves-shared-interlock
  - the-named-return-tuple-not-object
  - the-named-Required-handler-type-no-optional-methods
  - the-named-ts-expect-error-2454-with-named-cause
  - the-named-assert-without-condition-as-invariant-check
  - the-named-makePostponedOperation-as-factory-for-handler-methods
  - the-named-second-conformant-single-body-section-doc
  - forty-five-cycles-with-named-pivot-domain-stay
  - one-hundred-sixty-three-citation-arc-closures-in-pivot-now
---

@endo/eventual-send src/postponed.js is a 46-line file by Turadg Aleahmad (2024-10-17) implementing the postponedHandler that delays handler operations until a setup callback fires. Cycle 354 is the forty-fifth consecutive non-garden source after the pivot (310-354), continuing §forty-five-cycles-with-named-pivot-domain-stay. This is the SECOND conformant single-body section doc authored after the cycle 352-followup systematic refactor (cycle 353 was the first). The doc's single most structurally interesting move is §the-named-interlockP-as-shared-await-point: a single shared Promise `interlockP = new Promise(resolve => { donePostponing = () => resolve(undefined); })` gates all six handler traps. The `donePostponing()` callback resolves the interlock once; from that moment onward, all queued operations can proceed. Tier-3 framing: when multiple async operations need to wait for the SAME initialization event, share ONE Promise rather than each making its own. The shared interlock is more efficient (one Promise resolution wakes all subscribers) AND clearer (the discipline is named at one place rather than scattered). §the-named-donePostponing-callback-resolves-shared-interlock — first-explicit-observation. §the-named-async-bootstrap-via-queue — first-explicit-observation as a tier-3 meta-pattern. When messages arrive before a handler is ready, postpone them by chaining off a shared interlock Promise; once the handler is ready, calling donePostponing() releases all queued operations in their original arrival order (because Promise resolution preserves microtask ordering). §the-named-six-handler-traps-via-factory — first-explicit-observation as a tier-3 meta-pattern. The postponedHandler has SIX methods: get + getSendOnly + applyFunction + applyFunctionSendOnly + applyMethod + applyMethodSendOnly. Rather than writing six similar method bodies, the file defines a single `makePostponedOperation(opName)` factory that closes over interlockP and HandledPromise; each method is created by calling the factory with its operation name. §the-named-three-operations-times-two-modes-equals-six-traps — first-explicit-observation. The six traps factor into THREE operations (get / applyFunction / applyMethod) crossed with TWO modes (regular / sendOnly). §the-named-makePostponedOperation-as-factory-for-handler-methods — when a handler implements N similar methods differing only by a parameter, generate them via a factory function rather than writing N copies. §the-named-return-tuple-not-object — first-explicit-observation. The factory returns `[postponedHandler, donePostponing]` (a 2-element array tuple), not `{ postponedHandler, donePostponing }` (an object). Tier-3 framing: when returning two related but distinctly-typed values where the caller will destructure them, prefer a tuple over an object for ergonomic positional destructuring. Compare to cycle 335 @endo/promise-kit's §the-named-three-named-returns (which uses object destructuring for `{ promise, resolve, reject }`); the choice between tuple and object depends on whether the elements have natural NAMES (object) or natural POSITIONS (tuple). §the-named-Required-handler-type-no-optional-methods — first-explicit-observation. The TypeScript signature `Required<import('./types.js').Handler<any>>` declares that the postponedHandler implements ALL handler operations (none are optional). Required<> forces every property of the type to be non-optional. The discipline: when a handler interface has optional methods, mark explicit implementations as Required<> so callers know they get a complete handler. §the-named-assert-without-condition-as-invariant-check — line 41 `assert(donePostponing)` is an invariant check that the closure-captured variable was indeed assigned. The `// @ts-expect-error 2454` suppresses TypeScript's complaint that donePostponing might not have been assigned by this line (TypeScript can't see that the Promise executor runs synchronously before this assert). §the-named-ts-expect-error-2454-with-named-cause — first-explicit-observation. Compare to cycle 187's `@ts-expect-error 2454` for HandledPromise's synchronous executor invariant — both name the same TypeScript error code at the same kind of synchronous-executor-runs-before-this-line site. §two-cycles-with-named-ts-expect-error-2454 (187 + 354) — first-explicit-observation as a tier-2 multi-cycle pattern. The synthesis-target for a slot machine library §`@game/eventual-send/src/postponed.js` would be: use a single shared interlock Promise gating multiple async operations; generate similar handler methods via a factory function; return tuples (not objects) when destructuring positionally; use Required<> in TypeScript to declare complete handler implementations; name the same TypeScript error code at synchronous-executor-runs-before-this-line sites with `@ts-expect-error 2454`. Cycle 354 closes citation arcs with: cycle 353 (1 cycle; cross-package) + cycle 187 (167 cycles; postponed.js mentioned in shim cluster with §interlockP-as-shared-await-point and §six-handler-traps both originally observed; cycle 354 is the implementation-level closure of those references) + cycle 130 (224 cycles; message-breakpoints sibling in eventual-send) + cycle 132 (222 cycles; local.js sibling — receiver-side dispatcher) + cycle 146 (208 cycles; E.js sibling — send-side dispatcher; postponedHandler is what gets installed before the real handler is ready) + cycle 152 (202 cycles; memo-race.js promise-kit sibling using shared promise for memoization). §six-citation-arc-closures-in-cycle-354 contributes to §one-hundred-sixty-three-citation-arc-closures-in-pivot-now (158 + 5 net new). The library state after cycle 354: §library-reaches-866-sections from 396 source documents, §forty-five-cycles-with-named-pivot-domain-stay (310-354), §nineteen-named-packages-in-the-pivot-cluster, §the-named-interlockP-as-shared-await-point established as tier-3 meta-pattern, §the-named-async-bootstrap-via-queue established as tier-3 meta-pattern, §the-named-six-handler-traps-via-factory established as tier-3 meta-pattern, §the-named-return-tuple-not-object established as tier-3 meta-pattern, §the-named-Required-handler-type-no-optional-methods established as tier-3 meta-pattern, §the-named-second-conformant-single-body-section-doc-authored-in-post-refactor-era-as-eleventh-complementary-lens-re-ingest — second authored-conformant section in the post-refactor era; the new authoring discipline is now consistent. Authored by Mark S. Miller + Turadg Aleahmad + Endo project (collective).
