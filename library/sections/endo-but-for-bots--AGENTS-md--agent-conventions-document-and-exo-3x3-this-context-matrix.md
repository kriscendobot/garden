---
source: endo-but-for-bots--AGENTS-md
cycle: 387
lane: designs
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: root
cluster: pivot
shape: agent-conventions-document
shape_subtype: typescript-and-exo-and-commit-conventions
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 35
---

The 92-line `AGENTS.md` at the endo-but-for-bots/llm repo root is the repository's machine-readable conventions document for AI agents working in the codebase. The single most structurally interesting move is the file's existence. §the-named-AGENTS-md-as-agent-conventions-document marks the move: the repo has machine-readable conventions for agent contributors at the root. Sibling shape to cycle 383's evoke/SOUL.md (agent discipline at the workflow layer) and the project-root CLAUDE.md (Claude-specific). The TWO root-level agent-conventions documents differ in audience: CLAUDE.md addresses Claude specifically; AGENTS.md addresses agents generically. §the-named-claude-and-agents-md-pair-with-different-audiences names the tier-3 meta-pattern; the bot-fork has TWO agent-conventions documents at different specificity levels, with a more general document and a more specific one running together. The TypeScript section is the document's longest and most structurally rich. §the-named-types-index-convention marks lines 25-38: each package that exports types uses a PAIR of files. `types-index.js` for runtime re-exports of values with enhanced type signatures (M, matches, mustMatch). `types-index.d.ts` for pure type re-exports only — no type definitions allowed in `.d.ts` files. §the-named-types-index-js-and-d-ts-pair names the tier-3 meta-pattern; the convention has a sharp rule about which file holds which kind of content. The reason is named explicitly. §the-named-d-ts-files-silently-pass-but-ts-files-checked marks lines 30-31: "`.d.ts` files are not checked by `tsc` (we use `skipLibCheck: true`). Type definitions in `.d.ts` files silently pass even if they contain errors. Definitions in `.ts` files are checked." The file-extension's checking semantics directly shape where definitions belong. §the-named-typescript-checking-asymmetry-drives-convention names the tier-3 meta-pattern. The bundler constraint is named. §the-named-no-ts-in-runtime-bundles-rule marks lines 17-19: "Never use `.ts` files in modules that are transitively imported into an Endo bundle. The Endo bundler does not understand `.ts` syntax. We avoid build steps for runtime imports." §the-named-bundler-constraint-forces-js-runtime names the tier-3 meta-pattern; the bundling pipeline shapes the source-file-extension discipline. The build configuration enforces the discipline. §the-named-emit-declaration-only marks line 53's `emitDeclarationOnly: true` in `tsconfig-build-options.json`. tsc generates `.d.ts` only, never `.js`. The build config enforces the runtime/types separation. §the-named-build-config-enforces-source-discipline names the tier-3 meta-pattern. The JSDoc import syntax is named explicitly. §the-named-JSDoc-import-without-runtime-load marks lines 56-60: `/** @import { Pattern, MatcherNamespace } from './types.js' */` for type-only imports. Cycle 376's @endo/module-source DESIGN named this JSDoc syntax; AGENTS.md elevates it to documented convention for the whole repo. §the-named-jsdoc-import-as-documented-convention names the tier-3 meta-pattern. The exo section gives a structural revelation. §the-named-exo-this-context-table marks lines 64-78's table showing which `this` fields are available across the three exo APIs. Cycle 367 named the §the-named-this-state-this-self-this-facets triple; AGENTS.md gives the matrix. The three APIs (makeExo, defineExoClass, defineExoClassKit) cross the three this-fields (self, facets, state) to form a 3×3 table where 6 of 9 cells are filled. makeExo has self only. defineExoClass has self + state. defineExoClassKit has facets + state. §the-named-exo-three-by-three-this-context-matrix names the tier-3 meta-pattern. The pattern visible in the matrix is that singular APIs get singular self and multi-facet APIs get facets-not-self. §the-named-why-no-self-on-kits-explanation marks lines 72-73 giving the reasoning: "A kit has multiple facets (e.g. `public`, `admin`), each a separate remotable object. There is no single 'self'. Use `this.facets.facetName` to access any facet in the cohort." The MULTIPLE FACETS per kit means there's no singular self to bind. §the-named-multi-facet-precludes-singular-self names the tier-3 meta-pattern. The mutual-exclusion discipline is named. §the-named-never-mix-self-and-facets-in-same-context marks line 78's rule for type writers: the two fields are mutually exclusive per context-type declaration. §the-named-mutually-exclusive-binding-fields-must-not-be-mixed names the tier-3 meta-pattern. The commit conventions are named precisely. §the-named-conventional-commits-with-package-scope marks lines 90-91: `feat(pkg):`, `fix(pkg):`, `refactor(pkg):`, `chore:`, `test(pkg):`. Conventional Commits format with package scope in the prefix. §the-named-package-scope-in-commit-prefix names the tier-3 meta-pattern; the package name appears IN the commit prefix (not buried in the body), so the change scope is visible in `git log --oneline`. The file-conversion discipline is named. §the-named-file-conversion-as-own-refactor-commit marks line 92: "File conversions (`.js` to `.ts`) get their own `refactor:` commit." §the-named-cosmetic-changes-isolated-in-own-commit names the tier-3 meta-pattern; sibling shape to cycle 380's bundle-source-patched-because-it-depends — both are about keeping commits clean of unrelated churn. The document's size is itself worth marking. §the-named-ninety-two-line-agent-conventions-document marks the file's compactness: a substantial set of agent conventions in 92 lines. Sibling shape to cycle 369 daemon README, cycle 365 skel README, cycle 363 benchmark README — all carry substantial content in modest line counts. §the-named-minimal-readme-for-substantial-system extends to AGENTS.md. Seven citation arcs close on this cycle. Cycle 386 was the prior chat-lane source; cycle 387 is the agent conventions doc for the whole repo (arc 1). Cycle 383's evoke/SOUL.md was agent discipline at the workflow layer; AGENTS.md is at the code-style layer; the two complement (arc 1). Cycle 367's @endo/exo this-context triple from cycle 367 gets its 3×3 matrix here, with the missing-cell pattern revealed (arc 8). Cycle 376's JSDoc @import convention named explicitly in AGENTS.md as documented convention (arc 2). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 61). Cycle 327's @endo/patterns M is used in the types-index examples (arc 7). Cycle 322's @endo/errors is not directly named in AGENTS.md but is the substrate the conventions presume (arc 61). The cycle pushes citation-arc-closures-in-pivot to THREE-HUNDRED-EIGHTY-EIGHT (381 + 7 net new). §seventy-seven-cycles-with-named-pivot-domain-stay (310-387). §thirty-fifth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-five consecutive cycles (353-387).
