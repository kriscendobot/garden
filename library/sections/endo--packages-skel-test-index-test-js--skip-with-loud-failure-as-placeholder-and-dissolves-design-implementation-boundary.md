---
source: endo--packages-skel-test-index-test-js
cycle: 366
lane: chat
ingested: 2026-06-16
package: skel
cluster: pivot
shape: skeleton-test-as-template-default
shape_subtype: skip-with-loud-failure-placeholder
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 14
---

The 6-line `test/index.test.js` is the skel template's default test. Reading it line by line reveals two layered placeholder moves and one framing-level observation. Line 1 reads `import test from '@endo/ses-ava/prepare-endo.js';`, which is LITERALLY the canonical-import-pattern that cycle 361's @endo/ses-ava README recommended and cycle 362's prepare-endo.js implements. The skel test is the canonical import made manifest. Reading skel/test/index.test.js answers the question "what does the canonical import look like in practice?" §the-named-skel-test-shows-the-canonical-import names the move. Every derivative inherits this import line unchanged, which means the @endo/ses-ava ecosystem propagates through Endo via the skel template: a new @endo/* package starts with ses-ava as its test driver because skel does. §the-named-template-import-as-default-inheritance names the inheritance pattern; §the-named-template-propagation-of-testing-substrate names the tier-3 meta-pattern. The structurally interesting move is the placeholder body. Lines 3 through 5 read `test.skip('placeholder', async t => { t.fail('TODO: add tests'); });`. Two complementary moves are layered in this six-token construct. The `.skip` modifier means CI runs the test but does not fail on it; the test passes silently. The body `t.fail('TODO: add tests')` is what happens when the derivative REMOVES the `.skip`: immediate, loud failure with a specific TODO message naming the gap. §the-named-skip-with-loud-failure-as-placeholder names the single most structurally interesting move. The template designs for the moment when the derivative is ready to add real tests: removing `.skip` produces a louder signal than an empty test body would. §the-named-fail-loud-not-silent-pass names the tier-3 meta-pattern. A common alternative would be `test.skip('placeholder', () => {})` — silently empty body. The skel chose to make the placeholder loud once activated. The cycle's framing-level observation is sharper than the file's own moves. The four-shapes-of-design-vs-implementation-arc-from-README-to-source framing was established in cycle 364 with four instances: ABSTRACTING (cycle 357→358 zip), UNDERCOUNTING (cycle 359→360 eslint-plugin), WHAT-VS-HOW (cycle 361→362 ses-ava), and ANALOGY-VS-LITERAL (cycle 363→364 benchmark). The skel cycle 365→366 pair does not fit any of the four shapes. The skel README has no description to abstract, no enumeration to undercount, no what-to-pair-with-how, no analogy to ground. The README is a placeholder; the package.json is a blueprint; the test is a skip-marked loud-fail placeholder; the index.js is empty. EVERYTHING in skel is template. There is no design-vs-implementation relationship to map. §the-named-skel-dissolves-design-vs-implementation-boundary names the observation; §the-named-template-dissolves-design-implementation-distinction names the tier-3 meta-pattern. The four-shapes framing has a boundary case here that does NOT add a fifth shape. It reveals what the framing presumes: that a README attempts to describe an implementation that lives elsewhere. When the README and the source are co-blueprint, the framing's premise dissolves. §the-named-framing-has-a-dissolves-the-distinction-boundary-case names the tier-3 framing observation. This is parallel to cycle 363's earlier "outside-the-framing" observation (about benchmark's README delegating to AVA-conventions), but with a different resolution. Cycle 364 promoted cycle 363's outside-case to a fourth shape (ANALOGY-VS-LITERAL); cycle 366 does NOT promote the skel case to a fifth shape because the dissolution is genuine — the template's design IS its implementation. Two smaller moves deserve naming. §the-named-six-line-skel-test names the minimalism that mirrors the README's three-line minimalism; the package's design density is in the package.json, not in README or test. §the-named-async-t-arrow-as-template-default names the choice to write `async t => {...}` even though the placeholder body is synchronous. The derivative is expected to write async tests (consistent with Endo's promise-heavy substrate); the template sets the default to async to nudge the derivative in that direction. §the-named-default-shape-as-nudge-toward-convention names the tier-3 meta-pattern. Seven citation arcs close on this cycle. Cycle 365 was the prior skel README; the forward pair completes and reveals the dissolves-the-distinction boundary case (arc 1). Cycle 362's @endo/ses-ava prepare-endo.js is the LITERAL IMPORT on line 1 of skel's test (arc 1). Cycle 361's @endo/ses-ava README named the canonical import pattern; cycle 366 shows it as the actual import in skel (arc 1). Cycle 360's four-shapes framing finds its dissolves-the-distinction boundary case (arc 1). Cycle 358's first design-vs-implementation-arc instance now sits inside a framing that has both four shapes AND a boundary case that dissolves the distinction (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 39). Cycle 322's @endo/errors README is not used here either, consistent with the testing-minimalism the skel inherits (arc 43). The cycle pushes citation-arc-closures-in-pivot to TWO-HUNDRED-THIRTY-FOUR (227 + 7 net new). §fifty-six-cycles-with-named-pivot-domain-stay (310-366). §fourteenth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across fourteen consecutive cycles (353-366).
