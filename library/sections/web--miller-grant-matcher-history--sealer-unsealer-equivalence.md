---
title: "It's not so simple: EQ and Sealer/Unsealer pairs are mutually constructible"
source_kind: web
source_url: https://erights.org/elib/equality/grant-matcher/history.html
source_content_sha256: 192e45c43ca28de5ec06b15dbb9ed316b18922cd3b81678dbdc3d8257f0c4c8e
source_authors: [Mark S. Miller]
source_date: 2000-01-01
ingested: 2026-06-27
ingested_by: scholar
topics: [capability-theory, capability-security]
status: current
---

The "It's not so simple" coda: the cost of foregoing `EQ` is now understood and seen to be *fundamental* — for this cake, having and eating are mutually exclusive — but the controversy is subtler than "you need `EQ`". Dean Tribble demonstrated that the Grant Matcher Puzzle can be solved with **Sealer/Unsealer pairs** *without* `EQ`. In fact an `EQ` adequate to do grant matching can be built purely out of Sealer/Unsealer pairs (and plausibly out of other rights-amplification primitives, like sibling communication). Conversely, *Logical Secrets* had shown that Sealer/Unsealer pairs can be built (awkwardly) out of `EQ`. So a capability system enhanced with **either** primitive is adequate to build the other; the two are inter-constructible. Miller's remaining hypothesis is that in a *pure* capability system with **neither**, one cannot build either — a standing challenge. This is the in-corpus root of why Endo's [[brand-and-trademark]] family (the sealer/unsealer / rights-amplification primitives) and a pass-invariant equality primitive are two faces of the same expressive power.

The controversy remains, but the cost of foregoing `EQ` is finally understood, and seen to be a fundamental cost. The controversy over the cost of having `EQ` vs the cost of not having `EQ` remains. For this cake, having and eating are now known to be mutually exclusive.

*It's not so simple.* The [e-lang thread rooted here] tells of Dean demonstrating that the Grant Matcher puzzle can be solved with Sealer/Unsealer pairs without `EQ`. Indeed, an `EQ` adequate to do grant matching can be built purely out of Sealer/Unsealer pairs (and plausibly out of other rights amplification primitives, like sibling communication). In *Logical Secrets*, we had shown that Sealer/Unsealer pairs could be built (awkwardly) out of `EQ`. So a capability system enhanced with either of these primitives is adequate to build the other. My remaining hypothesis is that in a pure capability system without either, one cannot build either. Dean (and everyone else of course) is hereby challenged to demonstrate otherwise.

Source: [History of the Grant Matcher](https://erights.org/elib/equality/grant-matcher/history.html) § It's not so simple, Mark S. Miller, erights.org; ingested from the Internet Archive original-bytes capture, content SHA-256 `192e45c4`.
