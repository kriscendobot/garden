---
source: endo-but-for-bots--packages-lal-primer-messaging-md
cycle: 413
lane: designs
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: agent-facing-messaging-reference
shape_subtype: style-as-surface-boundary
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 61
---

The 97-line messaging.md is the agent-facing reference for message handling inside @endo/lal's primer corpus and the fifteenth lal-package artifact in the cluster. Cycle 413's most structurally interesting move extends cycle 405's three-surfaces-three-audiences framing into a new dimension. §the-named-style-as-part-of-surface-boundary names the observation. Lines 83-97 ("Communication Style" section) prescribe what the agent must OMIT from user-facing messages: internal tool calls, pet name choices, directory operations, retries, errors, technical details about locators or formula IDs or the message protocol. The agent must "try a different approach silently" and, when reporting failure, "frame it in terms of what you cannot do, not why." Cycle 405 named three surfaces for three audiences (LLM tool catalog ⊆ guest-power method surface ⊆ user-facing Chat surface) and noted that each surface had a different SHAPE — which methods were exposed to which audience. Cycle 413's messaging.md reveals that the surfaces differ not just in API SHAPE (which methods are exposed) but in STYLE and VOCABULARY (which content is appropriate). The agent must respect the surface boundary even in PROSE, not just in tool exposure. §the-named-surface-boundary-includes-style-not-just-API names the tier-3 meta-pattern. The agent's three-surface topology now spans three dimensions: API shape, content vocabulary, and prose style. §the-named-three-surface-styles-not-just-three-surface-APIs names the deeper structure. The reply-vs-send distinction is refined from preference to requirement. §the-named-reply-vs-send-as-required-distinction marks lines 41-44 and 52-54. Cycle 407 named reply() as PREFERRED over send() for responses. Cycle 413 strengthens: "ALWAYS use reply(messageNumber, ...) to respond — this threads the response to the original message. Do NOT use send() for responses — send() is only for initiating brand new conversations." And later: "You MUST reply to every message you RECEIVE (where from is not yourself)." Not preference, REQUIREMENT. §the-named-mandatory-reply-for-responses-mandatory-send-for-initiation names the tier-3 meta-pattern. The cluster's reply-vs-send framing is now a hard rule. The agent loop iteration shape is canonicalized. §the-named-five-step-message-processing-workflow marks lines 21-46. (1) locate(["@self"]) to get own locator; (2) listMessages() to see all messages (sent + received); (3) check both from AND to fields against the @self locator; (4) for received messages: adopt values + reply + dismiss; (5) proceed to the next message. §the-named-canonical-agent-loop-iteration-shape names the tier-3 meta-pattern; this is the canonical shape of one iteration of the agent's message-following loop, distilled into five steps. The mental model is constrained. §the-named-messages-as-data-not-directory-entries marks lines 3-13. "listMessages() returns message objects... messages are not named things in your directory. Do NOT try to lookup() message fields." §the-named-data-not-addressable-entity names the tier-3 meta-pattern; messages live in a different conceptual space from named capabilities. The agent must not confuse the two. The message shape is nine-fielded. §the-named-nine-field-message-object marks line 5-6: number + date + from + to + type + strings + names + messageId + replyTo. Confirms cycle 407's mention of messageId + replyTo. §the-named-canonical-message-object-shape names the tier-3 meta-pattern. The no-prose-only-tool-calls restraint is restated. §the-named-no-prose-only-tool-calls-restated marks lines 17-19. Confirms cycle 401's framing in the primer's LLM-facing voice. The adopt mechanism preserves both names. §the-named-adopt-preserves-sender-edge-name-and-creates-own-pet-name marks lines 32-40. The adopt() function takes the sender's edge name AND the receiver's chosen pet name — two distinct names, one per side of the trust boundary. §the-named-dual-naming-across-trust-boundary names the tier-3 meta-pattern. Message numbers are BigInts via SmallCaps. §the-named-message-number-as-smallcaps-BigInt marks line 38's `adopt("+3", "counter", "my-counter")`. The "+3" is SmallCaps for the BigInt 3n. Cycle 401's design doc named SmallCaps encoding; cycle 413 sees the LLM-facing syntax in action. §the-named-message-numbers-as-BigInt-via-smallcaps names the tier-3 meta-pattern. The bidirectional inbox requires a self-filter. §the-named-self-message-detection-as-safety-check marks lines 48-50: "The message list contains your own sent messages too! Always check if you are the sender before trying to reply to a message — you don't want to reply to yourself." Cycle 407's bidirectional-inbox observation now has a SAFETY consequence — the agent must filter own sends. §the-named-bidirectional-inbox-requires-self-filter names the tier-3 meta-pattern. The reply+dismiss pair is mandatory. §the-named-mandatory-reply-and-dismiss-pair marks lines 52-55: "You MUST reply to every message you RECEIVE... Always dismiss messages after handling them — this is essential for proper operation." §the-named-receive-reply-dismiss-as-required-triple names the tier-3 meta-pattern; cycle 403's canonical-conversation-as-reply-and-dismiss is now refined to MANDATORY operation. The message text format interleaves strings with edge references. §the-named-interleaved-strings-and-edge-references marks lines 70-75. `reply("+5", ["Here is ", " as requested."], ["result"], ["my-result"])` produces recipient text "Here is @result as requested." Strings array interleaves with edgeName array. §the-named-strings-array-as-text-segments-around-edge-names names the tier-3 meta-pattern; the message rendering is template-style. The edge name vs pet name distinction is confirmed at the messaging level. §the-named-edge-name-as-sender-label-pet-name-as-receiver-label marks line 73-74: "Recipient sees: 'Here is @result as requested.' They can adopt @result to get the value named 'my-result'." The @-reference in text is the EDGE NAME (sender's label); the receiver picks a PET NAME via adopt(). §the-named-edge-name-and-pet-name-distinction-confirmed-at-messaging-level names the tier-3 meta-pattern; sibling to cycle 386's petname-edgename-naming-inversion framing — now confirmed at the messaging protocol level. The cluster has come full circle: cycle 386 first identified the edge/pet name distinction; cycle 413 closes the arc with the protocol-level use of those names. The agent's exploration is silent. §the-named-silent-retry-discipline marks line 95: "try a different approach silently." §the-named-visible-success-invisible-process names the tier-3 meta-pattern. Failures are framed by limit, not by reason. §the-named-failure-framed-as-what-not-why marks lines 96-97: "frame it in terms of what you cannot do, not why." When the agent must report failure, it expresses the LIMIT (what cannot be done) rather than the REASON (internal mechanics). §the-named-what-not-why-as-failure-framing names the tier-3 meta-pattern. The IMPORTANT marker recurs. §the-named-IMPORTANT-marker-used-twice-in-this-document marks lines 17 and 48. Confirms cycle 407's framing of the IMPORTANT-uppercase-as-LLM-priority-signal. §the-named-IMPORTANT-as-distributed-attention-affordance names the tier-3 meta-pattern; the primer uses the marker consistently as an attention-allocation cue for the LLM. The cycle's session-level observation continues. §the-named-sixty-one-conformant-cycles-and-counting marks the session-level observation: sixty-first AUTHORED conformant single-body section doc in post-refactor era (cycles 353-413). §one-hundred-and-three-cycles-with-named-pivot-domain-stay (310-413). Nine citation arcs close on this cycle. Cycle 412 (1, adjacent forward; the provider boundary handles messages; the message protocol cycle 413 names is what the provider serializes between the common format and the LLM API). Cycle 407 (5, multiple cluster message-related framings refined here: reply-preferred-over-send now MANDATORY; inbox-as-bidirectional-journal now has safety implications; canonical-conversation-as-reply-and-dismiss now mandatory triple). Cycle 405 (5, three-surfaces-three-audiences extended to three-styles-three-vocabularies — surface boundary includes STYLE not just API). Cycle 386 (3, edge-vs-pet name distinction confirmed at messaging protocol level — full circle from cycle 386's petname-edgename framing to messaging protocol implementation). Cycle 401 (3, SmallCaps BigInt encoding confirmed in LLM-facing syntax; no-prose-only-tool-calls restated). Cycle 403 (3, reply-and-dismiss canonical pattern refined to mandatory). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 346 (3, name-aliasing for from/to field convention). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-NINETY-SEVEN (588 + 9 net new). The library has crossed 925 sections; the post-refactor era continues at sixty-one consecutive conformant cycles; the cluster's surface vocabulary now spans three dimensions for each surface tier — API shape (cycle 405), content vocabulary (cycle 413's omit-internal-mechanics), and prose style (cycle 413's silent-retry, what-not-why). The agent must respect the surface boundary along all three dimensions, not just one. The cluster's drift framings continue to refine prior framings into more precise structures: cycle 386's petname-edgename arc, opened many cycles ago, is closed in cycle 413's protocol-level use; cycle 407's reply-vs-send preference, named just five cycles ago, is refined to a mandate.
