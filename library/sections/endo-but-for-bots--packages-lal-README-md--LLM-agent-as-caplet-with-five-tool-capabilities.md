---
source: endo-but-for-bots--packages-lal-README-md
cycle: 399
lane: designs
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: LLM-agent-caplet
shape_subtype: tool-call-to-capability-bridge
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 47
---

The 50-line README for `@endo/lal` describes a concrete LLM-powered agent that runs as an unconfined `@endo/daemon` plugin. The single most structurally interesting move is the package's role as a CONCRETE INSTANCE of the agent-as-caplet shape that cycle 391's daemon-lore named in the abstract. §the-named-lal-as-LLM-agent-caplet-with-five-tool-capabilities names the move. The agent uses LLM tool calls to interact with the daemon via FIVE named capability surfaces: (1) manage pet names (list/lookup/remove/move/copy); (2) send and receive messages; (3) adopt capabilities from messages; (4) request capabilities from its host; (5) inspect capabilities via help() methods. §the-named-LLM-agent-as-named-caplet-shape names the tier-3 meta-pattern. LLM agents are caplets that happen to use LLM internally for decision-making, parameterized over the daemon's capability graph. The structural relationship between LLM tool-calls and daemon capability invocations is the bridge. §the-named-tool-calls-as-named-capability-invocations marks the central observation: the LLM tool calls translate to capability invocations on the daemon. The TOOL ABSTRACTION at the LLM layer maps to the CAPABILITY ABSTRACTION at the daemon layer. §the-named-LLM-tool-capability-bridge names the tier-3 meta-pattern. The lal agent IS the bridge between LLM-world tool-calls and daemon-world capability invocations. This is the architectural seam where two distinct vocabularies meet: tool-call semantics on one side, capability semantics on the other. The provider-dispatch heuristic is named. §the-named-LAL_HOST-anthropic-vs-llama-cpp-dispatch marks lines 16-17: "If `LAL_HOST` contains `anthropic.com`, the Anthropic provider is used; otherwise the llama.cpp (OpenAI-compatible) provider is used." Heuristic provider dispatch via URL string match. §the-named-heuristic-provider-dispatch-via-url-match names the tier-3 meta-pattern. The dispatch logic is not via config declaration but via URL inspection at runtime — a minimal config surface trades explicit declaration for inferred provider choice. The config surface is named. §the-named-five-env-var-config-surface marks LAL_HOST + LAL_MODEL + LAL_AUTH_TOKEN + LAL_MAX_TOKENS + LAL_MAX_MESSAGES. Five environment variables configure the agent. §the-named-five-env-var-as-LLM-config-surface names the tier-3 meta-pattern. The context-window workaround is named explicitly. §the-named-LAL_MAX_MESSAGES-truncation-as-context-size-workaround marks lines 25 + 33-34: "Truncate to last N messages before sending (avoids context-size errors)"; "For a llama.cpp server that returns 'context size' errors, set `LAL_MAX_MESSAGES` (e.g. to `30`) to send only the last N messages and stay under the server's limit." §the-named-context-window-as-config-knob names the tier-3 meta-pattern. The workaround for a real LLM constraint is surfaced in the configuration rather than hidden in code. Deployment shapes are named by example file. §the-named-four-example-env-files marks local.env.example + cloud.env.example + openai.env.example + opus.env.example. Four deployment shapes with named example files: local Ollama, cloud Ollama with auth, OpenAI-compatible, Anthropic Opus. §the-named-deployment-shape-examples-as-files names the tier-3 meta-pattern. The default-model attribution is a time fossil. §the-named-claude-opus-4-5-as-named-default-model marks line 22's "claude-opus-4-5-20251101". The README dates from when Opus 4.5 was the recent default. Sibling shape to cycle 360's Agoric-specific-plugin attribution fossil — both name a moment in time via the version or default they pin. §the-named-default-model-as-time-fossil names the tier-3 meta-pattern. The agent lifecycle is named explicitly. §the-named-four-step-agent-lifecycle marks lines 46-50: (1) Create a guest profile named `lal`; (2) Start monitoring its inbox for messages; (3) Respond to messages using LLM-driven tool calls; (4) Send replies back to message senders. §the-named-create-monitor-respond-reply names the tier-3 meta-pattern. The LLM agent's lifecycle is an event-loop with explicit phases — receive message → invoke LLM → translate tool calls to capability invocations → emit reply. The provider conflation observation. §the-named-llama-cpp-as-OpenAI-compatible marks line 17's "llama.cpp (OpenAI-compatible) provider." The design treats llama.cpp's OpenAI-compatibility as a single named provider. §the-named-protocol-compatibility-as-named-provider-conflation names the tier-3 meta-pattern; multiple backends can share one provider identifier if they speak the same wire protocol. The shell-native config-loading is named. §the-named-source-your-configuration-as-shell-idiom marks line 40's `source local.env.example`. The README uses bash `source` to load environment variables. §the-named-source-pattern-as-config-loading names the tier-3 meta-pattern; the configuration mechanism is shell-native rather than via a configuration library. The compactness observation. §the-named-fifty-line-README-for-LLM-agent-package marks the substantial LLM-agent caplet documented in 50 lines. Sibling shape to cycle 369's daemon (14-line README), cycle 363's benchmark (8-line README) — all minimal-but-rich. Seven citation arcs close on this cycle. Cycle 398 was the prior chat-lane source; the check-bundle utility was about validation; cycle 399 names a concrete CONSUMER of the daemon — lal is the actual LLM agent that runs on the substrate (arc 1). Cycle 391's daemon-lore named the caplet shape abstractly; cycle 399's lal is a concrete LLM-agent caplet (arc 3). Cycle 392's runlet/caplet relationship; lal is a caplet that runlets like setup-ws-relay can install (arc 2). Cycle 381's @endo/genie was the bot-fork's agent FRAMEWORK; @endo/lal is one concrete agent built using that framework substrate — the genie + lal pair is framework + instance (arc 3). Cycle 374's @endo/cli names-in-transit demo named the social-network-already-shipping observation; lal is the LLM-driven participant in that social network — the network now has both human-typed messages and LLM-generated tool-calls (arc 8). Cycle 326's pure-naming-as-discipline reaches one more sibling (arc 74). Cycle 322's @endo/errors not directly invoked (arc 74). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-SEVENTY-TWO (465 + 7 net new). §eighty-nine-cycles-with-named-pivot-domain-stay (310-399). §forty-seventh-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty-seven consecutive cycles (353-399). The 400-cycle milestone is one cycle away.
