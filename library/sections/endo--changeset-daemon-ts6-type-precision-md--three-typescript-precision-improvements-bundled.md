---
source: endo--changeset-daemon-ts6-type-precision-md
cycle: 395
lane: designs
ingested: 2026-06-18
repo: endo
package: changeset
cluster: pivot
shape: typescript-precision-changeset
shape_subtype: branded-types-and-ERef-and-shadowing-fixes
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 43
---

The 11-line `.changeset/daemon-ts6-type-precision.md` is a TypeScript 6 conformance changeset for @endo/daemon that bundles three distinct precision improvements in one patch bump. The single most structurally interesting move is the bundling itself. §the-named-three-typescript-precision-improvements-in-one-changeset names the move. Three precision issues are addressed together: tightening string parameters to branded FormulaIdentifier, switching Promise types to ERef types, and renaming a generic parameter to avoid shadowing the built-in Record utility type. §the-named-bundled-typescript-precision-improvements names the tier-3 meta-pattern; the changeset bundles related TypeScript fixes that share a single semver bump rather than splitting into separate patches. The first improvement applies a discipline that has recurred throughout the cluster. §the-named-string-tightened-to-branded-FormulaIdentifier marks line 7: "tightened from `string` to `FormulaIdentifier` (a branded string type)." Cycle 387's AGENTS.md named the branded-types-from-validators discipline; cycle 394's pet-name.js was the canonical application of that discipline; cycle 395 shows the discipline applied to a different named type (FormulaIdentifier) in a different surface (the daemon's public types). §the-named-branded-types-discipline-recurs-across-types names the tier-3 meta-pattern; the discipline composes across packages and types — it is not package-local. The second improvement names a remote-reference type. §the-named-ERef-vs-Promise-distinction marks line 8: the changes prefer `ERef<EndoGateway>` over `Promise<EndoGateway>` in type signatures. ERef from @endo/eventual-send is the typed remote reference. Using ERef signals to TypeScript consumers that the value can be a remote reference, not just an in-process Promise. §the-named-ERef-as-remote-promise-type names the tier-3 meta-pattern; the type expresses the eventual-send semantics. The third improvement names a TypeScript-specific pitfall. §the-named-rename-Record-to-RecordT-to-avoid-shadowing marks line 9: "EndoInspector generic type parameter renamed from `Record` to `RecordT` to avoid shadowing the built-in `Record` utility type." TypeScript pitfall: generic parameters named `Record` shadow the built-in `Record` utility type, making code that references built-in `Record` inside the generic scope ambiguous. §the-named-generic-parameter-name-clashes-with-builtin-utility-type names the tier-3 meta-pattern. A subtle TypeScript flag detail is named. §the-named-method-syntax-for-strictFunctionTypes-assignability marks line 9's "`lookup` and `list` now use method syntax so that `EndoInspector<'some literal'>` remains assignable to `EndoInspector<string>` under `strictFunctionTypes`." TypeScript's `strictFunctionTypes` flag distinguishes between method syntax (`foo(x): T` — bivariant parameter checking) and arrow property syntax (`foo: (x) => T` — contravariant). The bivariance is required here for the assignability the design wants. §the-named-method-syntax-bivariance-for-assignability names the tier-3 meta-pattern. The motivation is named explicitly. §the-named-typescript-6-as-conformance-target marks line 5's "TypeScript 6 conformance: public types in `types.d.ts` are now more precise." §the-named-version-bump-of-toolchain-as-conformance-target names the tier-3 meta-pattern; the changeset's GOAL is alignment with the next TypeScript major version's stricter checking. The consumer impact is named. §the-named-consumer-update-acknowledgment marks line 11: "TypeScript consumers implementing or calling these interfaces may need to update their types accordingly." §the-named-downstream-impact-named-in-changeset names the tier-3 meta-pattern; sibling shape to cycle 380's @endo/base64 changeset which named the TypeError-under-SES impact. The semver shape is structurally meaningful. §the-named-patch-bump-for-type-only-change marks line 2's `'@endo/daemon': patch`. The changeset is a patch bump even though it requires downstream consumers to update their types. §the-named-patch-bump-for-type-only-discipline names the tier-3 meta-pattern; semver semantics treat runtime-compatible-but-type-incompatible changes as patch. The expertise level of the audience is named implicitly. §the-named-strictFunctionTypes-as-flag-mentioned-in-changeset marks the explicit naming of a specific TypeScript flag. §the-named-changeset-assumes-typescript-expertise names the tier-3 meta-pattern; the changeset's vocabulary presumes TypeScript-savvy consumers. The compactness observation recurs. §the-named-eleven-line-changeset-three-issues marks the file's structure: three precision improvements + motivation + consumer note fits in 11 lines after the YAML frontmatter. Sibling shape to cycle 380's 13-line base64-harden changeset — both compact and structurally rich. Seven citation arcs close on this cycle. Cycle 394's pet-name.js was the canonical application of the branded-types discipline; cycle 395 shows the discipline applied to FormulaIdentifier in another package, demonstrating composition across packages (arc 1). Cycle 387's AGENTS.md branded-types discipline plus the ERef-vs-Promise type-precision tightening that aligns with AGENTS.md's TypeScript conventions (arc 3). Cycle 367's @endo/exo introduced ERef from eventual-send (arc 13). Cycle 321's @endo/eventual-send is the source of the ERef type (arc 13). Cycle 380's base64-harden changeset is the shape sibling: bundled-with-consumer-impact-acknowledgment (arc 3). Cycle 326's pure-naming-as-discipline reaches one more sibling — the rename Record → RecordT IS pure naming applied to disambiguate (arc 69). Cycle 322's @endo/errors not directly invoked (arc 69). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-FORTY-FOUR (437 + 7 net new). §eighty-five-cycles-with-named-pivot-domain-stay (310-395). §forty-third-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across forty-three consecutive cycles (353-395).
