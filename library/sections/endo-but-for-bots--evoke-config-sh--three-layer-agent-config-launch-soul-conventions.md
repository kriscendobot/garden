---
source: endo-but-for-bots--evoke-config-sh
cycle: 390
lane: chat
ingested: 2026-06-18
repo: endo-but-for-bots
branch: llm
package: evoke
cluster: pivot
shape: agent-launch-config-script
shape_subtype: shell-script-for-invocation
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 38
---

The 19-line `evoke/config.sh` is the shell-script configuration for invoking the agent in endo-but-for-bots. It sits alongside cycle 383's `evoke/SOUL.md` in the `evoke/` directory and complements it: where SOUL.md is the file LOADED INTO the agent's system prompt, config.sh is the file that LAUNCHES the agent in the first place. The single most structurally interesting move is the cluster-level observation. §the-named-three-layer-agent-config-launch-soul-conventions names the move. Cycle 383's `evoke/SOUL.md` is the agent's DISCIPLINE document (workflow, planning, constraints). Cycle 387's `AGENTS.md` is the repo's CODE CONVENTIONS document (TypeScript, types-index, exo conventions, commit rules). Cycle 390's `evoke/config.sh` is the agent's LAUNCH CONFIGURATION (binary, args, paths, notification URL). THREE LAYERS of agent configuration coexist in the bot-fork's repo. §the-named-three-files-for-three-layers-of-agent-config names the tier-3 meta-pattern; each layer answers a different question — "how to launch?" + "how to behave?" + "how to code?" — and each lives in its own file. The directory name carries semantic meaning. §the-named-evoke-as-agent-invocation-tool marks the inference: the `evoke/` directory holds the tool that launches (evokes) the agent. Companion to cycle 383's `evoke/SOUL.md` which is loaded INTO the agent by evoke. §the-named-evoke-as-companion-to-soul names the tier-3 meta-pattern. Several smaller config fields deserve naming. §the-named-AGENT_NAME-and-AGENT_ARGS-pair marks lines 16-17: `AGENT_NAME='claude'` and `AGENT_ARGS=('claude' '--dangerously-skip-permissions')`. The agent name is a separate variable from the launch command, so they can be referenced independently. §the-named-name-vs-args-as-separated-config names the tier-3 meta-pattern. The agent-choice comment is structurally interesting. §the-named-claude-not-pi-comment marks line 15: "Evoking Claude not Pi for now." The comment names BOTH options (Claude AND Pi) and the CURRENT CHOICE. §the-named-multi-agent-support-implied-via-not-X-for-now names the tier-3 meta-pattern; the comment names the alternative even when not used, so future readers know the toolchain supports multiple agents. The danger acknowledgment is named. §the-named-dangerously-skip-permissions-flag marks line 17's `--dangerously-skip-permissions`. The flag's name acknowledges the danger; the bot-fork deliberately opts in. §the-named-dangerous-flag-named-as-dangerous names the tier-3 meta-pattern; the developer acknowledges they're skipping safety for development velocity. The path encoding is named. §the-named-AGENT_SESSIONS-path-via-PWD-substitution marks line 18's `AGENT_SESSIONS="$HOME/.claude/projects/${PWD//\//-}"`. The current working directory is substituted into a sessions path with `/` → `-`. §the-named-pwd-as-project-identifier-with-substitution names the tier-3 meta-pattern; the path encoding survives the OS's directory-separator semantics. The webhook is named. §the-named-NOTIFY-URL-as-webhook marks line 8's `NOTIFY=http://127.0.0.1:8077/chat`. The agent posts notifications to a local webhook — likely the @endo/chat app from cycle 385 running locally on port 8077. §the-named-localhost-webhook-for-agent-notifications names the tier-3 meta-pattern. The commented-out fields are themselves structurally interesting. §the-named-TASK_FILE-commented-out-with-TODO marks lines 2-3: "TODO want to disable this, but not sure evoke supports empty string / # TASK_FILE=TODOs.md". The developer named the desired state (disable) AND the obstacle (uncertainty about empty-string support). Sibling honest-acknowledgment shape from cycles 357 (for-expedience), 359 (honest-placeholder), 372 (exported-for-tests), 375 (TODO-as-future-removal), 377 (bikeshedding-acknowledged), 378 (honesty-via-shared-lie), 379 (empty-file-as-no-change). §the-named-want-but-not-sure-as-named-tension names the tier-3 meta-pattern. §the-named-TASKS_IN-TASKS_OUT-task-section-markers marks lines 5-6's commented-out `TASKS_IN=TODO` and `TASKS_OUT=TADA` — section markers for the input and output task lists. The default is presumably the same. The "TODO / TADA" pair is a cute matched pair (rhymes; opposite tense). §the-named-TODO-TADA-as-task-section-pair names the tier-3 meta-pattern. §the-named-NEXT_TASK_DELAY-1m-as-pacing-knob marks line 10's `# NEXT_TASK_DELAY=1m`. The autonomous loop's between-task delay is configurable. §the-named-autonomous-loop-pacing-via-config names the tier-3 meta-pattern. The identity format is named. §the-named-AGENT_IDENTITY-as-whoami-repo-hostname marks line 13's `AGENT_IDENTITY="$(whoami)+${REPO_NAME}@$(hostname)"`. The identity format is "user+repo@host". §the-named-three-part-identity-user-repo-host names the tier-3 meta-pattern; the agent identity ties to the user, the repo, and the machine — a three-part naming for multi-host multi-repo agent operation. §the-named-AGENT_CURRENT_SESSION-empty-as-fresh-start marks line 19's `AGENT_CURRENT_SESSION=` (empty). The current session is set to empty, meaning the next invocation starts a fresh session. The variable's existence implies the agent CAN resume a session if set. §the-named-empty-session-variable-as-resume-shape names the tier-3 meta-pattern. Seven citation arcs close on this cycle. Cycle 389 was the prior designs-lane source; method-bank/exo two-layer pattern → three-layer agent config; both are about layered structure but at different domains (arc 1). Cycle 387's AGENTS.md is one of the three layers cycle 390 names (arc 1). Cycle 383's SOUL.md is the second of the three layers (arc 1). Cycle 385's chat README; the NOTIFY URL on port 8077 likely points to the @endo/chat app (arc 1). Cycle 326's pure-naming-as-discipline reaches one more sibling; the three-layer naming is pure naming applied to agent config (arc 64). Cycle 357's for-expedience-as-honest-acknowledgment is the want-but-not-sure sibling shape (arc 2). Cycle 322's @endo/errors not directly invoked (arc 64). The cycle pushes citation-arc-closures-in-pivot to FOUR-HUNDRED-NINE (402 + 7 net new). §eighty-cycles-with-named-pivot-domain-stay (310-390). §thirty-eighth-authored-conformant-single-body-section-doc — the post-refactor authoring discipline holds across thirty-eight consecutive cycles (353-390).
