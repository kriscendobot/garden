---
source: endo-but-for-bots--packages-lal-primer-README-md
cycle: 405
lane: designs
ingested: 2026-06-19
repo: endo-but-for-bots
branch: llm
package: lal
cluster: pivot
shape: agent-primer-index
shape_subtype: three-surfaces-three-audiences
authored_conformant: true
post_refactor_era: true
post_refactor_sequence: 53
---

The 37-line primer/README.md is the index of @endo/lal's agent-facing primer directory and the seventh lal-package artifact in the cluster (after the README, providers/config.js, LAL-ARCHITECTURE.md, agent.types.d.ts, the simulator README, and mock-powers.js). The primer itself contains 13 files totaling 1168 lines — a substantial agent-facing documentation corpus. Cycle 405's most structurally interesting move surfaces a third surface the cluster has not yet named. §the-named-third-surface-for-third-audience names the observation. Cycle 401 named the LLM-exposed tool catalog (16-18 tools). Cycle 404 named the guest-power method surface (22 methods in the mock; the LLM-tool surface is a subset of this). Cycle 405's primer/README.md reveals a third surface: the user-facing Chat slash-command surface plus the `@recipient message` addressing form (line 22: "`/ls`, `/view`, `/edit`, `@recipient message`"). The agent system has THREE distinct surfaces for THREE distinct audiences: (1) LLM-exposed tool catalog (16-18 tools; for the LLM); (2) Guest-power method surface (22 methods; for the agent's internal use); (3) User-facing Chat slash commands and @-addressing (for the human user; the web UI inside Familiar). §the-named-three-surfaces-three-audiences names the tier-3 meta-pattern. The cluster's surface vocabulary is now three-tiered, and the primer is the document that bridges all three audiences — agent reference for the LLM, user interface reference for the human user, how-to guides for the agent itself. The tool-name drift extends. §the-named-tool-name-drift-across-agent-facing-docs marks line 10's example list. The primer README gives examples as "list, readText, inspect, etc." But cycle 401's LAL-ARCHITECTURE.md tool catalog has `lookup` (not readText) and `inspectCapability` (not just inspect). The primer README mentions `readText` which is NOT in the LAL-ARCHITECTURE catalog at all. §the-named-primer-tool-examples-drift-from-architecture-catalog names the tier-3 meta-pattern; the cluster's drift vocabulary now identifies drift even WITHIN the agent-facing documentation set. The primer's location is significant. §the-named-primer-as-package-local-internal-docs marks the primer's location at packages/lal/primer/*.md, inside the code package directory — not on a public docs site, not in a separate documentation repo. §the-named-internal-package-docs-as-runtime-resource names the tier-3 meta-pattern; the docs are PART of the package's distribution surface, suggesting they may be consumed at runtime by the agent. This tension deserves its own framing. §the-named-primer-existence-vs-no-dynamic-context-claim names the structural fact. Cycle 401's LAL-ARCHITECTURE.md said: "There is no dynamic context building — no workspace files, no memory consolidation, no skills system. The system prompt is a single hardcoded string." But the primer directory contains 13 files totaling 1168 lines of substantial agent-facing content. Three possibilities: (a) the primer is INLINED into the hardcoded system prompt (making the system prompt enormous, on the order of 1200+ lines); (b) the primer is available via some lookup mechanism (contradicting the no-dynamic-context claim); (c) the primer is for human readers / maintainers / future versions and not consumed by the current agent at runtime. The design doc and the primer's framing both phrase the agent as having no workspace, yet here is a workspace of documents shipped in the package. §the-named-primer-claim-vs-design-claim-tension names the tier-3 meta-pattern. The cluster's drift vocabulary now has another instance — an ARCHITECTURE-LEVEL tension between "no dynamic context" and "1168 lines of agent-facing primer." The primer's organization reveals its philosophy. §the-named-primer-organized-by-audience marks the README's three-section structure (Agent Reference, User Interface Reference, How-To Guides). §the-named-docs-organized-by-audience-rather-than-topic names the tier-3 meta-pattern; the organization is by AUDIENCE rather than by TOPIC. The Chat-vs-CLI hierarchy is explicit. §the-named-chat-as-primary-CLI-as-secondary marks lines 19-24. The Chat surface is positioned as primary; the CLI surface is reserved for operations Chat doesn't expose. §the-named-Chat-primary-CLI-fallback names the tier-3 meta-pattern. The CLI's reserved territory is named. §the-named-CLI-reserved-for-program-files-and-snapshot marks lines 22-24: "primarily `endo run` for running program files and `endo checkin`/`endo checkout` for snapshotting filesystem subtrees." §the-named-CLI-territory-as-program-files-and-filesystem-snapshots names the tier-3 meta-pattern. Chat has two input shapes. §the-named-chat-supports-slash-and-at-addressing marks line 22: `/ls`, `/view`, `/edit` (slash commands) plus `@recipient message` (at-addressing). §the-named-two-input-shapes-in-chat names the tier-3 meta-pattern; sibling to cycle 386's petname-edgename framing (also a two-shape addressing system). The Reference vs How-To distinction is preserved. §the-named-how-to-guides-as-distinct-shape-from-reference marks the separation. §the-named-reference-vs-how-to-as-doc-shapes names the tier-3 meta-pattern; sibling to standard tech-writing distinction. The four how-to domains align with tool categories. §the-named-four-how-to-guides-for-four-domains marks lines 33-36: howto-inventory (names, directories, values), howto-messaging (sending, receiving, replying), howto-capabilities (inspecting, requesting, sharing), howto-code (evaluating and defining programs). These four domains correspond roughly to the four tool categories in cycle 401's LAL-ARCHITECTURE.md catalog: Directory, Mail, Inspection+Identity, Evaluation. §the-named-how-to-guides-aligned-with-tool-categories names the tier-3 meta-pattern; the primer's procedural organization mirrors the agent's tool taxonomy. The agent is addressed directly. §the-named-ocap-environment-named-in-first-line marks line 3: "You are an Endo agent operating in an object-capability (ocap) security environment." The primer addresses the agent in second person and names the ocap security model directly. §the-named-second-person-agent-address names the tier-3 meta-pattern; the document is written for the AGENT to read, not for a human reader — confirming the primer's intended runtime audience. Familiar is named as Chat's host. §the-named-Familiar-as-Endo-Chat-host marks line 19's identification of Endo Chat as "the web UI inside Familiar". §the-named-Familiar-as-Chat-shell-inside-Endo names the tier-3 meta-pattern; the outer CLAUDE.md describes Familiar as an Electron shell, and the primer here positions it as the host of the user's Chat interface. The cycle is a continuation. §the-named-fifty-three-conformant-cycles-and-counting marks the session-level observation: fifty-third AUTHORED conformant single-body section doc in post-refactor era (cycles 353-405). §ninety-five-cycles-with-named-pivot-domain-stay (310-405). Nine citation arcs close on this cycle. Cycle 404 (1, adjacent forward; the guest-power-surface framing now extended by a THIRD user-facing surface — cycle 404 named two surfaces, cycle 405 names a third). Cycle 403 (2, the simulator README discusses lal but the primer is the agent's INTENDED runtime context). Cycle 401 (3, the LAL-ARCHITECTURE tool catalog the primer README example tools drift from; the primer-claim-vs-design-claim tension cuts directly against cycle 401's "no dynamic context" claim). Cycle 386 (3, petname-edgename inversion sibling — the two-input-shapes-in-chat is a parallel two-shape addressing system). Cycle 384 (3, design-doc-trails-code sibling — cycle 405 finds primer-tool-examples-drift-from-architecture-catalog as another design-doc-drift instance). Cycle 326 (75, pure-naming-as-discipline). Cycle 322 (75, errors framing). Cycle 364 (4, shapes count now SEVEN-plus-and-counting). Cycle 346 (3, name-aliasing-for-domain-vocabulary). Pushes citation-arc-closures-in-pivot to FIVE-HUNDRED-AND-TWENTY-TWO (513 + 9 net new). The library has crossed 917 sections; the post-refactor era continues at fifty-three consecutive conformant cycles; the cluster's surface vocabulary is now three-tiered (LLM tool surface ⊆ guest-power surface ⊆ user-facing surface), each tier filtering or extending the others. The primer's existence raises a structural question the cluster's drift vocabulary now poses: how does the primer reach the agent at runtime when the design doc claims the agent has no dynamic context? The tension is unresolved within the documents themselves; future cycles may resolve it by reading agent.js directly.
