---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1084
panel_kind: design
base_ref: 059e18d07c717a2d89dd852b374b82dde765c016
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 36fbd6f2620f
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1084 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `68face71`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=comment ergonomist=must-fix novice=must-fix pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: **Must-fix.** `designs/exo-stream-adaptive-pacing.md` carries typist-hostile code points throughout the prose and for...
- copyeditor: **Should-fix.** Line 120, `` `alpha ∈ (0, ∞)` ``, uses `∈` (element-of) and `∞` (infinity), which are not in ...
- copyeditor: **Should-fix.** "Where the policy belongs" (line 39) explicitly glosses `initiator (consumer)`, but `responder` (firs...
- copyeditor: **Should-fix.** Line 88, "…no delivered data is ever discarded, because our queue is reliable," shifts to first-per...
- copyeditor: Mermaid `stateDiagram-v2` used for the control-loop diagram, correctly avoiding ASCII art.
- copyeditor: `designs/README.md`'s new "Recently added" entry matches the file's existing long-sentence, em-dash-heavy house style...
- copyeditor: Section transitions, tense, and paragraph flow are otherwise consistent end-to-end; the document reads as a coherent ...
- critic: **`## Control loop`'s `outstanding` counts the wrong event, undermining the design's central memory-bound claim.** `o...
- critic: **No stated cold-start value for `W`.** `iterate-reader.js`'s existing static path primes `buffer` syn nodes synchron...
- ergonomist: The `## Surface and compatibility` section states the `buffer` option is "detected by `typeof`" but then shows *three...
- ergonomist: `makeCodelBuffer(opts)` is documented as returning a `CreditController`, and the design explicitly invites third part...
- ergonomist: Naming mismatch between the factory and its product: `makeCodelBuffer` returns a `CreditController`, not a buffer —...
- ergonomist: Cross-surface inconsistency: `iterateReader`'s `buffer` now accepts `number | CreditController`, while sibling `Reada...
- ergonomist: Minor: `target` is introduced in `## Control loop` as a *derived* value (`target = alpha · target0`), but the `## Su...
- ergonomist: Whether `makeCodelBuffer` validates `alpha`'s documented domain `(0, ∞)` at construction (throw vs. silent clamp) i...
- novice: `## Problem`, first technical sentence: "the consumer issues undefined synchronization ('give me more') credits" read...
- novice: `## Control loop`: the mechanism is introduced entirely through a `mermaid` state diagram plus formula-dense prose (`...
- novice: `## Limits and failure behavior` introduces "occupancy AIMD" without expanding the acronym; minor since "additive-inc...
- pedant: `designs/exo-stream-adaptive-pacing.md` uses em-dashes throughout prose (13 instances — lines 23, 44, 51, 53, 55, 7...
- pedant: The new design file uses hard-to-type code points the project requires spelled out in ASCII: `→` for "leads to" in ...
