---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1083
panel_kind: design
base_ref: upstream/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: f840b71d0552
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1083 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `bcb98e8d`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=comment skeptic=must-fix
must-fix items (20):
- copyeditor: **Must-fix — misleading pronoun antecedent.** `designs/npm-registry-as-directory-tree.md:153-157`: "The npm and sco...
- copyeditor: **Should-fix — jargon used before introduction.** `designs/npm-registry-as-directory-tree.md:170` first uses "packu...
- copyeditor: **Comment-only — overloaded metaphor.** `designs/npm-registry-as-directory-tree.md:279` and `:241` call `RegistryRe...
- copyeditor: **Comment-only — adverb placement.** `designs/npm-registry-as-directory-tree.md:108-109`: "`list()` on a package di...
- critic: **Must-fix.** The design's central efficiency claim — "`resolveRegistryTree` and the `registryRoot` tree it walks a...
- critic: **Should-fix.** Given the above, § Migration step 2 should say explicitly where `resolveRegistryTree` executes relat...
- critic: Implementation-level verification of the actual E()/CapTP call count under the new tree-walk resolver belongs to the ...
- decomplector: The `lookup(path: string | string[])` method (`designs/npm-registry-as-directory-tree.md` § Tree shape) overloads on...
- decomplector: § Migration and compatibility step 3 reincarnates the `@registry` formula **under its existing identifier** while ch...
- decomplector: The `Read consistency` section's explicit value-vs-place distinction (immutable version leaf vs. live `list()`) and t...
- ergonomist: The `LookupTree` interface (design § "Non-enumerability is a capability shape") spells its own sibling methods incon...
- ergonomist: The not-found error shape (a `RangeError` from `@endo/errors`) diverges from the not-found convention the design's ow...
- ergonomist: The "Tree shape" table (§ "Tree shape") uses `/`-delimited path notation (`/npm/@scope/<package>`) as the primary wa...
- novice: The design's very first paragraph (`## Summary`) says "The Node Endo daemon and the Rust-hosted Endor daemon expose t...
- novice: `## Tree shape`'s table uses `EnumerableTree`, `LookupTree`, `SnapshotTree`, and `EndoReadableTree` as capability-sur...
- novice: In `### Non-enumerability is a capability shape`, the doc defines a TS-shaped `EnumerableTree` interface, then a few ...
- novice: "packument" is used unglossed in two places ("Package-directory `list()` reads the packument..."; "...mock packuments...
- novice: `## What is the Problem Being Solved?` leans on "the filesystem vocabulary both already consume" as an established fa...
- skeptic: **`resolutionHash` semantics silently change, contradicting the design's own "presentation layer, not a rewrite" fram...
- skeptic: **Testing plan omits the deprecated compatibility adapter itself.** [should-fix]
