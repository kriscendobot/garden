---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 995
panel_kind: design
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 78dd06793959
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #995 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `43efff82`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: ## Juror: copyeditor — PR #995 (`designs/endo-claude.md`, `designs/README.md`)
- critic: **The code-eval deny is applied at exactly the layer the design declares insufficient.** § *Working around the `mcp_...
- critic: **DD2's membership rule is unsatisfiable against the only live surface the design names.** DD2 requires each allow-li...
- critic: **The credential tension is weighed with pre-slice values for `--bare`.** § *Pooling* and DD5 frame the residual as ...
- critic: **Option (b)'s "shallowest dependency" rationale omits dependencies the design's own decisions create.** § *Build se...
- critic: The credential verification (DD5, ~an hour of measurement) invalidates a named v1 artifact (`credentials-pool.js`) if...
- critic: Both diagrams are mermaid and the README sync (summary row, M6 note, `eat`/`dsand` graph edges, estimate, totals) is ...
- decomplector: **must-fix — the code-eval deny is applied to the belt, not the boundary.** The design settles that withholding is ...
- decomplector: **should-fix — the subtractive-vs-additive axis is never recorded, though the prompt offered it.** The captured pro...
- decomplector: **should-fix — one invariant, two modules, no owner.** DD7 states a single spawn-scoped rule for both secret files ...
- decomplector: `tool-permissions.js` braids a per-guest value derived from a live snapshot with a per-CLI-version constant (the buil...
- decomplector: The pinned-snapshot-as-one-value decision (DD2) and the no-designator-on-`infer` decision (DD4) are both correct deco...
- ergonomist: **must-fix — the error convention splits two ways and the rule is never stated.** DD8 (line 990) makes `infer` reso...
- ergonomist: **should-fix — the same operation spells differently across siblings.** `ClaudeClient` is `send(prompt, opts?)` ret...
- ergonomist: **should-fix — neither exo carries `help()`.** Both cross CapTP (DD8) and are the surface a guest holds; the siblin...
- ergonomist: **should-fix — the one public export's parameter is unspecified.** `make(powers)` (line 833) names no keys or types...
- ergonomist: **should-fix — `selectSubscription(pool, sessionTag) -> exo` defeats its own seam split** (line 664): the policy re...
- ergonomist: **comment-only — implementation-perspective case names.** `nonzero-exit` / `parse-error` describe "we spawned a CLI...
- ergonomist: **comment-only — a second `makeGuestInference` for one guest is unspecified**; the bridge is "stood up (or reused)"...
- novice: `designs/endo-claude.md:876` — DD1 is titled "The confinement takes three flags, not one," then its own body requir...
