---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1100
panel_kind: code
base_ref: f9cbcfc426f726858a671bcb09f7c2c774cc659e
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 5520c20fce9b
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1100 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `860574a2`

seat verdicts (32): archivist=pass assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=comment fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=pass orthographer=pass packager=pass prover=pass pruner=comment purist=must-fix reexport-auditor=pass releaser=comment saboteur=must-fix scribe=pass spec-keeper=pass stylist=pass surfacer=comment thesaurus=pass transplanter=pass typist=comment warden=must-fix wire-watcher=pass
must-fix items (20):
- breaker: **should-fix: an attenuated blob sends its whole selection as one frame, which breaks the default reader limit.** `pa...
- breaker: **comment-only: I tried to make an aborted write commit, and it didn't.** I attacked the "an aborted write commits no...
- breaker: the initiator calls `throw()`
- breaker: the initiator sends a non-bytes `next()`
- breaker: the responder rejects an over-limit frame or an over-limit total
- breaker: **comment-only: a non-bytes frame can't be written as a silent empty write.** A peer sending a string or other non-by...
- breaker: **comment-only: `Number.MAX_SAFE_INTEGER` is used as "no limit".** The default write limit at `bytes-writer-from-iter...
- integrator: **should-fix — commit grouping for the merge-commit reader.** The repo merges by rebase (AGENTS.md § Pull requests...
- integrator: **should-fix — the description no longer matches the head.** Commit `fca0b5fbec` adds a public option: `wrapBackend...
- integrator: **comment-only — design documents still describe `streamBase64`.** 16 files under `designs/` still name `streamBase...
- integrator: **comment-only — an error message no longer matches the check.** `packages/daemon/src/host.js:1716` says "has both ...
- locksmith: **must-fix: a reused write capability commits zero-padded, corrupt bytes.** `packages/platform/src/fs/extended/shared...
- locksmith: **should-fix: when a writer is shared, one holder can veto another's write.** The buffer and the sticky `rejected` fl...
- locksmith: **comment-only: remote error values are now thrown into local sinks.** `packages/exo-stream/bytes-writer-from-iterato...
- locksmith: **comment-only: `bytesWriterFromIterator` still defaults to no frame limit.** The default `byteLengthLimit` is `Numbe...
- purist: **should-fix: the blob discriminator now depends on a method being absent.** `packages/platform/src/fs/interfaces.js:...
- purist: **should-fix: the error label hand-rolls quoting that `@endo/errors` already provides.** `packages/platform/src/fs/ex...
- purist: **comment-only: `Number.MAX_SAFE_INTEGER` stands in for "no limit."** `packages/exo-stream/bytes-writer-from-iterator...
- purist: **comment-only: blob code builds a full Exo only to borrow its method.** `blob.js:33` and the `mount.js` read paths c...
- purist: **comment-only: a type check that can never fail.** `buffered-bytes-writer.js:49` tests `chunk instanceof Uint8Array`...
