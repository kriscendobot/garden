---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1100
panel_kind: code
base_ref: f9cbcfc426f726858a671bcb09f7c2c774cc659e
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 70157a4a6bce
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1100 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `4c016bca`

seat verdicts (32): archivist=pass assessor=pass benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=comment fast-checker=comment gateway=pass integrator=must-fix locksmith=pass migrator=must-fix orthographer=pass packager=comment prover=pass pruner=must-fix purist=must-fix reexport-auditor=pass releaser=comment saboteur=must-fix scribe=pass spec-keeper=comment stylist=pass surfacer=comment thesaurus=pass transplanter=pass typist=pass warden=pass wire-watcher=comment
must-fix items (20):
- breaker: **must-fix: an abort started by the initiator still commits the buffered prefix.** The claimed invariant is in `packa...
- breaker: **Attack:** `packages/exo-stream/iterate-bytes-writer.js` `fail()` handles both `iterator.throw(err)` and any excepti...
- breaker: **Result:** the pump takes the normal close branch, calls the sink's `return()`, and the sink commits. The new `throw...
- breaker: **Reproduced:** I ran a throwaway test against `wrapBackend(makeInMemoryBackend())` and deleted it afterwards.
- breaker: `File.write`, then `w.next(utf8('PARTIAL'))`, then `w.throw(Error('abort'))`: the file reads `"PARTIAL"`, where it pr...
- breaker: `w.next(utf8('BAD-PREFIX'))`, then `w.next('not bytes')`: the file reads `"BAD-PREFIX"`.
- breaker: In both cases the initiator gets a rejection while the truncated write lands.
- breaker: **Fix:** the initiator's `fail()` needs an abort signal the pump can tell apart from a close, for example rejecting t...
- breaker: **Missing test:** `frame-limits.test.js` covers only rejections on the responder side. Add an initiator-`throw()` cas...
- breaker: [proposed-rule: a writer-sink invariant of "abort commits nothing" must be tested from both ends of the stream, a res...
- breaker: **comment-only: the new abort path is applied the same way to every writer sink (sibling family).** The in-tree `byte...
- breaker: The three sinks that commit a buffer all gained a discarding `throw()`.
- breaker: The sandbox sink has no `throw()`, so on a rejected frame it falls back to `return()` and closes stdin. That is accep...
- breaker: The host-shell sink uses `makeNodeWriter`, whose `throw` already calls `destroy(error)` (`packages/stream-node/writer...
- breaker: No sibling was skipped or converted differently.
- breaker: **comment-only (mitigated): a stale base64 frame is caught.** The invariant is that a base64 string frame is not sile...
- breaker: **comment-only (out of scope): the writer has no size limit by default.** When `byteLengthLimit` is omitted, `bytesWr...
- integrator: **must-fix: the PR description no longer covers what the branch lands.** [rule: skills/pr-formation/SKILL.md] The tit...
- integrator: `@endo/exo-http-client`'s `HttpResponse.stream()` was renamed to `body()`, and its chunks changed from base64 frames ...
- integrator: The reader option `stringLengthLimit` became `byteLengthLimit`, and its unit changed from characters to bytes. Leavin...
