---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1100
panel_kind: code
base_ref: f9cbcfc426f726858a671bcb09f7c2c774cc659e
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: f14f371c2fe5
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1100 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `2618acd3`

seat verdicts (32): archivist=comment assessor=pass benchmarker=pass breaker=comment changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=pass duality-auditor=pass engine-realist=comment fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=comment orthographer=pass packager=comment prover=must-fix pruner=comment purist=comment reexport-auditor=pass releaser=comment saboteur=comment scribe=must-fix spec-keeper=comment stylist=pass surfacer=pass thesaurus=pass transplanter=pass typist=must-fix warden=pass wire-watcher=pass
must-fix items (20):
- fast-checker: **Three new per-frame `byteLengthLimit` constants ship with zero tests — neither example nor property — despite t...
- fast-checker: `packages/daemon/src/manager.js:4851` — `byteLengthLimit: 7_500_000` (recomputed from the old base64-derived 10,000...
- fast-checker: `packages/platform/src/fs/extended/wrap-backend.js:66` — `WRITE_FRAME_BYTE_LENGTH_LIMIT = 16 * 1024 * 1024`, wired ...
- fast-checker: `packages/platform/src/fs/extended/shared/xattrs-exo.js:34` — `XATTR_FRAME_BYTE_LENGTH_LIMIT = 64 * 1024`, matching...
- fast-checker: `grep -rn "XATTR_FRAME_BYTE_LENGTH_LIMIT\|WRITE_FRAME_BYTE_LENGTH_LIMIT\|byteLengthLimit" packages/platform/test/ pac...
- fast-checker: **Failure scenario:** a future edit to `wrap-backend.js` (e.g. converting `1024 * 1024` to `1024 ** 2` or fat-fingeri...
- fast-checker: **Proposed properties**, mirroring the established `bytes-writer.test.js` pattern:
- fast-checker: `xattrs-exo.js`: `fc.assert(fc.property(fc.uint8Array({minLength: 64*1024, maxLength: 64*1024}), bytes => /* set() ac...
- fast-checker: `wrap-backend.js`: same shape at the 16 MiB boundary (`fc.uint8Array` at that size is expensive per-run; keep `numRun...
- fast-checker: `manager.js`: an integration-level example test at exactly 7,500,000 and 7,500,001 bytes through `iterateBytesReader`...
- fast-checker: Disposition: `must-fix-loop` — this is a security-relevant guard (bounding heap residency of an untrusted remote in...
- fast-checker: **Commendation, not a defect:** the PR's core round-trip contracts are already exemplary fast-check citizens — `byt...
- integrator: **should-fix: the rename sweep missed parts of `packages/9p-server/PERFORMANCE.md`.** The PR changed `streamBase64` t...
- integrator: `:81` still describes layer 5 `@endo/exo-stream` as "base64 chunks".
- integrator: `:509` points at `base64LimitFor` (`server.js:71`). The PR removed that function; `server.js:65-66` now says the limi...
- integrator: `:693`, in a paragraph this PR edited, still says "F²/msize bytes of base64 through the daemon".
- integrator: The ranked measurements at `:277-292` and `:579-581` still treat base64 encode/decode as the Twrite cost. Base64 is n...
- integrator: **should-fix: some commit subjects will not make sense in `git log` after merge.** The repo merges by rebase (AGENTS....
- integrator: `d8f9fbbb45`, `b5fc4caeb7`, `48e1baddf5`: "address panel round-N must-fix items".
- integrator: `ae192bfa86`: "panel round-6".
