---
kind: panel-run
repo: kriscendobot/minion.town
pr: 84
panel_kind: code
base_ref: c6b864c02250018a561726560238fd6803819b09
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 73c2592ed14c
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #84 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `c78a1e15`

seat verdicts (29): archivist=comment assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=pass engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=comment locksmith=must-fix migrator=pass packager=comment prover=must-fix pruner=must-fix purist=comment releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=comment transplanter=comment typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- assessor: `deploy/clips/clipometer/src/app.js:1280-1285` (`main()`): the visit count is a bare **read-then-increment-then-write...
- assessor: `deploy/clips/clipometer/src/app.js:1288-1296` (the `for await (const _change of changes)` loop) plus `:1204-1246` (`...
- assessor: `connectBack()`'s returned `closed` promise (`app.js:1245`) is destructured away and never observed (`const { back } ...
- benchmarker: The PR's only optimization claim — esbuild tree-shaking keeps the `@endo/captp` bundle small (README.md § Build: "...
- benchmarker: The `mustMatch`/`@endo/patterns` trim (README.md § "Concrete finding") is framed as removing "a fragile transitive s...
- breaker: `deploy/clips/clipometer/src/app.js:22-28,49` claims (JSDoc, "SES REQUIRED AT ENTRY"): `import '@endo/init'` "MUST be...
- breaker: `deploy/clips/clipometer/src/app.js:144-153`: `main()`'s read→increment→write is not atomic (`before = await read...
- breaker: Capability-attack overlap checked and mitigated: `makeCapTP('clipometer', rawSend)` passes no bootstrap object, match...
- breaker: `src/vendor/iterate-reader.js`'s trimmed `mustMatch` validation is confirmed dead code as claimed: `app.js:160` calls...
- corner-prober: `parseCount` (`src/app.js`) has no tests at all, and at least two of its boundaries are wrong or silently lossy: (1) ...
- corner-prober: `main()`'s read-increment-write on `back` (`src/app.js`) is not atomic: two windows/tabs opening concurrently can bot...
- corner-prober: `readCount`'s `await` and the `for await` live-update loop over `iterateReader(...)` have no timeout and no reconnect...
- corner-prober: `parseArgs` (`scripts/publish.mjs`): `--hash` given with an empty/falsy value (e.g. `--hash ""`, or `--hash` as the l...
- corner-prober: `src/vendor/iterate-reader.js` is declared byte-for-byte vendored from upstream, so its own boundary gaps are not thi...
- corner-prober: The whole `deploy/clips/clipometer` package ships with zero test files (no `test`/`spec` anywhere under it besides `n...
- engine-realist: **[must-fix] `deploy/clips/clipometer/src/app.js:49` — SES `lockdown()` never runs in the shipped bundle; the whole...
- engine-realist: **[comment-only] `src/app.js:83,89` — `Error(...)` (no `new`) still closes over the page's global `Error` binding**...
- engine-realist: Once finding 1 is fixed, re-verify by grepping the **minified production** artifact (not just source) for `function l...
- fast-checker: ## Findings
- locksmith: `deploy/clips/clipometer/src/app.js:90-125` — `connectBack()` obtains the clip's `back` power via `getBootstrap()` ...
