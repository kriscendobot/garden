---
kind: panel-run
repo: kriscendobot/minion.town
pr: 84
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 02475f9a532c
recorded_by: oros-studio-garden-ce242c49
---

# Panel run — kriscendobot/minion.town #84 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `c8df6c2a`

seat verdicts (30): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=pass orthographer=pass packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=must-fix surfacer=comment transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix**: `deploy/clips/clipometer/src/app.js:86` says `connectBack()` "Returns the `back` far reference plus a `...
- archivist: **should-fix**: The header of `deploy/clips/clipometer/scripts/assert-lockdown.mjs` (`:372-381`) and the `SES_MARKERS...
- archivist: **should-fix**: In `scripts/assert-lockdown.mjs`, the failure message (`:437`) always blames the "lockdown() side eff...
- archivist: **should-fix**: The comment at `scripts/publish.mjs:414-415` says `checkToolResult` throws on "either" of two shapes....
- archivist: **should-fix**: `README.md:32-34` says the header of `app.js` carries "PROVENANCE / SES-REQUIRED / TRANSPORT / `back`...
- archivist: **comment-only**: `.github/workflows/test.yml:19-20` says the pure suite "only exercises `findMissingMarkers`". That ...
- archivist: **comment-only**: `publish.mjs:5-6` cites "the transcription-corruption risk the clip primer documents", but no prime...
- archivist: **comment-only**: Some quoted status strings don't match the UI. `app.js:78` quotes a "connecting…" status, but `:2...
- assessor: **should-fix: a bad CapTP message is logged and dropped, not failed.** `deploy/clips/clipometer/src/app.js:120-141`. ...
- assessor: **should-fix: the connect timeout stops too early.** `src/app.js:115-119`, `:175-184`. The `'open'` handler clears th...
- assessor: **should-fix: `checkToolResult` accepts ambiguous `serving` values.** `scripts/publish.mjs:358-367`. It only rejects ...
- assessor: **comment-only: the socket stays open after a counting error.** `src/app.js:257-263`. When the read or write fails, `...
- assessor: **comment-only: the engine check in `index.html` is only a proxy.** `index.html:505-517`. Safari 15.4–16.3 has `Obj...
- assessor: `parseMcpBody` (`publish.mjs:234-241`): when an `id` is given but matches no frame, it silently falls back to the las...
- breaker: **should-fix: the connect deadline stops too early, so the page can still hang.** The comment at `src/app.js` (`CONNE...
- breaker: **should-fix: `checkToolResult` accepts unclear `serving` values.** Its JSDoc (`scripts/publish.mjs`) says the helper...
- breaker: **comment-only: the check doesn't run on the exact bytes that get published.** The comment on `assertBundleHardened` ...
- breaker: **comment-only: the "narrowing" note overstates what it does.** It says the code refuses to retain the wide `back` re...
- breaker: **comment-only: `parseCount` can reject a value the app itself wrote.** If the stored count is `"9007199254740991"` (...
- breaker: **Mitigated:** `assert-lockdown` only checks that SES code is present, not that `lockdown()` was called. If a build d...
