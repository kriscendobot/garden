---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 977
panel_kind: code
base_ref: origin/llm-0ac48c5
rounds: 6
disposition: seat-error
must_fix_total: 120
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2fb870bd9b99
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #977 (code)

Terminal disposition: **seat-error** after **6** round(s).

## Round 1 — head `dad2cf08`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=comment fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=pass prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- assessor: `packages/daemon/test/endo.test.js:6268` — must-fix. The `@host` assertion cannot distinguish "method absent" from ...
- assessor: `packages/daemon/test/endo.test.js:6249` — should-fix. The exact-set `deepEqual` is a deliberate tripwire, but noth...
- assessor: The tightened matcher from the first finding would also generalize the 7 `hostOnlyCalls`: `new RegExp(methodName)` at...
- assessor: This test's guest half subsumes the `provideHostPath` assertions in the immediately preceding test (`:6161`–`:6175`...
- breaker: `packages/daemon/test/endo.test.js:6215` — the gold list is compared against `hostMethods.filter(n => !guestMethods...
- breaker: `packages/daemon/test/endo.test.js:6247` — `{ message: new RegExp(methodName) }` passes for the wrong reason. Every...
- breaker: `packages/daemon/test/endo.test.js:6262` — the `@host` probe is the weakest assertion in the file against the most ...
- breaker: The `deepEqual` holds only because `__getMethodNames__` sorts (`@endo/eventual-send` `local.js:74`, `compareStringifi...
- breaker: Breaker's capability-overlap lane: the guest keeps `evaluate` and `adopt`, so unconfined execution could be reached b...
- corner-prober: **`must-fix-loop` — the set difference is pinned one-sidedly; guest-widening passes green.** `hostMethods.filter(n ...
- corner-prober: **`must-fix-loop` — the `@host` probe can pass vacuously.** `parentHandle` is never asserted to be a live remotable...
- corner-prober: **`summary-fix` — `message: new RegExp(methodName)` is not load-bearing for absence.** All 7 calls pass deliberatel...
- corner-prober: **`summary-fix` — reserved-name enumeration is a singleton, not a sweep.** The guest holds `@agent`, `@self`, `@hos...
- corner-prober: **`summary-fix` — the direct-send door is probed, the evaluator door is not.** `evaluate(worker, source, codeNames,...
- corner-prober: **`comment-only` — guest provenance is a singleton.** Only a locally `provideGuest`-minted guest is tested; the com...
- fast-checker: ## Juror: fast-checker — PR endojs/endo-but-for-bots#977
- integrator: **[must-fix]** PR description, Description ¶3 vs Upgrade Considerations — self-defeating disclosure. ¶3 says the ...
- integrator: **[should-fix]** No `Refs:` / `Closes:` line. The template opens with "All PRs should at least reference one or more ...
- integrator: **[should-fix]** Testing Considerations carries fleet-internal provenance: "a temporary, uncommitted short socket dir...
- integrator: **[should-fix]** `endo.test.js:6178` titles the test `'host-only capabilities…'`; its sibling at `:6132` uses the p...

## Round 2 — head `dad2cf08`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=pass prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=pass surfacer=pass transplanter=pass typist=pass warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR endojs/endo-but-for-bots#977
- assessor: `packages/daemon/test/endo.test.js:6267-6274` — the `@host` assertion can pass vacuously. If `lookup(['@host'])` re...
- assessor: `packages/daemon/test/endo.test.js:6260` — `new RegExp(methodName)` is unanchored, and the thrown message enumerate...
- assessor: `packages/daemon/test/endo.test.js:6227-6243` — the comment states the call set covers operations that "execute Nod...
- assessor: `packages/daemon/test/endo.test.js:6256-6259` (comment-only) — `t.false(guestMethods.includes(methodName))` is enta...
- assessor: Static verification passed: the pinned 40 names are exactly `host.js:2421-2516` minus `guest.js:340-393`, and `getMet...
- assessor: The exhaustive `deepEqual` will fail on any future host-only method addition. That is the intended canary, but a one-...
- assessor: The test is registered as plain `test`, not gated like `testNeedsNodeWorker` (`:6161`); whether the `test:go`/`test:r...
- breaker: **should-fix** — `packages/daemon/test/endo.test.js:6249`. Invariant (b) is not what the diff pins. Attack: add `ma...
- breaker: **should-fix** — `endo.test.js:6267-6275`. The second surface is probed with one method out of the seven already en...
- breaker: **comment-only** — `packages/daemon/src/guest.js:94-103` seeds `@agent`, `@self`, `@host`, `@nets`, `@planes`, `@ma...
- breaker: **comment-only** — `endo.test.js:6260`. `new RegExp(methodName)` does not discriminate: `localApplyMethod` formats ...
- corner-prober: **`@host` probe can pass vacuously — `must-fix-loop`.** `parentHandle` is never asserted to be a remotable. If `'@h...
- corner-prober: **Unanchored `new RegExp(methodName)`; identity/prefix collision — `summary-fix`.** The `target has no method X, ha...
- corner-prober: **Set direction is one-way; `define` already violates the implied subset — `summary-fix`.** The filter is host→gu...
- corner-prober: **Reserved-name set probed as a singleton — `summary-fix`.** A guest gets six (`guest.js:94-103`): `@agent` (the gu...
- corner-prober: **33 of 40 names are reflection-only — `comment-only`.** `hostOnlyCalls` invokes 7. Highest-blast uninvoked: `provi...
- engine-realist: `packages/daemon/test/endo.test.js:6260` — `t.throwsAsync(call, { message: new RegExp(methodName) })` does not dist...
- engine-realist: `packages/daemon/test/endo.test.js:6267-6275` — the `@host` handle leg has the same weak matcher and, unlike the gu...
- engine-realist: `:6227` — the stated criterion ("execute Node.js code outside a compartment") also selects `provideShell`, `provide...

## Round 3 — head `dad2cf08`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=pass pruner=pass purist=must-fix releaser=pass saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=pass warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: `packages/daemon/test/endo.test.js:6229-6232` — the comment "These operations either execute Node.js code outside a...
- archivist: `packages/daemon/test/endo.test.js:6180-6218` — `hostOnlyMethodNames` is a frozen golden list asserted with `deepEq...
- archivist: `:6247` — "its parent's mail handle" is accurate (`src/guest.js:97` maps `@host` → `hostHandleId`), but the packa...
- archivist: The new test subsumes the narrower `provideHostPath` guest assertion at `:6133-6176`; neither references the other. A...
- assessor: `packages/daemon/test/endo.test.js:6234` — the `t.throwsAsync(call, { message: new RegExp(methodName) })` assertion...
- assessor: `packages/daemon/test/endo.test.js:6221-6224` — the rationale comment misdescribes three of the seven pinned calls....
- assessor: `packages/daemon/test/endo.test.js:6217` — `hostOnlyMethodNames` is an exact-equality pin with no maintenance note,...
- assessor: I verified the 40-name array statically: `getInterfaceMethodKeys(HostInterface)` minus `GuestInterface` in `src/inter...
- assessor: The `deepEqual` is self-guarding against a broken `guest` (an empty guest surface would fail it), which is good; the ...
- assessor: `guest` retains `evaluate` (confined) — correctly excluded from the host-only set. [proposed-rule: none]
- breaker: `packages/daemon/test/endo.test.js:6249` asserts `hostMethods.filter(name => !guestMethods.includes(name))` equals a ...
- breaker: `packages/daemon/test/endo.test.js:6270` probes `parentHandle` with exactly one method name, yet the handle is the on...
- breaker: `packages/daemon/test/endo.test.js:6263`: `{ message: new RegExp(methodName) }` does not pin the throw site. The inte...
- breaker: `evaluate` is deliberately absent from the list (both agents carry it), so the pinned boundary is unconfined Node exe...
- breaker: The test largely subsumes the preceding `provideHostPath is an EndoHost-only capability ...` test at line 6155; consi...
- engine-realist: **should-fix — the `@host` handle is the one surface with no interface guard, and it gets a one-method spot-check.*...
- engine-realist: **should-fix — `{ message: new RegExp(methodName) }` can pass for the wrong reason, and the 7-call subset is arbitr...
- engine-realist: **comment-only — tripwire legibility.** `EndoHost` is at 91 methods and `interfaces.js:511` already notes pressure ...
- fast-checker: ## Juror: fast-checker — PR endojs/endo-but-for-bots#977
- integrator: **must-fix — the throw assertions do not pin what the description claims they pin.** `packages/daemon/test/endo.tes...

## Round 4 — head `dad2cf08`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=comment changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=comment fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=pass prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=pass warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: `packages/daemon/test/endo.test.js:6216-6219` — the comment above `hostOnlyCalls` ("These operations either execute...
- archivist: `packages/daemon/test/endo.test.js:6229` — the `t.deepEqual` message, "the complete EndoHost-only surface remains a...
- archivist: `packages/daemon/test/endo.test.js:6178` — the test has no leading prose, unlike both tests it sits between (`:6133...
- archivist: The `@host` comment at `:6248-6250` checks out: `packages/daemon/src/guest.js:97` binds `'@host'` to `hostHandleId`, ...
- archivist: Overlap with the preceding `provideHostPath is an EndoHost-only capability…` test (`:6133`) is near-total for the `...
- assessor: `packages/daemon/test/endo.test.js:6262-6272` — the `@host` sub-assertion can pass vacuously. `E(guest).lookup(['@h...
- assessor: `packages/daemon/test/endo.test.js:6243` — `{ message: new RegExp(methodName) }` matches any rejection that merely ...
- assessor: `packages/daemon/test/endo.test.js:6234-6241` — the `t.false(guestMethods.includes(methodName))` inside the loop is...
- assessor: Call-level proof covers 7 of 40 host-only names; the other 33 are pinned by name only. The comment at 6216-6219 justi...
- assessor: The pin fails on any benign new host-only method. That is the tripwire's purpose; the assertion message ("the complet...
- corner-prober: ## Juror: corner-prober — PR #977 (endojs/endo-but-for-bots)
- fast-checker: ## Juror: fast-checker — PR #977 (endojs/endo-but-for-bots)
- integrator: PR body, Testing Considerations: "A temporary, uncommitted short socket directory was used because the isolated **job...
- integrator: PR body, Description and Upgrade Considerations: two sentences point at an unnamed private remediation ("tracked priv...
- integrator: PR body: no `Closes:` / `Refs:` line, which the template's leading instruction asks of every PR. If no public issue e...
- integrator: `packages/daemon/test/endo.test.js:6178`: the new test is inserted between `provideHostPath is an EndoHost-only capab...
- integrator: `packages/daemon/test/endo.test.js:6178`: this generalized test subsumes territory already held by three existing tes...
- integrator: `packages/daemon/test/endo.test.js:6180`: `hostOnlyMethodNames` is an exhaustive pin, so the next EndoHost method add...
- integrator: Title `host-only capabilities are not reachable through an EndoGuest` uses `host-only` where the sibling directly abo...
- integrator: `6236-6244`: per-method `t.false(guestMethods.includes(methodName))` re-asserts what the `deepEqual` at 6229 already ...

## Round 5 — head `dad2cf08`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=comment migrator=must-fix packager=pass prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=must-fix transplanter=pass typist=pass warden=must-fix wire-watcher=pass
must-fix items (20):
- archivist: **must-fix — `test/endo.test.js:6227-6230`: the rationale comment misdescribes 3 of the 7 calls it introduces.** It...
- archivist: **should-fix — `:6252`: the `deepEqual` failure message describes only one of two failure modes.** "the complete En...
- archivist: **comment-only — `:6264-6266` verified accurate.** "the guest's reserved `@host` name is its parent's mail handle, ...
- assessor: **should-fix** — `test/endo.test.js:6260` and `:6268-6272`. The missing-method error embeds the recipient's *entire...
- assessor: **should-fix** — `:6247-6252`. The `deepEqual` golden list fails on any *purely additive* host method, in an unrela...
- assessor: **comment-only** — `:6260`: `new RegExp(methodName)` omits `u` while every nearby regex literal carries `/u` (`:627...
- assessor: **comment-only** — 7 of 40 pinned names get an actual CapTP call; the block comment at `:6215-6218` reads as if it ...
- benchmarker: I fetched the PR metadata and the actual head to make sure I wasn't reviewing a superseded state — the worktree is ...
- breaker: `packages/daemon/test/endo.test.js:6249` — the failure matcher `{ message: new RegExp(methodName) }` is unanchored ...
- breaker: `packages/daemon/test/endo.test.js:6222-6234` — the `hostOnlyCalls` keys are never cross-checked against `hostOnlyM...
- breaker: `packages/daemon/test/endo.test.js:6240-6245` — the `deepEqual` pins only the host∖guest **difference**, so a pri...
- breaker: `packages/daemon/test/endo.test.js:6259-6268` — the `@host` handle is probed with a single negative method. `Handle...
- breaker: Matchers pin message only, never an error class, contra the "pin the class and message regex" step. [rule: skills/adv...
- breaker: The guest's `evaluate` / `adopt` / `request` recovery routes to host authority remain unpinned by this test; a siblin...
- corner-prober: **Reserved-name reachability enumerated at 2 of 6.** A guest's special names are `@agent`, `@self`, `@host`, `@mail`,...
- corner-prober: **The `@host` probe is a singleton where the direct probe is exhaustive** (`:6267-6277`). The handle exo exposes only...
- corner-prober: **`hostOnlyCalls` (`:6231`) omits the strongest members of its own stated class and is unpinned against the name list...
- corner-prober: **`provideGuest` options domain tested at one of three boundary values.** Only `{agentName}` is exercised. Omitted-op...
- corner-prober: *(comment-only)* `new RegExp(methodName)` is unanchored, so `/makeUnconfined/` is also satisfied by a `makeUnconfined...
- engine-realist: `packages/daemon/test/endo.test.js:6264-6274` — the `@host` block's *only* assertion is `{ message: /makeUnconfined...

## Round 6 — head `dad2cf08`

seat verdicts (28): archivist=must-fix assessor=comment benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=comment prover=comment pruner=comment purist=must-fix releaser=comment saboteur=comment scribe=comment spec-keeper=comment stylist=comment surfacer=comment transplanter=comment typist=comment warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: `makeArchive` (`packages/daemon/src/host.js:1434`) and `makeFromTree` (`packages/daemon/src/host.js:1626`) formulate ...
- archivist: `endow` (`packages/daemon/src/host.js:2244`) fulfils a guest's pending define request by evaluating source in a worke...
- breaker: **should-fix — the `@host` handle is probed negatively, never pinned.** `HandleInterface` is `M.interface('EndoHand...
- breaker: **should-fix — what the guest *does* reach through `@host` goes unexamined.** `receive(envelope, allegedFromId)` (`...
- breaker: **comment-only — `{ message: new RegExp(methodName) }` is a weak oracle.** Any rejection *mentioning* the name pass...
- breaker: **comment-only — the golden list needs a hand-off message.** Any PR adding an `EndoHost` method now fails with a 40...
- integrator: ## integrator
- locksmith: **should-fix.** The test name and the `@host` comment claim "host-only capabilities are not reachable through an Endo...
- locksmith: **comment-only.** `endo.test.js:6215` pins the *difference* between the two surfaces. A method added to `HostInterfac...
- locksmith: **comment-only.** `new RegExp(methodName)` (`endo.test.js:6228`) is unanchored. A future guest that gains `endow` und...
- locksmith: **comment-only.** The live-call probe covers the unconfined-execution family only. The credential family (`sign`, `pr...
- locksmith: **comment-only.** `@host` is the only reserved name probed; the guest's special names are `@agent`, `@self`, `@host`,...
- locksmith: The 40-name list is exact. I derived the host-only set from the interface guards (`getInterfaceMethodKeys(HostInterfa...
- locksmith: `define` is the one guest-only method (on `EndoGuest`, absent from `EndoHost`). Unrelated to this diff, but a reader ...
- locksmith: **Not verified by execution.** `npx ava test/endo.test.js -m 'host-only capabilities...'` fails with `ENOENT ... endo...
- purist: `packages/daemon/test/endo.test.js:6260` — the negative probe `{ message: new RegExp(methodName) }` cannot distingu...
- purist: `packages/daemon/test/endo.test.js:6231-6262` — the `hostOnlyCalls` keys are never cross-checked against `hostOnlyM...
- purist: `packages/daemon/test/endo.test.js:6228-6230` — family-consistency. The stated selection criterion is "execute Node...
- purist: `packages/daemon/test/endo.test.js:6249-6253` — the `deepEqual` asserts a stronger and different claim than the tit...
- purist: `:6260` uses `new RegExp(methodName)` while `:6272` uses `/makeUnconfinedFromTree/u`; one test, two flag conventions....
