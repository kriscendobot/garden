---
kind: panel-run
repo: kriscendobot/minion.town
pr: 64
panel_kind: design
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 16
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 60ece2a7c4cf
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #64 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `5fb1db0c`

seat verdicts (7): copyeditor=comment critic=must-fix decomplector=comment ergonomist=must-fix novice=must-fix pedant=pass skeptic=must-fix
must-fix items (16):
- critic: The `## Capability shape` and `## User-driven authentication` sections re-introduce the full `@claude-account` concie...
- critic: The `## Production sequence and stop gates` confinement canary (step 4) only exercises a depth-1 child's confinement ...
- critic: The entire indelible-names mechanism (`introducedSpecialNames` on `provideGuest`) is a prerequisite this PR cannot la...
- ergonomist: `ClaudeAgent.run(prompt)` is the only entry point that actually drives inference, yet the design's own vocabulary for...
- ergonomist: The two admission-limit outcomes use near-synonyms for the same shape of failure: `ClaudeAgents.create`'s retained-ch...
- ergonomist: `run` is explicitly documented as "never a rejection" (§ Capability shape); by parallel construction `create` presum...
- ergonomist: `ClaudeAgents.list()` returns bare `readonly string[]`, while `create()`'s return is a live, capability-bearing `Clau...
- ergonomist: `ClaudeAgent.guest()` returns the raw `EndoGuest`, which leaks an implementation-level object onto an otherwise task-...
- novice: `## Capability shape`, paragraph beginning "Each *introduction*...": introduces "factory **facet**" ("mints a fresh f...
- novice: `## User-driven authentication`, step 4: "The server stores the token through the `ClaudeCredentials` subscription **...
- novice: `## Production sequence and stop gates`, step 1: "Land `introducedSpecialNames`, the subscription **materialization p...
- novice: The "Admission is bounded by per-user quota data..." paragraph (`## Capability shape`) packs five distinct claims (qu...
- skeptic: The setup-token workflow (§ User-driven authentication, step 3) is the load-bearing security choice ("does not put s...
- skeptic: Test-catalog completeness gap: `ClaudeAgents.create` exposes a caller-controlled `options?: { model?: string }`, but ...
- skeptic: Rationale-integrity gap: § Open questions asserts "None block the production-validation slice," but the design's own...
- skeptic: The claimed `guest.js`/`host.js` line ranges and `specialNames`/`platformNames` shapes were checked against `endojs-e...
