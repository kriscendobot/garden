---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1125
panel_kind: code
base_ref: ff3ca7d45d706b55cf56480f36aea4eed67ae540
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 70a2d9845dd7
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1125 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `d9297be4`

seat verdicts (30): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=pass engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=comment migrator=pass orthographer=comment packager=pass prover=must-fix pruner=pass purist=comment releaser=must-fix saboteur=comment scribe=comment spec-keeper=pass stylist=must-fix surfacer=pass transplanter=pass typist=must-fix warden=comment wire-watcher=must-fix
must-fix items (20):
- assessor: Now I have enough to write the per-juror block.
- breaker: Now writing the per-juror block.
- corner-prober: **No test exercises the concurrent/racing `accept`↔`accept` or `accept`↔`cancel` case the PR's own design comment...
- corner-prober: **"Superseded" invitation case is claimed but untested.** The changeset (line 20) and code comment (`manager.js:1015-...
- corner-prober: **New validation error paths on `provideGuest({ pins, nets })` are entirely untested.** `host.js:449-478` adds four t...
- corner-prober: **The pre-existing-guest-without-pin-directories fallback at `accept()` time is untested.** `manager.js:1089-1100` ha...
- corner-prober: **Minor: `EndoGuest.invite` with a multi-segment `NameOrPath` (`namePath.length > 1`) is untested**, unlike `EndoHost...
- engine-realist: `packages/daemon/src/mail.js:114-131` (`reincarnateMailboxPins`) walks every pin directory with `Promise.all(pinDirec...
- engine-realist: Same location, should-fix: `reincarnateMailboxPins` unconditionally re-walks and re-`provide()`s **every** entry in *...
- engine-realist: `manager.js` `accept()`'s new atomic-consume ordering (rebind `guestName` + cancel controller inside `invitationJobs`...
- fast-checker: **[must-fix-loop]** `packages/daemon/src/manager.js:962-1032` — The PR's own doc-comment claims the `invitationJobs...
- fast-checker: **[summary-fix]** Same site — concrete property: `fc.assert(fc.asyncProperty(fc.array(fc.constantFrom('accept','can...
- fast-checker: **[summary-fix]** `packages/daemon/src/directory.js:189` (new `readOnly()`) — this is a textbook round-trip/equival...
- fast-checker: **[comment-only, low priority]** `packages/daemon/test/mail-pins.test.js` — `reincarnateMailboxPins`'s real contrac...
- integrator: **[must-fix]** — PR description contradicts the code it describes, and misattributes a shipped behavior to an unrel...
- integrator: **[comment-only]** — Rename sweep for `hostAgent`/`hostHandle` → `invitingAgent`/`invitingHandle` is complete and...
- integrator: **[comment-only]** — Commit grouping is sensible (feat → docs → test(chat) → fix(spaces-util) is inverted in ...
- prover: `packages/daemon/src/manager.js:6834` adds a per-invitation `invitationJobs` serial-job queue specifically so that a ...
- prover: `manager.js:7003-7020` adds a fallback for guest formulas "deployed before pin directories existed" (`invitingFormula...
- prover: `host.js:2024-2054`'s new `pins`/`nets` option validation in `provideGuest` (three distinct `makeError` throws for a ...
