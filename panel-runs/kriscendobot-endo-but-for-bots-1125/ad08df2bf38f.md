---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1125
panel_kind: code
base_ref: ff3ca7d45d706b55cf56480f36aea4eed67ae540
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: ad08df2bf38f
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1125 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `d91b1efe`

seat verdicts (30): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=must-fix engine-realist=comment fast-checker=comment gateway=pass integrator=pass locksmith=comment migrator=pass orthographer=comment packager=pass prover=comment pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=pass stylist=must-fix surfacer=comment transplanter=pass typist=comment warden=pass wire-watcher=comment
must-fix items (20):
- archivist: `packages/spaces-util/src/formula-view-registry.js:86` — the `guestPins`/`hostPins` override-label comment says the...
- archivist: `designs/formula-inspector.md:175` — "The catalog covers all 33 formula types currently in `formula-type.js`" is st...
- archivist: None.
- assessor: **should-fix** — `packages/daemon/src/mail.js:105` (function `reincarnateMailboxPins`, called from `deliver()` at `...
- assessor: **should-fix** — `packages/daemon/src/manager.js:6821` (invitation `accept()`, `guestPinName` derivation). `guestPi...
- breaker: **[should-fix]** `packages/daemon/src/manager.js:7057` — The `guestPinName` collision-avoidance invariant the code ...
- breaker: **[should-fix]** `packages/daemon/src/directory.js:696` — `EndoDirectory.readOnly()` is only shallowly read-only (s...
- breaker: **[comment-only]** `packages/daemon/src/manager.js:6785` — `EndoGuest.invite`'s "no network authority" claim (guest...
- corner-prober: cancel() before accept (slot freed, invitation subsequently unredeemable).
- corner-prober: cancel() after accept() (must be the promised idempotent no-op, not an error and not un-naming the accepted guest).
- corner-prober: accept() and cancel() issued concurrently on the same invitation (the exact race `invitationJobs` was added to preven...
- duality-auditor: `GuestFormula.hostAgent`/`hostHandle` (packages/daemon/src/types.d.ts:216-217) name the creating **host**, but this P...
- duality-auditor: Compounding the above: `packages/chat/test/unit/formula-view-registry.test.js`'s new test "the guest spec surfaces th...
- duality-auditor: `guestPins`/`hostPins` (packages/daemon/src/types.d.ts:224-227) are a well-formed pair once read carefully — `hostP...
- purist: `packages/daemon/src/manager.js:6877` — `guestPinName = \`guest-${guestNamePath.join('-')}\`` joins the guest name ...
- purist: `packages/daemon/src/manager.js:2820` and `:3207` — `EndoDirectory.readOnly()` on `mailHub`/`messageHub` returns `m...
- purist: The rest of the PR reuses the existing `ReadableNameHub`/`readable-directory` machinery cleanly rather than inventing...
- saboteur: **[must-fix] Ambiguous path-join lets two distinct guest invitations collide on one retention key.** `packages/daemon...
- saboteur: **[should-fix] Unbounded, guest-controlled fan-out on every mailbox delivery.** `reincarnateMailboxPins` (packages/da...
- saboteur: **[mitigated]** Concurrent/replayed `accept()` and `cancel()` races on a single invitation: covered by the new `invit...
