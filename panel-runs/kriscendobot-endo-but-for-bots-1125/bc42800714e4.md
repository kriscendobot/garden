---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1125
panel_kind: code
base_ref: ff3ca7d45d706b55cf56480f36aea4eed67ae540
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: bc42800714e4
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1125 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `f4566ca9`

seat verdicts (30): archivist=pass assessor=comment benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=comment migrator=pass orthographer=comment packager=pass prover=pass pruner=pass purist=must-fix releaser=comment saboteur=comment scribe=comment spec-keeper=pass stylist=must-fix surfacer=pass transplanter=comment typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- benchmarker: **Open optimization claim: `reincarnateMailboxPins`'s per-message pin-directory walk has no posted measurement and no...
- benchmarker: **Minor, same gap: `EndoDirectory.readOnly()` unmemoized formula-minting (engine-realist, comment-only) is likewise u...
- breaker: `packages/daemon/src/manager.js:6940-6970` (invitation `accept()`): the docstring at `manager.js:6915-6939` claims th...
- breaker: `packages/daemon/src/manager.js:6786-6799,6855-6875` and `guest.js:355-390` (`EndoGuest.invite`): the JSDoc states pl...
- breaker: `packages/daemon/src/mail.js:99-135` (`reincarnateMailboxPins`): the docstring justifies `Promise.allSettled` specifi...
- duality-auditor: `InvitationFormula.hostAgent`/`hostHandle` were renamed to `invitingAgent`/`invitingHandle` (`packages/daemon/src/typ...
- duality-auditor: `mail.js`'s new `reincarnateMailboxPins` (`packages/daemon/src/mail.js:100-133`) performs the identical shape as `man...
- engine-realist: Confirmed. Now I'll submit the review.
- integrator: The PR body abandons the upstream `.github/PULL_REQUEST_TEMPLATE.md` shape entirely: it has no `Closes:`/`Refs:` issu...
- integrator: None. Rename sweep (`hostAgent`/`hostHandle` → `invitingAgent`/`invitingHandle`, and `nets` → `networks` on the n...
- integrator: The related design PR #1227 (bot-supervision incarnation) is explicitly reconciled in the body as out-of-scope/distin...
- integrator: `MakeHostOrGuestOptions` was correctly split into a guest-only `MakeGuestOptions` so `pins`/`networks` no longer sile...
- purist: `packages/daemon/src/manager.js:2811` and `:3192` — `MailHub`/`MessageHub`'s new `readOnly()` returns `mailHub`/`me...
- purist: `packages/daemon/src/manager.js:6360-6369` (`getAllNetworkAddresses`) — when a guest's `@nets` formula is `readable...
- purist: `formulateReadOnlyDirectory` (manager.js:5271) mints a fresh formula/id on every `directory.readOnly()` call rather t...
- stylist: **`formulateReadOnlyDirectory` breaks the established `formulate<FormulaType>` naming pattern.** The new formula type...
- stylist: **`normalizeHostOrGuestOptions`'s own JSDoc `@param` type contradicts its name and one of its two call sites.** The f...
- typist: **Must-fix — inline `import()` JSDoc despite an existing `@import` alias.** `packages/daemon/test/endo.test.js:1426...
- typist: **Should-fix — typist-hostile arrow in an added comment.** `packages/daemon/test/formula-record.test.js:104`: `// R...
- typist: `InvitationFormula`'s new optional `invitingAgent`/`invitingHandle`/`hostAgent`/`hostHandle` fields are correctly doc...
