---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1124
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2ef62426d3c3
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #1124 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `54da570a`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=pass packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — the README calls `remoteDesignator` authenticated; the type doc landed in the same PR says it is spoof...
- archivist: **should-fix — "its `get` is usable directly as `makeOcapn`'s `locator`" is false.** `packages/daemon/src/networks/...
- archivist: **should-fix — the one security-relevant knob is undocumented in the export's JSDoc.** `formula-nonce-locator.js:11...
- archivist: **should-fix — stale paragraph beside changed code.** `packages/ocapn/src/client/index.js:172-177` still states fla...
- archivist: **comment-only — the changeset reads as if the daemon now serves formula nonces.** Nothing outside tests constructs...
- archivist: **comment-only — undocumented drive-by public type.** `packages/daemon/types.d.ts:5,51` re-exports `EndoMountContro...
- archivist: **comment-only — "crosses `missBound`"** (`:90`, changeset:30) vs `misses >= missBound` (`:217`): it *reaches*, not...
- assessor: `packages/daemon/types.d.ts:5,51` — the `EndoMountControl` re-export landed in the "close the pipelining bypass" co...
- assessor: No test drives K *concurrent* `bootstrap.fetch` frames against the bound, which is the exact behavior commit `53af1a4...
- breaker: **should-fix — the miss bound dies with the connection, and the abort *causes* the reconnect.** JSDoc claims the bo...
- breaker: **should-fix — the counter is lifetime, not rate; an honest peer gets severed.** JSDoc: "a peer that holds a valid ...
- breaker: **should-fix — log amplification from unbounded attacker bytes.** `logger.error('… presentation missed', error)` ...
- breaker: **comment-only — non-oracularity is value-uniform, not timing-uniform.** Malformed / wrong-node presentations retur...
- breaker: **comment-only — the unbounded `get` is the easy mis-wire.** `makeLocatorForSession` is optional and nothing in the...
- breaker: **comment-only — the "one turn courtesy" comment (`index.js`, `abortSession`) over-claims.** `abort()` runs `doUnpl...
- changeset-auditor: **Body overstates the designator's authentication — must-fix.** The changeset says the per-session locator is built...
- changeset-auditor: **Enumerated signature is incomplete.** The body spells out `makeFormulaNonceLocator({ provideLocalFormula, localNode...
- changeset-auditor: **Unmentioned public surface: `EndoMountControl`.** `packages/daemon/types.d.ts` newly re-exports `EndoMountControl` ...
- corner-prober: ## corner-prober — PR #1124 (kriscendobot/endo-but-for-bots)
- duality-auditor: `packages/daemon/test/formula-nonce-locator.test.js:39,58` — the string↔bytes edge is mislabeled and the labeled ...
