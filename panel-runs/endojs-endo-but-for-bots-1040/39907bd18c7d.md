---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1040
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: error
must_fix_total: 15
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 39907bd18c7d
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1040 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `91e55f98`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=pass integrator=pass locksmith=pass migrator=pass packager=must-fix prover=must-fix pruner=comment purist=comment releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=comment surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=pass
must-fix items (15):
- assessor: `packages/hardened262/scripts/agents/xs.js:24-28` — the `xs`/`sesXs` agent reads `test.module`, `test.raw`, and `te...
- assessor: `packages/hardened262/scripts/agents/node.js:9-16` — unlike `xs.js`'s (broken but intended) raw-skip branch, `node....
- assessor: `packages/hardened262/scripts/test.js`'s `generateScenariosForTests` treats each `test262-stream` item as the single ...
- breaker: `packages/hardened262/scripts/agents/xs.js:24,25,27` — `testXs` reads `test.module`, `test.raw`, and `test.includes...
- breaker: Related, same code path — `packages/hardened262/scripts/agents/xs.js:20,25-28`: `test.contents` (written to `tempor...
- breaker: `packages/hardened262/scripts/agents/xs.js:378` — `unlinkSync(temporaryFile)` runs only on `ok: true`; failing scen...
- breaker: Capability-attack overlap (locksmith secondary surface): this package claims no attenuated capability boundary — it...
- packager: `yarn.lock` churn (adding the `@endo/hardened262` workspace entry and the `test262-stream@npm:^1.4.0` range) is bundl...
- packager: None. Commit split otherwise reads clean: `53e694691` cleanly isolates the tsconfig/typedoc exclude-list fix from the...
- prover: `packages/hardened262/scripts/test.js:59` — `flags.onlyStrict = flags.strict` unconditionally overwrites the front-...
- prover: Related, lower severity: because `agentRunsScenario` (line 195-196) only ever runs the `module`/`lockdownModule` scen...
- prover: `scripts/test.js`'s `main()` never sets a non-zero exit code on a `not ok` result (only the outer `.catch` sets `proc...
- saboteur: **Must-fix — XS-agent scenarios never actually run in the parse-goal they claim.** `packages/hardened262/scripts/ag...
- saboteur: **Must-fix — real `onlyStrict` front-matter flag is clobbered, not promoted.** `packages/hardened262/scripts/test.j...
- scribe: **Missing completion-summary comment for the round-1 gauntlet response.** The panel's round-1 review (`#pullrequestre...
