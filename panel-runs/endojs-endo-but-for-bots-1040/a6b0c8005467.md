---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1040
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a6b0c8005467
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1040 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `92fdc112`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=comment locksmith=pass migrator=pass packager=must-fix prover=comment pruner=must-fix purist=pass releaser=pass saboteur=comment scribe=must-fix spec-keeper=comment stylist=must-fix surfacer=comment transplanter=pass typist=pass warden=comment wire-watcher=comment
must-fix items (20):
- assessor: `packages/hardened262/scripts/agents/node.js:15` passes `test.file` (the on-disk path) to `node-helper.js`, which imp...
- assessor: `generateScenariosForTests` (`packages/hardened262/scripts/test.js:77-80`) skips the `Strict` scenario for `raw` case...
- assessor: The rest of the diff (mirrored `harness/`/`test/` trees, LICENSE/README/tsconfig plumbing) is vendored test262-format...
- breaker: `packages/hardened262/scripts/agents/node.js:11-20` (`testNode`) never checks `scenarioIsRaw(test)`, unlike `packages...
- breaker: `packages/hardened262/scripts/agents/xs.js:60-65`: the temp file under `tmp/` is only `unlinkSync`'d on success; a fa...
- breaker: The capability-attack overlap does not apply here: this package is local dev/test tooling with no attenuated capabili...
- corner-prober: `filterNoRules` (`packages/hardened262/scripts/test.js:96-114`) has zero test coverage in `scripts/scenarios.test.js`...
- corner-prober: The raw+Strict skip (`packages/hardened262/scripts/test.js:64-69`, `if (test.attrs.flags.raw && mode === 'Strict') co...
- corner-prober: `agentRunsScenario` (`packages/hardened262/scripts/test.js:150-151`) is pure and cheaply testable (`module`/`lockdown...
- corner-prober: `qualifiers` in `generateScenariosForTests` (`packages/hardened262/scripts/test.js:99-105`) is built by computed-key ...
- engine-realist: `packages/hardened262/scripts/test.js:76` (`agentRunsScenario`) wires only the `module`/`lockdownModule` scenario nam...
- engine-realist: `packages/hardened262/scripts/agents/node.js:8-20` (`testNode`) never reads `test.contents`; it imports the on-disk `...
- engine-realist: `packages/hardened262/scripts/agents/xs.js:52-57`: `unlinkSync(temporaryFile)` runs only on the `code === 0` branch, ...
- packager: **must-fix — `yarn.lock` bundled into the substance commit instead of split into its own `chore: Update yarn.lock` ...
- packager: **comment-only — changeset bump-level (`major`) is internally consistent but worth a second look.** `.changeset/add...
- packager: **comment-only — changeset content is accurate but doesn't call out the round-2 behavior fixes.** The changeset pro...
- pruner: `packages/hardened262/README.md:76-83` — the `designs/ironhorse-test262-convergence.md` bullet re-derives the same ...
- pruner: `packages/hardened262/README.md:85-106` — the "Two consumers" subsection plus the closing paragraph is forward-look...
- pruner: SECURITY.md, CHANGELOG.md, NEWS.md, and LICENSE are byte-identical to the repo's standard per-package boilerplate (ve...
- scribe: **[open]** The round-2 gauntlet review (`#4970921406`, disposition must-fix) raised a should-fix item, itself sourced...
