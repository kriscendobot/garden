---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1040
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a84b6a4dbe73
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1040 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `91e55f98`

seat verdicts (28): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=pass locksmith=pass migrator=pass packager=must-fix prover=must-fix pruner=comment purist=comment releaser=pass saboteur=comment scribe=must-fix spec-keeper=comment stylist=must-fix surfacer=pass transplanter=pass typist=pass warden=pass wire-watcher=pass
must-fix items (20):
- assessor: `packages/hardened262/scripts/agents/xs.js:24-28` — the `xs`/`sesXs` agent reads `test.module`, `test.raw`, and `te...
- assessor: `test.module` is always `undefined`, so `-m` is never passed to `xst`. The only two scenarios actually wired to run (...
- assessor: `test.raw` is always `undefined` (falsy), so `assert.js`/`sta.js` are unconditionally passed to `xst` as separate fil...
- assessor: `test.includes` is always `undefined`, so per-test frontmatter `includes:` beyond `assert.js`/`sta.js` (e.g. `propert...
- assessor: `packages/hardened262/scripts/test.js` — `generateScenariosForTests` iterates the raw `TestStream`, but `test262-st...
- assessor: `packages/hardened262/scripts/agents/node.js` spawns `node runPath test.file ...` and `node-helper.js` resolves `test...
- breaker: `packages/hardened262/scripts/agents/xs.js:24-27` reads `test.module`, `test.raw`, and `test.includes` off the top-le...
- breaker: `test.module` is always `undefined` → `-m` is never passed to `xst`. The **only** mode scenario actually wired to a...
- breaker: `test.raw` is always `undefined` → a case declaring `flags: [raw]` (no harness wrapper wanted) still gets `assert.j...
- breaker: `test.includes` is always `undefined` → any case's declared `includes:` front matter (e.g. `propertyHelper.js` in `...
- breaker: Secondary/capability-attack overlap: none found — this diff introduces no capability-bearing runtime surface (atten...
- corner-prober: **`includes` front-matter replaces, not merges, the default `assert.js`/`sta.js` — and every current user of a cust...
- corner-prober: **Unrecognized `--agent` name is indistinguishable from a not-yet-wired scenario.** `runTests` (`scripts/test.js`) fa...
- corner-prober: **Conflicting `no*`/`only*` flags on the same axis silently yield zero scenarios.** `filterNoRules`/`filterOnlyRules`...
- corner-prober: **Minor: `strictTest`'s `insertionIndex` (`scripts/test.js:44-48`) is computed but never read anywhere in the diff** ...
- engine-realist: `packages/hardened262/scripts/agents/xs.js:24` reads `test.module` to decide whether to pass `xst -m`, but no test ob...
- engine-realist: `packages/hardened262/scripts/agents/xs.js:27` reads `test.includes` (also never set — same class of bug), so `xs`/...
- packager: **should-fix — yarn.lock churn not split into its own `chore: Update yarn.lock` commit.** Commit `a3b2e0b50` combin...
- packager: **comment-only — commit-message accuracy, praise.** Commit `a3b2e0b50`'s message itemizes six concrete adaptations ...
- packager: **comment-only — changeset and new-package shape are correct.** `.changeset/add-endo-hardened262.md` bumps `major` ...
