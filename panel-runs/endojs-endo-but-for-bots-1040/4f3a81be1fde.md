---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1040
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 4f3a81be1fde
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1040 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `53e69469`

seat verdicts (28): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=comment gateway=pass integrator=comment locksmith=pass migrator=must-fix packager=must-fix prover=comment pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=pass
must-fix items (20):
- assessor: `packages/hardened262/scripts/test.js:177-201`: `runTests` dispatches a scenario to any agent only when `scenario ===...
- assessor: `packages/hardened262/scripts/agents/node.js:18-30` and `packages/hardened262/scripts/agents/xs.js:33-45`: `spawn(......
- assessor: `packages/hardened262/scripts/test.js:179-191`: the `agent === 'xs'` branch is a bare `if` while the following `sesXs...
- breaker: `packages/hardened262/scripts/test.js:180-200` (`runTests`) only ever dispatches scenarios whose `scenario` string is...
- breaker: Deeper than the above: `grep -rn "Compartment(" scripts/` returns nothing. No agent (`scripts/agents/xs.js`, `scripts...
- breaker: `scripts/agents/xs.js:34-40` and `scripts/agents/node.js:19-27` register only `child.on('exit', ...)` with no `'error...
- breaker: `package.json`'s `test:xs` script is `"exit 0"` — always green regardless of state; consistent with README's "preli...
- engine-realist: ## engine-realist — PR #1040 (endojs/endo-but-for-bots)
- migrator: `packages/hardened262/package.json:2` sets `"version": "1.0.0"` directly, with **no `.changeset/*.md` file added anyw...
- migrator: No downstream package currently depends on `@endo/hardened262` (`grep -rl "hardened262" **/package.json` finds only t...
- packager: **[must-fix] No changeset for a brand-new package.** The diff adds `@endo/hardened262` as a new workspace package but...
- packager: **[must-fix] `package.json` version and `CHANGELOG.md` don't follow the new-package shape.** `packages/hardened262/pa...
- packager: **[must-fix] `yarn.lock` churn is bundled into the substance commit, not split out.** Commit `a3b2e0b50` ("feat(harde...
- packager: `exports: {}` and `"private": true` in the new package's `package.json` — no public-API surface introduced, so the ...
- packager: Commit `53e694691` (the tsconfig/typedoc exclude fix) correctly follows the typedoc/tsconfig-parity rule this seat ha...
- packager: `.prettierignore`/`eslint.config.js` additions correctly parallel the existing `test262-runner` corpus-exclusion patt...
- pruner: **`packages/hardened262/README.md:53-99`** — the `## Relationship to the rest of the repository` section (including...
- pruner: **`packages/hardened262/README.md:98-99`** — "this package lands the harness as its upstream author shaped it, reco...
- purist: `packages/hardened262/scripts/test.js:180-198` — `runTests` only ever matches `scenario === 'module'` or `scenario ...
- purist: `packages/hardened262/scripts/agents/node.js:1-15` vs `packages/hardened262/scripts/agents/xs.js:16-30` — the `lock...
