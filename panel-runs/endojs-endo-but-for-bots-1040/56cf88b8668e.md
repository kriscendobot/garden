---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1040
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 18
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 56cf88b8668e
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1040 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `6c528dc6`

seat verdicts (28): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=comment integrator=pass locksmith=pass migrator=pass packager=must-fix prover=pass pruner=pass purist=comment releaser=pass saboteur=comment scribe=pass spec-keeper=must-fix stylist=pass surfacer=pass transplanter=pass typist=pass warden=pass wire-watcher=pass
must-fix items (18):
- assessor: `packages/hardened262/scripts/test.js:77` — the `raw` guard only special-cases `mode === 'Strict'`, skipping strict...
- assessor: None. The rest of the diff (`generateScenariosForTests`, `filterNoRules`/`filterOnlyRules`, the `scenario.js` shared ...
- breaker: README's claimed invariant — "the harness _reports_ per-scenario pass/fail" — is falsified for every `async`-flag...
- breaker: Every other checked invariant held under direct attack: the `onlyStrict`/`noStrict`/module-aliasing filter contract (...
- breaker: `node.js:66`/`xs.js:64` only `unlinkSync` the temp file on `code === 0`; a failing scenario leaks its rendered case i...
- changeset-auditor: `.changeset/add-endo-hardened262.md:5-12` — the body is a single hard-wrapped paragraph (three sentences: "Add `@en...
- changeset-auditor: The skill this seat is told to cite (`skills/changeset-discipline/SKILL.md`) has no literal "Sentence-per-line" sub-h...
- packager: **[must-fix] `packages/hardened262/LICENSE` ships with the unfilled Apache boilerplate placeholder instead of a real ...
- packager: **[should-fix] `yarn.lock` churn is bundled into the substance commit `a3b2e0b50` instead of its own `chore: Update y...
- packager: Changeset (`add-endo-hardened262.md`): `major` bump, prose confined to what the package is, no process commentary —...
- packager: `exports: {}` + `"private": true` in `package.json` — no public `exports["."]` surface, so the src-shim/test-only-e...
- packager: Root `tsconfig.json`/`tsconfig.typedoc.json` correctly exclude `packages/hardened262` (commit `53e69469`); package ca...
- packager: `.prettierignore`/`eslint.config.js` additions correctly scope to the borrowed `test/`/`harness/`/`tmp/` trees, paral...
- packager: `SECURITY.md` is a verbatim copy of the standard boilerplate — fine.
- packager: No stray refactors or drive-by changes riding in any of the six commits; each commit's diff matches its message.
- spec-keeper: **[should-fix]** `packages/hardened262/test/harden/proto-of-property.js` is byte-identical to `packages/hardened262/t...
- spec-keeper: **[comment-only]** `packages/hardened262/test/harden/stamp.js`'s comment cites "PrivateFieldAdd (ECMA-262 §7.3.28)" ...
- spec-keeper: Verified (not a bug, noting for the record): the `async`-flagged Compartment/VirtualModuleSource tests reference `$DO...
