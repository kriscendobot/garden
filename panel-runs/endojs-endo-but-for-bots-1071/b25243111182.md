---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1071
panel_kind: design
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b25243111182
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1071 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `7f37dd1f`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: `designs/ocapn-network-transport-separation.md:241` — Test Plan still says "duplicate-**scheme** rejection", but th...
- copyeditor: `:71-75, :148` — three names for one axis. The table has a "Carrier" column (`TCP`) *and* a "Transport" column (`tc...
- copyeditor: `:53-58` vs `:60-61` — the hint grammar is stated twice, eight lines apart, in incompatible vocabularies: first as ...
- copyeditor: `:230-233` — the diff newly leans on a **location** / **locator** distinction ("OCapN location codec", "External lo...
- copyeditor: `:94-95` — "Only one published endpoint per transport-and-codec combination is allowed in a location, which the one...
- copyeditor: `:82-84` — faulty parallelism: "permits a browser **to ignore** ... while a LAN peer **can use** ...". Make the sec...
- copyeditor: `:73` — `e.g.` in newly added prose, and `:77`/`:79` in the same passage use "for example". [rule: skills/no-latin-...
- copyeditor: `:65, :175, :206` — rewrap artifacts leaving orphan fragments mid-sentence: a 6-column line holding only "Kowal,", ...
- copyeditor: `:56-58` — "alleged attributes such as `type`, consistently with [daemon-locator-reference.md]": `consistently with...
- copyeditor: `:78` shows `@wss+cbor:...@tcp+cbor:...` while `:138` shows the reverse order, and `:241` unit-tests "deterministic h...
- copyeditor: `:62-69` packs a semicolon clause, an em-dash aside, a 40-word parenthetical citation, and a block quote into one sen...
- copyeditor: `:191`/`:216` reference "PR #1072" and "PR #684" bare, while `:23` qualifies the repo and `:66` uses a full URL. Peda...
- copyeditor: `:47` — the mermaid node still reads "WebSocket + CBOR hint" though the table now splits `ws+cbor` from `wss+cbor`....
- critic: § Target API mandates that the network "validates both the composite key and **authority**", and `TransportListener....
- critic: § Target Model's `<transport>+<codec>` key conflates two orthogonal axes. For TCP, `cbor` names the *framing* — th...
- critic: § Target Model asserts "the same carrier may appear once for each supported codec (for example, `tcp+cbor` and `tcp+...
- critic: The example paths (`/@tcp+cbor:127.0.0.1:3469@wss+cbor:peer.example:443`) are unencoded, while [daemon-locator-refere...
- critic: § Test Plan is unrevised: it still says "duplicate-**scheme** rejection" and adds no hint parse/serialize round-trip...
- critic: Dropping the path from the WebSocket hint (was `wss://peer.example/ocapn-cbor-np`) forecloses reverse-proxy path rout...
- critic: `ws` vs `wss` as distinct schemes is undiscussed; `makeWebSocketTransport({ secure: true })` must flip the readonly `...
