---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 832
panel_kind: design
base_ref: 38ca1d189384245dd9accfcc2f79763a3b8ec5cb
rounds: 1
disposition: must-fix
must_fix_total: 19
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b166d8c5cf48
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #832 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `e8008dc4`

seat verdicts (7): copyeditor=comment critic=must-fix decomplector=comment ergonomist=must-fix novice=comment pedant=must-fix skeptic=must-fix
must-fix items (19):
- critic: The rationale for skipping the narrow-guard containment precedent rests on "every producer already exposes incrementa...
- critic: The design commits daemon mount to "preserve confinement and re-check revocation while reading" and the verification ...
- critic: The choice to diverge from `rangeReadText`/`textRange`'s LF-only, trailing-empty-line boundary model is justified alm...
- critic: The roadmap table's "M-L, 1 week" estimate for landing the shared adapter plus seven producer implementations (includ...
- ergonomist: **Bare `lines()` name undercuts this repo's own method-name-based feature detection.** `packages/platform/src/fs/inte...
- ergonomist: **`lines()` and `streamBase64` are the guard's only two stream-returning members but use incompatible invocation prot...
- ergonomist: The implementation table's "Public surfaces" and per-producer rows don't uniformly call out updating each producer's ...
- pedant: **Must-fix — stray typographic arrow, not ASCII.** `designs/readableblob-lines.md:219` (in "Implementations and mig...
- pedant: No em-dashes introduced (`grep` of added lines only turns up pre-existing em-dashes in untouched table rows). [rule: ...
- pedant: No other hard-to-type code points (curly quotes, ellipses, ≤/≥/≠) in added text; `§` section-reference marks a...
- pedant: All new cross-references (`readableblob-lines.md`, `fs-interface-consolidation.md`, `platform-range-and-tree-reads.md...
- pedant: Headings are consistently sentence-case across the new document; no title-case/sentence-case mixing.
- pedant: Hyphenation of compound modifiers (`zero-based`, `end-exclusive`, `non-negative`, `byte-to-line`, `one-use`) is consi...
- pedant: The Comparison/Verification tables (`| Input bytes... | lines() values |`, the migration table, the two README rows) ...
- pedant: No diagrams added outside the one mermaid node insertion, which correctly extends the existing `flowchart TD` rather ...
- pedant: No banner-style comment rules in any code block.
- skeptic: The Problem statement names "process a log ... record-by-record" as the motivating case, but a log is the one produce...
- skeptic: The design repeatedly frames the win as "flow-controlled ... so the source is not read ahead of demand" and "never ma...
- skeptic: The design narrates composing `.textRange(a, b)` then `.lines()` as a benefit of the design (byte-level composition a...
