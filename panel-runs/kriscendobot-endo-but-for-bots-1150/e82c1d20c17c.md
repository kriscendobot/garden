---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1150
panel_kind: code
base_ref: e84a4c83c049f1b113436c0fcceddfb5be9be9a4
rounds: 1
disposition: error
must_fix_total: 9
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: e82c1d20c17c
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1150 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `e8e4f452`

seat verdicts (29): archivist=comment assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=comment locksmith=comment migrator=comment packager=pass prover=comment pruner=pass purist=pass releaser=pass saboteur=must-fix scribe=error spec-keeper=comment stylist=pass surfacer=pass transplanter=pass typist=comment warden=comment wire-watcher=comment
must-fix items (9):
- breaker: `rust/endo/xsnap/src/lib.rs:1985` (and the identical pattern at `:1121`/`:1133` inside `handle_envelope`'s `debug-att...
- breaker: Capability-attack overlap (locksmith adjacency): `FILE_MAP`/`DIR_MAP` (`powers/fs.rs`), `DB_MAP`/`STMT_MAP` (`powers/...
- breaker: The `Halt::is_panic()` / `ExecutionOutcome::classify` machinery is well-isolated and its "sole source of truth" / "st...
- engine-realist: **Must-fix — `flush_debug_outbound()` is an unguarded side-effecting network send that can fire after a worker is a...
- engine-realist: **Should-fix (adjacent, lower confidence) — `handle_suspend`'s `machine.suspend_to_cas` (`lib.rs:1170`) is not re-c...
- saboteur: **Must-fix — poisoned worker can still emit an outbound send after a caught FFI panic, contradicting the design's o...
- saboteur: **Should-fix — nested `guard_ffi` fully absorbs an inner panic, silently masking it from the caller's control flow....
- saboteur: **Mitigated / acknowledged, not a new gap.** The shared process-wide `FILE_MAP`/`DIR_MAP`/`DB_MAP`/`STMT_MAP`/`HASHER...
- saboteur: **Out of scope.** `Halt::Panic` variant and the `EngineFault`/`ReferenceError` `RaiseSite` machinery target the prosp...
