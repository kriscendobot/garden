---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1150
panel_kind: code
base_ref: e84a4c83c049f1b113436c0fcceddfb5be9be9a4
rounds: 1
disposition: must-fix
must_fix_total: 18
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 9cbb5abeea59
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #1150 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `b7fa8517`

seat verdicts (29): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=comment packager=pass prover=comment pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=comment stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=pass
must-fix items (18):
- assessor: **[must-fix] `ExecutionOutcome::classify` violates its own documented delegation invariant for `Halt::Unsupported`.**...
- assessor: **[comment-only] Crank-boundary poison detection is coarser than "the next pure-Rust frame."** `rust/endo/xsnap/src/l...
- breaker: ## Per-juror block — breaker
- corner-prober: **`Option<String>` location field: only the `Some` branch is ever exercised.** Both `Display` impls that branch on `l...
- corner-prober: **`panic_payload_message`'s non-string fallback (`"unknown panic"`) is untested.** (rust/endo/xsnap/src/worker_io.rs:...
- corner-prober: **`ExecutionOutcome::classify`'s fail-closed catch-all (`debug_assert!(false, ...)`) is never triggered by a test.** ...
- corner-prober: **Zero test coverage for the `inproc.rs` outer `catch_unwind` net.** (rust/endo/src/inproc.rs:250-267) This is entire...
- corner-prober: **Empty-string panic message (`panic!()`/`panic!("")`) untested.** All `guard_ffi` panic tests use non-empty literal ...
- engine-realist: ## Per-juror block: engine-realist — PR #1150
- integrator: I have sufficient evidence. Now writing the per-juror block.
- locksmith: `rust/endo/xsnap/src/worker_io.rs:469,497,502` — `guard_ffi`, `ffi_panicked`, and `take_ffi_panic` are declared `pu...
- locksmith: `rust/endo/xsnap/src/worker_io.rs:84-99` — `install_capture_hook` takes over the process-wide panic hook once, chai...
- purist: **[should-fix] Family-consistency gap: a new `PanicKind` variant won't get its own `describe_halt` message.**
- purist: **[should-fix] `FfiPanic` and `PanicKind::EngineFault` are hand-duplicated payload shapes with no coupling.**
- saboteur: **Must-fix — poison observed only at crank boundaries, not inside the reactive-pump loop, letting a poisoned worker...
- saboteur: **Overlap note (breaker's primary axis, flagged briefly per remit):** the same gap above also falsifies the published...
- saboteur: **Mitigated:** `guard_ffi`'s early poison short-circuit (`worker_io.rs:125-128`) leaves the XS result frame slot unto...
- saboteur: **Out of scope:** `panic_payload_message`'s `&str`/`String`-only downcast (`worker_io.rs:106-114`) falls back to `"un...
