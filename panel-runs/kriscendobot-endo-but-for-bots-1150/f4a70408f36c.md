---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1150
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: f4a70408f36c
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #1150 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `c64f084d`

seat verdicts (29): archivist=must-fix assessor=comment benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #1150 (`build/ironhorse-panic`)
- breaker: ## Juror: breaker — PR kriscendobot/endo-but-for-bots#1150
- corner-prober: ## Juror: corner-prober — PR #1150 (`build/ironhorse-panic`, head `c64f084d9`)
- curator: ## Curator — PR #1150 (`endojs/endo-but-for-bots`, base `llm` @ `e84a4c83c`)
- duality-auditor: ## Juror: duality-auditor
- engine-realist: **must-fix** — `rust/endo/src/ironhorse_engine.rs:157` `classify`'s catch-all fails *toward commit*: unknown halts ...
- engine-realist: **must-fix** — `rust/endo/xsnap/src/lib.rs:1878` drains the poison only *after* a crank body, but the crank's first...
- engine-realist: **should-fix** — `worker_io.rs:427` short-circuits by returning normally, so XS sees `undefined` where a contracted...
- engine-realist: **should-fix** — `worker_io.rs:479` clears `CAPTURING` unconditionally. `host_debug_poll` (`run_debugger`) and `hos...
- engine-realist: **should-fix** — `rust/endo/src/inproc.rs:252` drops the payload (`Err(_)`) and fabricates a fixed string; `panic_p...
- engine-realist: **comment-only** — the "one definition" invariant is stated but unadopted: `ironhorse-262/src/xst.rs:377` still han...
- fast-checker: **`ExecutionOutcome::classify`'s delegation invariant is a `forall` verified by a 7-item hand list, in a crate where ...
- fast-checker: **The `forall` is unwritable today, which is the tell that the contract is partial.** Quantifying over all 12 variant...
- fast-checker: **`guard_ffi`'s poison state machine is tested at length 2.** The contract — "once poisoned, no further body runs u...
- fast-checker: **Message and location capture are tested with one literal each.** `panic_payload_message` has three arms; only `&'st...
- fast-checker: **`XsnapError::Panicked`'s `Display` is new and untested.** Two arms on `location`. Property: for all `(message, loca...
- fast-checker: **No property-testing dependency exists in `rust/`.** Findings 3–5 are cheap as byte-seeded loops in the existing `...
- gateway: `worker_io.rs` gains four tests including `ffi_panic_is_confined_to_one_worker_thread` — the design's named isolati...
- gateway: `ironhorse_engine.rs` gains five `ExecutionOutcome`/`classify` tests. CI runs `cargo test -p endo --features ironhors...
- gateway: Only the single `ironhorse-vm` test is genuinely covered (`^rust/engine/` matches the `test-ironhorse` path filter).
