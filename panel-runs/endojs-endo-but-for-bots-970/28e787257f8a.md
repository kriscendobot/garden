---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 970
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 28e787257f8a
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #970 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `54a81fa5`

seat verdicts (28): archivist=must-fix assessor=comment benchmarker=must-fix breaker=comment changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=comment migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR endojs/endo-but-for-bots#970
- benchmarker: The PR's only recorded measurement contradicts the timing claim it repeats in four places. `rust/engine/ironhorse-262...
- benchmarker: The 10s `--case-timeout` default has no measured per-case duration distribution behind it. A legitimate slow case abo...
- benchmarker: The RSS claim justifying the entire batch architecture is unmeasured. "The XS oracle retains process RSS" and "peak m...
- benchmarker: `baseline/provenance.json`'s `config` predates `--case-timeout`, so a later child compares a bounded run against an e...
- benchmarker: My brief cites `skills/benchmark-comparative-report/SKILL.md` and `roles/scout/AGENT.md` as the seat's standing rules...
- changeset-auditor: Review complete. Here is my per-juror block.
- corner-prober: **`RecvTimeoutError::Disconnected` folds an ironhorse-VM panic into `Infrastructure`** — `xst.rs:858`. Only `ironho...
- corner-prober: **No completeness check that every discovered batch produced a file.** `run_one` swallows the xst exit (`full-run.sh:...
- corner-prober: **`aggregate_merges_batch_files_deterministically` (`report.rs:1212`) does not exercise its stated case** — the com...
- corner-prober: **`batch_filename` (`/` → `__`) is non-injective** — `batch_filename("a/b") == batch_filename("a__b")`. test262 s...
- corner-prober: **`json_str` escaping is never round-tripped on a hostile reason.** `result divergence: oracle={:?} ironhorse={:?}` (...
- curator: **must-fix — public field doc states the opposite of the code.** `rust/engine/ironhorse-262/src/xst.rs:870`: `cases...
- curator: **should-fix — one wire shape declared twice, in two languages.** `report::batch_filename` (`src/report.rs:619`) en...
- curator: **should-fix — batch wire lacks the version discriminator its sibling carries.** `to_json` stamps `"schema": "ironh...
- curator: **should-fix — module doc names an export that does not exist.** `report.rs:32` links `[`read_report`]`; the module...
- curator: **comment-only — misleading namespacing.** `RunReport::to_batch_json(cases: &[CaseRecord])` never touches `RunRepor...
- curator: **comment-only — `baseline/baseline.json` is a third case-wire shape** (`failures`/`infrastructure`/`covered`) that...
- engine-realist: `rust/engine/ironhorse-262/src/xst.rs:649` — `thread::Builder::new()` sets no `.stack_size()`, so every case now ru...
- engine-realist: `xst.rs:681` — the `Disconnected` arm maps a worker panic to `harness-assembly:worker-crash` → Infrastructure. Pr...
