---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 970
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 6aa07345ae0c
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #970 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `75e9b5e0`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `rust/engine/ironhorse-vm/src/interp.rs:16877-16885` — the `is_ordinary_object` doc edit **mangled the sentence**: ...
- archivist: Three coercion docstrings now contradict their bodies: `binary_arith` (interp.rs:17341-17348) still says a string ope...
- archivist: `rust/engine/README.md:478-484` documents the strict integrity-violation throw as an honest named skip "which needs t...
- archivist: Two new doc references resolve to nothing: `src/test262.rs:262` "design § the OOM-bounded run" and `src/xst.rs:1140`...
- archivist: `interp.rs:2529` — `/// ECMAScript `length` of the intrinsic constructor` but the body groups `DataView` with the t...
- archivist: `rust/engine/ironhorse-262/scripts/full-run.sh` adds six banner rules (lines ~55, 76, 89, 105, 124, 157: `# --- Resol...
- archivist: No `rust/engine/README.md` section records this increment, breaking the per-child acceptance-evidence convention ever...
- archivist: `ironhorse-regexp/src/compile.rs:1192` says "the **lexer** must still reproduce XS's complete accept/reject decision"...
- archivist: `to_number_value` (interp.rs:17692) is documented and named `ToNumber` but passes `BigInt` through unchanged — that...
- assessor: ## Juror: assessor — PR endojs/endo-but-for-bots#970
- benchmarker: ## benchmarker
- breaker: **must-fix — `__proto__` early error is applied through the cover grammar** (`ironhorse-compile/src/parser.rs:1278`...
- breaker: **must-fix — Error stringification regressed from an honest fold to a silently wrong value** (`interp.rs`, `call_pr...
- breaker: **should-fix — `(?i:a){2}` accepted; XS rejects it.** `quantifier_parse` is called after the modifiers group, but X...
- breaker: **should-fix — `error_to_string` treats a pruned symbol as an absent property.** `symbol_ids.get("message")` is `No...
- breaker: **should-fix — `to_primitive`'s `wrapper_data` short-circuit falsifies its own doc comment** ("including `@@toPrimi...
- breaker: **comment-only — native methods answer `.name === ""` / `.length === 0`.** Widening the own-`length`/`name` arm to ...
- corner-prober: **`to_primitive` wrapper shortcut precedes both hooks** (`interp.rs`, `if !string_hint { wrapper_data → return }`)....
- corner-prober: **New `constructor`/`prototype` links are enumerable** (`proto_methods` → `set_own_unmetered`, `flag = 0`). `var x ...
- corner-prober: **Ordinary `ToPrimitive` fallback depends on the program's symbol lexicon.** `({}) - 1`, `({}) + 1`, `[] - 1`, `1 < {...
