---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1014
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 7b55bea74ea1
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1014 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `32e4d809`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `packages/cli/src/commands/http-mk.js:49-50` — the comment's stated reason is false: "the daemon applies its own de...
- archivist: `packages/cli/test/http-mk-command.test.js:47-50` (as committed) — "Spin up a tiny HTTP server for the allowlist or...
- archivist: `designs/cli-http-client.md:29,32,37,58,70` and `.changeset/cli-http-mk-phase-1.md` — new prose packs multiple sent...
- archivist: `packages/cli/src/commands/http-mk.js:32` and `designs/cli-http-client.md:52` document `--as <agent>` with no host-on...
- archivist: The document's H1 still reads "Controller + Client Pair under `endo http`" while its own new § states that pair "no ...
- archivist: **The worktree is dirty and does not match HEAD**: `packages/cli/src/commands/http-mk.js` on disk drops `allowedOrigi...
- assessor: **Re-running `mk` on an existing pet name silently rebinds it and strands the previous client's revocation authority....
- assessor: **`--max-requests-per-minute` / `--max-response-bytes` coerce with bare `Number(val)`** (`packages/cli/src/endo.js:82...
- assessor: **comment-only:** the `--origin` collector at `packages/cli/src/endo.js:820-826` mutates Commander's default array in...
- assessor: `bin/endo.cjs:4` discards `main()`'s return value, so `main`'s carefully computed exit codes are lost: bare `endo htt...
- assessor: `packages/cli/test/http-mk-command.test.js:57-73` spins up a real HTTP server and never sends a request to it; the no...
- breaker: **`--policy-mode tofu-auto` dissolves the allowlist the verb advertises, and nothing on the CLI says so.** `packages/...
- breaker: **`mk` on an existing pet name silently rebinds and orphans a live, unrevocable client.** `provideHttpClient` (`host....
- breaker: **The delegation this PR is built on is claimed three times and tested zero times.** `http-mk.js:20-23`, the changese...
- breaker: `--origin <url>` help says "URL", but `assertHttpClientOrigin` (`host.js:174-195`) requires `new URL(o).origin === o`...
- breaker: `t.notRegex(stdout, /controller-name/)` (`http-mk-command.test.js:60-61`) passes against any output that never had th...
- breaker: Numeric coercion `val => Number(val)` (`endo.js:817,823`) is safe against the attacks I tried (`NaN`, `Infinity`, `1e...
- changeset-auditor: *Package-set*: front-matter lists `'@endo/cli'` only; the diff touches `packages/cli/{src/commands/http-mk.js,src/end...
- changeset-auditor: *Bump level*: `minor` for a purely additive CLI verb on an existing package (`@endo/cli@2.3.13`) matches the in-tree ...
- changeset-auditor: *Body-vs-diff identifiers*: `provideHttpClient(name, policy)`, `getHttpClientControl`, `normalizeHttpClientPolicy`, a...
