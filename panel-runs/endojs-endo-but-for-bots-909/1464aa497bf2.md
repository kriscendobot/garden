---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 909
panel_kind: code
base_ref: llm
rounds: 1
disposition: must-fix
must_fix_total: 17
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 1464aa497bf2
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #909 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `89110084`

seat verdicts (28): archivist=must-fix assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=pass fast-checker=comment gateway=comment integrator=pass locksmith=comment migrator=pass packager=pass prover=must-fix pruner=pass purist=comment releaser=pass saboteur=comment scribe=pass spec-keeper=comment stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=comment
must-fix items (17):
- archivist: `packages/cli/demo/README.md` documents the CLI's confined-script workflow section by section, each narrated feature ...
- archivist: None. The rest of the diff's prose checks out: `packages/cli/src/cli-archive.js`'s new JSDoc (`makeCliArchive`, `make...
- breaker: `.ts`/`.mts`/`.cts` in the confined *application* are type-stripped and archived as JS.
- breaker: "TypeScript dependencies under `node_modules` must still be built for publication" — i.e. TS-stripping is scoped to...
- breaker: Type-directed syntax (enums, namespaces, parameter properties) is unsupported and reported clearly.
- breaker: A custom JS parser supplied via `options.parserForLanguage` becomes the delegate for the matching TS parser unless a ...
- breaker: **[should-fix]** Invariant 2 is falsifiable for the common case where a "dependency" is actually a symlinked workspac...
- breaker: **[should-fix]** The "unsupported syntax" invariant is asserted for three categories (enums, namespaces, parameter pr...
- breaker: **[comment-only]** JSDoc at `cli-archive.js:80-84` claims custom-JS-parser delegation is symmetric for both ESM and C...
- corner-prober: **Contract-claimed-but-untested: `endo archive` and `endo make` with TypeScript sources.** The changeset (`.changeset...
- corner-prober: **Untested boundary: package.json with no `"type"` field.** `workspaceModuleLanguageForExtension`/`workspaceCommonjsL...
- corner-prober: **Untested null/falsy override corner in `cli-archive.js`.** `mts: options.parserForLanguage?.mts || makeTypeScriptPa...
- corner-prober: **Minor: lenient UTF-8 decode on TypeScript sources.** `makeTypeScriptParser` does `textDecoder.decode(sourceBytes)` ...
- prover: `packages/cli/test/typescript-archive.test.js:24-43` ("CLI TypeScript parser delegates to an overridden JavaScript pa...
- prover: `packages/cli/src/commands/run.js:139` dynamically `import()`s `../cli-archive.js` inline, while `archive.js` and `ma...
- prover: Neutered both the `mts` and `cts` TypeScript-parser wiring (fell back to the plain JS source parsers, i.e. no type-st...
- prover: Neutered only the `cts` explicit-override fallback (see finding above) — all 4 tests still passed, confirming the c...
