---
kind: panel-run
repo: kriscendobot/minion.town
pr: 90
panel_kind: code
base_ref: HEAD~1
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0c9ab2c3c00c
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #90 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `69a38525`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR kriscendobot/minion.town #90
- assessor: `deploy/aws/scripts/deploy-www.sh:37` — the rewrite turns one SSM command into N, and `ssm_run` calls `die` on a no...
- assessor: `deploy/aws/www/shell.html:209,216` — `#chrome { isolation: isolate }` creates a stacking context, so the descendan...
- assessor: `deploy/aws/www/shell.js:560-567` — `createChromeOverlay` is called *between* gutter creation and `addButton.addEve...
- assessor: `deploy/aws/www/shell.html:354` / `shell.js:294` — `aria-modal="true"` with no focus containment and nothing `inert...
- assessor: `deploy/aws/www/shell.html:356` — `createChromeOverlay`'s JSDoc contract is "any failure leaves that section hidden...
- assessor: `deploy/aws/www/shell.js:257` — `makePlaceholderClip(clips.length)` derives `order` from array length, but `load()`...
- assessor: `deploy/aws/www/shell.html:330,339` and `designs/clip-shell-framework.md:109` name `frameConfig`; the export is `fram...
- assessor: `shell.js:549` — the `if (config.src)` branch is unreachable: `frameConfiguration` always returns `{ sandbox: '', s...
- assessor: `shell.js:582` — comment says "Restore the last view"; the code selects `clips[0]`. No active-clip persistence exis...
- assessor: `deploy-www.sh:34-35` — `$name` is interpolated unquoted into the remote script. Repo-controlled filenames today, p...
- breaker: `deploy/aws/www/shell.html:213` (`#chrome { isolation: isolate; }`). The design calls the z-index layering "the prima...
- breaker: `deploy/aws/caddy/conf.d/minion-town.caddy:180`. `frame-ancestors 'none'` lands only on the default `handle {}`. `han...
- breaker: `deploy/aws/www/shell.html:29`. `viewport-fit=cover` is new and is the repo's only occurrence, with no `env(safe-area...
- breaker: `deploy/aws/www/shell.html:325` / `shell.js:498`. `aria-modal="true"` with no focus trap and no focus restore. The ba...
- breaker: `deploy/aws/scripts/deploy-www.sh:26-38`. Atomicity regression the comment does not acknowledge (it frames the change...
- breaker: `shell.js:78-92`. `isValidClipRecord` bounds the character shape of `id` and nothing else. `name`, `icon`, `about` ar...
- breaker: `shell.js:63`. Exported `escapeHtml`'s JSDoc promises "safe interpolation into template-literal HTML" but escapes onl...
- breaker: `test/shell-clip.test.ts:9-10` as committed declares `shellModulePath` and then imports the string literal anyway, so...
- corner-prober: ## Juror: corner-prober — PR #90 (kriscendobot/minion.town)
