---
kind: panel-run
repo: kriscendobot/minion.town
pr: 90
panel_kind: code
base_ref: HEAD~1
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 087dadc13f9a
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #90 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `3e07631d`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR kriscendobot/minion.town#90
- assessor: **must-fix** — `deploy/aws/www/shell.html:164-168` + `:327`: `#clip-frame { display: block; }` is an author-origin ...
- assessor: **must-fix** — `designs/clip-shell-framework.md` § 4 fact 2 states the chrome layer is "a `position: fixed` siblin...
- assessor: **should-fix** — `shell.js:310`: `overlay.focus()` is a no-op; `#overlay` (`shell.html:...role="dialog" aria-modal=...
- assessor: **should-fix** — the frame-bust (`shell.js:534`) lives in a deferred `type="module"` script at `shell.html:384`, so...
- assessor: **comment-only** — `shell.js:246`: `makePlaceholderClip(clips.length)` derives `order` from array length, so any re...
- assessor: **comment-only** — `shell.js:317-323`: on close, `.open` is removed immediately but `backdrop.hidden = true` waits ...
- assessor: `deploy/aws/scripts/deploy-www.sh:33`: `$name` is interpolated unquoted into the remote script, so a filename contain...
- assessor: `wireAccountSurfaces` fires all five `fetch`es at construction, before the overlay is ever opened; the already-noted ...
- assessor: `Promise.reject()` with no reason (`shell.js:365`, `:394`) yields `undefined` in the handler — harmless here since ...
- benchmarker: **`deploy/aws/scripts/deploy-www.sh:14-22` — the posted numbers are already wrong against the tree they ship with.*...
- benchmarker: **PR body, "Deploy" bullet — "so the tree can grow without a cliff" is contradicted by the PR's own measurement.** ...
- benchmarker: **`deploy-www.sh` — only the winning axis was measured.** The restructure converts **1** `SendCommand` into **N+1**...
- benchmarker: **No executable guard on the headroom.** The 56 KB cliff lives only in prose, so finding 1's drift recurs silently an...
- breaker: I read the design's claimed invariants, then attacked each against the code and the CSS geometry. Verified findings b...
- changeset-auditor: **Body overstates the iframe as live cross-origin** — *must-fix, summary-fix*. The commit body says the main pane s...
- changeset-auditor: **Undisclosed scope in the body** — *summary-fix*. The body enumerates what lands ("Design doc and dark-mode test c...
- changeset-auditor: **Incomplete typist sweep** — *summary-fix*. The `…`→`...` / `→`→`->` conversion is right in direction but ...
- changeset-auditor: **Stale headroom figure** — *nit*. `deploy-www.sh:20` says "shell.js at ~22 KB"; it is 24191 B. The comment exists ...
- corner-prober: I read the full diff (`shell.js`, `shell.html`, `shell-clip.test.ts`, `deploy-www.sh`, `DEPLOYMENT.md`) and empirical...
