---
kind: panel-run
repo: kriscendobot/minion.town
pr: 90
panel_kind: code
base_ref: HEAD~1
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: dbc85b19b851
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #90 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `77c76df9`

seat verdicts (20): archivist=must-fix assessor=must-fix benchmarker=error breaker=error changeset-auditor=error curator=must-fix duality-auditor=error engine-realist=error integrator=error locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix purist=must-fix saboteur=must-fix spec-keeper=error stylist=must-fix typist=must-fix warden=must-fix wire-watcher=error
must-fix items (20):
- archivist: **must-fix — design states a gutter width the code does not use.** `designs/clip-shell-framework.md:61` says "a per...
- archivist: **must-fix — the operator doc's enumeration of the gated static surface is now stale.** `DEPLOYMENT.md:418-424` nam...
- archivist: **should-fix — § 4's unforgeability mechanism describes a structure that isn't there.** `designs/clip-shell-framew...
- archivist: **should-fix — JSDoc overstates the failure behavior.** `shell.js:189-190`: "any failure leaves that section hidden...
- archivist: **should-fix — unresolvable reference.** `designs/clip-shell-framework.md:28` writes `[endojs/endo-but-for-bots \`d...
- archivist: **should-fix — banner horizontal rules.** `shell.html:91`, `:149`, `:178` (`/* ---- Gutter … ---- */`) and `shell...
- archivist: **should-fix — per-browser persistence is undocumented.** `shell.js:9-10`/`:28` persist the clip list in `localStor...
- archivist: **comment-only — two smaller drifts.** The design's `ClipRecord` (`:70-79`) omits `placeholder` and marks `about` o...
- assessor: `contactHtml`'s mailto/URL linkification (`index.html:271-277`) is dropped in favor of `textContent` at `shell.js:241...
- assessor: `window.location = res.body.url` and the `allow-scripts allow-same-origin` pairing on the real-clip branch are carrie...
- assessor: `store.load()` shape validation beyond the `Array.isArray` check — see finding 4; a full validator belongs with the...
- assessor: `test/dark-mode.test.ts:8` addition passes: `shell.html` contains all four required tokens (`color-scheme: light dark...
- assessor: `deploy-www.sh:18-26` globs `"$WWW_SRC"/*`, so `shell.js` deploys and is reconciled by the manifest; `/shell.js` fall...
- assessor: `randomLabel`'s `b & 31` over `Uint8Array` is bias-free (256 is a multiple of 32).
- assessor: `render()` → `selectClip` is a forward reference inside an arrow body, invoked after initialization — no TDZ hazard.
- curator: `index.html` renders `elevation_contact` through `contactHtml()` (a `mailto:`/`https:` value becomes a link, mirrorin...
- curator: presets read `${cents / unit} credits` in `index.html` vs `${Math.round(...)} cr` in `shell.js:352`.
- locksmith: ## Juror: locksmith — PR #90 (kriscendobot/minion.town)
- migrator: ## Juror: migrator
- packager: ## packager — PR #90 (`kriscendobot/minion.town`), base `HEAD~1` (`c6b864c`), head `77c76df`
