---
kind: panel-run
repo: kriscendobot/minion.town
pr: 90
panel_kind: code
base_ref: HEAD~1
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: f9eacc2e87f3
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #90 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `cc27cc14`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: Reviewed the worktree diff (`cc27cc1`, base `89904a9`) — `deploy/aws/www/shell.{html,js}`, `designs/clip-shell-fram...
- assessor: `$name` is interpolated unquoted into the remote script (`deploy-www.sh:29-30`) — pre-existing shape, not introduce...
- assessor: `shell.js` `wireAccountSurfaces` duplicates `index.html`'s account wiring; the PR already names the dedupe as a follo...
- assessor: `bootstrap`'s "Restore the last view" comment selects the *first* clip, not the last viewed — cosmetic.
- benchmarker: **`deploy/aws/scripts/deploy-www.sh:14-22` + PR body ("Deploy") — half-measured payload claim; the residual per-fil...
- benchmarker: **`deploy/aws/scripts/deploy-www.sh` — the restructure's cost side is unmeasured and undeclined.** N files now cost...
- benchmarker: **`deploy/aws/www/shell.js:~700` (`wireAccountSurfaces`) + PR body "same same-origin fetches".** Cost-parity is asser...
- breaker: **must-fix — `shell.html:73-74`: a phantom implicit grid row halves the landscape layout.** `body` sets `grid-templ...
- breaker: **must-fix — `shell.js:96` `clipOrigin` is dead code; the id shape is never validated.** Design §2 claims the orig...
- breaker: **must-fix — `shell.js:499-505`: the frame-bust `catch` is unreachable.** A sandbox-blocked top navigation is **sil...
- breaker: **should-fix — `shell.html:161`: `#clip-frame { display: block }` overrides UA `[hidden]{display:none}`, so `frame....
- breaker: **should-fix — `shell.js:296-300`: for 200ms after close, `#overlay-backdrop` is still `hidden=false` at `opacity:0...
- breaker: `shell.html:180` credits the "large z-index" with putting chrome above the iframe — but `isolation: isolate` (`:186...
- breaker: `deploy-www.sh` per-file `ssm_run` makes the deploy non-atomic; glob order lands `shell.html` **before** `shell.js`, ...
- breaker: `viewport-fit=cover` (`shell.html:17`) with no `env(safe-area-inset-*)` puts the load-bearing corner button under the...
- breaker: `clips` `order` is never renormalized after load-sort; `addClip` uses `clips.length`, so a gap/duplicate in `order` r...
- corner-prober: **must-fix — identity collision: duplicate `id`.** Two records sharing an `id` both pass validation. `render` gives...
- corner-prober: **must-fix — `#overlay` has no `tabindex`, so `overlay.focus()` is a silent no-op** (`shell.html:…` `<div id="ove...
- corner-prober: **`clipOrigin` has zero production callers** (grep: only the test). `placeholderDocument` re-derives the origin inlin...
- corner-prober: **String boundaries on `name`/`icon`:** empty string passes (only `id` checks `.length > 0`) → a blank, effectively...
