---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 938
panel_kind: design
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 596677e78ee5
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #938 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `2046c511`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: `designs/endo-reminder-familiar-integration.md:118` — "(design § Delegation, Mode B)" uses a bare "the design" to ...
- copyeditor: `:187-192`, `:210` — "IP1".."IP6" are used six times and never introduced; § Integration points is a plain numbere...
- copyeditor: `:171-180` — the caveat's parenthetical does not parse on one read: "(which would leave the name pointing at a dead...
- copyeditor: `:253` vs `:109` — the same source is quoted two different ways, and neither matches it. `endo-reminder.md:474-479`...
- copyeditor: `:131-133` — "both are pure JS over node builtins": "both" reads first as `setup.js`/`agent.js` (the nearest plural...
- copyeditor: `:247` — "the coordination note is on the bus to the Chat peer" is garden-internal process vocabulary with no refer...
- copyeditor: `:36` — "LAL" is used twice before its only gloss ("the LLM agent", :38); `:99` "reconciled writable-tree verbs" is...
- copyeditor: Redundancy and voice: "merged and approved" is restated at `:16`, `:21`, `:233`; two headings lean on "actually" (`##...
- copyeditor: `:140` "Read directly from the merged sources and tests, not assumed:" reads first as an imperative to the reader; `:...
- copyeditor: In-text cites say "Open questions" (`:119`, `:180`, `:193`) while the heading is `## Open Questions`. [rule: skills/r...
- copyeditor: No typist-hostile code points: the doc uses `->`, straight quotes, and only `§`/em dash, both permitted. [rule: skil...
- critic: **IP1's recommended adapter body cannot wake the agent.** `packages/lal/inbox-loop.js:118` skips every message whose ...
- critic: **`makeUnconfined(worker, '@endo/reminder', …)` will not load.** `packages/daemon/src/worker.js:98` runs the specif...
- critic: **"`packages/familiar`: likely no source change" is wrong, and IP6's reachability argument does not apply.** A caplet...
- critic: **IP3's "guarded the way `setup.js` already guards"** misreads the guard: `setup.js:29` is a whole-function early `re...
- critic: `make()` awaits `lookup('reminder-recipient')` before returning (`reminder/src/index.js:101`), so a recipient unavail...
- decomplector: **should-fix — the powers guest braids the plugin's private state with the agent's own namespace, voiding Design De...
- decomplector: **should-fix — `env` and the store are two places holding one value, with an undocumented precedence.** § Integrat...
- decomplector: **should-fix — retention policy sits inside the shared mechanism.** Decision 2 puts `makeUnconfined` + `@pins` in `...
- decomplector: Well decomplected and worth keeping as-is: § Persistence and lifetime's recipient-durability caveat (name-binding vs...
