---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 4
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a83118ac3736
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #4 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `a99c87b9`

seat verdicts (28): archivist=must-fix assessor=error benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=error curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=error locksmith=must-fix migrator=comment packager=must-fix prover=comment pruner=must-fix purist=comment releaser=comment saboteur=must-fix scribe=error spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **should-fix** — `packages/harness/sandbox/permissive.js:172`: The `runCompartmentLlm` `@returns` says "assistant m...
- archivist: **should-fix** — `packages/harness/README.md:59` and `designs/cap-attenuation.md:120`: Both prose passages say the ...
- archivist: **comment-only** — `designs/cap-attenuation.md:3`: Frontmatter `status: stub` is increasingly stale now that the do...
- breaker: **[should-fix] `makeFetch` vends unbounded host `fetch`, falsifying the design doc's "pinned `fetch`" invariant.** `d...
- breaker: **[should-fix] `bounded` ambient token is identical to `full`, falsifying the "bounded (no live executor)" invariant....
- breaker: **[should-fix] `validateAssistantMessage` accepts arbitrary `content[].type` values.** The `runCompartmentLlm` JSDoc ...
- breaker: **[comment-only] `copyJsonData` has no recursion-depth cap.** A program returning a 100k-deep object triggers `Maximu...
- breaker: **[mitigated] `Compartment`/`harden`/`lockdown`/`eval` intrinsics are exposed to the role program.** Probed: a sub-co...
- changeset-auditor: **Missing changeset for a new exported API.** The diff adds `runCompartmentLlm` to the public exports of `packages/ha...
- changeset-auditor: **Bump level should be `minor`, not `patch`.** `packages/harness` is an existing package (the diff modifies pre-exist...
- changeset-auditor: **Body must name the actual identifiers and the behavior change.** When the changeset is added, its body should name ...
- changeset-auditor: **Body style: sentence-per-line, no process commentary.** The added changeset body must follow sentence-per-line and ...
- corner-prober: **`assertJsonData` rejects `NaN`, `Infinity`, `-Infinity` but no test pins it.** `copyJsonData` → `assertJsonData` ...
- corner-prober: **`-0` (negative zero) silently becomes `0` across the JSON boundary — untested.** `JSON.stringify(-0)` produces `"...
- corner-prober: **Program that throws at call-time is untested and unwrapped.** `runCompartmentLlm` does `const result = await progra...
- corner-prober: **Non-Error tool throws (`throw 'string'`, `throw null`, `throw 42`) access `err.stack` which is `undefined`.** The n...
- corner-prober: **Empty `content` array passes `validateAssistantMessage` — untested.** `content: []` is valid per the validator (t...
- corner-prober: **Unknown `content` item types (e.g. `{ type: 'image' }`) pass validation silently.** `validateAssistantMessage` only...
- corner-prober: **`makeConsole` vends only 5 methods; role programs calling `console.dir`/`console.table`/`console.trace` get a `Type...
- corner-prober: **Never-settling program promise has no `runCompartmentLlm`-level timeout — untested.** A program returning `new Pr...
