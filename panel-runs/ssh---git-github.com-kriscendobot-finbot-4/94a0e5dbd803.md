---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 4
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 94a0e5dbd803
recorded_by: endolin-garden-ece02cb4
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #4 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `b58b125f`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #4 (`feat/harness-compartment-role-program`)
- assessor: **must-fix.** `describeGlobals` (`packages/harness/sandbox/permissive.js:99-115`) honors an attenuator that *drops* a...
- assessor: **should-fix.** `runCompartmentLlm` documents `timeoutMs` "Omitted ⇒ no deadline" (`permissive.js:189`), and `runIn...
- assessor: **should-fix.** The `seed` parameter is documented (`permissive.js:191`) and threaded to the worker, but is unreachab...
- assessor: `copyJsonData` (`boundary.js:100-113`) re-wraps its own `assertJsonData` messages, producing doubled labels such as `...
- assessor: The spawn-level deadline (`spawn.js:85-90`) aborts the race but does not terminate the worker; reclamation relies on ...
- assessor: Per-turn worker cost measured at roughly 42ms (fresh thread plus lockdown), so the spawn-fresh-per-turn choice costs ...
- breaker: **`packages/harness/sandbox/permissive.js:99-110` — the token round-trip silently *widens* a narrowed endowment; th...
- breaker: **`packages/harness/sandbox/permissive.js:287-289` — the host promise can never settle.** `exit` only rejects when ...
- breaker: **`packages/harness/test/spawn.test.js:387` — the regression guard tests the wrong half of its own invariant.** `na...
- breaker: **`packages/harness/spawn.js:212-220` — the documented `seed` is unreachable, so the forecaster ensemble is one rep...
- breaker: A `Far`/`Exo` ref cannot be vended as a global at all now; nested methods fall through to `copyJsonData` and surface ...
- breaker: The branch tree carries an uncommitted **revert** of `b58b125` in `permissive.js` plus an untracked `mut.sh` mutation...
- engine-realist: **`sandbox/permissive.js:96-115` + `sandbox/boundary.js:76-95` — the token round-trip *widens* an attenuator-narrow...
- engine-realist: **`spawn.js:213-228` — the RNG seed never crosses; every turn and every spawn draws the identical stream.** `makeCo...
- engine-realist: **`sandbox/boundary.js:17-30` — host-motivated lockdown tamings are applied to the untrusted worker realm.** The co...
- engine-realist: **`spawn.js:85` — an orphaned worker outlives the aborted spawn.** The deadline race abandons `runLoop` but nothing...
- engine-realist: Under ses 2.x both `Math.random()` and `Date.now()` **throw** inside the Compartment ("secure mode %SharedMath%.rando...
- engine-realist: `copyJsonData` added a BigInt replacer explicitly to avoid engine-specific `JSON.stringify` wording, but the cycle pa...
- engine-realist: Worker startup measured ~40ms/turn (SES lockdown per fresh isolate). Acceptable at `maxTurns = 8`; noting the budget ...
