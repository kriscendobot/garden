---
kind: panel-run
repo: kriscendobot/minion.town
pr: 79
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: e8fce3198eec
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #79 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `5711a12f`

seat verdicts (29): archivist=must-fix assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=pass fast-checker=comment gateway=pass integrator=comment locksmith=must-fix migrator=pass packager=pass prover=comment pruner=must-fix purist=comment releaser=pass saboteur=pass scribe=must-fix spec-keeper=comment stylist=comment surfacer=comment transplanter=pass typist=pass warden=comment wire-watcher=pass
must-fix items (20):
- archivist: **must-fix** — `src/endo/mcp-tool-names.ts:4` attributes the whole manifest to PR #77: "The reconciliation that fix...
- archivist: **must-fix** — `src/endo/mcp-tool-names.ts:16-19` says the baseline-ranked names are "the `baseline` and `baselineR...
- archivist: **must-fix** — `src/endo/mcp-tool-names.ts:24` "Inner keys are the native facet *method* names" is false for `basel...
- archivist: **should-fix** — README:100-112 tells the reader to reserve a name in `mcp-tool-names.ts` and stops there, but a re...
- archivist: **should-fix** — `src/endo/guest-tools.ts:450-454` lists the clip surface as `publish` / `listSites` / `unpublish`;...
- archivist: **comment-only** — `:122` says "ASCII case", yet `toLowerCase()` folds Unicode and the case pass runs before the gr...
- archivist: **comment-only** — `:3` uses `../../README.md` while the same comment uses repo-root-relative `designs/…`, `src/s...
- breaker: **The guard covers the manifest, not the registrations** — `src/endo/mcp-tool-names.ts:236`. README.md:107-109 now ...
- breaker: **Dispatch-side twin of the prototype hazard is untested (mitigated)** — SDK `mcp.js:102` indexes the same prototyp...
- breaker: `mcp-tool-names.ts:29` says inner keys are "native facet *method* names," but `baseline.status` has no `GuestFacet` m...
- breaker: Attacks that **failed** (correctly): `__proto__` as an inner *key* silently drops a reservation (literal proto-setter...
- duality-auditor: `src/endo/mcp-tool-names.ts:27-30` teaches a qualifier model its own data falsifies. The docstring says the suffix "i...
- duality-auditor: `src/endo/mcp-tool-names.ts:191` — `assertWellFormedToolNames` breaks the sibling shape the module declares at `src...
- duality-auditor: `MCP_TOOL_NAMES` names only the codomain of a directional map (method name → tool name; direction stated in prose a...
- duality-auditor: `RECONCILED_MCP_TOOL_NAMES` (:235) and `REGISTERED_MCP_TOOL_NAMES` (:251) are near-twins that are mutually assignable...
- duality-auditor: README.md:91's `<method><Facet>` rule carries the same inflection gap, pre-existing to this PR.
- locksmith: **The manifest pins names but does not gate the registration edge.** `src/endo/guest-tools.ts:174` … `:574` pass `M...
- locksmith: **The grammar forecloses namespacing.** `TOOL_NAME_GRAMMAR` (`src/endo/mcp-tool-names.ts:177`) admits no `_`, `.`, or...
- locksmith: `REGISTERED_MCP_TOOL_NAMES` (`:251`) is a superset of a clip-less deployment's live surface (`src/server.ts:143` moun...
- pruner: **The scope caveat is written three times.** Module header `src/endo/mcp-tool-names.ts:32-40`, `RECONCILED_MCP_TOOL_N...
