---
kind: panel-run
repo: kriscendobot/minion.town
pr: 79
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: ceb5bca6150f
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #79 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `5711a12f`

seat verdicts (29): archivist=must-fix assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=must-fix duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=comment locksmith=comment migrator=pass packager=must-fix prover=comment pruner=must-fix purist=comment releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=comment surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=comment
must-fix items (20):
- archivist: `src/endo/mcp-tool-names.ts:15-19` — the header names the baseline-ranked groups as "(the `baseline` and `baselineR...
- archivist: `src/endo/guest-tools.ts:452-454` — the `registerClipTools` header inventories the clip surface as `publish` / `lis...
- archivist: `README.md:91-95` vs `src/endo/mcp-tool-names.ts:27-30` — rule 3 states the qualification form as "`<method><Facet>...
- archivist: `src/endo/mcp-tool-names.ts:180-183` — the prose motivates the guard with "A camelCase name can still be an `Object...
- archivist: `src/endo/mcp-tool-names.ts:248-249` — "must be added both here and to that test in `test/endo-clip-tools.test.ts`"...
- archivist: `README.md:112` / `mcp-tool-names.ts:35-36` cite the deferred reminder names in their pre-reconciliation spelling (`s...
- archivist: `designs/endo-reminder-minion-town.md:60` and `designs/ertp-credits.md:169` don't mention the new manifest; they reac...
- breaker: **should-fix — the guard sits *beside* the registration path, not *on* it, so a literal bypasses it.** Invariant (e...
- breaker: **should-fix — the two *registered* groups are the only ones not pinned key→value.** `test/endo-mcp-tool-names.te...
- breaker: **comment-only — a rationale comment is false.** `test/endo-mcp-tool-names.test.ts` ("A duplicate *key* inside one ...
- breaker: **comment-only — the 64-char bound overclaims.** `TOOL_NAME_GRAMMAR`'s JSDoc calls 64 "the boundary a name reaches ...
- breaker: **Mitigated, no change.** `isObjectPrototypeKey` is correctly motivated — verified the SDK's prototype-bearing stor...
- curator: **(should-fix)** `ReconciledToolName` (`src/endo/mcp-tool-names.ts:239`) is exported with **zero consumers**, and the...
- curator: **(should-fix)** The baseline surface keeps a second independent inventory: `test/endo-guest-tools.test.ts:77`–`:90...
- curator: **(comment-only)** Rank is prose-only and one group key actively misleads: `claudeForm` (`:135`–`:141`) is **baseli...
- curator: **(comment-only)** The reminders qualifiers disagree in number — `listReminders` vs `cancelReminder` (`:130`–`:13...
- curator: **No wire-visible surface delta.** All 16 registered names are byte-identical to the literals they replace, and the r...
- curator: Docstring type claims hold: `RECONCILED_MCP_TOOL_NAMES.includes("statuz")` and `const x: ReconciledToolName = "statuz...
- curator: 6 of 9 value exports (`find*`/`assert*`) have no non-test consumer. Acceptable here — private package, no `exports`...
- curator: Bump-level overlap is moot: `package.json` is `"private": true` with no changesets directory.
