---
kind: panel-run
repo: kriscendobot/minion.town
pr: 79
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: c2605d2671e0
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #79 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `24ef869b`

seat verdicts (29): archivist=must-fix assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=pass migrator=comment packager=pass prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=comment scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: **must-fix — README and the manifest's own JSDoc contradict each other on what the manifest contains.** `README.md:...
- archivist: **should-fix — PR attribution in the header is wrong for the registered names.** `src/endo/mcp-tool-names.ts:5` say...
- archivist: **should-fix — the inner key↔value convention is undocumented.** The header explains only that *top-level* keys a...
- archivist: **should-fix — the `invitation` group is the only reserved group with no provenance.** `baselineReserved` cites `de...
- archivist: **should-fix — the hand-listed tool surfaces in `guest-tools.ts` headers are now a second, unguarded copy.** `guest...
- breaker: **should-fix — I1 is falsified by the module's own scope note.** `README.md:99` (new) says "The maximal surface is ...
- breaker: **comment-only — I3 is enforced only for today's two register functions.** The coherence test (`test/endo-clip-tool...
- breaker: **comment-only — I5's check is test-side and hardcoded to four prefixes.** `test/endo-mcp-tool-names.test.ts:76-84`...
- breaker: **comment-only — I4's test is partial.** `test/endo-mcp-tool-names.test.ts:120` asserts only `MCP_TOOL_NAMES.baseli...
- breaker: **comment-only — exact-identity collision is codified as correct** (`test/endo-mcp-tool-names.test.ts:29-35`): `lis...
- duality-auditor: `MAXIMAL_MCP_TOOL_NAMES` (`src/endo/mcp-tool-names.ts:85`) and its subset `REGISTERED_MCP_TOOL_NAMES` (`:95`) are a d...
- duality-auditor: `reminders.list: "listReminders"` (`:68`) and `reminders.cancel: "cancelReminder"` (`:69`) are same-facet counterpart...
- duality-auditor: `invitation` (`:63`) yields qualifier `Invite`, `reminders` (`:67`) yields `Reminder`, `sites` (`:51`) yields `Sites`...
- duality-auditor: `baselineReserved` (`:58`) marks not-yet-wired, yet `invitation`, `reminders`, and `claudeForm` are equally unwired a...
- duality-auditor: `resolve` (registered) and `request` (`:60`, reserved) read cleanly as a directional pair; `publish`/`unpublish` like...
- engine-realist: `src/endo/mcp-tool-names.ts:119` — the load-time guard asserts uniqueness *within the manifest*, but the namespace ...
- engine-realist: `src/endo/mcp-tool-names.ts:36`, `test/endo-guest-tools.test.ts:77` — group membership carries *precedence rank* pe...
- engine-realist: `test/endo-mcp-tool-names.test.ts:123` — the case claims freezing "at both levels so a post-load graft cannot evade...
- engine-realist: `Object.freeze` rather than `harden` is the correct call and should stay: `vitest.config.ts` declares no `setupFiles`...
- engine-realist: The literal snapshot at `test/endo-mcp-tool-names.test.ts:96` is the right instinct — a wire-visible rename must fa...
