---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 935
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: bdc9b9b617f3
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #935 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `988c2eb0`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=comment fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `designs/reminder-integration-chat.md:110-111`, `:151`, `:435` — three references to **"§ What changes"**; no such...
- archivist: `:281-286` — the cadence-band prose is wrong where it is load-bearing for the implementer. It says the band is "**1...
- archivist: `:8`, `:19`, `:208`, `:381` — the doc's central premise is that `@endo/reminder`/#721 is **merged** (correct: merge...
- archivist: `:164` "resolves **open question 3**" indexes an *unnumbered* bullet list (`:413-437`); the ordinal silently rots if ...
- archivist: Spot-checked and **accurate**: `daemon/src/interfaces.js:203-210` (4 required `send` args), `guest.js:96`/`host.js:48...
- assessor: **must-fix — the stated cadence band is not the accept band; the affordance will pass values `makeReminder` rejects...
- assessor: **must-fix — the auto-ack contract races the message deadline.** § What changes: "Only after the `send` promise re...
- assessor: **should-fix — persistent delivery failure is silently dropped, and the design has no visibility path.** `onMessage...
- assessor: **should-fix — `firstDelayMs` defaults to `0`, so `/remind 1h …` fires immediately.** `nextTickAt = createdAt + f...
- assessor: `MAX_COALESCED_MESSAGES = 10_000` caps `missedMessages`; the § Test strategy revival assertion ("the right `missedMe...
- assessor: Line cites drift a few lines (`scheduler.js:47` covers 43-52); the `store.js`/`interfaces.js` cites are exact.
- benchmarker: ## Juror: benchmarker — PR #935
- breaker: **The cadence band the affordance is told to enforce is not the band the plugin enforces (must-fix).** § What change...
- breaker: **`(reminderId, messageNumber)` is not unique per delivery (must-fix).** § interactive-response gap justifies the ke...
- breaker: **"The UI holds only `ReminderScheduler`" is falsified by the pinning recipe (must-fix, capability overlap).** `Remin...
- breaker: **"`reminder-store` reachable only through the powers namehub" is falsified by the recommended mint (must-fix).** `pr...
- breaker: **The send-then-ack invariant loses to `messageTimeoutMs` (should-fix).** § What changes forbids auto-acking a faile...
- breaker: Coalesced recovery caps at `MAX_COALESCED_MESSAGES = 10_000` (`scheduler.js:570`), so an `annotation: 'timestamps'` c...
- breaker: `@host`, the four-arg `send` guard, the `Directory` cursor contract, and the `Math.random` id are all correctly quote...
- corner-prober: **Identity collision — the prescribed snooze key does not disambiguate a retry.** `designs/reminder-integration-cha...
