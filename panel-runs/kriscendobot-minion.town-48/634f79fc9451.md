---
kind: panel-run
repo: kriscendobot/minion.town
pr: 48
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 634f79fc9451
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #48 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `c0109c84`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — `§ 1.3` and `§ 1.5` resolve to nothing in the cited design.** `designs/git-content-substrate.md` has...
- archivist: **must-fix — `content-server.ts:61` asserts an invariant the code contradicts 25 lines later.** "A git-content-subs...
- archivist: **should-fix — the projector's docs claim coverage of `styles`/`fonts` that the transform does not have.** `project...
- archivist: **should-fix — banner horizontal rules.** `projector.ts:183`, `:460`; `content-server.ts:262`, `:282` (`// ── T...
- archivist: **should-fix — `content-source.ts:29-30` names a field this module does not export and overstates the ETag.** It sa...
- archivist: **should-fix — review-seat attributions leaked into shipped prose.** `content-store.ts:90` "(curator: one canonical...
- archivist: **should-fix — the design's `## Status` still reads as unbuilt.** `designs/git-content-substrate.md:1` is titled "(...
- archivist: **comment-only — pre-existing dangling doc reference, re-asserted here.** `content-server.ts:4` cites `designs/webl...
- assessor: `src/endo/gateway/content-server.ts:201` — **must-fix.** `Content-Length` is set from `blob.length` *before* the se...
- assessor: `src/endo/gateway/content-server.ts:60-65,208` — **should-fix.** The JSDoc asserts a document "always carries the s...
- assessor: `src/endo/gateway/projector.ts:15` — **should-fix.** "every asset it pulls comes from the one root that produced th...
- assessor: `content-server.ts:277` — the immutable tier's strong `ETag` is `entry.blobId`, but the served representation is a ...
- assessor: `vhost-table.ts:395` — `advanceVhostContentRoot` stamps `mutable: true` unconditionally, so it would flip a content...
- assessor: `publish.ts` now rejects `.content/` via the shared `RESERVED_FIRST_SEGMENTS` — a behavior change for existing publ...
- breaker: **must-fix — the serve-time sentinel swap corrupts a non-UTF-8 HTML blob and desyncs `Content-Length`.** `src/endo/...
- breaker: **must-fix — `advanceVhostContentRoot` can seize a content-derived `weblet_publish` record.** `vhost-table.ts:~380`...
- breaker: **should-fix — the swap is a blind whole-document `split/join`, so it rewrites author text.** The projector careful...
- breaker: **should-fix — "bound the INPUT first (it bounds the transient rewrite allocation)" (`projector.ts:~470`) is false....
- breaker: `input.limits` is caller-overridable, so `maxFileBytes > MAX_HTML_REWRITE_BYTES` (16 MiB) makes a large document stre...
- breaker: An advance retains 16 roots, so retracting mis-published content takes 17 advances, not one.
