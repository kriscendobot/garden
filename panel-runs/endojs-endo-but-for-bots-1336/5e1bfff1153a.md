---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1336
panel_kind: code
base_ref: 6726b0fba2d3208ec10b05c2fa9fde2aff7e11cf
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 5e1bfff1153a
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1336 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `814e3baa`

seat verdicts (33): archivist=comment assessor=comment benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=must-fix duality-auditor=pass engine-realist=comment fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=pass orthographer=must-fix packager=pass procurer=comment prover=must-fix pruner=comment purist=must-fix reexport-auditor=pass releaser=pass saboteur=comment scribe=comment spec-keeper=must-fix stylist=pass surfacer=pass thesaurus=pass transplanter=pass typist=comment warden=comment wire-watcher=must-fix
must-fix items (20):
- benchmarker: The `engine-realist 2` fix on this PR (commit `4004a13426`, "linear framing with a frame cap") is an explicit optimiz...
- benchmarker: `packages/agent-tools/src/adapters/mcp.js` commit `be59e2073f` ("callTool computes the declared argument names once")...
- benchmarker: The PR body's "Not in this PR (follow-ups)" section ("Streaming search: `glob`, `grep`, and `glorp` are eager and cap...
- benchmarker: The follower-cap and `makePath`-serialization changes (`ded37e77dd`) are resource-bound/concurrency correctness fixes...
- breaker: **should-fix: argument scope is only enforced at the top level. Keys the schema forbids inside nested objects still r...
- breaker: **The claim:** `packages/agent-tools/src/adapters/mcp.js` (`declaredArgumentNames`) says: "An argument outside them i...
- breaker: **Why it breaks:** the adapter checks only the top-level keys. The matching pattern, `M.splitRecord({ label: M.string...
- breaker: **Attack:** a `tools/call` to `define` with `{ source: 'x', slots: { a: { label: 'l', smuggled: { deep: 1 } } } }`. I...
- breaker: **Fix:** use `M.splitRecord({ label: M.string() }, {}, {})` or `M.recordOf`-exact, or check keys against the nested s...
- breaker: [proposed-rule: every nested `additionalProperties: false` in a tool `inputSchema` is mirrored by a closed-rest `spli...
- breaker: **comment-only: the guest check trusts what the resolved object says about itself.**
- breaker: `resolveGuest` (`server.js`) claims to refuse "anything that ... carries host authority". It decides this from `__get...
- breaker: **Attack:** set `ENDO_GUEST_FORMULA_ID` to the id of an exo made in a worker with `M.interface(guestInterfaceName, �...
- breaker: **Mitigated in practice:** only the harness sets that environment variable. Either say in the JSDoc that the check gu...
- breaker: **comment-only: a huge decimal `messageNumber` may stall the whole server.**
- breaker: `stdio.js` claims "a slow `tools/call` does not block `ping`". `toMessageNumber` deliberately allows decimal strings ...
- breaker: I did not measure how long this takes. Consider a digit cap well under the frame cap.
- breaker: **mitigated (no action):** I tried these attacks and the code already handles each of them:
- breaker: **Frame cap:** an oversized piece at the end of a chunk and an oversized piece at the end of a frame are both refused...
- breaker: **Followers:** a queued read that expires takes nothing, and a timed-out pull keeps its pending next item for the nex...
