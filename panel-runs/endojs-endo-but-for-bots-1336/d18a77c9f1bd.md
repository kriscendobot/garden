---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1336
panel_kind: code
base_ref: 6726b0fba2d3208ec10b05c2fa9fde2aff7e11cf
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: d18a77c9f1bd
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1336 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `73d4b644`

seat verdicts (33): archivist=pass assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=comment migrator=pass orthographer=pass packager=comment procurer=comment prover=comment pruner=comment purist=must-fix reexport-auditor=pass releaser=pass saboteur=comment scribe=pass spec-keeper=comment stylist=pass surfacer=comment thesaurus=pass transplanter=pass typist=comment warden=must-fix wire-watcher=comment
must-fix items (20):
- breaker: **should-fix: the "returns within `waitMilliseconds` in all" bound fails when reads are queued.** `packages/agent-mcp...
- breaker: **should-fix: the design's argument-scope rule contradicts the implementation and the design's own table.** `designs/...
- breaker: **comment-only: `maxItems` and `maxResults` accept non-integers.** The JSON Schemas say `type: 'integer'`, but the sh...
- breaker: **comment-only (mitigated): a queued read after `closeFollower` still pulls from the closed iterator.** A read that g...
- breaker: **mitigated: guest detection holds.** `hostOnlyMethods` (`provideGuest`, `provideHost`, `provideWorker`) matches `Hos...
- changeset-auditor: **Missing changeset entry for the new `@endo/agent-mcp-stdio` package.** The diff introduces a brand-new package (`pa...
- changeset-auditor: **Changeset body is not sentence-per-line.** `.changeset/agent-tools-mcp-adapter.md`'s two body paragraphs each pack ...
- engine-realist: **should-fix — every `tools/call` adds to one long-lived `Promise.race`, which holds its result for the whole sessi...
- engine-realist: Where: `packages/agent-tools/src/adapters/mcp.js`, in `callTool`, the `Promise.race([bridgeDownPromise, …])` call.
- engine-realist: `bridgeDownPromise` is one promise shared for the server's whole life. It is never-settling when `connectionClosed` i...
- engine-realist: In V8 that reaction holds the race's resolve functions, and those hold the settled result. So every tool result stays...
- engine-realist: Fix: keep a `Set` of per-call promise kits, have `connectionClosed` reject whatever is in the set, and delete each ki...
- engine-realist: [proposed-rule: never `Promise.race` against a promise that outlives the call; register a per-call canceller that is ...
- engine-realist: **should-fix — a closed stdout can crash the server on Node.**
- engine-realist: Where: `packages/agent-mcp-stdio/bin/endo-mcp-stdio.js`, which puts no `'error'` listener on `process.stdout`; and `s...
- engine-realist: When `claude` exits first, `onEof` settles pending calls as bridge-down, and their replies are written to a pipe nobo...
- engine-realist: Separately, the second argument of `.then(onOk, onError)` does not catch a throw from `writeLine`. In that case `done...
- engine-realist: Fix: add `stdout.on('error', …)` in the bin, and use `.then(onOk).catch(onError)` in `dispatch`.
- engine-realist: **comment-only — `process.exit` straight after the last write.**
- engine-realist: Where: `bin/endo-mcp-stdio.js:204-213`.
