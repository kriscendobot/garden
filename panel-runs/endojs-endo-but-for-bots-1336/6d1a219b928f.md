---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1336
panel_kind: code
base_ref: 6726b0fba2d3208ec10b05c2fa9fde2aff7e11cf
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 6d1a219b928f
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1336 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `64ca00f9`

seat verdicts (33): archivist=pass assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=pass migrator=pass orthographer=must-fix packager=pass procurer=comment prover=must-fix pruner=must-fix purist=must-fix reexport-auditor=pass releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=pass surfacer=comment thesaurus=pass transplanter=pass typist=must-fix warden=comment wire-watcher=must-fix
must-fix items (20):
- breaker: **must-fix: a malformed `logging/setLevel` frame gets no reply at all.** [rule: designs/endo-guest-stdio-mcp.md § Te...
- breaker: `packages/agent-tools/src/adapters/mcp.js` builds its error with `` `Unknown log level: ${level}` `` without checking...
- breaker: The input `{"jsonrpc":"2.0","id":7,"method":"logging/setLevel","params":{"level":{"toString":1}}}` makes that templat...
- breaker: `handleLine` then rejects, and `serveStdio` (`packages/agent-mcp-stdio/src/stdio.js`, the `() => {}` rejection arm) s...
- breaker: Fix: check `typeof level === 'string'` before interpolating. Also, make `serveStdio`/`handleLine` turn any handler th...
- breaker: **should-fix: two parallel `readFollower` calls on one follower duplicate or lose items.** [proposed-rule: every stre...
- breaker: In `agent-interface.js` `readFollower`, both calls race the same `record.pending`, so both receive item *x*. Each the...
- breaker: B's reset orphans A's newly started pull. If A then times out on that orphaned pull, its item is lost for good, becau...
- breaker: This is reachable because `serveStdio` dispatches frames in parallel and Claude Code issues parallel tool calls.
- breaker: Fix: keep one pull in flight per follower (a queue or mutex), or reject a read while another is active. Add a test wi...
- breaker: **should-fix: a slow but steady stream can hold a `readFollower` call open for about 2 hours.** [rule: packages/agent...
- breaker: `waitMilliseconds` bounds the wait for *each* item, not the whole pull.
- breaker: The attack: a stream that yields one item every 29 s, read with `maxItems: 256` and `waitMilliseconds: 30000`, holds ...
- breaker: Fix: set one deadline for the whole pull, or reword the comment and the tool description so they don't promise a boun...
- breaker: **comment-only (mitigated): the check that the formula id names a guest.** `resolveGuest` rejects any facet that has ...
- breaker: **comment-only (mitigated): extra or oddly named argument keys.** A JSON `"__proto__"` key survives the parse → str...
- corner-prober: **`serveStdio` (packages/agent-mcp-stdio/src/stdio.js) has zero test coverage of its framing boundaries, including th...
- corner-prober: **`toMessageNumber` (packages/agent-mcp-stdio/src/agent-interface.js:71-79) boundary values are untested.** The funct...
- corner-prober: **`LIMIT_FOLLOWERS` (agent-interface.js:61, = 64) has no test at the boundary.** `openFollower` throws once `follower...
- corner-prober: **`renderToolResult` (packages/agent-tools/src/adapters/mcp.js:263-292) is untested for `promise`, `error`, and `tagg...
