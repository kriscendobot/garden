---
kind: panel-run
repo: kriscendobot/minion.town
pr: 87
panel_kind: code
base_ref: origin/main
rounds: 3
disposition: error
must_fix_total: 40
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: db4548412320
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #87 (code)

Terminal disposition: **error** after **3** round(s).

## Round 1 — head `48486ba0`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=comment fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=pass prover=must-fix pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=pass stylist=comment surfacer=comment transplanter=pass typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix** — `src/endo/claude/wiring.ts:136` claims the default `sendConnectForm` seam is "a no-op that logs," bu...
- archivist: **must-fix** — `designs/claude-agents-capability.md:330` still reads "...is a deliberate follow-up before step 6's ...
- assessor: **[must-fix]** The documented `neverReject` outer boundary does not exist. Three files (`types.ts:26`, `agents.ts:23`...
- assessor: **[should-fix]** `ClaudeAgents.list()` (`src/endo/claude/agents.ts:167-173`) documents itself as reconciling the quot...
- assessor: **[should-fix]** `credentials.store()` (`credentials.ts:42-48`) flips `status()` to `"connected"` (line 40) the insta...
- breaker: `src/endo/claude/agents.ts:23` and `src/endo/claude/account.ts:18` both claim "the wiring's outer `neverReject` bound...
- breaker: `wiring.ts:265-289` (`handleSetupToken`) calls `sub.credentials.store(token, {models: planModels})` — which flips `...
- breaker: `links.ts:113-120` (`redeem`) marks the nonce `consumed = true` on any structurally-valid, unexpired nonce *before* `...
- breaker: `agents.ts:130-141` validates `options.model` against the per-plan allow-list but the resulting `model` is never thre...
- corner-prober: ## Findings
- integrator: `designs/claude-agents-capability.md:330-331` and `designs/claude-agents-capability.md:657-663` (**Open questions**) ...
- integrator: The related-design reconciliation is otherwise handled exactly right: PR #64 (the design amendment) is merged and mai...
- integrator: Commit split (feature commit + a clearly-labeled `chore(claude): address pre-push gates` follow-up for identifier spe...
- locksmith: `src/endo/claude/agents.ts:138-154` — `create()` computes and validates `model` (`options?.model ?? allowed[0]`, ch...
- locksmith: `src/endo/claude/wiring.ts:265-282` (`handleSetupToken`) has no `rootSubjects.has(authenticatedSubject)` check, unlik...
- locksmith: `agents.ts:9-15` docstring describes recursive `@claude-agents` re-introduction into spawned children via `agentsForG...
- prover: `test/claude-account-endpoint.test.ts` "rejects a mismatched authenticated identity" — verified `autoProvision:true...
- prover: `test/claude-wiring.test.ts` "maintainer amendment" root-gating test pins `wiring.ts:253,262` (`if (!rootSubjects.has...
- prover: `test/claude-submit.test.ts` fail-closed assertion pins the `message.type !== "form"` guard in `src/endo/guest-memory...
- prover: `test/claude-links.test.ts`, `test/claude-credentials.test.ts`, `test/claude-quota.test.ts` are direct unit tests of ...

## Round 2 — head `48486ba0`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=pass engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=comment migrator=comment packager=pass prover=comment pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=comment transplanter=pass typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **Must-fix — three module-header comments describe a `neverReject` boundary in `wiring.ts` that does not exist.** `...
- archivist: **Should-fix — new `ENDO_CLAUDE_*` environment variables are undocumented in README.md/.env.example.** `src/endo/cl...
- assessor: `agents.ts:143-165` (`create`): if `childProvider.provideChild(name)` or `context.provider.mintInferExo(childFormulaI...
- assessor: `agents.ts:76-84` (the `infer` closure inside `makeClaudeAgent`): `exo.infer(prompt)` is wrapped in `try { … } fina...
- assessor: `agents.ts:175-182` (`dismiss`): `await childProvider.removeChild(name)` is unguarded and can reject `ClaudeAgents.di...
- assessor: `ClaudeDeployment.agentsFor`/`accountFor` are never actually invoked from any guest-facing surface in this diff (no M...
- breaker: `src/endo/claude/agents.ts:167-173` `list()`'s own comment claims it "reconcile[s] the ledger's global set for THIS c...
- breaker: `src/endo/claude/agents.ts:23-25`, `account.ts:17-20`, `types.ts:22-26` all assert a "NEVER-REJECT" contract: "the wi...
- breaker: `agents.ts:145-165`: between the synchronous `reserveChild` and the awaited `provideChild`/`mintInferExo`, a concurre...
- corner-prober: **Identity-collision in the quota key derivation for nested agent names.** `childKeyOf(callerFormulaId, name) = `${ca...
- corner-prober: **`submit()`'s fail-closed claim is only half-tested.** `test/claude-submit.test.ts` proves `submit` rejects an ordin...
- corner-prober: **Exact-instant boundary on both expiry checks is untested.** `links.ts` (`entry.expiresAt <= now`) and `quota.ts` (`...
- corner-prober: **"Connected but zero usable models" contract has no test.** `agents.ts`'s `create()` explicitly comments the case "C...
- corner-prober: **Concurrent identical `create(name)` calls for a brand-new name is unexercised.** `create` awaits `credentials.statu...
- corner-prober: **`maxRetainedChildren: 0` and non-integer/negative `ENDO_CLAUDE_MAX_CHILDREN` are unpinned.** `makeQuotaLedger` expl...
- engine-realist: ## Per-juror block: engine-realist
- fast-checker: ## fast-checker — PR #87 (kriscendobot/minion.town)
- integrator: I have enough to write the review.
- pruner: `src/endo/claude/wiring.ts:7-24` restates the maintainer amendment (which guest gets the factory by default, the deli...
- pruner: The "never-reject" contract is written out as a multi-sentence paragraph three separate times — `src/endo/claude/ty...

## Round 3 — head `48486ba0`

seat verdicts (9): archivist=error assessor=error curator=error locksmith=error migrator=error packager=error prover=error stylist=error typist=error
must-fix items (0):
