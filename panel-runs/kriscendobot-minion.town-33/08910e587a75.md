---
kind: panel-run
repo: kriscendobot/minion.town
pr: 33
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: max-rounds-exceeded
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 08910e587a75
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #33 (code)

Terminal disposition: **max-rounds-exceeded** after **1** round(s).

## Round 1 — head `d306b293`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=pass gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=comment wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — the documented publish flow reverses the code's order.** `src/endo/gateway/publish.ts:19` says "(5) de...
- archivist: **must-fix — `VhostRecord.powers` no longer holds only a formula id.** `src/endo/gateway/vhost-table.ts:50` still r...
- archivist: **should-fix — two adjacent docs contradict on the bearer line, and neither documents its throw.** `powers-source.t...
- archivist: **should-fix — `makeFakePowersSource`'s JSDoc (`powers-source.ts:120-123`) is now wrong twice.** It is no longer "p...
- archivist: **should-fix — API contract stranded in a body comment.** `guest-control.ts:216-218` explains why `@`-prefixed name...
- archivist: **should-fix — DEPLOYMENT.md:218 calls the pin "the minimum supported revision"**, implying a floor; `test/endo-pow...
- archivist: **should-fix — `deploy-endo-daemon.sh:7-11` ("single source of truth … MUST equal `PINNED_ENDO_COMMIT` … Bump *...
- archivist: **should-fix — DEPLOYMENT.md:239** still describes vhost records as `{ contentRoot, powers, owner }`; `powersScope`...
- archivist: **comment-only** — the § heading widened to "Increments 4 and 5" (`DEPLOYMENT.md:286`) but the edge-verification b...
- assessor: `src/endo/gateway/powers-source.ts:186-245` — the daemon source dropped its self-healing retry, and the replacement...
- assessor: `src/endo/guest-control.ts:225` — `await guest.identify(petName)` is a **new** daemon round-trip inside the `weblet...
- assessor: `src/endo/gateway/content-store.ts:210-230` — the atomicity fix removed the `fs.access` short-circuit, so every int...
- assessor: `src/endo/gateway/weblet-store.ts:97-99` — a corrupt existing record throws a bare `Error`, not `PublishError`, so ...
- assessor: `src/endo/gateway/publish.ts:352-354` — `pin` now runs *after* `register`, inverting § 7.1 step 4. Harmless today ...
- assessor: `guest-control.ts:218-222`: the `@`-prefix guard is unreachable — `assertValidPetName` (`:172`) already rejects `@`...
- assessor: `content-store.ts` / `vhost-table.ts`: a crash between `writeFile` and the `finally` `rm` leaks `.tmp` siblings forev...
- benchmarker: `src/endo/gateway/content-store.ts:216-227` — `internBlob` previously short-circuited on `fs.access(file)` and wrot...
- benchmarker: `test/endo-powers-contract.test.ts:4` + `.github/workflows/test.yml:25-35` — "cheaper source check" is a comparativ...
- benchmarker: `DEPLOYMENT.md:304` — the standing "measure under on-demand" TLS precondition stays open while this PR's § Increme...
- benchmarker: The `noAuthorityBootstrapP` module-level memo (`powers-source.ts:63-80`) is an unclaimed optimization — no closure ...
