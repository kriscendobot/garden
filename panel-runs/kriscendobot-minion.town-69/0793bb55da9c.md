---
kind: panel-run
repo: kriscendobot/minion.town
pr: 69
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0793bb55da9c
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #69 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `39dbc8bb`

seat verdicts (29): archivist=pass assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=pass engine-realist=pass fast-checker=comment gateway=pass integrator=must-fix locksmith=comment migrator=pass packager=must-fix prover=must-fix pruner=pass purist=must-fix releaser=pass saboteur=comment scribe=pass spec-keeper=pass stylist=comment surfacer=comment transplanter=pass typist=pass warden=pass wire-watcher=comment
must-fix items (20):
- breaker: **`register(directoryId)` accepts any well-formed 64-hex formula id, not one the caller introduced/holds — contradi...
- breaker: The vulnerable files (`site-registry-exo.ts`, `site-register-caplet.ts`) predate this diff (landed in #53/#55) and ar...
- breaker: `@`-built-in delegation (`assertPowerReferenceName`, `publish.ts:87`) and the powers-plane fail-closed resolve-before...
- corner-prober: **`back === null` isn't fail-closed, and no test pins it.** `powers-plane.ts` (`onUpgrade`) and `gateway.ts:157` both...
- corner-prober: **Special-name max-length boundary is asserted only on its violation, never at the edge itself.** `assertPowerReferen...
- corner-prober: **`"powers" in raw` / `"powers" in doc` reject on key presence, not value — untested for a present-but-`undefined` ...
- corner-prober: **Un-enumerated `@` names pass syntax but are never probed for behavior.** The regex accepts any `@[a-z][a-z0-9-]{0,1...
- integrator: `designs/weblet-ocap-synthesis.md:327-330` — § 5's `weblet_publish` bullet still describes the tool's `powers` par...
- integrator: Merge-commit readability, related-design reconciliation, and rename-completeness otherwise check out cleanly: the PR ...
- integrator: The commit history reads as an incremental fixer-loop stack (base change, docs, three "address/close … review findi...
- packager: **must-fix — diff-hygiene: unrelated typographic sweep bundled into a substance commit.** Commit `927033b` ("fix(ga...
- packager: **comment-only — same typographic normalization recurs legitimately elsewhere.** The bulk of the `…`→`...` / `�...
- packager: **comment-only — no changeset present.** This repo has no `.changeset/` directory or published-package release proc...
- packager: **Public-API/exports and typedoc/tsconfig axes:** not applicable — the diff touches no `package.json`, `exports` ma...
- prover: **[must-fix]** `src/endo/gateway/gateway.ts:204-216` (the new `try { front = await siteDirectories.read(...) } catch ...
- prover: **[comment-only]** The `@host`/`@agent` method-surface check and the "unheld name fails in the guest's own authority"...
- prover: `captp-vhost-table.test.ts` "fails closed on a legacy record carrying a powers field" pins `captp-vhost-table.ts`'s n...
- prover: `publish.test.ts`'s `mixedId`/`malformedId` cases and `listVhostRecords` assertion pin the equivalent `"powers" in do...
- prover: `publish.test.ts` "accepts a held @ name and rejects an unheld name" and "rejects malformed power reference names" pi...
- purist: `src/endo/gateway/gateway.ts:163-166` (`handleWeblet`) now logs and fails closed on a `siteDirectories.read` fault ("...
