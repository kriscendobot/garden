---
kind: panel-run
repo: kriscendobot/minion.town
pr: 69
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 4855392a6079
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #69 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `4a108a8d`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=comment engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `designs/weblet-ocap-synthesis.md:454` — "Units 4-5 landed **before the weblet-to-clip rename**" cites a rename not...
- archivist: `deploy/aws/systemd/endo-gateway.service:42-47` — the rewrite deletes the still-true fact "the gateway holds the da...
- archivist: `test/gateway/powers-plane.test.ts:5` — docstring says the bootstrap is "an in-process `Far` behind a fake `PowersS...
- archivist: `test/endo-daemon-integration.test.ts:445` — "`@self` is the guest's held mail handle" is wrong and security-releva...
- archivist: `designs/weblet-ocap-synthesis.md:471-472` (and criterion :381) — unit 4 is marked "**Landed.**" for deleting `asse...
- archivist: `designs/weblet-ocap-synthesis.md:10-12` — the status header routes readers by section (§§ 2.2/3.1 landed shape; ...
- archivist: `src/endo/gateway/powers-plane.ts:149` — "Fail-closed: unknown hash, or a live weblet that granted no powers" sits ...
- archivist: `src/endo/gateway/daemon-site-registry.ts:56` — "§ Known residuals (deferred to § 9 units 3-4)"; unit 4 is now La...
- archivist: Pre-existing, not this PR's: `designs/weblet-gateway.md` does not exist, yet is cited by `src/endo/gateway/powers-pla...
- assessor: `test/endo-daemon-integration.test.ts:486` — `expect(methodNames).not.toEqual(expect.arrayContaining(hostOnlyMethod...
- assessor: `src/endo/gateway/publish.ts:188-193` — dropping the `@` check removes the last pre-charge validation, so `publish`...
- assessor: `src/endo/gateway/publish.ts:87-98` — `assertPowerPetName` no longer mirrors the daemon grammar it now delegates to...
- assessor: Commit `4a108a8` claims "special references stay guest-scoped" but checks only `@host`/`@agent`. The guest's special ...
- assessor: `src/endo/gateway/powers-plane.ts:48` — missing blank line before `export interface PowersPlaneDeps` after the dele...
- assessor: `sites` is itself a guest-held pet name, so `powers: "sites"` publishes the register-only owner-pinned facet at the p...
- assessor: The whole units-4/5 acceptance is `ENDO_CHECKOUT`-gated; plain CI is green with zero coverage of the removed guard. �...
- breaker: **The evidence retiring the `@`-guard and host-shape probe runs in no CI.** The invariant this PR leans on (`designs/...
- breaker: **A legacy record is now unservable *and* un-deletable.** `vhost-table.ts:119` makes any record with `powers: <string...
- breaker: `powers: "@agent"` binds the publisher's whole guest agent (including `evaluate`) as `back`, which `powers-plane.ts:1...
- breaker: `vhost-table.ts:23,35` still say "Increment 3 fills the optional `powers` slot" and call `powers` a "forward seam lat...
