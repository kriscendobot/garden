---
kind: panel-run
repo: kriscendobot/minion.town
pr: 69
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 17
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: aec1af1a83b3
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #69 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `56513902`

seat verdicts (29): archivist=comment assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=comment locksmith=must-fix migrator=comment packager=comment prover=pass pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=pass surfacer=pass transplanter=pass typist=pass warden=comment wire-watcher=comment
must-fix items (17):
- breaker: `src/endo/gateway/publish.ts:205-216` claims (comment at 209-211) "every authority has resolved the caller's referenc...
- breaker: The `## Invariants`-adjacent fail-closed checks I attacked and found solid: the new `back === undefined || back === n...
- engine-realist: ## Per-juror block: engine-realist — PR #69 (kriscendobot/minion.town)
- fast-checker: `assertPowerReferenceName` (`src/endo/gateway/publish.ts:92`) has a fully universally-quantified contract — "for al...
- fast-checker: The fail-closed rule "a record carrying a `powers` key, of any type, is always unservable" is asserted identically in...
- fast-checker: fast-check is not yet a devDependency of this package (no `fast-check` in `package.json`, no `fc.` usage anywhere in ...
- fast-checker: `formulaBearerLocator` (`well-known.ts`, relocated from `powers-source.ts` unchanged) is a trivial pure transform (``...
- locksmith: `src/endo/gateway/publish.ts:24-30` (`assertPowerReferenceName`) — this PR widens what a guest can publish as a pub...
- locksmith: `daemon-site-registry.ts:245-250` (`assertReferenceHeld`) only checks the reference is *held* (`guest.lookup`), not t...
- purist: `src/endo/gateway/publish.ts:221-241` — `upgrade()` bills/interns caller content (`changes.front = await intern(own...
- purist: The move from a syntactic `@`-name blocklist to a possession-based check (`assertPowerReferenceName`'s `validBuiltInR...
- saboteur: **Must-fix — TOCTOU race defeats the new "resolve powers before billing" invariant.** `publish()` calls `authority....
- saboteur: **Should-fix — `upgrade()` didn't get the same fix.** `upgrade()` still runs `intern()` (which calls `processor.cha...
- saboteur: **Mitigated (noted, not a bug).** `terminateCapTP` (`powers-plane.ts:81-113`) now attaches `ws.on("message", ...)` sy...
- spec-keeper: `test/endo-daemon-integration.test.ts:~500-540` — `expect(methodNames).toEqual(expectedMethods[special])` pins the ...
- spec-keeper: `src/endo/gateway/powers-plane.ts` `terminateCapTP`: `ws.on("message", (data) => { ... data.toString() ... })` — fi...
- spec-keeper: Reviewed the `powers` → `powerReferenceName`/`@`-builtin-reference rename (`publish.ts`, `daemon-site-registry.ts`,...
