---
kind: panel-run
repo: kriscendobot/minion.town
pr: 69
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: max-rounds-exceeded
must_fix_total: 18
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2fb245e69148
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #69 (code)

Terminal disposition: **max-rounds-exceeded** after **1** round(s).

## Round 1 — head `86e342df`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=comment changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=pass packager=pass prover=comment pruner=comment purist=comment releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=pass surfacer=comment transplanter=pass typist=pass warden=comment wire-watcher=must-fix
must-fix items (18):
- archivist: ## Findings
- assessor: `evaluateRegister` failures on the memory/degraded test authorities (`site-registry.ts`) leave the created directory ...
- corner-prober: ## Per-juror block — corner-prober — PR #69 (kriscendobot/minion.town)
- engine-realist: `src/endo/gateway/daemon-site-registry.ts:274-296` (`createDirectory`): the daemon `guest.evaluate` call already **mi...
- engine-realist: `publish.ts:232-234`: `catch (error) { await authority.discardDirectory(directory); throw error; }` — if `discardDi...
- integrator: `designs/clip-ocap-synthesis.md:19-22` (new text, added by this PR's own commit `3408d4e`) reads: "The newer immutabl...
- integrator: PR description, "Related design boundary": "PR #63 documents the already-landed register-by-id boundary; it does not ...
- integrator: Everything else integrates cleanly: `powerPetName`/`powersPetName` sweeps completely to `powerReferenceName`/`back` w...
- locksmith: Now composing the review.
- saboteur: **Must-fix — cleanup-on-error masks the original failure reason.** `guestRegisterSource` (`src/endo/gateway/daemon-...
- saboteur: **Should-fix — same defect class in `publish()`.** `src/endo/gateway/publish.ts:230-235`: on an `intern()` failure ...
- saboteur: **Mitigated — good example nearby, for contrast.** `guestCreateDirectorySource`'s own try/catch (`daemon-site-regis...
- saboteur: **Out of scope — verified fine.** `assertPowerReferenceName`'s pet-name/built-in regexes reject embedded `@`, `/`, ...
- spec-keeper: `src/endo/gateway/daemon-site-registry.ts`, `guestRegisterSource` (evaluated on the live daemon as `try { return awai...
- spec-keeper: Same file, `guestCreateDirectorySource`: the mirror-image issue on the create path. `catch (error) { await E(self).re...
- spec-keeper: The `formulaId` acceptance regex in `createDirectory` (`^[0-9a-f]{64}(?::[0-9a-f]{64})?$`) correctly widens for node-...
- wire-watcher: **[must-fix]** `src/endo/gateway/daemon-site-registry.ts:339-383` (`evaluateRegister`): once `E(sites).register(direc...
- wire-watcher: **[should-fix]** `src/endo/gateway/publish.ts:214-233` (`publish`): the `try { intern } catch { discardDirectory }` g...
