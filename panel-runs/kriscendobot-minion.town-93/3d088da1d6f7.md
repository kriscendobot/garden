---
kind: panel-run
repo: kriscendobot/minion.town
pr: 93
panel_kind: code
base_ref: b83741a3543cc598cdd7fe513243c35df24ba5db
rounds: 1
disposition: must-fix
must_fix_total: 17
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 3d088da1d6f7
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #93 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `957036ad`

seat verdicts (29): archivist=pass assessor=must-fix benchmarker=comment breaker=comment changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=comment integrator=comment locksmith=must-fix migrator=comment packager=pass prover=must-fix pruner=pass purist=comment releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=comment surfacer=comment transplanter=pass typist=comment warden=pass wire-watcher=pass
must-fix items (17):
- assessor: `src/endo/gateway/publish.ts:257-268` — the best-effort guest-directory cleanup runs and is logged unconditionally ...
- assessor: `src/endo/gateway/content-gc-cli.ts:534-545` — `siteDirectories?.close?.()` runs unguarded inside a `finally` after...
- assessor: `src/endo/gateway/config.ts:57` — `Number(process.env.GATEWAY_GC_GRACE_MS ?? 3_600_000)` treats an explicitly-empty...
- corner-prober: **`GATEWAY_GC_GRACE_MS=""` silently zeroes the grace period instead of falling back to the documented default.** `con...
- corner-prober: **`readManifestStrict` has no size ceiling, unlike the audit's own `MANIFEST_INSPECTION_MAX_BYTES` bound.** `content-...
- corner-prober: **Grace-period tie (`modifiedAtMs === now - graceMs`) is never pinned.** Both the unit test (ages of `2*HOUR` vs `30_...
- corner-prober: **Minor:** `--drop-unresolved <id>` for a recordId that never existed as a vhost file silently no-ops (`readVhostReco...
- engine-realist: ## engine-realist — PR #93 (kriscendobot/minion.town)
- locksmith: `src/endo/gateway/publish.ts:161-163` (`ClipPublisherDeps.unpublish` signature) and `publish.ts:239` — `unpublish` ...
- locksmith: `src/endo/gateway/content-gc.ts:423` and `:485` (`excisionSiteDirectories.read(record.directoryId, "front")` / `optio...
- locksmith: `daemon-site-registry.ts:374-384` (`removeDirectory`)'s `/^clip-[a-z0-9-]+$/` pattern check on `directoryPetName` is ...
- prover: **must-fix** — `src/endo/gateway/content-gc.ts:305-338` (`inventoryBlobs`'s `fs.lstat`/`fs.readFile` ENOENT toleran...
- prover: **must-fix** — `src/endo/gateway/content-store.ts:213-231` (`internBlob`'s ENOENT-tolerant dedup-touch fallthrough,...
- saboteur: **[must-fix] Boundary value: empty `GATEWAY_GC_GRACE_MS` silently coerces to `0`, the most dangerous setting, not the...
- saboteur: **[should-fix] Type confusion: the orphan-audit's manifest-detection is purely structural, letting an attacker-publis...
- saboteur: **[nit, tight-try discipline] `content-store.ts:223-227`** — `return blobId;` sits inside the `try` that wraps `fs....
- spec-keeper: ## Per-juror block — spec-keeper — PR #93 (kriscendobot/minion.town)
