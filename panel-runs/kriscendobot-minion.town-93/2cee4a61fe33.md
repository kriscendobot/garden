---
kind: panel-run
repo: kriscendobot/minion.town
pr: 93
panel_kind: code
base_ref: origin/main-b83741a
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2cee4a61fe33
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #93 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `fd6821a9`

seat verdicts (30): archivist=must-fix assessor=comment benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix orthographer=pass packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — a safety comment misstates the shipped deployment.** `src/endo/gateway/content-gc.ts:406-409` says the...
- archivist: **should-fix — `.excised` tombstones are documented without a lifecycle.** `DEPLOYMENT.md:296-299` tells the operat...
- archivist: **should-fix — `writeVhostRecord`'s docstring did not follow its behavior change.** `src/endo/gateway/vhost-table.t...
- archivist: **should-fix — design § B.10 understates what landed.** `designs/clip-formula-id-origin-and-content-gc.md:688-694`...
- archivist: **comment-only — three prose nits.** `content-gc.ts:386` "Audit is the default (`remove: false`)" — `remove` is a...
- breaker: ## Juror: breaker — PR #93 (kriscendobot/minion.town)
- corner-prober: **must-fix-loop — absent store directory is indistinguishable from a clean one.** `content-gc.ts:238` and `:306` bo...
- corner-prober: **summary-fix — `--grace-ms " "` silently becomes 0.** `content-gc-cli.ts:42-47`: `" "` is truthy so `!value` passe...
- corner-prober: **summary-fix — `graceMs === 0` in delete mode is accepted, untested, unwarned.** `content-gc-cli.test.ts:20` pins ...
- corner-prober: **summary-fix — colliding temp name in the new atomic vhost write.** `vhost-table.ts:180` uses `${file}.tmp-${proce...
- corner-prober: **summary-fix — vhost-side crash residue is never collected.** That new temp-then-rename leaves `vhosts/<id>.json.t...
- corner-prober: **summary-fix — `--drop-unresolved` has no outcome in the report.** `runGc` `continue`s on a vanished record (`cont...
- curator: **should-fix** — `publish.ts:143` declares `removeDirectory?()` optional and `publish.ts:265` guards `record.guestD...
- curator: **should-fix** — `config.ts:32` types `graceMs?: number` while `loadGatewayConfig` always populates it; the optiona...
- curator: **comment-only** — `graceMs` is a GC-only knob on the shared `GatewayConfig`, sourced from `GATEWAY_GC_GRACE_MS`; `...
- curator: **comment-only** — `runGc` (`content-gc.ts:387`) and `computeOrphans` (`:90`) take anonymous inline option bags whi...
- curator: **comment-only** — `runContentGcCli` returns explicit `--help` as `{code: 0, stderr: USAGE}` (`content-gc-cli.ts:98...
- duality-auditor: **should-fix — `createDirectory` / `removeDirectory` are presented as a pair on one interface but are not counterpa...
- duality-auditor: **should-fix — `guestRegisterSource` / `guestRemoveDirectorySource` are a partial inverse presented as a whole one....
- duality-auditor: **comment-only — `pin` is now a half-pair with neither a consumer nor a release counterpart.** `ClipStore.pin` (`sr...
