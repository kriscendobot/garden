---
kind: panel-run
repo: kriscendobot/minion.town
pr: 93
panel_kind: code
base_ref: b83741a3543cc598cdd7fe513243c35df24ba5db
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 91eefb6ff18b
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #93 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `63b5de98`

seat verdicts (29): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=pass locksmith=must-fix migrator=comment packager=pass prover=pass pruner=pass purist=must-fix releaser=pass saboteur=must-fix scribe=pass spec-keeper=pass stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- assessor: **[must-fix]** `src/endo/gateway/publish.ts:245-249` — `unpublish` now calls `authority.removeDirectory(record.gues...
- assessor: **[should-fix, invariant overlap]** `src/endo/gateway/content-gc.ts:1-7` claims "It completes every mark before perfo...
- assessor: **[comment-only]** `src/endo/gateway/content-store.ts:213-224` — the dedup-refresh branch checks `fs.access(file)` ...
- breaker: Now I have concrete line numbers. Writing up the per-juror block.
- corner-prober: ## Findings
- duality-auditor: `GuestSiteAuthority.createDirectory(front, powerPetName): Promise<Directory>` and `GuestSiteAuthority.removeDirectory...
- duality-auditor: `guestRegisterSource` and `guestRemoveDirectorySource` (`daemon-site-registry.ts:182`, `:213`) are the guest-evaluate...
- duality-auditor: `content-gc-cli.ts` documents `--delete` and `--remove` as aliases of one forward action with "deliberately no `--aud...
- engine-realist: ## Per-juror block — engine-realist — PR #93 (kriscendobot/minion.town)
- fast-checker: ## Jury seat: fast-checker — PR #93 (kriscendobot/minion.town)
- gateway: `.github/workflows/deploy.yml:113` adds a new deploy step gated on `env.DEPLOY_TARGET == 'endo-gateway-gc'`, but the ...
- gateway: [proposed-rule: when a `.github/workflows/*.yml` job step's `if:` condition tests a new value against an env var sour...
- gateway: Root `package.json:18` adds `"clip-content-store-gc"` and root `.github/workflows/deploy.yml` are otherwise purely ad...
- locksmith: `src/endo/gateway/content-gc.ts:330-345` (the `dropUnresolved` loop in `runGc`) — the `--drop-unresolved` escape ha...
- locksmith: `src/endo/gateway/content-gc.ts:295-430` (`runGc`'s `remove: boolean` option gating the `fs.unlink` loop over `report...
- locksmith: `src/endo/gateway/content-gc-cli.ts` / systemd unit correctly default to audit-only and require an explicit `GATEWAY_...
- purist: `src/endo/gateway/content-gc.ts:606-634` (`parseVhostRecord`) re-derives the vhost-record shape predicate that `src/e...
- purist: `src/endo/gateway/content-store.ts:900-904` — `internBlob` now `fs.utimes`'s an existing blob on every dedup hit, a...
- saboteur: `content-gc.ts`'s `readVhostRecordStrict` (src/endo/gateway/content-gc.ts:~185-200) treats a vhost record that legiti...
- saboteur: `content-store.ts:206-208`'s docstring on `internBlob` — "Used by the fixture seeder and tests; the production gate...
