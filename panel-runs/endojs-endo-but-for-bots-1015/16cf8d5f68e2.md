---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1015
panel_kind: code
base_ref: 71e9fcf7d022ec6e891baff455af92b650128ec0
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 16cf8d5f68e2
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1015 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `971fe22c`

seat verdicts (29): archivist=must-fix assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=comment fast-checker=comment gateway=pass integrator=must-fix locksmith=comment migrator=pass packager=comment prover=must-fix pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=comment wire-watcher=comment
must-fix items (20):
- archivist: **`release`'s JSDoc/type describes a conditional `failed` parameter the implementation never reads.** `packages/claud...
- breaker: **Name-only tool prune does not stop argument-side capability escape.** `packages/claude/src/tool-permissions.js` pru...
- breaker: **The claimed "server-side dispatch check" is not wired to anything delivered in this PR.** `tool-permissions.js`'s h...
- breaker: **`sessionTag` uniqueness-per-spawn is a documented invariant with no enforcement.** `credentials-pool.js`'s header s...
- breaker: `pinnedCatalog`/`allowList` are computed once at `makeGuestInference` grant time and reused for every subsequent `inf...
- changeset-auditor: `packages/claude/package.json:3` — new package `@endo/claude`'s version is `"0.0.0"`, not `0.1.0`. `.changeset/add-...
- changeset-auditor: `packages/claude/` ships no `CHANGELOG.md` at all (not even the required bare stub). The rule requires the stub to ex...
- changeset-auditor: Both new changeset bodies (`.changeset/add-endo-claude.md`, `.changeset/claude-sandbox-subscription-kind.md`) are har...
- changeset-auditor: The sibling package `@endo/claude-sandbox` (pre-existing, not touched by this diff's package.json) was itself created...
- changeset-auditor: Package-set coherence and body-vs-diff identifier coherence otherwise check out: both changesets' front-matter packag...
- corner-prober: **`credentials-pool.js` `acquire`: duplicate/colliding `sessionTag` across concurrent calls is untested.** The module...
- corner-prober: **`argv.js` `buildArgv`'s five input-validation throws are entirely untested.** `mcpConfigPath`/`settingsPath` empty-...
- corner-prober: **`credentials-pool.js` `markCooling`: the `coolingUntil <= nowMs` equality boundary and non-positive/past `untilMs` ...
- corner-prober: **`results.js` `nonzeroExit(code)` has no range check and no boundary test.** `Number(code)` accepts `NaN` (from `Num...
- corner-prober: **`mcp-config.js` `assertLoopbackUrl`'s `[::1]` (IPv6 loopback) branch is dead in the test suite.** `mcp-config.test....
- corner-prober: **`shim.js`: JSON-RPC `id: 0` (falsy-but-valid) is untested.** `isNotification = id === undefined || id === null` cor...
- integrator: The PR body abandons the upstream template. `endojs/endo-but-for-bots`'s `.github/PULL_REQUEST_TEMPLATE.md` requires ...
- integrator: HEAD commit `971fe22c3` ("test(claude): prettier-format the new shim handler tests") is a pure line-wrap reformat of ...
- integrator: `packages/claude/src/formula-id.js`'s `GuestFormulaId` validates a bare 64-hex string and calls it a "formula id," wh...
- prover: `packages/claude/src/argv.js` — `buildArgv` always emits `--disallowedTools <KNOWN_BUILTIN_TOOLS.join(',')>` (the "...
