---
kind: panel-run
repo: kriscendobot/minion.town
pr: 83
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 86a0eb8e52c1
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #83 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `dfdd593c`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## archivist
- assessor: **must-fix — the root set misses an entire class of LIVE record, silently.** `content-gc.ts:117-119` skips any reco...
- assessor: **must-fix — a malformed `minAgeMs` silently disables the interlock.** `content-gc-main.ts:31-33`: `Number(flagValu...
- assessor: **should-fix — the published race-safety invariant is falsified on the happy path.** `content-gc.ts:33-38` and `des...
- assessor: **should-fix — the deploy step aborts before printing the report it exists to print.** `deploy/aws/scripts/deploy-e...
- assessor: **should-fix — `process.exit()` can truncate the `--json` report.** `content-gc-main.ts:85` exits immediately after...
- assessor: **comment-only — unlink reconstructs the shard instead of remembering it.** `content-gc.ts:203` rebuilds the path f...
- assessor: **comment-only — resident set is O(all blobs) under `MemoryMax=128M`.** Every blob's `GcBlobInfo` plus the full `re...
- breaker: **The mark step fails CLOSED on an unreadable manifest, not open.** `src/endo/gateway/content-gc.ts:120-135`: on a `r...
- breaker: **The test asserts the unsafe behavior under a title claiming the safe one.** `test/gateway/content-gc.test.ts:139` i...
- breaker: **The mtime interlock has a dedup hole; the JSDoc's dismissal of it is wrong.** `content-store.ts:internBlob` is writ...
- breaker: **Mark-before-list widens that same window.** `runContentGc` reads records, then lists blobs. A record written in bet...
- breaker: No `reachable-but-missing-on-disk` counter. `collectLiveBlobIds` marks names without stat'ing them, so content lost t...
- breaker: `listStoredBlobs` keeps only the filename; the unlink recomputes the shard from the id. A blob in a non-canonical sha...
- breaker: `deploy-endo-content-gc.sh:58` runs `systemctl start` under `set -e`; the oneshot exits 1 whenever `errors` is non-em...
- corner-prober: **A `directoryId`-only vhost record marks nothing — live content is swept.** `collectLiveBlobIds` does `if (!isBlob...
- corner-prober: **Resurrection race defeats the mtime interlock — the docstring's safety claim is false.** `content-gc.ts:34-38` as...
- corner-prober: **`minAgeMs = NaN` silently voids the interlock and vanishes from the audit.** `content-gc-main.ts:32` is `Number(fla...
- corner-prober: **`--json` truncates at 128 KiB.** `process.exit()` (`content-gc-main.ts:85`) follows an async `stdout.write`; agains...
- corner-prober: **Unlink path is reconstructed, not discovered.** `content-gc.ts:203` rebuilds `blobs/<id[0:2]>/<id>` rather than usi...
