---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 891
panel_kind: design
base_ref: b377b0ed051c7b7730067643ef592cb964c5b533
rounds: 1
disposition: must-fix
must_fix_total: 15
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 43ab391e6adf
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #891 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `8ad854cb`

seat verdicts (7): copyeditor=comment critic=must-fix decomplector=must-fix ergonomist=must-fix novice=comment pedant=must-fix skeptic=must-fix
must-fix items (15):
- critic: Phase 2 ("Agoric compatibility adapter") states the plan will "Change `@agoric/zone` to depend on and re-export the E...
- critic: The regrets table's first row notes `@agoric/base-zone` already exists as a portable-helpers split, but says it "make...
- decomplector: `## Naming and \`makeOnce\`` complects two distinct concerns in one primitive: `detached()` is (a) the zone system's ...
- decomplector: The `mermaid` dependency diagram draws `heap`/`daemon`/`agoric` all flowing into `ertp`, which reads as ERTP holding ...
- decomplector: The rest of the design disciplines itself well on this axis: it deliberately keeps policy (category-to-key naming, du...
- ergonomist: The design names `makeHeapZone` as a concrete implementation but leaves its sibling — "the package also exports a n...
- ergonomist: The core public surface lists `isStorable` as an `@endo/zone` export, but the host-adapter constructor separately req...
- ergonomist: `detached()` is introduced only in the "Naming and `makeOnce`" prose, outside the enumerated public-surface bullet li...
- pedant: `designs/endo-zone.md:1` — Title hyphenation breaks the corpus's title-case convention for hyphenated compounds: `#...
- pedant: `designs/endo-zone.md:94,100` — "Zone" capitalization is inconsistent within the document. It's capitalized as the ...
- pedant: `designs/endo-zone.md:160` (comment-only) — The `## Prompt` source citation diverges from the two citation shapes a...
- skeptic: The "Design Regrets and Constraints" table asserts specific facts about `@agoric/zone`'s current dependency graph ("m...
- skeptic: Phase 2 ("Agoric compatibility adapter") requires *changing* `@agoric/zone` inside `agoric-sdk` — a repo this proje...
- skeptic: "Naming and `makeOnce`" states duplicate-key reuse "fails before the provider runs," but never says what happens when...
- skeptic: The three Open Questions (store-package boundary, which daemon MapStore phases gate the adapter, `watchPromise` host-...
