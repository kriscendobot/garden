---
kind: panel-run
repo: kriscendobot/minion.town
pr: 68
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: cc5cd6980732
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #68 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `af21d405`

seat verdicts (30): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=comment migrator=must-fix orthographer=pass packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — the size-bound prose and error strings say "byte", the code counts UTF-16 code units.**
- archivist: **should-fix — the caller-visible ceilings are documented only in an internal comment.**
- archivist: **should-fix — `resolveGuestMainWorker`'s JSDoc mislocates one of its two call sites.**
- archivist: **comment-only — README rule 1 now contradicts its own example.**
- assessor: Reviewed the diff at `origin/main...HEAD` (15 commits, `publishNamedContent` + the shared `@main`/`MAIN` worker bridg...
- benchmarker: The per-entry cap is `z.string().max(…)`, i.e. **UTF-16 code units**, while the ceiling it defends is **decoded UTF...
- benchmarker: The OOM claim is about the *shared process*, but the bound is *per call* and concurrency is uncapped, so peak RSS sca...
- breaker: `src/endo/guest-tools.ts:159-168` claims the module-level invariant "**Every bound rejects BEFORE the allocation it g...
- breaker: `src/endo/guest-tools.ts:242-256`: the pre-parse `MAX_CONTENT_VALUE_JSON_BYTES` guard is inside `if (typeof raw === "...
- breaker: `dev/mock-as.ts:36` — "A code → scope map, populated at /authorize and **consumed** at /token." Nothing consumes ...
- breaker: Invariants I attacked and found **holding**: the `.strict()` union genuinely enforces "exactly one of `text`/`bytes`"...
- breaker: `publish.publish` runs the cheap `assertPowerPetName` (`src/endo/gateway/publish.ts:189`) only *after* `resolveNamedC...
- duality-auditor: ## Juror: duality-auditor
- engine-realist: ## engine-realist
- integrator: **[should-fix]** `src/endo/guest-tools.ts:664` still registers `"publishNamedContent"` as a bare literal, and `test/e...
- integrator: **[should-fix]** The head branch `feat/weblet-publish-dir` lands verbatim in the merge subject — `origin/main`'s st...
- integrator: **[should-fix]** Commit grouping is unfixed since round 4: `e5d3547 feat(clip): add a publish tool…` also carries t...
- integrator: **[comment-only]** `guest-tools.ts:596`'s new inventory line calls `upgrade` "re-publish … **in place**", while kri...
- migrator: `src/endo/guest-tools.ts:189` + `:243` — the "pre-decode" size guards count **UTF-16 code units, not bytes**, so th...
- migrator: Bump-level axis is **vacuous here** and I want that on the record rather than silently absent: `package.json` is `pri...
