---
kind: panel-run
repo: kriscendobot/minion.town
pr: 68
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2fbbdfbb337c
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #68 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `50245566`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=must-fix pruner=must-fix purist=comment releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Per-juror block — archivist (PR kriscendobot/minion.town#68)
- assessor: Reviewed the worktree diff (`origin/main...HEAD`, 11 commits, 8 files). Typecheck clean; `test/endo-clip-tools.test.t...
- benchmarker: ## benchmarker
- breaker: **`src/endo/guest-tools.ts:194` — the `@main` bridge is dead by construction at the new call site, falsifying the i...
- breaker: **`src/endo/guest-tools.ts:172-215` — the new path removes the only size ceiling `publish` had.** Inline `publish` ...
- breaker: `guest-tools.ts:184` — `typeof contentValue !== "string" || length === 0` is unreachable behind `z.string().min(1)`...
- breaker: `guest-tools.ts:207` — `bytesFromText` silently replaces a lone surrogate (reachable via JSON `"\ud800"`) with U+FF...
- breaker: `dev/mock-as.ts:36` — `grantedScopes` is never evicted; unbounded in a long-lived mock. Dev-only.
- corner-prober: **`resolveGuestMainWorker` can never return `@main` at the new call site — the "one shared decision" claim is false...
- corner-prober: **Every `text` fixture is pure ASCII, so the `bytesFromText` migration is untested where it differs.** `bytesFromText...
- corner-prober: **The new `is not valid base64` error branch is untested.** `decodeBase64` is strict (throws on bad char, missing pad...
- corner-prober: **`.min(1)` parity is claimed in the schema comment but not pinned.** Covered: non-array, non-JSON text, both/neither...
- corner-prober: **Pet names with non-identifier code points are unpinned (low).** `source` is built by interpolating `JSON.stringify(...
- corner-prober: **`dev/mock-as.ts` `grantedScopes` has no eviction or single-use boundary (dev-only).** A code is never deleted after...
- duality-auditor: `test/endo-clip-tools.test.ts:270` invents `toUtf8Bytes` for the text→bytes edge that the implementation declares a...
- duality-auditor: `src/endo/guest-tools.ts:154-157` claims the new schema is "one input contract across all three tools", but the contr...
- duality-auditor: `README.md:77-79` — "A composed tool names its *operation*, never its terminal method" is contradicted by the examp...
- duality-auditor: `src/endo/guest-tools.ts:211,217` — the two edges into `bytes` are named on different axes: `bytesFromText` names b...
- duality-auditor: Only `publish` gained the named-value input axis; `upgrade`, documented as sharing its input contract, has no `upgrad...
- duality-auditor: `resolveGuestMainWorker` (`src/endo/guest-control.ts:59`) is a one-directional selection with no counterpart; correct...
