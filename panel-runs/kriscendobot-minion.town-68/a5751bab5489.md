---
kind: panel-run
repo: kriscendobot/minion.town
pr: 68
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a5751bab5489
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #68 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `a6ad4a99`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR kriscendobot/minion.town#68
- assessor: `src/endo/guest-tools.ts:177` hardcodes the legacy worker name `"MAIN"`. The sibling live path deliberately does *not...
- assessor: `src/endo/guest-tools.ts:159-161`: the JSDoc says the pet name may resolve to "a text `storeValue`d JSON array, or a ...
- assessor: `dev/mock-as.ts:41`: `request.query?.scope ?? (request.query?.scope as unknown)` coalesces a value with itself — a ...
- assessor: `dev/mock-as.ts:56`: the fallback stamps the JWT, but the mock's `/token` **response body** still carries `scope: "du...
- assessor: `test/endo-clip-tools.test.ts:250-254`: the comment claims "text was UTF-8 encoded and bytes base64-decoded, and both...
- assessor: `src/endo/guest-tools.ts:171-173`: the `typeof !== "string" || length === 0` guard is unreachable — the parameter i...
- assessor: The `dev/mock-as.ts` OAuth-scope work is unrelated to the publish-directory feature and would read better as its own ...
- assessor: `ContentDirectory` omits the `.min(1)` that `publish`'s inline schema puts on `path`/`contentType` and on the array i...
- assessor: The module header still claims this file "imports NOTHING from `@endo/*`"; that was already false (`@endo/base64`) an...
- breaker: ## Juror: breaker — kriscendobot/minion.town#68
- changeset-auditor: **PR title names an identifier that exists nowhere in the diff.** Title: `weblet_publish_dir tool`. The diff register...
- changeset-auditor: **PR body uses vocabulary retired on the base.** Body says "Publish a weblet"; `weblet` → `clip` was completed on `...
- changeset-auditor: **PR body cites a nonexistent test path.** Body: `test/endo-weblet-tools.test.ts`. The diff touches `test/endo-clip-t...
- changeset-auditor: **An undeclared runtime dependency is buried in a rename commit.** `9a6ece8 "fix(weblet): spell out publish directory...
- changeset-auditor: Lockfile split is correct: `a6ad4a9 chore: Update package-lock.json` is last and alone, per `skills/yarn-lock-separat...
- changeset-auditor: Identifier rename is complete and consistent in the final tree (no `publishDir`/`contentDir`/`ContentDir` residue).
- changeset-auditor: Lockfile fact for the packager/wire-watcher, outside my lens to adjudicate: `@endo/bytes@1.0.2` pulls a **second** co...
- corner-prober: **`text` is unreachable through the only value a client can create.** `src/endo/guest-tools.ts:161` claims the pet na...
- corner-prober: **Empty `contentType` silently degrades to `application/octet-stream`.** `ContentDirectory` uses bare `z.string()` wh...
