---
kind: panel-run
repo: kriscendobot/minion.town
pr: 68
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a5751bab5489
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #68 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `a6ad4a99`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix — `src/endo/guest-tools.ts:159-160`: the JSDoc names an input the code rejects.** "Resolve a `contentDir...
- archivist: **must-fix — `src/endo/guest-tools.ts:33-36` vs `:43`: the module header is now falsified by the import block this ...
- archivist: **should-fix — `src/endo/guest-tools.ts:498-500`: the `registerClipTools` docstring's tool inventory omits the tool...
- archivist: **should-fix — `README.md:67-95` vs the new tool name.** The README asserts a new tool's name is "fully decidable" ...
- archivist: **should-fix — `src/endo/guest-tools.ts:571-578`: the description drops caller-facing constraints it inherits.** `p...
- archivist: **comment-only — `dev/mock-as.ts:41`: `request.query?.scope ?? (request.query?.scope as unknown)` reads the same ex...
- assessor: `guest-tools.ts:171` — the `typeof … !== "string" || length === 0` guard is unreachable; the tool's `content: z.s...
- assessor: `dev/mock-as.ts:35` — `grantedScopes` is never pruned, including after `/token` consumes the code; unbounded under ...
- assessor: `ContentDirectory` (`:151`) omits the `.min(1)` its `publish` sibling carries; an empty array reaches `publish.publis...
- assessor: The `registerClipTools` header list (`:498`) still names three tools; `upgrade` (pre-existing) and `publishDirectory`...
- assessor: The module header's "imports NOTHING from `@endo/*`" claim (`:33-34`) was already false on `main` (`@endo/base64`); t...
- assessor: Worktree hygiene: `src/endo/guest-tools.ts` was transiently dirty in the shared worktree mid-review (the `new Uint8Ar...
- benchmarker: PR body — "hand-pasting base64 … corrupts large files inline for MCP (see also #65)". **Closed by cited rationale...
- benchmarker: Commit `f801479` — "publishes through the existing publish facet — so no client-side byte round-trip." **Closed b...
- benchmarker: `src/endo/guest-tools.ts:163-166` JSDoc — **OPEN** (finding below).
- benchmarker: Round-1 juror proposals to replace the source construction with `facet.evaluate(worker, "content", ["content"], [cont...
- benchmarker: **must-fix-loop** — `src/endo/guest-tools.ts:163-166`: the JSDoc closes with "`text` is UTF-8-encoded here, so a **...
- benchmarker: **summary-fix** — the "drops … a worker round-trip" clause riding on the round-1 `evaluate`-values findings. The ...
- breaker: **The tool's stated purpose is to escape the MCP body bound, and no bound replaces it.** `src/endo/guest-tools.ts:161...
- breaker: **The JSDoc claims a shape the code rejects.** `:157-160` says the pet name may be "a text `storeValue`d JSON array, ...
