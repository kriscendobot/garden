---
kind: panel-run
repo: kriscendobot/minion.town
pr: 68
panel_kind: code
base_ref: 89904a9c992c3bc1c6d42865a2e23efbe52c5c0d
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b6d2892611f0
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #68 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `606092bb`

seat verdicts (29): archivist=must-fix assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=comment locksmith=must-fix migrator=comment packager=comment prover=must-fix pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=pass stylist=comment surfacer=comment transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **Must-fix — `src/endo/guest-tools.ts:151-167`: `ContentFiles`'s docstring is stranded above the wrong declaration....
- archivist: **Comment-only — verified accurate, no action needed.** The `guest-tools.ts` module docstring's new claim that `@en...
- breaker: `src/endo/guest-tools.ts:194-243` (`resolveNamedContent`) claims (comment, line 236-237): "Bound the total decoded si...
- breaker: `ContentFiles.parse(raw)` (guest-tools.ts:222) throws a raw `ZodError` on a malformed shape; the module elsewhere (JS...
- breaker: The `@main`/`MAIN` worker-selection bridge (`guest-control.ts` `resolveGuestMainWorker`/`mainWorkerName`) and its cap...
- corner-prober: ## Findings
- engine-realist: `src/endo/guest-tools.ts:227-254` (`resolveNamedContent`): the `MAX_TOTAL_CONTENT_BYTES` guard checks `totalBytes` on...
- engine-realist: `resolveGuestMainWorker` (`src/endo/guest-control.ts:60-70`) collapses every failure of `has(@main)` — including a ...
- locksmith: `src/endo/guest-tools.ts:194-227` (`resolveNamedContent`): the size attenuation this PR adds (`MAX_CONTENT_FILES`/`MA...
- locksmith: `src/endo/guest-control.ts:188-198` (`resolveGuestMainWorker`) and its two call sites correctly narrow what they pass...
- locksmith: `resolveNamedContent` (guest-tools.ts:203-210) correctly gates `contentValue` through `assertValidPetName` before spl...
- prover: **[must-fix]** `src/endo/guest-tools.ts:204` — `resolveNamedContent`'s `assertValidPetName(contentValue)` guard is ...
- prover: **[should-fix]** `dev/mock-as.ts:30-44` (the `grantedScopes` map + `beforeAuthorizeRedirect` handler, falling back to...
- prover: Byte-decode correctness (`text`→UTF-8, `bytes`→base64) is pinned by the manifest read-back tests (`test/endo-clip...
- prover: The privilege-bearing `evaluate` arguments (worker + `["guest"]`/`["@agent"]` endowment) are pinned at `test/endo-cli...
- prover: The `@main`/`MAIN` bridge (`resolveGuestMainWorker`) is pinned directly via `facet.mainWorkerName()` at `test/endo-cl...
- prover: The file-count and total-byte ceilings are each independently pinned (`:462`, `:479`) — disabling the byte check re...
- purist: **`resolveNamedContent`'s decoded-size ceiling is enforced after the unbounded allocation it claims to prevent.** `sr...
- purist: **Family-consistency gap: `publishNamedContent` has no `upgrade` counterpart.** The three-tool family now reads `publ...
- saboteur: **Must-fix — total-byte cap in `resolveNamedContent` checks *after* full decode, not before, so a single oversized ...
