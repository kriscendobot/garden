---
kind: panel-run
repo: kriscendobot/minion.town
pr: 17
panel_kind: code
base_ref: af3057499622978cb2d36bf078d1c067aeb7b007
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a3db4e4ec8f6
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #17 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `760c9ee1`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **`README.md:23-26` documents the `npm run client` surface but was not updated for the new `guest` mode.** This PR ad...
- archivist: **`dev/client.ts:3` summary line still claims the CLI calls "every tool".** In guest mode the call list is `guest_sta...
- archivist: **`dev/client.ts:5` "requests all toy scopes; every tool succeeds" is half-corrected.** The scope half was qualified ...
- archivist: **New env knobs carry no prose anywhere.** `MCP_GUEST_NAME`, `MCP_GUEST_TEXT` (`dev/client.ts:27-28`) and `MCP_DEMO_S...
- archivist: **Guest mode writes durable state with no lifecycle sentence.** `guest_write_text` persists `mcp-note` in the caller'...
- archivist: `test/endo-guest-http.test.ts:8-10` — the header's pinning bullet still describes only the 403 rejection case; this...
- archivist: `README.md:8` "three MCP tools" predates this PR (guest tools landed in B2) — stale, but not this PR's regression. ...
- assessor: `dev/client.ts:75-83` (must-fix) The post-restart read check has no failing branch. `guest_read_text` returns an abse...
- assessor: `dev/client.ts:29,57-66` (must-fix) `MCP_GUEST_OPERATION` is an unvalidated free string whose fallthrough branch is t...
- assessor: `test/endo-guest-http.test.ts:150-158` (must-fix) The "refreshed token" is not reliably a different credential, so th...
- assessor: `dev/client.ts:6-8` (should-fix) `npm run client -- guest` cannot exercise the guest tools under the documented invoc...
- assessor: `test/endo-guest-http.test.ts:172` `expect(res.status).not.toBe(403)` also passes on a 400 or 500. Pre-existing, but ...
- assessor: `dev/client.ts:22` guest mode requests `mcp/minions:write`, which no call in that mode uses. [proposed-rule: a demo m...
- assessor: The remaining hunks in both files are prettier reflow with no behavior change.
- breaker: **[must-fix]** `test/endo-guest-http.test.ts:184` — `expect(res.status).not.toBe(403)` is a near-vacuous oracle for...
- breaker: **[should-fix]** `test/endo-guest-http.test.ts:158,162` — the rename's entire added value rests on `tokenFor` minti...
- breaker: **[should-fix]** `dev/client.ts:29,57` — only the exact string `read` is recognized; every other value falls throug...
- breaker: **[should-fix]** `dev/client.ts:73-83` — guest mode exits 0 even when all three guest calls fail. `read` mode's cla...
- breaker: Attack **mitigated**, recorded so the panel does not re-raise it: the new test blesses "any second credential with ma...
- breaker: `dev/client.ts:42` prints `granted scope:` from `runOAuthFlow`'s `token.scope ?? opts.scope` (`dev/oauth-client.ts:14...
