---
kind: panel-run
repo: kriscendobot/minion.town
pr: 17
panel_kind: code
base_ref: origin/main-af30574
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: e5c6bd8c234f
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #17 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `23fdeda9`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=comment integrator=comment locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=comment stylist=must-fix surfacer=comment transplanter=comment typist=must-fix warden=pass wire-watcher=comment
must-fix items (20):
- archivist: **should-fix — `README.md:30` does not say *which process* needs `ENDO_SOCK`.** The gate is server-side (`src/confi...
- archivist: **should-fix — `README.md:8` and § *The tools and their scopes* still describe a three-tool surface.** This PR is ...
- archivist: **comment-only — `README.md:31-32` says guest mode "is expected to be denied" but omits that the command exits nonz...
- archivist: **comment-only — `dev/mock-as.ts:6-7` header is now stale about its own hook.** "We add one hook so issued tokens c...
- assessor: **must-fix — the round-trip assertion false-passes on the exact failure it exists to catch.** `dev/client.ts:205` u...
- assessor: **should-fix — `README.md:24` / `dev/client.ts:6` claim "every toy tool succeeds"; on a clean checkout every toy to...
- assessor: **comment-only — `test/endo-guest-http.test.ts:187-190` reads only the first `data:` line.** SSE permits a payload ...
- assessor: **comment-only — `test/endo-guest-http.test.ts:196`** dereferences `payload.result.tools` unguarded; a JSON-RPC err...
- assessor: `guestFailures` is incremented in the toy modes too but never read, so `npm run client -- read-only` still exits 0 wi...
- assessor: The second (render) `try` at `dev/client.ts:183-192` is largely defense-in-depth: the SDK validates `callTool` result...
- assessor: Verified clean: `tsc --noEmit` passes; full suite 131 passed / 3 skipped. Two `deploy/thunks/siwe` suites fail to loa...
- assessor: The `jti` addition (`dev/mock-as.ts:37-43`) is correct and load-bearing: `expect(refreshedAliceToken).not.toBe(initia...
- breaker: ## Per-juror block — breaker (PR kriscendobot/minion.town#17)
- locksmith: `dev/client.ts:95` — the default guest pet name `mcp-note` is written with an **overwriting** capability into a nam...
- locksmith: `dev/client.ts:60-66` — new capability *surface*, not new authority: the demo now plumbs `MCP_DEMO_SUB/_IDP/_EMAIL`...
- locksmith: Guest mode requests exactly `[SCOPES.TOOLS, SCOPES.GUEST]` and no minion scopes — correct POLA for its call list, a...
- locksmith: `dev/mock-as.ts:37` `jti` is inert server-side (no consumer in `src/` or `test/`) — a distinctness aid only, no new...
- migrator: `dev/client.ts:23` — the documented expected failure for guest mode is wrong for the deployed edge, which is the on...
- migrator: `dev/client.ts:60-72` — `MCP_GUEST_OPERATION` gets closed-set validation with `process.exit(2)`, and the comment at...
- migrator: `dev/client.ts:87,205` — the round-trip assertion false-negatives across the documented two-run workflow. `guestTex...
