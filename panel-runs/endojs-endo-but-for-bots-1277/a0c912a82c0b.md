---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1277
panel_kind: design
base_ref: 65902a8f8c2e5f198d479ff0d9a77eb25a7c36d3
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a0c912a82c0b
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1277 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `05d387f9`

seat verdicts (8): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix orthographer=pass pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: **must-fix — `designs/daemon-retention-labels.md:11-18`: the coined term `inviter` means the opposite of its own re...
- copyeditor: **must-fix — `:287-293`: the CLI paragraph does not parse.** "`endo pins` prints state… . **So** the operator-fac...
- copyeditor: **should-fix — `:118-120`: broken parallelism.** "must therefore apply the same … fallback the summary uses, neve...
- copyeditor: **should-fix — `:46`: garden-path parenthetical.** "the operator-navigable slot callers and legacy databases alread...
- copyeditor: **should-fix — `:20` vs `:33`: near-verbatim restatement** of "acceptance retains the guest's handle … in the inv...
- copyeditor: **comment-only — `:207-210`, `:270-278`:** the legacy-vs-empty rationale is argued three times (code comment, then ...
- copyeditor: **comment-only — `:62-76`:** invariant 1's body names the concrete `<len>_<segment>` encoding, while its own blockq...
- critic: `designs/daemon-retention-labels.md` § *Host browse and prune surface* — `prune` is specified as "one atomic SQLit...
- critic: § *Retention reason records* — `remoteHandle` is annotated "the pin's live target; resolvable while the pin exists...
- critic: § *Mint-time key validation* — the design defers the legacy-fallback length check into `accept()` because the `-fr...
- critic: § *What is the Problem Being Solved?*, ¶1 — the terminology paragraph defines *inviter* as "the local party that ...
- critic: The `edgeReasons` label-pairing holds: a segment carries one `referencedBy` store and pet names are unique per store ...
- critic: The durable-Set rejection is honest and not a strawman; the three-property bar for a future primitive is the right ga...
- critic: `endo unpin --expect <locator>` leans on `internalizeLocator` round-tripping a display locator back to the stored bar...
- decomplector: **must-fix — `## Derived lifecycle state` reads state from a place a different principal owns than the one `prune` ...
- decomplector: **should-fix — `## Invariants` 5 cannot produce the API's tri-state.** Invariant 5 mandates "a single-statement gua...
- decomplector: **should-fix — `storeIdentifier(name, id, reason?)` complects "absent argument" with "no reason".** `## Retention r...
- decomplector: **comment-only — precomputed `summary` on the wire fixes one audience's presentation.** `RetentionPathEdgeReason.su...
- ergonomist: **`endo pins` overclaims an established noun and excludes the host's own pins.** "Pins" already denotes persisted pin...
- ergonomist: **One concept, three spellings.** API `EndoHost.invitationRetentionPins()`, CLI `endo pins` / `endo unpin`, inspector...
