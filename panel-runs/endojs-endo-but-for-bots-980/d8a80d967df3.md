---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 980
panel_kind: code
base_ref: origin/llm-a54c3ad
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: d8a80d967df3
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #980 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `3353924c`

seat verdicts (28): archivist=pass assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=pass migrator=comment packager=comment prover=comment pruner=pass purist=comment releaser=comment saboteur=comment scribe=must-fix spec-keeper=must-fix stylist=pass surfacer=pass transplanter=pass typist=must-fix warden=must-fix wire-watcher=pass
must-fix items (20):
- breaker: `packages/goblin-chat/src/use-goblin-chat.js:47,79` still constructs `new TextDecoder('ascii')` and calls `.decode(by...
- breaker: Verified (not a bug): the pre-PR `TextDecoder('ascii')` behavior was a real capability-confusion vector — a peer-su...
- breaker: Verified (not a bug): `makeHandoffSessionKey`'s `\uXXXX` escaping of peer-supplied `exporterLocation` designators (`p...
- breaker: `decode.js`'s Proxy/subclass/detached-buffer defenses (`TypedArrayPrototype` intrinsic accessors via `Reflect.apply`)...
- engine-realist: `packages/ascii/src/decode.js:20-34` — the *entire* safety property of `decodeAscii`'s typed-array validation rests...
- engine-realist: `packages/goblin-chat/src/use-goblin-chat.js:47,79` still constructs `new TextDecoder('ascii')` and calls `.decode(by...
- integrator: **[must-fix]** PR title/description are stale against the shipped diff — merge-commit readability. Title is `fix(oc...
- integrator: **[should-fix]** `packages/goblin-chat/src/use-goblin-chat.js:48` still constructs `new TextDecoder('ascii')` (`ASCII...
- scribe: **[summary-fix, carried over from prior round]** The push `1a95fbac86` ("close the swissnum ASCII decode gap; correct...
- scribe: **[acknowledge]** Standing-orders/note-this surface remains **clean** at the current head. `pulls/980/comments` is em...
- scribe: **[acknowledge]** The round-2 completion-summary closure is sound: review `#pullrequestreview-4932358648`'s request-c...
- spec-keeper: ## Per-juror block — spec-keeper — PR #980 (endojs/endo-but-for-bots)
- typist: **must-fix** — Inline `import()` type reference in a JSDoc `@type` tag. `packages/ocapn/test/cryptography.test.js:1...
- typist: **should-fix** — Newly exported helper's parameter is typed `any` where the actual runtime shape is known and alrea...
- typist: `decodeAscii`'s `@param {Uint8Array} bytes` / `@param {string} [name]` / `@returns {string}` in `packages/ascii/src/d...
- typist: The `makeSturdyRef` signature widen from `secret: string` to `secret: string | Uint8Array` in `ref-kit.js` brings it ...
- typist: `swissnumHex`/`publish`/`publishHeld`/`unpublish` widened to `string | Uint8Array | ArrayBufferLike` in `hub.js` corr...
- typist: En dash in `0x80`–`0xff` range mentions (README, decode.js, util.js docstrings) is the tolerated numeric-range exem...
- warden: `packages/ocapn/src/client/sturdyrefs.js:75` (`enlivenSturdyRef`, local branch) and `packages/ocapn/src/client/sturdy...
- warden: `packages/ocapn/src/codecs/descriptors.js:333` (the `RangeError` fallback added in this diff): `secret = secretBytes`...
