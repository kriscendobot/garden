---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1046
panel_kind: code
base_ref: origin/llm-e22e67a
rounds: 1
disposition: must-fix
must_fix_total: 16
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: bf85266e564b
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #1046 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `9203e885`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=comment gateway=pass integrator=comment locksmith=pass migrator=comment packager=pass prover=pass pruner=pass purist=must-fix releaser=pass saboteur=comment scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=pass typist=pass warden=pass wire-watcher=pass
must-fix items (16):
- assessor: `packages/hardened262/scripts/agents/ironhorse.js:40-60` (`ensureIronhorse`): on a `cargo build` failure the thrown e...
- assessor: `packages/hardened262/scripts/agents/ironhorse.js:23-34` (`ironhorseHarnessSource`): the `.replace(regex, ...)` patch...
- assessor: `packages/hardened262/scripts/agents/ironhorse.js`: on a non-zero-exit/non-skip outcome, `decodeIronhorseOutcome` nev...
- breaker: **[must-fix] `run_preassembled_case` silently drops the `negative:` frontmatter, breaking the seam's own documented i...
- breaker: **[should-fix] Same bypass silently drops the `features:` skip list, degrading a clean `PreSkip("feature:...")` into ...
- breaker: **[should-fix] The `sta.js`/`assert.js` `toString` patch fails silently if the harness text ever drifts.**
- corner-prober: **`compactEnd`'s new trailing reason field is entirely untested, despite being a documented contract.** `packages/har...
- corner-prober: **`decodeIronhorseOutcome`'s crash/timeout branch silently drops any reason text.** `code !== 0 || signal !== null` r...
- corner-prober: **`ironhorseHarnessSource`'s regex rewrite silently no-ops on non-match.** `ironhorse.js`'s `source.replace(/Object\....
- engine-realist: `packages/hardened262/scripts/agents/ironhorse.js` `ironhorseHarnessSource` replaces `Object.defineProperty(Test262Er...
- purist: `packages/hardened262/scripts/agents/ironhorse.js:14-23` (`ironhorseHarnessSource`): the regex substitution replaces ...
- purist: `packages/hardened262/scripts/agents/ironhorse.js:109` names the failure-reason field `ironhorseReason` while the adj...
- spec-keeper: `packages/hardened262/scripts/agents/ironhorse.js:20-29` (`ironhorseHarnessSource`): the regex substitution replaces ...
- spec-keeper: The strict+async skip (`ironhorse.js:120-127`, `structural:async-strict-latch`) is honestly named and consistent with...
- stylist: `ironhorse+ses` (used as an agent-identifier string throughout `packages/hardened262/scripts/test.js:303,331,508`, `p...
- stylist: `decodeIronhorseOutcome` (`packages/hardened262/scripts/agents/ironhorse.js:97-110`) names the failure-reason field `...
