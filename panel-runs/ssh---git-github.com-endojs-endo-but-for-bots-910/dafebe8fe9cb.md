---
kind: panel-run
repo: ssh://git@github.com/endojs/endo-but-for-bots
pr: 910
panel_kind: code
base_ref: llm-3ec5585
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: dafebe8fe9cb
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/endojs/endo-but-for-bots #910 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `44d53c7c`

seat verdicts (26): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=comment integrator=comment locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #910
- assessor: **must-fix** — `packages/daemon/src/mount.js:1562`. `makeSha256` is an *optional* parameter (`makeMountFileExo` lin...
- assessor: **must-fix** — `packages/platform/src/fs/blob-range.js:192`. `base64Chunks(selected())` invokes `readWindow` *eager...
- assessor: **should-fix** — `packages/daemon/src/mount.js:1543`. `readWindow` calls `assertLive()`, but `range` itself does no...
- assessor: **should-fix** — `blob-range.js:181-185`. `compose` clamps `newHi` to `hi` but leaves `newLo = lo + start` unclampe...
- assessor: **comment-only** — `packages/platform/src/fs/interfaces.js:159` claims "The tag is distinct from the whole-value `R...
- assessor: **comment-only** — `interfaces.js:80`: `getInfoMethodGuard`'s doc still says "live blobs add `fetch` for windowed r...
- assessor: `packages/platform/test/blob-range.test.js:4` advertises "the design's cross-producer test matrix", but only the `Blo...
- breaker: `packages/platform/src/fs/blob-range.js:189-195` (must-fix) `streamBase64` calls `selected()` synchronously, but `bas...
- breaker: `packages/platform/src/fs/blob-range.js:181-185` (should-fix) `compose` adds `lo + start` with no re-validation, so n...
- breaker: `packages/platform/src/fs/interfaces.js:115` (should-fix) The comment claims the guards "require the returned value b...
- breaker: `packages/daemon/src/mount.js:1563-1566` (should-fix) `makeSha256` is optional and cast with `/** @type {() => Sha256...
- breaker: `packages/daemon/test/mount-revocation.test.js` (should-fix) Zero `range`/`textRange` occurrences. The design's matri...
- breaker: `blob-range.js:238` awaits the *entire* selection and `lineByteSpan:129` scans every byte, so `textRange(0, 1)` on a ...
- breaker: `blob-range.js:148` `Math.min`/`Math.max` is unreachable: `from > to` cannot occur given `endLine > startLine` and mo...
- changeset-auditor: ## Juror: changeset-auditor — PR #910
- curator: **must-fix** — `packages/platform/src/fs/interfaces.js:162`: `RichReadableBlobInterface = M.interface('ReadableBlob...
- curator: **should-fix** — `packages/platform/src/fs/blob-range.js:259`: the new public export `makeBlobRangeMethods` is type...
- curator: **should-fix** — `packages/platform/src/fs/extended/type-guards.js:299`: `BlobRefInterface` hand-copies `help` / `s...
- curator: **should-fix** — `packages/daemon/src/types.d.ts:1188`, `:1243`, `:1287`: `EndoReadable`, `ReadableBlobView`, and `...
