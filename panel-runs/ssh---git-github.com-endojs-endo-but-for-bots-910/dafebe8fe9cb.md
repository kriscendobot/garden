---
kind: panel-run
repo: ssh://git@github.com/endojs/endo-but-for-bots
pr: 910
panel_kind: code
base_ref: llm-3ec5585
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: dafebe8fe9cb
recorded_by: endolin-garden-ece02cb4
---

# Panel run — ssh://git@github.com/endojs/endo-but-for-bots #910 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `44d53c7c`

seat verdicts (24): archivist=error assessor=comment benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=comment curator=comment engine-realist=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=comment prover=comment pruner=comment purist=must-fix saboteur=must-fix scribe=comment spec-keeper=comment stylist=comment surfacer=comment typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- breaker: Probe files removed (the two remaining `zz-sab-*` are another seat's).
- changeset-auditor: `@endo/platform` (`src/fs/interfaces.js`, `types.d.ts`): drops exported `rangeReadMethodGuards`, `rangeReadConvenienc...
- changeset-auditor: `@endo/daemon` (`interfaces.js`, `mount.js`, `manager.js`, `help.md`): `EndoBlob` / `EndoMountFile` lose CapTP-visibl...
- changeset-auditor: `@endo/git`: `GitBlob` drops `fetch`, gains `range` / `textRange`.
- changeset-auditor: `@endo/exo-git`: exported `ReadableBlob` re-aliased to `RichReadableBlob` with a different member set.
- changeset-auditor: `@endo/agent-tools`: generated code-mode declaration `GitReadableBlobRange` removed.
- integrator: `packages/daemon-cas/README.md:40` — public `ReadableBlobRange.fetch(bigint, bigint)`.
- integrator: `packages/platform/src/fs/extended/DESIGN.md:559,618` — `E(blob).fetch(0n, expectedSize)`, in the doc for the very ...
- integrator: `designs/README.md:80,732` and `designs/agentry-git-eval-scenarios.md:127,133,158,160,171,431` — the roadmap still ...
- locksmith: ## locksmith — PR #910 (`feat(platform): ReadableBlob range attenuation`)
- purist: **must-fix** `packages/platform/src/fs/interfaces.js:162` — `RichReadableBlobInterface` is minted with the tag `'Re...
- purist: **must-fix** `packages/daemon/src/mount.js:1524,1562` — `makeSha256` is an *optional* parameter defaulted to `undef...
- purist: **should-fix** `packages/platform/src/fs/blob-range.js:148` — `return [Math.min(from, to), Math.max(from, to)]` *sw...
- purist: **should-fix** `packages/platform/src/fs/blob-range.js:181-185` — `compose` clamps `newHi` against the parent's `hi...
- purist: **should-fix** `blob-range.js:38,96` and `extended/shared/blob-ref.js:26,31` — `BASE64_CHUNK_RAW_BYTES` and `base64...
- purist: **should-fix** `blob-range.js:168,259` — `@returns {any}` / `Promise<any>` at the core of the new abstraction, whil...
- purist: Family gap: `SnapshotBlobInterface` / `snapshotBlobMethods` get no `range`/`textRange`, yet `interfaces.js:151` claim...
- purist: `packages/platform/src/fs/interfaces.js:80` — `getInfoMethodGuard`'s comment still says "live blobs add `fetch` for...
- purist: `interfaces.js:112-114` overclaims: `.returns(M.promise())` constrains nothing about the resolution, so it does not m...
- purist: `range()` cannot express "from offset to end-of-content" (both args required) though `readWindow` supports `end === u...
