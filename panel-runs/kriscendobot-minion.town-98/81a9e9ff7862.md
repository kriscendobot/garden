---
kind: panel-run
repo: kriscendobot/minion.town
pr: 98
panel_kind: design
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 81a9e9ff7862
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #98 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `0216e083`

seat verdicts (8): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix orthographer=pass pedant=must-fix skeptic=comment
must-fix items (20):
- copyeditor: **must-fix — "content address" contradicts the section it was rewritten to match.** §3 states the origin "is keyed...
- copyeditor: **must-fix — §2 Setup steps 3–4 name the wrong actor.** Line 94 calls the single CapTP message "the send that bo...
- copyeditor: **should-fix — Flow A / Flow B used ~300 lines before definition.** First uses are the status header (line 5), the ...
- copyeditor: **should-fix — inverted predication (line 397).** "Flow B **is** a hard dependency on PR 1125's guest-owned primiti...
- copyeditor: **should-fix — number disagreement (line 378).** "**they** [broker, facet, and kind] are **an explicit unbuilt prer...
- copyeditor: **should-fix — unparseable noun pile (line 377).** "a single-shape setup-token to subscription store" needs hyphena...
- copyeditor: **should-fix — objectless verb (line 156).** "Partial stages distinguish:" takes no object; read "The partial stage...
- copyeditor: **should-fix — near-verbatim duplication.** The `EndoHost.invite`-is-not-an-overload disclaimer appears twice (176�...
- copyeditor: **should-fix — non-parallel conjunctions and a forward reference.** "accept ... **and** consume ... **and** introdu...
- copyeditor: **comment-only.** §2 step 1: "**It** can change `0` to `1` exactly once" — antecedent is either `incrementChalleng...
- critic: §3 asserts "The driver sees the run id and ticket, not the body", and §6's defense against a driver-published clip ...
- critic: §2 setup 1 says a second `incrementChallenge()` returns `already-used` "without changing state", yet the §2 partial...
- critic: §7 lists ten unbuilt prerequisites (endowment seam, broker, `api-token`, draft PR 1125 with open maintainer directio...
- critic: The harness fetch appends `?run=$RUN_ID` (l.269) while the authoritative verifier fetches plain `proof.txt`; say the ...
- decomplector: **The oracle record is a mutable place with time complected out, yet the design grades on ordering.** `{ value, calls...
- decomplector: **`endowments: { challenge: EndowmentTicket }` (line 44) braids naming with authorization.** The key is fixed and the...
- ergonomist: `designs/claude-on-minion-town-evaluation.md:41-49` — the endowment seam breaks the sibling `create`'s **never-reje...
- ergonomist: `:44-49` — the `endowments` bag is an extensible-looking record with exactly one legal, semantically inert key. The...
- ergonomist: `:246,256,268` — `minion-evalctl` siblings spell inconsistently. `observe --expect pending` and `wait --timeout 300...
- ergonomist: `:313-318` with `:1` report schema (`:127-131`) — Flow A's `evaluate` fallback makes the gate it exists to enforce ...
