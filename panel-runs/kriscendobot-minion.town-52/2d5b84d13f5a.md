---
kind: panel-run
repo: kriscendobot/minion.town
pr: 52
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2d5b84d13f5a
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #52 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `1f378d95`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `src/endo/gateway/daemon-site-registry.ts:41-43` (§ Verification status, **V1**) states that the durable-across-rest...
- archivist: `src/endo/gateway/daemon-site-registry.ts:75-79` (R2), `:375-376`, and `:383-384` all justify the `directoryFor` upgr...
- archivist: `src/endo/gateway/site-registry-exo.ts:8-10` says "every fresh guest, introduced to it as `sites` (`@sites`) at provi...
- archivist: `src/endo/gateway/site-registry-exo.ts:27-33` (EMIT STABILITY) claims the install-source test makes "a future toolcha...
- archivist: `src/endo/gateway/publish.ts:121-126`: the `evaluateRegister` JSDoc ends "(The JSDoc no longer pins the exact evaluat...
- archivist: `src/endo/gateway/site-registry-exo.ts:45` cites `packages/daemon/src/{guest,directory}.js` for the store arities whi...
- archivist: `src/endo/gateway/daemon-site-registry.ts:252-253`: "two gateway processes minting in the same millisecond" — the n...
- archivist: Checked clean: all design cross-references (§§ 2.2, 3.1, 3.2, 3.4, 5, 7, 9) resolve; every `DEPLOYMENT.md` quoted b...
- assessor: **`daemon-site-registry.ts:181-189` — the `store` endowment is the wrong facet; every exo store call will fail.** `...
- assessor: **`daemon-site-registry.ts:243, 268` — the guest evaluate binds the guest's Handle, then calls directory methods on...
- assessor: **`daemon-site-registry.ts:322-329` — orphan registration on a partial write.** `evaluateRegister` registers in the...
- assessor: **`config.ts:119-128` / `DEPLOYMENT.md` claim more than the gate delivers.** Both state that while `WEBLET_SITES_LIVE...
- assessor: **`site-registry-exo.ts:222-241` — `list(owner)` is O(all sites in the deployment) serial CapTP round-trips.** One ...
- assessor: `daemon-weblet-wiring.ts:130-133` — `wiring.close()` is never called from `http.ts:183`, and no `cancelled` is thre...
- assessor: `site-registry.ts:62` still hardcodes `E(conn.host).lookup("sites")` while the PR introduces `SITES_HOST_NAME` specif...
- assessor: `daemon-site-registry.ts:409` — `revision: () => 0` means read-side caches can never observe a change on the live p...
- breaker: **The `WEBLET_SITES_LIVE` gate arms but never disarms — the "cannot occur" claim is false.** `src/config.ts:119-129...
- breaker: **`evaluateRegister` writes an fs vhost record for a hash it never established the owner owns.** `daemon-site-registr...
- breaker: **`register` pins an *arbitrary* formula id; `directory()` resolves it with a full-host authority.** `installSiteRegi...
- breaker: `guestRegisterSource` (`daemon-site-registry.ts:263-277`) bakes `front`/`powerPetName`/`owner` into source a SES work...
