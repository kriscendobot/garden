---
kind: panel-run
repo: kriscendobot/minion.town
pr: 52
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a61874fbd76c
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #52 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `1f378d95`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=comment engine-realist=comment fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=error purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=comment transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #52 (`kriscendobot/minion.town`)
- assessor: `src/endo/gateway/daemon-site-registry.ts:296` (`guestPetName = "@self"`) with `:236` (`E(guest).lookup('@self')`) �...
- assessor: `src/endo/gateway/site-registry-exo.ts:236` (`unregister`) — `E(store).remove(hash)` is not the no-op its own JSDoc...
- assessor: `src/endo/gateway/publish.ts:220` (`unpublish`) vs `:214` (`list`) — on the live path the two surfaces read differe...
- assessor: `src/endo/gateway/daemon-site-registry.ts:363` — `store.register({ id, directoryId: id, owner })` drops the cross-o...
- assessor: `src/http.ts:216` — `authorities` caches one authority per owner against the *first* `GuestAgent`; `guest-service.t...
- assessor: `daemon-site-registry.ts:411` — `directoryFor` returns a stub for *any* id; harmless today (the gateway uses `makeD...
- assessor: Verified correct: the `{sites: '@sites'}` → `{sites: 'sites'}` fix is real (`pet-name.js:14-23` rejects `@` in a st...
- breaker: ### Seat: breaker — PR #52 (kriscendobot/minion.town)
- integrator: ## Juror: integrator — PR #52 (kriscendobot/minion.town), base `origin/main`
- locksmith: **locksmith** — PR #52 (`kriscendobot/minion.town`), diff base `origin/main`
- migrator: **must-fix — the live path introduces a SECOND authoritative registry with no cutover for records the first one wro...
- migrator: **should-fix — identities provisioned while the flag was off get no `sites` name after the opt-in.** `guestPowersFr...
- migrator: Bump/cascade axis is N/A and correctly so: `package.json` is `private: true` with no `.changeset`, so the `makeGuestS...
- migrator: The `labelToId` canonicality tightening (`src/endo/gateway/base32.ts:131`) is safe for prior callers: every label eve...
- migrator: `GuestSiteAuthority.assertUpgradable?` is optional, so the memory and degraded authorities need no change — additiv...
- packager: **The PR's central safety claim is unpinned by CI.** `src/config.ts:200` adds `webletSitesLive` (parser `/^(1|on|true...
- packager: **`1f378d9` conflates a feature, a bug fix, and two mechanical sweeps.** One commit carries the new `WEBLET_SITES_LIV...
- packager: **The authoritative record doesn't name the lever the other two surfaces do.** `WEBLET_SITES_LIVE` has 7 mentions in ...
- packager: Changeset discipline is **n/a** here: no `.changeset/`, `private: true`, single package — the equivalent record sur...
