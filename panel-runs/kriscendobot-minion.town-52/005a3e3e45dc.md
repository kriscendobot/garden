---
kind: panel-run
repo: kriscendobot/minion.town
pr: 52
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 005a3e3e45dc
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #52 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `8c5910b4`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: Review complete. Here is my per-juror block.
- assessor: `src/http.ts:159` — swapping in the daemon registry drops the **only** writer of the fs vhost table. `makeSiteRegis...
- assessor: `daemon-site-registry.ts:174-181` — `guestRegisterSource` emits top-level `await` (×4) and a top-level `return` in...
- assessor: `daemon-site-registry.ts:179` — `E(self).identify([dirPetName])` passes an array to a **variadic** `identify(...pet...
- assessor: `site-registry-exo.ts:179-180` — `E(store).remove([hash])` hits variadic `remove(...petNamePath)` (`directory.js:41...
- assessor: `daemon-weblet-wiring.ts:52` / `http.ts:155` — `connectDaemonHost` is called with no `cancelled`, so it defaults to...
- assessor: `daemon-site-registry.ts:230` — unchecked `directory as PendingSiteDirectory`. A `SiteDirectory` from any other aut...
- assessor: `daemon-site-registry.ts:248` — `unregister: async () => false` falsifies the published `SiteHandle.unregister` con...
- assessor: The header's R2 claim ("`writeDirectory` throws a clear not-yet error") is unreachable: `upgrade` fails earlier at `p...
- assessor: `provideHost(storeName)` endows the exo with a full **host** agent (`evaluate`, `makeUnconfined`), where a directory ...
- breaker: ## Juror: breaker — PR #52 (kriscendobot/minion.town)
- corner-prober: **Valid-hex-but-wrong-length id — the inlined codec drops the guards the canonical one calls load-bearing.** `base3...
- corner-prober: **V1 (restart durability) is asserted nowhere.** `daemon-site-registry.ts` says V1 is "asserted by the B1-shaped inte...
- corner-prober: **`evaluateRegister` result is unvalidated.** `daemon-site-registry.ts:222` casts to `{hash,url}`; `sites` resolves i...
- corner-prober: **Concurrent-arrival on the ownership gate.** `register` awaits `maybeReadText` before `writeText` — two interleave...
- corner-prober: **The miss boundary pins the fake, not the daemon.** `expect(await exo.directory(ID_B)).toBeUndefined()` — the real...
- corner-prober: **`localeCompare` on hex ids** (`site-registry-exo.ts:171`) makes `list` order ICU-build/locale-dependent. **summary-...
- corner-prober: **`http.ts` fallback has no test and two corners**: `await installDaemonWebletRegistry` has no timeout (a socket that...
- curator: **`src/endo/gateway/site-registry-exo.ts:105,125` — the inlined base32 codec is *not* the exported contract it shad...
- curator: **`src/endo/gateway/daemon-site-registry.ts:268` — `makeDaemonSiteRegistry` declares the full `SiteRegistry` but ho...
