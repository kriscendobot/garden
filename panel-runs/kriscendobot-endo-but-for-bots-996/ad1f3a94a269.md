---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 996
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: ad1f3a94a269
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #996 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `cad85efe`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: `designs/worker-constraint-model.md:414` and `:754` cite `designs/endo-daemon-aws-storage.md`. **That file does not e...
- archivist: `:528-530` names `bus-manager-node-powers.js` and `manager-go-powers.js` as the two backends that "stop before `marsh...
- archivist: Two citations land near-but-off the claim. `:237` cites `bus-manager-node-powers.js:206` for "fall back to a default ...
- archivist: `README.md:1401` is the **only** markdown-linked Design cell among 122 milestone-constituent rows; every sibling (inc...
- archivist: `## Status` (`:806`) sits second-to-last; `designs/AGENTS.md` § Document Structure scopes that prose section to "doc...
- archivist: `WorkerTarget`'s JSDoc (`:386-394`) documents only omitted and `{ os, architecture }`, but both sub-fields are option...
- assessor: **must-fix** — `designs/worker-constraint-model.md:591-596`: a non-seed runtime persists as `constraints: { runtime...
- assessor: **must-fix** — `:432-434` + mermaid `:441` type `encodeWorkerConstraints` over `ResolvedWorkerConstraints`, whose e...
- assessor: **should-fix** — `:421-423` places `resolveWorkerConstraints` "between formulation and `controlPowers.makeWorker`",...
- assessor: **should-fix** — `:528-534` and OQ1 `:766-771` name `bus-manager-node-powers.js` and `manager-go-powers.js` as the ...
- assessor: **should-fix** — `:597-601`/`:637-641` define "explicit spelling of the flexible default" only over fixed *tokens*,...
- assessor: **comment-only** — `:686-690` states `constraints.runtime > kind > defaultWorkerKind` as a "single rule", but disag...
- breaker: **The three-edge seam contract cannot implement the migration rule, and falsifies the stated round-trip property.** M...
- breaker: **`metered`/`retention` are erased on both paths, falsifying the #984 acceptance criterion.** They are absent from `R...
- breaker: **The two late-binding reads are not rewritten.** `manager.js:2172` (`workerFormula.kind ?? defaultWorkerKind`) and `...
- breaker: **Fail-closed cannot hold: the gate can't see per-backend serviceability.** The allowlist sits in `resolveWorkerConst...
- breaker: **`WorkerTarget.os`/`architecture` are open `(string & {})` yet feed a path/URL join, and `ResolvedWorkerVersion = { ...
- breaker: Precedence `constraints.runtime > kind` is unreachable — disagreement is rejected, so it applies only when they agr...
- breaker: Open Question 1's arity-skew premise names the wrong pair: the backends stopping before `marshalLoadError` are `bus-m...
- breaker: § Passability 1 says a "hardened copy-record" pass forecloses the varying-Proxy-getter attack, but `harden` freezes ...
