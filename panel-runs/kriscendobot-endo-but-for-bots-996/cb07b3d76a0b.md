---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 996
panel_kind: design
base_ref: 16fa8ebcc726ef4979cbc62e5f73a878cc922eee
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: cb07b3d76a0b
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #996 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `1ccd4a8f`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=comment pedant=comment skeptic=must-fix
must-fix items (20):
- copyeditor: `designs/README.md:16` — "count 151 → 152, Proposed 26 → 27" (bare prose) → `151 -> 152`, `26 -> 27`.
- copyeditor: `designs/worker-constraint-model.md:88` — `` `kind → constraints.runtime` `` is a code span carrying other text b...
- copyeditor: `designs/worker-constraint-model.md:300` — `⇆` inside the flagged diagram (finding 2) → resolved automatically ...
- copyeditor: `designs/worker-constraint-model.md:315` — `` `locked`↔archive path `` in the mapping table → `` `locked` <-> a...
- copyeditor: `designs/worker-constraint-model.md:368,370` — bare-prose `⟹` ("axis ⟹ persist as legacy…") → `=>` or the w...
- critic: `## Migration of Today's Two Kinds` — the inbound normalization rule ("`constraints.runtime ∈ {'locked','node'}` ...
- critic: `## The Seam It Plugs Into` — the widened `makeWorker` signature types the new parameter as `constraints?: Resolved...
- critic: `WorkerRuntime = 'locked' | 'node' | 'xs-in-rust' | string` trades away compile-time typo protection for open extensi...
- critic: The README's own delta accounting ("count 151 → 152, Proposed 26 → 27") doesn't cleanly reconcile against the mul...
- critic: All claims this design makes about the current codebase (`types.d.ts`, `manager.js` line-level behavior, `bus-manager...
- decomplector: The zero-identity-churn invariant (`## Migration`) is stated as a structural rule ("`constraints.runtime ∈ {'locked...
- decomplector: The runtime axis's own definition ("Engine + supervision axis") bundles two independent concerns into one open string...
- decomplector: The `persistence` axis's `durable` variant nests `journal` (a mechanism choice), `metered`, and `retention` (governan...
- ergonomist: Sibling axis-value type names spell inconsistently: `WorkerRuntime` and `WorkerPersistence` carry no suffix, while `W...
- ergonomist: The four axes use four different value-shape idioms for what the design frames as parallel, independently-optional sl...
- ergonomist: `makeWorker`'s control-power contract grows to an 11th optional positional parameter (`constraints`, after `label`, `...
- ergonomist: Open Question 3 ("how does the daemon report it cannot service a constraint") leaves error visibility completely unsp...
- ergonomist: `defaultWorkerKind` keeps its legacy name while the rest of the surface adopts "runtime" as the term of art, forking ...
- skeptic: The `persistence` axis (`## Axis 2`) frames durability/metering/retention as a **spawn-time constraint baked into the...
- skeptic: **Behavioral acceptance** ("every formula, every spawn, and every content address is identical to `llm` today") is th...
