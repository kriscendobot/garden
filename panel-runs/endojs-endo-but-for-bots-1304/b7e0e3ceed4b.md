---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1304
panel_kind: code
base_ref: llm-387ea66
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b7e0e3ceed4b
recorded_by: oros-studio-garden-ce242c49
---

# Panel run — endojs/endo-but-for-bots #1304 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `672ed337`

seat verdicts (31): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=pass curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=comment orthographer=pass packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=pass surfacer=must-fix thesaurus=pass transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- assessor: `packages/daemon/src/directory.js:761-770` — `readOnly()` mints its `ReadableNameHub` view via `formulateEval(direc...
- assessor: `packages/daemon/src/directory.js:761-770` — every call to `readOnly()` passes `specifiedWorkerId: undefined` to `f...
- assessor: `packages/daemon/src/types.d.ts` marks `EndoDirectory.readOnly?()` optional while `DirectoryInterface` in `interfaces...
- breaker: **1. (must-fix) The read-only eval formula is never pinned or rooted — collection can reclaim it and kill the view....
- curator: **Must-fix — new `EndoDirectory.readOnly()` collides in name but diverges in attenuation depth from the pre-existin...
- curator: **Should-fix — `EndoDirectory.readOnly` is typed optional (`readOnly?()`) but the runtime guard makes it mandatory,...
- curator: **Comment-only — `readOnly()` return shape diverges across implementers of the same guarded method.** `directory.js...
- duality-auditor: `EndoDirectory.readOnly()` and the `MailHub`/`MessageHub` implementations of the same interface method are named and ...
- duality-auditor: The cross-reference in `packages/daemon/src/types.d.ts:992` ("Contrast `EndoMount.readOnly()`, whose `SubMount` narro...
- engine-realist: ## Findings
- integrator: ## Findings
- locksmith: **must-fix — `readOnly()` grants every directory holder authority to spawn unbounded worker processes.** `packages/...
- locksmith: **should-fix — the recognized recipe is forgeable, so recognition confers no authority.** `readOnlyDirectorySource`...
- locksmith: **comment-only — the view's formula is neither pinned nor retained.** `pin` is `undefined` at `packages/daemon/src/...
- locksmith: **comment-only — `readOnlyHelp` inherits from `Object.prototype`.** `packages/daemon/src/directory.js:59`: `readOnl...
- packager: `.changeset/readonly-directory-attenuation.md:6-7` describes the read-only view's exposed surface as "`has`, `list`, ...
- packager: `packages/daemon/src/interfaces.js:234` adds `readOnly: M.call().returns(M.promise())` to `DirectoryInterface` with n...
- packager: Commit split is clean: `a8d2980ed` (feat) and `672ed3372` (test) are each self-contained with no conflated autofix; c...
- prover: **[must-fix]** `packages/daemon/src/manager.js:2792` and `packages/daemon/src/manager.js:3179` — the new `readOnly:...
- prover: **[should-fix]** `packages/daemon/test/endo.test.js:7855` doesn't exercise the single most safety-relevant property t...
