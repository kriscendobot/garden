---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1304
panel_kind: code
base_ref: 387ea66146a0b64d9d94a858369b0d37b09c55db
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: bf80adfcdcc0
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1304 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `bd7e57aa`

seat verdicts (31): archivist=pass assessor=pass benchmarker=pass breaker=pass changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=must-fix migrator=pass orthographer=comment packager=pass prover=comment pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=comment stylist=pass surfacer=pass thesaurus=pass transplanter=pass typist=pass warden=must-fix wire-watcher=must-fix
must-fix items (20):
- corner-prober: **`mailHub.readOnly()` and `messageHub.readOnly()` are completely untested — zero coverage on 2 of the 3 call sites...
- corner-prober: **The documented `EndoGuest`/`EndoHost` rejection behavior is asserted but not pinned by a test.** `types.d.ts`'s `re...
- corner-prober: **Empty/zero-length path arguments through the read-only view are unexercised.** `lookup`/`maybeLookup` accept `NameO...
- corner-prober: **Concurrent (non-sequential) `readOnly()` calls on `EndoDirectory` are untested.** The memoization test (`'is memoiz...
- engine-realist: **must-fix — the read-only view escapes formula collection's sever path.** `packages/daemon/src/directory.js:753-76...
- engine-realist: **should-fix — the memo comment is wrong about amplification** (`directory.js:715-720`). `@endo/captp` defaults `gc...
- engine-realist: **comment-only — eager mint in `manager.js:2764` and `3160`.** `mailReadableView`/`messageReadableView` are allocat...
- engine-realist: **comment-only — `t.is(first, second)`** (`test/endo.test.js`, memoization test) pins CapTP presence identity, whic...
- integrator: ### Findings
- locksmith: **should-fix — the holder-visible self-description understates the leak.** `directory.js:52` (`readOnlyHelp['']`), ...
- locksmith: **should-fix — stale comment now guards a load-bearing guard.** `src/interfaces.js:86` still says `ReadableNameHubI...
- locksmith: **comment-only — the view has no formula identity, so it is neither grantable nor revocable.** `mail.js:948` requir...
- locksmith: **comment-only — `readOnly` is unconditional in `DirectoryInterface` but absent from `GuestInterface`/`HostInterfac...
- pruner: ## Findings
- purist: **should-fix — the new type does not join the lattice the runtime guard already asserts.** At runtime `nameHubMetho...
- purist: **should-fix — `readOnly` now means two different depths in one package.** `EndoMount.readOnly()` / `EndoMountFile....
- purist: **should-fix — `readOnlyHelp` (`directory.js:59,89`) hand-rolls `@endo/helpdown`'s `makeHelp`.** Every other exo us...
- purist: **should-fix — `readOnly?()` (`types.d.ts:1038`) is optional where optionality buys nothing.** `EndoAgent extends E...
- saboteur: **should-fix — the PR contradicts itself on how strong the attenuation is.** `packages/daemon/src/types.d.ts:990` c...
- saboteur: **should-fix — the module comment claims a validation the guard does not perform.** `src/directory.js:36-40` says m...
