---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1304
panel_kind: code
base_ref: 387ea66146a0b64d9d94a858369b0d37b09c55db
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a43edb561d3f
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1304 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `672ed337`

seat verdicts (31): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix orthographer=must-fix packager=comment prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=pass surfacer=must-fix thesaurus=pass transplanter=pass typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- assessor: **must-fix** — `packages/daemon/src/directory.js:758-772`: `EndoDirectory.readOnly()` formulates the read-only-view...
- assessor: **should-fix** (secondary/invariant overlap) — `packages/daemon/src/types.d.ts:1013` (`readOnly?(): Promise<Readabl...
- breaker: **must-fix — the eval formula is neither pinned nor named, so the next daemon start sweeps it.** `directory.js:761`...
- breaker: **must-fix — `readOnly()` is unmemoized and spawns a worker per call.** `formulateEval` draws a fresh `randomHex256...
- breaker: **should-fix — retention inversion.** Until that restart sweep, the eval's `hub` endowment edge retains the backing...
- breaker: **should-fix — `isReadOnlyDirectoryFormula` is forgeable.** Any guest with `evaluate` can formulate `{type:'eval', ...
- breaker: **should-fix — `mailHub`/`messageHub.readOnly()` returns `this`** (`manager.js:2790`, `3177`), which still *has* `r...
- breaker: **comment-only — `readOnlyHelp` inherits `Object.prototype`**: `help('toString')` yields a function, so the `M.stri...
- breaker: **comment-only —** no `help.md` entry for `EndoDirectory.readOnly`, unlike sibling `EndoMount.readOnly`.
- changeset-auditor: `.changeset/readonly-directory-attenuation.md`: the body is written as word-wrapped prose (each of the four sentences...
- changeset-auditor: None.
- corner-prober: **[must-fix-loop]** The PR's central claimed contract — attenuation is *shallow*: a nested directory reached via `l...
- corner-prober: **[summary-fix]** `mailHub`/`messageHub`'s `readOnly: async () => mailHub` (manager.js:~2789, ~3176) has no test cove...
- corner-prober: **[summary-fix]** Multi-segment/array-form paths through the read-only view are untested: every call in `endo.test.js...
- corner-prober: **[summary-fix]** Empty-collection boundary: `list()` on a `readOnly()` view of a directory with **zero** entries, an...
- corner-prober: **[comment]** `readOnly()` is not idempotent — `formulateEval` (manager.js:5812) mints a fresh random formula numbe...
- corner-prober: **[comment]** `readOnlyHelp`'s fallback (`readOnlyHelp[method] ?? readOnlyHelp['']`) silently returns the top-level d...
- curator: `packages/daemon/src/directory.js:56-70` hand-reconstructs the `ReadableNameHub` interface guard (`help`/`has`/`list`...
- curator: Good handling of the sibling-surface axis this seat watches for: `EndoDirectory.readOnly()` is deliberately **shallow...
- curator: Changeset bump (`minor` on `@endo/daemon`, version `2.5.3`) correctly matches a pure-addition public surface (new `re...
