---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: e1a9d90f36e2
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `f43b20e6`

seat verdicts (16): archivist=must-fix assessor=comment breaker=comment curator=must-fix engine-realist=comment locksmith=must-fix migrator=must-fix packager=must-fix prover=comment purist=comment saboteur=comment spec-keeper=comment stylist=must-fix typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: `skills/pre-execution-audit/SKILL.md:108-137` — § 7 is declared *the canonical statement* of the data-sufficiency ...
- archivist: `designs/ensemble-forecasting.md:1059-1061` — the sentence inverts its own claim: "Both halves of the well-formedne...
- archivist: `packages/pipeline/forecaster.js:489` — `countObservedFramesAndReturns(frames, observed, length = safeLength(frames...
- archivist: `bin/finbot-ooda:179-180` — comment breaks mid-sentence: "…the base of the DENOMINATOR its ratio is taken over. /...
- archivist: `bin/finbot-ooda:231-249` — the new `printHelp()` block is headed "Options (see the file header for the full per-fl...
- archivist: Checked and accurate, no action: the `packages/pipeline/README.md` recording-primitives table (every named export exi...
- curator: **should-fix** `packages/pipeline/auditor.js:867` declares `sanitizeLabel(text)` as `@param {string} text`, and `pack...
- curator: **should-fix** The `audit()` config bag is declared independently three times: `packages/pipeline/auditor.js:49` (JSD...
- curator: **comment-only** `package.json` enumerates `./forecaster` and `./auditor` as subpaths, so the new `computeDataSuffici...
- curator: Round-4 M8 is resolved: the restated criterion holds for all six promoted names. `bin/finbot-ooda:83` really does imp...
- curator: Bump-level surface is inert here: no `.changeset/` directory, and every workspace package is `private: true` at `0.0....
- locksmith: `designs/resumable-gardeners.md:57,69` — the session id is a **capability handle**: whoever holds `<sid>` plus the ...
- locksmith: `designs/resumable-gardeners.md:106,260` — "**Same-host only**" is load-bearing containment here (it is why the doc...
- locksmith: `gardener-claude.sh:124-127` resumes on **base identity alone**, unattenuated by role or authorization. A reaped ferr...
- locksmith: `designs/resumable-gardeners.md:253` — recording the scratch worktree path in the work record publishes the on-disk...
- locksmith: Approach 3's idempotency guards (`pr_exists_for_branch`, `comment_already_posted`) are capability-neutral and land in...
- locksmith: The doc is 5 weeks stale against the code it describes (PR is CLOSED upstream); if revived, §2 needs a re-sense pass...
- migrator: **`bin/finbot-ooda:212` — `--warmup`/`--fit-window` validation is UNCONDITIONAL, breaking the gate-OFF CLI contract...
- migrator: **`packages/pipeline/auditor.js:108` — the type-scoped knob guard also rejects numeric-string knobs, which previous...
- migrator: **`packages/pipeline/agent-tools.js:250` — the model-facing `config` schema still does not state that knobs must be...
