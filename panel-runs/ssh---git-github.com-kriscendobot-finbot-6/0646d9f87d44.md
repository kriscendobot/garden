---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 2
disposition: error
must_fix_total: 40
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0646d9f87d44
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **error** after **2** round(s).

## Round 1 — head `76bffd40`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #6 (`feat/forecast-data-sufficiency`)
- assessor: **[must-fix]** `packages/pipeline/ooda-cycle.js:94,102` — the new `??`/`validTickCount` coercion silently changes `...
- assessor: **[should-fix]** `bin/finbot-ooda:212` — the `--fit-window`/`--warmup` validation loop is unconditional, so `--warm...
- assessor: **[should-fix]** `packages/pipeline/forecaster.js:835` — the balances filter drops `Number.isFinite`, which its sib...
- assessor: **[should-fix]** `packages/pipeline/auditor.js:838` vs `forecaster.js:647` — the documented bare-count overload of ...
- assessor: **[comment-only]** `packages/pipeline/auditor.js:689` — the forecast-horizon check precedes the descriptor-presence...
- assessor: On my first pass `packages/pipeline/test/executor.test.js` "an armed gate completes NO steps under a forged data-suff...
- assessor: `--horizon=abc` with the gate off completes a dry-run step under a `NaN` horizon. Pre-existing on `main`, untouched b...
- assessor: `Object.freeze` on `fit`/`fit.assets`/per-asset records is safe: no writer to `volFit.*` exists outside tests, so the...
- breaker: **must-fix** — `packages/pipeline/auditor.js:100-111`. The claimed invariant is *"a malformed knob can never degrad...
- breaker: **should-fix** — `packages/pipeline/auditor.js:797-805` and the design note's *"tampered at rest or in flight … f...
- breaker: The producer's own-data discipline stops at `project()`'s asset union (`forecaster.js:660-668` reads `targetWeights` ...
- changeset-auditor: **Stale body contradicts the diff it describes — `designs/ensemble-forecasting.md:1110`.** The trailing "Still outs...
- changeset-auditor: **Process commentary in the durable note — `designs/ensemble-forecasting.md:1012`.** "the forged-descriptor path **...
- changeset-auditor: **Reconciliation inventory is incomplete — `designs/ensemble-forecasting.md:1100-1108`.** The "Reconciled in this c...
- changeset-auditor: **Cite-back to `packager` (overlapping surface).** Five commits carry the byte-identical subject `fix(pipeline): addr...
- curator: `packages/pipeline/index.js:37` — `MAX_LABEL_CODE_POINTS` is promoted to the `.` entry point but has **no consumer ...
- curator: `bin/finbot-ooda:297,305,349,357` — the CLI re-expresses arming inline as `args.dataSufficiencyMin > 0` while impor...
- curator: `packages/pipeline/ooda-cycle.js:51` — `@param {object} [input.config]` still names only `windowTicks` / `fitWindow...
- curator: Surface delta is **purely additive**: new exports `round12`, `computeDataSufficiency` (forecaster), `sanitizeLabel`, ...

## Round 2 — head `76bffd40`

seat verdicts (18): archivist=must-fix assessor=must-fix benchmarker=comment breaker=comment curator=must-fix engine-realist=comment integrator=comment locksmith=must-fix migrator=must-fix packager=must-fix prover=comment purist=comment saboteur=comment spec-keeper=comment stylist=must-fix typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: `AGENT.md:25`: "it bounds forgery rather than provenance" — the r2 wording ("What it bounds is FORGERY, not provena...
- archivist: `designs/ensemble-forecasting.md:1110`: "Still outstanding: the descriptor is not bound to the projection the proposa...
- assessor: **must-fix** — `packages/pipeline/auditor.js:107-115`. `dataSufficiencyMinCoverage` is the one safety knob read out...
- assessor: **should-fix** — `packages/pipeline/ooda-cycle.js:94,102`. `config.windowTicks ?? 10` replaces `|| 10`, so **with t...
- assessor: `packages/pipeline/forecaster.js:~815` — `project()` builds its asset set with bare `Object.entries(input.targetWei...
- assessor: The balance filter accepts non-finite balances (`typeof b === 'number' && b !== 0` admits `NaN`). Fail-closed in dire...
- curator: `packages/pipeline/index.js:30-38` promotes `MAX_LABEL_CODE_POINTS` to the package entry point, but it fails the prom...
- curator: Bump-level correctness, the curator's secondary axis, is **not applicable** here: the repo has no `.changeset` direct...
- curator: The cross-package option-type norm is **satisfied**: `DataSufficiency` is declared once, at `packages/pipeline/foreca...
- curator: Minor, and strictly documentary: `auditor.js:682` names `DataSufficiency` bare in JSDoc without an `import('./forecas...
- locksmith: `packages/pipeline/agent-tools.js:249` (handler `:265`) — the new gate's arming knob is supplied by the party being...
- locksmith: `packages/pipeline/agent-tools.js:158` (handler `:194`, `auditConfig: args.config || {}`) — same inversion on the *...
- locksmith: `packages/pipeline/agent-tools.js:244` tells the caller to "cite its projectionId in the proposal", but nothing in `A...
- locksmith: The six promotions in `packages/pipeline/index.js` (`round12`, `sanitizeLabel`, `MAX_LABEL_CODE_POINTS`, `formatCover...
- locksmith: The in-process split-view residual in `recomputeProjectionId` (a Proxy disagreeing between `getOwnPropertyDescriptor`...
- migrator: **must-fix** — `runOodaCycle` changes the observed window for prior callers **with the gate off**. `origin/main:ood...
- migrator: **should-fix** — `audit()`'s **config** contract narrowed silently and is undocumented on the exported surface. `au...
- migrator: Bump level / peer-dep cascade: **N/A**, cleanly. `@finbot/pipeline` is `private`, `0.0.0`, workspace-only; no `.chang...
- migrator: `bin/finbot-ooda:212` validates `--warmup`/`--fit-window` unconditionally while its sibling `--horizon` / `--regime-h...
- migrator: One non-reproducing failure of `auditor-data-sufficiency.test.js` under concurrent load; 31/31 pass on re-run and the...
