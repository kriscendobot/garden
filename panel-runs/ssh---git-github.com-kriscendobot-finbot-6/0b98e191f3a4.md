---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0b98e191f3a4
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `f43b20e6`

seat verdicts (28): archivist=must-fix assessor=comment benchmarker=comment breaker=must-fix changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=comment fast-checker=comment gateway=comment integrator=comment locksmith=must-fix migrator=comment packager=must-fix prover=comment pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=comment transplanter=comment typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: ## archivist — PR #6 (kriscendobot/finbot, round 5)
- breaker: `cash: 5000` → **rejected**, floor 5600.
- breaker: `cash: "5000"` (plain JSON string) → **approved**, floor 1600, no invariant emitted.
- breaker: `cash: null` → **approved**, floor 1600.
- breaker: inherited `cash` (`Object.create({cash:5000})`) → **approved**; the *same* hostile shape on a config knob (`Object....
- breaker: Invariant 7 held under every attack I brought — arming/OFF, unusable threshold, missing descriptor, count self-refu...
- breaker: SES `lockdown()` is active via `cap-attenuation.js`, so `Object.prototype` pollution is already blocked in-process �...
- curator: `packages/pipeline/index.js:35-45` promotes `MAX_LABEL_CODE_POINTS` to the root entry point on a ground **no consumer...
- curator: **No test imports the entry point.** `grep` over `packages/` and `bin/` finds zero `from '../index.js'` / `from '@fin...
- curator: The promotion prose reads "promoted"/"NOT promoted" as if it decided public-ness. `./forecaster` and `./auditor` are ...
- curator: Clean on the axes I own: no export removed, renamed, or retyped; `project`/`audit`/`runOodaCycle`/`execute` keep thei...
- curator: Bump-level axis is vacuous here: `@finbot/pipeline` is `private: true` at `0.0.0` and the repo has no `.changeset/` d...
- locksmith: `bin/finbot-ooda:297,305,349,357,438` — the arming decision (including line 305, which decides whether the threshol...
- locksmith: `packages/pipeline/auditor.js:742` — the disclosed split-view residual has a half that needs no Proxy: a descriptor...
- locksmith: Cleared, no new grant: `round12`, `sanitizeLabel`, `MAX_LABEL_CODE_POINTS`, `formatCoverage`, `coverageThresholdUsabl...
- locksmith: `packages/pipeline/executor.js:41-44` advertises the fail-closed re-audit without repeating the provenance binding's ...
- packager: **must-fix.** Five commits carry a byte-identical, body-less subject, `fix(pipeline): address panel must-fix items on...
- packager: **must-fix.** Six test files are named for the review round that produced them: `packages/pipeline/test/panel-r2-hard...
- packager: **should-fix.** The PR body is stale against the diff. Its § "Round-2 panel hardening" is the last round described, ...
- packager: Clean on the axes I check hardest: no lockfile or generated-file churn, no drive-by lint sweep, and `f235f94` (`style...
