---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a62771560946
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `fdcefd16`

seat verdicts (28): archivist=must-fix assessor=pass benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=pass coverage-auditor=must-fix curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=pass locksmith=must-fix migrator=must-fix packager=pass prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=must-fix typist=comment warden=must-fix wire-watcher=pass
must-fix items (20):
- archivist: The new gate accepts a self-consistent, caller-supplied descriptor without binding it to the cited forecast artifact,...
- benchmarker: `priceFramesForCoverage()` materializes every `fitReadings` entry before `computeDataSufficiency()` applies its 1,000...
- breaker: The new gate accepts any self-consistent caller-supplied descriptor and is not bound to the forecast cited by the pro...
- changeset-auditor: `--warmup=0` is accepted under an armed gate, but `runOodaCycle` converts it to 10 via `config.windowTicks || 10`; th...
- coverage-auditor: Add an executor-level test proving an armed `dataSufficiencyMinCoverage` rejects a forecast without a valid descripto...
- curator: Update the canonical pseudocode to compare quantized recomputed and reported coverage (and clarify the reported ratio...
- engine-realist: The new gate accepts a self-constructed, internally consistent `forecast.dataSufficiency` object and does not bind it...
- fast-checker: `--data-sufficiency-min=0e1` (and other zero literals with a nonzero exponent) is rejected because the underflow chec...
- fast-checker: When the coverage gate is armed, an invalid or negative `--regime-horizon-stretch` can silently suppress the adaptive...
- gateway: `worstAssetPersistence()` reads `volFit.assets` and each `st.persistence` through normal property access, executing a...
- locksmith: The data-sufficiency gate accepts a caller-supplied, internally consistent descriptor without binding it to the propo...
- migrator: Findings:
- prover: The data-sufficiency gate accepts a hand-built, internally consistent `forecast.dataSufficiency` object without bindi...
- pruner: Remove `MAX_LABEL_CODE_POINTS` from the public package exports: no production consumer uses it, so this adds an unnec...
- purist: The new safety invariant accepts a caller-supplied forecast/descriptor without binding it to `proposal.cited_forecast...
- releaser: The new gate accepts a caller-supplied descriptor and only checks its internal arithmetic; a forged `{historyReturns,...
- saboteur: The coverage measurement discards reading timestamps and counts duplicate/nonconsecutive frames as adjacent returns. ...
- saboteur: The auditor accepts any internally consistent caller-supplied descriptor and never binds it to the proposal’s cited...
- spec-keeper: The gate accepts any self-consistent caller-supplied descriptor without binding it to `proposal.cited_forecasts`; a h...
- stylist: The `--data-sufficiency-min` help text is a dense 10-line implementation essay, making the CLI’s flag reference har...
