---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 6
disposition: seat-error
must_fix_total: 120
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 964af85defb1
recorded_by: endolin-garden-ece02cb4
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **seat-error** after **6** round(s).

## Round 1 — head `ffe7df67`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=comment transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: Reviewed the committed PR diff (`origin/main...HEAD`, 7 files) plus the docs it should have touched.
- assessor: ## Juror: assessor — PR #6 (`feat(pipeline): name and gate forecast data-sufficiency`)
- breaker: All four attacks I ran reproduce. Here is my per-juror block.
- changeset-auditor: ## Juror block — seat: `changeset-auditor` — PR #6
- corner-prober: All 10 new tests pass; findings below are gaps, not regressions. Everything asserted here was executed against the br...
- curator: **must-fix — one identifier, two thresholds.** `dataSufficiencyMinCoverage` names the *scarce-flag* threshold on th...
- curator: **comment-only — `--data-sufficiency-min=0` is half-on.** `bin/finbot-ooda:163` keys the report off `!= null`; `aud...
- curator: **comment-only — fail-open knob for direct callers.** `audit()` with `dataSufficiencyMinCoverage: 0.8` and a foreca...
- curator: **comment-only — union param coercion.** `computeDataSufficiency({frames})` accepts `Array|number` (`forecaster.js:...
- engine-realist: ## Juror: engine-realist — PR #6 (feat(pipeline): name and gate forecast data-sufficiency)
- fast-checker: ## Juror: fast-checker — PR #6 (`feat(pipeline): name and gate forecast data-sufficiency`)
- integrator: **[must-fix] `dataSufficiencyMinCoverage` names two different thresholds with two different defaults.** `packages/pip...
- integrator: **[should-fix] "Resolved" overstates what landed.** The open question prescribed "let the **planner** downweight"; `p...
- integrator: **[should-fix] New design-doc convention without precedent.** `designs/ensemble-forecasting.md:67` introduces `~~stri...
- integrator: **[comment-only] Policy leaks into a content-identity artifact.** `forecaster.js:438` folds `dataSufficiency` — inc...
- integrator: **[comment-only] `test/auditor-data-sufficiency.test.js:120` guards the "bites" assertions behind `if (result.audit)`...
- integrator: Good integration: `ooda-cycle.js:225` → `executor.js:85` threads `auditConfig`, so fire-time re-verification picks ...
- integrator: Title reads cleanly as a merge commit. The `## Governance` section ("Draft. Does not self-merge.") is transient proce...
- integrator: Verified locally: the 10 new tests pass (`node --test` in `packages/pipeline`).
- locksmith: `packages/pipeline/executor.js:76-86` — the fire-time drift guard, the *last* gate before `caps.wallet` is reachabl...

## Round 2 — head `8cf30199`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## archivist — PR #6 (forecast data-sufficiency)
- assessor: Verified the diff by reading it, running the suite (`npm test` green), and probing the gate directly.
- breaker: **should-fix — the armed invariant can vanish at fire time.** `executor.js:85` re-audits with `config.auditConfig |...
- breaker: **should-fix — coverage is scoped to `targetWeights`, not the projected set** (`forecaster.js:433`). Confirmed: wit...
- breaker: **comment-only — three descriptor overstatements.** A *flapping* feed (5 alternating frames of 10) reads identicall...
- changeset-auditor: Cleaned up. Here is my per-juror block.
- corner-prober: **`packages/pipeline/auditor.js:69,205` — a threshold that *coerces* to 0 silently disarms the gate. `must-fix-loop...
- corner-prober: **`auditor.js:217` — the absolute `1e-12` slack swallows thresholds ≤ 1e-12.** A forecast with **zero** observed ...
- corner-prober: **`auditor.js:207` — `Number()` coercion admits non-number evidence.** `{coverageRatio: '3'}` and `{coverageRatio: ...
- corner-prober: **`forecaster.js:computeDataSufficiency` — empty/singleton window unpinned.** `frames: []` → coverage 0 with `wor...
- corner-prober: **Bare-count overload silently drops `assets`** (`{frames: 8, assets: ['ATOM','NEW']}` → `worstAsset: null`, portfo...
- corner-prober: Cosmetic: `worstAsset: ''` renders `coverage 3.000 on (…)`; a non-integral `--horizon` rides into the ratio denomin...
- curator: **must-fix — the auditor's config bag is re-declared in the LLM-facing tool schemas and is now stale.** `packages/p...
- curator: **should-fix — new public export carries an untyped-union overload with no in-repo producer.** `packages/pipeline/f...
- curator: **should-fix — package README's invariant inventory is stale.** `packages/pipeline/README.md:31` lists the auditor ...
- curator: `computeDataSufficiency` is subpath-only (`@finbot/pipeline/forecaster`), not re-exported from `index.js` — consist...
- curator: The `horizonRegime` doc change (`forecaster.js:315`) from `@property {object} [horizonRegime]` to `{object|null}` **c...
- curator: New invariant name `forecast-data-sufficiency` matches the kebab-case convention of its siblings; `failed_invariants`...
- engine-realist: ## engine-realist — PR #6 (`feat/forecast-data-sufficiency`)
- fast-checker: **`packages/pipeline/auditor.js:207` — the fail-closed claim is false for coercible descriptors, and the example se...

## Round 3 — head `a5a6a20a`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #6 (`feat/forecast-data-sufficiency`)
- assessor: **`packages/pipeline/auditor.js:275,330,346` — the fail-closed gate fails OPEN when the forecast's own horizon is u...
- assessor: **`packages/pipeline/forecaster.js:337` — the descriptor is normalized into contradicting the projection carrying i...
- assessor: **`bin/finbot-ooda:79` — `--horizon` is unvalidated while `--data-sufficiency-min` is validated at :121.** The hori...
- assessor: Empty `targetWeights` falls back to portfolio-wide counting (any one own finite price counts the frame) — the most ...
- assessor: Default `windowTicks=10` / `horizon=20` caps coverage at 0.45, so `--data-sufficiency-min=1` rejects every default ru...
- benchmarker: **`must-fix-loop` — the PR body inverts the shipped safety semantics.** The body states *"A legacy forecast lacking...
- benchmarker: **`summary-fix` — "10 new tests" is stale.** Actual: 25 (13 forecaster + 12 auditor). It was accurate at `ffe7df6` ...
- benchmarker: **`summary-fix` — "default cycle unchanged" is narrower than it reads.** True for the report line; `--json` gains `...
- breaker: Reviewed the diff (`forecaster.js`, `auditor.js`, `ooda-cycle.js`, `bin/finbot-ooda`, tests, design note) against `or...
- changeset-auditor: **Inverted safety semantics — must-fix.** Body: *"A legacy forecast lacking the descriptor passes vacuously."* The ...
- changeset-auditor: **Missed surface — summary-fix.** The body never names **worst-constituent per-asset coverage** / `worstAsset`, the...
- changeset-auditor: **Count drift — comment.** "10 new tests"; actual is 25 (13 forecaster + 12 auditor).
- corner-prober: ## Juror: corner-prober — PR #6 (`feat/forecast-data-sufficiency`)
- curator: ## curator — PR #6 (finbot, `feat/forecast-data-sufficiency`)
- engine-realist: `packages/pipeline/auditor.js:~245` — `round12` is a hand-copy of `forecaster.js:103`. The gate's entire fail-close...
- engine-realist: `packages/pipeline/forecaster.js` (`countHistoryFrames`, ~line 260): the no-assets fallback uses `Object.keys` (enume...
- engine-realist: Allocation budget, `countHistoryFrames` named path: `frames.map(...)` materializes one boolean array **per asset** be...
- engine-realist: Engine-variance, **positive**: the worst-constituent tie-break uses `asset < worst.worstAsset` (UTF-16 code-unit orde...
- fast-checker: Read the brief, the full diff, and fuzzed the new surface with a hand-rolled generator (no `fast-check` in the tree)....

## Round 4 — head `6c9b13ad`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #6
- assessor: **should-fix** — `bin/finbot-ooda:84`. The PR validates `--horizon` because it is the gate ratio's denominator, but...
- assessor: **should-fix** — `packages/pipeline/auditor.js`, `readDataSufficiency` null path. A forecast carrying a fully valid...
- assessor: **should-fix** — `packages/pipeline/forecaster.js:349-350`. The `DataSufficiency` typedef says `historyFrames` coun...
- assessor: `bin/finbot-ooda:350` interpolates `dataSufficiency.worstAsset` raw, while `packages/pipeline/auditor.js` routes the ...
- assessor: `worstAssetPersistence` reads `st.persistence` by plain `[[Get]]` on a caller-supplied `volFit`; a hostile accessor t...
- assessor: `--horizon=0` now exits 2 unconditionally, even with the gate off. Deliberate and documented; flagging for the migrat...
- benchmarker: **The Verification section is stale by three commits.** It reports "10 new tests" across two files; the branch carrie...
- benchmarker: **No optimization is open anywhere on this PR.** Four commit messages, the PR body, and the design doc's new *Notes f...
- benchmarker: **No cost claim is owed.** `computeDataSufficiency` walks the window once per projected asset with a descriptor probe...
- benchmarker: **This seat's own cited rules do not exist.** `roles/jurors/benchmarker/AGENT.md § Cite the rule` names `skills/benc...
- benchmarker: finbot has no benchmark surface at all: no `BENCH.md`, no `npm run bench`, no perf test. Worth knowing before a futur...
- breaker: Probes complete. Here is my per-juror block.
- corner-prober: `packages/pipeline/auditor.js:349` — the same function iterates by **code point** to scrub but truncates by **UTF-1...
- corner-prober: `packages/pipeline/auditor.js:91` + `bin/finbot-ooda` error text — the real usable/unusable boundary is **5e-13**, ...
- corner-prober: `packages/pipeline/forecaster.js:324` — a malformed `assets` (`'ATOM'` as a bare string, or `[42]`) is not an empty...
- corner-prober: `bin/finbot-ooda` `printReport`'s `scarce` fallback (the `res.audit`-absent branch, and the `UNMEASURABLE` label) has...
- corner-prober: `--horizon` is validated whole and `>= 1` but has no upper bound: `--horizon=9007199254740992` is accepted and hangs....
- curator: **`packages/pipeline/forecaster.js:116` — `round12` is promoted from module-private to a package entry point (`expo...
- curator: **The `DataSufficiency` shape is declared three times across the modules this PR touches.** Canonical at `forecaster....

## Round 5 — head `4940fa52`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix** — `bin/finbot-ooda:161-165`: the `--horizon` validation comment says "a zero-tick projection is the on...
- archivist: **must-fix** — `bin/finbot-ooda:31-33` and `:46-49`: both call `--horizon` "the denominator of the coverage ratio" ...
- archivist: **should-fix** — `packages/pipeline/forecaster.js:405-407` (and the same example at `:374-381`): the JSDoc lists "`...
- archivist: `designs/ensemble-forecasting.md` frontmatter still carries `status: stub` on a 1040-line document with sixteen field...
- archivist: Cross-references verified: the open-question pointer at `designs/ensemble-forecasting.md:67` resolves to the `## Note...
- assessor: **should-fix — `bin/finbot-ooda:163` asserts an exemption the code refuses.** The `--horizon` validation is justifi...
- assessor: **should-fix — `bin/finbot-ooda:392` prints coverage at a resolution the PR's own reasoning calls a lie.** `formatC...
- assessor: **should-fix — `forecaster.js:165` changes a hashed field with no test.** The new lexicographic tie-break in `worst...
- assessor: **comment-only — `auditor.js:532` disagrees with its own sibling.** `labelOr` treats `''` as absent; `onAsset` uses...
- breaker: ## breaker — PR #6 (`feat/forecast-data-sufficiency`)
- changeset-auditor: **The Orthogonality claim is now false, in the precise direction it was written to deny.** The body asserts "**no ove...
- changeset-auditor: **File inventory is stale.** "Touches only `forecaster.js`, `auditor.js`, `ooda-cycle.js`, `bin/finbot-ooda`, the des...
- changeset-auditor: **Test count understated.** "10 new tests" vs **50** top-level `test()` calls (20 + 23 + 7). [rule: same §]
- changeset-auditor: **An undisclosed governance change rides in a feature PR.** `CLAUDE.md` § Conventions is rewritten from "No PR workf...
- changeset-auditor: The working tree is **dirty**: an uncommitted edit to `packages/pipeline/forecaster.js` re-inlines `safeLength(frames...
- corner-prober: I've verified each finding empirically. Here is my per-juror block.
- curator: `packages/pipeline/index.js:22` promotes `computeDataSufficiency` to the package entry point with **no consumer**: th...
- curator: The threshold-usability/arming contract is declared **three times**. `packages/pipeline/auditor.js:98-102` is canonic...
- curator: `sanitizeLabel` (`packages/pipeline/auditor.js:379`, entry point at `index.js:30`) ships an observable truncation con...
- curator: No `.changeset/`; every package is `private: true` at `0.0.0`, so the curator's bump-level overlap is unenforceable h...

## Round 6 — head `21ae87af`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=comment pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=pass typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: **must-fix** — `skills/pre-execution-audit/SKILL.md:181` now declares this section canonical ("The configured floor...
- archivist: **should-fix** — `skills/pre-execution-audit/SKILL.md:191`: "a positive one **below the descriptor's 1e-12 resoluti...
- archivist: **should-fix** — `packages/pipeline/auditor.js:69`: "One read per knob, uniformly — **the same discipline the for...
- archivist: `designs/ensemble-forecasting.md:1011` says `--fit-window` and `--warmup` are validated "on the same footing as `--ho...
- archivist: Cross-references all resolve: `roles/auditor/AGENT.md` → `../../skills/pre-execution-audit/SKILL.md § 7`, the desi...
- archivist: `CLAUDE.md:150` correctly reconciles the "no PR workflows" convention with `packages/` PRs; no other doc still assert...
- assessor: **must-fix** — `packages/pipeline/test/auditor-data-sufficiency.test.js:791-799`: the loop enumerates hostile shape...
- assessor: `packages/pipeline/forecaster.js:490-522`: `measureHistoryCoverage` walks frames once per named asset, so the work is...
- breaker: Worktree restored to as-found state (dirty `forecaster.js`, stash cleared). Auditor-side tests: 40/40 pass.
- changeset-auditor: `MAX_LABEL_CODE_POINTS` — `bin/finbot-ooda` never imports or mentions it. Its only consumer is `test/auditor-data-s...
- changeset-auditor: `coverageGateArmed` — also absent from `bin/finbot-ooda`. Its only consumer is `packages/pipeline/ooda-cycle.js:24`...
- curator: `packages/pipeline/forecaster.js:54` — `fitForecastWorld`'s `@returns {{ world, fit: object|null }}` is unchanged, ...
- curator: `packages/pipeline/executor.js:51` — the auditor's config bag is now enumerated in four independent places: the `@p...
- curator: `packages/pipeline/index.js:20` — the comment justifies withholding `computeDataSufficiency` because it "has no con...
- curator: Bump-level axis is inapplicable, not omitted: no `.changeset` directory exists and `@finbot/pipeline` is `private: tr...
- curator: Passes worth recording: `DataSufficiency` is declared once (`forecaster.js:403`) and pulled into the auditor by `@imp...
- engine-realist: Reviewed the diff (`origin/main...HEAD`, 19 files) plus the three consumer paths (`ooda-cycle.js`, `bin/finbot-ooda`,...
- fast-checker: **`packages/pipeline/forecaster.js:375` — uncommitted edit drops the length bound and hangs the suite.** The workin...
- fast-checker: **`auditor.js:439` `sanitizeLabel` claims a hard `forall` over untrusted strings; the tests spot-check ~4.** The docs...
- fast-checker: **The producer/consumer pair is an equivalent-implementations property and is untested as one.** `computeDataSufficie...
