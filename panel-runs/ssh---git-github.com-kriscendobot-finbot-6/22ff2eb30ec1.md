---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 6
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 22ff2eb30ec1
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #6 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `bdc96c1b`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=pass gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=comment releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=pass typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Per-juror block — archivist, PR #6
- assessor: **[should-fix]** `packages/pipeline/ooda-cycle.js:95-98` — the `coverageGateOn && windowTicks === 0 ? []` branch is...
- assessor: **[should-fix]** `packages/pipeline/auditor.js:78-85` — rewriting the nine knob reads to `readOwn(config, …)` mak...
- assessor: `bin/finbot-ooda:297,305,349,357,438` — five local `args.dataSufficiencyMin > 0` tests where the file's own import ...
- assessor: `packages/pipeline/forecaster.js:835` — the balances filter omits the `Number.isFinite` its sibling `targetWeights`...
- breaker: `packages/pipeline/forecaster.js:796`, `:60`, `:84` — **the fail-closed coverage gate is defeated at the producer.*...
- breaker: `packages/pipeline/forecaster.js:441` vs `auditor.js` gate — the documented **bare-count overload** of `computeData...
- breaker: `forecaster.js:665` — the `balances` filter accepts a non-finite balance (`typeof x === 'number' && x !== 0` admits...
- corner-prober: Reviewed the diff at `bdc96c1` vs `origin/main` in the panel worktree, probing boundaries empirically.
- curator: **must-fix.** `packages/pipeline/index.js:37` promotes `MAX_LABEL_CODE_POINTS` to the package entry point, but it has...
- curator: comment-only. The `audit()` config field set is hand-enumerated in three places: the JSDoc at `auditor.js:50-63` and ...
- curator: Verified clean, no action: the `exports` map in `packages/pipeline/package.json` needs no change (the PR adds no new ...
- curator: My secondary bump-level axis is inapplicable to this repo: every package is `private: true` at version `0.0.0` and th...
- engine-realist: `packages/pipeline/auditor.js:391-395` — the one unbounded walk in an otherwise carefully bounded gate (4096 citati...
- engine-realist: `packages/pipeline/auditor.js:23-24` — comment-only. The capture of `Object.getOwnPropertyDescriptor`/`Object.hasOw...
- engine-realist: `packages/pipeline/ooda-cycle.js:95` — the comment justifying the zero special-case is false for the function it na...
- engine-realist: CI pins node 22 while `engines` declares `>=20`; the new tests are the repo's first consumers of `String.prototype.is...
- gateway: `skills/pre-execution-audit/SKILL.md:190-199` — the canonical risk-bound defaults are rewritten in this PR: `per_st...
- gateway: **PR body § Orthogonality is materially false**, and falsely in the direction that hides the above: it claims the PR...
- gateway: `CLAUDE.md:150` rewrites the repo's own contribution governance ("No PR workflows for finbot's own repo" → PRs requ...
