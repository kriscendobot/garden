---
kind: panel-run
repo: kriscendobot/minion.town
pr: 99
panel_kind: code
base_ref: b83741a3543cc598cdd7fe513243c35df24ba5db
rounds: 1
disposition: must-fix
must_fix_total: 17
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 230b0685161e
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #99 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `2484775b`

seat verdicts (30): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=comment integrator=comment locksmith=comment migrator=comment orthographer=pass packager=pass prover=pass pruner=pass purist=must-fix releaser=pass saboteur=comment scribe=pass spec-keeper=comment stylist=must-fix surfacer=pass transplanter=must-fix typist=comment warden=pass wire-watcher=must-fix
must-fix items (17):
- assessor: `tools/claude-harness/{install,lib,refresh,check}.mjs`: control flow there is sound — `validateRelease`/`checkPinCo...
- assessor: Test file correctness/coverage (prover's remit), types (typist's remit), and CI workflow YAML structure are out of sc...
- breaker: **Docker/App Runner runtime path lets the running process overwrite the "integrity-checked" binary it just installed,...
- breaker: `refresh.mjs`'s `expectedFingerprint` (used to gate the GPG signature check) and the `signingKeyFingerprint` literal ...
- corner-prober: `install.mjs`'s network path — `download()` (fetch with `redirect: 'error'`, `response.ok` check, `response.body` n...
- corner-prober: `refresh.mjs` has zero tests (no `refresh.test.mjs`), yet it is the only place that validates the Anthropic GPG signa...
- corner-prober: `validateRelease`'s `buildTime` check is regex-shape (`\d{4}-\d{2}-\d{2}T...`) plus `Date.parse` non-NaN, but `Date.p...
- corner-prober: `install()`'s final `rename(pendingPath, outputPath)` — two concurrent `install()` calls targeting the same `--outp...
- engine-realist: ## Per-juror block — engine-realist — PR #99 (kriscendobot/minion.town)
- purist: `tools/claude-harness/refresh.mjs:64-65` converts a file URL to a native path via `keyPath.pathname` instead of `file...
- purist: `lib.mjs:1` carries `// prefer-endo-primitives-exempt: bootstrap verifier has no runtime dependencies.` — good, thi...
- purist: The Anthropic signing-key fingerprint is hardcoded in two different string shapes (`lib.mjs`'s spaced `'31DD DE24 ......
- stylist: `deploy/aws/scripts/deploy-app.sh:84` — `CLAUDE_RELEASE_B64` is a freshly-authored name with a bare abbreviation (`...
- stylist: `deploy/aws/scripts/deploy-app.sh:188,190` — the deployment-receipt JSON uses two different words for the same "val...
- stylist: Otherwise this diff is unusually clean on naming: `tools/claude-harness/*` consistently spells out every identifier i...
- transplanter: ## Findings
- wire-watcher: # wire-watcher review — PR #99 (kriscendobot/minion.town)
