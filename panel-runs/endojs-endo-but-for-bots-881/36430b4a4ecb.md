---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 881
panel_kind: code
base_ref: origin/build-endo-google-sheets-client
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 36430b4a4ecb
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #881 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `f7d47328`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=pass integrator=pass locksmith=pass migrator=comment packager=comment prover=must-fix pruner=pass purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix** — `designs/exo-google-sheets.md:269-272` (new prose in this PR) claims: *"whole-row, whole-column, and...
- archivist: `part()`/`sheet()`/`range()` interface additions in `designs/exo-google-sheets.md` match `facets.js`/`interfaces.js`/...
- archivist: `follow()` → `PassableReader`/`@endo/exo-stream` prose matches `interfaces.js` (`M.remotable('PassableReader')`) an...
- archivist: `revokeWrites()`/`revoke()`, `setMaxRequestsPerMinute`, and the removal of `setReadOnly` are consistently documented ...
- archivist: No banner-comment violations found in the new files.
- archivist: No new "see X" cross-references were added whose targets fail to resolve.
- assessor: `packages/exo-google-sheets/src/facets.js:133-134` — `sheets()` unconditionally dereferences `properties.gridProper...
- assessor: `packages/exo-google-sheets/src/powers.js:` in `confine()`, the cap check (`limits.boundRange`) runs before the allow...
- breaker: `designs/exo-google-sheets.md:118-119` / `packages/exo-google-sheets/src/a1.js:120-123` claim the *only* tab-name/A1 ...
- breaker: `designs/exo-google-sheets.md:112-115` explicitly promises "whole-row, whole-column, and named ranges remain valid se...
- breaker: `designs/exo-google-sheets.md:335-338` states `readBatch`/`writeBatch` "map to `values.batchGet` / `values.batchUpdat...
- breaker: Capability-overlap check: `narrowScope`'s sheet/range containment checks (`powers.js:106-134`) and `confine`'s parall...
- corner-prober: `a1.js` `columnNumber`: row overflow past `Number.MAX_SAFE_INTEGER` is tested (`A${MAX_SAFE_INTEGER+1}` → `undefine...
- corner-prober: `powers.js` `limits.boundRange`/`boundCells`: the cap is only tested one cell *over* (`maxCellsPerRead:4` against 6 c...
- corner-prober: `powers.js` `charge()`: the only exercised path is "spend all N starting tokens, N+1th throws" (`maxRequestsPerMinute...
- corner-prober: `facets.js` `readRecords`: no test for (a) an empty result set (`values: []` → headers default to `[]`, returns `[]...
- corner-prober: `facets.js` `makeFollower`: the async iterator closes over shared mutable `target`/`prior`/`done` with no reentrancy ...
- corner-prober: `powers.js` `narrowScope`/range containment: reflexive/transitive containment is fuzz-tested for `contains()` directl...
- engine-realist: `packages/exo-google-sheets/src/facets.js:87-99` — `makeFollower`'s poll loop lets an uncaught throttle exception (...
- engine-realist: `packages/exo-google-sheets/src/facets.js:92` — the revision check does `JSON.stringify(values)` on the full read r...
