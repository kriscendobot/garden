---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 881
panel_kind: code
base_ref: origin/build-endo-google-sheets-client
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b696eb1e5b5b
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #881 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `193083a3`

seat verdicts (29): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=comment fast-checker=must-fix gateway=comment integrator=pass locksmith=must-fix migrator=comment packager=pass prover=must-fix pruner=pass purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=comment stylist=pass surfacer=must-fix transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- assessor: `title()`/`sheets()` metadata methods answer against the whole document (not filtered by range scope) — plausible b...
- assessor: `makeFollower`'s async-iterator loop, `Promise.all` batch methods, and the token-bucket throttle's charge/refill orde...
- breaker: **Must-fix — a facet narrowed by bare `range()`/`part(<range>)` (no sheet ever named) confines the rectangle but no...
- breaker: Capability-attack overlap (locksmith axis): `makeFollower`'s `unscoped()` always returns the read-powers family's roo...
- changeset-auditor: **`.changeset/calm-sheets-facets.md`** — the body violates sentence-per-line style: two sentences are hard-wrapped ...
- changeset-auditor: **Package-set coherence**: front-matter lists only `@endo/exo-google-sheets`; the diff touches only that package's fi...
- changeset-auditor: **Bump-level / new-package initial-release shape**: `package.json` version is `0.1.0`, `CHANGELOG.md` is the bare Con...
- changeset-auditor: **Body-vs-diff identifier coherence**: `makeExoSpreadsheet(client)`, the reader/writer/append-only/write-only facets ...
- changeset-auditor: **No process commentary; no stale draft language.**
- changeset-auditor: **Bundling**: one changeset, one package — no unjustified split.
- corner-prober: `packages/exo-google-sheets/src/powers.js:74-98,159-198` (`narrowScope`/`confine`) — the empty string `''` is the b...
- corner-prober: `packages/exo-google-sheets/src/facets.js:65-98` (`makeFollower`) — two concurrent `follower.next()` calls (an "int...
- corner-prober: `readMethods.readRecords` (`facets.js:129-143`) silently pads a short row with `null` (`row[index] ?? null`) and sile...
- fast-checker: `contains(outer, inner)` (`packages/exo-google-sheets/src/a1.js`) is the sole primitive every confinement check (`nar...
- fast-checker: `designs/exo-google-sheets.md` explicitly claims a universally-quantified structural invariant: "The two axes are ort...
- fast-checker: `parseA1`'s corner-normalization (`min`/`max` on the two corners) is checked with one spot example (`'Tasks!Z9:A1'` v...
- fast-checker: `sheetPrefix`/`parseA1` form a round-trip pair (quote a tab name, parse it back, recover the name) checked with a sin...
- fast-checker: The package has no fast-check dependency yet, unlike sibling packages (`patterns`, `marshal`, `pass-style`, `agentry`...
- fast-checker: `narrowScope`'s own claim ("must never replace an axis with a wider... designation") is the same invariant as `contai...
- locksmith: **`packages/exo-google-sheets/src/a1.js:147` (`rangeScope`) + `packages/exo-google-sheets/src/powers.js:159-169` (`co...
