---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 881
panel_kind: code
base_ref: origin/build-endo-google-sheets-client
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 7cd3e4d88511
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #881 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `43a9a3ec`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=must-fix fast-checker=comment gateway=comment integrator=must-fix locksmith=comment migrator=pass packager=must-fix prover=must-fix pruner=pass purist=must-fix releaser=pass saboteur=must-fix scribe=comment spec-keeper=pass stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: **Must-fix — design doc claims a "range-scoped appender" is a valid attenuation, but the implementation rejects it....
- archivist: **Should-fix — README overstates append-only narrowing to match the same false claim.** `packages/exo-google-sheets...
- archivist: **Comment-only — minor grammar slip introduced by this diff.** `designs/exo-google-sheets.md:305`: "a producer hold...
- assessor: `packages/exo-google-sheets/src/powers.js:235-260` (`confine`) — a non-A1 selector that names a *different, real* s...
- assessor: `writeBatch`/`readBatch` (`facets.js:157-158`, `:292-298`) fire every element via `Promise.all` with no fail-fast seq...
- breaker: `packages/exo-google-sheets/src/powers.js:170-175` (`assertAppendFitsRange`) silently no-ops whenever `parseA1(select...
- breaker: `packages/exo-google-sheets/src/powers.js:148-161` (`assertWriteFitsScope`) returns immediately when `!scope.range` (...
- breaker: `a1.js:128-136` (`partScope`): `part('A1')` on a sheet-scoped facet silently resolves to cell A1 of the *current* she...
- breaker: Walked the orthogonality claim ("`writer.part('Tasks').writeOnly()` and `writer.writeOnly().part('Tasks')` land in th...
- corner-prober: ## Findings
- engine-realist: `packages/exo-google-sheets/src/facets.js:102-118` (`makeFollower`'s `next()`) + `packages/exo-google-sheets/src/powe...
- engine-realist: `packages/exo-google-sheets/src/powers.js:204,210` (`pollIntervalMs` validation) and `DEFAULT_POLL_INTERVAL_MS` at li...
- engine-realist: `packages/exo-google-sheets/src/powers.js:216-221` (`charge()`): the token-bucket refill assumes `now()` is monotonic...
- integrator: The head carries 8 separate narrowly-scoped `fix(exo-google-sheets):` commits landed after the initial `feat`/`test` ...
- integrator: `part(designation)` (`packages/exo-google-sheets/src/interfaces.js:20-26`, `src/facets.js`, `designs/exo-google-sheet...
- integrator: Related design #874 (this PR's declared base branch, `build-endo-google-sheets-client`) is open/draft with no changes...
- integrator: Rename sweep from the prior `AsyncIterableIterator`/`setReadOnly()` shapes to `PassableReader`/`revokeWrites()` is cl...
- packager: flips `.changeset/calm-sheets-facets.md` from `minor` → `major` and rewrites its prose, with zero mention in the co...
- packager: deletes 76+ lines of `packages/exo-google-sheets/README.md` — the entire "How the attenuation is arranged" / "Why `...
- prover: `test/exo-google-sheets.test.js:182-189` — the two `appendOnly().append(...)` assertions are supposed to demonstrat...
