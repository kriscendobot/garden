---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 881
panel_kind: code
base_ref: origin/build-endo-google-sheets-client
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: dd62763b87dd
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #881 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `d1f7b1fe`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- assessor: `title()`/`sheets()` are unscoped (`facets.js:107-122`), so a tab-scoped reader still enumerates every tab and its di...
- assessor: `boundCells` caps reads only; writes and appends are uncapped.
- benchmarker: Walked every optimization / resource-cost claim in the PR body, the four review threads, the commit messages, and `de...
- breaker: All probes were run against the worktree and then removed; the tree is clean.
- changeset-auditor: Review complete.
- corner-prober: **`range()` with a sheet-qualified selector does not confine the tab.** `spreadsheet.range('Tasks!A1:C10').read('Othe...
- corner-prober: **An unparseable or unbounded scope fails *open*.** `range('A:C')`, `range('1:5')`, `range('nonsense')`, `range('')` ...
- corner-prober: **Empty-string designation widens.** `sheet('Tasks').sheet('')` → `{...scope, sheet: ''}`, falsy, so the sheet guar...
- corner-prober: **Reversed rectangle escapes.** `range('Tasks!C1:D10').read('D1:A1')` → admitted (`left:4 > right:1` makes `contain...
- corner-prober: **`follow()` cannot cross CapTP.** `E(spreadsheet).follow('A1')` rejects: *Remotables must be explicitly declared*. T...
- corner-prober: **`pollIntervalMs: 0` is swallowed by `||`** (`src/powers.js:88`) → 30000. The PR's own first test passes `pollInte...
- corner-prober: **Clock step-back wedges the throttle.** `charge()` has no `Math.max(0, instant - lastRefill)`; a one-hour `Date.now`...
- corner-prober: **Untested caps/interleavings, summary-fix:** `boundCells` has *zero* tests (add exactly-at-cap passes / cap+1 throws...
- curator: **Initial-release bump is wrong.** `.changeset/calm-sheets-facets.md:2` bumps `'@endo/exo-google-sheets': minor` for ...
- curator: **The package ships no types for its only export.** `src/exo-google-sheets.js:54` takes `@param {any} client` and `:1...
- curator: **The read surface is declared twice.** `src/interfaces.js:33-48` and `:57-72` repeat `title`/`sheets`/`read`/`readBa...
- curator: `package.json:4` sets `"private": true` alongside `publishConfig.access: "public"`, a publish-shaped `files` list, an...
- curator: `index.js` re-exports exactly the public surface of `src/exo-google-sheets.js`, with a single `exports` entry — a s...
- curator: `follow()` returns `M.raw()` — a local async iterator that cannot cross CapTP. The test exercises the rest of the s...
- engine-realist: `packages/exo-google-sheets/src/powers.js:118` — **confinement escape via unparsable A1.** The range-scope check is...
