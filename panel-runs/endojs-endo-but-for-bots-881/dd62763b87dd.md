---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 881
panel_kind: code
base_ref: origin/build-endo-google-sheets-client
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: dd62763b87dd
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #881 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `d1f7b1fe`

seat verdicts (29): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=comment engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=pass surfacer=comment transplanter=pass typist=must-fix warden=pass wire-watcher=must-fix
must-fix items (20):
- assessor: `packages/exo-google-sheets/src/powers.js:100-104` (`narrow`, used by `facets.js`'s `part`/`sheet`/`range` methods): ...
- assessor: `packages/exo-google-sheets/src/powers.js:76-99` (`confine`): the "Range escapes the range scope" check is gated on `...
- breaker: ## Per-juror block — breaker
- changeset-auditor: ## Per-Juror Block — changeset-auditor
- corner-prober: `powers.js` `confine()`'s range-scope check (`scopeRange && parsed && !contains(...)`) is guarded on `parsed` being t...
- corner-prober: `facets.js` `readRecords` builds records via `Object.fromEntries(headers.map(...))`; two columns with the same header...
- corner-prober: `makeFollower`'s change detection is `JSON.stringify(values) !== prior`. `JSON.stringify` renders `0` and `-0` identi...
- corner-prober: `writeBatch` (`facets.js:261`) fires all updates via `Promise.all` with no test for two entries in the same batch tar...
- engine-realist: `packages/exo-google-sheets/src/facets.js:52-62` — `makeFollower`'s `next()` calls `unscoped.read(target)` unguarde...
- engine-realist: `packages/exo-google-sheets/src/facets.js:52-62` — each open `follow()` allocates a fresh `JSON.stringify(values)` ...
- engine-realist: Storage choice is correct as-is: `makeExo` here mints heap-only (non-durable, non-virtual) exos (`facets.js`), which ...
- fast-checker: **`a1.js` has zero direct tests** [proposed-rule: pure powerless parsing/comparison modules get direct property-based...
- fast-checker: **`contains()` reflexivity/transitivity untested** [proposed-rule: order-relation helpers backing a security invarian...
- fast-checker: **`sheetPrefix`/`parseA1` round-trip untested** [proposed-rule: quote/escape pairs get a round-trip property test] �...
- fast-checker: **`must-fix-loop`: `narrow()`'s confinement invariant is the PR's headline security claim and is unverified by a `for...
- fast-checker: **`readRecords()` silently drops duplicate-header data, untested** [rule: skills/adversarial-tests/SKILL.md § corner...
- integrator: **Must-fix — stray unrelated `agentry` commit rides along, undescribed and duplicated by a separate open PR.** The ...
- integrator: **Comment-only — verify #874 stacking note stays accurate at merge time.** The PR body states "Stacks on #874 and m...
- locksmith: `tasksFacet.sheet('Budget')` — directly overwrites `scope.sheet` from `'Tasks'` to `'Budget'`, or
- locksmith: `tasksFacet.part('Budget!A1:Z100')` — `partScope()` (`a1.js:113-116`) returns `{sheet: 'Budget', range: 'Budget!A1:...
