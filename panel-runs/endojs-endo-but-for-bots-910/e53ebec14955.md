---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 910
panel_kind: code
base_ref: 200def11b3900417ccca5eb8fb3323d9fd8b4875
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: e53ebec14955
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #910 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `4fa0a45f`

seat verdicts (28): archivist=pass assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=pass integrator=comment locksmith=pass migrator=pass packager=must-fix prover=pass pruner=pass purist=comment releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=pass stylist=comment surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- breaker: Invariant claimed: `designs/readableblob-range-attenuation.md` § Proposed interface: *"The runtime guards should req...
- breaker: Implementation: `rangeAttenuationMethodGuards` in `packages/platform/src/fs/interfaces.js:120-122` types both methods...
- breaker: Attack: this guard is shared by every producer this PR touches (`BlobRef`, `LocalBlob`, `GitBlob`, `EndoBlob`, `Trans...
- breaker: This is a real regression, not merely an unfulfilled aspiration: the method it replaces, `fetch`, was guarded `M.call...
- breaker: The fix is mechanical and already the codebase's own live idiom in this exact file: `type-guards.js` still uses `M.er...
- breaker: No test in `packages/platform/test/blob-range.test.js` exercises guard behavior on the resolved value, so this gap sh...
- changeset-auditor: **Bump-level mismatch, `@endo/git`: marked `patch` for a breaking change the changeset's own body calls "a semantics ...
- changeset-auditor: **Bump-level mismatch, `@endo/platform`: marked `minor` for the same class of change this repo's own precedent bumps ...
- changeset-auditor: **Style: sentence-per-line violated twice in `readableblob-range-attenuation.md`.** [rule: `skills/changeset-discipli...
- changeset-auditor: Line 21: `methods. Correspondingly the cap methods \`fetch\`,` — the sentence ending "...builds a producer's `range...
- changeset-auditor: Line 32: `- \`blob.fetch(offset, length)\` → \`blob.range(start, end)\`. **This is a` — same pattern inside the m...
- changeset-auditor: Package-set coherence for the new `readableblob-range-attenuation.md` entry (`@endo/platform`/`@endo/daemon`/`@endo/g...
- changeset-auditor: `@endo/daemon: major` and `@endo/exo-git: patch` (pure type-alias repoint) are both correctly leveled.
- changeset-auditor: Body-vs-diff identifier coherence: the body's `fetch`/`rangeRead`/`rangeReadText` → `range`/`textRange` migration n...
- changeset-auditor: Two `.changeset/*.md` files touched, but they cover genuinely independent release-note concerns (declaration-generati...
- changeset-auditor: Noted but not flagged as a must-fix: `readable-blob-declarations.md`'s front matter (`@endo/agentry: patch`, unmodifi...
- packager: `dc4c40d92` (chore: add changesets for range attenuation) — the commit message claims release notes "span every aff...
- packager: The eight-commit split is otherwise clean: feature (`d3fbe9012`, `5c9bc8db3`) is separated from mechanical follow-ups...
- packager: `@endo/platform` is versioned `0.1.0` and takes a `minor` bump for a change the changeset itself calls a "semantics c...
- saboteur: **Must-fix — timing race**: `EndoMountFile`'s new `streamBytes` generator (mount.js:1601) checks `assertLive()` bef...
