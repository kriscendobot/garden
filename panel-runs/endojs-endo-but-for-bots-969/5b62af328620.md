---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 969
panel_kind: code
base_ref: llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 5b62af328620
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #969 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `14f26d0a`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR endojs/endo-but-for-bots#969
- assessor: `full-run.sh:93` gates re-vendoring on `harness/sta.js` existing, so a stale `--out` dir from an older `TEST262_SHA` ...
- assessor: `json_escape` (`full-run.sh:118`) escapes only `\` and `"`, not control characters. Inputs are SHAs/dates/hostnames, ...
- benchmarker: ## Juror: benchmarker — PR #969 (endojs/endo-but-for-bots)
- breaker: ## Juror: breaker — PR endojs/endo-but-for-bots#969
- corner-prober: **Empty/crashed batch is silently absent from the report** [must-fix-loop] — `run_one` swallows the batch exit (`||...
- corner-prober: **`read_batch`'s documented resume contract is not the code's** [must-fix-loop] — the doc says a malformed file is ...
- corner-prober: **Zero-case report is a false all-clear** [must-fix-loop] — missing results dir, a typo'd `--subtree` (`discover_sc...
- corner-prober: **`--jobs 0` defeats the memory bound** [must-fix-loop] — `xargs -P 0` is *unlimited*, contradicting "`--jobs N` �...
- corner-prober: **`json_escape` (full-run.sh) escapes only `\` and `"`** [summary-fix] — a newline/tab/control char in `--subtree` ...
- corner-prober: **`pct` rounding** [summary-fix] — 49999/50000 → `100.0%`; 1/50000 → `0.0%`. Clamp: never 100.0% unless `n == t...
- corner-prober: **Untested escaping/parse corners** [summary-fix] — no round-trip for a `reason` containing `"`, `\`, `\n`, `\t`, `...
- corner-prober: **Dedup claimed, not exercised** [summary-fix] — `aggregate_merges_batch_files_deterministically`'s comment says "o...
- curator: **must-fix.** `rust/engine/ironhorse-262/src/xst.rs:773` documents the new public field as "empty when a caller uses ...
- curator: **should-fix.** The per-batch wire carries no schema key: `report.rs:453` `to_batch_json` emits `{ "cases": [...] }`,...
- curator: **should-fix.** One shape, two independent declarations across the Rust/shell boundary. `report::batch_filename` (`re...
- curator: **should-fix.** `RunReport::to_batch_json(cases: &[CaseRecord])` (`report.rs:453`) takes neither `self` nor a `RunRep...
- curator: **comment-only.** Broken intra-doc link: `report.rs:32` cites `[`read_report`]`, which does not exist (the module def...
- curator: Exported surface exceeds its callers: `read_batch`, `read_cases_from_str`, `classify`, `Outcome::parse` have no calle...
- curator: `to_json` emits a per-case `"category"` (`report.rs:438`) although `Category`'s own doc (`report.rs:83`) says it "nee...
