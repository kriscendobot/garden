---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 796
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: bbf4e2ef14d4
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #796 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `56d160f5`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=must-fix integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Per-juror block — archivist (PR endojs/endo-but-for-bots#796)
- assessor: `packages/daemon/src/hashline.js:499` — `validateEditPatch` captures `const opCount = ops.length` and only bounds i...
- assessor: `designs/cli-edit-verb.md:981`, `:1407`, `:1411` — the PR rewrites this section as "the v1 algorithm, **as landed**...
- assessor: `hashline.js:399` — capping a single op's payload *line count* at `MAX_RESULT_CHARS` (268,435,456) is ~4 orders of ...
- assessor: `crc32.js:213` — a `DataView` (no `.length` at all) exits as a `RangeError`, while the JSDoc `@throws` prose groups...
- breaker: `packages/daemon/src/hashline.js:380` — the payload line-count cap is **per-op and never accumulated**, so the modu...
- breaker: `packages/daemon/src/types.d.ts` (`EditResult` doc) — the read-oracle mitigation is incomplete. It tells a mount to...
- breaker: `hashline.js:302` — `validateAnchor` uses `Number.isInteger`, not `Number.isSafeInteger`, so `parseHashlineText`'s ...
- breaker: crc32's detachment probe, brand getters, and `Reflect.construct` path are reasoned about for XS but `test:xs` is `exi...
- changeset-auditor: **must-fix — `@endo/zip: patch` contradicts the changeset's own third paragraph.** The body states "an observable i...
- changeset-auditor: **summary-fix — implementation detail in the `@endo/crc32` paragraph.** "branded through the intrinsic `%TypedArray...
- corner-prober: **`normalizeLineForHash`: the negative half of the wire contract is unpinned.** All three normalization assertions ar...
- corner-prober: **Empty payload: the two readers of one wire format disagree.** `{op:'replace', anchor, payload: []}` is accepted by ...
- corner-prober: **Degenerate range `anchor.line === anchorEnd.line`.** `consumedSpan`'s `span.start === span.end` pivot routes it to ...
- corner-prober: **Zero-op patch.** `{expectedFileHash, ops: []}` succeeds as a no-op (`newText === fileText`, `fileHashAfter === file...
- corner-prober: **`crc32(bytes, undefined, index)`.** `length` defaults to the whole `byteLength`, not `byteLength - index`, so the n...
- corner-prober: **Unpaired surrogate in `fileText`.** Payload lines are rejected for exactly this non-injectivity, but the file side ...
- corner-prober: **`validateAnchor` uses `Number.isInteger`, not `isSafeInteger`.** `line: 2**53` validates (verified) where every oth...
- curator: **should-fix** — `packages/daemon/src/hashline.js:644` accepts `@replace-range A..B` as a textual op header (the to...
- curator: **should-fix** — `packages/daemon/src/hashline.js:241,258`: `splitLines(text) => { lines, trailingNewline }` but `j...
