---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 796
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b41a8bad2ab8
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #796 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `7ebb7c85`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## archivist
- assessor: **`packages/daemon/src/hashline.js:668` — reapply relocates a range op's two anchors independently, silently resizi...
- assessor: **`packages/daemon/src/hashline.js:657` — two distinct anchors may relocate onto the same line, and compose silentl...
- assessor: **`packages/crc32/src/crc32.js:88` — the `.at` protocol costs ~11x on zip's hot path.** Measured over 8 MiB: `.at` ...
- assessor: **`packages/crc32/src/crc32.js:88` — the JSDoc invariant is falsified on the happy path.** The docstring asserts a ...
- assessor: **`packages/daemon/src/hashline.js:419` — a duplicate `@expected-file-hash` header silently last-wins.** Two confli...
- assessor: `packages/daemon/test/hashline.test.js:617` is titled "reapply hash checks at the relocated line strip the seed", but...
- benchmarker: **`.at`-protocol rework lands an unmeasured hot-loop change on an already-shipped package.** `packages/crc32/src/crc3...
- benchmarker: **The range API's allocation-avoidance rationale is unmeasured and unexercised.** `packages/crc32/README.md:23-27` ju...
- benchmarker: *(comment-only, adjacent)* `packages/crc32/README.md:29` still says "`bytes` must be a genuine `Uint8Array`", contrad...
- breaker: **must-fix — `validateEditPatch` re-reads untrusted properties, defeating the read-once discipline the module docum...
- breaker: **should-fix — the blank-line seed makes reapply's "exactly one candidate" guard unsound.** `lineAnchorHash:104` se...
- breaker: **should-fix — `@endo/crc32`'s range check is bypassable on the emulated views it advertises.** `packages/crc32/src...
- breaker: `parseHashlineText:411-419` lets a second `@expected-file-hash` header silently override the first (last wins), so a ...
- breaker: README:31 says "`bytes` must be a genuine `Uint8Array`" while `src/crc32.js:24-31` and the tests deliberately support...
- corner-prober: **`crc32` JSDoc claims a self-validating protocol that does not validate.** `packages/crc32/src/crc32.js:24-31` says ...
- corner-prober: **Lone surrogates are a deterministic anchor collision.** `lineAnchorHash('a\uD800b', 1) === lineAnchorHash('a\uFFFDb...
- corner-prober: **`parseHashlineText` is not CRLF-tolerant** (`hashline.js:395`, split on `\n` only). A CRLF-authored patch silently ...
- corner-prober: **Degenerate payloads unpinned.** `{op:'replace', payload:[]}` is an exact alias for `delete` (yields `""` on a 1-lin...
- corner-prober: **Minor, untested:** `crc32(new Uint8Array(0))`; `crc32(bytes, undefined, index)` with `index > 0` throws `RangeError...
