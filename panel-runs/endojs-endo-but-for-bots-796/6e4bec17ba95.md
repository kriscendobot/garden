---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 796
panel_kind: code
base_ref: llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 6e4bec17ba95
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #796 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `5fd12dd4`

seat verdicts (28): archivist=pass assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=pass integrator=must-fix locksmith=pass migrator=comment packager=pass prover=must-fix pruner=pass purist=must-fix releaser=comment saboteur=comment scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=comment typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- assessor: `packages/daemon/src/hashline.js:286` (`validateAnchor`) and `:468` (`parseAnchorToken`) validate `anchor.hash` with ...
- assessor: The `applyEditPatch` docstring's composition note ("inserts anchored *strictly inside* a consumed range are patch-syn...
- breaker: `packages/daemon/src/hashline.js:1-32` (module JSDoc) claims the per-line anchor is a content-identity guarantee ("ha...
- breaker: `packages/daemon/src/hashline.js:292-294` (`validateAnchor`) and the textual parser's `parseAnchorToken` (`hashline.j...
- breaker: The `file-rev-mismatch` gate (`hashline.js:656-662`) runs strictly before any per-line anchor check, which per `desig...
- corner-prober: **`anchor.hash` accepts a 3-char length the wire contract forbids.** The module docstring fixes anchor width at "2-ch...
- corner-prober: **Empty `ops: []` patch (identity no-op edit) is untested.** `applyEditPatch` with a structurally valid, zero-op patc...
- corner-prober: **Empty-array `payload` for `replace`/`insert-*` (validator-accepted, degenerates to delete) is never exercised throu...
- corner-prober: **Range-conflict boundary asymmetry.** The "insert anchored inside a consumed range" test only covers the range's **e...
- corner-prober: **No surrogate-pair/astral/lone-surrogate line content tested.** `crc32`/`normalizeLineForHash` run over `TextEncoder...
- corner-prober: **`reapplyWindow` valid boundary values untested.** Tests cover invalid `0` and `201`; the valid extremes `1` and `20...
- integrator: `designs/cli-edit-verb.md:7` — Status still reads "Proposed" though this PR lands the design's first substantial im...
- integrator: `designs/README.md:1468` — the M7 roadmap row for `cli-edit-verb` still reads "CLI-side, no daemon surface change."...
- integrator: The design names the reference implementation as living in both `packages/cli/src/hashline.js` and `packages/daemon/s...
- prover: The PR body's "Evidence (local)" section reports the 38-test pass count and lint/type cleanliness but contains no reg...
- prover: `packages/daemon/src/hashline.js:606-609` and `:806-809` both describe the composition-conflict check as rejecting in...
- prover: Everything else checks out as load-bearing on manual trace-through: the bottom-up splice-ordering test (`test/hashlin...
- purist: `packages/daemon/src/hashline.js:144-155` hand-rolls a CRC32 (IEEE polynomial) table-generator and digest function th...
- purist: `packages/daemon/src/hashline.js:292` `validateAnchor` accepts hash lengths of 2, 3, or 4 hex chars (`/^[0-9a-f]{2,4}...
- purist: `hashline.js` uses `throw makeError(X\`...\`)` at ~33 call sites; `packages/daemon/src/formula-type.js` and `manager....
