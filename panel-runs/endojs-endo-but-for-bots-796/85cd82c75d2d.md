---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 796
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 85cd82c75d2d
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #796 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `4bb3ebee`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=comment saboteur=must-fix scribe=comment spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR endojs/endo-but-for-bots#796
- assessor: A `Uint8Array` with an own `length` data property (legal — `length` is a prototype accessor) forges `byteLength`; t...
- assessor: Non-byte views are checksummed over *elements* via `& 0xff`. Verified: `crc32(new Float64Array([1.5,300]))` = `6a1a4f...
- benchmarker: `packages/crc32/src/crc32.js:88` — the fast path added *for speed* silently widens what the function accepts. `Arra...
- benchmarker: `packages/daemon/src/hashline.js:80,368` — the cap bounds the search but the commit's cost claim is never quantifie...
- benchmarker: The `~11×` claim is the PR's only quantitative performance statement and it is asserted post-fix, not measured post-...
- benchmarker: This seat's brief and round-1's benchmarker block both cite `skills/benchmark-comparative-report/SKILL.md` and `roles...
- benchmarker: No `packages/crc32/test/*.bench.js` accompanies a package whose sole export is a per-byte loop shared with `@endo/zip...
- breaker: `packages/crc32/src/crc32.js:88` — the `ArrayBuffer.isView(bytes)` fast path defeats the module's central claimed i...
- breaker: `crc32(new DataView(buf), 4)` → `558161692`, exactly `crc32(new Uint8Array(4))`. `DataView.length` is `undefined`, ...
- breaker: `crc32(new Float64Array([1.5, 2.5]))` → `3066839698` === `crc32(new Uint8Array([1, 2]))`. Elements are `ToInt32`-tr...
- breaker: `class S extends Uint8Array { get length() { return 8 } }` over 4 real bytes → `crc32(new S([1,2,3,4]))` === `crc32...
- breaker: `packages/daemon/src/hashline.js:73-79` — `MAX_EDIT_OPS`' JSDoc claims it bounds the reapply search "so a pathologi...
- breaker: `packages/daemon/src/hashline.js:597-604` — blank-line anchors are *structurally* unrelocatable under `reapply`: th...
- breaker: `packages/daemon/src/hashline.js:474` — `parseHashlineText` splits on `\n`; a CRLF-authored patch fails on the blan...
- corner-prober: ## Juror: corner-prober — PR endojs/endo-but-for-bots#796
- curator: `packages/daemon/src/types.d.ts:3205` adds `export type Sha256Hex = (bytes: Uint8Array) => string`, re-exported from ...
- curator: `packages/daemon/package.json:35` adds `"./src/hashline.js"` to `exports`, which makes all nine of that module's expo...
- curator: `packages/crc32/README.md:31` states "`bytes` is read purely through the `.at`/`.length` protocol ... there is no bra...
- curator: `packages/crc32/src/crc32.js:44` freezes `crc32(bytes, length, index, previous)` — three adjacent numeric positiona...
