---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 715
panel_kind: design
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 980ff9f77a15
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #715 (design)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `4651ee07`

seat verdicts (7): copyeditor=must-fix critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=must-fix skeptic=must-fix
must-fix items (20):
- copyeditor: `designs/inspect-package.md:86-90` (Package surface) is a dangling appositive with no antecedent. The sentence assert...
- copyeditor: Panel-process narration leaks into the design's normative voice at five sites: `:87` ("the decomplector and ergonomis...
- copyeditor: `:100-103`: "semantically a thin, safety-hardened wrapper over `util.inspect`" attaches to the nearest nominal, which...
- copyeditor: `:53-62` is one 60-word sentence in which the subject ("the existing defense of `passStyleOf`") is separated from its...
- copyeditor: Hyphenation drifts: "reentrancy" at `:209`, `:214`, `:225` against "re-entrancy" at `:233`, `:254`, `:259`, while the...
- copyeditor: `:279-281`: ragged wrap mid-paragraph ("(`Proxy` / plus its / `typeof`)"), an edit artifact; re-wrap contract step 2....
- copyeditor: `:79-81`: the three comment columns in the surface snippet do not align. comment-only.
- copyeditor: `:300-306`: the environment table mixes cells with and without terminal punctuation. comment-only. [rule: roles/juror...
- copyeditor: Clean on typist-hostile code points: the only non-ASCII in the diff is U+2014, and the diagram is mermaid, not ASCII ...
- critic: **The "SES integration" premise is wrong about the console half, and the browser payoff depends on it.** § *The shim...
- critic: **The seam open question omits the invariant that decides it.** `quote()` is SES's redaction-adjacent surface; a muta...
- critic: **The node row's "near-faithful" needs an engine floor.** `util.types.isProxy` at the top level does not cover *neste...
- critic: **The adopter guidance has no enforcement artifact.** Phase 4 ships `shim-portable.js`/`shim-xs.js` and an "optional ...
- critic: `console.js:368` already constructs a `Console` with `inspectOptions: { customInspect: false }`; contract step 1 rest...
- critic: Phase 4's "byte-for-byte unchanged" is output-identical, not source-identical; the seam edits SES. Tighten the wording.
- critic: `log` as a silent XS no-op is a debuggability trap; the design's own open question about `print`/`trace` should be an...
- decomplector: **should-fix — `inspect`'s contract contradicts its own entry table.** § Package surface calls `inspect` "the port...
- decomplector: **should-fix — `colors` defaulting to "detect from the destination TTY" complects a value with an ambient place.** ...
- decomplector: **should-fix — the shim braids threat model with build target, and fails unsafe.** § Adopter guidance makes "is th...
- decomplector: **should-fix — the `setInspector` open question omits the axis that decides it.** A module-singleton mutator is a m...
