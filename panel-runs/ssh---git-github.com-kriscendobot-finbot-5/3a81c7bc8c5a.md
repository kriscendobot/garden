---
kind: panel-run
repo: ssh://git@github.com/kriscendobot/finbot
pr: 5
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: seat-error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 3a81c7bc8c5a
recorded_by: endolin-garden-ece02cb4
---

# Panel run — ssh://git@github.com/kriscendobot/finbot #5 (code)

Terminal disposition: **seat-error** after **1** round(s).

## Round 1 — head `c1427a66`

seat verdicts (28): archivist=must-fix assessor=comment benchmarker=comment breaker=comment changeset-auditor=comment corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=comment fast-checker=comment gateway=comment integrator=comment locksmith=comment migrator=must-fix packager=must-fix prover=must-fix pruner=comment purist=comment releaser=comment saboteur=comment scribe=comment spec-keeper=comment stylist=must-fix surfacer=comment transplanter=comment typist=must-fix warden=comment wire-watcher=comment
must-fix items (20):
- archivist: **must-fix** — `designs/cap-attenuation.md` gets no note for this landing. That file carries a twice-established pe...
- archivist: **must-fix** — `packages/pipeline/agent-tools.js:81-83` claims "Both the tool closure and the canonical recompute r...
- archivist: **should-fix** — `packages/pipeline/role-dispatch.js:78-83` opens "The stage's product ... is extracted from the to...
- archivist: **should-fix** — `roles/oracle-watcher/AGENT.md` is now load-bearing at runtime: `packages/harness/spawn.js:63` loa...
- archivist: **comment-only** — `packages/pipeline/agent-tools.js:90` throws `'observerToolRegistry requires dispatch-bound trus...
- archivist: `designs/cap-attenuation.md:97` and `:107` already claimed `bin/finbot-dispatch` "drives OBSERVE → ORIENT → … e...
- archivist: `README.md:116` still says "the role `AGENT.md` briefs describe the LLM-dispatch form of each role". Per finding 4 th...
- archivist: No banner horizontal rules found in the diff; the section comments in `role-dispatch.js` (`// OBSERVE stage: ...`) ar...
- archivist: The new `oracle-watcher.js:54-56` dedupe comment accurately describes `[...new Set(assets)].sort()` below it.
- curator: **must-fix.** `designs/remove-driver.md:74` (DELETE row `designs/driver.md`) and removal step 5 delete the design doc...
- curator: **should-fix.** Removal step 1 specifies the CI rename as a *file* rename only. The workflow's exported identifiers (...
- curator: **should-fix.** `logs.sh --lane` and `GARDEN_DRIVER_LANES` are operator-facing surface *removals* filed under section...
- curator: **comment-only.** Step 2 makes the per-host `rm ~/.config/systemd/user/garden-driver@.service` optional, but `install...
- curator: The PR adds no `designs/README.md` index row for itself, calling it "optional". Merged design PR #7 set the same prec...
- curator: Em-dash density in the new doc is high; that is the copyeditor's seat, not mine. [rule: skills/em-dash-style/SKILL.md]
- migrator: `packages/pipeline/agent-tools.js:26` — `import 'ses'` does **not** install `harden`; only `lockdown()` does (verif...
- migrator: `packages/pipeline/role-dispatch.js:170` — `guardedObservation`'s JSDoc claims "no LLM-chosen value can reach ORIEN...
- migrator: `packages/pipeline/agent-tools.js:466` vs `:490` — two tools now share the name `observe_opportunities` with incomp...
- migrator: `bin/finbot-dispatch:139` — new `process.exit(1)` at OBSERVE. Prior CLI callers could not fail there (deterministic...
- migrator: `packages/pipeline/oracle-watcher.js:54` — the `opts.assets` dedupe is a behavior change for 20+ existing call site...
