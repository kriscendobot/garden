---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 790
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: d858aff0f824
recorded_by: endolin-garden-ece02cb4
---

# Panel run — endojs/endo-but-for-bots #790 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `468f5fa1`

seat verdicts (28): archivist=pass assessor=pass benchmarker=pass breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=pass locksmith=must-fix migrator=pass packager=comment prover=comment pruner=pass purist=pass releaser=must-fix saboteur=must-fix scribe=pass spec-keeper=comment stylist=pass surfacer=pass transplanter=pass typist=comment warden=pass wire-watcher=pass
must-fix items (20):
- breaker: `packages/fae/src/tool-makers.js` `makeGrepTool.execute` passes the caller-supplied `pattern` straight into `@endo/pl...
- breaker: `resolveSearchRoot` (`tool-makers.js:429-443`) computes the search `root` via the pre-existing `resolveSafe`, whose c...
- breaker: `search.js`'s glob engine collects and sorts the *entire* match set before yielding any batch (its own JSDoc: "Glob's...
- changeset-auditor: **Missing changeset for a new user-facing tool surface on `@endo/fae`.** The diff adds two new exported tool factorie...
- curator: **Must-fix — missing changeset for new public exports.** `packages/fae/src/tool-makers.js:450` and `:527` add two n...
- curator: **Comment-only — new runtime dependency not changeset-visible.** `packages/fae/package.json` adds `@endo/platform: ...
- engine-realist: `packages/fae/src/tool-makers.js:432-450` (`resolveSearchRoot`) and its two call sites in `makeGlobTool`/`makeGrepToo...
- engine-realist: The confinement mechanics themselves (physical `confinementRoot` via `fs.promises.realpath`, per-child `isPathWithin`...
- locksmith: `packages/fae/src/tool-makers.js:43-49,440-444` — The new `glob`/`grep` tools reuse `resolveSafe`'s boundary check ...
- locksmith: No other findings: the new `glob`/`grep` caplets otherwise attenuate correctly — `resolveSearchRoot` realpaths `cwd...
- locksmith: `packages/fae/src/tool-makers.js:43-49` (`resolveSafe`) is shared by `read-file`/`write-file`/`edit`/`list-dir`, none...
- releaser: **Changeset absent but required.**
- releaser: Recommended action: add a changeset under `.changeset/` for `@endo/fae` (minor bump — new capability, no breaking c...
- releaser: Disposition: `must-fix-loop`.
- releaser: [rule: skills/changeset-discipline/SKILL.md § When to write one — "A new exported API"]
- releaser: No other changesets are touched or added by this PR, so no present-but-unnecessary, wrong-audience, or bump-mismatch ...
- saboteur: **Real, should-fix — grep ReDoS hangs the whole agent process.** `grep` evaluates a fully caller-controlled ECMAScr...
- saboteur: **Real, should-fix — glob's `GLOB_MAX_RESULTS` caps output, not work.** The truncation loop in `makeGlobTool` (`pac...
- saboteur: **Mitigated, no action needed.** `resolveSearchRoot` (`packages/fae/src/tool-makers.js:429-444`) reuses `resolveSafe`...
- saboteur: **Minor.** `pattern`/`dirPath`/`glob` args get only a truthiness check, not a type check; a non-string arg throws a r...
