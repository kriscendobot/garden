---
kind: panel-run
repo: kriscendobot/minion.town
pr: 81
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0c25be817e79
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #81 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `0092d49a`

seat verdicts (29): archivist=comment assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=pass gateway=pass integrator=must-fix locksmith=must-fix migrator=comment packager=comment prover=comment pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=must-fix transplanter=comment typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- breaker: ## Juror: breaker — PR #81 (kriscendobot/minion.town)
- corner-prober: **`setItem` throwing aborts the whole resume — untested error path.** `normalizeToGuest` (`src/web/invitation-sessi...
- corner-prober: **`buildInvitationUrl`'s documented guarantee holds only for a bare origin.** The doc comment claims `new URL(result)...
- corner-prober: **Label bound is UTF-16 code units, not bytes.** `MAX_LABEL_LENGTH` claims a label "cannot be used to smuggle a large...
- corner-prober: **`reason: "malformed-fragment"` is unreachable.** `new URLSearchParams(<string>)` never throws (verified with `%%%&=...
- corner-prober: **Builder's silent drops untested.** `buildInvitationFragment` throws on a bad identifier but *silently omits* an emp...
- corner-prober: **Idempotency guard is encoding-sensitive.** Browsers preserve a literal `:` in `location.hash`; `buildGuestFragment`...
- corner-prober: **Stale count.** `designs/invitation-only-guest-onboarding.md` § 10 says "34 tests"; actual is 36. *summary-fix.*
- duality-auditor: ## duality-auditor
- engine-realist: `src/web/invitation-envelope.ts:121-125` — the `try`/`catch` around `new URLSearchParams(raw)` is unreachable on ev...
- engine-realist: `test/web/invitation-session.test.ts:138` — `expect(env.location.hash).toBe("#")` pins the fake, not the browser. R...
- engine-realist: `src/web/invitation-session.ts:54,66,99,121` — every store access is unguarded. With site data blocked, or in a par...
- engine-realist: `src/web/invitation-envelope.ts:164` — `guest as string` is redundant; the `!isValidFormulaIdentifier(guest)` early...
- engine-realist: `parseFragment` bounds nothing before parsing: the 128-char limit is post-decode and label-only, so a multi-MB fragme...
- engine-realist: `src/web/**` builds with `lib: ["ES2022"]` and no DOM lib, so `URLSearchParams` types come from `@types/node` — bro...
- engine-realist: `ID_PATTERN` is correctly non-global (no `lastIndex` carry across calls) and linear (no backtracking blowup) — no e...
- engine-realist: `URLSearchParams` and `Error` are read from the global at call time. SES `lockdown()` does not freeze host constructo...
- integrator: **must-fix — the landed design note states a false test count.** `designs/invitation-only-guest-onboarding.md:419` ...
- integrator: **should-fix — broken relative link, twice.** `designs/ocapn-cbor-browser-frontend-lessons.md:7` and `designs/invit...
- integrator: **should-fix — commits record intra-PR churn.** `9684d66` deletes `isValidFormulaNumber`/`NUMBER_PATTERN` that `7ad...
