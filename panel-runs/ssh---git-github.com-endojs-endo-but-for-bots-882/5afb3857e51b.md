---
kind: panel-run
repo: ssh://git@github.com/endojs/endo-but-for-bots
pr: 882
panel_kind: code
base_ref: 3b2129924644c67afb80fd2d41b6822498f74168
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 5afb3857e51b
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/endojs/endo-but-for-bots #882 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `a763ab32`

seat verdicts (28): archivist=must-fix assessor=pass benchmarker=comment breaker=must-fix changeset-auditor=must-fix corner-prober=comment coverage-auditor=comment curator=pass engine-realist=comment fast-checker=comment gateway=comment integrator=must-fix locksmith=comment migrator=must-fix packager=comment prover=must-fix pruner=must-fix purist=comment releaser=pass saboteur=must-fix scribe=pass spec-keeper=comment stylist=must-fix surfacer=pass transplanter=pass typist=must-fix warden=comment wire-watcher=must-fix
must-fix items (20):
- archivist: **[should-fix] README misstates the clean-checkout build flow for the first two bundles.** `rust/endo/README.md:26` s...
- archivist: **[comment-only] "lockdown replaces `globalThis`" is loose mechanism prose.** `designs/worker-rust-xs.md:521` and `pa...
- archivist: `@endo/harden` `make-selector.js` quote ("prevent any HardenedJS's lockdown from succeeding") is verbatim; `polyfills...
- archivist: `fx_lockdown` declared-but-never-called (`ffi.rs:275`); `bootstrap_ses` evaluates `POLYFILLS` then `SES_BOOT`; `HOST_...
- archivist: Cross-refs resolve: "§ Module Loading item 3" (`designs/worker-rust-xs.md:294`), "§ Known Gaps" (`:501`), `[Impleme...
- archivist: `worker-facet-interface.js` rationale (`interfaces.js` imports `@endo/platform/fs/lite`) confirmed; re-export preserv...
- archivist: `endowCompartment` / `makeXsWorkerFacet` JSDoc matches code; `bundle-bus-daemon-rust-xs.mjs` stale `ses_boot.js` prod...
- archivist: Three artifacts are `.gitignore`d (`.gitignore:34-36`); all three generators write to `rust/endo/xsnap/src`.
- breaker: The design doc claims the XS worker is "the same trust architecture as today's SES-based workers, but enforced by the...
- breaker: `packages/daemon/src/bus-worker-xs-facet.js:107` comment asserts `terminate` only "raises the flag the Rust main loop...
- breaker: `packages/daemon/test/xs-worker-bundles.test.js:61` asserts `/require\(['"]node:/.test(text)` is false to catch a Nod...
- breaker: `packages/daemon/src/bus-worker-xs-facet.js:48` `standardEndowments` filters `v !== undefined` so XS-missing globals ...
- breaker: `endowCompartment` (`packages/daemon/src/bus-worker-xs-facet.js:71`) throws on a non-configurable collision and skips...
- breaker: The `evaluate` interface guard accepts `M.arrayOf(M.any())` for endowment values (`packages/daemon/src/worker-facet-i...
- changeset-auditor: [proposed-rule: a PR that touches a published, changeset-tracked package must add or update a `.changeset/*.md` entry...
- changeset-auditor: [rule: skills/changeset-discipline/SKILL.md § Bump level] If a changeset is added, the bump should be `patch`, not `...
- integrator: **must-fix** [rule: AGENTS.md § Hardened JavaScript — `harden()` is mandatory] `packages/daemon/src/worker-facet-i...
- integrator: **must-fix** [rule: AGENTS.md § Pull requests — rebase by default, no squash; integrator § Commit grouping] `a78e...
- integrator: **must-fix** [proposed-rule: rename-sweep completeness — when a PR corrects a concept name in a document it edits, ...
- integrator: **should-fix** [proposed-rule: convention probe — a new entry follows the file's stated convention unless it delibe...
