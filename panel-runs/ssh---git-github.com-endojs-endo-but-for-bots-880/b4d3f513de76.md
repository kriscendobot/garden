---
kind: panel-run
repo: ssh://git@github.com/endojs/endo-but-for-bots
pr: 880
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: b4d3f513de76
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — ssh://git@github.com/endojs/endo-but-for-bots #880 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `a4f1dc0a`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=comment changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=comment engine-realist=pass fast-checker=pass gateway=comment integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=must-fix pruner=pass purist=must-fix releaser=comment saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=comment surfacer=pass transplanter=pass typist=comment warden=comment wire-watcher=must-fix
must-fix items (20):
- archivist: `package.json:109` versus commit `e30b1dfa2d` subject. The subject says "Pin form-data to 4.0.6", but the specifier a...
- archivist: Both commits have empty bodies. The branch is `fix/form-data-advisory` and this is advisory remediation, yet nothing ...
- archivist: Coverage checks out for the record: `^4.0.6` overrides `axios@1.10.0`'s `form-data: ^4.0.0` and `@types/node-fetch@2....
- archivist: Lock file is correctly isolated in its own `chore: Update yarn.lock` commit. [rule: AGENTS.md § Pre-PR checklist]
- assessor: **(should-fix)** The commit body is empty and names no advisory. `package.json:109` installs a permanent tree-wide ov...
- assessor: **(should-fix)** The subject says "Pin form-data to 4.0.6", but `^4.0.6` is a floor within 4.x, not a pin. The siblin...
- assessor: **(should-fix)** The `resolutions` entry is not required to remediate this. Both dependents already admit 4.0.6 (`axi...
- assessor: Remediation is complete. One form-data copy at 4.0.6, no 2.x/3.x copies; `yarn why form-data` confirms both paths.
- assessor: `yarn install --immutable` passes; only pre-existing peer warnings.
- assessor: No workspace imports form-data. It arrives via `nx` (dev tooling) and a types package, so no published runtime path i...
- assessor: Lockfile ships in its own trailing `chore: Update yarn.lock` commit, correctly ordered. [rule: `skills/yarn-lock-sepa...
- assessor: 4.0.6 requires `hasown@^2.0.4`, so the lock now carries hasown at both 2.0.2 and 2.0.4. Unavoidable upstream, no corr...
- assessor: No `yarn npm audit` CI gate exists, so nothing guards against regressing this advisory. Separate job.
- corner-prober: **`package.json:109`** — title, PR body, and commit subject all say *"Pin form-data to 4.0.6"*, but `^4.0.6` is a *...
- corner-prober: **`package.json:109`** — the resolution key is **unscoped**, so Yarn applies it to *every* `form-data` descriptor r...
- corner-prober: **Repo-wide** — nothing pins this invariant. `.github/workflows/` has no advisory gate (`zizmor.yml` lints workflow...
- corner-prober: The lock now carries **two `hasown` instances** (2.0.2 via `^2.0.2`, 2.0.4 via `^2.0.4`). Harmless — `hasown` is st...
- corner-prober: Commit body is empty; both GHSA ids live only in the PR body. Step 6 of the pinning skill asks for the verification s...
- corner-prober: `@endo/lal` is `private: true` today. If it is ever published, its consumers resolve `form-data` themselves at `^4.0....
- corner-prober: The `chore: Update yarn.lock` split is correct. [rule: `AGENTS.md` § Pre-PR checklist]
