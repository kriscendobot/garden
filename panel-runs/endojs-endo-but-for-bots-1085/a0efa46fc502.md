---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1085
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: a0efa46fc502
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1085 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `ed5fbada`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=pass duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=comment prover=comment pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=pass typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #1085 (`feat/mount-stream-glob-grep`)
- assessor: `grepMatchPattern.file` (`mount.js:1027`) keeps `M.string()`'s default 100,000 limit while the comment above it asser...
- assessor: The full-path-set materialization inside `globPaths` is unbounded, but eager `glob` already pays it identically — p...
- assessor: `mount.js:2073-2074` has a stray mid-sentence line break ("The daemon's `mount` /") left by the edit. Copyeditor's su...
- benchmarker: `designs/mount-stream-glob-grep.md:426-430` + `packages/daemon/src/mount.js:39-68` — **`STREAM_BUFFER_MAX = 1024` s...
- benchmarker: `packages/daemon/src/mount.js:995-998` and `designs/mount-stream-glob-grep.md:121` — **the `batchSize: 1` rationale...
- benchmarker: `packages/daemon/test/mount-stream-search.test.js:690` — **"Empty files keep this cheap" is an unmeasured cost clai...
- breaker: **should-fix — "revocation bounds post-revoke filesystem work" is falsified during the eager enumeration.** `packag...
- breaker: **should-fix — the 10 MB `stringLengthLimit` residual is stream-fatal, not record-local.** `mount.js:111-112` says ...
- breaker: **comment-only — concurrent `stream()` partitions the result set silently.** `makeReaderPump` shares one `asyncIter...
- breaker: **comment-only — nothing bounds stream *count* per mount.** `STREAM_BUFFER_MAX` is per `stream()`; a grantee can mi...
- changeset-auditor: `.changeset/daemon-mount-stream-glob-grep.md:11` — the revocation-window bound is stated as global ("after `EndoMou...
- changeset-auditor: `.changeset/daemon-mount-stream-glob-grep.md:12` — "newly exported from `@endo/daemon`'s `mount.js`" names the file...
- changeset-auditor: The body omits a user-observable failure shape the diff introduces: `readPattern` is `mustMatch`-enforced on every el...
- corner-prober: Cleaned up my probe files (the `.tmp-prover/` artifacts are another seat's). Findings below are all empirically verif...
- duality-auditor: **`streamGrep` is documented as `grep`'s counterpart but is parameterized as `glorp`'s.** `streamGrep(pattern, { glob...
- duality-auditor: **"exactly like `grep`'s `paths`" pairs the wrong axis.** `packages/daemon/src/mount.js:1027` (echoed in `interfaces....
- duality-auditor: `EndoMount` now carries two prefix families of synchronously-returned readers: `follow*` (`interfaces.js:107`) and th...
- duality-auditor: `grepMatchPattern.file` uses bare `M.string()` (100,000 default) at `mount.js:122` while `streamGlob`'s element — t...
- engine-realist: `packages/daemon/src/mount.js:956-968` and `:2066-2071` (and `designs/mount-stream-glob-grep.md` § Producer / § Rev...
