---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1117
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 64c28655eaec
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #1117 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `5d2fb0a4`

seat verdicts (19): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix curator=must-fix duality-auditor=comment engine-realist=comment integrator=comment locksmith=comment migrator=must-fix packager=must-fix prover=must-fix purist=must-fix saboteur=comment spec-keeper=comment stylist=must-fix typist=comment warden=must-fix wire-watcher=comment
must-fix items (20):
- archivist: **must-fix — the breaking `registry` → `registryRoot` rename is documented only in a changeset.** `packages/exo-n...
- archivist: **should-fix — stale design pointer in a comment this diff rewrote.** `packages/daemon/src/types.d.ts:272`–`276` ...
- archivist: **should-fix — `RegistryResolution`'s docstring still names the deprecated producer.** `packages/exo-npm/types.d.ts...
- archivist: **should-fix — API documentation stranded outside the export.** `packages/exo-npm/src/registry-tree.js:493` (`list:...
- archivist: **comment-only — design header.** `designs/npm-registry-as-directory-tree.md:3`–`8` flips `Status` to `Complete` ...
- assessor: `endorTreeByHash` / `endorBlobByHash` (`manager.js`) are unbounded process-lifetime maps over every CAS node traverse...
- assessor: `makeVersionLeaf` re-runs `listVersions` + `providePackageTree` on every `lookup`, so the memo never short-circuits t...
- breaker: `packages/exo-npm/src/registry-tree-resolver.js:212-224` — the design's load-bearing invariant is that an unreachab...
- breaker: `packages/exo-npm/src/registry-tree.js:126-163` — `makeLookupTreeView` is billed (README:20, design § Resolver/moc...
- breaker: `packages/exo-npm/src/registry-tree-resolver.js:246-249` — a leaf whose `getInfo()` omits `integrity` silently yiel...
- breaker: `packages/exo-npm/src/errors.js:48,63` — `X` redacts substitutions under production `errorTaming: 'safe'`; the mess...
- breaker: `registry-tree.js:169,171` — the scope-hub comment justifies non-memoization as anti-exhaustion, yet `packageDirect...
- breaker: `npmHub.has` normalizes **every** segment through `scopedPackageSegments` while `lookup` normalizes only the head, so...
- breaker: Untracked debris in the worktree (`packages/exo-npm/test/tmp-err.test.js`, `zz-sab-probe.test.js`) — not in the dif...
- curator: ## curator — PR #1117 (kriscendobot/endo-but-for-bots)
- migrator: **must-fix** — `makeDeprecatedEndoRegistryAdapter`'s `list()` returns `harden([])` unconditionally (`packages/exo-n...
- migrator: **should-fix** — the same adapter's `lookup(name, version)` silently changes I/O class. Old: `table.getTree(name, v...
- migrator: **should-fix** — `.changeset/registry-directory-tree.md` and the revised `registry-capability.md` narrate the PR's ...
- migrator: Bump levels are otherwise correct: `@endo/platform` minor is purely additive (`readableTreeMethodGuards` retained as ...
- migrator: This is the first changeset naming `@endo/exo-npm`, so it fixes its next version at 0.2.0; the skill prescribes `majo...
