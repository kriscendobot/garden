---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 1117
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: error
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 4fc270ffe427
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/endo-but-for-bots #1117 (code)

Terminal disposition: **error** after **1** round(s).

## Round 1 — head `77238ff2`

seat verdicts (25): archivist=must-fix assessor=must-fix benchmarker=error breaker=must-fix changeset-auditor=error corner-prober=error curator=must-fix duality-auditor=must-fix engine-realist=must-fix gateway=error integrator=error locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=error purist=must-fix saboteur=must-fix scribe=error spec-keeper=must-fix stylist=must-fix surfacer=must-fix typist=must-fix warden=must-fix wire-watcher=error
must-fix items (20):
- archivist: **must-fix — design index contradicts the design.** `designs/npm-registry-as-directory-tree.md:7` now reads `Status...
- archivist: **must-fix — types declare a root export that does not exist.** `packages/exo-npm/types.d.ts:432` declares `compare...
- archivist: **should-fix — the amendment was written for one design, not the other.** `designs/snapshot-mapper.md:119`, `:240`,...
- archivist: **should-fix — half-updated comment points at the superseded design.** `packages/daemon/src/types.d.ts:273` was rew...
- archivist: **should-fix — "alias" describes something the code does not do.** `packages/platform/src/fs/interfaces.js:60` says...
- archivist: **comment-only — README undercounts the backend seam.** `packages/exo-npm/README.md:13` says `makeNpmRegistryTree(o...
- archivist: **comment-only — a deprecated capability still introduces itself as the registry.** `packages/daemon/src/registry.j...
- assessor: `packages/exo-npm/src/snapshot-mapper.js:47` — `readTreeBytes` passes the slash-joined `modulePath` (`src/main.js`)...
- assessor: `packages/exo-npm/src/registry-tree.js:404` — `npmHub.has` runs `path.flatMap(scopedPackageSegments)` over **every*...
- assessor: `packages/exo-npm/src/registry-tree.js:353` — `packageDirectories` is an unbounded `Map` keyed on package name, and...
- assessor: `packages/daemon/src/manager.js:1932-1933` — `endorTreeByHash` / `endorBlobByHash` grow without eviction for the da...
- assessor: `packages/exo-npm/src/registry-tree.js:187` vs `:192` — the view's `has()` (empty path) forwards `true` while `look...
- assessor: `packages/daemon/src/registry-tree.js:157` and `manager.js:1946` — `JSON.parse` on the Endor envelope is unguarded ...
- breaker: ## Juror: breaker — PR kriscendobot/endo-but-for-bots#1117
- curator: ## Per-juror block — curator (PR kriscendobot/endo-but-for-bots#1117)
- duality-auditor: **must-fix — `isPackageRegistryError` is the narrower predicate but the more-qualified name.** `isRegistryError` (`...
- duality-auditor: **must-fix — `has`/`lookup` disagree at the empty path in `makeLookupTreeView`.** `has: (...path) => tree.has(...pa...
- duality-auditor: **should-fix — one wire format, three names for its two edges.** Producer `encode` (`rust/endo/xsnap/src/powers/reg...
- duality-auditor: **comment-only — same conversion, two verbs.** `decodePackageJson` (`snapshot-mapper.js:117`) and `parsePackageJson...
- engine-realist: **must-fix — a guest-reachable capability blocks the single XS manager thread on an untimed HTTP call.** `@registry...
