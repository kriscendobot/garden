---
kind: panel-run
repo: kriscendobot/minion.town
pr: 119
panel_kind: code
base_ref: 561472a21571eb0dd6f57388d5040bbc0546efd1
rounds: 1
disposition: must-fix
exit_code: 0
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 7c425e261a0d
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #119 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `793c89a8`

seat verdicts (33): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=comment duality-auditor=pass engine-realist=must-fix fast-checker=comment gateway=comment integrator=pass locksmith=comment migrator=pass orthographer=pass packager=pass procurer=pass prover=pass pruner=pass purist=must-fix reexport-auditor=pass releaser=pass saboteur=must-fix scribe=comment spec-keeper=comment stylist=must-fix surfacer=pass thesaurus=pass transplanter=pass typist=comment warden=comment wire-watcher=must-fix
must-fix items (20):
- archivist: **[must-fix] Stale function reference in recordOutcome JSDoc** — agents.ts:199 says "The epoch advance lives in `ha...
- archivist: **[should-fix] Instructions prose does not reference path location** — src/auth/claude-account-endpoint.ts:27-30 em...
- assessor: **[must-fix]** `dismiss()` never removes an agent from the reauth tracker's parked ledger, so a dismissed/removed age...
- assessor: `src/endo/claude/agents.ts:250-257` (`dismiss`) only touches `context.quota.releaseChild` and the child directory; it...
- assessor: Failure scenario: an agent hits `needs-auth` (parked, `reportNeedsAuth("infer", childFormulaId)`), the caller then ca...
- assessor: This is untested: `test/claude-wiring.test.ts:289-290` exercises `dismiss` only for quota bookkeeping, and no test in...
- assessor: [proposed-rule: any state that tracks live agent identifiers by key (parked ledgers, resume lists) must be wired to t...
- assessor: **[comment-only]** `handleSetupToken` calls `sub.advanceCredentialEpoch()` immediately before `await sub.credentials....
- breaker: **should-fix: the pre-check `needs-auth` path skips the stale guard.** Commit 793c89a says it guards every stale outc...
- breaker: Attack: the live credential store is async, and `status()` resolves `needs-auth` from the old credential. While it wa...
- breaker: Fix: check `context.credentialEpoch() !== epoch` again before reporting, and return `unavailable` if it changed, as t...
- breaker: [proposed-rule: every `await` between reading a generation token and acting on that generation must be followed by a ...
- breaker: **should-fix: an error from `deriveCredentialExpiry` leaves the reauth event open after a good probe.** `wiring.ts` s...
- breaker: Attack: the expiry seam throws, for example because `auth status` output has an unpinned shape. `handleSetupToken` th...
- breaker: Fix: catch the error, treat the expiry as unknown (the default the design documents), and still call `rootReconnected`.
- breaker: **should-fix: "One notice per window (keyed by resetAt)" does not hold in two cases.** `reportUsageExhausted` only de...
- breaker: Attack A: the provider reports its reset time with jitter (a rolling "resets in N s" turned into an absolute time). E...
- breaker: Attack B: the reset time is already past, from clock skew or a pinned path that reads epoch *seconds* as milliseconds...
- breaker: Fix: dedupe on "a window is active" rather than exact equality, and treat `resetAt <= now()` as unreadable in `classi...
- breaker: [proposed-rule: dedupe keys taken from provider data need a tolerance or a monotonic rule, never exact equality]
