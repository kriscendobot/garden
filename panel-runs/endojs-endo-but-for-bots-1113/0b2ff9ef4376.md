---
kind: panel-run
repo: endojs/endo-but-for-bots
pr: 1113
panel_kind: code
base_ref: origin/llm
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 0b2ff9ef4376
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — endojs/endo-but-for-bots #1113 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `be5eef82`

seat verdicts (29): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=comment corner-prober=must-fix coverage-auditor=comment curator=comment duality-auditor=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=must-fix prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR endojs/endo-but-for-bots#1113
- assessor: **must-fix** — `interp.rs:18417`. The `IteratorToList` snapshot re-introduces exactly the unmetered length-proporti...
- assessor: **should-fix** — `interp.rs:26237`. The new comment promises other primitives "stay a named gap", but the preceding...
- assessor: **should-fix** — both new test helpers assert `result_agrees` only, but the ratchet floor is meter-exact and two fi...
- assessor: **should-fix** — `rust/engine/ironhorse-262/src/xst.rs:579`. The wildcard swallows `Halt::StepLimit`, a common iron...
- assessor: **comment-only** — `xst.rs:570`. `wrong-throw:{constructor_name(error)}` is unbounded for non-Error throws (`throw ...
- assessor: `test262.rs:173` keeps the coarse `"ironhorse-aborted"` label, so the ranking refinement exists only on the `xst` pat...
- assessor: `interp.rs:32897`: `.as_deref()` on `Option<&u16>` is a no-op.
- assessor: The baseline README's "What this round changed" documents two clusters and never mentions commit `299b57fb3a` or `tes...
- benchmarker: ## Juror: benchmarker — PR #1113 (endojs/endo-but-for-bots)
- breaker: Cross-frame `Halt::Resume` through native drivers — `try{[1].map(()=>JSON())}catch` in `map`/`forEach`, with `final...
- breaker: `Halt::Resume` leaking past the un-updated `Err(h) => return h` arms at `interp.rs:13674`/`13874`/`13776` — unreach...
- breaker: `array_generic_has` uninterned fast path — proxies, typed arrays and wrappers all fall back to the MOP walk; `argum...
- breaker: `truthy` correctly reads `""`/`0n` payloads; the `getOwnPropertyDescriptor` ToObject arm is reachable (`undefined`/`n...
- corner-prober: ## corner-prober — PR #1113 (endojs/endo-but-for-bots)
- duality-auditor: **should-fix — `enter_call` and `raise_js` share one untyped pc channel with opposite meanings.**
- duality-auditor: **should-fix — the spec name sits on the weaker half of `to_boolean` / `truthy`.**
- duality-auditor: **comment-only — the intern triangle's new non-minting edge is unnamed.**
- duality-auditor: **comment-only — the two halves of the abort vocabulary diverge.**
- engine-realist: I reviewed the diff at `origin/llm...HEAD` (4 commits; `interp.rs`, `xst.rs`, two dual-run test files, the refresh sn...
