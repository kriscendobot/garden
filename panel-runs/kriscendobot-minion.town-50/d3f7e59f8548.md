---
kind: panel-run
repo: kriscendobot/minion.town
pr: 50
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: d3f7e59f8548
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #50 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `142f1459`

seat verdicts (28): archivist=must-fix assessor=comment benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=comment coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=comment gateway=pass integrator=must-fix locksmith=must-fix migrator=pass packager=comment prover=pass pruner=pass purist=must-fix releaser=pass saboteur=comment scribe=pass spec-keeper=comment stylist=comment surfacer=must-fix transplanter=pass typist=comment warden=pass wire-watcher=must-fix
must-fix items (20):
- archivist: `README.md` is not updated for the new `whoami` tool. The top-level description ("It's a tiny 'minion town': three MC...
- archivist: `deploy/aws/www/index.html:180` now describes the `mcp/tools` scope ladder entry as "call the baseline tools" (plural...
- archivist: `designs/remove-toy-tools-and-prune-minion-scopes.md` (untouched by this PR) claims to be "Grounded against… `src/a...
- breaker: `src/server.ts` (`whoami` handler): the displayed `role` is computed as `policy.isKnown(caller.iss, caller.sub) ? "ad...
- breaker: `whoami` is unconditionally `server.registerTool(...)`'d for every session, unlike the guest tools, which are mounted...
- breaker: The `whoami` handler calls `resolveEffectiveScopes` twice (once inside `authorize()`, again directly after) with iden...
- curator: **[must-fix]** `whoami`'s role field diverges from `GET /account`'s role field for the same identity, contradicting t...
- curator: **[comment-only]** New exports `TOOL_SCOPES: Record<string, string>` and `isToolAllowed(tool, granted): boolean` (`sr...
- engine-realist: `src/server.ts:165-172` — the `whoami` handler resolves the caller's effective scopes and account record **three se...
- engine-realist: The same handler recomputes `identityOf(extra.authInfo)` and `authInfo?.scopes ?? []` a second time (lines 166-167) p...
- integrator: **[should-fix] `whoami`'s role derivation reimplements — and diverges from — the account-view resolution the desi...
- integrator: **[comment-only] `authorize()` computes `resolveEffectiveScopes` but discards it, forcing its only caller to recomput...
- locksmith: `src/auth/scopes.ts:27-30` — `TOOL_SCOPES` is exported as a plain, unfrozen `Record<string, string>` and is the sol...
- locksmith: `src/server.ts` — the new `whoami` handler calls `resolveEffectiveScopes` twice per invocation: once inside `author...
- locksmith: The `whoami` tool itself is capability-neutral by design: it reports only the caller's own `iss`/`sub`/`email`/`provi...
- purist: `src/server.ts:167-171` computes `role` inline as `policy.isKnown(caller.iss, caller.sub) ? "admin" : account?.role ?...
- purist: `src/auth/scopes.ts:28` — `TOOL_SCOPES: Record<string, string>` breaks the family's immutability convention: siblin...
- purist: `src/server.ts:88-95` (`authorize`) resolves `effective` scopes and then discards them (returns `void`); the `whoami`...
- surfacer: `README.md` quickstart transcript (untouched by this diff) shows the exact output of `dev/client.ts`'s `tools/list` d...
- surfacer: `README.md` § "The guest tools and admission" is the project's canonical scope-to-tool documentation table (`Tool | ...
