---
kind: panel-run
repo: kriscendobot/endo-but-for-bots
pr: 988
panel_kind: design
base_ref: upstream/llm
rounds: 1
disposition: passed
must_fix_total: 17
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: c156075b1bc9
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/endo-but-for-bots #988 (design)

Terminal disposition: **passed** after **1** round(s).

## Round 1 — head `df8569c4`

seat verdicts (7): copyeditor=comment critic=must-fix decomplector=must-fix ergonomist=must-fix novice=must-fix pedant=comment skeptic=must-fix
must-fix items (17):
- critic: The `GitTreeEntry` schema requires mode `160000` (gitlink/submodule) to target a resolvable `FormulaIdentifier` of ty...
- critic: § Object formulas states a `git-tree` directory entry (mode `40000`) may target either a `git-tree` **or a `readable...
- critic: The Dependencies table lists `namehub-interface-unification` (itself still Proposed/unimplemented) as a dependency, b...
- decomplector: **`FormulaRefStore.compareAndSwap`'s optional `terminal` check undermines the CAS invariant the design claims two par...
- decomplector: **`GitActor`/`GitHeader`'s `rawBase64` (source-of-truth bytes) and parsed fields (`nameBase64`/`emailBase64`/`seconds...
- decomplector: Whether `FormulaPath.path` may itself traverse through another `FormulaRef`/ref store (recursive-authority amplificat...
- ergonomist: The design never states whether `FormulaGitObjectStore` / `FormulaRefStore` are guest-facing capabilities or host-int...
- ergonomist: `FormulaGitObjectStore`'s four methods spell inconsistently: `resolve`, `readObject`, `ingestObject` are verb-first (...
- ergonomist: `FormulaRefStore` exposes only `list(prefix)` for reads; there is no singular ref-read method. Reading one known ref ...
- novice: The problem statement's second paragraph says this design "blocks Strategy B" and is required to unblock it, but the ...
- novice: "Formula paths" (the first subsection of `## Design`) leans on "formula", "name hub", and "pet-store edge" as if alre...
- novice: "Synthetic refs tree" introduces the word "selector" ("even when a stored selector has not changed…") for a concept...
- novice: "`rawBase64` is the authoritative complete actor header value; the parsed fields are validated indexes for inspection...
- skeptic: The Security section commits to enforcing "size, depth, entry-count, and parent-count limits before formulation" and ...
- skeptic: The "Synthetic refs tree" section singles out a specific race as the reason CAS re-resolves terminal identifiers on e...
- skeptic: Rationale-integrity overlap: `GitActor.rawBase64` is declared "the authoritative complete actor header value," with t...
- skeptic: None — the spec reading (Git modes, tree sort order, object-ID hashing grammar, tag-vs-commit signature placement) ...
