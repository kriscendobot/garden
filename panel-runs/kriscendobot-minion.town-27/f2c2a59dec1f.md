---
kind: panel-run
repo: kriscendobot/minion.town
pr: 27
panel_kind: code
base_ref: origin/main
rounds: 1
disposition: must-fix
must_fix_total: 20
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: f2c2a59dec1f
recorded_by: endolin-garden2-5bcdff64
---

# Panel run — kriscendobot/minion.town #27 (code)

Terminal disposition: **must-fix** after **1** round(s).

## Round 1 — head `5009b1d9`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=must-fix breaker=must-fix changeset-auditor=must-fix corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=must-fix integrator=comment locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=must-fix saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: **must-fix** — `src/endo/gateway/gateway.ts:5-6` ("**Control** (`/gateway/*`, reserved namespace)") and `:52` ("Res...
- archivist: **must-fix** — `DEPLOYMENT.md:242-243` still reads "Until the publish capability (Increment 4) exists, seed a weble...
- archivist: **should-fix** — `DEPLOYMENT.md:279-280` DoD says "a manifest naming a reserved **prefix** is rejected at publish"....
- archivist: **should-fix** — half-migrated future tense. The PR updated `vhost-table.ts:162` but left the identical claim uncha...
- archivist: **should-fix** — `publish.ts:95-98`: `serving` is documented as "reports whether the origin is registered and will ...
- archivist: **should-fix** — `guest-control.ts:127-128`, edited by this PR to "1:1 with the `guest_*` / `weblet_*` tools the to...
- archivist: **comment-only** — `designs/weblet-gateway.md` does not exist in this repo; the PR adds 14 more references to it (4...
- archivist: `guest-tools.ts:1-26` header still describes only the guest family; a one-line mention of the weblet family would kee...
- archivist: `weblet-payment.ts:37` names the parameter `identity` where the interface it implements names it `owner` (`publish.ts...
- assessor: `src/endo/gateway/publish.ts:198` — `if (key in entries)` tests an object **literal**, so every `Object.prototype` ...
- assessor: `src/endo/gateway/publish.ts:87,283` — `PublishResult.warning` is declared, documented, and rendered by the tool (`...
- assessor: `src/endo/gateway/weblet-store.ts:57` / `publish.ts:270,304` — `pin` is published as *the* GC-deferral seam (§ 7.1...
- assessor: `src/endo/guest-tools.ts:266` — `Buffer.from(f.bytes, "base64")` silently discards non-alphabet characters (verifie...
- assessor: `src/endo/gateway/publish.ts:277-280` — first-writer-wins is a read-then-write, not a CAS: two concurrent publishes...
- assessor: `src/endo/gateway/publish.ts:245` (`planContent`) — no cap on total bytes or file count. The only bound is `express...
- assessor: `src/endo/gateway/base32.ts:120` — `labelToId` is not injective: a 52-char label carries 260 bits and the trailing ...
- assessor: `publish.ts:283,291` — `serving` is hardcoded `true` in both `publish` and `list`; since `list` rows come from regi...
- assessor: `src/config.ts:186` — `process.env.MINION_TOWN_DOMAIN ?? "weblet.minion.town"` does not guard the empty string, unl...
- assessor: `vhost-table.ts:190` — `listVhostRecords` reads every record file on each `weblet_list`, so an owner-scoped list is...
- assessor: The gateway change (`gateway.ts:96,128`) is correct as far as I can check it: dropping the `/gateway/*` catch-all in ...
