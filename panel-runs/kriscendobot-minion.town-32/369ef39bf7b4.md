---
kind: panel-run
repo: kriscendobot/minion.town
pr: 32
panel_kind: code
base_ref: HEAD~1
rounds: 4
disposition: error
must_fix_total: 80
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 369ef39bf7b4
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #32 (code)

Terminal disposition: **error** after **4** round(s).

## Round 1 — head `1393ca1b`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=must-fix packager=comment prover=must-fix pruner=comment purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=pass transplanter=must-fix typist=must-fix warden=must-fix wire-watcher=comment
must-fix items (20):
- assessor: Verified the diff by running it (28/28 pass) and by checking each assertion against its source, plus what the two B3 ...
- breaker: **`test/deploy-coherence.test.ts:57,64` — first-match `String.match()` makes both scope extractors blind to a shado...
- breaker: **`test/deploy-coherence.test.ts:96-100` — the policy guard asserts nothing when the shape drifts (vacuous loop).**...
- breaker: **`DEPLOYMENT.md:622-628` — the newly canonized ordering invariant is the one invariant with no guard.** The paragr...
- breaker: **`test/deploy-coherence.test.ts:139` — `bash -n` failures report no location.** `stdio: "pipe"` plus `.not.toThrow...
- breaker: `import.meta.dirname` (line 34) needs Node >= 20.11; `package.json` declares `engines.node: ">=20"`. CI pins 22 so th...
- breaker: The policy guard reads `identities` only; `policy.default.scopes` is unchecked, though that object's own note says "D...
- breaker: Pin guard is sound as written (both regexes anchor past the doc comments, and every miss fails loudly with a named me...
- corner-prober: Scope set: 4-scope vocabulary ✓; `mcp/guest` present ✓; empty-string env ✗; duplicate scope ✗; quoted `.env` ...
- corner-prober: Policy grants: identity scopes ✓; `default.scopes` ✗; empty-grant vacuity ✗
- corner-prober: Pin: 40-hex both sides ✓; second/earlier occurrence ✗; unanchored match ✗
- corner-prober: Scripts: `*.sh` top-level ✓; heredoc payload ✗; non-`.sh` deploy scripts ✗
- curator: **must-fix — the guard skips `policy.json`'s highest-consequence grant surface, but the doc claims it is covered.**...
- curator: **should-fix — the pin guard does not exercise the exported identifier it exists to protect.** `src/endo/captp-clie...
- curator: **should-fix — `SCOPES` is now a load-bearing 4-site contract with nothing saying so at the declaration.** `test/de...
- engine-realist: Reviewed the diff at `/home/kris/garden/scratch/project-wt-minion-town-endo-b3-daemon-deploy-verify-dccf0e2b` (`1393c...
- fast-checker: ## Juror: fast-checker — PR #32 (kriscendobot/minion.town)
- integrator: Verified: the new suite passes locally (28 tests), and I checked it against the repo's existing structure — the pre...
- locksmith: ## Juror: locksmith — PR #32 (kriscendobot/minion.town)
- migrator: Read the brief, ran the diff, verified every claim against the tree (and ran the new suite: 28/28 green).

## Round 2 — head `1393ca1b`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=comment breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=must-fix engine-realist=must-fix fast-checker=must-fix gateway=comment integrator=must-fix locksmith=comment migrator=must-fix packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=pass spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=comment typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR #32 (kriscendobot/minion.town)
- assessor: ## Per-juror block — assessor (PR #32, kriscendobot/minion.town)
- breaker: `deploy/aws/scripts/deploy-endo-daemon.sh:97-180` — the entire remote payload is an unquoted `read -r -d '' SCRIPT ...
- breaker: `test/deploy-coherence.test.ts:104-113` — the policy.json guard is **one-directional** (granted ⊆ known) and so c...
- breaker: `test/deploy-coherence.test.ts:100` — the test reads only `policy.identities`, never `policy.default`. `config/poli...
- breaker: `DEPLOYMENT.md:622-627` newly asserts a CD **ordering** invariant (daemon before app). Nothing in the diff guards it;...
- breaker: `test/deploy-coherence.test.ts:36` — `import.meta.dirname` needs Node ≥20.11, but `package.json` declares `engine...
- breaker: `test/deploy-coherence.test.ts:136-138` — `expect(...).not.toThrow()` discards bash's stderr, so a failure names th...
- corner-prober: ## Juror: `corner-prober` — PR #32 (`kriscendobot/minion.town`)
- curator: **should-fix — the scope vocabulary still has no canonical exported home.** `src/config.ts:126` hard-codes `"mcp/to...
- curator: **should-fix — `policy.json`'s `default.scopes` is an ungated grant surface.** `test/deploy-coherence.test.ts:96` w...
- curator: **comment-only — the `granted` loop can pass vacuously.** Same block: if `identities` is empty or the `scopes` key ...
- curator: **comment-only — `PINNED_ENDO_COMMIT`'s literal form is now load-bearing.** `test/deploy-coherence.test.ts:113` mat...
- engine-realist: ## engine-realist — PR #32 (kriscendobot/minion.town)
- fast-checker: ## Juror: fast-checker — PR #32 (kriscendobot/minion.town)
- integrator: `test/deploy-coherence.test.ts:6-21` and `DEPLOYMENT.md:635` attribute the guards to a history that did not happen. T...
- integrator: `test/deploy-coherence.test.ts:104-116` hand-parses `config/policy.json` behind an inline `as { identities: { scopes:...
- integrator: `test/deploy-coherence.test.ts:36` uses `import.meta.dirname`, added in Node 20.11, against `package.json` `engines: ...
- integrator: PR description § The added guard: "219 passed | 3 skipped (up 4 from the new file)". Measured here: 191 baseline →...
- integrator: `deploy-cognito-github-idp.sh:18` / `deploy-cognito-siwe-idp.sh:31` carry `# shellcheck disable=SC2206` — the proje...

## Round 3 — head `1393ca1b`

seat verdicts (28): archivist=comment assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=must-fix fast-checker=must-fix gateway=pass integrator=must-fix locksmith=must-fix migrator=comment packager=must-fix prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=must-fix wire-watcher=must-fix
must-fix items (20):
- assessor: Reviewed the committed diff only (`HEAD~1...HEAD`: `DEPLOYMENT.md` + new `test/deploy-coherence.test.ts`); the workin...
- breaker: ## Juror: breaker — PR #32 (kriscendobot/minion.town)
- corner-prober: **Shadowing guard is *form*-specific — the claimed contract fails.** Docblock claims duplicates are rejected "which...
- corner-prober: **Pin parse is first-match, not `soleCapture`.** Appending `ENDO_COMMIT="0000…0"` (bash last-wins ⇒ wrong artifac...
- corner-prober: **Heredoc check covers only the first `<<EOF`.** Appending a second `read -r -d '' SECOND <<EOF` whose body is `if [ ...
- corner-prober: **The expansion executes repo text.** `bash -c "cat <<EOF…"` on an *unquoted* heredoc: I injected `$(touch /tmp/b3-...
- corner-prober: **`describe.skipIf(!hasBash)` vacuums silently** — all 11 syntax tests, *including* the "finds the deploy scripts" ...
- corner-prober: **`.env` quoting form unsupported.** `MCP_SCOPES_SUPPORTED="…"` yields `'"mcp/tools'`, `'mcp/guest"'` — a confusi...
- corner-prober: `mkdtempSync` never cleaned — 836 `/tmp/b3-bash-n-*` accumulated (removed). `summary-fix`, low.
- engine-realist: Verified locally: 31 tests pass, the docblock's claims about the current script hold (no unescaped `$(…)`/backticks...
- fast-checker: ## Juror: fast-checker — PR #32 (kriscendobot/minion.town)
- integrator: **should-fix — the heredoc reach covers one script; the idiom is fleet-wide.** `test/deploy-coherence.test.ts:229` ...
- integrator: **should-fix — the CDK exclusion is narrated, not asserted.** `infra/lib/minion-town-stack.ts:105-110` documents th...
- integrator: **should-fix — the description leads with an audit of already-merged commits.** The merge-commit reader gets five "...
- integrator: **comment-only — commit `71b30da` is process-referential and mistyped.** "address panel — …" names the review, ...
- integrator: **comment-only — the syntax third can self-disable silently.** `describe.skipIf(!hasBash)` (`:204`) drops all 22 sc...
- locksmith: **`test/deploy-coherence.test.ts:238` — the heredoc guard grants shell-execution authority to deploy-script text.**...
- locksmith: **`test/deploy-coherence.test.ts:148` — `expect([...granted]).toContain(SCOPES.GUEST)` ratchets a capability grant ...
- locksmith: **`test/deploy-coherence.test.ts:107` — `appDefault` must equal the whole `SCOPES` vocabulary.** This forecloses "d...
- locksmith: `infra/lib/minion-town-stack.ts:111` is exempted by comment and drifts *narrower* (no `mcp/guest`) — fail-closed, s...

## Round 4 — head `71b30da4`

seat verdicts (18): archivist=comment assessor=must-fix benchmarker=comment breaker=comment curator=comment engine-realist=comment integrator=comment locksmith=must-fix migrator=must-fix packager=must-fix prover=must-fix purist=comment saboteur=comment spec-keeper=comment stylist=must-fix typist=pass warden=comment wire-watcher=comment
must-fix items (20):
- assessor: `bashSyntaxCheck` (`:203`) leaks one `mkdtempSync` directory per script (~23/run), never removed. Cosmetic: temp file...
- assessor: Failure output is adequate — vitest surfaces bash's stderr with a usable line number. For the heredoc-body case, li...
- assessor: `ROOT` via `fileURLToPath(import.meta.url)` + two `..` (`:52`) is correct, and the stated Node <20.11 rationale check...
- assessor: `infra/lib/minion-town-stack.ts:105` comment and the DEPLOYMENT.md rewrite both accurately narrow prior overclaims. T...
- locksmith: Verified the key claim empirically (a `cat <<EOF` expansion does execute an unescaped `$(...)`), and read `config/pol...
- migrator: **should-fix — `mcp/guest` grant assertion pins a B3-provisional fixture, not an invariant.** `test/deploy-coherenc...
- migrator: **should-fix — the guarded grant surface is the one about to become secondary.** Every scope in `policy.json` is ch...
- migrator: **should-fix — the heredoc reconstruction executes deploy-script code at CI time.** `bash -c "cat <<EOF\n…"` with...
- migrator: **should-fix — heredoc coverage is hard-coded to one script.** Nine peers ship the same opaque-to-`bash -n` bodies ...
- migrator: **comment-only — `describe.skipIf(!hasBash)`** green-lights a bash-less host. CI is `ubuntu-latest` (`.github/workf...
- packager: **must-fix** — `test/deploy-coherence.test.ts:171-176`: `bashSyntaxCheck` calls `mkdtempSync` per script and never ...
- packager: **should-fix** — `test/deploy-coherence.test.ts:214`: the heredoc extractor `/<<EOF[^\n]*\n([\s\S]*?)\nEOF$/m` is *...
- packager: **should-fix** — same test: the on-box check is hard-coded to one script, but **eight** siblings ship the identical...
- packager: **should-fix** — `:169-175`: syntax errors now surface the temp path, not the real script name. Report `scriptFile`...
- packager: **comment-only** — `71b30da` is typed `test(deploy)` but touches `infra/lib/minion-town-stack.ts:105-110` (non-test...
- packager: **Changeset:** none required. Repo is `private: true` with no `.changeset/`; test + doc + comment changes are not use...
- packager: Prose accuracy verified: `## Provider portability boundary` exists (`DEPLOYMENT.md:10`), the "~45%" heredoc claim is ...
- prover: All ten "does it redden" mutations pass; two coverage gaps reproduce green. Worktree restored clean.
- stylist: `test/deploy-coherence.test.ts:206` — the filter callback parameter is a bare single-letter `f` (`.filter((f) => f....
- stylist: `test/deploy-coherence.test.ts:238` — `onBox` is an adjectival phrase, not a name for what the value is: the *expan...
