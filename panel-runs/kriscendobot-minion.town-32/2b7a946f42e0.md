---
kind: panel-run
repo: kriscendobot/minion.town
pr: 32
panel_kind: code
base_ref: 6368346
rounds: 2
disposition: error
must_fix_total: 31
appellate_ran: false
appellate_proposals: 0
epoch:
run_id: 2b7a946f42e0
recorded_by: endolin-garden-ece02cb4
---

# Panel run — kriscendobot/minion.town #32 (code)

Terminal disposition: **error** after **2** round(s).

## Round 1 — head `71b30da4`

seat verdicts (28): archivist=must-fix assessor=must-fix benchmarker=pass breaker=must-fix changeset-auditor=pass corner-prober=must-fix coverage-auditor=comment curator=pass engine-realist=pass fast-checker=pass gateway=comment integrator=must-fix locksmith=must-fix migrator=must-fix packager=pass prover=must-fix pruner=must-fix purist=must-fix releaser=pass saboteur=must-fix scribe=must-fix spec-keeper=must-fix stylist=must-fix surfacer=must-fix transplanter=must-fix typist=comment warden=comment wire-watcher=must-fix
must-fix items (20):
- archivist: ## Juror: archivist — PR kriscendobot/minion.town#32
- assessor: ## Juror: assessor — PR #32 (kriscendobot/minion.town), base 6368346
- breaker: All four falsification attacks confirmed against a green suite; working tree restored clean.
- corner-prober: **Unquoted `Environment=` shadow defeats the sole-match guard** — `unitScopes` matches only `^Environment="MCP_SCOP...
- corner-prober: **`ENDO_COMMIT` is first-match, not sole-match** — `test/deploy-coherence.test.ts:181` uses non-global `.match`; sh...
- corner-prober: **The heredoc reconstruction executes unescaped command substitution** — `:233` shells out `cat <<EOF\n${body}\nEOF...
- corner-prober: **Only the first `<<EOF` is checked** — non-global `.match` at `:225`. Appending a second SSM heredoc containing `i...
- corner-prober: **`describe.skipIf(!hasBash)` (`:213`) can silently void the whole shell suite**, sentinel included — zero failures...
- integrator: ## integrator
- locksmith: **`test/deploy-coherence.test.ts:238` — the heredoc-reach check grants shell-execution authority to repo text, on a...
- locksmith: **`:148` — the `mcp/guest` assertion unions across all identities, so it cannot see *which* principal holds the hig...
- locksmith: **`:107` — `advertised == entire known vocabulary` is an anti-attenuation ratchet.** Equality usefully catches a st...
- locksmith: **`:151-156` — the `default.scopes` guard is the right shape.** Pinning `default.scopes` to `[]` turns policy.json'...
- migrator: **should-fix — the pin guard reads first-match, so a shadowing `ENDO_COMMIT=` survives.** `test/deploy-coherence.te...
- migrator: **comment-only — exact-equality couples the authZ vocabulary to the advertised set.** `expect(appDefault).toEqual([...
- migrator: **comment-only — the unit guard forbids a legitimate simplification.** Deleting the redundant `Environment="MCP_SCO...
- migrator: **comment-only — heredoc reach is single-script.** Eight other scripts ship on-box `<<EOF` bodies, five of them (`d...
- prover: **must-fix** — `DEPLOYMENT.md:622` newly asserts daemon-before-app CD ordering is *the* sequencing mechanism ("deli...
- prover: **should-fix** — same paragraph claims `SupplementaryGroups=endo-daemon` grants socket access statically. Deleting ...
- prover: **should-fix** — `test/deploy-coherence.test.ts:227` matches the *first* `<<EOF` only, contradicting the `soleCaptu...

## Round 2 — head `71b30da4`

seat verdicts (11): archivist=comment assessor=comment curator=comment locksmith=comment migrator=comment packager=comment prover=comment saboteur=comment stylist=must-fix typist=comment warden=comment
must-fix items (11):
- stylist: `test/deploy-coherence.test.ts:206` — `.filter((f) => f.endsWith(".sh"))` uses a bare one-letter abbreviation for a...
- stylist: `test/deploy-coherence.test.ts:51` — `ROOT` does not carry into the new code the convention its own comment cites. ...
- stylist: `test/deploy-coherence.test.ts:52` — `read` is the `data`/`helper` smell in verb form: it is not a general read, it...
- stylist: `test/deploy-coherence.test.ts:198` vs `:205` — `directory` names two unrelated things: an absolute temp directory ...
- stylist: `test/deploy-coherence.test.ts:164,171` — `scriptPin` / `appPin` hold `RegExpMatchArray | null`, not pins; the pin ...
- stylist: `test/deploy-coherence.test.ts:197` — parameter `tag` plays the same role as `soleCapture`'s `label` (`:68`): a hum...
- stylist: `test/deploy-coherence.test.ts:58` — `scopeSet` returns `string[]`, while `granted` at `:143` is a real `Set`. The ...
- stylist: `test/deploy-coherence.test.ts:238` — `onBox` is an adjective naming a string of shell source. `onBoxScript` (or `e...
- stylist: No renames in this diff — it is additive across all three files, and `MCP_SCOPES_SUPPORTED` is left untouched at `i...
- stylist: No abbreviation findings against `captpClientSource`, `mkdtempSync`, `tmpdir`, `SSM`, `bash -n`: platform and domain ...
- stylist: Docblocks and signatures agree throughout (`soleCapture`, `bashSyntaxCheck`, `envScopes`, `unitScopes`); no name-vers...
