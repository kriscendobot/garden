---
ts: 2026-06-06T05:25:13Z
kind: result
role: builder
host: endolinbot
project: endo-but-for-bots
dispatch_root: /home/kris/dispatches/builder--d94d05
to: steward
refs:
  - entries/2026/06/06/051300Z-result-researcher-ec33c4.md
  - entries/2026/05/15/013547Z-result-liaison-2264a5.md
  - entries/2026/05/21/054802Z-result-weaver-7d7d5e.md
  - entries/2026/06/03/012906Z-result-weaver-496105.md
  - https://github.com/endojs/endo-but-for-bots/pull/426
---

# result: builder 101dc2 — sync actual/master into llm via PR #426

Merged `endojs/endo@master` (tip `4a04d078b`) into the bot fork's `llm` (roadmap) branch via a fork-side DRAFT PR, copying the canonical shape of PR #257 from the 2026-05-15 precedent. CI is the validation gate; a follow-on shepherd dispatch handles the green-up.

## SHAs

- Pre-merge llm tip: `2bd9e0cbb` (`docs(designs): daemon-git follow-ups …, #370`).
- Absorbed upstream tip: `4a04d078b` (`feat(compartment-mapper): Host module exits (#2422)`).
- Sync-branch tip: `618046786` (`chore: merge actual/master into llm (2026-06-06)`).
- Parents of the merge commit: `2bd9e0cbb` (llm) and `4a04d078b` (endo-upstream/master).

## Opened PR

`endojs/endo-but-for-bots#426`: <https://github.com/endojs/endo-but-for-bots/pull/426>

- Base: `llm`. Head: `merge/actual-master-into-llm-20260606`. State: OPEN, DRAFT.
- Title: `chore: merge actual/master into llm (2026-06-06)`.
- Body cites absorbed upstream tip, conflicts resolved file-by-file, and CI as validation.

## Conflict resolutions (by file)

Nine files entered conflict on `git merge endo-upstream/master --no-ff`. All other files auto-merged.

- **.github/workflows/release.yml**: HEAD pinned `changesets/action@6a0a831…` (v1.7.0); master bumped to `changesets/action@63a615b9c…` (v1.8.0). Took upstream's newer pin. Net change is the action version bump; both sides preserve the same hash-pin + comment discipline.
- **package.json** (root, `devDependencies`): kept the curated bot-side block (`eslint-config-prettier ^10.1.8`, `eslint-import-resolver-exports ^1.0.0-beta.5`, `eslint-plugin-import ^2.31.0`, `eslint-plugin-jsdoc ^62.5.5`, `lerna ^8.2.4`, `prettier ^3.5.3`). Did not adopt master's `eslint-plugin-unicorn ^56.0.1`, `eslint-plugin-import: catalog:dev`, or its other `^9.x` / `^50.x` / `^3.8.x` downgrades. Matches the 2026-06-03 weaver's precedent on the same axis.
- **packages/eslint-plugin/lib/configs/internal.js**: folded both intents. Kept bot-side three rule disables (`no-void`, `no-use-before-define`, `no-nested-ternary`) and added master's `import/no-unresolved` ava workaround and `unicorn/numeric-separators-style` rule. The `unicorn` plugin reference and the package's new `eslint-plugin-unicorn` peerDep auto-merged through cleanly in `packages/eslint-plugin/package.json`. `node --check` passes.
- **packages/ocapn-noise/test/failures.test.js**: three conflict regions, same shape. Kept bot-side IK-handshake test rewrite (`prefixedSyn`/`initiatorReadSynack` API introduced four weeks ago in PR #59 layer-2) and dropped master's older `_initiatorReadSynackWriteAck`/`responderReadAck` form. Propagated master's numeric-separator convention to bot-side literals: `65535` → `65_535`, `65536` → `65_536` (two sites). `node --check` passes.
- **packages/ocapn/test/codecs/passable.test.js**: two conflict regions. Kept bot-side `for (const codec of AllCodecs)` parameterized rewrite (PR #59 layer-1 codec-injection refactor); dropped master's older `makeSyrupReader`/`makeSyrupWriter` form. Propagated `3.141_59` numeric-separator convention to two bot-side `3.14159` literals. `node --check` passes.
- **packages/ocapn/test/codecs/subtypes.test.js**: two conflict regions, same shape as passable.test.js. Kept bot-side codec parameterization; propagated `1_000_000n` and `123_456_789_012_345_678_901_234_567_890n` numeric-separator convention to two bot-side `1000000n` / `123456789012345678901234567890n` literals.
- **packages/ocapn/test/python-test-suite/index.js**: kept the prior weaver's resolution (the broken `client.registerNetlayer` block stays dropped because `client` is an undefined reference from the in-flight master refactor — documented in the 2026-06-03 result). Propagated master's `22_046` numeric-separator convention to the surviving `specifiedPort` literal. `node --check` passes.
- **packages/zip/src/format-writer.js**: one large conflict region. Dropped master's duplicate trailing block of legacy functions (`encodeFile` / `compressFileWithStore` / second `externalFileAttributes` / `makeFileRecord` / older `writeZip`). The bot-side modernized API (`fileToRecord` arrow, separate `compressionMethod` handling, single `externalFileAttributes`) supersedes them. Propagated `0o10_0000` numeric-separator convention to the surviving `externalFileAttributes` literal. `node --check` passes.
- **yarn.lock**: skipped during merge, regenerated by `corepack yarn install` against the merged tree. Net change: two lines (the new `eslint-plugin-unicorn ^56.0.1` peerDep declaration on the `@endo/eslint-plugin` workspace entry).

## Rerere cache hits

The merge command emitted nine `Recorded preimage for '…'` lines (one per conflicted file) on the merge invocation, and nine matching `Recorded resolution for '…'` lines on the commit. No prior cache hits surfaced (the 2026-06-03 sync was a different conflict set; the 2026-05-21 cache hits the prior weaver noted are on workflow files and yarn.lock paths that did not enter conflict this time).

## Out-of-scope drift surfaced for follow-on

Two items the dispatch told me to surface rather than fix under the sync banner:

- **`makeClient` / `registerNetlayer` API drift** between upstream `packages/ocapn/src/client/` and the bot-side `makeOcapn` shape. The 2026-05-21 weaver flagged this; the 2026-06-03 weaver re-flagged it. Master continues to evolve `client/index.js`'s `registerNetlayer` surface (now also referenced in `packages/ocapn/test/netlayer-tcp-syrup.test.js`, a master-side test file the bot has carried verbatim through two syncs). The bot-side `client/index.js` does not export `registerNetlayer`. A builder dispatch to port master's `makeClient`/`registerNetlayer` rewrite into llm would resolve the standing gap; the conductor's auto-CI on PR #426 will surface the failure as evidence.
- **`eslint-plugin-unicorn` peerDep without root devDep**. The merged `packages/eslint-plugin/package.json` declares `eslint-plugin-unicorn ^56.0.1` as a peerDep but the root `package.json` does not list `eslint-plugin-unicorn` (the prior sync's precedent kept the root curated and skipped unicorn). `corepack yarn install` warned `YN0002: root-workspace-0b6124@workspace:. doesn't provide eslint-plugin-unicorn (pde1a41), requested by @endo/eslint-plugin.` The peerDep declaration is benign at install (yarn warns and proceeds) and the `unicorn/numeric-separators-style` rule will fire only when a config pulls eslint-plugin-unicorn in transitively. If the rule actually loads in this lockfile, a follow-on dispatch can add `eslint-plugin-unicorn` to the root devDependencies; until then, the precedent stands.

## Validation

- All eight non-lockfile conflict resolutions syntax-check (`node --check` for `.js` files; manual inspection of `.yml` and `.json` for those).
- Root `package.json` round-trips through `python3 -c "import json; json.load(open(...))"`.
- No conflict markers remain in tracked files (only `node_modules/` and the pre-existing `designs/cli-edit-verb.md` literal-search/replace example contain `<<<<<<<` strings).
- Per-package tests not run (per dispatch scope: CI is the validation gate via shepherd).
- Pre-push gates not run (per the merge-commit shape; the 2026-05-15 PR #257 precedent and the 2026-06-03 direct-push precedent both skipped the gate because gate findings on master content the bot does not own are out of the merge's scope).

## Self-improvement

Self-improvement: nothing this time. The role's "merge upstream into roadmap branch, copy PR #257 shape, surface API drift rather than fix it" played as written; the researcher's `## Library and project references` section pointed at exactly the precedents and skills I consulted, with no gaps in coverage. The rerere cache silently absorbed nine new preimages for the next sync's benefit. The two surfaced out-of-scope items (`makeClient` / `registerNetlayer` API drift and the `eslint-plugin-unicorn` peerDep gap) are recurring across syncs and could merit a project-level topic file (`journal/projects/endo-but-for-bots/master-into-llm-sync.md` per the researcher's *Open questions*), but that is queued for the librarian / scholar rather than something a builder result entry resolves.
