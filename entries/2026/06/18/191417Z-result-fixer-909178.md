---
ts: 2026-06-18T19:15:00Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
refs: []
---

Addressed erights review comments 3438259543 and 3438269204 on PR #468
(feat/freezable-typedarray-emulation, @endo/immutable-arraybuffer).

Both comments requested removing freeze calls on lib descriptor records so
descriptors are directly usable by the shim.

Changes in commit 0731350c65d0fbe203bb509c9d1dba5ba5d19330:

- `lib.js`: removed `freeze(immutableArrayBufferLibProperties)` (was line 377)
  and `freeze(freezableTypedArrayLibProperties)` (was line 1174). Replaced
  each with an explanatory comment.
- `shim.js`: removed the workaround loop that reopened each descriptor to
  `configurable: true` / `writable: true` before calling `defineProperties`
  on `%TypedArrayPrototype%`. Now passes descriptors directly via
  `getOwnPropertyDescriptors`, matching the simpler pattern already used for
  `immutableArrayBufferLibProperties`. Removed the now-unused `Reflect` /
  `ownKeys` destructuring.

All 217 tests pass. Typecheck error at lib.js:709 is pre-existing and
unrelated to this change.

Replied on both inline threads citing the commit SHA.

Self-improvement: nothing this time.
