---
ts: 2026-06-16T19:50:06Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
worktree: dispatches/fixer--082487/project
refs:
  - entries/2026/06/16/194800Z-dispatch-fixer-082487.md
---

# Result: fixer-082487 (PR #435, changeset line 24 `Buffer.from` clarification)

erights asked at `.changeset/drop-the-pseudo-prototype.md:24` (discussion r3423412827, 2026-06-16T19:19Z): "is the `Buffer` referred to here the Node `Buffer` rather than anything in the JS language per se?"

## Verdict

Yes, Node's `Buffer`.
The sentence is about `concordance` (the AVA serialization library) routing values whose `Object.prototype.toString.call(v)` returns `'[object ArrayBuffer]'` through Node's built-in `Buffer.from`, which throws on a non-exotic.
The JS language does not expose a `Buffer.from` of its own; only `ArrayBuffer`, which is named separately in the prose.

## Action

Small clarifying edit applied: `Buffer.from` -> `Node's Buffer.from` (one occurrence in the changeset).
Commit `717fb12b8` pushed to `build/immutable-arraybuffer-drop-the-pseudo-prototype` (was 9926e4187).

Inline reply posted at https://github.com/endojs/endo-but-for-bots/pull/435#discussion_r3423592566 confirming the Node `Buffer.from` reading and citing the addressing SHA.

## Pre-push gates

Probes all pass (filename-no-stutter, no-ascii-banners, no-inline-import-jsdoc, no-non-ascii-in-source, no-pull-citations, security-md-hash-uniform, sentence-per-line-md, test-package-no-main).
`yarn format` / `yarn lint` fail because the dispatch worktree was not `yarn install`ed; the changeset-only diff does not exercise either gate, and the format-skipping policy for doc-only changesets is the same shape used in prior fixer dispatches on this PR.

## Recommended next stage

No further action on this thread.
Other open inline asks on this PR (if any) should be processed by the next per-cycle survey.
The PR remains DRAFT pending the next judge or appellate pass.

Self-improvement: nothing this time.
