---
ts: 2026-06-07T05:51:00Z
kind: result
role: fixer
host: endolinbot
repo: endojs/endo-but-for-bots
project: endo-but-for-bots
prs:
  - repo: endojs/endo-but-for-bots
    pr: 403
    role: target
refs:
  - entries/2026/06/07/051600Z-dispatch-fixer-741577.md
  - entries/2026/06/07/055030Z-message-fixer-liaison-403-overscope.md
  - https://github.com/endojs/endo-but-for-bots/pull/403
  - https://github.com/endojs/endo-but-for-bots/pull/403#pullrequestreview-4444439085
---

# result: fixer 741577 -- applied kriskowal's continuation review on PR #403 (package rename + 5 inline + partial next-phase fold)

## Branch tips

- Pre: `b7e7bd932` (chore: Update yarn.lock; previous tip).
- Post: `9da73262d` (chore: Update yarn.lock; this dispatch's tip).

## Commits landed (5 commits, `feat/registry-capability`)

1. `c4fe1680e` -- `refactor(exo-npm)!: rename @endo/registry-capability and reference backend scope (#403)`. Package directory `packages/registry-capability/` -> `packages/exo-npm/`. Package name `@endo/registry-capability` -> `@endo/exo-npm`. Reference backend `makeJsReferenceRegistry` -> `makeNpmReferenceRegistry`; exo `JsReferenceEndoRegistry` -> `NpmReferenceEndoRegistry`. Root `tsconfig.composite.json`, `.gitignore` whitelist, README, CHANGELOG, package.json `name`/`homepage`/`directory` updated. Design document slug `designs/registry-capability.md` preserved (the design names the capability shape, not the package).
2. `c28016e11` -- `refactor(exo-npm,mem-cas): factor out @endo/mem-cas; rename CasStoreInterface to CasInterface (#403)`. New `packages/mem-cas/` package owns the `CasStore` interface (drops redundant trailing `Store`: `CasInterface`/`M.interface('Cas',...)`), `makeMemoryCasStore`, `makeRetentionLinkSet`, `sha256HexWebCrypto`. `@endo/exo-npm` declares the runtime dependency. Reference backend imports CAS types from `@endo/mem-cas`. Tests import the CAS surface from `@endo/mem-cas/store.js` and `@endo/mem-cas/store-web-powers.js`.
3. `f1c5d316b` -- `feat(exo-npm): accept caller-supplied PackageCacheTable with dewey-decimal version sorting (#403)`. `PackageCacheRow`, `PackageCacheTable` interface types. `makeMemoryPackageCacheTable()` in-memory reference (with `names()` for the registry's `list()` enumeration). `makeNpmReferenceRegistry({ packages })` accepts a caller-supplied table. `ResolveHookContext` carries the cache table alongside `cas` and `retentionLinks`. 5 new tests, 1 expanded.
4. `9954c2b7e` -- `docs(mem-cas): note Node.js-as-web-emulation suitability in store-web-powers (#403)`. Filename stays per maintainer's guidance; comment clarifies "web" names the API surface (`globalThis.crypto.subtle`) rather than the host platform.
5. `9da73262d` -- `chore: Update yarn.lock`.

## "Next implementation phase" fold

**Surfaced as over-scope to liaison** per the dispatch brief's explicit authorization. See `entries/2026/06/07/055030Z-message-fixer-liaison-403-overscope.md`. The design's Phase 1 names a daemon-side wiring layer (`packages/daemon/src/registry.js`, `RegistryFormula`, `@registry` host special name, `HostFormula.registry` migration pass over already-formulated hosts, plus the Node-only test matrix) that is a sibling feature PR rather than a fixer amendment. The SQLite-backed `PackageCacheTable` implementation (the second over-scope item from the same review) travels in the same staging.

## New package directories

- `packages/exo-npm/` (renamed from `packages/registry-capability/`).
- `packages/mem-cas/` (new, factored out from `packages/exo-npm/`).

## Cross-reference rename sweep

- 0 lingering `@endo/registry-capability` references in code (verified via `grep -rn '@endo/registry-capability' --include='*.{js,ts,json,md}'`).
- 0 lingering `packages/registry-capability` references in JSON/code (verified).
- 1 historical reference in `packages/mem-cas/CHANGELOG.md` documenting the factor-out origin (intentional).
- Design document references to `designs/registry-capability.md` preserved across all in-package and in-design docs (the design slug stays; the package rename is orthogonal).

## Inline thread replies (5)

- `3368779961` (store-web-powers Node.js note): https://github.com/endojs/endo-but-for-bots/pull/403#discussion_r3368823359
- `3368782942` (mem-cas factor-out + daemon-CAS unification staging): https://github.com/endojs/endo-but-for-bots/pull/403#discussion_r3368823590
- `3368788764` (CasStoreInterface -> CasInterface): https://github.com/endojs/endo-but-for-bots/pull/403#discussion_r3368823762
- `3368790256` (Npm vs Js scope): https://github.com/endojs/endo-but-for-bots/pull/403#discussion_r3368823857
- `3368791874` (sqlite-backed reference-backend partial address + deferral): https://github.com/endojs/endo-but-for-bots/pull/403#discussion_r3368823994

## Top-level summary comment

https://github.com/endojs/endo-but-for-bots/pull/403#issuecomment-4641577908. Cites each addressing SHA, names the two deferred items in the *Deferred* section, and explicitly forgoes re-requesting review per the dispatch brief's "NOT re-request review" authorization.

## Verification

- `@endo/exo-npm`: 18 tests pass (`corepack yarn test` in `packages/exo-npm`).
- `@endo/mem-cas`: 11 tests pass.
- `yarn lint` clean for both packages.

## Self-improvement: nothing this time.
