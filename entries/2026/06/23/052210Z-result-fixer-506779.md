---
ts: 2026-06-23T05:22:10Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
refs:
  - entries/2026/06/23/052034Z-dispatch-fixer-506779.md
---

Restored vendored `packages/lal/agent-round.js` on PR #290
(`feat/lal-pi-harness`) per 0xpatrickdev's inline comment
`discussion_r3453545144` (2026-06-22T15:37:54Z).

**Root cause:** the retcon/rebase chain that landed ~3 hours before this
dispatch produced commit `ab3070cd4`, which deleted `agent-round.js` and
re-adopted `@endo/genie` at runtime, clobbering the vendor commit
`8055a33c3` (13 days ago, authored by `endolinbot` per kriskowal's
original PR #290 review).

**Restoration method:** option (a) — restored `agent-round.js` verbatim
from `8055a33c3` via `git show 8055a33c3:packages/lal/agent-round.js`.
No porting needed: the vendor commit had already adapted the file for
pi-agent-core 0.79 (ignoring `bashExecution`, `custom`, `branchSummary`,
`compactionSummary` message roles silently).

**Changes committed:**

- `37d9d15e6`: restore `packages/lal/agent-round.js`; switch
  `packages/lal/agent.js` import from `@endo/genie` to `./agent-round.js`;
  drop `@endo/genie: workspace:^` from `packages/lal/package.json`
  dependencies; update prose in `README.md` and `LAL-ARCHITECTURE.md`.
- `14affdaa5`: `chore: Update yarn.lock` (separate commit per policy).

**`@endo/genie` dep:** dropped. No remaining runtime import in lal.

**Tests:** `corepack yarn workspace @endo/lal test` — 26 passed, 1
skipped (simulator, requires `LAL_HOST`). No new test added; the hot
path is covered via `pi-agent-tools.test.js` which scripts a `PiAgent`
with tool calls through the same event loop `runAgentRound` drives.

**Push:** force-pushed with lease anchor `370f246e6` to
`origin/feat/lal-pi-harness`. New HEAD: `14affdaa5`.

**Inline reply:** `discussion_r3457200976`
(https://github.com/endojs/endo-but-for-bots/pull/290#discussion_r3457200976)

**Top-level summary:** `issuecomment-4775874810`
(https://github.com/endojs/endo-but-for-bots/pull/290#issuecomment-4775874810)

Self-improvement: nothing this time.
