---
ts: 2026-06-23T01:44:54Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
refs:
  - entries/2026/06/23/014454Z-result-fixer-c88d8f.md
---

RSVP on kriskowal's review 4548978783 of PR #475 (feat/narrow-bytearray-to-uint8).
Force-pushed from lease anchor ce8d05782; HEAD is 54a8e7bb0.

This dispatch resumed a prior fixer that aborted mid-flight (context limit, 222 tool
uses). Inline replies on comments 1-5 were posted in the prior run; this run posted
replies on comments 6-9, then the top-level summary, then re-requested review.

## Asks addressed

1. **r3456181991** (chacha12-fast-check-test/package.json) - Restored
   `"exports": {"./package.json": "./package.json"}`. Probe `test-package-no-main`
   conflicts with this but maintainer's directive takes precedence; surfaced in summary.

2. **r3456190636** (hex/src/encode.js line 35) - Removed `asUint8View` and broad-type
   acceptance. Both `jsEncodeHex` and `encodeHex` now take `Uint8Array` only. Removed
   `ArrayBuffer.isView` and `Reflect.apply` captures.

3. **r3456195720** (ocapn/src/client/util.js line 17) - Removed `toHex` wrapper.
   All call sites in `index.js` and `ocapn.js` now import `encodeHex` from `@endo/hex`
   directly.

4. **r3456203874** (ocapn/src/client/util.js line 55) - Created `@endo/ascii` package
   (`packages/ascii/`) with `encodeAscii`, `decodeAscii`, `strictDecodeAscii` sub-path
   exports matching the `@endo/utf8` shape. `decodeSwissnum` copies to mutable
   `Uint8Array` before decoding (shim compat). Added `@endo/ascii: major` to changeset.

5. **r3456206347** (ocapn/src/codecs/components.js line 86) - Narrowed `OcapnSignature`
   typedef's `r` and `s` from `ArrayBufferView | ArrayBufferLike` to `Uint8Array`.

6. **r3456208612** (ocapn/src/syrup/codec.js line 207) - Removed ArrayBuffer migration
   compat from `SyrupNumberPrefixCodec.write`; only `value instanceof Uint8Array`.

7. **r3456209964** (ocapn/src/syrup/codec.js line 411) - Removed `toBytes()` wrapper
   around `encodeUtf8(label)`; `writeBytestring` receives the `Uint8Array` directly.
   Removed now-unused `import { toBytes }`.

8. **r3456214799** (ocapn/src/syrup/compare.js line 3) - Removed misnamed
   `compareImmutableArrayBuffers` re-export. Call sites use local `compareUint8Arrays`.
   Test file rewritten for plain `Uint8Array` inputs.

9. **r3456216969** (ocapn/src/syrup/js-representation.js line 93) - Removed ArrayBuffer
   migration compat from `NumberPrefixCodecWithSelectorAsSymbol.write`; same pattern.

## Commits

- 84861330e: all 9 fixes + new @endo/ascii package
- 54a8e7bb0: tsconfig.composite.json drift (generated by build:types:gen)

## CI notes

All jobs green except `test-ocapn-python`, which failed with
`test_crossed_hellos_mitigation_outbound_aborts: TimeoutError: timed out` in the
01:37Z run. The same code (commit 84861330e) passed this test in the 00:20Z run.
The failure is a timing-sensitive flake in the Python interop test suite, not caused
by these changes. Re-review requested from kriskowal.

## Probe conflicts surfaced (not fixed)

- `test-package-no-main` vs restored exports in chacha12-fast-check-test
- `sentence-per-line-md` vs `security-md-hash-uniform` on SECURITY.md
