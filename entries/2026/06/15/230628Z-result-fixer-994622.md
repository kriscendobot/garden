---
ts: 2026-06-15T23:06:28Z
kind: result
role: fixer
worktree: dispatches/fixer--994622/project
repo: kriscendobot/agoric-sdk
project: agoric-sdk
refs:
  - entries/2026/06/15/230400Z-dispatch-fixer-994622.md
---

# Result: fixer 994622 (refine bundle-source cast per turadg)

## Outcome

Switched the JSDoc cast at `packages/SwingSet/tools/test-swingset.js:44` from
the local `EndoZipBase64Bundle` (from `../src/types-external.js`) to Endo's
parameterized `BundleSourceResult<'endoZipBase64'>` (from
`@endo/bundle-source`). This brings the file in line with the canonical
pattern already used in `packages/deploy-script-support/src/{writeCoreEvalParts,
cachedBundleSpec}.js` and `packages/agoric-cli/src/scripts.js`.

## SHAs

- Pre: `9216c5a936758c06c6b91e970b3ed30f8a885581`
- Post: `e435b5cdca334c4d2370383932254f33fbc2c371`

## Type change

In `packages/SwingSet/tools/test-swingset.js`:

- JSDoc `@import` block: dropped `EndoZipBase64Bundle` from the
  `../src/types-external.js` import, added a new
  `@import {BundleSourceResult} from '@endo/bundle-source';` line.
- Call-site cast (line 44): `Promise<EndoZipBase64Bundle>` -> `Promise<BundleSourceResult<'endoZipBase64'>>`
  (reflowed onto its own line for line-length).

Net diff: +5 / -2 in one file.

Verified: `@endo/bundle-source` is already a direct dependency of
`@agoric/swingset-vat` (`packages/SwingSet/package.json` line 42:
`"@endo/bundle-source": "^4.2.0"`); the parameterized
`BundleSourceResult<T extends ModuleFormat>` is re-exported from
`@endo/bundle-source` via `src/exports.d.ts` and lives in
`src/types.ts` lines 191-209 of the installed 4.2.x copy.

## lint:types

`corepack yarn workspace @agoric/swingset-vat lint:types` exits 0 cleanly.

Spot-checked `lint` too: 0 errors, 115 pre-existing `jsdoc/reject-any-type`
warnings unchanged.

## PR comment

Top-level summary posted on PR #5 @-mentioning @kriskowal:
https://github.com/kriscendobot/agoric-sdk/pull/5#issuecomment-4713196367

Cites turadg's inline ask, the parameterized type's location, the
canonical pattern, and the new head SHA.

## Authorizations exercised

- Append-push to `kriscendobot/agoric-sdk` branch `mirror/12527-endo-sync-refresh`.
- One top-level comment on `kriscendobot/agoric-sdk` PR #5.

Did NOT touch upstream `Agoric/agoric-sdk` and did NOT post on upstream PR
#12527, as per dispatch constraints.

## Out of scope

CI failures on this PR are deferred to a separate serial-fixer chain per
dispatch instructions; not pursued here.

## Recommended next stage

`next: fixer` Class B (the separate serial-fixer chain on CI failures, per
dispatch brief).

## Self-improvement

Nothing this time. The dispatch brief named the exact file, the exact line,
the prior fix SHA, and the canonical pattern via a hint about Endo's
parameterized type. Resolution to `BundleSourceResult<'endoZipBase64'>`
followed directly from cross-checking `@endo/bundle-source/src/types.ts`
and `exports.d.ts` plus a `grep` of agoric-sdk for existing call sites; no
process gap surfaced.
