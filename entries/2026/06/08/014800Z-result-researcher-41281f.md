---
ts: 2026-06-08T01:48:00Z
kind: result
role: researcher
host: endolinbot
project: endo-but-for-bots
refs:
  - entries/2026/06/08/013700Z-dispatch-researcher-41281f.md
  - https://github.com/endojs/endo-but-for-bots/pull/89
  - https://github.com/endojs/endo-but-for-bots/pull/89#pullrequestreview-4342320898
---

# result: researcher refinement for designer on PR #89 (apply CHANGES_REQUESTED + author scheduler design)

The proposed designer dispatch addresses kriskowal's CHANGES_REQUESTED review `4342320898` on `endojs/endo-but-for-bots#89` (`docs(designs): propose genie-integration`). Five inline asks plus a rebase: Prettier format at line 8, pet-store-as-memory framing rewrite at line 452, **author a new scheduler design as a prerequisite refactor and include it in this PR** at line 481, rename to "scheduler" at line 485, remove an irrelevant section at line 560, and rebase against current `llm` to pick up the upstream zizmor CI fix (endo#3297 merged into bot-master 2026-06-04). The refinement below points the designer at the existing library material so it does not reinvent (a) the genie / Pi-embedding worldview the design itself sits inside, (b) the daemon's current scheduling primitives (`timer` formula, `serial-jobs`, the genie-side `makeIntervalScheduler` prototype) and the already-named successor (`daemon-capability-timer` / interval-scheduler formula), or (c) the pet-store-as-typed-namespace precedent for the maintainer's "stand the agent on the pet store" framing.

```markdown
## Library and project references

### Library concepts and sections

- [endopi.md source page](../../library/sources/endo-but-for-bots--llm-designs-endopi.md) — the family keystone for the endopi-* cluster, including the Genie section that documents `packages/genie` 0.0.1 as the *third Endo-side surface* that embeds Pi rather than re-implementing it.
- [endopi / Genie: Pi inside Endo, and the four architectural contrasts](../../library/sections/endo-but-for-bots--llm-designs-endopi--genie-pi-inside-endo-and-the-four-architectural-contrasts.md) — the canonical exposition of the Genie surface that `designs/genie-integration.md` is the integration survey for. Names the observer / reflector subagent pair, the heartbeat autonomous executor, `makeIntervalScheduler` cron-style periodic prompts, the SOUL.md / HEARTBEAT.md Claw-compatible workspace, the §confinement-is-the-open-question framing (packages/sandbox podman/bwrap drivers OR a 9p filesystem server exporting endo's space). Also: *Genie is closer to pi-agent than to pi-coding-agent* — depends on the embedding-shaped SDK, not the cli-shaped one.
- [endopi / comparative pi-mapping with eight spinout-gaps and architectural contrasts](../../library/sections/endo-but-for-bots--llm-designs-endopi--comparative-pi-mapping-with-eight-spinout-gaps-and-architectural-contrasts.md) — the sister Lal/Fae section of the same keystone, useful for cross-referencing how the genie-integration proposal lines up with the §Feature-by-Feature Mapping tables and the §Already-available / §Designed / §Endo-specific-advantages lists.
- [daemon-capability-bank / family-of-designs-and-six-design-principles](../../library/sections/endo-but-for-bots--llm-designs-daemon-capability-bank--family-of-designs-and-six-design-principles.md) — names **Timer / scheduling** (`daemon-capability-timer.md`, *Planned*) as one of the nine sibling capabilities. The new scheduler design at PR #89's line 481 sits in this slot. The six §Design Principles (capabilities-not-configurations, recursive attenuation, caretaker separation, optional deny patterns, LLM discoverability, existing Endo patterns) are the criteria the scheduler design must satisfy to be capability-shaped.
- [daemon-agent-tools / dir-shell-git-capabilities-and-dynamic-tool-registration](../../library/sections/endo-but-for-bots--llm-designs-daemon-agent-tools--dir-shell-git-capabilities-and-dynamic-tool-registration.md) — the concrete-tool-shapes design that bridges capability-bank with Claw-like AI agents. Useful as a worked example of how a per-capability design lays out tool shapes, the §pet-name capability granting pattern (`endo grant fae fs /home/user/project`), and the §dynamic tool-discovery pattern.
- [daemon-mount / two-formula-type-split-with-shared-exo-interface-and-realpath-at-operation-time-confinement](../../library/sections/endo-but-for-bots--llm-designs-daemon-mount--two-formula-type-split-with-shared-exo-interface-and-realpath-at-operation-time-confinement.md) — the §live-mutable-filesystem-as-capability primitive (`mount` external-host-managed; `scratch-mount` daemon-managed). `ScratchMount` is what genie-integration §2 already names as the daemon-native replacement for the `vfs-node` memory backing. The §two-formula-type-split with §lifecycle-asymmetry-vs-implementation-symmetry is also a candidate shape for the scheduler (host-managed vs guest-revocable intervals).
- [daemon-capability-bus / daemon-as-message-router-with-envelope-protocol-and-handle-rewriting](../../library/sections/endo-but-for-bots--llm-designs-daemon-capability-bus--daemon-as-message-router-with-envelope-protocol-and-handle-rewriting.md) — the worldview-shift to move the daemon into a dedicated Go/Rust binary. Tangential to genie-integration but relevant context for any scheduler proposal that touches the boundary between worker and supervisor.
- [chat-spaces-gutter / space-model-and-persistence](../../library/sections/endo-but-for-bots--llm-designs-chat-spaces-gutter--space-model-and-persistence.md) — the canonical precedent for the maintainer's *"stand the agent fully on the pet store"* framing at PR #89 inline 452. The chat-spaces design encodes a *typed namespace* on top of the daemon's untyped `pet-store.write` / `list` / `lookup` / `remove` + `host.storeValue` primitives, with *no new daemon API*. The same shape applies to memory: observations / reflections / profile become a typed sub-namespace of the agent's own pet store, not a vfs-backed markdown tree.
- [space concept page](../../library/concepts/space.md) — the *pet store holds formula keys not locators* idiom + the *typed namespace over untyped pet-store* idiom, both relevant to the pet-store-as-memory rewrite at inline 452.
- [dehydrate-hydrate concept page](../../library/concepts/dehydrate-hydrate.md) — the pet-store-holds-formula-keys discipline. Useful when the rewrite at inline 452 distinguishes what the agent stores (formula references, values) from what is ambient (a host filesystem path).
- [readme / milestones-overview](../../library/sections/endo-but-for-bots--llm-designs-readme--milestones-overview.md) and [readme / timeline-and-strategic-items](../../library/sections/endo-but-for-bots--llm-designs-readme--timeline-and-strategic-items.md) — confirm that **endoclaw-timer is a strategic-early M1 item** because *SES lockdown removes setTimeout / setInterval, so Timer is the only mechanism for scheduled agent execution*. The scheduler design at PR #89's line 481 lands in this M1 slot.

### Project context

- [endo-but-for-bots project README](../../projects/endo-but-for-bots/README.md) — § Rules of engagement (design PRs land on `llm`; implementations on `master`; designs and implementations are never combined); § Standing authorizations (the garden's roles may post comments, reviews, and reactjis on `endojs/endo-but-for-bots` without per-action authorization, for this repo only); § Authority structure (every commenter is maintainer-equivalent by virtue of the repo's permission gate). Confirms that the design-only PR #89 is the right shape and that the maintainer-equivalent kriskowal review routes through the standard designer dispatch.
- The dispatch's referenced design file `designs/genie-integration.md` on the PR head (`97b16962`) carries the §3 *Scheduling* section the maintainer's "Let's simply call it 'scheduler'" comment at line 485 lands inside. That section already proposes graduating the genie interval scheduler into a daemon `interval-scheduler` formula with `IntervalControl` host facet, `IntervalHandle` guest capability, mail-based tick delivery, and `serial-jobs`-backed coalescing. The new prerequisite scheduler design at line 481 extracts §3 (and the spirit of `designs/endoclaw-timer.md`) into its own sibling design under `designs/scheduler.md`, leaving the genie-integration PR to *reference* it rather than *propose* it.
- The existing `designs/endoclaw-timer.md` on `origin/llm@11a76ae6` (M1 strategic-early item; status *In Progress* with Phase 1 prototype shipped in `packages/genie/src/interval/`) is the most direct precedent for what the new scheduler design is graduating: the prototype the design names as *"this facility can graduate out to a proper @endo/xxx package, or maybe just move into @endo/daemon more generally"*. The new design is essentially the daemon-side graduation that endoclaw-timer's Phase 1 anticipated.
- Related designs on `origin/llm` worth citing rather than reinventing: `designs/daemon-capability-bank.md` (the meta-framework Timer / scheduling slots into); `designs/daemon-commands-as-messages.md` (the message shape mail-delivered ticks would adopt); `designs/daemon-value-message.md` (the reply primitive `tickResponse.resolve` / `.reschedule` map onto); `designs/daemon-mount.md` (the `ScratchMount` that genie-integration §2 already cites for memory backing); `designs/lal-fae-form-provisioning.md` and `designs/familiar-bundled-agents.md` (the manager-agent worker-setup form the scheduler capability would arrive in).
- Zizmor CI fix context: the maintainer's "rebase for the zizmor CI fix" comment refers to upstream `endojs/endo#3297` (*chore(ci): fix zizmor warning*), merged into upstream master at `07aff334e` and synced into bot-master 2026-06-04 per [dispatch 000200Z-dispatch-liaison-55a546](../../entries/2026/06/04/000200Z-dispatch-liaison-55a546.md). The current bot-fork `origin/llm@11a76ae6` already carries the master-into-llm sync; a clean rebase of #89's head `docs/design-genie-integration` onto a fresh `llm-<sha>` frozen base picks up the fix without any per-PR zizmor work. Per [frozen-base-branch skill](../../../skills/frozen-base-branch/SKILL.md), the rebase mints a new `llm-<short-sha>` snapshot rather than retargeting `llm` directly.

### Why each reference is relevant

- **endopi.md / Genie section**: the worldview the genie-integration PR is integrating *into*. The rewrite at inline 452 ("stand the agent on the pet store") aligns the design with the §pet-store-as-typed-namespace precedent rather than the §vfs-node ambient-filesystem default. The new scheduler design (inline 481) is the daemon-side graduation of `makeIntervalScheduler` that the §Genie section already names as a substrate Pi-side ships and Endo-side needs to absorb.
- **daemon-capability-bank**: locates the new scheduler design in the existing capability family (slot is **Timer / scheduling** → `daemon-capability-timer.md` *Planned*). The six §Design Principles are the rubric.
- **daemon-agent-tools**: worked example of how to lay out a per-capability design (Dir / Shell / Git / Search pattern; dynamic tool registration). The scheduler design can follow the same shape (host facet vs guest facet split; pet-name granting; LLM-discoverability via `help()`).
- **daemon-mount + space concept + chat-spaces-gutter persistence**: the three together cover the *pet-store-as-typed-namespace* precedent for inline 452's rewrite (the maintainer asks for the same shape applied to agent memory).
- **endoclaw-timer + readme milestones**: confirm that the scheduler design is M1 strategic-early, names the existing prototype the design is graduating, and supplies the §Phase-1-already-shipped baseline so the new design names a §Phase-2-and-beyond delta rather than restating Phase 1.
- **daemon-capability-bus**: tangential but useful when the scheduler design has to choose between supervisor-owned vs worker-owned tick delivery (the same envelope-vs-CapTP framing applies).
- **frozen-base-branch + 2026-06-04 zizmor dispatch**: pre-cooks the rebase (target `llm-<sha>` snapshot of current `origin/llm@11a76ae6`, not bare `llm`); no per-PR zizmor edit required because the fix is already in the new base.

### Open questions for the designer

- The maintainer's "Let's simply call it 'scheduler'" at line 485 disambiguates a name within the existing §3. The library does not yet have a concept page that distinguishes *scheduler* (the agent-side capability) from *interval-scheduler* (the daemon formula type the existing §3 names) from *timer* (the existing simple formula type). If the new design lands these as distinct first-class names, a follow-up scholar pass should add a concept page; the researcher noted this as a structural-gap candidate.
- The inline-452 rewrite raises a deeper question: how much of the existing genie-integration §2 *Memory* section becomes "use the pet store directly" vs "use `ScratchMount` over the pet store's state directory"? The maintainer's framing favors the former; the existing §2 text mostly assumes the latter. The designer surfaces the trade-off as an *Open Questions* item rather than picking unilaterally.
- The §3 *Scheduling* section that becomes a sibling design also raises whether the scheduler design supplants the existing `designs/endoclaw-timer.md` or extends it. The convention is *Superseded by* rather than deletion (per [designer AGENT.md § Operating norms](../../../roles/designer/AGENT.md)); the new design names endoclaw-timer as its Phase-1-shipped predecessor and adds a *Superseded by* pointer on endoclaw-timer itself if the maintainer agrees.
```

## Library writeback

Added 17 keyword-index shortcuts to `journal/library/keywords.md` for terms that came through source-page / section-file reads rather than the keyword index, so the next reader (designer or future researcher) lands on the right concept page directly:

- `genie-integration`, `` `@endo/genie` integration survey ``, `` `@endo/llm-engine` extraction proposal `` → `endopi` source page (the survey lives in the genie-integration PR but the worldview it sits inside is documented in `endopi.md`).
- `endoclaw-timer prototype in genie`, `` `makeIntervalScheduler` ``, `Genie heartbeat subagent`, `Genie observer reflector compaction` → the endopi Genie section (the canonical exposition of these primitives).
- `interval-scheduler formula type (planned)`, `` `daemon-capability-timer` `` → the daemon-capability-bank section that names the Planned Timer slot.
- `pet store as agent memory`, `pet-store-as-memory` → the `space` concept page (the canonical pet-store-as-typed-namespace exposition).
- `` `ScratchMount` ``, `scratch-mount as daemon-managed workspace` → the daemon-mount section (the §two-formula-type-split that defines `ScratchMount`).
- `zizmor CI fix endo#3297` → the 2026-06-04 weaver dispatch entry that established the upstream fix and the bot-master sync.
- `serial-jobs daemon task queue` → the daemon-message-streaming source (the closest existing entry; a more direct source would warrant a concept page in a later scholar pass).

No concept-page drafts and no distraction prunes this engagement; the existing concept pages and section files were on-point for every term. Two structural gaps surfaced for a later scholar / librarian pass: (a) no concept page distinguishes *scheduler* / *interval-scheduler* / *timer* as the three first-class names the design space already uses, and (b) no source page on `designs/endoclaw-timer.md` exists in the library (the design is named in the readme timeline and the genie section, but the doc itself is not yet ingested).

## Open questions

- The truncated body of inline `3369680410` was fetched from the GitHub API (`gh api repos/endojs/endo-but-for-bots/pulls/comments/3369680410`); the full text reads: *"Every agent in the daemon has a 'pet store' which can read and write files and make and remove directories. This should be a sufficient space for the agent's memory and references to shared context. It should not be necessary for this virtual filesystem to be backed by an actual file system. If we stand the agent fully on the pet store, which is daemon native, rather than communicating with a physical filesystem."* The designer's downstream dispatch will want to refetch to confirm before drafting.
- `designs/endoclaw-timer.md` itself is not in the library yet; the new scheduler design will want to read it directly from the bot-fork `origin/llm` worktree at design time. Flagging as a scholar-pass candidate for ingestion.

Self-improvement: nothing this time. The dispatch fit cleanly within the researcher role's existing skills (library-lookup with on-the-fly indexing, project-README citation). The library-lookup skill's three-axis fallback (keywords → concepts → sources) worked as intended: every relevant primitive was reachable; the gaps that surfaced (no `endoclaw-timer` source page, no scheduler concept page) are corpus-growth signals rather than role / skill bugs.
