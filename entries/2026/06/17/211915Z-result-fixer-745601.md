---
ts: 2026-06-17T21:19:15Z
kind: result
role: fixer
worktree: dispatches/fixer--745601/project
repo: endojs/endo-but-for-bots
project: endo
refs:
  - entries/2026/06/17/211100Z-dispatch-fixer-745601.md
---

# Result: fold erights's PR #449 decisions into the design and re-base onto master-4a04d07

erights answered all three open questions on PR #449 in
[issuecomment-4735477238](https://github.com/endojs/endo-but-for-bots/issues/comments/4735477238)
(2026-06-17). This dispatch rebased the PR onto the post-#435 base
branch, renamed `DESIGN.md` to `DESIGN-immutable-arraybuffer.md` per
the parallel-naming decision, folded the three resolutions into
`DESIGN-freezable-typedarray.md`, retargeted the PR base, refreshed
the PR body, and posted a top-level summary comment naming the
maintainer and senior contributor.

## SHAs

- Pre-rebase head: `d8b8a93fb7c08bc1e31116842d21caf26cfd82ce`
- After rebase onto `master-4a04d07` (`855a8f7bc`): `d0d7c6f80`
- After rename commit: `ec6c284bd`
  ("docs(immutable-arraybuffer): rename DESIGN.md to DESIGN-immutable-arraybuffer.md")
- After decisions-fold commit: `ce6108477d4b0d553e2a056cfa3f183cb2f98ddf`
  ("docs(immutable-arraybuffer): record erights's decisions on PR #449 open questions")
- Force-pushed to `origin/design/immutable-arraybuffer-freezable-typedarray-emulation`
  with `--force-with-lease=...:d8b8a93fb...` (lease anchored on the
  pre-rebase head). Push succeeded.

## Rebase outcome

Clean rebase, no conflicts. The branch's only file
(`packages/immutable-arraybuffer/DESIGN-freezable-typedarray.md`,
new) does not overlap with anything in #435's merge.

## Per-decision file changes

### Decision 2: parallel-naming rename (commit `ec6c284bd`)

- `packages/immutable-arraybuffer/DESIGN.md` -> `packages/immutable-arraybuffer/DESIGN-immutable-arraybuffer.md`
  via `git mv` (file history preserved).
- `packages/immutable-arraybuffer/src/lib.js`: one comment updated
  from "DESIGN.md Move 2 paragraph 7" to
  "DESIGN-immutable-arraybuffer.md Move 2 paragraph 7" (the comment
  was authored in PR #435; updating it in this PR keeps it
  consistent with the rename it triggered).
- `packages/immutable-arraybuffer/test/shim-amplifier.test.js`: same
  rename in a one-line comment.

### Decisions 1, 2, 3: design content fold (commit `ce6108477`)

`packages/immutable-arraybuffer/DESIGN-freezable-typedarray.md`
(125 insertions, 117 deletions):

- Opening paragraph: `DESIGN.md` -> `DESIGN-immutable-arraybuffer.md`.
- *Semantics* § `[Symbol.toStringTag]`: rewritten from
  "deferred, working assumption is shape (a)" to
  "the shim does not install the tag; emulated views read as
  `[object Uint8Array]` via inheritance from `T.prototype`", citing
  erights's option-(b) resolution.
- *Test plan* § Cross-package consumer touchpoints: the note about
  the parallel concordance risk rewritten to acknowledge erights's
  "happy not to add complexity to avoid it until we find out if it
  is an actual problem" framing while keeping the consumer sweep as
  a builder-level guardrail.
- *Open questions* section (4 questions) -> *Decisions* section
  (3 resolved decisions) + *Open question* section (1 remaining
  framing question, the `internal-heir.js` shape). Cross-references
  in the Implementation outline updated to point at "*Open question*
  § 1".
- *References* section: PR #435's design now linked as
  `DESIGN-immutable-arraybuffer.md` rather than `DESIGN.md`, with a
  parenthetical note about the rename; a new reference entry links
  erights's resolution comment.

## PR-side edits

- `gh pr edit 449 --base master-4a04d07`: base retargeted from
  `master` to `master-4a04d07`. Confirmed via
  `gh pr view 449 --json baseRefName`.
- `gh pr edit 449 --body-file ...`: body rewritten to drop the
  three-questions block, reflect option (b) in the API surface
  summary, point at the renamed `DESIGN-immutable-arraybuffer.md`
  sibling, and surface the one remaining framing question in an
  "Open question" subsection.
- Top-level comment posted at
  [issuecomment-4735614214](https://github.com/endojs/endo-but-for-bots/pull/449#issuecomment-4735614214)
  with `@erights @kriskowal` mention, decision summary, commit
  links, and the "ready for design panel" note.

PR #449 remains DRAFT (not un-drafted; per the dispatch brief and the
PR-creation flow, the design panel and judge un-draft, not the fixer).

## Recommended next stage

`next: solicitor` (design panel). The design's resolved decisions
are recorded; the remaining `internal-heir.js` framing question is a
builder-level call that does not block the design panel from
reviewing the substantive shape (API surface, semantics, test plan,
scope).

Self-improvement: nothing this time.
