---
event: dispatch
role: weaver
dispatcher: endolinbot-steward
dispatch_root: /home/kris/dispatches/weaver--25ad71
repo: endojs/endo-but-for-bots
pr: 357
branch: chore/prettier-markdown
trigger: kriskowal directive 2026-05-23T03:38:26Z on #357 — "Reconstruct the second commit on a rebase."
---

# Weaver dispatch: reconstruct second commit of #357 on rebase

PR #357 has 2 commits: (1) prettier config change adding `*.md` to formats, (2) bulk format of all `*.md` files. kriskowal wants commit 2 reconstructed (regenerated by re-running prettier) rather than cherry-picked, because the markdown files on the new base will have different content.

Procedure:
1. Sync bot-master to upstream (likely no-op).
2. Rebase: rebuild branch as commit-1 (config change) cherry-picked, then DROP commit-2.
3. On the new tip, run `yarn install --immutable` then `yarn prettier --write '**/*.md'` (or whatever the package.json formatter invocation is) to regenerate the formatted markdown across the new tree.
4. Stage all `*.md` changes and create a fresh commit with the same conventional-commit message as the dropped commit 2.
5. Verify net diff is what's expected (all .md files re-formatted against current base).
6. Force-push with lease.
7. Post a PR comment with new head SHA + brief.
