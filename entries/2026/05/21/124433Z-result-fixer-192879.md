---
ts: 2026-05-21T12:44:33Z
kind: result
role: fixer
project: endo-but-for-bots
repo: endojs/endo-but-for-bots
worktree: dispatches/fixer--79a090/project
refs:
  - jobs/claimed/20260521T121549Z--endolinbot--steward--795c--b39d4a--retention-paths-phase-1-summary.md
---

# Fixer summary-fix bundle on PR #284 (six commits)

All seven `summary-fix` items from the 2026-05-21 close-review panel
landed on `feat/daemon-retention-paths-phase-1` as six commits
(7d4f3ebb9 .. 909e87694). PR is un-drafted; this was post-gauntlet
polish, not part of any must-fix loop.

## Per-item disposition

1. **Late-subscriber snapshot in `retention-path-accumulator.js`** —
   7d4f3ebb9. `subscribe()` yields a synthetic `{ snapshot }` when
   `primed === true`. Regression test asserts both subscribers on a
   primed accumulator receive the snapshot first.
2. **CLI output alignment with design's example** — 977c03bc2.
   Extended `RetentionPathSegment` with `formulaTypes: string[]`
   populated in the daemon's `listRetentionPaths` rewrite from
   `formulaForId`. CLI renders `endo (root)`, `pins (pet-store)`,
   `shared-file (eval)` with Unicode `→`. Banner now reads
   `Path N (rooted at <root-member-name>):`. Extracted `renderPath`
   to `render-retention-path.js` so it can be unit-tested without
   pulling SES into the worker. Test asserts segment-type rendering
   and absence of the legacy ASCII arrow.
3. **Lifecycle-log routing for flush failures** — 99657e4aa. Added
   optional `onError` callback to `makeRetentionPathAccumulator`
   (defaults to silent drop). Daemon wires both the flush-failure
   (`RETENTION_PATH_FLUSH_FAILED`) and change-pump-failure
   (`RETENTION_PATH_PUMP_FAILED`) through `logLifecycle(targetId,
   ...)`, replacing the two ad-hoc `console.error` calls.
4. **`@ts-nocheck` → `@ts-check` on `paths-command.test.js`** —
   folded into 977c03bc2 along with the new `renderPath` assertion
   (the test file was rewritten in one stroke).
5. **`host.js` no-op-vs-throw consistency** — fb6806a8d. Kept the
   no-op pattern (retention-path introspection is opt-in
   instrumentation) and added a JSDoc comment that contrasts the
   choice with `getMountHostPath`'s loud-failure default.
6. **`labelCache` lifetime docs + parallel store-controller
   resolution** — 219ac8e19. Added a one-line per-call-lifetime
   note; refactored the serial await loop to
   `Promise.all(Array.from(...))`.
7. **Unused-identifier refactor in `followRetentionPaths` change
   pump** — 909e87694. Refactored to `.next().then(loop)` recursion
   so the change value is never named in the body. (The
   `for await (const {} of ...)` alternative falls afoul of the
   project's `no-empty-pattern` rule.)

## Files touched

- `packages/daemon/src/retention-path-accumulator.js`
- `packages/daemon/test/retention-path-accumulator.test.js`
- `packages/daemon/src/daemon.js`
- `packages/daemon/src/graph.js`
- `packages/daemon/src/host.js`
- `packages/cli/src/commands/paths.js`
- `packages/cli/src/render-retention-path.js` (new)
- `packages/cli/test/paths-command.test.js`

## Verification

- `npx ava test/retention-path-accumulator.test.js test/retention-paths.test.js`
  in `packages/daemon`: 15 passed.
- `npx ava` in `packages/cli`: 18 passed (full suite, including the
  four `paths-command` tests).
- `yarn lint:prettier` at root: clean.
- `yarn lint:eslint` on touched files: 0 errors.
- `yarn lint:types` in `packages/daemon` and `packages/cli`: clean
  (the pre-existing `@libp2p/utils/src/adaptive-timeout.js` baseline
  error is unrelated and present on the prior PR head).

No dependency changes, so no separate `chore: Update yarn.lock`
commit.

## Push and CI

Pushed 909e87694 to `feat/daemon-retention-paths-phase-1`. CI run
26226658137 was kicked off at 2026-05-21T12:44Z; all jobs pending at
report time. Top-level summary comment posted at
endojs/endo-but-for-bots#284 (issuecomment-4508368832) mapping each
item to its fix-commit.

## Operational notes

- The pre-push gates' eslint `--fix` stage attempts to rewrite a
  pre-existing multi-`@param`-on-one-line JSDoc in `host.js`
  (`writeRemoteAgentKey`) into a malformed multi-line form. The
  same rewrite touches several other baseline files
  (`better-sqlite3-xs.js`, `bus-daemon-rust-xs.js`,
  `bus-xs-daemon-polyfills.js`, `evasive-transform/src/index.js`,
  `ses/src/compartment.js`). Reverted those auto-fix artifacts and
  re-ran with `--probes-only`. The probe failures that remain
  (filename-no-stutter, no-ascii-banners, no-inline-import-jsdoc,
  no-pull-citations, security-md-hash-uniform, sentence-per-line-md)
  all reference baseline paths unrelated to PR #284's diff.

Self-improvement: nothing this time. Worth flagging for the
gardener that the pre-push gate's eslint `--fix` stage can mangle
unrelated baseline files in this repo; a future field-note on
`skills/pre-push-gates/SKILL.md` § Pitfalls might say "if eslint
auto-fix touches files outside the fixer's intended change set,
revert them and run with `--no-auto-fix` or `--probes-only`."
