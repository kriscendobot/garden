---
ts: 2026-05-29T05:27:12Z
kind: dispatch
role: liaison
repo: endojs/endo
project: endo
refs: []
---

Dispatched boatman (dispatch-root `dispatches/boatman--1947d5`) to ferry the bot-side PR kriscendobot/endo#1 upstream to endojs/endo.

Source: kriscendobot/endo#1, branch `fix/3289-install-engines-retry`, head `82f66580`, single commit `fix(benchmark): retry esvu installs in install-engines.sh (#3289)` (author endolinbot). Bot-side PR is still DRAFT; the maintainer directed the ferry explicitly so the boatman proceeds regardless of draft state. Frozen base on the bot side is `master-052b048`; upstream PR uses upstream's natural base `master`.

Upstream: endojs/endo, base `master` (tip `c49fb048b` in the bare clone at ferry time). Upstream issue #3289 (commonly repeated flake: test-xs fails to Install engines) is OPEN, so `Fixes #3289` is upstream-valid.

Human attribution: `Kris Kowal <kriskowal@kriskowal.com>` (current boatman default, supersedes the older `kris@cixar.com`). `identity_switch_authorized: true` granted by the maintainer in-session (this session holds full kriskowal authority and was tasked the ferry directly).

Host: kmkmbp2026 (renamed from the journal's recorded `kmkmbp2021`); `gh auth status` shows kriskowal active with `push: true` (admin) on endojs/endo. Preconditions verified before dispatch. This is a freshly-provisioned garden checkout: local bot identity (endolinbot), the local `journal` branch, and the `worktrees/endojs-endo.git` bare clone were all created in this session to support the ferry.

Expected report: upstream PR URL, upstream branch + head SHA, attribution/trailer verification, CI status, and the garden-side mirror cross-link comment ID.
