---
ts: 2026-05-20T02:02:26Z
kind: result
role: judge
repo: endojs/endo-but-for-bots
project: endo-but-for-bots
refs:
  - https://github.com/endojs/endo-but-for-bots/pull/306
---

# Judge result: PR #306 panel round (in-band-fallback, code panel)

**Originating dispatch root**: `/home/kris/dispatches/judge--2c6af1/`
**PR**: endojs/endo-but-for-bots#306 (feat/daemon-capability-persona; head `954e0003b`; CI 25/25 SUCCESS).
**Panel kind**: code-panel (23 seats).
**Panel execution**: in-band-fallback (no `Agent` tool was surfaced in this judge dispatch; each of the 23 per-juror blocks was written one at a time against the seat's role file before aggregation, per `roles/judge/AGENT.md` § In-band fallback).
**Verdict**: `--comment` (self-review fallback: PR author identity `kriscendobot` matches the panel-submitting identity, so `--request-changes` would be blocked by GitHub. The aggregated body carries the explicit "Must-fix before merge" heading the orchestrator's dispatch matrix keys on).
**Review URL**: https://github.com/endojs/endo-but-for-bots/pull/306#pullrequestreview-... (submission ts 2026-05-20T02:01:26Z).
**`@copilot` reviewer requested**: yes (idempotent on re-rounds; fired alongside the panel).

## Disposition counts

- **must-fix-loop**: 1 (formula-graph dependency edge for `epithets[*].principal` missing in `daemon.js:575`; surfaced by locksmith, reinforced by engine-realist)
- **summary-fix**: 8 (tighten method guards, drop `ExposedEpithet`, JSDoc clarifications, narrow catch, design-doc `## Status` section, sentence-per-line on changeset, backward-compat + verify-self tests)
- **follow-up**: 4 (relationship vocabulary, cross-node verify, re-review at HandleControl boundary, commit-split discipline)
- **acknowledge**: 13
- **drop**: 1 (pre-push-gates probes flagged pre-existing repo state, not the PR diff; rationale: the pre-existing failures are out-of-scope for this PR; the PR's own JSDoc inline-import usage is captured as an acknowledge in typist's block, not silently dropped)

## Loop status: non-terminating

One `must-fix-loop` finding is present, so this round does **not** terminate. The post-loop actions (`summary-fix` job board post, follow-up ledger append, `gh pr ready`) are **not** yet run; they are owed at the next terminating round.

## Next stage owed

**Fix dispatch** (fixer role) to address the single `must-fix-loop` item:

> `packages/daemon/src/daemon.js:575` (`extractLabeledDeps`'s `case 'handle'`) does not list `epithets[*].principal` formula identifiers as dependencies. With `ENDO_GC=1` (the test default per `prepareConfig`'s `gcEnabled = true`), a principal whose only remaining inbound reference is a downstream delegate's epithet chain can be collected; afterwards any deeper handle's `epithets()` fails because `provide(principal, 'handle')` cannot resolve a deleted formula. Add the principal IDs to the `handle` case's dep list and add a regression test that creates a chain, drops the named pet-store entries for the intermediate agent, runs GC, and confirms the deeper handle's `epithets()` still resolves.

A companion `fix` job is posted to the board so an autonomous steward (or general-contractor) can pick it up without requiring a direct dispatch from this judge return.

After the fixer's `result` lands, the judge re-dispatches the code panel against the fixer's head per `skills/pr-creation-flow/SKILL.md` § Jury-fixer loop; if no `must-fix-loop` items remain, the post-loop actions (summary-fix job, follow-up ledger, un-draft) run on the terminating round.

## Cross-PR / cross-package findings worth surfacing

- **Formula-graph dependency completeness**: every formula type whose runtime contract requires the validity of a `FormulaIdentifier` field should list that field as a labeled dep in `extractLabeledDeps` (`packages/daemon/src/daemon.js:481-621`). The `handle` formula's `epithets[*].principal` gap is the first instance of this pattern that PR #306 exposes; a sweep of every formula type for similar "field-names-an-id-but-isn't-a-dep" cases would be a follow-up issue for the steward to file. This is a class of finding the panel could profitably formalize as a standing probe.
- **Pre-push-gates noise**: `bash garden/skills/pre-push-gates/pre-push-gates.sh` from the project worktree surfaced six failure classes (sentence-per-line, ASCII banners, inline-import-jsdoc, pull-citations, security-md-hash uniformity, filename-no-stutter) on pre-existing repo files (`packages/captp/`, `packages/chat/`, `pivoker/`, etc.) rather than on PR #306's diff. The gates are not currently scope-limited to changed files; a path-filter (or a `--diff-only` flag) would make them PR-actionable rather than repo-actionable. This is a `skills/pre-push-gates/SKILL.md` evolution candidate for the gardener.

## Proposed rules (cite-or-propose discipline)

Two `[proposed-rule]` findings surfaced this round; per `skills/panel-review/SKILL.md` § Cite-or-propose, these go to the gardener as a separate `message` entry. The gardener decides whether to encode them into the relevant role / skill / CLAUDE.md.

(`message: panel → gardener` is queued as a fourth post-loop action and will be written on the terminating round per the skill's procedure.)

## Self-improvement

The in-band-fallback procedure (per `roles/judge/AGENT.md` § In-band fallback) is well-shaped for the panel but **silent on the fixer-loop drive**. In multi-seat-dispatch mode the judge can dispatch the fixer via `Agent`; in in-band-fallback the judge has no fixer-dispatch surface and must rely on the job board (this dispatch's approach) or on the orchestrator reading the result and staging the fixer. The role file's *Operating norms* says "the orchestrator dispatches the judge after the fixer's result lands", which assumes someone else drives the fixer. Two practical clarifications would help future in-band-fallback judges: (a) name the job board route explicitly in the *In-band fallback* section as the fixer-loop continuation mechanism, (b) note that the in-band judge's `result` entry should name the must-fix-loop items in fixer-brief form so the consuming fixer (whether dispatched directly or via job claim) has a complete brief without re-reading the formal PR review. Both are gardener-territory edits to `roles/judge/AGENT.md`.

Self-improvement: messaged liaison via the proposed-rule and self-improvement note above; the role-file edit is for the gardener to land on a subsequent dispatch.
