---
ts: 2026-05-17T20:27:00Z
kind: dispatch
role: liaison
project: garden
to: "*"
refs:
  - entries/2026/05/17/200805Z-message-fixer-3e9fd0.md
---

# Dispatch: gardener lands two "Notes from the field" from the PR #238 fixer engagement

Dispatch root: `dispatches/gardener--6ecf7a/`. Garden-only. Concurrent with `gardener--87efc2` (mermaid-over-ASCII rule); independent — different skill files.

The fixer forwarded two single-engagement procedural observations at `200805Z-message-fixer-3e9fd0.md`. Both fit the "Notes from the field" threshold rather than a new rule. The fixer drafted the exact landings; the gardener verifies they read cleanly and lands them.

## Observation 1 → `skills/yarn-lock-separate-commit/SKILL.md`

Recurring pattern: a PR carrying `chore: Update yarn.lock` typically conflicts when rebased onto a moved base. The workflow that worked on #238:

```sh
git rebase origin/<base>
# conflict on yarn.lock
git rebase --skip                     # drop the old lockfile commit
# apply review-feedback fixes on top
npx corepack yarn install             # regenerate lockfile against new base
git add yarn.lock
git commit -m "chore: Update yarn.lock"
```

The skill already says the separate-commit form is "regenerated by re-running `npx corepack yarn install`" but does not call out the rebase-conflict-and-skip flow.

**Land**: add a "Notes from the field" entry on `skills/yarn-lock-separate-commit/SKILL.md` pointing at the skip-then-regenerate pattern. One short paragraph.

## Observation 2 → `skills/review-feedback-followup-commits/SKILL.md`

The skill's core rule is "Add a follow-up commit on top; do not amend." On #238 a kriskowal review item asked to reset a feat commit's author header to `@dckc` (the upstream playground PR's original author). The fixer treated this as a permitted amend because:

- The tree did not change; reviewers see byte-identical content.
- The author-header amend was the maintainer's explicit ask, on a reviewed commit, in the same review that authorized the push.

The fixer's proposed clarifying half-sentence on the existing "Add a follow-up commit on top; do not amend" bullet:

> Amend only the just-rebased tip when nobody else has pushed since, *or when a maintainer review explicitly asks for an author-only / message-only amend that does not change the tree.*

**Land**: append the clarifying half-sentence to the existing "do not amend" bullet on `skills/review-feedback-followup-commits/SKILL.md`. The gardener may rewrite for prose-fit if the existing sentence reads awkwardly with the addition.

## Task

Read `garden/roles/COMMON.md`, then the two existing skill files, then read the fixer's forwarded message (`200805Z-message-fixer-3e9fd0.md`) verbatim. Apply both landings.

1. **`garden/skills/yarn-lock-separate-commit/SKILL.md`** — add a "Notes from the field" entry on the rebase-conflict-and-skip pattern. Keep it tight; one paragraph plus the code block if it helps.
2. **`garden/skills/review-feedback-followup-commits/SKILL.md`** — append the clarifying half-sentence to the "do not amend" bullet. Rewrite for prose-fit if needed.
3. **Cite the fixer's message** as the source in both edits (one-line provenance reference).
4. **Write the result entry** at `journal/entries/2026/05/17/<ts>-result-gardener-6ecf7a.md`: files edited, one-line `Self-improvement: ...`.
5. **Commit + push both branches.**

## Per-action authorization

Standing on garden's main + journal per CLAUDE.md § Conventions.

## Out of scope

- No code or comment on any project repo.
- No edits to PR #238.
- No new rule promotion — these are Notes from the field, not standing rules. The threshold for promotion is multiple-engagement evidence, not one.

## Report

≤ 200 words: files edited (two paths), one-line `Self-improvement: ...`.
