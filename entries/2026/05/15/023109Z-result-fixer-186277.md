---
ts: 2026-05-15T02:31:09Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
worktree: dispatches/fixer--186277/project
refs:
  - entries/2026/05/15/022448Z-message-liaison-ef7995.md
  - entries/2026/05/15/022525Z-result-weaver-ef7995.md
---

Reshaped PR #107 (`design/random-pure-rand-v8-interface`) onto `origin/kriskowal-random-chacha12` (tip `b0d9f31ce`) per the weaver's 6-step recipe.

Reset HEAD to the new base, cherry-picked the feat commit `4e8e1ea20` (it conflict-frees as the weaver predicted), then amended in the reshape edits: (1) `packages/random-fast-check/index.js` import `@endo/random/read-uint.js` -> `@endo/random/uint.js`; (2) `test/_make-source.js` `makeSource = seed => makeChaCha12(seed)` -> `makeChaCha12(seed).fillRandomBytes`; (3) parallel `.fillRandomBytes` fix to `test/fast-check.test.js` (two call sites at line 12 and line 48 in the original) and to the README example. The weaver's analysis flagged the two callers it could see; grep surfaced two more in the same package.

Walked `designs/random-pure-rand-v8-interface.md` (437 lines) against the new-base APIs. Updated five paragraphs: the lockfile-pin paragraph (yarn.lock now resolves both `fast-check@^3` -> 3.1.1 / `pure-rand@^5` -> 5.0.1 and `fast-check@^4` -> 4.7.0 / `pure-rand@^8` -> 8.4.0, the latter via the new `@endo/chacha12-fast-check-test` workspace); the "PR #75 no longer ships fast-check.js" paragraph (added the `ChaCha12Generator` record shape so readers know `makeChaCha12(seed).fillRandomBytes` is the `RandomSource` entry point now); the "PR #75 drops" enumeration (PR #75's reshape went further: `random.types.d.ts` renamed to `types.d.ts`, `read-uint.js` renamed to `uint.js`, so the `PureRandomGenerator` typedef relocates rather than tracks the rename); the `@endo/random` import paragraph (now imports `randomUint32` from the `@endo/random/uint.js` subpath); the type-definitions section header (acknowledges the rename); and the chacha12-snapshot Open Question (the generator record now exposes `getState`/`clone`/`next` and pairs with `makeChaCha12FromState(state)`, which changes the contour of the future snapshot adapter even though that adapter remains out of scope here). The "Dependencies" section's `bots-ssh/llm` rebase note stays accurate as-is; the parent is still PR #75.

Regenerated `yarn.lock` with `corepack yarn install --mode update-lockfile`; only change was registering the new `@endo/random-fast-check@workspace:...` resolution (21 lines added). Committed as `chore: Update yarn.lock`.

Per CRITICAL "commit + push before extended local validation": pushed `--force-with-lease=design/random-pure-rand-v8-interface:436f07c6b HEAD:design/random-pure-rand-v8-interface` to `origin`; lease accepted, forced update from `436f07c6b` to `d24a854d6`. Two commits on top of `origin/kriskowal-random-chacha12`: `fff385dd4 feat(random-fast-check): new package with v5 + v8 pure-rand adapters` and `d24a854d6 chore: Update yarn.lock`.

Post-push local validation: ran `corepack yarn install` (clean), `yarn lint:types` (clean, exit 0), and `yarn test` on `@endo/random-fast-check` (13 tests pass on each of the three `ses-ava` configs: `lockdown`, `unsafe`, `endo`). Adapter shape change is bit-equivalent: the v5/v8 byte-stream cross-check test still passes, confirming the `.fillRandomBytes` substitution preserves entropy semantics.

Self-improvement: nothing this time.
