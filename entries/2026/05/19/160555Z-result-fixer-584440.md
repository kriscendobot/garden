---
ts: 2026-05-19T16:05:55Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo
---

PR #75 (kriskowal-random-chacha12): addressed two kriskowal directives.

**Commits** (rebased onto `origin/kriskowal-random-chacha12` after `git fetch` revealed two new upstream commits; clean rebase, no conflicts):

- `a6a47720a` `refactor(random): rename bobsCoffee64 to bobsCoffee32 per kriskowal #75` — the seed is 32 bytes (256 bits); the prior `64` tracked the 8-byte `b0b5c0ffeefacade` pattern repeated four times. Renamed across `packages/random/seeds.js`, `packages/random/README.md`, `.changeset/endo-chacha12.md`, and four consumer files (`packages/hex/test/{encode,decode}.bench.js`, `packages/ocapn/test/{syrup/fuzz,codecs/passable-fuzz}.test.js`). `grep -rn bobsCoffee64` clean post-rename.
- `365c26657` `chore(random): drop hand-rolled CHANGELOG placeholder per kriskowal #75` — deleted `packages/random/CHANGELOG.md` (was a copy of the `@endo/skel` template). Matches the cbor-frame #288 and syrup-frame #109 retcons; changesets writes the file on first release.

**Prettier**: `yarn lint:prettier --check` clean on the rebased tip.

**Push**: `365c26657` pushed to `kriskowal-random-chacha12` (after fetch + rebase resolved the non-fast-forward against `4d797a59d`).

**Thread replies**: both inline comment ids (3267589099, 3267593234) returned 404 on the `/replies` endpoint (REST index lag; comments visible in browser but not yet propagated, the documented pending/draft pitfall). Fell back to a single top-level summary mapping each id to its addressing SHA: `https://github.com/endojs/endo-but-for-bots/pull/75#issuecomment-4489661725`.

**Re-request**: `gh pr edit 75 --add-reviewer kriskowal` succeeded.

Self-improvement: nothing this time. The "REST index lag" fallback in `skills/pr-review-thread-replies/SKILL.md` § Pitfalls fit this case exactly; followed it as documented.
