---
ts: 2026-05-19T20:32:11Z
kind: result
role: fixer
repo: endojs/endo-but-for-bots
project: endo-but-for-bots
---

PR #290 (`feat/lal-pi-harness`): rebased onto current `origin/llm` (carrying #291, #292, #293) and dropped duplicate tests per @0xpatrickdev's directive.

**Rebase**: two conflict zones (agent.js, package.json), both resolved in favor of #290's pi-harness design.
Retained `@endo/promise-kit` alongside the new pi-ai deps because `tools/mock-powers.js` (from #293) imports it.
New head SHA: `4e6ed35`.

**Removals** (one commit, `4e6ed35` "chore(lal): drop tests + types now owned by pi-agent-core"):

- `test/openai-compatible-messages.test.js` (added in #292)
- `test/mock-provider-fixtures.test.js` (added in #293)
- `test/fixtures/llm-provider-traces.json`

**Collateral**: inlined a `ChatMessage` typedef in `providers/openai-compatible-messages.js` (the broader type was dropped from `agent.types.d.ts` and the file is dead code in the new agent but stays buildable for downstream consumers).

**Borderline cases left in place** (conservative defaults):

- `test/provider-config.test.js`: tests a small utility module, unaffected by harness swap.
- `test/simulator/run-simulator.test.js` + `.js`: still compatible with the new `spawnWorkerLoop` signature, skips when `LAL_HOST` is unset.

**Verification**: `yarn test` in `packages/lal` (5 pass, 1 skip); `yarn lint:types` clean for lal (the 3 errors in `packages/genie/src/tools/{common,memory}.js` are pre-existing on `origin/llm`); `yarn lint:prettier --check` clean repo-wide.

**Reply**: https://github.com/endojs/endo-but-for-bots/pull/290#issuecomment-4491794317
**Review re-requested**: kriskowal, jcorbin, 0xpatrickdev.

Self-improvement: nothing this time.
