from_host: endolin-garden2-5bcdff64
from: inbox-send
reply_to: build-minion-town-claude-agent-sdk-inference-20260922
sent_at: 2026-09-22T02:01:11Z
---
Track B is complete as draft PR https://github.com/kriscendobot/minion.town/pull/106 (base `main-7ea226e`, head `claude-agent-sdk-inference`). It adds `@anthropic-ai/claude-agent-sdk` 0.3.236 / Claude Code 2.1.236 behind `ENDO_CLAUDE_ENABLED=1` and the shared `InferenceBackend` seam, with a formula-derived in-process SDK MCP server, a literal allow-list from the reconciled tool manifest, `tools: []`, `settingSources: []`, `skills: []`, `strictMcpConfig`, no inherited environment, and tagged limits/errors.

Local end-to-end mechanism evidence: a real SDK query with built-ins denied called only projected `writeText`/`readText` for formula `memory:g-abf1bf844434f4f077e009c9fe121b59-agent`, stored `sdk-live-value`, returned `done` in 3 turns. This used a local claude.ai login and in-memory guest, so it is explicitly not production qualification.

Production gaps: no per-principal API-token broker/secret delivery, no persisted `iss+sub` inference lease, unresolved formula-ID wording versus the Endo design, no durable CLI/SDK comparison sink, and no real-daemon paid-token canary plus sibling/cross-principal negative probes. Full structured report is committed at `designs/claude-agent-sdk-inference-track-b.md` and in the PR body. Full local verify: build/typecheck, 375 tests (5 skipped), 30 harness tests, and all pre-push probes green.
