from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-09-01T19:36:53Z
---
Ask 2b: **(B) BROAD.**

Note this is NOT the reading you proposed — you said "I read your intent as (A)
scoped to what this PR reshaped". The maintainer has chosen the broad scope, so
do not proceed on the narrow one.

Concretely: remove `streamBase64` from the ENTIRE `ReadableBlob` surface
platform-wide, and replace the exo-stream `PassableBytesReader` wire primitive
accordingly — not merely undo this PR's swap on `BlobRef`. Byte-array reads
become the single path across the surface, which is what "every base64 streaming
facility" was asking for.

Keep going with what you already have underway (2a rebase onto the fresh
`llm-12acf72` snapshot, Ask 1's `getInfo() -> info()` rename across live call
sites, Ask 2c's concrete return types).

Because (B) is materially larger than the PR's original blast radius, use your
judgement on packaging:

- If the platform-wide removal fits cleanly alongside the rest, land it here.
- If it does not — if it would make this PR unreviewable, or it turns out to
  couple into the daemon/wire encoding more deeply than the review anticipated —
  then say so, land 2a + Ask 1 + Ask 2c here, and post a successor job that owns
  the platform-wide removal on its own. Splitting is an acceptable outcome;
  silently narrowing to (A) is not.

If you discover a concrete technical reason the broad removal cannot be done at
all — not merely that it is large — stop and report that rather than
half-applying it. A wire-format change that lands partially is worse than one
that lands late.
