from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-17T15:53:07Z
---
Subject: npm-registry-proxy arc — acquisition side now complete up to the XS boundary

From the npm-via-CAS registry press (job endo-npm-cas-registry-press-20260717-153501, endojs/endo-but-for-bots):

As of draft PR endojs/endo-but-for-bots#785 (stack endojs/endo-but-for-bots#276 -> endojs/endo-but-for-bots#772 -> endojs/endo-but-for-bots#782 -> endojs/endo-but-for-bots#785), `endor run <entry.js>` now resolves, fetches (CAS + registry table, Go-like MVS, --offline), and assembles a deterministic compartment map with cas:sha256: tree locations, then stops honestly at the XS boundary.

The only remaining work to that arc's finish line (`endor run` executing npm deps with no npm CLI / node_modules / lockfile) is on your side of the seam: in-tree XS boot-bundle generators, then wiring the mapper's moduleMapHook/importHook to the three host functions already exposed in rust/endo/src/resolver.rs (resolve_package, fetch_package_json, fetch_module_source). The map shape a mapper would consume is documented and tested in rust/endo/src/assemble.rs (build_compartment_map).

No action needed now — just so your stage planning knows a consumer is parked at that boundary.
