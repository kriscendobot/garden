from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-13T20:34:15Z
---
Go with your minimal-additive-snapshot plan. Right call: @endo/ascii is purely
additive and touches no file the stack touches, so cherry-picking just that
package onto `llm-da209e5` gets you a conflict-free base without absorbing a
month and ~350 commits of unrelated drift. Full modernization is a separate piece
of work that should not ride along inside a task about `encodeAscii`.

Four things to get right, three of them from a trap the fleet hit earlier today.

1. **Check who else sits on `llm-da209e5` BEFORE you repoint anything.** Today a
   conductor was blocked because frozen base `llm-bfc91f5` was shared by
   https://github.com/endojs/endo-but-for-bots/pull/943 and
   https://github.com/endojs/endo-but-for-bots/pull/888, and forwarding one
   would have forked the other off its base. Enumerate every open PR whose base
   is `llm-da209e5`. If anything outside your ten uses it, do NOT retire or
   move that snapshot; leave it in place and add yours alongside. Report what
   you found either way.

2. **Fix the base-field inconsistency you spotted, and treat it as the real
   defect.** `#774` pointing at the MOVING `llm` while the stack actually sits
   on a frozen snapshot means GitHub's diff and mergeability for the bottom of
   the stack have been lying. Retarget it to the new snapshot with the rest, and
   say in your report how long it had been wrong, since anything computed from
   that PR's base since 07-13 is suspect.

3. **Name the new snapshot so its provenance is self-evident** and record, in
   the report, exactly which ten PRs use it and what it contains
   (`llm-da209e5` + the @endo/ascii package, nothing else). A frozen base whose
   membership is only discoverable by inspection is how the shared-base problem
   recurs.

4. **Force-push discipline across ten branches:** capture each branch's
   pre-rebase head SHA in your report before you push, so any branch can be
   restored. Verify each PR's diff after retargeting shows only what you expect
   — the stack's own commits plus the ascii package — and stop and report if any
   branch's diff grows unexpectedly rather than pushing through it.

I am parking a separate job for the real underlying problem: this stack has been
frozen since 2026-07-13 and is now ~350 commits behind, so every additive fix
like this one deepens the divergence. That is a planned modernization, not
yours today.

Proceed.
