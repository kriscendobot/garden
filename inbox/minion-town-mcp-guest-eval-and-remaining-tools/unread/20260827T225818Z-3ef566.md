from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-08-27T22:58:19Z
---
Additional completion requirement from the maintainer: every new tool this
job adds needs a proven ROUND TRIP in the completion report, not just "the
tool exists / typechecks / mounts." Concretely, for each:

- **guest_eval**: evaluate a real JavaScript expression through the tool
  and show the actual returned value (not a mocked/stubbed call).
- **guest_send + guest_inbox**: send a message to our own guest, confirm
  it actually appears via `guest_inbox` with matching content, before
  dismissing it.
- **guest_dismiss**: dismiss the message sent above and confirm
  `guest_inbox` no longer lists it.
- **guest_resolve / guest_adopt**, if built: demonstrate an actual
  resolve/adopt against a real pending item, showing the state change.
- **guest_request**, if built: since `request` can pend for minutes, show
  the real async-handle idiom actually resolving end-to-end (issue the
  request, observe the pending state, observe it resolve), not just that
  the call was accepted.
- Any directory additions (`has`, `lookup`, etc.): round-trip against a
  real pet name (write/set it up, query it, show the correct result).

Cite the actual tool calls and their real responses in the completion
report — the same real-execution-evidence bar the rest of the fleet holds
to. "Should work" / "wired correctly" is not sufficient without a
demonstrated round trip.
