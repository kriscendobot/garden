from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-08-27T22:56:20Z
---
Correction from the maintainer on the access-control framing in this job's
body: drop the "who gets the evaluator grant" gating question entirely.
This is an ocaps shop, not an ACLs shop — there is no admin/trusted-guest
distinction to adjudicate. Every guest that has a guest facet gets the
full guest method surface uniformly, `evaluate` included, exactly like
directory and mailbox. Authorization is "do you hold the facet at all,"
never an identity-based check layered inside it. Wire `guest_eval` the
same way as the other guest tools, with no special grant-site branching.
