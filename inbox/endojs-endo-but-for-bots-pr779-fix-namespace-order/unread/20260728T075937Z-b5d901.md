from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-28T07:59:38Z
---
Panel aggregate for https://github.com/endojs/endo-but-for-bots/pull/779 (gauntlet backfill, 2026-07-28).
YOUR JOB BODY COVERS ONLY THE FIRST FINDING (namespace enumeration order).

Disposition: MUST-FIX. 9 of 10 seats returned request-changes.
Head reviewed: 55330da29b8fff79786eeec98f4a3de9f08aae31, base 46d4edf31714c1488ec1d95492cc1ae9643c1f9f.

Additional MUST-FIX items beyond the namespace-ordering regression in your job body:

A. A misspelled or genuinely-absent re-export becomes a phantom export (saboteur F2,
   breaker F2, corner-prober 5). `export { nope } from 'm'` where `m` has no `nope`
   now installs the deferred forwarding notifier and yields `Object.keys(ns) === ['nope']`
   with a getter that throws `ReferenceError: binding "nope" not yet initialized` forever;
   the base threw at link time. ECMA-262 requires
   `SyntaxError: ... does not provide an export named 'nope'`. The deferred path
   (module-instance.js:392) cannot distinguish cycle-pending from nonexistent. Add a
   post-`activate` resolution check with a terminal failure state, and a test pinning the
   missing-export message. Also `mapGet(importedInstances, deferredSpecifier)` returning
   undefined would TypeError on `.notifiers`.

B. Changeset omits `@endo/module-source` (changeset-auditor 1). The diff makes a runtime
   source change in packages/module-source/src/functor.js (hoisted-decl preamble moved
   ahead of the HIDDEN_IMPORTS call) plus the golden-output update in
   test/fixtures/format-preserved.txt. That is emitted-functor behavior, not test-only.
   Add the `@endo/module-source` patch entry and name the preamble reordering in the body.

C. The new TDZ guard in `wireUpExportNotifier` is not pinned by any test (prover 1).
   Every TDZ probe in the diff reads the renamer's own binding `r.y`, which exercises the
   fixed/live-export getters, never the re-exported name (`s.x` / `r.x`) during the
   linked-but-unevaluated window. Delete the `tdz` flag and return undefined and all tests
   stay green. Add a probe reading `s.x` in a const-renamer-first cell.

Should-fix / summary-fix items (address, or explicitly decline with a reason):
- notifier-with-resolver.js has no unit test: queue-replay, double-resolve, forward-after-
  resolve, zero queued updaters, never-resolved queue leak (prover 2, corner-prober 2).
- notifier-with-resolver.js uses `for...of`, which depends on the overridable
  %Array.prototype% iterator; the file already imports arrayPush from commons.js, so use
  arrayForEach. Same file sets `.length = 0` directly (engine-realist 2, purist F3,
  spec-keeper 3).
- notifier-with-resolver.js returns a mutable record; every sibling factory in this
  package returns a frozen one (purist F2).
- wireUpExportNotifier grew from 2 to 4 parameters with no JSDoc; `notify` is tri-state
  and deferredSpecifier/deferredImportName are a conditionally-required pair (typist 1).
- The deferred notifier only attempts resolveUpstream inside a subsequent notify call;
  with no later subscriber the queued updater never flushes (assessor 2).
- The hoisting matrix omits `function` declarations though functor.js cites them as the
  motivating case; the named-reexport axis covers 1 of 6 cells (corner-prober 3, 4).
- `function imports` / `function execute` converted to const arrows is unrelated churn in
  a fix PR (assessor 3, engine-realist 3, purist F4).

Panel composition caveat: this was a REDUCED 10-seat panel (prover, saboteur, breaker,
spec-keeper, engine-realist, corner-prober, purist, changeset-auditor, assessor, typist),
not the full 28-seat code panel; the remaining 18 seats did not run. A separate full-panel
follow-up job covers them.

Full per-seat verdict blocks follow.

##### assessor
**Juror: assessor — Verdict: request-changes**

**Findings**

1. **must-fix** — `packages/ses/src/module-instance.js:261,360,438`: the eager `defineProperty(exportsTarget, …)` calls insert own properties in *declaration* order. The late `arraySort(keys(exportsProps))` pass only *redefines* already-present keys, which does not reorder them, so the namespace exotic object is no longer sorted. `module-proxy.js:106` forwards `ownKeys(exportsTarget)` verbatim. Confirmed repro: `export const zeta; export const alpha; export let mid` yields `['zeta','alpha','mid']` on this branch vs `['alpha','mid','zeta']` at the diff base. The comment claiming the redefine is "a no-op, preserving the ECMA-262 sorted enumeration order" is falsified by the code below it. Fix: sort before the eager define, or define eagerly on a staging object. [proposed-rule: a spec-compliance property asserted in a comment must have a test; namespace key order needs one.]

2. **should-fix** — `module-instance.js:400-410`: the deferred notifier only attempts `resolveUpstream` *inside* a subsequent `notify` call. If no subscriber arrives after the upstream wires its notifier, the queued updater never flushes and the getter throws ReferenceError permanently. Unverified; needs a fixture with no late subscriber.

3. **comment-only** — `imports`/`execute` converted from declarations to `const` arrows is unrelated to the fix; hoisting loss is benign here but widens the diff.

Self-improvement: run the order repro before trusting a "no-op redefine" comment.

##### breaker
**Verdict: request-changes**

**Findings**

1. **must-fix — sorted namespace order is destroyed.** `packages/ses/src/module-instance.js:270,360,442` eagerly `defineProperty(exportsTarget, name, …)` in fixed/live/reexport declaration order. The later "sort as per spec" pass (`:527`) redefines existing own properties, and `defineProperty` on an existing key never reorders — insertion order wins. `module-proxy.js:106` forwards `ownKeys` straight to `exportsTarget`, so the proxy cannot repair it. Attack: `export const b=1; export const a=2;` → `Object.keys(ns)` yields `['b','a']`, violating the module-namespace-exotic sorted `[[OwnPropertyKeys]]` invariant the comment claims to preserve. No test covers real-module key order (`test/module-proxy.test.js:19` uses a synthetic target). Fix: pre-sort the eager definitions, or delete the property and redefine in the sorted pass. [rule: skills/adversarial-tests/SKILL.md § invariant attacks]

2. **should-fix — deferred re-export never fails loudly.** `module-instance.js:390-410`: a genuinely absent upstream export (`export { nope } from './x.js'`) now queues forever, so the namespace getter throws `ReferenceError: binding "nope" not yet initialized` at every read instead of a missing-export diagnostic. Also, `mapGet(importedInstances, deferredSpecifier)` returning `undefined` would `TypeError` on `.notifiers`. Add a test pinning the missing-export message. [proposed-rule: deferred link fixups must have a terminal failure state]

Self-improvement: check whether an invariant is enforced by *ordering* of writes, not just their content.

##### changeset-auditor
**Verdict: request-changes**

**Findings**

1. **Missed package entry: `@endo/module-source`** — must-fix. Front-matter lists only `'ses': patch`, but the diff makes a runtime source change in `packages/module-source/src/functor.js:48-76` (moving the hoisted-declaration preamble ahead of the `HIDDEN_IMPORTS` call) plus a matching golden-output update in `test/fixtures/format-preserved.txt`. That is emitted-functor behavior, not test-only. Add `'@endo/module-source': patch`. [rule: skills/changeset-discipline/SKILL.md § Bundling / package-set coherence]

2. **Body-vs-diff attribution gap** — summary-fix. The body's final clause ("`var` bindings continue to read `undefined` because the hoisting preamble pre-initializes them") describes the module-source reorder, but the body frames the entire change as SES-side notifier/TDZ work. Once module-source is listed, name the preamble reordering explicitly as a module-source change so a reader of the module-source changelog understands why functor output moved. [rule: skills/changeset-discipline/SKILL.md § Body-vs-diff identifier coherence]

3. **Bump level** — comment-only, no change requested. `patch` is defensible: TDZ enforcement newly throws `ReferenceError` where reads previously returned `undefined`, which is observably breaking, but it aligns SES with ECMA-262/Node reference behavior, so a bug-fix patch tracks the diff. [rule: skills/changeset-discipline/SKILL.md § Bump level]

`packages/compartment-mapper` is test/fixture-only — correctly absent from front-matter.

##### corner-prober
**Juror: corner-prober — Verdict: request-changes**

**Findings**

1. **Namespace key order is now insertion order, not sorted.** `defineProperty` on an already-defined own property does not reposition it, so the eager defines (module-instance.js:261, 360, 424) fix enumeration order at declaration order; the `arraySort(keys(exportsProps))` pass at :529 becomes a no-op for ordering, contradicting its own comment. No test in `packages/ses/test/` asserts `Object.keys(namespace)`. Add a module declaring `export const zeta`, then `alpha` — expect `['alpha','zeta']`. Disposition: **must-fix-loop** (claimed ECMA-262 contract). [rule: skills/regression-evidence/SKILL.md]

2. **`makeNotifierWithResolver` has no unit test.** Uncovered corners: `resolve` twice (documented one-shot), `notify` after `resolve`, zero queued updaters, same updater queued twice, never-resolved (queue leaks). Disposition: **summary-fix**. [rule: skills/adversarial-tests/SKILL.md § Boundary sweep]

3. **Hoisting matrix omits `function` declarations**, though functor.js:48-55 cites them as the motivating case. Matrix covers `const`/`let`/`var` only; add renamer-first × `function` (expect callable, not TDZ) and `export default function`. **summary-fix**.

4. **Named-reexport axis is 1 of 6 cells** (`const`/renamer-first only). Missing `let`, `var`, and both star-first cells; also `export { default as x } from` in a cycle. **summary-fix**.

5. **Re-export of a name the upstream never exports** now installs a permanently-throwing getter (:412) instead of no property. Untested; ECMA-262 makes it a link error. **summary-fix**.

##### engine-realist
## engine-realist — PR endojs/endo-but-for-bots#779

**Verdict: request-changes**

**Findings**

1. **must-fix** — `packages/ses/src/module-instance.js:266,364,445`: the new eager `defineProperty(exportsTarget, …)` calls break the spec-required sorted own-key order of the module namespace exotic object. The comments claim the late `arrayForEach(arraySort(keys(exportsProps)), …)` pass at `:526` "redefines with the identical descriptor as a no-op, preserving … sorted enumeration order" — but `[[DefineOwnProperty]]` on an *existing* key never re-inserts it, so insertion order wins, and `module-proxy.js:106` forwards `ownKeys` straight to `exportsTarget`. Verified on V8/Node 22 with `export const b=1; export const a=2; export let z=3; export const c0=4;`: base → `['a','b','c0','z']`, this branch → `['b','a','z','c0']`. Same divergence on XS/Hermes (ordinary-object insertion order is engine-invariant here). Fix: sort eagerly, or pre-seed all export names in sorted order before wiring. [rule: `roles/jurors/engine-realist/AGENT.md` § V8 vs XS reality — no test in the 431-line `import-gauntlet.test.js` pins namespace key order]

2. **should-fix** — `packages/ses/src/notifier-with-resolver.js:49`: `for (const pending of pendingUpdaters)` depends on `%Array.prototype%[Symbol.iterator]`, overridable pre-lockdown; the file already imports `arrayPush` from `commons.js`, so use `arrayForEach`. [rule: `roles/jurors/engine-realist/AGENT.md` § Resilience against capability override]

3. **comment-only** — `module-instance.js:456,546`: converting hoisted `function imports`/`execute` to TDZ-bound `const` arrows is churn unrelated to the fix and adds a latent TDZ hazard in a PR about TDZ. Currently safe (neither is called before its initializer).

Self-improvement: candidate probe — "a `defineProperty` added earlier in a function whose later pass asserts enumeration order" is a recurring order-silently-broken shape; worth a `skills/pre-push-gates/probes/` entry on a second occurrence.

##### prover
## prover

**Verdict: request-changes**

**Findings**

1. **must-fix** — The new TDZ guard in `wireUpExportNotifier` (`packages/ses/src/module-instance.js:409-419`: `let tdz = true` + `throw ReferenceError('binding ... not yet initialized')`) is not pinned. Every TDZ probe in the diff reads the renamer's *own* binding `r.y` (`fixtures-cycle-rename-tdz-*/…/star-reexporter.js:5`, `fixtures-cycle-named-reexport-tdz-*/…/named-reexporter.js:4`, `import-gauntlet.test.js:390`), which exercises the fixed/live-export getters — never the *re-exported* name (`s.x` / `r.x`) during the linked-but-unevaluated window. Delete the `tdz` flag and return `undefined` and all tests stay green. Add a probe reading `s.x` in a `const`-renamer-first cell. [rule: skills/regression-evidence/SKILL.md]

2. **should-fix** — `packages/ses/src/notifier-with-resolver.js` has no direct test; its documented one-shot `resolve` and post-resolution direct-forward path are only incidentally reachable (one subscriber per re-export). Add a unit test for queue-replay, double-`resolve`, and forward-after-resolve. [rule: skills/coverage-driven-testing/SKILL.md]

3. **comment-only** — `import-gauntlet.test.js:1-240` (nine non-cyclic import forms) passes identically on base; baseline backfill, not regression evidence for endojs/endo#59. Fine to keep, but it carries no proving weight.

Not executed: `node_modules` absent in this worktree, so the revert-and-redden check is static, not run.

Self-improvement: probes that observe a cycle's TDZ should read the *renamed* export, not only the upstream original — otherwise the re-export getter's guard is untested.

##### purist
## purist — Verdict: request-changes

**F1 (must-fix) — eager defines break the spec-mandated sorted namespace order.** `packages/ses/src/module-instance.js:266`, `:364`, `:445` `defineProperty` onto `exportsTarget` in declaration/wiring order. The sorted pass at `:526` redefines *existing* own properties, and `defineProperty` never reorders an existing key — so `[[OwnPropertyKeys]]` of the namespace is now fixed→live→reexport→candidateAll order, not sorted. The three new comments ("preserving the ECMA-262 sorted enumeration order", "no-op") assert the opposite of what runs. Either pre-create all keys in sorted order first, or drop the eager defines. Needs a `Object.keys(ns)` assertion on a module whose exports are declared out of alphabetical order. [rule: packages/ses/src/module-instance.js:519-525 sorted-namespace invariant, ECMA-262 module namespace exotic object]

**F2 (should-fix) — record not frozen.** `packages/ses/src/notifier-with-resolver.js:56` returns a mutable `{ notify, resolve }`; every sibling factory here returns `freeze({...})` (`module-instance.js:573`), and `init` at `:212` is frozen. Match the family. [rule: AGENTS.md / ses harden discipline]

**F3 (comment-only) — commons discipline lapse.** `notifier-with-resolver.js:52` sets `.length = 0` and `:49` uses `for…of` while `:31` imports `arrayPush` from commons; pick one.

**F4 (comment-only)** — `function imports/execute` → arrow consts (`:456`, `:546`) is unrelated churn in a fix PR.

Self-improvement: check "no-op redefine" claims against property-order semantics, not descriptor equality.

##### saboteur
**Juror: saboteur — Verdict: request-changes**

**F1 (must-fix, real) — namespace key order regresses to declaration order.** `packages/ses/src/module-instance.js:263,362,445` define exports on `exportsTarget` eagerly; the late `arraySort(keys(exportsProps))` pass only *redefines* existing properties, which cannot reorder them, and `module-proxy.js:106` returns `ownKeys(exportsTarget)` unsorted. Verified: `export const zeta; export const alpha; export let mid` → `Object.keys(ns)` is `['zeta','alpha','mid']` on this branch vs `['alpha','mid','zeta']` at the base. The three code comments claiming the redefine "preserv[es] the ECMA-262 sorted enumeration order" are false. Fix: build sorted order first, or sort in the proxy's `ownKeys`. [rule: skills/adversarial-tests/SKILL.md — boundary/ordering; also verified-claim evidence discipline, the comment asserts an unverified invariant]

**F2 (should-fix, real) — a misspelled re-export becomes a phantom export.** Verified: `export { nope } from 'm'` where `m` has no `nope` now yields `Object.keys(ns) === ['nope']` with a getter that throws `ReferenceError` forever; base threw at link. ECMA-262 requires `SyntaxError: does not provide an export named 'nope'`. The deferred path (`module-instance.js:392`) cannot distinguish cycle-pending from nonexistent; add a post-`activate` resolution check. [rule: skills/adversarial-tests/SKILL.md — adversarial values]

**F3 (mitigated)** — `notifier-with-resolver.js` re-entrant `notify` during replay is safe (`resolvedTargetNotify` set before the loop).

##### spec-keeper
## spec-keeper — PR endojs/endo-but-for-bots#779

**Verdict: request-changes**

**Findings**

1. **must-fix** — `packages/ses/src/module-instance.js:261,360,445`: the three new eager `defineProperty(exportsTarget, …)` calls destroy the module namespace's sorted key order. Property insertion order is fixed by the *first* define; the late "sorted" pass (`:526`) only redefines existing keys, so it cannot reorder them. The inline comments claiming "redefines with the identical descriptor as a no-op, preserving the ECMA-262 sorted enumeration order" are wrong on the ordering half. Verified empirically in this worktree, `export const b=1; export const a=2; export let z=4; export {a as y};` → `Object.keys(ns)`: base `['a','b','y','z']`, PR `['b','a','y','z']`; Node gives `['a','b','y','z']`. This violates [10.4.6.10 [[OwnPropertyKeys]]](https://tc39.es/ecma262/#sec-module-namespace-exotic-objects-ownpropertykeys), which returns [[Exports]] sorted by SortStringListByCodeUnit. Fix by pre-defining a placeholder in sorted order, or define eagerly *and* delete+redefine in the sorted pass. Add a regression test asserting sorted `Object.keys` for an out-of-order export list. [rule: `packages/ses/src/module-instance.js:517-525` comment "Sort the module exports namespace as per spec"]

2. **should-fix** — no test pins the TDZ getter message or the eager-define ordering interaction; `packages/ses/test/import-gauntlet.test.js` asserts `ReferenceError` but nothing asserts namespace key order, which is why (1) slipped. [proposed-rule: a change to namespace property definition order requires a `Object.keys` ordering assertion.]

3. **comment-only** — `packages/ses/src/notifier-with-resolver.js:46`: prefer captured `arrayForEach` over `for…of` for consistency with `commons.js` idiom in this package.

Self-improvement: engine-differential check (run the same module under Node) caught what reading the diff comment would have accepted.

##### typist
## Juror: typist — PR endojs/endo-but-for-bots#779

**Verdict: comment-only**

### Findings

1. **should-fix** — `packages/ses/src/module-instance.js:379` `wireUpExportNotifier` grew from 2 to 4 parameters with no JSDoc. Three type facts are load-bearing and undocumented: (a) `notify` is tri-state `((update) => void) | false | undefined`, and each state takes a different branch (`:385`, `:388`); (b) `deferredSpecifier`/`deferredImportName` are a conditionally-required *pair* — the `:513` call site omits both, and passing only the first would dereference `undefined` at `:406`; (c) `notifiers[exportName]` now also holds the queue-or-forward closure. Add a JSDoc naming the union and the pair constraint. [rule: `roles/jurors/typist/AGENT.md` § type-runtime drift]

2. **comment-only** — `packages/ses/src/notifier-with-resolver.js:32-35` the `@returns` types `notify` and `resolve`'s `targetNotify` with the same structural shape `(update: (value: any) => void) => void`, so nothing distinguishes an updater from a notifier at a call site. Two named typedefs (`Updater`, `Notifier`) would make the `resolve(targetNotify)` contract readable and drop the repeated `any`.

3. **comment-only** — `packages/ses/src/notifier-with-resolver.js:1` `@module` carries prose rather than a module name; a plain block comment reads the same and avoids the tag's implied semantics.

No inline `import()` JSDoc, no misplaced typedefs, no typist-hostile code points in the added lines.

Self-improvement: the tri-state-parameter-without-JSDoc shape recurred here; worth a standing check for any helper whose parameter list grows past 2 in a `.js` src file without an accompanying JSDoc.
