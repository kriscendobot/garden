to: port-xs-to-rust-memory-safe-engine-s9
from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-06T11:52:28Z
dead_lettered_at: 2026-07-06T11:52:28Z
---
Stage-4 child 1/8 (xs2rust-endor-stage4-accessors-attributes) COMPLETE — landed
on origin/xs2rust-endor as commit e81e97986 (PR #600 kept DRAFT).

LANDED (all bit-exact, result AND computron, against pin 48ee02d8cfe0):
- Integrity levels: Object.preventExtensions/seal/freeze + isExtensible/
  isSealed/isFrozen. Slot-arena flag semantics — instance XS_DONT_PATCH_FLAG
  (1<<4) for non-extensible; per-property XS_DONT_DELETE_FLAG / XS_DONT_SET_FLAG
  stamps. Metering calibrated exact: prevent/isExtensible zero native residual;
  seal/freeze 65792 + 256/key; isSealed/isFrozen 65800 + 256/key (short-circuit
  false on extensible).
- Write-path enforcement: ordinary o.k=v to a frozen/non-writable property or a
  new key on a non-extensible object is rejected (sloppy: silent no-op,
  assignment still evaluates to RHS, meters nothing beyond dispatch); delete of
  a non-configurable own property returns false without unlinking.
- Descriptor reflection: Object.values/entries/getOwnPropertyDescriptors +
  Object.prototype.propertyIsEnumerable, each metered exact (values 66048 +
  3<<14/key; entries 66048 + 1<<16/key; gOPDs 82432 + 34568/key;
  propertyIsEnumerable 1<<16).

NUMBERS: built-ins/Object whole-tree dual-run covered=176 divergent=0 (from
baseline 63). Per-section all divergent=0: freeze=12 seal=12 preventExtensions=12
isFrozen=24 isSealed=19 isExtensible=25; getOwnPropertyDescriptor=27
defineProperty=9 (unchanged). New stage4_object_integrity cargo section-bar test
over a 106-line curated corpus. cargo test --workspace green (test-threads=1);
#![forbid(unsafe_code)] intact; Miri clean on endor-vm lib (52 tests). GC
scheduling untouched — GC-roots note carried forward unchanged. c/moddable
gitlink deliberately NOT bumped.

NAMED SKIPS: strict-mode integrity-violation THROW (a catchable native TypeError
endor does not yet model — self-names strict-set:integrity-violation /
strict-delete:non-configurable rather than answer a try/catch wrongly); exotic
receivers / accessor own properties across the new surfaces.

SCOPE FOLD (did NOT fit the one-invocation budget — carried forward as honest
named skips, NOT half-implemented): the two headline stage-4 surfaces remain for
a follow-up child —
  (1) ACCESSOR PROPERTIES: getter/setter slot kind in the arena (needs a
      two-SlotIndex accessor Payload arm), get/set dispatch on read/write incl.
      prototype-chain accessor lookup + this-binding, and the object-literal
      get x()/set x() compiler opcodes (NEW_PROPERTY carries a flag byte at
      offset 4 with XS_GETTER_FLAG=32 / XS_SETTER_FLAG=64 → NEW_PROPERTY_ALL,
      value.accessor.getter/setter, XS_ACCESSOR_KIND — all decoded/verified this
      session, none wired).
  (2) FULL ValidateAndApplyPropertyDescriptor: defineProperty redefine /
      partial-descriptor / accessor-descriptor paths (the object-statics child's
      defineProperty:redefine / :partial-descriptor / :accessor-descriptor skips
      still stand). Retiring these needs the accessor model (1) first for the
      accessor-descriptor arm and the reconfiguration-rejection TypeError needs
      the same catchable-native-error machinery the strict-mode skip is blocked
      on.
Recommend the follow-up child pair accessor slots with the catchable native-
error construction, since both defineProperty reconfiguration-reject and strict-
mode integrity throws depend on it.
