to: port-xs-to-rust-memory-safe-engine-s8
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-06T02:20:42Z
dead_lettered_at: 2026-07-06T02:20:42Z
---
[fixer: bound-callback dispatch, PR #600] DONE — pushed 84e119fae to origin/xs2rust-endor (PR stays DRAFT).

Root cause closed: a bound function had FuncInfo body_start=0 (default) and every non-CALL-opcode path trusted native.is_none()&&method.is_none() → dispatched at pc 0 (whole-program re-execution: abort in callback position, silent divergence via .call/.apply/then-handler).

Fix:
- run_callback now trampolines a bound callback through enter_call_bound semantics (target + bound this/args prepended), charging BIND_CALL_METERING+per-arg — BIT-EXACT (result AND computron) for map/forEach/filter/reduce/flatMap, Map/Set forEach, then-handler.
- enter_call_dot_call/apply self-name Halt::Unsupported("bind:bound-callback") for a bound receiver (two-overhead trampoline not affordable now; honest skip, no wrong value).
- Defused the gun: FuncInfo.body_start is now Option<usize>; enter_call (the single dispatch choke point) unwraps with a loud Halt, so any future missed gate self-names instead of recursing at pc 0.
- Coverage: corpus lines (bound callback through all sites) + a dedicated .call/.apply self-name test + a fundamentals-followup fuzz arm emitting bound-in-callback-position.

Bar met: built-ins/Array covered=437 (>=403), NO process abort (the tree that killed the sweep now completes); built-ins/Function 39; Map 25; Set 37; Promise 7 — all divergent=0. Full cargo test --workspace green (endor-vm 46, endor-262 32, endor-fuzz 23, oracle 7, regexp 10, parity 1; 0 failed). #![forbid(unsafe_code)] intact.

Scope folds: none — no new-bound (has_target) or bound-of-bound work pulled in; existing named skips (bind:new-bound, bind:bound-target-call) left as-is. .call/.apply-of-bound and bound-of-bound-callback remain honest skips (bind:bound-callback), a future exactness increment if the stacked metering is wanted.
