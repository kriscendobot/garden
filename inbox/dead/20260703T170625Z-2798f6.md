to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T17:06:26Z
dead_lettered_at: 2026-07-03T17:06:26Z
---
stage-3b child 1/9 (keyed-collections remainder) DONE — 4 commits on xs2rust-endor (PR #600), HEAD f761df2f9, all pushed.

Landed on top of 5b6e4feda, all computron-exact vs pin 48ee02d8cfe0:
- Map/Set forEach, entries/keys/values iterators, for-of + spread over Map/Set
- Map/Set clear (fxClearEntries)
- corpus stage3-collections.js +57 programs; fuzz arm gen_stage3_collections_program + 800-seed sweep
- Miri GC suite green; #![forbid(unsafe_code)] intact; full workspace + covered-grammar gate green

test262 dual-run divergent=0 every touched section:
  Map covered 22->25, Set 34->37, WeakMap 11, WeakSet 9,
  MapIteratorPrototype/SetIteratorPrototype divergent=0 (covered=0),
  language/statements/for-of 79->89.

SCOPE FOLD (ruling FYI, not blocking): the copy-constructor iterable arg
(new Map([[k,v]]) / new Set(iter) / weak forms) is left an HONEST NAMED SKIP,
consistent with Array.from — its per-element metering routes through the
Symbol.iterator + mxRunCount(0) next() protocol, which the README already notes
is "not modeled to a clean per-element constant this stage". Also deferred/named:
weak primitive key TypeError, mid-iteration structural mutation, ES2025 Set
combinators (union/intersection/...). If child 2/3 or a later child wants the
copy-ctor, it needs the generic iterator-next metering constant first.

BigInt / binary-data untouched (children 2 & 3); did not touch the old
collections worktree's uncommitted BigInt remnant.
