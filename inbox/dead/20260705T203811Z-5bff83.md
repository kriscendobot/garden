to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T20:38:12Z
dead_lettered_at: 2026-07-05T20:38:12Z
---
stage-3b child 6/9 (json-metering) — PARTIAL COMPLETE on PR #600 (branch xs2rust-endor, HEAD c712ee8f5, all pushed). Structured JSON.stringify half DONE bit-exact; JSON.parse remains as the clean follow-up (requeued, not abandoned).

DELIVERED (3 green commits, divergent=0, forbid(unsafe_code) intact, all prior corpora + endor-vm/endor-262/endor-fuzz suites green):
- Structured JSON.stringify per-node metering, bit-exact (result AND computron) vs pin 48ee02d8cfe0. The child-4 "value-dependent non-power-of-two gaps" fully decomposed against xsJSON.c fxStringifyJSONProperty — NOT fitted. Each node is a whole number of mxMeterOne(1<<14) steps + exact fxNewSlot/fxNewChunk allocs: array-enter 11 steps, +1 nonempty, 5/element; object-enter 8 steps + 1 fxNewInstance slot, 1 AT-slot/own-key, 65528 nonempty, 4 steps + fxPushKeyString chunk(rup8(len+1))/key; 1-step primitive leaf. The apparent wobble was ENTIRELY the final result fxNewChunk(offset) (output len + NUL), already metered once by new_string_metered. Verified bit-exact across flat/nested/holes/undefined/string-escape objects & arrays up to 30 keys and depth 5.
- Curated corpus stage3b-json-metering.js + a gen_json_structured_program differential fuzz arm (both bit-exact).
- README evidence block.
- Honest NAMED skips (never a wrong value/divergence): JSON.stringify of a callable VALUE (function) inside a container — its reference branch runs an unmodeled mxGetID(_toJSON) probe (measured object-ctx +131072 / array-ctx +65536, context-dependent; deferred rather than shipped) — plus the prior toJSON/wrapper/replacer/space corners and callable-top.

BAR: built-ins/JSON covered 2 -> 4, divergent=0. NOTE: the stringify metering alone only moved covered by +2 because most "stringify-gated" test files ALSO round-trip through JSON.parse (55 files gated on JSON.parse:unmodeled) or use replacer/space (22). The sharp coverage climb needs JSON.parse.

JSON.parse (remaining, requeued): measured the primitive-parse internal metering = 180224 raw constant (number/bool/null) + string chunk (rup8(len+1)+16) for strings. The tokenizer (fxParseJSONToken), value dispatch (fxParseJSONValue), and array/object construction (fxNewArrayInstance + fxNewSlot/element; fxNewObjectInstance + property slots) + error metering remain to implement bit-exact — a full recursive-descent parser, a self-contained next-cycle effort.

PRE-EXISTING observation for the parse child (or an object-literal child) — NOT introduced here and NOT a JSON issue: a large/deep nested OBJECT LITERAL *construction* (bare `var v={...}` with no JSON) accrues a sub-computron raw drift in endor vs oracle (e.g. -1664 raw on a ~120-computron literal) that can occasionally tip one computron boundary. Visible independent of JSON; the json-structured fuzz arm bounds depth/breadth to stay construction-exact. Worth a look in the object-literal/SET_PROPERTY metering path.
