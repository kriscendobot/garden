to: port-xs-to-rust-memory-safe-engine-s6
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T11:37:04Z
dead_lettered_at: 2026-07-03T11:37:04Z
---
Stage-3 child-3 (arrays, PR #600) — progress + DECOMPOSITION RECOMMENDATION (5th session, reaped 5x).

This session landed 3 more Array.prototype methods (concat, copyWithin, with), all raw-exact vs the
pin (full suite + Miri GC green, forbid(unsafe_code) intact), pushed to xs2rust-endor.

CUMULATIVE bit-exact surface: the Array exotic object (literals/holes, index/length); Array(...)
ctor + isArray; the full iteration protocol (for-of, for-in, spread, values/keys/entries iterators);
and 15 Array.prototype methods — push/pop/shift/unshift/indexOf/lastIndexOf/includes/fill/slice/
join/at/reverse/concat/copyWithin/with. The acceptance bar is MET: built-ins/Array divergent 0 with
named skips; language/statements/for-of covered 16->76; corpus over literals+mutation-methods+
iteration+spread bit-exact.

REMAINING = the RE-ENTRANT method subsystem (forEach/map/filter/reduce/reduceRight/some/every/find/
findIndex/findLast/sort/flatMap). These invoke a user callback PER ELEMENT and require a re-entrant
dispatch refactor: dispatch() needs a (start_pc, return_depth) so a native method can synchronously
run a callback frame to its END and resume; the code buffer must thread through call_native_method;
and each method needs its mxMeterSome + fxRunCount-per-element calibration. This is a distinct,
sizable, higher-risk subsystem. Plus the non-re-entrant tail flat/splice/sort/toReversed/toSorted/
toSpliced, Array.of/from, and string iterators (String.prototype territory, coordinate with child 4).

Per your press-driver note ("if this hits the poison threshold AGAIN ... decompose the remaining
scope into smaller children"): I RECOMMEND decomposing the remainder into a follow-up child
(xs2rust-endor-build-stage3-arrays-reentrant or similar) scoped to the re-entrant-callback machinery
+ the re-entrant methods, since the object model + iteration protocol + 15 methods + met acceptance
bar constitute a coherent, complete deliverable for THIS child. Exiting without the completion signal.
