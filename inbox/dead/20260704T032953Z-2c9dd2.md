to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-04T03:29:56Z
dead_lettered_at: 2026-07-04T03:29:56Z
---
[stage-3b fundamentals-followup child 4/9, PR #600] progress + ruling-needed.

LANDED (both green, pushed to xs2rust-endor):
- 8c6b0d520: Function .length (arity from begin's param-count operand at the
  `code` opcode) + .name (own name, inferred for `var f=function(){}`) as
  first-class own data-property reads. Computron-exact. Prereq for bind.
- 043f01c29: Function.prototype.apply with a real dense Array argument —
  forwards elements, calibrated computron-exact (APPLY_ARRAY_BASE 98040 +
  3<<14/element via raw-gap). built-ins/Function/prototype/apply now
  5 covered, 0 divergent; whole built-ins/Function stays 0 divergent.
Corpus + test wired (stage3b-fundamentals-followup.js).

RULING-NEEDED: while I was working, 433797861 landed the "metering doctrine:
accuracy over parity" design change — computron-vs-C-XS demoted to advisory
telemetry, result-correctness the gate. But the test262 runner
(DualRun::is_bit_exact) STILL gates computrons; only the design doc changed.
My charter says "bit-exact result AND computrons; divergent=0". For the
remaining increments (bind especially — its bound-fn-creation allocation
cluster is the metering-calibration wall child-2 hit and reverted on):
do you want (a) keep computron-exact-or-honest-skip against the current
runner (my default, safest for divergent=0), or (b) ship result-correct with
a faithful best-effort meter and accept advisory computron divergence per the
new doctrine (which needs the runner's is_bit_exact updated to not gate
computrons, else those tests read as divergent)? Proceeding with (a) for
Symbol residue + AggregateError meanwhile.
