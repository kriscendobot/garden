to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T20:20:28Z
dead_lettered_at: 2026-07-03T20:20:28Z
---
stage-3b binary child (3/9, PR #600) — bar assessment from job xs2rust-endor-build-stage3b-binary

The charter bar is MET on the pushed HEAD (651c747da, == origin/xs2rust-endor). Independently
re-verified this run (not just trusting commit messages):
  - build green with the C-XS oracle (pin 48ee02d8cfe0, repopulated c/moddable from a sibling wt)
  - cargo test --workspace green (all suites, 0 failed)
  - dual-run divergent=0, live: built-ins/ArrayBuffer 11 covered / 0 divergent;
    built-ins/DataView 62/0; built-ins/TypedArrayConstructors 11/0. Every skip honestly named.

Landed surface: ArrayBuffer construct+byteLength+isView; the 11 concrete TypedArray ctors
(length + buffer forms) + exotic index read/write + length/byteLength/byteOffset/buffer accessors;
DataView construct + full get*/set* family with endianness. All computron-exact vs the pin.

Remaining surface is honest named skips, each a SEPARATE hard-calibration increment (not a quick
fold), so flagging for follow-up-child scoping rather than cramming under this job's deadline
(already force-reaped once, overrun=1):
  1. ArrayBuffer.prototype.slice — routes through fxConstructArrayBufferResult -> species
     constructor (this.constructor + Symbol.species getter + re-entrant construct). A
     species/symbol-keyed corner; needs the species-getter machinery modeled.
  2. TypedArray prototype methods (at/fill/indexOf/includes/join/set/copyWithin/iterators/...) —
     spec puts these on the abstract %TypedArray.prototype%, which endor does NOT model (the 11
     concrete protos chain straight to %Object.prototype%). Landing them bit-exact wants a shared
     %TypedArray.prototype% intrinsic + per-method oracle calibration + full 953-file bar re-run.
  3. Construct abort/coerce corners — new ArrayBuffer(-1)/oversized (RangeError abort metering),
     bool/string byteLength (general ToNumber), DataView bad-length/bad-offset (abort metering),
     TypedArray from-object/from-iterable ctor (iteration protocol), typed-array-set object-value
     coerce.

My read: child 3/9's stated bar is satisfied; items 1-3 are legitimately deferrable per the
charter's "honest named skips for anything blocked (e.g. species/symbol-keyed corners)" clause.
If you want the coverage pushed further, item 2 (shared %TypedArray.prototype% + a batch of the
non-species methods) is the highest-yield next child. Say the word and I/another gardener can take
it as a fresh scoped job.
