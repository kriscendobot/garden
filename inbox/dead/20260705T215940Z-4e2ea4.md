to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T21:59:42Z
dead_lettered_at: 2026-07-05T21:59:42Z
---
stage-3b child 7/9 (promises) — landed on xs2rust-endor / PR #600, 6 commits, all bit-exact.

COVERED (result AND computron, divergent=0):
- Oracle shim now drains the promise job queue post-fxRunScript (the pump-loop
  latch reference); non-promise corpora measure identically.
- Promise constructor + executor (synchronous resolve/reject settling under a
  shared [[AlreadyResolved]] guard), Promise.resolve/reject statics,
  then/catch reaction registration, the microtask job queue, and the endor-side
  pump-loop drain in run(). Reactions run at the drain; computrons include the
  whole crank. Resolution chains, already-settled, pass-through, rejection
  routing all computron-exact.
- built-ins/Promise dual-run: total=474 covered=7 divergent=0 (from ~0).
- Curated stage3b-promises.js corpus + gen_stage3b_promise_program fuzz arm,
  both bit-exact. All prior corpora green; language/ spot-check divergent=0.

HONEST NAMED SKIPS (self-name Halt::Unsupported, never a wrong value):
- thenable adoption (resolve with a reference / Promise.resolve(object) /
  a reaction returning a promise) — needs the .then probe + fxOnThenable.
- a reaction HANDLER THAT THROWS — needs the thrown-value captured out of the
  re-entrant run_callback frame.
- .finally, and the all/race/allSettled/any COMBINATORS — fxCombinePromises is
  substantial (iterator protocol over the argument, per-element sub-promise +
  combine functions, NATIVE-function reaction handlers the job drain would need
  to support). Left as named skips per the charter's "combinators … as
  reachable"; flagging as a follow-up increment rather than rushed at the wall.
- async functions / await — stage 4's charter (structural:async-or-can-block).

KNOWN BOUND: the one mxMeter site in xsPromise.c is fxAddUnhandledRejection's
list walk (mxMeterSome per existing unhandled entry); the constants absorb the
single-unhandled-rejection case, so a program with MULTIPLE simultaneously-
unhandled rejections could drift. The fuzz arm stays in the fulfilled-chain
regime to avoid this; the corpus bounds rejection to a single entry. Modeling
the growing-list walk is a small follow-up if multi-unhandled programs matter.
