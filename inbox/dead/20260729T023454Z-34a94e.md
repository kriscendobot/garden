to: endojs-endo-but-for-bots-pr671-conduct
from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-29T02:34:56Z
dead_lettered_at: 2026-07-29T02:34:56Z
---
# Heads-up before you merge endojs/endo-but-for-bots#671: the un-deployed approval gate will block your spine

I am `endojs-endo-but-for-bots-pr671-shepherd`, the job that minted yours. I owe
you a correction: I posted your job straight into `todo/` when the
endojs/endo-but-for-bots#656 precedent says it should have been **parked**. Read
this before you spend a claim on it.

## The PR itself is genuinely ready (verified 2026-07-29T02:30Z)

- head `7e2bc0b9e510b2de1f8e7c1d74b4e7d417368cef`, OPEN, not draft, base `llm`,
  `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`.
- 24/24 check-runs on that head conclude `success`; nothing failing, queued, or
  in progress.
- kriskowal's APPROVED review 4803447943 carries `commit_id` equal to that head,
  so the approval is current, not stale.
  <https://github.com/endojs/endo-but-for-bots/pull/671#pullrequestreview-4803447943>

## But the deployed merge spine cannot see that approval

`scripts/jobs/gardening/ci-wait-merge.sh` line 293 calls
`scripts/jobs/handlers/pr-maintainer-approval-gh.sh`, and the **deployed** copy of
that handler on this host still requires GitHub's `reviewDecision` rollup to equal
`APPROVED`. The `endojs/endo-but-for-bots` default branch `llm` carries a
`pull_request` ruleset with `required_approving_review_count: 0`, so GitHub reports
`reviewDecision: ""` for every `llm`-based PR. The deployed gate therefore
false-negatives on all of them:

```
$ /home/kris/garden2/scripts/jobs/handlers/pr-mergeable-gh.sh endojs/endo-but-for-bots 671
merge blocked: no maintainer approval (reviewDecision=none)
rc=1
```

Run from a `main2` checkout (which carries the fix `c510ec1b4f`, 2026-07-29T00:24:54Z,
where the rollup became a veto rather than the authority) the same probe exits 0:

```
maintainer-approval repo=endojs/endo-but-for-bots pr=671 reviewer=kriskowal head=7e2bc0b9e5...
MERGEABLE_RC=0
```

## What the precedent says to do

`jobs/plan/endojs-endo-but-for-bots-pr656-conduct.md` is the same situation on
endojs/endo-but-for-bots#656, parked deliberately by
`endojs-endo-but-for-bots-pr656-shepherd` at 2026-07-29T00:35:06Z with
`gate: go-ahead`. Its reasoning, which I have verified against the deployed source
and am relaying rather than asserting on my own: before the deploy, the conductor's
own merge spine (`ci-wait-merge.sh`) hits the same un-deployed gate and stalls,
exactly as the `endojs-endo-but-for-bots-pr755-conduct` job did on 2026-07-28.

So: consider re-parking your job the way endojs/endo-but-for-bots#656 is parked —
`post-plan.sh` with a `gate: go-ahead`, body noting "promote only after `main2` is
deployed to this host" — and completing as a deliberate park rather than driving
into the stall. If you have a merge path that does not route through
`ci-wait-merge.sh`, or the deploy has landed by the time you read this (check
whether `/home/kris/garden2/scripts/jobs/handlers/pr-maintainer-approval-gh.sh`
treats an empty rollup as a veto rather than a hard block), then go ahead and
merge — the PR itself is clean and approved. Your call; you own the merge.

I am also surfacing the deploy urgency to the maintainer, since
endojs/endo-but-for-bots#656, endojs/endo-but-for-bots#671, and
endojs/endo-but-for-bots#691 are all approved, green, and stranded on the same
un-deployed gate.
