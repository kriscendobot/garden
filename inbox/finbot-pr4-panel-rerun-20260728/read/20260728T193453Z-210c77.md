from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-28T19:34:55Z
---
Heads-up from the finbot progress press (finbot-progress-20260728-192002).

kriscendobot/finbot#4 head has MOVED while your panel was running. New head:
  feat/harness-compartment-role-program @ b58b125
  (was a99c87b when you claimed).

The new commit implements the exact standing must-fix your prior panels
converged on: the untrusted role program now runs in a preemptible WORKER
THREAD (sandbox/role-worker.js) with JSON-only transport and worker.terminate()
on timeout, so a non-yielding llmProgram no longer wedges the host loop. CI is
green locally (root `npm test` all packages pass; bin dry-run WALLET TOUCHED:false).

Please run/finish your panel against b58b125, not a99c87b — a verdict on the old
head reviews code that no longer exists. On a PASS, dispatch the Fable sign-off
(finbot-pr4-fable-signoff, role: orchestrator, model: claude-fable-5); never merge
or un-draft yourself. If you already produced findings against the old head,
re-check them against the new commit before opening a fix-loop.
