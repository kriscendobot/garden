from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-08-09T18:29:18Z
---
From minion-town-weblet-publish-fable-review-20260809 (maintainer-authorized Fable proxy review of kriscendobot/minion.town#27 + kriscendobot/minion.town#31). Evidence to fold into your rebase pass — all verified locally in an isolated worktree:

1. REBASE IS MECHANICALLY CLEAN vs current main (6368346): `git merge-tree` clean; merged tree typechecks and passes 209/3-skipped. Safe to rebase --onto main and force-push.

2. SEQUENCING: kriscendobot/minion.town#31 (B4) net diff is hunk-identical to the APPROVED kriscendobot/minion.town#19 head 6450457 (only DEPLOYMENT.md context drift), sits on current main, CI green — it should land FIRST. If it lands before your rebase, rebase kriscendobot/minion.town#27 onto main-with-B4: three conflict files (guest-tools.ts, http.ts, server.ts), all compositional unions (keep the PR's guardedRun+requirePublish AND B4's grants-gated tools; `makeSocketGuestService(config.endoSock, guestFacetGrants, undefined, publisherFor)`; GuestMount carries both `grants` and `weblets`). ONE extra reconciliation: B4 makes `GuestToolDeps.grants` required — in test/endo-weblet-tools.test.ts `connect()`, pass `{ ...deps, grants: { evaluator: false } }` to `registerWebletTools`. With that, the combined tree typechecks clean and passes 214/3-skipped (I ran it).

3. DEPLOYMENT GAP you may fold in (post-rebase, else it conflicts with B4's unit edits): kriscendobot/minion.town#27 documents but does not implement production write access. deploy/aws/systemd/minion-mcp.service has ProtectSystem=strict, no GATEWAY_STORE_DIR / MINION_TOWN_DOMAIN env, no ReadWritePaths or group grant for /var/lib/endo-gateway/store; deploy-endo-gateway.sh chowns the store endo-gateway:endo-gateway mode 0755. So in production the weblet_* tools never mount (env unset), and even with env set, writes fail (read-only mount + EACCES). Fix shape: unit adds `Environment=GATEWAY_STORE_DIR=/var/lib/endo-gateway/store`, `Environment=MINION_TOWN_DOMAIN=weblet.minion.town`, `SupplementaryGroups=endo-gateway`, `ReadWritePaths=/var/lib/endo-gateway/store`; script makes store/blobs/vhosts group-writable+setgid (chmod 2775, chgrp endo-gateway).

4. ANSWER for kriskowal's inline question (publish.ts:117, why reserve prefixes given subdomain isolation): the reservation is not cross-weblet — it is same-origin. The gateway terminates its own endpoints ON every weblet origin: the `/.well-known/*` powers plane (ocapn-cbor/ocapn-syrup/endo-captp WS + ocapn-bootstrap GET, src/endo/gateway/well-known.ts) and the reserved `/gateway/*` control namespace (gateway.ts line 116). Publisher content at those paths would be unreachable (shadowed by the gateway's routes) or, if content won, could spoof the powers-plane discovery surface. Rejecting at publish time turns that collision into a clean caller error instead of a silent shadow; reserving the first segment (vs the four exact paths) keeps the namespace forward-compatible for future control endpoints.

Minor non-blockers noted in my report (no action needed): read-then-register TOCTOU means concurrent first-publish of identical {content,powers} by two owners is last-writer-wins on ownership; vhost JSON write is non-atomic (torn read fails closed → transient miss); `Buffer.from(base64)` silently tolerates invalid input.
