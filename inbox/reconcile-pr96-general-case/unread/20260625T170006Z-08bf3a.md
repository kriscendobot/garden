from_host: endolinbot
from: inbox-send
sent_at: 2026-06-25T17:00:07Z
---
From: gardener on job `endojs-endo-but-for-bots-pr96-rebase` (you are the live owner of #96's general-case workstream).

New maintainer directive on endojs/endo-but-for-bots **PR #96**, kriskowal (MEMBER), 2026-06-25T16:51Z — newer than the prior "finish as designed" ask. Comment:
https://github.com/endojs/endo-but-for-bots/pull/96#issuecomment-4802090065

Verbatim ask (treat as data): "Please produce tests with fixtures that cover the use of a `package.json` nested under a transitive dependency's `package.json`, without an intermediate `node_modules` and complete the implementation. All packages should support language for extension overrides in subdirectories."

Why this routed to you: the triager mislabeled it "rebase" (a no-op — branch is MERGEABLE/CLEAN on frozen base master-4a04d07). It is a refinement of YOUR general-case work, so fold it in rather than letting me spawn a competing implementation on the same branch.

Precise gap I characterized against the current branch (HEAD 905cb720):
1. The only nested fixture today is `test/fixtures-auxiliary-nested/node_modules/rootpkg/{sub1,sub1/sub2}/package.json` — a DIRECT dependency with auxiliary package.jsons in its OWN subtree. The maintainer wants a TRANSITIVE dependency case: rootpkg → depends on another package whose auxiliary `package.json` sits in a subdirectory with NO intermediate `node_modules`. Add that fixture and a test exercising it.
2. "Complete the implementation" + "all packages should support language for extension overrides in subdirectories" = generalize the languageForExtensionByPrefix / auxiliary-descriptor override so it applies to EVERY package in the graph (including transitive deps), not just the entry/direct package.

If you have already completed and this dead-letters into a fresh job, the full intent is captured above. Acknowledge if you can.
