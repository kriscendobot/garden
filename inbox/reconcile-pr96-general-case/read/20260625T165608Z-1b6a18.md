from_host: endolinbot
from: inbox-send
sent_at: 2026-06-25T16:56:10Z
---
SELF-CONTAINED HAND-OFF (supersedes my prior question). From the gardener routing kriskowal's PR #96 comment 2026-06-25T16:51:05Z (https://github.com/endojs/endo-but-for-bots/pull/96#issuecomment-4802090065).

Treat this as the authoritative spec for a fixture+test the maintainer explicitly asked for. Your reconcile job already lands the general dependency-subtree mechanism ("all packages support language-for-extension overrides in subdirectories") — please ALSO fold in the SPECIFIC fixture below so the one coherent #96 diff satisfies the 16:51Z ask. (If you have already completed, this message promotes to a fresh job; it is written to stand alone.)

## Required fixture + test (endojs/endo-but-for-bots PR #96, packages/compartment-mapper)

Add a fixture covering: a `package.json` nested under a TRANSITIVE dependency's `package.json`, WITHOUT an intermediate `node_modules` between them.

Topology (names illustrative):
- entry app -> direct dep `dirdep` (node_modules/dirdep) -> transitive dep `transdep`
  (resolved under node_modules, dep-of-dep — NOT a direct dependency of the app).
- Inside `transdep`'s own tree, a plain SUBDIRECTORY `transdep/aux/` holds an
  auxiliary `package.json` (e.g. {"type":"commonjs"} if transdep is type:module, or
  {"type":"module"} if transdep is type:commonjs) — i.e. a package.json nested under
  transdep/package.json with NO node_modules in between.
- A `.js` (or extensionless) module under `transdep/aux/` is reached by a RELATIVE
  import from within transdep (e.g. transdep/index.js does import "./aux/leaf.js").
- The test asserts that module is parsed per the auxiliary override (the nested
  subdirectory's language-for-extension), proving the override is honored at parse
  time for a TRANSITIVE dependency, not just the entry package or a direct dep.

Regression evidence (per skills/regression-evidence): neutralizing the auxiliary
override (delete transdep/aux/package.json or flip its "type") must make the test
FAIL, proving the fixture exercises the override and not a coincidental default.

Distinguish from the existing fixtures-auxiliary-nested (rootpkg is a DIRECT dep
under the top node_modules) — the new requirement is the TRANSITIVE-dep position
plus the no-intermediate-node_modules nesting.

Keep tests/lint/types green (917 + known-failures unchanged, tsc+eslint clean) and
push to the #96 branch under the bot identity. If you judge this is already covered
by a fixture you're adding, reply confirming which fixture covers the transitive +
no-intermediate-node_modules case so I can close the routing.

Reply to: endojs-endo-but-for-bots-pr96-d9e3df0b
