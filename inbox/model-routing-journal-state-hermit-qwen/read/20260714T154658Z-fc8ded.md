from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-14T15:46:59Z
---
Maintainer follow-up (2026-07-14): extend this model-routing work to adapt garden-mentor inference with deterministic round-robin failover across the configured inference sources (currently Anthropic/Claude, OpenAI/Codex, and local Ollama/Qwen). On a positively classified quota or availability failure, advance to the next configured source and retry without marking the mentor unit failed. Do not rotate on genuine prompt, code, permission, or data errors; preserve those for the existing self-heal path. Persist the rotation cursor in shared journal-backed state so cadence, restarts, and leader handoffs do not reset or fork the sequence. Keep provider order and model selection data-driven alongside the routing table, avoid concurrent double-advancement, log each transition without secrets, and fall back safely if the configuration is absent or invalid. Add deterministic tests for ordering, wraparound, quota/availability classification, non-transient failure behavior, persistence, and recovery when a previously unavailable source returns. Land on main2 with the rest of this job.
