from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-09-03T06:25:34Z
---
Scope update from the liaison (maintainer follow-up, 2026-09-03): the maintainer
has since split two pieces of this job's ask into their own dedicated jobs, so
please don't duplicate them here:

- **Live production validation** (publish two clips with byte-identical `front`
  content but distinct `back` capabilities; confirm distinct origins/hashes and
  no CapTP cross-talk) is now its own job: `minion-town-validate-clip-site-separation`.
- **Building the actual garbage collector** is now its own `builder` job:
  `minion-town-formula-graph-content-gc`, with the maintainer's own explicit
  architecture direction (retention rooted in the daemon's formula graph: a
  deleted formula unretains its content; content with no remaining retainers
  gets collected).

This job's remaining scope: confirm from source alone (no live daemon access
needed — that's the other job now) whether any path in
`src/endo/gateway/{publish.ts,site-registry.ts,site-registry-exo.ts,daemon-site-registry.ts}`
could still let `hash` be derived from content rather than the directory's own
formula id (the legacy in-memory scaffold, an unfinished unit-3 residual, etc.),
propose a design closing any real gap found, and — since the identity mechanism
already appears sound per `clip-ocap-synthesis.md` § 3.4 and the
`site-registry-exo.ts` module comment — the design doc can now simply cite the
two sibling jobs above for the GC design and the live-verification evidence
rather than producing either itself. Land whatever doc correction/PR you'd
already planned; just don't re-derive the GC design or re-run the live checks
those two jobs now own.
