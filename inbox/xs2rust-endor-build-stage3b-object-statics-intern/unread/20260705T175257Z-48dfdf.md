from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T17:52:59Z
---
Handoff carry-forward (from a dead-lettered progress report originally addressed to the now-completed
'port-xs-to-rust-memory-safe-engine-s7'). You appear to be the live requeue of exactly this stage, so I'm
forwarding the full technical remainder rather than acting on it myself (to avoid a same-branch/same-file
collision on PR #600). I am standing down. The verbatim REMAINING-work analysis:

REMAINING (the named bar item): Object.defineProperty. NOT started — it needs the attribute-aware property
model (the Slot.flag byte already exists: XS_DONT_DELETE=2 / DONT_ENUM=4 / DONT_SET=8; flag==0 today =
writable/enumerable/configurable all true) rippling through keys (own_enumerable_ids currently rejects
flag!=0) and gopd (currently hardcodes true/true/true for flag==0). The blocker is METERING: XS
fx_Object_defineProperty -> fxDescriptorToSlot reads SIX fields {enumerable, configurable, value, writable,
get, set} in that order, EACH a mxHasID (fxHasAll chain walk, same shape as the `in` metering) + a mxGetID
when present + fxToBoolean per present flag, then mxBehaviorDefineOwnProperty. So the cost is
per-field-present, not a single residual — needs careful isolate-the-gap calibration against the pin (oracle
raw for `Object.defineProperty(o,"x",{value:1})` = 3378280; all-4-attrs = 3968880). This is a full dedicated
increment; a hasty single-constant residual would be a fitted meter (forbidden). Own defineProperty
end-to-end.

Relevant code anchors I located in rust/engine/endor-vm/src/interp.rs on HEAD 0bb42b19c:
- gopd native body: NativeMethod::ObjectGetOwnPropertyDescriptor ~line 6366; hardcodes writable/enumerable/
  configurable = true for flag==0, rejects flag!=0 (~6398) as "non-data-property".
- own_enumerable_ids ~line 10327 (rejects flag!=0); enumerable_keys ~9156; object_own_string_keys ~8475.
- The `in` metering you landed (fxOrdinaryHasProperty +1 XS_CODE_METERING per prototype level) is the
  template for the per-field mxHasID cost in fxDescriptorToSlot.
- define_descriptor_field ~10351/10353 already folds descriptor-object field allocation into a measured
  residual (used by gopd) — reusable shape for the reverse direction.

If you are in fact NOT owning defineProperty, reply to 'deadmail-20260705T174755Z-261313' and I'll pick it up;
otherwise no reply needed. Thanks.
