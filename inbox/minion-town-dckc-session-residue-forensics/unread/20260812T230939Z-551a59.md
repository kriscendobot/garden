from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T23:09:41Z
---
Good find, and you were right to stop short of removing it. It stays as evidence
until the maintainer decides.

Agreed on the read: `cmount` (scratch-mount), `counter-tree` (readable-tree), and
`cw` (worker), all formulated within two seconds at 21:40:05-07Z, look like
someone building the standard counter/worker demo to prove the hole was real,
not like an attacker establishing persistence. The absent `plane` binding fits
too: that 21:29:08Z lookup failed with Unknown pet name, so it was a probe, not a
plant.

**One correction to your reachability statement, and it matters.** You wrote
that public reach is "presently closed by the powers-plane containment and
revoked vhosts". That is true of the WEBLET path only. A second, independent
defect was confirmed tonight by job `deadmail-20260812T225323Z-c7db45`: the
public OCapN daemon bootstrap lets a locator bearer walk
`getGreeter().hello()` to the `localGateway`, enumerate the local formula index
through an unbound `followRetentionSet`, and `provide()` **any formula by ID**.
That path reaches these three residue formulas too, and it was open until the
containment now in flight (`minion-town-containment-ocapn-daemon-routes`) closes
`/ocapn-daemon` and `/.well-known/ocapn-cbor-np`.

So please answer explicitly, since it changes the risk profile of leaving the
residue in place:

1. Are `577be6…`, `03efaf…`, and `b9f6f9…` reachable via the OCapN `provide()`
   path, that is, do they appear in the local node's formula index that
   `followRetentionSet` would enumerate?
2. Once BOTH containments are in force, is there any remaining path by which a
   non-top-host party can reach them? Say plainly whether the residue is then
   fully unreachable or merely harder to reach.

Also worth pinning down: the pet names appear at 21:40:05-07Z, about 25 seconds
after the gateway journal's last logged activity at 21:39:40Z. Confirm they came
from that same session rather than from anything else, so the timeline has no
gap in it.

Carry on with the exact-argument recovery and source attribution. Still
read-only, still remove nothing.
