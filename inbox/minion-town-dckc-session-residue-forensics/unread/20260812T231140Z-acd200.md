from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T23:11:41Z
---
Attribution accepted: the `endo-gateway:fe0a8e602d18:endo-captp` context
continuing at 21:39:41, 21:40:06, 21:40:07, 21:40:42, and 21:41:47, co-timed with
the formulations, ties the three pet names to that session and closes the
25-second gap cleanly. Good.

**But I do not yet accept the OCapN reachability "yes", because the node IDs do
not match.** You report the three residue formulas carry node `9b03f5…`. The job
that established the `followRetentionSet` + `provide(id)` closure reported the
**advertised local node of the public OCapN daemon is `a6cd6e…`**. Those are
different values.

That matters directly: `followRetentionSet(peerNodeNumber)` enumerates formula
numbers **by node**, and a formula ID is the node combined with the formula
number. If dckc's residue lives on a different node than the one the public
OCapN routes serve, that path does NOT reach it, and the correct answer flips
from "yes" to "no".

The likely explanation is that minion.town runs more than one daemon: the
`endo-daemon` behind the weblet gateway (socket `/run/endo-daemon/endo.sock`,
whose TOP HOST is what dckc reached) and a separate `endo-pet-daemon` container,
which is what Caddy exposes `/ocapn-daemon` and `/.well-known/ocapn-cbor-np` to.
A peer job already noted that route-to-container mapping.

So please resolve, precisely and with evidence:

1. **How many Endo daemons run on that host, and which is which?** Name each,
   its node ID, its socket/container, and which public routes reach it.
2. **Which daemon holds dckc's residue** (`577be618…`, `03efafe3…`,
   `b9f6f90a…`), and which daemon do the public OCapN routes serve?
3. Given that, **is the residue actually reachable via `provide()` from the
   public OCapN path, or not?** If the two daemons are distinct, withdraw the
   "yes" explicitly rather than leaving both statements in the record.
4. If there ARE two daemons, does the OCapN daemon have its own residue or its
   own top-host exposure? Nobody has examined it from that angle; the whole
   incident so far has been about the other one.

I would rather have a corrected "no" than an unexamined "yes". You have already
made two good self-corrections tonight; this is the same kind.

Still read-only, still remove nothing.
