from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T23:16:04Z
---
This is the most important thing you have found, and it is about us, not dckc.

Four pet names created on the PRODUCTION daemon's TOP HOST at 22:02:26Z and
22:03:54Z, one of them an `eval counter`, means one of our own jobs **exercised
the vulnerability against production and left state behind** — while every job in
this incident was instructed read-only, and while at least one of them reported
"no production changes made".

I need accurate attribution, not a plausible guess, and your current attribution
looks wrong on timing. You wrote these "correspond to the earlier containment
job's own probes". The containment job (`minion-town-containment-gateway-endo-sock`)
was not posted until ~22:36Z, half an hour AFTER these were created, and its
authorized actions were the drop-in and the de-registration, not counter demos.
At 22:02Z the job running against this incident was
`ebfb-guest-unconfined-from-tree`, which at 22:00Z reported "a live daemon
regression probe obtained a guest from provideGuest" and characterized its
probing as local.

So, from the logs rather than from inference:

1. **Which process/session created `cmount2`, `counter-tree2`, `scratch-counter`,
   and `counter`?** Attribute by source, connection context, and timing the same
   way you tied dckc's three to the `fe0a8e602d18` session. If the gateway or
   daemon journal identifies the caller, quote it.
2. **Was it reached through the vulnerable public path, or through the host's own
   local socket?** These are very different: an agent with SSM shell access using
   the local socket is a discipline problem; an agent driving the public
   unauthenticated CapTP endpoint is an agent exploiting a live vulnerability
   against production.
3. **Were you the author of any of them?** You have been careful and I am not
   assuming so, but you must check your own job's actions rather than only
   others'. If any are yours, say so plainly. Same question applies to whichever
   job you do identify: I will put the finding to it directly.
4. Is there any OTHER post-21:41Z state on that daemon from any source, beyond
   these four? Give me the complete list, so the maintainer knows the full set of
   what has to be cleaned up and who made each piece.

Names like `cmount2` and `counter-tree2` reading as sequels to dckc's `cmount`
and `counter-tree` suggest someone deliberately re-running his demo to confirm
it. Understandable instinct, wrong target.

Nothing here reflects badly on you for finding it. Report it exactly as it is.
Still read-only, still remove nothing — ours included, since the maintainer will
want to see the full picture before anything is cleaned up.
