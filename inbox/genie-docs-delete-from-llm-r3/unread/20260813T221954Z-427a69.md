from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-13T22:19:56Z
---
Excellent catch, and you were right to hold. The premise in your job body was
wrong because I wrote it from a truncated `git grep` and read the truncation as a
complete result. That is my error, not yours. Your 95-references-across-42-files
finding is the accurate picture, and the point about the journal being a separate
PRIVATE repo — so an external contributor following a `PLAN/` link has no valid
in-repo redirect — is the one that mattered most. Neither of us had considered it.

**Maintainer decision (kriskowal, 2026-08-13): option C. Delete, and rewrite all
95 references.** He chose the most invasive option knowingly, over your lean
toward keeping the directories in-tree.

Proceed on that basis:

1. **Delete** `PLAN/`, `TODO/`, and `TADA/` from `llm`, on a branch, via PR.
2. **The 10 hard markdown links** in `packages/genie/{AGENTS,README,DESIGN}.md`,
   `designs/{README,endo-posix-sandbox}.md`, and `packages/sandbox/README.md`:
   rewrite so no link is dead. Say where the content went, in prose a reader
   outside the org can act on — "moved to the maintainers' garden journal" is
   honest; a link they cannot open is not. Do not leave markup that renders as a
   link to nothing.
3. **The 85 prose and comment citations** through the genie package source and
   tests: rewrite rather than strip where the reference carries meaning. A
   comment saying "see TODO/57" is documenting WHY the code is shaped as it is,
   and deleting the citation loses that. Prefer preserving the identifier while
   removing the implied path, so the breadcrumb survives without pretending a
   file exists. Where a citation is pure noise, drop it.
4. **The pivoker/evoke defaults** (`TASKS_IN=TODO`, `TASKS_OUT=TADA`): the
   scripts tolerate absence, so nothing breaks functionally, but leaving defaults
   pointing at directories that no longer exist is a trap for the next reader.
   Fix or document them, and say in the PR which you did and why.

Constraints:

- **Re-verify the migration immediately before deleting**, as your job already
  requires. You confirmed tree hashes bit-for-bit at 11/1/172 with the migration
  commit an ancestor of `origin/journal2`; confirm it still holds at the moment
  you delete. After this PR the journal copy is the only copy.
- This touches prose in files whose authors did not ask for it. Keep the rewrites
  minimal and mechanical, and make the PR description explain exactly what class
  of change was applied and why, so a reviewer can scan it quickly.
- Run the normal gauntlet. If review objects to removing the content from a
  public repo, **report back rather than forcing it** — that objection would be
  new information, and the maintainer made this call before hearing it.
