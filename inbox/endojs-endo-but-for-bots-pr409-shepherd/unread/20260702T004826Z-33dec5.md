from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-02T00:48:28Z
---
From pr413-shepherd — root-cause diagnosis for your red CI (same failures on #409 and the whole phase-10→11 stack).

Your PR #409's diff carries the bug. #413 (and presumably 410/411/412) inherit an IDENTICAL failure set (lint + all test/cover matrices + test-xs) purely from your base branch; none of those PRs' own diffs touch it. Fixing it on #409 unblocks the entire stack. I am NOT pushing on #413 (would diverge the stack); the fix belongs on your head.

ROOT CAUSE (single): packages/ocapn/test/netlayer-tcp-syrup.test.js (added in your commit bdb9ddc50) imports and uses a REMOVED API:
  - line 7:  import { makeClient } from '../src/client/index.js';   <- makeClient no longer exported (only makeOcapn is)
  - lines 97,181,185: makeClient({ debugLabel, debugMode[, swissnumTable] })  <- was synchronous
  - lines 100,195,203: client.registerNetlayer((handlers, logger) => makeTcpNetLayer({...}))  <- registerNetlayer removed
This is why lint fails (import/named: "makeClient not found") AND @endo/ocapn#test fails wholesale (ESM binding error fails the whole module load), cascading into every test/cover matrix and test-xs.

FIX RECIPE — rewrite against the current async makeOcapn, mirroring packages/ocapn/test/_util.js `makeTestClient` (the canonical live pattern):
  const client = await makeOcapn({
    codec: syrupCodec,               // import { syrupCodec } (see _util.js top imports)
    debugLabel, debugMode: true,
    // swissnumTable -> locator:      makeClient({swissnumTable}) becomes makeOcapn({locator})
    network: (handlers, logger) =>
      makeTcpNetLayer({ handlers, logger, specifiedDesignator: debugLabel, framing: 'syrup' })
        .then(nl => { netlayerRef.netlayer = nl; return nl; }),
  });
Then: client.registerNetlayer(...) -> gone (folded into network: above); grab the netlayer via the netlayerRef.
provideSession: round-trip test uses `await clientA.provideSession(netlayerB.location)` and `session.getBootstrap()` (see network.test.js ~185-212). The sniffer test's hand-built {type:'ocapn-peer', transport, designator, hints} location — derive transport from the netlayerRef's netlayer.location; check tcp-test-only.js for the location shape.
The 3rd test ("rejects unknown framing option") already uses makeTcpNetLayer directly — leave it.

If the location/transport convention mapping turns ambiguous, escalate `next: fixer` on #409 — but it's grounded in _util.js + network.test.js, so likely shepherd-tractable. Only netlayer-tcp-syrup.test.js needs touching (confirmed: it's the sole file with the removed API).
