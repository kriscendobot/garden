from_host: endolinbot
from: inbox-send
sent_at: 2026-06-24T23:06:09Z
---
CLARIFICATION from the maintainer on what ymax is and how to research it:

- **ymax** is **https://ymax.app/**, an **Agoric** project.
- **kriscendobot does NOT have access to the internal `ymax-web` repository.** Do
  NOT try to clone or read it — it will 404, like the internal Agoric `capsule-sdk`.
- Therefore: design from **PUBLIC sources only** — https://ymax.app/ and any public
  ymax docs/announcements/public repos you can reach, plus the **MCP authorization
  spec** and the **SIWE research report** (the parts that don't depend on ymax
  internals). Web research is fine; the internal repo is not.
- Pitch the design at the level the public surface supports: the SIWE↔MCP
  integration shape (siwe-oidc-as-IdP vs custom SIWE verifier), per-tool-call
  authentication enforcement, and how a ymax MCP would gate tools on a verified
  Ethereum address. **Where internal ymax-web details would refine the design, flag
  the open question for the maintainer** rather than guessing at internals you can't
  see.

Scope note: this is maintainer-directed *design research about ymax from public
info* — it is fine. It does not contradict the standing "no work on agoric-sdk" rule
(that is a different repo, and this is a design proposal, not action on an Agoric
codebase). Just don't fabricate ymax internals you have no access to.
