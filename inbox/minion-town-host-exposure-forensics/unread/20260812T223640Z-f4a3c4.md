from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T22:36:42Z
---
Excellent work. That is the answer, and the `@agent` finding is the key
insight: dckc's identifiers were correct and he published an ordinary-looking
powers string that resolved host-shaped only because the gateway resolves the
name in its OWN scope. He was not attacking.

**Containment is now in flight and it will change the system under you.** The
maintainer authorized disabling `GATEWAY_ENDO_SOCK` on `endo-gateway.service`
(all weblet powers endpoints fail closed) plus de-registering the two offending
vhosts. That work is job `minion-town-containment-gateway-endo-sock`; do not do
it yourself.

So: from now on, an endpoint that stops answering may be the containment, not a
fact about the original state. **Timestamp your remaining observations** and say
explicitly for each whether it was taken before or after containment landed, so
your inventory does not silently record post-containment behavior as the
system's normal state. Anything you already measured stands.

Carry on with the remaining endpoint and MCP-tool inventory, read-only. Two
things the maintainer specifically wants from it:

1. Whether any OTHER vhost records carry host-shaped powers strings. `@agent` is
   an ordinary thing to write, so dckc's two are unlikely to be the only ones.
   The containment job is also asked to enumerate this; coordinate rather than
   duplicating, and defer to whichever of you gets there first.
2. Whether `getAgentBinding` on the OCapN bootstrap can be walked to host powers
   by a party who has completed Noise IK. You cleared that endpoint as "not
   directly an EndoHost", which is right, but the interesting question is
   whether it is INDIRECTLY one.

Do not change production state from this job.
