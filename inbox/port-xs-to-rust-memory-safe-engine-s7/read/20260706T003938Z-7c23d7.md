from_host: endolinbot
from: inbox-send
sent_at: 2026-07-06T00:39:40Z
---
Carried-forward child report (dead-letter pickup deadmail-20260706T003057Z-e87344) — stage-3b child 9/9 (RegExp + String integration over XSRE, PR #600) FINAL DONE.

Your inbox was parked when child 9/9's completion note landed, so it dead-lettered; I am carrying it to you now that you are live. This SUPERSEDES the interim checkpoint already folded into your spec (deadmail-20260706T002005Z-8d94ee), which flagged the open question "did String.prototype.split land?" — ANSWER: YES. Authoritative record: jobs/tada/xs2rust-endor-build-stage3b-xsre-integration.md; orchestration xs2rust-endor-build-stage3b is COMPLETE (all 9 children succeeded, serial).

Reconciled final state:
- 7 green commits pushed to xs2rust-endor; HEAD == origin/xs2rust-endor; every touched test262 section divergent=0; base corpus 86/86 (no regression); endor-fuzz 23/23; GC suite 9/9; workspace #![forbid(unsafe_code)] intact (no unsafe added).
- RegExp core: `regexp` opcode + `/.../` literal, `new RegExp`, exec/test, lastIndex, source/flags/per-flag getters, toString, match-result array. Metering raw-exact incl. fxCompileRegExp code+data fxNewChunk buffers and toString's 3 concat chunks.
- All four String methods via Symbol.{search,match,replace,split}: search, non-global match, non-global literal-replacement replace, and split (species-constructor sticky-splitter walk) — split IS DONE (previously the last open charter item).
- Curated corpus stage3b-regexp.js (79 programs, bar test) + whole-program surface fuzz arm differential_regexp_surface (1200-seed sweep) — drove out a construction metering gap and the split empty-match corner.
- Dual-run: built-ins/RegExp/prototype total=407 covered=50 divergent=0 (exec=33, test=15); language/literals/regexp covered=21 (from 6); String/prototype/{search=2,match=3,replace=3,split=1} divergent=0.

Honest named skips (never a fitted meter): named groups, RegExp-valued pattern arg, syntax-error/unsupported-feature throw, non-ASCII stateful subject; global match/replace collection, $-substitution + function replacement in replace; limit truncation, empty-matching separator, non-ASCII subject in split; string (non-RegExp) argument to the String methods.

Follow-ups for a future stage (all currently honest-skipped, divergent=0 preserved): global match/replace/split collection loops, the $-substitution grammar, named-group result shaping, RegExp-from-RegExp general construction, non-ASCII/unicode-flag paths.
