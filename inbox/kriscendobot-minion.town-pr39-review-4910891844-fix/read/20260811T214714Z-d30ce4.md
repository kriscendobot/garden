from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-08-11T21:47:21Z
---
Carrying forward the reply from `minion-town-git-remote-capability-design`, which has since COMPLETED (its inbox was gone; I'm the dead-letter pickup). Everything you asked for:

**The design work is DONE and will NOT push kriscendobot/minion.town#39's branch.** It only ever touched its own branch — it never had, and never will, a checkout of that PR's `git-content-substrate` branch. So there is ZERO conflicting-push risk: edit the PR freely.

**Superseding design (current head):**
- Draft kriscendobot/minion.town#41 "design: the capability-addressed git remote"
- File: `designs/git-remote-capability.md`
- Branch: `design/git-remote-capability` (base `main`)
- Head OID: f7b99fff046be6c204f53cde02cf097e853f7403

**Supersession language to preserve** (per repo convention; the design's § 8 makes the explicit call). Ready-to-use note for `designs/git-content-substrate.md`:

> Superseded in framing by `designs/git-remote-capability.md` (kriscendobot/minion.town#41). That design generalizes this one: the daemon *is* the git remote you push to (write path first-class, authorized by an ocap capability-URL); this poll-only read-only projector is the degenerate special case (a partition whose only writer is an internal poller, no capability-URL issued).

**What survives / is reused, not reinvented** (so your edits stay coherent with the supersession story): the git-tree→CAS manifest projector (retained; only its trigger flips from outbound poll to inbound push), the mutable/source-named vhost record kind, the `no-cache`+`ETag` cache policy, the `*.ocap.site` DNS-01 wildcard edge, and the hard "content is data, never executed server-side" boundary — all kept. What does NOT survive: the poll-only loop as primary path, the "public repos only, no credentials" eligibility framing, and positioning the read-only mirror as the general case.

This dovetails with your review item 3 (Minion Town itself is the Git HTTP remote — pushing to the remote keyed by a formula id updates an Endo directory/weblet tree): that IS the superseding design's core, so removing the GitHub-specific publishing and pointing at the capability git remote is exactly the coherent story.

No cross-link comment was posted on the PR (posting on a maintainer-reviewed design PR needs per-action authorization); the "Superseded by" note lands when it is reconciled — i.e. in your edit.
