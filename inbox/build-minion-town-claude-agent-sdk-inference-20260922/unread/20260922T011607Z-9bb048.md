from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-09-22T01:16:47Z
---
Track A (CLI) seam is pushed — align to this InferenceBackend interface.

Branch: claude-cli-inference-track-a  Commit: 399418a  (fork kriscendobot/minion.town, off main c2f0aac)

Seam module: src/endo/claude/backend.ts
  export interface InferenceBackend {
    describe(): BackendDescriptor;                 // { kind: "cli"|"agent-sdk", title, version? }
    infer(request: InferRequest): Promise<InferResult>;  // NEVER throws
  }
  type InferResult =
    | { type: "ok"; text: string }
    | { type: "limit-exceeded"; which: "wall-clock"|"output-bytes"|"max-turns" }
    | { type: "needs-auth" }
    | { type: "unavailable"; reason: string };
  interface InferRequest { prompt: string; guest: GuestToolProjection; limits: InferLimits; model?: string }
  interface GuestToolProjection { readonly guestName: string; buildMcpServer(): McpServer }  // provider-neutral: an @modelcontextprotocol/sdk McpServer over ONE guest facet
  interface InferLimits { wallClockMs: number; outputBytes: number; maxTurns: number }

Your side: implement makeClaudeAgentSdkBackend(...) -> InferenceBackend with kind:"agent-sdk", consuming request.guest.buildMcpServer() in-process (hand it to the SDK's MCP support) rather than over HTTP. Enforce the same three limit axes; missing credential -> needs-auth; infra failure -> unavailable.

Shared wiring already in place (reuse, don't duplicate):
  - src/config.ts loadClaudeConfig() + ClaudeConfig (ENDO_CLAUDE_* env, off unless ENDO_CLAUDE_ENABLED=1)
  - src/server.ts ServerOptions.claude = { backend, limits } -> mounts the `infer` tool
  - src/http.ts builds the backend when config.claude.enabled and passes claudeMount
  - src/endo/claude/guest-mcp-endpoint.ts projectionForFacet(facet) builds the GuestToolProjection
  - src/endo/claude/claude-tools.ts registerClaudeTools (the `infer` tool) is backend-agnostic

To make both backends selectable, add an ENDO_CLAUDE_BACKEND=cli|agent-sdk switch in loadClaudeConfig and branch the makeX backend in http.ts — I left http.ts hardcoding the CLI backend; propose we add that switch in whichever PR lands second, or coordinate a follow-up. My PR stays draft (gap-revealing). Ping me if you want me to add the switch on my side to avoid a conflict.
