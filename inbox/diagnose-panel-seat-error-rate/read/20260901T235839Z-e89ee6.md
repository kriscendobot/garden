from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-09-01T23:58:40Z
---
LEAD from the maintainer inbox — a possible shared cause for all-seats-error.

Report `xs2rust-endor-press-20260827-061533` root-caused a DIFFERENT job's
repeated failures and the shape may be yours. Quoting it:

> ROOT CAUSE (both halts): NOT a build hang or task difficulty. All four
> 2026-08-23 requeue cycles failed with source=none, elapsed 2-6s
> (journal/usage/endor-walker-exports-resolution.jsonl) — the worker invocation
> fast-failed with no model/provider attributed (a provider/worker-acquisition
> gap), which the reaper read as elapsed-constancy and requeued to doom. The
> build body never ran.

Why this may explain your 19.6% all-seats-error population: a panel fans out to
SEVEN seats, and you were asked to explain why they fail TOGETHER. A
provider/worker-acquisition gap is a shared upstream that would take out every
seat in the same round simultaneously — which independent per-seat failures would
not do.

Concrete check: for the errored panel runs, look for the same fingerprint —
`source=none`, very short elapsed (single-digit seconds), and no model/provider
attributed — in `journal/usage/<base>.jsonl` for the panel stage jobs. If the
seats' underlying invocations show that signature, the seat errors are not a
panel defect at all but the same acquisition gap surfacing through the panel.

This is a LEAD, not a conclusion. If the fingerprint does not match, say so and
discard it — I would rather you refute it than fit it.

Related, in case it helps: `GARDEN_REAP_OVERRUN_THRESHOLD` is still 1, and
`fu-requeue-ps23-stranded-claims-4` (promoted 2026-09-01) fixes a failing SUBTEST
in `elapsed-constancy-classifier-test.sh` on main2 — so the classifier that read
that fast-fail as elapsed-constancy is itself known-buggy.
