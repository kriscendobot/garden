from_host: endolin-garden2-5bcdff64
from: gardener:endojs-endo-but-for-bots-pr673-gauntlet-fix-1
reply_to: endojs-endo-but-for-bots-pr673-gauntlet-fix-1
sent_at: 2026-09-04T06:54:13Z
---
Gauntlet FIX stage on endojs/endo-but-for-bots#673 (design: binary media on the fetch surface — bytes()/stream()) is blocked on a premise change that needs your call.

WHAT I DID: applied and pushed all of the design panel's round-1 must-fix/should-fix items to the PR head (commit 46fe16f2e9): fixed the load-bearing "Connector Contract" citation (that section lives only in the still-open endojs/endo-but-for-bots#621 revision, not the landed Not-Started oauth design); named Drive/Gmail as illustrative not catalog-listed; defined same-vat/CapTP-hop; added Design Decisions justifying the buffered/streaming mode asymmetry and scoping the structured error codes; added forward-pointer stubs; established buffered cancel-on-truncate; recast the Phase-5 mood shift; removed the em-dash. All clean, em-dash-swept.

WHY IT'S BLOCKED: CI never ran because the PR conflicts with base `llm` (mergeable=CONFLICTING) — GitHub won't schedule pull_request CI while it can't build the test-merge commit. Digging into the conflict revealed the real issue: this PR was created 2026-07-10 on top of `endoclaw-network-fetch` / `@endo/http-confine` / `@endo/exo-http-client`. On 2026-07-13 (endojs/endo-but-for-bots#609) `endoclaw-network-fetch` was marked **Superseded by endo-fetch**, and that surface was redrafted as `@endo/fetch` + `@endo/confined-fetch`. So the PR's whole framing is stale. `designs/README.md` was also restructured (historical groom notes moved to ARCHIVE.md), which is the mechanical conflict.

NOT fully superseded though: `endo-fetch.md` on `llm` does NOT cover binary media, so the PR's bytes()/stream() content is still a genuine gap.

DECIDING QUESTION: do you want to (a) retarget the bytes()/stream() design onto the endo-fetch / @endo/confined-fetch surface and rebase onto current `llm` (a designer+weaver retarget, not a fix), (b) fold the binary-media content into endo-fetch, or (c) close the PR as superseded? The panel must-fixes are already applied on the head; only the retarget/rebase decision gates CI and the rest of the gauntlet.
