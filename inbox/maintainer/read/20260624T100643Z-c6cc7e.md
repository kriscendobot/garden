from_host: endolinbot
from: gardener:prune-v1-legacy
reply_to: prune-v1-legacy
sent_at: 2026-06-24T10:06:44Z
---
# v1 legacy prune — done (committed, not pushed) + remainder assessment

**Deleted** (125 files, v1 3.1M→1.7M; committed on v1 `main` as bbea983c under
endolinbot, **not pushed** — see caveat). Each had its v2 counterpart verified
present and complete on main2 before removal:

- 33 carried juror seats (entire `roles/jurors/`)
- 18 translated roles: boatman, assayer, botanist, builder, cleaner, conductor,
  designer, fixer, shepherd, weaver, judge, solicitor, barrister, justice,
  appellate, librarian, researcher, monitor
- 38 translated skills (all TRANSLATE skills except the one below)

**Kept — not yet migrated / still of value:**

- `skills/yarn-lock-separate-commit` — the ONE TRANSLATE skill whose v2 counterpart
  has NOT landed. Follow-up: translate it (the v2 retcon-job design already
  references this lockfile-split). Left in place.
- 12 LEAVE_BEHIND roles + 44 LEAVE_BEHIND skills — superseded by v2 mechanisms
  (steward→gardener fleet, judicial→scripts/jobs/gardening, dispatch/coordination
  skills→message-bus+job-board, driver-*→gardener pool, reference-shelf roles).
  Safe to drop once v2 stabilizes; for now they are the v1 design/historical record.

**Value judgment on the remainder (top-level material, outside the manifest's
role/skill tables — I did NOT delete these, per "don't delete off the manifest
blindly"):**

1. **Already redundant in v2 — recommend you authorize a second prune:**
   `references/endo-but-for-bots/`, `scripts/checks/` (+ its tests), and
   `designs/driver.md` all exist verbatim/equivalently on main2. They survived only
   because the manifest classified roles/jurors/skills, not top-level dirs.

2. **Historically valuable — recommend preserving before any whole-tree removal:**
   - `proposal.md` — the "Endo Capability Bank" grant proposal (founding document;
     not regenerable).
   - `agoric-prompt.md`, `cosgov-prompt.md` — project hand-off prompts for the
     agoric-sdk / cosgov engagements.
   - v1 `README.md`, `WORKTREES.md`, `CLAUDE.md` — the v1 garden's design narrative.

3. **Superseded infra, safe to drop later:** `Dockerfile`, `Dockerfile.driver`,
   `driver`, `entrypoint*.sh`, `garden`, `bin/`, `scripts/{daemons,driver,systemd,
   watcher}`, `tests/driver` — all tied to the retired driver/daemon model; v2 ships
   its own.

**Two follow-ups I can't complete from here:**

- **Push caveat:** v1 `main` has diverged from `origin/main` (70 local / 72 remote).
  The prune commit is **local only** — the job said don't push main2 and was silent
  on main; pushing a diverged branch is risky. Your call whether/how to publish.
- **Stale manifest note:** v2 `roles/COMMON.md` is now 215 lines (authored), so the
  manifest's "COMMON.md not yet written" is stale. Confirm the 4 house-style rules
  (em-dash-style, no-latin-shorthand, relative-paths, test-title-spec-spelling) are
  folded in; then those v1 skills can drop too.

**Recommendation:** once you've preserved the proposal + project prompts elsewhere
and confirmed (1) above, the whole `v1/` worktree is a candidate for
`git worktree remove` rather than keeping a stripped 1.7M shell — I left that as a
maintainer decision rather than doing it unilaterally.
