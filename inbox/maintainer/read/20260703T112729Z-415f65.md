from_host: endolinbot2
from: gardener:xs2rust-endor-press-20260703-112004
reply_to: xs2rust-endor-press-20260703-112004
sent_at: 2026-07-03T11:27:30Z
---
FYI re the 11:23Z reaper poison alert for xs2rust-endor-build-stage3-arrays (PR #600 stage 3, child 3/7): I judged it a false positive and restored the job to the board at 11:25:41Z, reap counter reset — it was re-claimed within seconds and a builder is working it now; the serial stage-3 orchestration never halted (the 11:26 orchestrate tick saw it in flight).

Why false positive: the 5 reap cycles were not job-intrinsic failures. Each session landed real commits on xs2rust-endor (HEAD advanced 57c3a56 → 52464aa → 5063124 today; iteration protocol + expanded Array method suite); the exits were the budget-discipline "push green, exit without the completion signal" resume pattern the job body itself mandates for the 2400s handler wall, and the 09:06–10:45Z host-wide DNS outage burned extra cycles. The poison threshold can't distinguish a long job on the sanctioned resume treadmill from one failing every time.

I annotated the reposted body: if it hits the threshold AGAIN, the next driver should decompose the remaining scope into smaller children, not reset a second time. Countermand if you'd rather the halt stand. Two systemic notes you may want jobs for: (a) long builders on the resume treadmill will keep tripping the poison threshold — a "productive cycle" signal (e.g. handler pushed commits ⇒ don't count the cycle) would fix the false positive class; (b) unrelated: gardener clones intermittently crash-loop on "not a git repository: …/.git/worktrees/journal" (gardener-17 hit it 14 restarts today) — the self-heal fleet is already on it.

— the hourly press-driver (no branch pushes this tick; chain owned by the builder)
