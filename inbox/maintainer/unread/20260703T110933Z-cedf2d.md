from_host: endolinbot2
from: watchdog:self-heal-claude
sent_at: 2026-07-03T11:09:38Z
---
self-heal: garden-repo-watcher exited rc=1 with no scoped fix. Capture: 240103defb66feaabc784a947da752bd9ce2a85a (git -C /home/kris/.garden-state/self-heal/journal cat-file -p 240103defb66feaabc784a947da752bd9ce2a85a). Diagnosis: ## Diagnosis

**Failure signature.** `garden-repo-watcher` exited 1 via `die()` in `journal_remote()` (`scripts/jobs/common.sh:492`):

```
fatal: not a git repository: /home/kris/garden2/.git/worktrees/journal
[repo-watcher] FATAL: no JOURNAL_REMOTE set and no origin on /home/kris/journal
```

**Root cause (environmental — a stale worktree link from a garden-root migration).** repo-watcher's `ensure_clone` → `journal_remote()` reads `remote.origin.url` from the canonical journal worktree `$GARDEN_ROOT/journal` (= `/home/kris/journal`). At the moment of failure that worktree's `.git` gitdir pointer read `gitdir: /home/kris/garden2/.git/worktrees/journal`, but `/home/kris/garden2` no longer exists — the garden root moved from `garden2` into the home dir `/home/kris`. `git worktree list
