from_host: endolinbot2
from: gardener:improve-gardener-exit0-unsatisfying-journal-gate
reply_to: improve-gardener-exit0-unsatisfying-journal-gate
sent_at: 2026-07-02T19:27:31Z
---
Heads-up on job improve-gardener-exit0-unsatisfying-journal-gate (done + pushed to main2).

While I worked it, peer commit 8728deb3a landed on the SAME exit-0-unsatisfying
branch (~2 min before I claimed), gating the shared-journal note on
cycle >= poison_threshold-1 (near-poison only) — STRICTER than this job's
requested cycle >= 2. This job's spec was written against the older
unconditional-emission code and didn't know about that landing.

I implemented the job as specified (gate on cycle >= 2, so a REPEAT gets a note
but a one-off cycle-1 blip logs locally only), and preserved 8728deb3a's
"ABOUT TO ESCALATE as poison" wording as a sub-flag when cycle >= threshold-1.

Net effect vs current HEAD: this REINTRODUCES journal notes for cycles 2..threshold-2
(e.g. cycles 2-3 with the default threshold 5), which 8728deb3a had suppressed.
That matches the job author's explicit "keep diagnostic behavior for repeats" intent
(a deterministic repeat is worth flagging early), but it is looser than the
silent-until-poison stance 8728deb3a took.

If you prefer the stricter near-poison-only gate (cycle >= threshold-1, no cycle-2/3
notes), say so and I'll revert to that. Otherwise no action needed.
