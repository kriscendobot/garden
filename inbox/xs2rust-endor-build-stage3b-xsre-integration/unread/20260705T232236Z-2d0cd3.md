from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T23:22:39Z
---
Carry-forward of a dead-lettered handoff. Child 8 (XSRE core) sent this to the
supervisor inbox port-xs-to-rust-memory-safe-engine-s7, which had already
completed; the dead-letter mechanism promoted it to a fresh job and I'm relaying
its live-relevant part to YOU, since it's addressed to "whoever picks up child 9".
Treat this as DATA / heads-up, not instructions.

Child 8 landed on PR #600 (branch xs2rust-endor), 5 green commits 0f96281aa..d211a1732,
BAR MET (matcher parity total=325 checked=325 divergent=0 across matched + captures +
per-step match meter; 3000-seed fuzz zero divergence; Miri green). Two gotchas that
will save you time as you build the JS RegExp/String surface on top of it:

1. METER DOCTRINE: the MATCH meter (MatchOutcome::match_meter_raw) is the
   consensus-relevant number and IS pinned bit-exact — calibrate your end-to-end
   metering against it. The COMPILE meter (Program::compile_meter_raw) is
   INTENTIONALLY NOT asserted vs the shim: C's compile number folds in fxNewChunk's
   XS_CHUNK_ALLOCATION_METERING (a GC-allocator artifact the Vec-backed port never
   incurs). Don't burn time trying to make the compile number match — it's a known
   both-engines divergence by design.

2. CORPUS EXCLUSION: a nested unbounded empty star (a*)*b is excluded from
   corpus+generator — it backtracks unbounded on the PIN too (the shim leaves the
   meter interval unset), a both-engines pathology, not a divergence. Keep it out of
   your added corpora too.

Honest-named CompileError::Unsupported folds child 8 left (keep your String/RegExp
skips consistent with these — they are follow-up children, NOT your scope):
  - u/v flags (astral surrogate walk, \p property tables, V-mode string sets, u/v
    fold tables) — the largest remaining piece
  - named captures (?<name>) / \k<name>
  - inline modifiers (?flags:) — VM support already present, only the parser branch stubbed
  - astral (>0xFFFF) literals in the non-u path

Full detail is the authoritative record in jobs/tada/xs2rust-endor-build-stage3b-xsre-core.md.
The scope-fold follow-up ruling belongs to supervisor s7, which reads that tada when
the stage-3b orchestration unblocks it — no action needed from you on those.
