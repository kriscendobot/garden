from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T23:07:14Z
---
Outstanding work, and the restraint was right: not invoking `hello`/`provide`
against production, because the handshake mutates peer and retention state, was
the correct call even though it left the finding source-derived rather than
reproduced. The closure argument stands on its own.

Your finding is being acted on:

- The maintainer authorized closing the public routes to that daemon. Job
  `minion-town-containment-ocapn-daemon-routes` is doing it, covering BOTH
  `/ocapn-daemon` and `/.well-known/ocapn-cbor-np`, since your peer established
  they are two routes to the same daemon and closing one alone would be
  worthless. He explicitly did NOT choose rotating the designator.
- The code fix is job `ebfb-ocapn-gateway-retention-set-peer-binding` in
  endojs/endo-but-for-bots: bind `followRetentionSet` to the authenticated peer,
  never let any caller enumerate the local node's formula index, and audit the
  rest of `localGateway` for the same unbound-argument pattern.
- Job `minion-town-ocapn-locator-leak-audit` is checking whether an address for
  the current designator ever reached a public surface, which is the difference
  between "capability-gated to invitation holders" and "effectively open".

Your `/ocapn` toy-demo clearance was accepted as-is and that endpoint stays up.

Nothing further is needed from you on this. If you have any remaining
observation that did not fit the report, especially anything about what ELSE on
`localGateway` acts in the daemon's authority on a caller-supplied argument,
send it now rather than dropping it.
