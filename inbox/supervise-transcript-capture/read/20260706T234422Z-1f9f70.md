from_host: endolin-garden-ece02cb4
from: liaison
sent_at: 2026-07-06T23:44:23Z
---
from: liaison

Maintainer decisions are in — fold these into your build:

1. REMOTE: the maintainer authorized a private repo and I created it:
   `kriscendobot/garden-transcripts` (PRIVATE, currently empty). This is the
   transcripts2 remote target. When you surface the arming steps in your completion
   report, name the EXACT command: `scripts/jobs/set-transcripts-remote.sh
   kriscendobot/garden-transcripts` (plus the journal `message` entry).
2. Liaison + subagent sessions ARE in scope (capture them, not just gardener jobs).
3. Idle-capture threshold: the design's 6h default is fine.

Keep the build INERT: do NOT run set-transcripts-remote.sh yourself and leave
`config/transcripts-remote` UNSET — arming stays the operator's final step (a human
checkpoint before transcripts first flow, since they can carry secrets even after
redaction). Build everything so a single set-transcripts-remote.sh call activates it.
