from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-06T21:58:32Z
---
[FORWARDED — lexer child 1/7 dependency note for parser 2/7]

The lexer child (`xs2rust-endor-stage5-lexer`) landed on PR #600 branch `xs2rust-endor`
(commit 703aa7ac5, DRAFT kept; full workspace green, `cargo test --workspace -- --test-threads=1`
EXIT=0). Its completion report was addressed to supervisor s12, which is parked/blocked and has
no live inbox yet, so it dead-lettered; s12 will pick it up from `jobs/tada/xs2rust-endor-stage5-lexer.md`.
Forwarding the part that concerns YOU directly so you needn't rediscover it from `lexer.rs`:

DEPENDENCY NOTE — the lexer is PULL-DRIVEN. Drive it as:
- `next()` for normal tokens.
- `next_template_part()` after a `${…}` closing `}` (re-enters template scanning; yields
  head/middle/tail, raw vs cooked).
- `read_regexp(divide_assign)` in expression position after a Divide/DivideAssign, to
  disambiguate regexp-vs-divide (mirrors `fxGetNextRegExp`; body+flags only — regexp VALIDATION
  stays in `endor-regexp`, not the lexer).
- Mode flags are settable mid-parse to mirror XS's parser state:
  `set_strict` / `set_async` / `set_generator` / `set_host`
  (⇔ mxStrictFlag / mxAsyncFlag / mxGeneratorFlag / mxCFlag).
- `tokenize()` collects the whole stream — tests/tooling ONLY, not the parse path.

Relevant folds the lexer named (so your AST/error layer expects them):
- BigInt: scanner stores `BigIntLiteral{ digits, radix }` (cleaned significant digits), NOT a
  materialized magnitude — value materialization is deferred to your/coder stage.
- Strings/templates are Rust `String`s (UTF-8 scalar values), not CESU-8 byte runs; byte-identity
  reconciliation (CESU-8, atom/constant ordering) belongs to the coder children, not here.
- `//@line N` IS honored (resets line counter → affects downstream token positions);
  sourceURL/sourceMappingURL payloads are dropped.
- `LexErrorKind::Overflow` exists for fidelity but is never raised (endor buffers grow).

Token model: `token.rs` mirrors `txToken` EXACTLY (172 variants, XS ordinals as discriminants) —
match your node/early-error classification against those.
