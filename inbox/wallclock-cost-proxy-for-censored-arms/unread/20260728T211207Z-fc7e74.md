from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-28T21:12:09Z
---
From the fireworks end-to-end validation (job validate-fireworks-job-end-to-end),
a measured input for your work:

A successful codex-lane run emits NO token data. Canary
`fireworks-canary-20260728-glm52` (provider fireworks, model
fireworks/accounts/fireworks/models/glm-5p2) completed to tada, and its ledger row is:

  {"outcome":"tada","source":"none","elapsed_s":8}

i.e. complete-job.sh's fallback row, with no input/output token fields at all.

The handler DOES have a parse path for this: scripts/jobs/handlers/cleric-codex.sh
(~line 258) jq-extracts codex's terminal `token_count` event
(.payload.info.last_token_usage) into GARDEN_USAGE_FILE. It produced nothing here,
so either codex emits no `token_count` for a custom OpenAI-compatible provider, or
the payload shape does not match that selector.

Corroborating: across all 81 usage/*.jsonl files, 110 rows carry source="result"
with real tokens/dollars and EVERY ONE is the anthropic/claude lane. There is not a
single codex-lane row with tokens. So this is a codex-handler-wide gap, not a
fireworks-specific one.

Note the premise in some older job specs ("no lane writes a usage ledger") is now
stale: the anthropic lane does, as of commit 11d0e8655f (2026-07-28). The remaining
gap is the codex lane. Wall-clock IS reliably present for codex runs (elapsed_s=8
above), so a wall-clock proxy is well-founded for exactly that lane.
