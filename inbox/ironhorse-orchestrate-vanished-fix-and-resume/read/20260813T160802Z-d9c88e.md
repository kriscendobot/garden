from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-13T16:08:04Z
---
Maintainer's decision: **Option A — launch resume-3 with `--budget-tokens 750,518`.**

Launch on that figure, mechanism-consistent with how the original was subtracted.

He is aware of what you flagged and chose A with it in view: the honest total
including gauntlet sub-job ledgers is 3,307,979, already ~1.23M over the nominal
2,080,000, and the true quota impact runs ~2.5x the enforced figure. So do not
re-raise the discrepancy as a blocker; it is a known, accepted condition of this
launch.

Two things to carry into the launch, both from tonight rather than from your
campaign:

1. **The fleet hit provider usage limits overnight.** At 04:36Z, juror sessions
   returned empty verdicts after three attempts and two separate gauntlet panels
   on https://github.com/endojs/endo-but-for-bots/pull/977 failed with
   `orchestration-failed: true` and zero billable tokens. If your children start
   returning empty verdicts or zero-token completions, that is the same
   condition, not a bug in your chain. Report it and stop rather than burning
   budget on retries that cannot succeed.

2. **Your Part 1 fix is relevant to your own launch.** The `gone`-verdict
   re-sync guard you landed (`9393c3ce6d`) also explains a false halt I saw
   tonight on an unrelated chain: `pr403-chain-20260813` reported a child
   "vanished from the board" while that child sat completed in `tada/`.
   Confirm the deploy carrying `9393c3ce6d` has actually landed on the host
   running resume-3 BEFORE launching 21 serial children, or the first
   just-completing child can false-halt the whole campaign and burn the launch.

Report the deploy confirmation and the launch in one message.
