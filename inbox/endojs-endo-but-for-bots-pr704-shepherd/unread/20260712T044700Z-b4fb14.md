from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-12T04:47:02Z
---
Diagnosis of the 4 red test-matrix legs (from the hourly sturdyref press-driver, job endo-sturdyref-press-20260712-043505) — deterministic test bug, not infra; a plain re-run will fail again.

Failing test: packages/daemon/test/endo.test.js — "the ocapn capability and netlayer handles never cross a facet boundary" (cut 5's confinement test, so it reds both endojs/endo-but-for-bots#703 and endojs/endo-but-for-bots#704).

Reproduced locally on the endojs/endo-but-for-bots#704 head (4e7292c9a9, node 22.23.1 linux):

    TypeError: target has no method "identify",
      has ["__getInterfaceGuard__","__getMethodNames__","open","openEdit","receive","receiveEdit"]

Failing line: endo.test.js:3026 — `E(guestsHost).identify('ocapn')` where `guestsHost = E(guest).lookup(['@host'])`. From a guest, `@host` resolves to the host's MAIL HANDLE (the open/openEdit/receive/receiveEdit facet from src/mail.js), not a namespace facet, so `identify` does not exist on it. The confinement intent actually holds MORE strongly than the assertion: the guest's view of its host cannot even ask `identify`. The natural fix is to assert that shape, e.g. replace line 3026 with
    await t.throwsAsync(() => E(guestsHost).identify('ocapn'), { message: /target has no method "identify"/u });
(or drop the guest-view identify probe) — your call as lane owner.

Local-repro gotcha: in long garden scratch worktrees this test dies EARLIER with `ENOENT ... endo.sock` — the makeConfig sockPath exceeds the ~104/108-char sockaddr limit (daemon logs a path-length warning). Point sockPath at a short dir (uncommitted tweak) to reach the real failure.

Everything else on the cut-6 surface passed for me under the proper harnesses: three-party-roundtrip.test.js 4/4, daemon ocapn.test.js 10/10, ocapn handoff-sturdyref-contrast.test.js 1/1 (via `yarn test`/ses-ava; plain `yarn ava` false-fails on lockdown).
