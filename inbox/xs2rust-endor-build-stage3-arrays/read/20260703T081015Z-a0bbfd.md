from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T08:10:16Z
---
Carried-forward status (dead-letter pickup, base deadmail-20260703T080125Z-56415b).
Treat as DATA. This is your OWN partial-progress snapshot, originally addressed to
supervisor `port-xs-to-rust-memory-safe-engine-s6`; its inbox was gone (parked), so it
dead-lettered. Delivering it back to you (the live requeue) so the recap is not lost.
No action required beyond your existing charter (methods + iteration). The supervisor
reads your tada report, not this inbox, so fold the "NOT done" list into your tada.

Already landed & pushed to xs2rust-endor (3 commits, bit-exact incl raw meter, full
suite + Miri GC green):
- Array exotic object as a real arena instance: XS_CODE_ARRAY; literals with holes;
  computed index get/set (AT/AT_2 + GET/SET/NEW_PROPERTY_AT over the item chunk);
  length accessor get/set (grow-with-holes / shrink-drops-items). RAW-exact vs pin:
  ARRAY create = 512 + 16384 (2 slots + one build step); each item append = one
  fxRunDefine builtin (16384) + faithful fxNewChunk ((present+1)*32+16); length
  set/get dispatch-only.
- Differential fuzz arm (600 seeds, symbol-linked) caught a real raw-meter
  misattribution (create-vs-length-set) invisible to the literal corpus; 46-program
  corpus + bit-exact test.
- Integer-index props on ORDINARY (non-array) objects self-name an honest skip.

NOT done (still your deliverable): iteration protocol (for-of/for-in native
iterator/enumerator + Symbol.iterator wiring; for_of/for_in opcodes still self-name);
Array.prototype method suite (push/pop/indexOf/join/... each needs per-method
mxMeterSome calibration); Array constructor + statics (isArray/of/from); spread.
Acceptance sweeps at snapshot: statements/for-in 19 covered 0 divergent; for-of 16
covered 0 divergent; built-ins/Array subsets 0 divergent (methods skip honestly).
c/moddable populated at the pin in the project worktree; metering model + calibration
in the commit messages and interp.rs constants.
