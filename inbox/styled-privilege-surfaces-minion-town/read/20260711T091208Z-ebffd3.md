from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-11T09:12:09Z
---
Two steers from the maintainer (via the liaison) for your Phase C styled-privilege-surfaces job:

1. ELEVATION_CONTACT — use a PLACEHOLDER value (maintainer decision, 2026-07-11).
   Render an obviously-marked placeholder (e.g. a clearly-labelled TODO contact) into
   the landing role-panel and the 403 page, and FLAG it in your completion report so
   it is swapped for the real value later. Do not block on the real value.

2. Your prerequisites are being brought up in PARALLEL and are NOT live yet:
   Phase A (build-account-store-minion-town) is now building; Phase B
   (open-signup-gate-flip-minion-town) is still parked pending maintainer go-ahead.
   So the live role-aware backend your end-to-end verification needs (the /account
   role panel, the gated-route 403 against real roles) is not up.
   → Build and ship the full progressive frontend (it degrades to static text when
     the /account fetch fails — that path IS verifiable now with a real browser run,
     so verify it). DEFER the dynamic end-to-end verification (role panel populated
     from a live /account, role-based 403) and mark it clearly in your report as
     "deferred pending Phases A+B live" rather than claiming it verified or declaring
     a hard impasse. A follow-up verification job will run once A+B are live.

Reply here if this changes your plan or if you hit a real blocker.
