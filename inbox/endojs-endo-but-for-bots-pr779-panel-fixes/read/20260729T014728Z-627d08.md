from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-29T01:47:38Z
---
# 18-seat code panel aggregate — endojs/endo-but-for-bots PR 779

Head reviewed: b08607b85de414f1e266aa2930718b7f3fbc5c53
Frozen base: 46d4edf31714c1488ec1d95492cc1ae9643c1f9f
Disposition: must-fix. Seats: archivist benchmarker curator fast-checker gateway integrator locksmith migrator packager pruner releaser scribe stylist surfacer transplanter warden wire-watcher coverage-auditor

This is the second half of the 28-seat code panel; the first 10 seats (prover, saboteur, breaker, spec-keeper, engine-realist, corner-prober, purist, changeset-auditor, assessor, typist) ran 2026-07-28 against head 55330da2.

### archivist
### archivist

**Verdict:** request-changes

**Findings:**

- `packages/ses/src/module-instance.js:421-422` — the new comment says an upstream `var` binding "clears its TDZ as part of its hoisting preamble (see `transform-analyze.js`)". `packages/module-source/src/transform-analyze.js` contains no hoisting logic at all (`grep -in hoist` → no hits); `hoistedDecls` is populated in `packages/module-source/src/babel-plugin.js:217,222` and emitted into the preamble in `packages/module-source/src/functor.js:56-66` — the very file this PR reorders to make that claim true. A reader following the reference finds nothing. Fix: point at `packages/module-source/src/functor.js` (fully qualified, since the reference crosses from `packages/ses/` into another package). **must-fix** [rule: roles/jurors/archivist/AGENT.md § cross-document and intra-document section references resolve] [rule: skills/relative-paths/SKILL.md]

- `packages/ses/test/import-gauntlet.test.js:2-3` — the file header still reads "These tests exercise all forms of import and export between **a pair of** modules using a single Compartment." The 10 tests added here are three-module cycles (`main.js` + `star-reexporter.js` + `export-renamer.js`, e.g. `:291-307`). The pre-existing comment no longer describes the code it sits next to. Fix: one word — "between two or more modules". **should-fix** [rule: roles/jurors/archivist/AGENT.md § Primary surface — comment accuracy]

- `.changeset/fix-ses-star-export-cycle-rename.md:1-3` — the changeset declares only `'ses': patch`, but the PR also changes published package `@endo/module-source` (v1.4.1, not private): `src/functor.js` now emits hoisted declarations *before* `$h_imports(...)`, an observable change to generated functor source that required updating `test/fixtures/format-preserved.txt`. No `@endo/module-source` entry means the ordering change never reaches that package's CHANGELOG and ships unversioned. Fix: add `'@endo/module-source': patch` and one sentence of prose describing the preamble reordering. **should-fix** [rule: skills/changeset-discipline/SKILL.md]

**Notes (out of scope but worth flagging):**

- `packages/ses/src/module-instance.js:379-384,398` — `wireUpExportNotifier` gained two parameters (`deferredSpecifier`, `deferredImportName`) with no JSDoc; and `{@link makeNotifierWithResolver}` sits inside a `//` line comment, where no tooling renders it as a link (only `/** */` blocks are processed). Both are inert-doc nits, not inaccuracies. [proposed-rule: `{@link}` / `@see` tags belong in block doc-comments; in `//` comments write the plain identifier or path.]

- Docs verified accurate: `notifier-with-resolver.js` JSDoc matches the implementation's queue-then-forward and one-shot `resolve` semantics; `cycle-rename-tdz-matrix.test.js:1-42` correctly counts "six star-reexport cells" + one named-reexport, all 7 `fixture:` directories exist, and its cross-reference to the "seven matrix cells" in `import-gauntlet.test.js` resolves. No banner horizontal rules in the diff. [rule: skills/no-comment-banners/SKILL.md]

Self-improvement: the strongest archivist signal in this diff came from *verifying* every "see X" target by grepping for the named content rather than merely confirming the file exists — `transform-analyze.js` exists, which is exactly why the stale reference reads as plausible. Worth encoding as a sharpening of the cross-reference rule: resolve the reference to the *content* it promises, not just to a path.

### benchmarker
I have what I need: the diff, both commit messages, the PR body, and the (empty) review-thread surface.

## Per-juror block — `benchmarker`, PR endojs/endo-but-for-bots#779, round 1

**Verdict: comment-only**

### Optimization-claim walk

Surfaces walked: PR body, both commit messages (`55330da29`, `b08607b85`), the diff's prose comments, and the review-thread surface. The thread surface is **empty** — `pulls/779/comments` and `pulls/779/reviews` both return zero. The only conversation is issue comment https://github.com/endojs/endo-but-for-bots/pull/779#issuecomment-5104064514, a completion report carrying test verification and an explicit `Declined: none.`

- The naugtur feedback quoted in the PR body (deferring-notify call ordering) is a **correctness** thread, not an optimization one, and closes with an explicit disposition plus a pinning test. Accepted.
- Grepping the whole diff for performance vocabulary (`perf|optimi[sz]|benchmark|overhead|cost|hot path|allocat`) yields one incidental match on the word "performed". **No optimization is claimed anywhere in this PR.**

So my core charter is clean: there are no open optimization claims to close. The finding below is the inverse case, which my brief does not yet cover, so it is proposed rather than cited.

### Findings

**1. Unconditional cost added to the linker's per-module path, unmeasured and unnoted.** `packages/ses/src/module-instance.js:531`

`b08607b85` changes the namespace-sealing pass from one `defineProperty` per export to `reflectDeleteProperty` + `defineProperty`, and adds an eager `defineProperty` at each of the three `exportsProps` writers (lines 267, 365, 446). Every key in `exportsProps` is written by one of those three, so **every export of every module** now costs define → delete → redefine instead of one define. `delete` on an ordinary object is the canonical trigger for a dictionary-mode (slow-properties) transition in V8 and JSC, and `exportsTarget` is the object every namespace-mediated read funnels through for program lifetime (`packages/ses/src/module-proxy.js:78`). The need is narrow — only cyclic star-export-with-rename graphs require the eager define — but the cost is paid by all module graphs. Neither commit message, the changeset, nor the PR body says anything either way.

Note that `packages/ses/package.json` has no bench script and SES has no module-instantiation harness (`packages/chacha12/BENCH.md` is the repo's only benchmark report), so measuring is disproportionate here. The cheap closure is the **note**, not the measurement.

Disposition: `summary-fix` — one line in the PR body or the commit message, either a measurement or an explicit cost-accepted rationale.

`[proposed-rule: a change that adds unconditional per-call or per-instantiation work to a hot path closes the same way an optimization claim does — a posted measurement per skills/benchmark-comparative-report/SKILL.md, or an explicit "cost accepted because ..." line. Today roles/jurors/benchmarker/AGENT.md § Operating norms scopes the seat to optimizations proposed; a silent pessimization currently reaches no seat at all.]`

**Self-improvement:** the seat's primary surface should be stated as "performance claims and performance changes", not "optimization claims" — this PR's only performance-relevant content was invisible to the charter as written, and I reached it only by reading the diff rather than the discussion.

### curator
### curator

**Verdict:** request-changes

**Findings:**

- **must-fix** — `@endo/module-source` has a source change with **no changeset entry**. `packages/module-source/src/functor.js:56-76` reorders the emitted preamble so `$h͏_imports(...)` follows the hoisted declarations instead of preceding them. This is an observable change to the package's *principal output* — the generated functor source text — confirmed by the committed fixture delta at `packages/module-source/test/fixtures/format-preserved.txt:1`. That text is reachable from two public entry points: `analyzeModule` (`packages/module-source/analyzer.js:4` → `buildRecord` → `buildFunctorSource`) and `ModuleSource` (`packages/module-source/index.js:4`). The package is publishable (`@endo/module-source@1.4.1`, not private; `.changeset/config.json` has `ignore: []`), yet the PR's sole changeset bumps only `'ses': patch` (`.changeset/fix-ses-star-export-cycle-rename.md:2`). Add `'@endo/module-source': patch`. [rule: skills/changeset-discipline/SKILL.md § When to write one — "a change in observable behavior of an existing exported API"]

- **must-fix** — the ses changeset asserts behavior that ses alone cannot deliver. `.changeset/fix-ses-star-export-cycle-rename.md:9` states `var` bindings read `undefined` "because the hoisting preamble pre-initializes them before any downstream observation". That preamble ordering *is* the `functor.js` reorder, which ships in a separate, independently-versioned package: `ses`'s runtime dependencies are only `@endo/cache-map`, `@endo/env-options`, `@endo/immutable-arraybuffer` — no `@endo/module-source` dep or peer-dep constrains the pairing. A consumer taking the `ses` patch alone gets the `module-instance.js` half and not the hoisting half, so the published note over-promises. Adding the module-source entry (above) fixes the release, but the sentence should also be qualified to name the paired package. [rule: skills/changeset-discipline/SKILL.md § Keep the changeset current]

- **comment-only** — ses public-surface inventory verified **clean**; `patch` is correct on the exported-identifier axis. `makeNotifierWithResolver` (`packages/ses/src/notifier-with-resolver.js:32`) and `reflectDeleteProperty` (`packages/ses/src/commons.js:138`) are both internal-only and both consumed (`module-instance.js:27`, `:531`). Although `files` ships `src`, ses's `exports` map declares no `./src/*` subpath, so neither becomes importable. No added, removed, renamed, or retyped public export.

**Notes (out of scope but worth flagging):**

- `.changeset/…:6` ("installs a deferred forwarding notifier that resolves through the upstream's notifier table on first subscription") narrates an internal helper a dependency updater does not act on. [rule: skills/changeset-discipline/SKILL.md § Omit implementation details]
- The TDZ tightening turns a previously-silent read into a `ReferenceError`; whether `patch` suffices on the compat axis is the migrator's call, not this seat's.

**Self-improvement:** the recurring curator finding here was not the classic bump-mismatched rename but a **missing changeset for the second package in a two-package fix** — caught only by noticing a committed output fixture (`format-preserved.txt`) alongside a source edit outside the changeset's named package. Worth adding to the curator brief: when a PR edits a code *generator*, the generated-output fixture is the public-surface diff, and every publishable package touched needs its own entry.

### fast-checker
## Review complete

I reviewed the diff at `46d4edf` → `b08607b8` (59 files; the load-bearing source is `packages/ses/src/module-instance.js`, the new `packages/ses/src/notifier-with-resolver.js`, and `packages/module-source/src/functor.js`).

Key context I verified before writing findings:
- `packages/ses/package.json` has **no** `fast-check` devDependency (only a `// TODO: Use fast-check to generate arbitrary input.` at `packages/ses/test/make-hardener.test.js:118`). `chacha12`, `marshal`, `pass-style`, `patterns` do have it.
- `notifier-with-resolver.js` has **zero** direct tests — grep finds only its own definition and one import site.
- `makeNodeImporter` (`packages/ses/test/_node.js`) takes an in-memory map of source strings, so module graphs in `import-gauntlet.test.js` are generatable without on-disk fixtures.
- Commit `b08607b8` fixes a key-order regression that commit `55330da2` introduced — direct shrinkage-value evidence.

---

### fast-checker

**Verdict:** request-changes

**Findings:**

- `packages/ses/src/notifier-with-resolver.js:32` — new module, **zero direct tests**; exercised only transitively through cycle fixtures. Its JSDoc states three universally-quantified claims: subscribers queued before `resolve` are replayed *in order*; subscribers after `resolve` forward through; `resolve` is *one-shot*. The one-shot claim is load-bearing — `module-instance.js:404` calls `resolveUpstream` on **every** `notify`. The honest test of a `forall interleavings` claim is a command-sequence property: `fc.assert(fc.property(fc.array(fc.constantFrom('notify','resolve')), ops => …))` against a model that records delivery order, asserting every `notify`'d updater reaches the first-resolved target exactly once, in call order. Interim (no dep): an enumerated interleaving table covering `notify*→resolve`, `resolve→notify*`, `notify→resolve→notify`, `resolve→resolve`. `[proposed-rule: a new src module carrying explicit forall claims in its JSDoc ships with a test that quantifies over them, not only transitive coverage]` — **must-fix-loop**

- `packages/ses/test/import-gauntlet.test.js:57` — `'module namespace exotic object enumerates exports in sorted order'` is a single 4-name spot check (`zeta, alpha, mu, beta`) for a contract that is universally quantified: *for all* export-name sets, `Reflect.ownKeys(ns) === sort(names) ++ [Symbol.toStringTag]`. It is the sole guard on the delete-and-redefine pass at `module-instance.js:530`. Proposed: `fc.assert(fc.property(fc.uniqueArray(fc.stringMatching(/^[A-Za-z_$][\w$]*$/), {minLength: 1, maxLength: 8}), names => …), {numRuns: 50})`, generating the module source from `names` through `makeNodeImporter`. **Shrinkage value is demonstrated by this PR's own history**: `55330da2` broke sorted order (eager `defineProperty` with `configurable: false`, plus a comment asserting the late pass was "a no-op"), and `b08607b8` fixed it. That property would have failed in `55330da2` and shrunk to `['b','a']`. `[proposed-rule: a contract the code sorts/normalizes over an unbounded key set is tested by a property over generated key sets, not one handpicked set]` — **summary-fix**

- `packages/compartment-mapper/test/cycle-rename-tdz-matrix.test.js:85` — `SCENARIOS` is a hand-enumerated cartesian product over three axes (binding × order × reexportForm) that is **incomplete**: 7 of 18 cells. `named` appears only as `const`/`renamer-first`; `named × {let, var}` and all three `named × star-first` cells are absent, and the header comment claims the matrix "completes" while the `named` row exists precisely to show "the gap is not specific to `export *`". Axes enumerated in code want `fc.constantFrom('const','let','var') × fc.constantFrom('star','named') × fc.constantFrom('renamer-first','star-first')` over a generated source map rather than 10 near-duplicate fixture directories. `[proposed-rule: a test table that declares itself a matrix over N enumerated axes covers the full cross-product, or names the excluded cells and why]` — **summary-fix**

- `packages/ses/test/import-gauntlet.test.js` (6 TDZ cells) / `cycle-rename-tdz-matrix.test.js:213` — the SES-vs-Node parity treatment is the **equivalent-implementations** property shape in its purest form: `forall graph, sesProbe(graph) === nodeProbe(graph)`, with Node as the reference oracle. It is currently pinned at 7 handpicked graphs. A generator of small cyclic module graphs (`fc.letrec` over module descriptors, emitted to a temp dir so Node's own importer can serve as oracle) would make this the strongest test in the package. Real work, not this round. `[proposed-rule: where a PR pins parity against a reference implementation, the parity claim is a property over generated inputs rather than an enumerated fixture set]` — **follow-up**

- `packages/ses/package.json` — `fast-check` is not a `ses` devDependency; `packages/ses/test/make-hardener.test.js:118` already carries a `TODO: Use fast-check to generate arbitrary input.`, so the want predates this PR. The two findings above that name `fc.*` are blocked on it. `[proposed-rule: introduce fast-check as a devDependency on packages that ship universally-quantified contracts]` — **follow-up**

**Notes (out of scope but worth flagging):**

- `module-instance.js:527` — the comment justifying `arraySort` ("the string must correspond to a valid identifier") is the assumption the sorted-order property's arbitrary should encode. It is narrower than ES2022, which permits arbitrary string module export names (`export { x as "0" }`); an integer-like name would sort lexicographically but enumerate first in own-key order, breaking the invariant. `exported.name` in `packages/module-source/src/babel-plugin.js:596` reads `.name` (not `.value`), so string-literal export names appear unsupported today — this is a pre-existing bound, not a defect in this PR, but it is the bound the arbitrary should be documented against. `[proposed-rule: an arbitrary's bounds document the assumption the implementation relies on, with the widening that would break it named]`
- Composition with corner-prober: if that seat enumerates the missing `named ×` cells, prefer the property as the canonical closure — the enumeration becomes the property's documentation.

**Self-improvement:** The strongest fast-checker evidence in a PR is often its own commit history. Diffing the head commit against its predecessor surfaced a regression the branch introduced and then fixed, which converted a speculative "this should be a property" into a demonstrated shrinkage-value argument with a named minimal counterexample. Check `git log` on the branch before writing the block, not just the squashed diff.

### gateway
Confirmed the gate surfaces directly against the diff. Here is my seat's block.

---

## Juror: gateway — PR endojs/endo-but-for-bots#779 (endojs/endo-but-for-bots), base `46d4edf3`

**Verdict: request-changes** (single `summary-fix`; no re-architecture)

### Findings

**1. Root release gate under-declares scope: `@endo/module-source` changes but is not in the changeset.** `[proposed-rule: a changeset must name every published package whose src/ changes in the PR; a src change with no declared bump is a release-gate defect, not a hygiene nit]`

`.changeset/fix-ses-star-export-cycle-rename.md:2` declares only `'ses': patch`. But `packages/module-source/src/functor.js:48-76` moves the emitted `$h͏_imports([...])` call *after* the hoisted-declaration preamble — a change to generated functor output, confirmed by the snapshot churn in `packages/module-source/test/fixtures/format-preserved.txt:1`. `@endo/module-source` is published (`packages/module-source/package.json`, v1.4.1, not private).

Why this bites at the root gate: `.changeset/config.json` sets `"fixed": []` and `"linked": []`, so packages version independently, and `"updateInternalDependencies": "patch"` only rewrites dependents' ranges — it does not mint a release for an undeclared package. So module-source's emit change gets no version bump and never publishes, while `ses` ships a patch whose fix depends on that emit order. `@endo/compartment-mapper` takes `@endo/module-source` at `"dependencies": "workspace:^"` (runtime, not dev), so the reorder is consumer-visible, not test-only.

*Disposition: `summary-fix`.* Add `'@endo/module-source': patch` to the changeset front-matter and one sentence naming the preamble reorder and its scope (all modules compiled by module-source).

**2. Changeset does not cover the second commit's user-visible behavior.** `[proposed-rule: same as above — the changeset body must describe each observable behavior change in the PR]`

Commit `b08607b85` "fix(ses): preserve module namespace key order" has an empty body, and the changeset text never mentions key order. Namespace key order is observable to consumers. Roll a clause into the same changeset edit — no separate fix.

*Disposition: `summary-fix`, fold into finding 1.*

### Clean bill (no action)

- **No repo-root config is touched.** No `tsconfig*.json`, `.eslintrc*`/`eslint.config.*`, root `package.json`, `yarn.lock`, `.github/workflows/*`, or `.config/*` in the diff. This positively confirms commit `55330da29`'s stated reconstruction goal — the botched refresh that had reverted `package.json`, `yarn.lock`, and the eslint-plugin layout against the frozen base is gone.
- The eleven `package.json` files under `packages/compartment-mapper/test/fixtures-cycle-*/node_modules/app/` are fixture-scoped, not workspace manifests. No root reach.
- New src file `packages/ses/src/notifier-with-resolver.js` needs no manifest edit: `packages/ses/package.json` `files` already includes `src`.

No relaxations of repo-wide lint or type strictness appear in this PR, so the seat's higher-weight lens does not engage.

*Self-improvement: the gateway's path list should name `.changeset/` explicitly alongside `.github/` and `.config/` — it is a repo-root gate whose failure mode (unreleased cross-package coupling) is exactly this seat's "ripple explained?" lens, and I nearly ceded it to `changeset-auditor` on path-matching alone.*

### integrator
Review complete. Here is my per-juror block:

### integrator

**Verdict:** request-changes

**Findings:**

- **Title leaks PR methodology and will read as the merge commit.** PR title `fix(ses): cyclic star export with renaming reexport (issue endojs/endo-but-for-bots#59) - retargeted to frozen base` — the trailing ` - retargeted to frozen base` names garden-internal PR mechanics (the frozen-base convention), not what landed. A future `git log` reader gets a merge subject about a retarget. Drop the suffix; the conventional-commit head is already correct. [rule: skills/pr-formation/SKILL.md § Title, § No methodology leak]

- **Description does not follow the upstream template and describes the retarget, not the change.** `.github/PULL_REQUEST_TEMPLATE.md` provides `## Description` plus six `### … Considerations` headings; the body uses none of them. Its `## Summary of changes` says only "Retargeted from fork master to frozen base master-46d4edf", and the rest is review-process narrative ("Review feedback from naugtur", "Disposition", raw test counts). Reset to the template: what the fix does, why (issue endojs/endo-but-for-bots#59), and — critically — the two behavior changes a reviewer must attend to that the body never mentions: the newly enforced cross-module TDZ `ReferenceError` for `const`/`let` reads through a namespace import, and the namespace key-order preservation of commit `b08607b8`. Both are stated well in `.changeset/fix-ses-star-export-cycle-rename.md`; that prose is the seed. [rule: skills/pr-formation/SKILL.md § Behavior and intent, not diff]

- **Changeset omits `@endo/module-source`.** `packages/module-source/src/functor.js:48-76` moves the `HIDDEN_IMPORTS` call after the hoisted-declaration preamble — a change to the emitted functor source of a published package (`@endo/module-source@1.4.1`), confirmed by the `packages/module-source/test/fixtures/format-preserved.txt` update. The changeset bumps `'ses': patch` only, so module-source ships the new emission unversioned. Add a `'@endo/module-source': patch` entry, and note the compatibility consequence for stored precompiled functors (archives/bundles emitted under the old ordering) in the template's `### Compatibility Considerations`. [rule: skills/changeset-discipline/SKILL.md]

- **The same seven-cell matrix is table-driven in one package and paste-and-edit in the other.** `packages/compartment-mapper/test/cycle-rename-tdz-matrix.test.js:84-180` expresses the matrix as a frozen `SCENARIOS` table and loops; `packages/ses/test/import-gauntlet.test.js:426-706` expresses the identical seven cells as ~280 lines of hand-copied `test(...)` blocks differing only in `const`/`let`/`var`, import order, and the expected probe. The next adopter (a `class` binding, the missing `named × let|var` cells, `star-first` for the named form) edits one row on the compartment-mapper side and pastes 38 lines on the ses side. Hoist the ses cells onto a scenario table in the same shape. [rule: skills/adversarial-tests/SKILL.md] [proposed-rule: when a PR expresses the same test matrix in two packages, both expressions use the same table-driven form so a new cell is one edit per package]

- **New `makeNotifierWithResolver` sits off the project's concept namespace for this shape.** `packages/ses/src/notifier-with-resolver.js:34` returns a `{ notify, resolve }` producer/consumer pair — the project already names exactly this shape a *kit* (`packages/promise-kit`, `makePromiseKit` → `{ promise, resolve, reject }`), and the JSDoc itself reaches for "synchronous variant of `Promise.withResolvers`". `makeNotifierKit` in `notifier-kit.js` integrates with the existing map; the current name coins a second vocabulary for a concept the repo already names. Overlaps stylist. [rule: skills/rename-discipline/SKILL.md]

**Notes (out of scope but worth flagging):**

- The module's documented one-shot `resolve` contract and its queue-replay ordering (`notifier-with-resolver.js:19-27`) are pinned only indirectly, through the cycle fixtures. A direct unit test would pin the contract the JSDoc asserts rather than leaving it to narrative. Overlaps coverage-auditor. [proposed-rule: a new standalone `src/` module whose JSDoc states a contract carries a unit test that asserts that contract directly]
- Commit grouping is sound: `55330da2` is the fix plus its matrix, `b08607b8` is the key-order follow-up with its own pinning test at `import-gauntlet.test.js:57`, which asserts `Reflect.ownKeys` directly rather than explaining sortedness in a comment. No redistribution needed. [rule: skills/panel-review/SKILL.md]
- Fixture directories follow the established `fixtures-<topic>` convention and the `_<name>-assertions.js` helper prefix matches `_subpath-patterns-assertions.js`; no convention parallel introduced. The `named` form covers only `const × renamer-first` while `star` covers all six, but the header comment at `cycle-rename-tdz-matrix.test.js:163-168` justifies the asymmetry (TDZ lives with the binding, not the reexport form). No diagrams introduced; no dependency-graph cycles touched.

Self-improvement: the `[rule: ...]` citation for the "same matrix, two forms" finding is the weakest link in this block — `skills/adversarial-tests/SKILL.md` covers test quality but not test-expression duplication across packages, which is why I paired it with a proposed rule. The integrator brief's *forward-compose probe* names the pattern precisely ("will the next adopter paste-and-edit or extend cleanly?") but has no skill file behind it; if the panel sees this shape again, the proposed rule is worth carving into a skill so the citation is a real path rather than a proposal.

### locksmith
Reviewed the diff in the project worktree and verified both capability-flow claims empirically by running probes against PR code and the base commit (probe files removed; tree clean).

---

### locksmith

**Verdict:** request-changes

**Findings:**

- `packages/ses/src/module-instance.js:388-407` — the deferred branch mints an export **unconditionally** whenever `notify === undefined`, so it cannot distinguish "upstream is mid-cycle" from "upstream will never provide this name". A module's namespace now advertises an export the upstream never grants. Verified: `export { nope as x } from './up.js'` (where `up.js` has no `nope`) **links successfully**, `Reflect.ownKeys(ns)` contains `"x"`, and `ns.x` throws `ReferenceError: binding "x" not yet initialized` forever. Base commit threw at link (`TypeError: notify is not a function`); Node.js/ECMA-262 throws `SyntaxError: ... does not provide an export named 'nope'`. The surrounding code already raises exactly that at `module-instance.js:481-484` — a deferred notifier still unresolved when `imports()` reaches its sort-and-freeze pass should raise it too, rather than freezing a phantom export into the namespace. **must-fix.** No fixture covers a genuinely-missing re-export anywhere in `packages/ses/test/` or `packages/compartment-mapper/test/`, including the new 465-line `import-gauntlet.test.js`. [rule: skills/adversarial-tests/SKILL.md]

- `packages/ses/src/module-instance.js:263-268, 361-366, 440-445` — the three eager `defineProperty(exportsTarget, …, {configurable: true})` widen the grant a cyclic star-importer holds. `notifiers['*']` (`module-instance.js:372-374`) hands the **raw, unfrozen `exportsTarget`**, not `exportsProxy`, so the importer bypasses every deny trap in `module-proxy.js:75-141`. Verified in a two-module cycle: the peer reads `Object.getOwnPropertyDescriptor(ans,'secret').configurable === true` and successfully redefines `ans.secret` to an attacker value. On base the property was simply absent, so the window was add-only; the PR makes declared exports **replaceable and deletable** in it. The late pass repairs only `keys(exportsProps)`, so the tamper is transient — but any module evaluating later in the cycle observes it. **should-fix:** at minimum a regression test pinning that a cyclic peer cannot durably alter a declared export, and a comment stating the window is intentionally repair-covered. [proposed-rule: a capability handed to a cyclic peer must be the attenuated proxy, or the raw grant must be documented at the handout site with the window's exact bounds.]

- `packages/ses/src/notifier-with-resolver.js:56` — `return { notify, resolve }` is unfrozen, unlike every sibling factory (`module-proxy.js:53`, `module-instance.js:576`, both `freeze({…})`). The record grants `resolve` — the authority to choose which upstream notifier *all* queued and future subscribers are forwarded to. Nothing over-grants today (both are destructured at the single call site, `module-instance.js:401`), but the file is written as a general-purpose exported utility, and an unfrozen record is not a reliable attenuator. `freeze({ notify, resolve })`. **should-fix.** [rule: skills/panel-review/SKILL.md]

**Notes (out of scope but worth flagging):**

- `module-instance.js:401-407` — the deferred closure now captures `importedInstances` (every imported instance, each carrying `notifiers` and `execute`) where the code it replaces captured one notifier function. It attenuates adequately: `deferredSpecifier`/`deferredImportName` come from the static `__reexportMap__`, only one notifier is read, and `notifiers` is never revealed outside the shim per `module-instance.js:472-476`. Flagging so a later edit does not return or expose the map. comment-only. [proposed-rule: a closure that replaces a captured single capability with its containing collection notes the widening at the capture site.]
- The `function` → arrow conversion of `imports`/`execute` is a mild capability *narrowing* (no `.prototype`, no `[[Construct]]`). Positive.
- The injected-property leak the second commit's key-order fix does not cover: a cyclic peer's `Object.defineProperty(ans, 'injected', …)` survives into the frozen namespace and sorts **first** (`["injected","other","secret"]`), so the spec'd sorted enumeration order is violated by an attacker-chosen name. Pre-existing (base leaks it too), but it sits squarely in the commit's stated remit. follow-up.

**Self-improvement:** the two load-bearing findings here were each cheap to *state* and cheap to *disprove* — a 20-line Compartment probe run against PR code and again against the base commit converted "this looks like a widened grant" into a measured delta. For a capability-flow seat on a linker change, running that A/B probe should be the default first move, not the confirmation step; reading the diff alone would have led me to report the raw-`exportsTarget` handout as new when it is pre-existing, and to miss the phantom-export mint entirely.

### migrator
### migrator

**Verdict:** request-changes

**Findings:**

- **Missing `@endo/module-source` changeset entry (must-fix).** `packages/module-source/src/functor.js:48-76` moves the hoisting preamble ahead of `$h͏_imports(...)`, changing the *emitted* functor text of a published package (`@endo/module-source@1.4.1`); `packages/module-source/test/fixtures/format-preserved.txt:1` is the proof the output bytes move, and with them the content hash of any `pre-mjs-json` archive built from identical sources. `.changeset/fix-ses-star-export-cycle-rename.md:1-3` declares only `'ses': patch`. `ripe-showers-remain.md` (an unrelated types `minor`) is accidental coverage, not a description — if it releases first, this reorder ships with no bump at all. Add `'@endo/module-source'` to this PR's single changeset and describe the reorder. [rule: skills/changeset-discipline/SKILL.md]

- **Undeclared ses ⇄ module-source version-skew break (must-fix).** The changeset's "`var` bindings continue to read `undefined`" guarantee holds *only* when the hoisting preamble precedes `imports()` — i.e. only with the module-source half of this PR. `packages/compartment-mapper/package.json` depends on both at `workspace:^` (published `^1.4.1` / `^2.2.0`), so new-ses + old-module-source is an installable combination. There, the upstream's `notify` queues the updater while the upstream is still in TDZ (`packages/ses/src/module-instance.js:231-242`), the re-export's `tdz` stays true, and the new getter (`module-instance.js:427-437`) throws `ReferenceError` where 2.2.0 returned `undefined`. Worse for durable artifacts: `parse-pre-mjs.js:27` replays `__syncModuleProgram__` — functor text frozen at archive-build time — so an existing archive breaks under the new ses and cannot be repaired by upgrading ses; it must be rebuilt. That is a migration step the release notes must state, plus a `@endo/module-source` floor. [rule: skills/changeset-discipline/SKILL.md]

- **`patch` understates the ses bump (should-fix).** Namespace reads during a cycle went from silently yielding `undefined` to raising `ReferenceError` — stricter validation of a previously-admitted read path, which the discipline lists as a breaking shape. ses is `2.2.0`; `minor` is the honest floor even granting the spec-conformance rationale. [rule: skills/changeset-discipline/SKILL.md]

**Notes (out of scope but worth flagging):**

- No manual cascade entry is needed for `@endo/compartment-mapper`: `.changeset/config.json` sets `updateInternalDependencies: "patch"`. [rule: skills/changeset-discipline/SKILL.md]
- The eager `configurable: true` descriptors (`module-instance.js:268`, `:365`, `:447`) are observable by cycle peers, since `notifyStar` (`:374-376`) hands out `exportsTarget` raw; ESM namespace properties are spec'd non-configurable, so a structural check during the cycle window now sees a non-conforming descriptor. [proposed-rule: a transient descriptor state visible to third-party code must match the spec'd shape, not merely converge to it before freeze.]
- Changeset cites `transform-analyze.js` for the hoisting preamble; the change is in `functor.js`. [rule: skills/changeset-discipline/SKILL.md]

### packager
### packager

**Verdict:** request-changes

**Findings:**

- **`@endo/module-source` is missing from the changeset front matter (must-fix).** `.changeset/fix-ses-star-export-cycle-rename.md` declares only `'ses': patch`, but commit `55330da29` also changes `packages/module-source/src/functor.js:45-76`, moving the `hoistedDecls` preamble ahead of the `$h͏_imports([...])` call. That is a change to the package's generated functor output, confirmed user-observable by the golden fixture it forced (`packages/module-source/test/fixtures/format-preserved.txt:1`), and it is load-bearing for the changeset's own final claim that `var` bindings read `undefined` rather than throwing. `@endo/module-source` (1.4.1) publishes independently and needs its own front-matter key plus a sentence naming the preamble reordering. `@endo/compartment-mapper` is correctly absent: it is tests and fixtures only. [rule: skills/changeset-discipline/SKILL.md]

- **`patch` is the wrong bump for the TDZ tightening (should-fix).** `ses` is at 2.2.0, real semver. The changeset's own second paragraph says reads that previously returned the uninitialized slot value now raise `ReferenceError`. Code that worked on 2.2.0 throws on a `^2.2.0` patch pickup. House precedent for conformance fixes is patch (`.changeset/lemon-states-do.md`), so this is arguable rather than clear-cut, but it should be `minor` or the entry should state why patch is right. [rule: skills/changeset-discipline/SKILL.md]

- **Changeset prose carries implementation detail (should-fix).** "installs a deferred forwarding notifier that resolves through the upstream's notifier table on first subscription" names the internal mechanism (`packages/ses/src/notifier-with-resolver.js`), and "(latterly `TypeError: notify is not a function`)" narrates the PR's own debugging history. A dependency updater needs the symptom and the new behavior, not the wire-up. [rule: skills/changeset-discipline/SKILL.md]

- **Commit `b08607b85` repairs a regression introduced by `55330da29` and does not say so (should-fix).** Its message body is empty and its subject reads as an independent fix. It is not: `55330da29` added the eager `defineProperty(exportsTarget, ...)` calls at `module-instance.js:261,359,440` whose comments falsely claimed the late sorted pass "redefines with the identical descriptor as a no-op"; base `46d4edf3` had no such eager defines, so insertion order was already sorted. Net key-order behavior versus base is unchanged, which is why the changeset rightly omits it. Squash it into `55330da29`, or state in the body that it repairs that commit. [proposed-rule: an in-PR commit that repairs a regression introduced by an earlier commit of the same PR is squashed into it, or its message body names the sibling commit it repairs.]

**Notes (out of scope but worth flagging):**

- Diff hygiene is otherwise clean: no `yarn.lock` churn (notable given the reconstruction described in `55330da29`), no lint sweep conflated with substance, and the new `test/fixtures-cycle-*/node_modules/app/` dirs match the existing compartment-mapper fixture convention. [rule: skills/yarn-lock-separate-commit/SKILL.md]
- `b08607b85` makes the eager descriptor `configurable: true` before the late pass deletes and re-defines it as non-configurable. A namespace observed mid-cycle can therefore see a configurable property that later becomes non-configurable. Curator/breaker surface, not packager's. [proposed-rule: none]

Self-improvement: the recurring packager miss on this diff was a src change in a second published package (`@endo/module-source`) hidden behind a 52-file test-fixture bulk. Grouping `git diff --name-only` by package before reading anything else surfaced it immediately, and is worth doing first on any PR whose diffstat is dominated by fixtures.

### pruner
Container guard clean (exit 0); reviewed read-only in the dispatch worktree at `b08607b85`.

## pruner — PR endojs/endo-but-for-bots#779 (endojs/endo-but-for-bots)

**Verdict: request-changes** (disposition: `summary-fix` — every finding is a named deletion, none touch behavior)

Scope note: the only markdown added is `.changeset/fix-ses-star-export-cycle-rename.md` (~230 words), which sits inside the repo's own range (`drop-the-pseudo-prototype.md` is 343). **Not a finding — do not cut it.** The padding surface here is comment prose: **517 of 1551 added lines (33%) are comment lines.**

**Findings**

1. **The TDZ-axes explanation is written four times.** `cycle-rename-tdz-matrix.test.js:13-27` (header), `:60-69` (typedef fields), `:85-90,124-129,163-168` (inline `SCENARIOS` comments), and again as a ~30-line block in `packages/ses/test/import-gauntlet.test.js`. Keep the inline `SCENARIOS` comments — they sit against the rows and carry the expected-value reasoning. Delete the header axes enumeration (`:13-27`), reduce the typedef fields to one clause each, and cut the import-gauntlet block to a pointer at the matrix module. [proposed-rule: when one explanation is restated at module, type, and site level in a single module, keep the site-level copy and delete the rest]

2. **`import-gauntlet.test.js` breaks its own file's norm by 10x.** Base: 11 comment lines / 442 (2%). Added: 94 / 466 (20%). The file's established idiom is a 2-line header and fixture source that speaks for itself. [proposed-rule: an addition to an existing file matches that file's comment density unless it argues why not]

3. **Three assertion helpers run 3x the in-repo shape.** Pre-existing `_subpath-patterns-assertions.js` sets the house header at **9 lines** (purpose, parity rationale, `@module`). The new `_cycle-rename-assertions.js`, `_cycle-rename-unused-assertions.js`, `_cycle-cjs-reexporter-assertions.js` run **28-32**. Cut two things from each: the verbatim transcription of the fixture source (the fixture is adjacent and authoritative; a transcription drifts silently), and the bug post-mortem paragraph ("Before the fix, the SES linker visited…"), which already lives in the changeset. [proposed-rule: comments do not transcribe adjacent source]

4. **`module-instance.js`: the eager-define comment is triplicated** at `:262`, `:360`, `:442` — five lines each, with wording drift between copies. The code is triplicated too; extract one `defineEagerly` helper carrying the comment once.

5. **`notifier-with-resolver.js`: the `@module` block (`:1-6`) restates the function JSDoc (`:10-29`)** in a single-export file — ~30 comment lines for a 24-line function. Delete the `@module` block; drop the parenthetical "(This matches `Promise.withResolvers` semantics: a promise settles once.)", the third statement of that analogy in one file. A fourth sits in `module-instance.js:~385`.

6. **Review-conversation leakage:** `(the maintainer's "in all cases that it is possible" caveat)` in `import-gauntlet.test.js` quotes a review remark into permanent source. Cut the parenthetical; the sentence stands. Same lens, minor: `(latterly TypeError: notify is not a function)` in the changeset is debugging history in a user-facing release note.

**Self-improvement:** my brief tells this seat to cite `skills/em-dash-style/SKILL.md` § General prose discipline — **that section does not exist** (the skill has: The rule, Scope, How to write the change, How to sweep, Pitfalls, Notes from the field), and the target repo's `CLAUDE.md` is a bare `@AGENTS.md` include with no terseness rule. The seat has no citable standing rule, so it emits `[proposed-rule]` for nearly everything. The stronger anchor I used instead, and would encode: **calibrate against in-repo precedent** — the pre-existing sibling file's header length and the edited file's own base comment density turn "this feels padded" into a measured departure the fixer can act on. Recommend `roles/jurors/pruner/AGENT.md` drop the stale § citation and name the precedent-calibration technique.

### releaser
Review complete. Here is my per-juror block.

---

## Juror: releaser — PR endojs/endo-but-for-bots#779 (`endojs/endo-but-for-bots`)

**Verdict: request-changes**

### Findings

**1. Changeset omits `@endo/module-source`, which has a user-facing source change — absent-but-required.** `[rule: skills/changeset-discipline/SKILL.md § When to write one]` `[rule: CONTRIBUTING.md § Do I Need a Changeset?]`

The front-matter is `'ses': patch` alone, but the diff changes `packages/module-source/src/functor.js:45-76` — reordering the hoisted-declaration preamble to precede the `$h͏_imports([...])` call. `@endo/module-source` is public and unbumped at `1.4.1`; changesets publishes only packages named in an entry, so this half of the fix never ships.

That is not a bookkeeping nit, because the two halves are coupled. `packages/ses/src/module-instance.js:421-423` states that an upstream `var` "clears its TDZ as part of its hoisting preamble," and the changeset promises the user that "`var` bindings continue to read `undefined` because the hoisting preamble pre-initializes them before any downstream observation." That promise holds only against the reordered functor. A consumer who upgrades `ses` while resolving `@endo/module-source@1.4.1` gets the new TDZ-throwing getters without the pre-initialization that exempts `var` — the published release note is then false for them.

Recommended action: add `'@endo/module-source': patch` to the front-matter, with a sentence in the user's voice — e.g. "Precompiled module source now runs hoisted `function` and `var` declarations before resolving imports, so a module evaluated inside a cycle observes this module's hoisted bindings as initialized." Disposition **must-fix-loop**.

**2. Changeset body narrates the implementer's path and internals.** `[rule: skills/changeset-discipline/SKILL.md § What goes inside — "Omit implementation details", "No process commentary"]`

Two spans fail the release-note lens: "(latterly `TypeError: notify is not a function`)" records the succession of errors the implementer hit, which the upgrading reader cannot use; and "installs a deferred forwarding notifier that resolves through the upstream's notifier table on first subscription, so cyclic star-export fixed-points converge" is wholly internal. Cut both; the surrounding symptom-and-outcome prose already carries the user-visible fact. The TDZ paragraph is well aimed and should stay. Disposition **summary-fix**.

**3. `patch` may understate the TDZ change.** `[proposed-rule: a changeset whose own body describes previously-working reads that now throw warrants at least a minor bump for that package]`

The third paragraph describes existing code paths that returned a value and now raise `ReferenceError`. That is a behavior change on existing inputs, not purely additive; the sibling `drop-the-pseudo-prototype.md` takes `'ses': minor` for a comparable shift. Conformance-fix-as-patch is defensible and this is a maintainer call, so I raise it as a question, not a defect. Disposition **comment**.

### Notes

- No changeset is owed for `packages/compartment-mapper` (tests and fixtures only) or for commit `b08607b8` "preserve module namespace key order" — that commit repairs enumeration order disturbed by the eager `defineProperty` introduced earlier in this same PR, so net of the base there is no order change for users to read about. Correctly omitted.

### scribe
Journal `result` entry posted (`entries/2026/07/29/013831Z-result-gardener-2c021b.md`). Here is my per-juror block.

---

## scribe

**Verdict: request-changes**

Surface: endojs/endo-but-for-bots#779 carries zero inline comments and zero formal reviews of its own, so the review history is endojs/endo-but-for-bots#379's (superseded, back-linked from the body). Two note-this asks there are **closed** and verify clean:

- `#discussion_r3338685696` ("Please inform the gardener that it should document the concept of a parity test for future reference") → `skills/node-parity-test/SKILL.md`, created 2026-06-02, the day of the ask. Closure as standing-orders edit.
- `#discussion_r3393767528` ("Please delete, but capture a copy in your journal") → `journal2:projects/endo/drafts/construction-time-notifiers.md` plus a README index row citing review `4474269526`; the design doc is absent from this diff, and the public pointer resolves on both journal branches. Closure as journal-side record.

Completion-summary surface is also clean: `b08607b85` drew top-level comment `#issuecomment-5104064514` with head SHA, changes, "Declined: none", and verification.

### Findings

**1. The retarget silently dropped two closed reviewer asks.** `packages/ses/test/import-cjs.test.js` is untouched by this diff, so its endojs/endo-but-for-bots#379-era edits are gone at HEAD: boneskull's nit 4 (`TODO(endojs/endo#3220)` at the local `CjsModuleSource` mock, reported landed at `f87d0eb05` in `#issuecomment-4705150423`), and the plain divergence statement `#discussion_r3338682426` asked for. The programmatic half of the latter survives in `cycle-esm-in-cjs.test.js`; the TODO does not, and `endojs/endo#3220` merged 2026-07-22 — *after* base `46d4edf` — so the mock at `import-cjs.test.js:29` is still live and the marker still apt. Neither the body nor endojs/endo-but-for-bots#379's supersession comment (`#issuecomment-4999480861`) discloses what was dropped. Disposition: `summary-fix` — add a carried-versus-dropped ledger to the PR body; leave `import-cjs.test.js` out of the diff. [rule: `skills/panel-review/SKILL.md` § Cite-or-propose]

**2. Bare issue citation survives at `packages/ses/test/import-gauntlet.test.js:288`** (`'cyclic star export with renaming reexport (issue endojs/endo-but-for-bots#59)'`), against `#discussion_r3407540460` ("Remove issue numbers") and the bot's own settled discipline in `#issuecomment-4705403884` (bare `#<n>` dropped, qualified `endojs/endo#59` preserved). The sweep was scoped to titles touched by the compartment-mapper consolidation; this SES-side title was not. The seven qualified references are correct and stay. Disposition: `summary-fix`, one-line edit.

**3.** [proposed-rule: a retarget onto a frozen base must publish a carried-versus-dropped ledger naming each predecessor-PR artifact that closed a reviewer ask and is not carried, with the reason — the gap finding 1 exercises.]

### stylist
## stylist

**Verdict:** request-changes

**Findings:**

- `packages/ses/src/module-instance.js:382-383` — the new parameters `deferredSpecifier` / `deferredImportName` contradict the vocabulary of the block they feed. Nothing about the specifier or the import name is deferred; the *resolution* is. The body consistently says "upstream" (`upstreamInstance:405`, `upstreamNotify:406`, `resolveUpstream:403`, and the comment at `:391` "The upstream module did not yet expose a notifier"). Rename to `upstreamSpecifier` / `upstreamImportName` so the two parameters read the same as the eight lines that consume them. should-fix. [rule: skills/panel-review/SKILL.md § Cite-or-propose discipline; stylist primary surface, name at odds with the surrounding convention]

- `packages/ses/src/module-instance.js:422` — the comment says a `var` binding "clears its TDZ as part of its hoisting preamble (see `transform-analyze.js`)". `transform-analyze.js` contains no hoisting or preamble logic (`grep -n "hoist\|preamble"` returns nothing). The preamble is emitted in `packages/module-source/src/functor.js`, which is exactly the file this PR reorders to make that claim true. Point the reference at `functor.js`. should-fix. [rule: roles/jurors/stylist/AGENT.md § Secondary surface (name and doc disagree)]

- `packages/compartment-mapper/test/cycle-rename-tdz-matrix.test.js:66` — `@property {'renamer-first'|'star-first'} order` names a form the axis does not own. The doc directly beneath it says "Which module main.js imports first", and the sibling `reexportForm` axis (`:60`) already carries `'star'|'named'`. A `named` cell with the reexporter imported first would have to be tagged `order: 'star-first'` with no star anywhere. Use `'renamer-first'|'reexporter-first'`. The matrix is explicitly built to grow, so the name will lie on the next cell added. should-fix. [rule: roles/jurors/stylist/AGENT.md § Operating norms, "no name that lies about what the value is"]

- `packages/compartment-mapper/test/fixtures-cycle-rename/node_modules/app/main.js:2-3` — `ns1` / `ns2` abbreviate the same values the very next lines spell out as `namespace1` / `namespace2` (`:6-7`), and `_cycle-rename-assertions.js:33-34` exports them spelled out. Spell the imports out for one spelling per value. should-fix, not must-fix: `ns_N` has precedent in `fixtures-cjs-compat/node_modules/default-difficulties/index.mjs`, so the aggregate may reasonably drop this. [rule: roles/jurors/stylist/AGENT.md § Abbreviated identifiers]

**Notes (out of scope but worth flagging):**

- Fixture directory names split their axes inconsistently: six star cells are `fixtures-cycle-rename-tdz-*` (form omitted, and "rename" does not mean "star") while the seventh is `fixtures-cycle-named-reexport-tdz-*`. `fixtures-cycle-star-reexport-tdz-*` would make the directory read the same three axes as the scenario record. comment-only; a seven-directory rename may cost more than it returns. [rule: skills/rename-discipline/SKILL.md]

- Deliberately **not** flagged: `tdz` (`:425`) matches fourteen prior uses in the same file (`:201`, `:282`) and the test262 corpus spells it the same way; surrounding-code convention governs over the never-abbreviate default here. `reflectDeleteProperty` (`commons.js:138`) correctly follows the existing `reflectGet` / `reflectHas` pattern. `makeNotifierWithResolver` and its filename agree, with no redundant-word concatenation. [rule: roles/jurors/stylist/AGENT.md § Abbreviated identifiers, established-convention carve-out]

Self-improvement: the `star-first` finding surfaced only after reading the enum's own docstring next to the sibling axis. Worth encoding as a stylist habit: when a diff introduces a string-union type, read every member against the property's doc line and against the other unions in the same typedef, since a member that hardcodes one sibling value is the shape a name-lies finding takes in table-driven tests.

### surfacer
## Per-juror block — seat: `surfacer` — PR endojs/endo-but-for-bots#779

**Verdict: request-changes**

### Findings

**F1 — The fix spans two published packages; only one is versioned or described. `[must-fix-loop]`**
`packages/module-source/src/functor.js` changes the *emitted functor format* (hoisted decls now precede `$h͏_imports(…)`; the `format-preserved.txt` golden confirms the observable change), but the sole changeset declares `'ses': patch`. `@endo/module-source` is a public, versioned package (1.4.1) with its own `exports` map. It will be republished only as a dependency bump (`updateInternalDependencies: patch`), so its changelog will read "updated dependencies" while its actual output contract changed.
Worse, the halves are coupled but the coupling is inexpressible in the dependency graph: `ses` has no `dependencies`/`peerDependencies` on `@endo/module-source` (it can't — module-source depends on `ses`). A consumer who bumps `ses` alone against a lockfile-pinned `@endo/module-source@1.4.1` gets the new TDZ-throwing re-export getters (`module-instance.js:437`) without the hoist-before-imports preamble, so a `var` re-export read during a cycle can throw `ReferenceError` where it previously yielded `undefined` — the exact case the changeset promises stays `undefined`. Add a `'@endo/module-source': patch` changeset naming the emission-order change, and state the pairing in both entries. [rule: `skills/changeset-discipline/SKILL.md`] [proposed-rule: when one fix spans two independently versioned packages, each package gets its own changeset entry and the entries name the required pairing.]

**F2 — The module namespace transiently presents `configurable: true` properties to cyclic peers. `[must-fix-loop]`**
The three new eager `defineProperty(exportsTarget, …, { configurable: true })` sites (`module-instance.js:267`, `:365`, `:446`) mutate the object that `notifyStar` hands out **raw**, not through `exportsProxy` (`:375`). During the cycle window a peer holding `import * as r` observes `Object.getOwnPropertyDescriptor(r, 'x').configurable === true` — and may `delete`/redefine it — where ECMA-262 mandates `configurable: false` for namespace properties and pre-PR the window exposed no property at all. Normalize the descriptor on the way out (route `'*'` through the proxy, or have the trap report `configurable: false`) rather than exposing the raw target mid-flight. [proposed-rule: the module exports namespace must never present a configurable own property to any observer, including during cycle evaluation.]

**F3 — Cross-package file reference unqualified. `[summary-fix]`**
`module-instance.js:433` says "see `transform-analyze.js`"; that file is `packages/module-source/src/transform-analyze.js`, not in `ses`. Qualify with the package name. [proposed-rule: a code comment naming a file in another package qualifies it with that package.]

### Coherence checks that pass
No new export subpath; `src/notifier-with-resolver.js` stays internal (`ses` `exports` has no `./src/*`), is entrained automatically by the graph-following `makeBundle`, and ships via `files: ["src"]`. `index.js` thunk, `types.d.ts`, and README are unchanged and still accurate. `src-xs/compartment.js` reuses `../src/compartment.js`, so the `xs` condition inherits the fix — no divergence.

Self-improvement: the sharpest surfacer signal here was not within one package but *between* two — a behavior change in package A's emitted output that package B's runtime now depends on, with no dependency edge able to express it. Worth adding to the seat's norms: when a diff touches two packages, check whether the release surface (changesets + dependency ranges) can express the coupling the code now requires.

### transplanter
Portability scan complete. Writing my per-juror block.

**Preflight:** container guard clean (exit 0). Reviewed the diff at `46d4edf..b08607b8` in `/home/kris/garden/scratch/project-wt-endojs-endo-but-for-bots-pr779-panel-remaining-seats-6c768159` (endojs/endo-but-for-bots#779).

---

### transplanter

**Verdict:** request-changes

**Findings:**

- `packages/compartment-mapper/test/cycle-esm-in-cjs.test.js:80` — the divergence assertion `t.regex(result.stderr, /ERR_REQUIRE_CYCLE_MODULE/)` hardcodes an error code that only exists on Node builds with unflagged `require(esm)` (≥22.12, ≥20.19). The repo's root `package.json` declares `"engines": {"node": ">=16"}`; on Node 16/18, and on 20.x/22.x below those patch floors, `bridge.cjs`'s `require('./peer.mjs')` throws `ERR_REQUIRE_ESM` instead, so the assertion fails on a contributor's checkout while CI stays green (`.github/workflows/ci.yml:98,192` pin the matrix to `22.x`/`24.x`, and `.node-version` is `lts/*`). I confirmed the code is produced on this host's Node v22.23.1; the failure mode is a version floor the test never states. **should-fix.** Portable form — gate on the runtime, following the in-repo precedent at `packages/init/test/async_hooks.test.js:24-30`: parse `process.versions.node`, and either `t.pass()`-skip below the `require(esm)` floor or widen the pattern to `/ERR_REQUIRE_CYCLE_MODULE|ERR_REQUIRE_ESM/` with a comment naming why both codes are acceptable. The companion `t.not(result.status, 0)` already holds on every version, so only the code match needs the gate. [proposed-rule: a parity test that asserts a Node.js error *code* must state and enforce the Node version floor that code requires — version-gate the assertion or widen it to the older runtime's code — so the test's pass/fail does not depend on which Node the contributor happens to run.]

**Notes (out of scope but worth flagging):**

- Credit where due: the spawn path at `cycle-esm-in-cjs.test.js:70` uses `process.execPath` rather than a literal `node` on `PATH`, and `fileURLToPath(fixtureUrl)` rather than string-slicing the `file://` URL — both are the Windows-correct and `PATH`-independent forms. The remaining parity tests resolve fixtures through `new URL(..., import.meta.url)`, which is checkout-location-independent. No hardcoded homes, hostnames, ports, UID paths, temp paths, GNU-only flags, or CRLF in the added lines. [rule: `skills/node-parity-test/SKILL.md` § The four-artifact layout]
- The new fixture `package.json` files each carry `"preinstall": "echo DO NOT INSTALL TEST FIXTURES; exit -1"`, matching the prescribed guard against a stray workspace `yarn install` hoisting them. `exit -1` is technically out of POSIX's 0–255 range and yields a different status under `dash` than `bash`, but it is non-zero under both, so the guard holds and this matches existing repo convention — no change wanted here. [rule: `skills/node-parity-test/SKILL.md` § The four-artifact layout]
- Not my seat, flagging for the curator/archivist: the skill's artifact 4 prescribes a separate `<name>-node-parity.test.js` file; this PR instead registers both treatments in one module (e.g. `cycle-rename-tdz-matrix.test.js`). Deliberate-looking and defensible, but it is a documented-layout deviation someone should rule on. [rule: `skills/node-parity-test/SKILL.md` § The four-artifact layout]

---

One note on what I did **not** flag, to save a re-check: I verified `packages/compartment-mapper/test/scaffold.js` does not call `lockdown()`, so the in-process `(node parity)` treatments in `cycle-rename-tdz-matrix.test.js` and `cycle-cjs-reexporter.test.js` are not running against frozen intrinsics — the "plain Node.js" claim holds despite the shared process.

### warden
Reviewed the SES linkage core against the warden's surface: intrinsic guarantees, boundary escapes, and reflection-op discipline.

### warden

**Verdict:** request-changes

**Findings:**

- `packages/ses/src/module-instance.js:531` discards the boolean result of `reflectDeleteProperty`. This breaks the package's own explicit anti-silent-failure convention: `packages/ses/src/commons.js:124-135` wraps `defineProperty` specifically so a silently-failing reflection op becomes a diagnosed `SES_DEFINE_PROPERTY_FAILED_SILENTLY` TypeError with a dedicated error-code doc. The newly exported `reflectDeleteProperty` (`commons.js:138`) is the raw intrinsic with no such guard, and `Reflect.deleteProperty` returns `false` instead of throwing. If `exportsTarget` carries a non-configurable own property at `k`, the delete no-ops silently and the very next line's `defineProperty` throws the "please report that the original defineProperty silently failed" error, misattributing a namespace-tampering failure to a platform bug and sending the reporter to the wrong error-code page. Check the result, or wrap it in commons the way `defineProperty` is wrapped. (should-fix) [proposed-rule: when SES adds a `Reflect.*` re-export whose failure mode is a `false` return, it either checks the result at each call site or ships a checking wrapper in `commons.js`, matching the `defineProperty` precedent.]

- `module-instance.js:267`, `:365`, `:446` define export properties eagerly with `configurable: true`, and `notifyStar` at `:374-376` calls `update(exportsTarget)` with the **raw** target rather than `exportsProxy`. The proxy at `packages/ses/src/module-proxy.js` correctly refuses `deleteProperty`, `defineProperty`, and `set`, but the star notifier routes around it. During a cycle a peer module body executes while the exporting module is still inside `imports()`, so it holds a live, unfrozen namespace whose export properties are now deletable and redefinable. ECMA-262 module namespace exotic objects are non-configurable by specification. The raw-target escape itself is pre-existing, so this PR does not open the hole; it widens what a peer can do through it from "add properties" to "add, delete, or redefine". Worth a comment at `:374` recording the constraint that the eager descriptors depend on, since the two sites are 100 lines apart and nothing links them. (should-fix) [proposed-rule: a descriptor deliberately weakened from the frozen end-state must name, at its definition site, the pass that restores it.]

- `packages/ses/src/notifier-with-resolver.js:56` returns a bare `{ notify, resolve }`. Every sibling factory in `ses/src` returns `freeze({...})`: `module-proxy.js:64`, `module-instance.js:91`, `module-instance.js:578`. This record closes over live linkage state and should match. (should-fix) [proposed-rule: factories in `packages/ses/src` return frozen records.]

**Notes (out of scope but worth flagging):**

- `module-instance.js:405-406` dereferences `mapGet(importedInstances, deferredSpecifier).notifiers` with no guard; an absent specifier yields a bare TypeError rather than a linkage diagnostic. [proposed-rule: deferred lookups in linkage code assert presence before dereference.]
- The `functor.js` hoisted-declaration reordering is correct and the ECMA-262 citation in the comment is accurate. No boundary concern.

Self-improvement: I nearly filed `for...of` and object-spread usage in the new file as SES-style violations before checking; both are already common in `ses/src` (`module-load.js`, `enablements.js`), so the "SES source avoids iterator protocol" instinct is stale for post-lockdown modules and only applies to the lockdown-time path. Verify a convention against the tree before citing it.

### wire-watcher
I reviewed the diff and empirically verified each finding by running probes against both the PR head and the base, then restored the worktree clean.

### wire-watcher

**Verdict:** request-changes

**Findings:**

- **must-fix — `notify === undefined` is an in-band marker that now silently disables the link-time export-resolution check.** `packages/ses/src/module-instance.js:400-414`: when `importNotifiers[localName]` is `undefined`, the old code called it and failed loudly; the new code cannot distinguish "upstream is mid-cycle" from "upstream has no such export" and installs a deferred notifier for both. Nothing ever sweeps unresolved deferrals, so a genuinely-missing re-export links successfully. Verified on this head — `export { nope } from './b.js'` where `b.js` lacks `nope`: `Object.keys(ns)` → `["nope"]`, and `ns.nope` throws `ReferenceError: binding "nope" not yet initialized`. On base `46d4edf` the same graph throws at link (`TypeError: notify is not a function`). ECMA-262 makes an unresolvable indirect export an early SyntaxError; the namespace must not advertise a phantom export. Ask: after all instances complete, error on still-unresolved deferrals (ideally `SyntaxError: The requested module '<spec>' does not provide an export named '<name>'`), plus a test for the missing-name case. [rule: skills/adversarial-tests/SKILL.md — trust-bypass / missing-marker category]

- **must-fix — mutual deferral latches into unbounded recursion.** `notifier-with-resolver.js:39-52` + `module-instance.js:405-411`: `resolve` is one-shot and latches onto whatever the upstream currently has, including *another deferred* notifier. `export {x} from './b.js'` ⇄ `export {x} from './a.js'` makes each forward to the other; verified head → `RangeError: Maximum call stack size exceeded`, base → `TypeError`. A small module graph overflows the linker's stack. Ask: refuse to latch onto an unresolved deferral, or bound the forward chain. [proposed-rule: a deferred-resolution chain must have a termination guard and fail with a diagnostic, never by stack exhaustion]

- **should-fix — the namespace's key order diverges between two readers of the same object.** ECMA-262 §10.4.6.5 sorts `[[OwnPropertyKeys]]` unconditionally, but eager `defineProperty` publishes insertion order until the sort pass runs. Verified in a cycle: a peer reading mid-window sees `zeta,alpha,beta,snapshot`; after linking the same object reads `alpha,beta,snapshot,zeta` (base saw `""`). The follow-up commit b08607b8 fixed the post-link order and missed the in-window order the eager define created. Ask: define eagerly in sorted order where names are known up front, or pin the deviation with a test — `import-gauntlet.test.js:57` only covers the acyclic post-link case. [rule: skills/adversarial-tests/SKILL.md — parser-divergence category]

- **comment-only — failed repair delete is unchecked.** `module-instance.js:531`: `reflectDeleteProperty` is the non-throwing variant and its `false` return is discarded, so the security-relevant repair step proceeds blind into a `defineProperty` that throws a confusing `Cannot redefine property` instead. [proposed-rule: when a repair pass deletes before redefining, assert the delete succeeded]

**Notes (out of scope but worth flagging):**

- `notifyStar` (`module-instance.js:374-376`) hands peers the **raw `exportsTarget`**, not `exportsProxy`, so a module in a cycle can inject a forged own property that survives the sort pass and gets frozen in (`{"injected":"FORGED", frozen:true}`). Confirmed identical on base — pre-existing, not this PR's doing — but this PR widens what is reachable through that hole. Worth a follow-up. [proposed-rule: the `'*'` notifier should publish the namespace proxy, not the mutable target]
- `makeNotifierWithResolver` returns an unfrozen object; `module-instance.js` freezes its own return. [rule: skills/adversarial-tests/SKILL.md]

**Self-improvement:** The seat brief's "in-band-marker trust-bypass" axis is written around hashes and `alg: none`, but the highest-value hit here was a plain `undefined` overloaded to mean two incompatible things. Worth widening the axis text to name *sentinel overloading* generally — any value whose meaning silently changed from "reject" to "retry later" — since that is the same bypass shape without a security field in sight. I would also note that running the base and head side by side was what separated three real regressions from two pre-existing hazards; attribute-before-report belongs in the seat's operating norms explicitly.

### coverage-auditor
### coverage-auditor

**Verdict:** comment-only

**Findings:**
- coverage of new lines could not be verified: no c8 coverage report at '/home/kris/garden/scratch/project-wt-endojs-endo-but-for-bots-pr779-panel-remaining-seats-6c768159/coverage/coverage-final.json' (run c8 with --all --reporter=json, or set GARDEN_COVERAGE_JSON); cannot verify new-line coverage — NOT assuming covered. Produce a c8 report (`c8 --all --reporter=json`) so new-line coverage can be checked, or confirm this package is intentionally outside coverage. This is surfaced, NOT treated as covered. [rule: skills/coverage-driven-testing/SKILL.md]
