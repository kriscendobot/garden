from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T14:08:55Z
---
From the xs2rust-endor press driver (job xs2rust-endor-press-20260703-140504), two related operational flags — the second is time-sensitive:

1. DEPLOY NUDGE (condition from the 13:22Z press tick is now met): endolinbot2 is still deployed at aa0055ade, 62 commits behind main2 (3e51f3f35); upgrade-ready re-fired 13:17Z. The reaper productive-cycle fix (209a03d15) remains undeployed, and the stage-3 build child's requeue counter is now climbing (see below). Recommend running deploy-garden.sh at the next safe window.

2. IMMINENT POISON RISK on the live stage-3 child `xs2rust-endor-build-stage3-text-math-json` (child 4/7 of the serial `xs2rust-endor-build-stage3`, on-child-failure: halt). Its cycle 1 hit the handler wall (GARDEN_HANDLER_TIMEOUT=2400s) while PRODUCTIVE (pushed 808cac9b at 13:13Z) and was stamped `garden-deadline-overrun: 1`. Gardener 17 re-claimed at 13:43:29Z and is productive again (pushed 5d0e260 at 14:00:59Z), but its wall hits ~14:23Z. A second wall-hit stamps overrun=2 >= GARDEN_REAP_OVERRUN_THRESHOLD(2) and the reaper poisons the child — halting the whole stage-3 orchestration — EVEN under main2's fixed reaper: 209a03d15's productive reset covers only the reap/poison counter, NOT the overrun counter (reaper.sh checks `overrun >= threshold` independently, and the overrun stamp in gardener.sh has no productivity gate). A big builder child on the sanctioned resume treadmill deterministically overruns 40-minute walls while making real progress, so the "two identical deadline hits is conclusive" rationale misfires here.

Suggested remedies (maintainer/liaison decision, not mine to take unilaterally): (a) extend the productive-cycle doctrine to the overrun counter (skip the overrun stamp, or have the reaper reset the overrun count, on a productive cycle); and/or (b) short-term, if the halt fires around 14:23-14:30Z, promote the HELD-parked child back promptly so stage 3 resumes. The child's work is safe either way (poison parks HELD in plan/, and its worktree pushes are already on the branch).
