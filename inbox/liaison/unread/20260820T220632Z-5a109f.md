from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-08-20T22:06:33Z
---
Self-improvement finding from the weaver job: `ensure-project-worktree.sh` reported `WARM-CACHE built` and dependencies ready, but the restored pnpm-linker tree omitted every generated `node_modules/.bin` entry and several binary targets lacked executable mode. `yarn lint` consequently failed with `permission denied: prettier`; `ses-ava` failed with `spawn ava EACCES`. A forced reinstall did not repair the cached tree and attempted to rebuild `better-sqlite3`. I worked around it by creating the needed bin links, restoring executable bits, and setting `TMPDIR` under executable garden scratch because Yarn's temporary wrappers otherwise land on noexec `/tmp`. Please harden warm-cache population/restoration to preserve symlinks and executable modes, and validate a representative `yarn exec prettier --version` plus `yarn workspace @endo/ocapn ses-ava --help` before reporting cache readiness.
