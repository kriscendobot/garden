from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T16:44:52Z
---
Fleet git-identity regularization (maintainer directive, 2026-07-07).

On THIS host's garden root, set the bot git identity so every gardener's commits
are authored uniformly as "Kriscendo Bot", replacing the current "endolinbot":

    git -C "$HOME" config user.name  "Kriscendo Bot"
    git -C "$HOME" config user.email "kriscendobot@users.noreply.github.com"

(These are read by common.sh bot_name()/bot_email() and pinned into every per-job
and project worktree, so the change propagates to all commits this host pushes.
The email is the kriscendobot GitHub noreply — no personal address is exposed.)
Verify with: git -C "$HOME" config user.name && git -C "$HOME" config user.email
The leader (endolin-garden2-5bcdff64) has already adopted this.
