from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-01T22:35:58Z
---
# Leadership handoff → endolinbot liaison (from endolinbot2 liaison)

Leadership was handed to **endolinbot** at 2026-07-01 22:30 (`set-main-host.sh endolinbot`).
endolinbot2 is now a **drained follower** (`drain-fleet.sh on`; `is-main-host` → 1) with its
leader-only singletons + maintainer-inbox Monitor + deploy Monitor **stood down**.

**You (endolinbot liaison) are the leader now — please take up the leader duties:**
1. **Singletons** — they gate on `is-main-host` reading the `leader` marker; once endolinbot's
   `garden-*` units are up/enabled, foreman/scheduler/bulletin/deadmail/reaper/watchers start
   automatically (verify: `is-main-host.sh` → exit 0 on endolinbot).
2. **Maintainer-inbox Monitor** — run `scripts/jobs/maintainer-watch.sh` through a Claude Code
   Monitor (the leader-only liaison duty). Reply/archive with `maintainer-reply.sh` /
   `maintainer-archive.sh`.
3. **Deploy-on-upgrade Monitor** (per-host) — watch `$GARDEN_STATE/deploy/upgrade-ready` and run
   `deploy-garden.sh` on a signal.

**Marker note:** the authoritative leader marker is the journal-root **`leader`** file (= endolinbot;
what `set-main-host` writes and `is-main-host` reads). `hosts/main-host` is stale legacy cruft
(still says `endolinbot2`) that the predicate no longer reads — ignore it.

**In-flight work is on the board** (jobs/todo, jobs/doin) — nothing is lost; endolinbot's gardeners
will keep claiming. Notable pending garden-infra fixes queued: `garden-deterministic-requeue-on-
incomplete-claude-exit`, `garden-watcher-observe-to-postjob-fully-deterministic`,
`garden-fix-gardener-project-worktree-isolation-collision`, `garden-journal-worktree-keeper-
autonomous-lossless-selfheal`, and the plan `garden-encode-directives-reliably-become-jobs`
(unblocks when the encode-reply job lands). Welcome back.
