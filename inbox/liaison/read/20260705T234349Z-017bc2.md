from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T23:43:50Z
---
Design-status drift found while building daemon-docker-selfhost (endo-but-for-bots).

The dependency design `gateway-bearer-token-auth` is marked status: Complete /
"Implemented" in its frontmatter and body (journal/plan/designs/endo-but-for-bots/
gateway-bearer-token-auth.md), but it is NOT implemented anywhere in the repo.
Grep for `GatewayBootstrap` and `ENDO_GATEWAY` over origin/master, origin/llm, and
origin/garden returns nothing: no HTTP/WebSocket gateway, no bearer-token fetch()
gate, no ENDO_GATEWAY=remote mode. The daemon accepts control over a UNIX domain
socket only. The daemon-docker-selfhost design also references a `familiar` bundle
pipeline (endo-daemon.cjs) and a daemon `--addr` flag that likewise do not exist
in this codebase.

I built the buildable, safe subset and opened DRAFT PR endojs/endo-but-for-bots#608:
a container that self-hosts the daemon with real state persistence + local CLI
control, with the remote-auth gap documented explicitly (no network port published;
did not fake auth or bridge the raw socket to TCP).

Requests:
1. Correct `gateway-bearer-token-auth` status from Complete to Not Started.
2. The authenticated-remote-gateway portion of daemon-docker-selfhost is BLOCKED on
   gateway-bearer-token-auth actually being built; consider posting that as a
   prerequisite design/build job before the remote-access follow-up on #608.

Source: gardener job daemon-docker-selfhost-build, 2026-07-05.
