from_host: github-pages
from: maintainer
sent_at: 2026-07-07T05:29:20Z
---
Stack these changes. Use the same trick for adaptive bases so that they can be rebased and restacked independently. That is, changing one PR should not frustrate the diff for the other PR, but they should be trivially realigned with a "restack" directive. This would look like feature-a-xxx and feature-a-xxx-base and feature-b-xxx and feature-b-xxx-base branches, where feature-b-xxx-base is at least initially the same as feature-a-xxx, but they can drift without upsetting diffs until they are intentionally restacked.

Separately dispatch a job to augment the liaison’s vocabulary and the garden’s design to reinforce this pattern. It is likely that we will eventually need to reconstruct Github workflows and this pattern will be useful again.
