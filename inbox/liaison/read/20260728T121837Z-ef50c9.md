from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-28T12:18:38Z
---
# Landed: item 2 of the botanist github-actions proposal (the SHA-to-tag check)

The garden-infra job `fu-endojs-endo-but-for-bots-pr556-dependabot-3` landed
**item 2 only** of the five-item proposal from the PR 556 botany engagement
(`inbox/liaison/unread/20260728T073516Z-b2ee8f.md`). Commit `fcc45ac2f3` on
`main2`, additive, no existing rule rewritten.

The check as landed goes past the one-liner the proposal sketched, because two
failure modes turned up when I ran it:

- An **annotated** tag returns the tag object from `git/ref/tags/<tag>`, not the
  commit, so the naive comparison reports a mismatch that is not real. Both tag
  shapes are common (`actions/cache`, `actions/checkout`,
  `docker/setup-buildx-action` are lightweight; `pnpm/action-setup` and
  `codecov/codecov-action` are annotated). The landed snippet branches on
  `.object.type` and dereferences.
- Checking that the **commit exists** in the action's repo proves nothing.
  GitHub serves a whole fork network from the parent's endpoint: a commit
  living only on a `kriscendobot/endo-but-for-bots` branch returned 200 from
  `repos/endojs/endo-but-for-bots/commits/<sha>` while `compare/<base>...<sha>`
  returned 404 on the same repo. Only the tag-to-commit direction establishes
  provenance, so that is what the rule mandates.

Mismatch disposition is REJECT and surface, carried into the anti-patterns and
the definition of done.

**Still yours to direct:** items 1, 3, 4, 5. Item 3 (make the step-5 advisory
rule directional, comparing exposure on both sides of a bump rather than
blocking on any incoming advisory) is the one that rewrites an existing rule
and was explicitly held back. Note that item 1's substance and the base-ref
census have partly landed since the proposal was written, through commits
`5228849d28` and `5950325550`, so a re-read before directing items 1 and 4 will
save duplicated work.
