from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T22:49:04Z
---
Enumeration accepted. Conclusion recorded: dckc's two `@agent` vhosts were the
only host-shaped exposures, no other publisher was exposed, both now closed
twice over (powers plane disabled globally, records de-registered). The
`f220b5fe` fixture and the inert `a0eeea3c` lookup failure are both properly
evidenced.

Your closing note is the important one and I have relayed it to the fix job:
`@agent`/`@host`/`@self`/`@main`/`cw` all sit in the SAME top-host namespace the
gateway looks up, so ANY publisher could have written a host-shaped string. dckc
was not special; he was first.

**Do NOT complete this job yet.** I sent you a message at 22:45Z, before your
enumeration report, that you either had not received or set aside. It is now the
highest-value work remaining, and your inbox disappears the moment you complete,
so it has to happen here.

Your own containment report recorded dckc driving `evaluate` against vhost
`fe0a8e60` at 21:27-21:39Z. Host powers were not merely reachable; they were
exercised. Read-only, from the journal and any other retained record:

1. **What did he evaluate, verbatim?** The actual expressions/payloads if
   retained. Do not summarize; the maintainer wants to read what ran.
2. **Did anything persist?** Files written or modified outside normal daemon
   operation, formulas minted, capabilities delegated or stored, pet names
   bound, workers that outlived the session. This is the difference between "he
   looked" and "the system now carries state he created". The de-registration
   you just performed does not remove anything created THROUGH those vhosts.
3. **Network egress** from the daemon during that window.
4. **Any other unauthenticated CapTP session** against either vhost across their
   lifetimes (21:04:12Z and 21:35:05Z to containment), from any source address
   other than his. This is the "did a third party find the URLs" question.

If the journal does not retain enough to answer one of these, **say so
explicitly** rather than omitting it. "The record does not exist" is a real
answer that bounds what can ever be known; silence is not. And caveat question 4
honestly, since anyone holding `evaluate` as the daemon user could have altered
these records.

Nothing else. Still read-only, no further production changes, and do not roll
back the containment drop-in.
