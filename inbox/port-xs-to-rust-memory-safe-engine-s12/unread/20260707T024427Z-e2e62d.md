from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T02:44:28Z
---
Stage-5 child 6/7 (coder, xsCode.c second half) — MAJOR PROGRESS, 8 slices landed & pushed to xs2rust-endor (HEAD 752a27a0f, PR #600 DRAFT). All byte-identical vs the oracle; `cargo test --workspace -- --test-threads=1` EXIT=0 (16 coder byte-identity tests).

LANDED THIS SESSION (each a pushed, green slice):
1. symbol-free control flow — while/do/C-for, labeled break/continue (target-stack), switch, throw, debugger, try/catch(no-binding)/finally (alias/finalize/jump). Wired program returnTarget.
2. this + regexp literals.
3. untagged template literals.
4. **THE ATOM TABLE** — fxNewParserSymbol hash (sum=Σ(sum<<1+ch)&0x7FFFFFFF, bucket=sum%1993), prepend insertion, the 58 fxInitializeParser seeds in order, fxParserCode's bucket-walk ID assignment (usage&1 only). Symbols pre-interned in AST pre-order = XS lex order. Unlocks every symbol op. + global variable/member access (EVAL_REFERENCE+GET_VARIABLE, GET_PROPERTY).
5. calls + computed member access — the codeThis family, fxParamsNodeCode RUN, AT/GET_PROPERTY_AT.
6. assignment (=), compound (+=…), short-circuit (&&=/||=/??=), new — the codeReference/codeAssign families.
7. increment/decrement (prefix+postfix) + delete (codeDelete family).
8. object + array literals (data + computed props, elisions).

The whole EXPRESSION surface + control flow is now byte-identical.

REMAINING (the harder half — functions/declarations and everything gated on them):
- PREREQUISITE: per-node access resolution. The scoper emits `accesses: Vec<AccessRecord>` but NOT keyed by node, so the coder can't pick GET_LOCAL/GET_CLOSURE vs GET_VARIABLE per Access. Add a node_key→Option<(scope,declare)> map to ScopeTree (small scoper addition) — the keystone for the next slice.
- Declarations: var/let/const + the full fxScopeCoding{Block,Body,Eval,Program,Params}/Coded/Refresh/Reset/Retrieve/Store family (my scope helpers are the declare-free no-ops, asserted on declare_count==0).
- Functions (plain/generator/async/async-gen, params/defaults/rest/destructuring, arguments, closures, arrows, super/home-object) + the function frame (BEGIN/END/RESERVE) + fxCoderOptimize's END-folding passes.
- Classes (ctor/derived/methods/accessors/static/private/static-blocks).
- for-in/for-of/for-await-of; with (WITH env + evalFlag); spread (fxSpreadNodeCode for-of); direct-eval params (EVAL op).
- Modules (import/export linkage, module wrapper); yield/await.
- Named folds inside landed slices: object ...spread/__proto__/shorthand/methods; array spread; tagged templates; fxCompoundExpressionNodeCodeName (anon fn/class naming — node_code_name stub returns false); catch(e) binding.

NEXT: the resolution-wiring + declaration slice, then functions. The 8 landed slices are independent and safe on the branch.
