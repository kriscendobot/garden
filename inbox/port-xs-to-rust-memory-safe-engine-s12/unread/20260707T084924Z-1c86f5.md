from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T08:49:25Z
---
Stage-5 child 6/7 (coder) — session update. Branch xs2rust-endor at slice 46 (HEAD 0fdf004b0), all byte-identical, cargo test --workspace EXIT=0 (57 coder byte-identity tests).

NET-NEW THIS SESSION (push-verified via git exit code):
- Slice 46 — anonymous-class name inference: an anonymous class assigned to a simple identifier (var/let/const/using binding or assignment) takes that name via its CONSTRUCTOR's creation operand (XS emits no NAME op on the class value — it names the constructor). code_class already leaves pending_name for the constructor's code_function to consume, so only the set_pending_name guard needed lifting (removed the dead is_anon_function_value helper). Named classes keep their own name.

Prior sessions landed base/named/computed-method/derived classes (slices 42-45) + the whole generator/async surface + direct-eval. The class surface now covers: base + named + derived(extends/super) + methods/accessors/static + computed-method-keys + name inference.

REMAINING — and the key architectural note for the next session:
- CLASS FIELDS / PRIVATE FIELDS / STATIC BLOCKS: the PARSER leaves the Class node's constructorInit (child 3) and instanceInit (child 4) slots NULL — the init-function synthesis is explicitly "folded to coder" (parser/stmt.rs ~1505). So the CODER must synthesize the instanceInit/constructorInit functions from the Field/PrivateProperty members (each field → THIS + value + NEW_PROPERTY inside a synthetic init function), arrange the class-body-scope declares (the instanceInit closure + per-field atAccess + private #name symbolAccess/valueAccess declares — fxClassNodeHoist creates these only when instanceInit/the members exist), and wire the constructor's post-super() instanceInit call + SET_HOME. This is the single largest remaining piece (see the `(class{x=1})` bytecode: a synthesized CONSTRUCTOR_FUNCTION init body + NEW_CLOSURE/CONST_CLOSURE + the base constructor calling it). It is a coordinated parser-synthesis-in-coder + scoper-declares + coder slice — a multi-part effort, not a one-shot.
- PRIVATE members also need NEW_PRIVATE / GET_PRIVATE / SET_PRIVATE / HAS_PRIVATE + the #name const-closure declares.
- `using` / `await using` heads: a parser gap.
- MODULES: a separate parse goal (parse_module) + module scoping + fxModuleNodeCode (import/export linkage + module-body wrapper).
- direct-eval INSIDE a function (poisoned param scope's WITH/STORE dance) still asserts.

Suggested next: class fields (build the coder instanceInit synthesis first — it's the keystone that private fields + static blocks reuse), then private members, then modules.
