from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T01:11:32Z
---
Stage-5 child 6/7 (coder, xsCode.c second half) — PARTIAL, three green slices landed & pushed to xs2rust-endor (PR #600 DRAFT). All byte-identical vs the oracle; `cargo test --workspace -- --test-threads=1` EXIT=0.

LANDED (byte-identical, new fixtures in coder_byte_identity.rs):
- Slice 1 (fe86f5df1): symbol-free control flow — while/do/C-for, labeled break/continue (the firstBreak/ContinueTarget label-target stacks + env/scope adjust), switch, throw, debugger, try/catch(no-binding)/finally incl. the alias/finalize/jump target machinery threading break/continue/return through finally via the selector local. Also wired coder->returnTarget onto the Program node (a try aliases it; its alias count feeds the try selector — without it every try selection was off by one).
- Slice 2 (009d1c9fa): this (THIS / derived GET_THIS) + regexp literals (REGEXP intrinsic).
- Slice 3 (bb5f3a68e): untagged template literals with literal substitutions.

THE ONE GATING FOLD — the atom/symbol table (named in child 5's fold, "child 6 wires atom-table assignment"). Everything remaining reaches a symbol-operand op (GET_VARIABLE/SET_PROPERTY/NEW_LOCAL/NEW_CLOSURE/SYMBOL/…) whose 2-byte txID operand is baked into codeBuffer. Matching those bytes requires replicating XS's symbol interning exactly: the parser's seeded symbol set, fxNewParserSymbol's string hash + symbolModulo (incl. rehash growth), the collision-chain order, and fxParserCode's bucket-walk ID assignment gated on `usage&1` (only emitted symbols get IDs, in bucket order). This is a self-contained subsystem; I did not start it rather than sit on a large uncommitted/broken tree at budget end.

DEFERRED behind the atom table (all assert loudly today, not silently wrong):
- Variable access/assignment, member/property get/set, calls, new, object/array/template(tagged) construction, destructuring.
- var/let/const & function declarations; the full fxScopeCoding{Block,Body,Eval,Params,Program}/Coded/Refresh/Reset/Retrieve/Store family (declaring loop/block/switch scopes, catch(e) binding, program/eval var hoists) — the scope helpers here are the exact declare-free no-ops asserted on declare_count==0.
- Functions (plain/generator/async/async-gen, params/defaults/rest/destructuring, arguments, closures, arrows, super/home-object), classes (ctor/derived/methods/accessors/static/private/static-blocks).
- for-in/for-of/for-await-of (GET_PROPERTY next/done symbols), with (WITH env + eval-flag), spread.
- Modules (import/export linkage, module-body wrapper), yield/await.

NEXT: build the atom table as its own slice; it unlocks ~everything above. The three landed slices are independent and safe on the branch.
