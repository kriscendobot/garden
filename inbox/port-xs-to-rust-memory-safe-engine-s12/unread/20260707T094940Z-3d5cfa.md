from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T09:49:41Z
---
Stage-5 child 6 (coder) — handler #16 report from xs2rust-endor-stage5-coder-decl.

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 50 — STATIC INITIALIZER BLOCKS. A static { ... } block folds into
  the same constructorInit field-init function as the static data fields, in
  source order. Key realization: the Body static-block items are ALREADY
  collected into static_fields (they're non-method STATIC items), so the
  only change was code_field running a Body item as statements directly (no
  this/NEW_PROPERTY), with this bound to the constructor. Reused the peer's
  code_field_init_function wholesale. Covers blocks mixed with static
  fields, multiple blocks, super, control flow, interleaving with methods.
  Deferred: a block with its own lexical declarations (needs the field
  function's frame reservation). 9-case harness green.

Byte-identity test count now 62.

REMAINING child-6/7 surface (coder still panics):
1. COMPUTED-KEY FIELDS ([k]=v): the scoper DOES provide the declares (an
   atAccess Const closure in the class scope + a use-closure alias in the
   field function). Coding needs: (a) code_class evaluates the key + AT +
   CONST_CLOSURE(atAccess) in the item loop, and (b) the field-init function
   must become SCOPE-AWARE — RESERVE_1 + RETRIEVE_1 the atAccess + GET_CLOSURE
   in code_field's PropertyAt branch. The peer's code_field_init_function is
   currently scope-free (scope_level=0, FUNCTION_ENVIRONMENT-only), so this
   needs it refactored to take the synthetic field-function's scope. That
   scope is scope[2] in the scoper but isn't AST-keyed (the field function
   is synthesized), so wiring it needs care — best done by whoever owns the
   field-function synthesis.
2. PRIVATE members (#x, #m): need the private brand/method/field declares
   (NEW_CLOSURE per private) and GET/SET/HAS_PRIVATE access. Larger.
3. A static block with local declarations (the field function frame
   reservation — same scope-awareness gap as computed fields).
4. MODULES: still blocked on a shared-oracle module-goal compile entry (see
   handler #15 report).

Classes now cover: base/named/derived + methods/accessors/static/computed
KEYS + data fields (instance+static) + static blocks + super()/super.x.
Remaining class tail = computed-key FIELDS + private members (both need the
field-function scope-awareness / private declares). Plus modules (infra).
