from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T03:44:12Z
---
Stage-5 child 6 (coder) — handler #2 report (after reaper requeue) from xs2rust-endor-stage5-coder-decl.

This handler added ONE new slice on top of the prior handler's slices 9-10
(declarations, with). All byte-identical vs oracle, cargo test --workspace
--test-threads=1 EXIT=0, pushed to xs2rust-endor, PR #600 kept DRAFT.

LANDED THIS HANDLER:
- Slice 11 — functions (first slice): fxFunctionNodeCode for plain
  (CONSTRUCTOR_FUNCTION) and arrow (FUNCTION) function values — nested CODE
  block (BEGIN/END/END_ARROW), per-function coder-state save/restore,
  FUNCTION_ENVIRONMENT storing, plain-function non-enumerable caller own
  property — plus function DECLARATIONS (fxDefineNodeCode with
  fxScopeCodeDefineNodes hoisting: defines emit at top of scope, in-list
  occurrence a no-op via a coded set), fxReturnNodeCode, fxBodyNodeCode,
  empty fxParamsBindingNodeCode, and a null-symbol operand (Byte payload on
  a symbol opcode -> id 0) for anonymous functions. 22-case byte-identity
  test added. Scoped to SIMPLE bodies (expression statements + return-expr)
  in non-naming contexts.

Hard-won findings (avoid rediscovering):
1. Anonymous function name operand: XS serializes a null txSymbol as id 0;
   emit with a non-Symbol payload on the symbol opcode (symbol_id returns 0).
2. Function-declaration hoisting: fxScopeCodeDefineNodes runs BEFORE the
   body statements in fxProgramNodeCode/fxBlockNodeCode/fxBodyNodeCode, so
   `f;function f(){}` emits the define first. Guard with a coded-node set.
3. The BIG blockers for the rest of the function surface:
   (a) CONTROL-FLOW function bodies hit XS's branch-threading OPTIMIZER
       (fxParserCode does branch-to-branch / branch-to-END threading and
       dead-code removal). The Rust port has ONLY branch-to-next elision.
       Porting the full optimizer is a prerequisite for non-trivial bodies.
   (b) DECLARING function bodies: a function-body var lives in the body
       BLOCK scope (scope[2]) but is NEW_LOCAL'd early with NO unwind — the
       returnTarget scope-level snapshot vs body scope-coding order needs
       care (the body's declareNodeCount/discount interaction). Chasing it
       produced spurious UNWIND/branch bytes; deferred.
   (c) NAME INFERENCE (`var f=function(){}` -> CONSTRUCTOR_FUNCTION(f)):
       XS's parser/grammar sets the function's symbol from the binding/
       assignment/property target. The Rust parser (child 3) does NOT set
       it, so the coder sees an anonymous node. Likely a parser gap to fix,
       or a coder pending-name mechanism at the naming sites.
   All three are guarded as named-gap asserts (panic, never wrong bytes);
   a 38-case differential harness confirmed 0 wrong-bytes on deferred cases.

STILL FOLDED (coder panics; the remaining child-6/7 surface):
- Function params (defaults/destructuring/rest) + arguments object.
- Captured closures (fxScopeCodeRetrieve/Store) + home-object/super.
- Named function expressions (CURRENT name binding) + name inference.
- Control-flow / declaring function bodies (need the XS optimizer port).
- Generators/async + yield/await.
- Classes (all of it).
- for-in/for-of/for-await-of + for(let) refresh.
- Modules: import/export linkage + module-body wrapper.

Suggested next: port the fxParserCode branch-threading OPTIMIZER first — it
unblocks all non-trivial function/loop bodies and is a prerequisite for
nearly everything else. Then params+arguments, then closures, then the
function-body declaration frame, then generators/classes/modules.
