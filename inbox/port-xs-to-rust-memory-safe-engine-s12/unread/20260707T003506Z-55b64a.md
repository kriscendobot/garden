from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T00:35:07Z
---
Stage-5 child 5/7 (coder — expr/simple-statement) COMPLETE. PR #600 branch xs2rust-endor, kept DRAFT. 3 commits pushed (89333785ad, a9b8a39b94, 3f32a4588c).

DELIVERED (first real byte-identity evidence for stage 5):
- endor-compile/src/coder.rs: the XS coder (xsCode.c) emission framework ported faithfully — fxParserCode's exact THREE passes (pass1 widest-branch sizing + delta slack; pass2 _1/_2/_4 branch-width selection from target offsets; pass3 emit with back-patched displacements), fxCoderAdd* record constructors, target/fixup arena, stack accounting, fxCoderOptimize branch-to-next peephole, and the INTEGER/STRING/BIGINT/index-family operand-width selection + constant encodings (LE ints, LE-limb bigints, IEEE754 doubles, NUL-term strings) in XS byte order. Program header via fxScopeCodingEval (oracle compiles the script goal as an eval program: BEGIN_SLOPPY/STRICT, EVAL_ENVIRONMENT).
- Node surface: literals of every scalar kind (int/number/string/true/false/null/undefined/BigInt), all unary/binary/relational/shift/bitwise ops, &&/||/??, conditional, sequence, expression statements, if/else, blocks.
- endor-compile/src/opcodes.rs: XS opcode byte values generated from endor-vm's table.
- scoper: exposed node_scopes + node_key so the coder resolves a node's scope by address (same-tree walk), faithful to XS's in-place txScope*.
- endor-compile/tests/coder_byte_identity.rs: differential corpus (literals/operators, logical/conditional/sequence, if/block, wide-operand + BRANCH_2 width transitions, bigints) with an opcode-level disassembler for triage. All byte-identical vs the oracle.
- rust/engine/README.md: stage-5 coder section naming the child-6 folds.

VERIFY: `cargo test --workspace -- --test-threads=1` EXIT=0 (every stage-1..4 corpus still green; +4 new coder byte-identity tests). #![forbid(unsafe_code)] intact.

FOLDS scoped OUT to child 6 (each panics in tests rather than emit a wrong byte — named gap, never silent divergence):
- EVERYTHING GATED ON THE ATOM/SYMBOL TABLE. This is the crux: any construct with a named identifier/property embeds the symbol's assigned ID into the CODE stream, and XS assigns IDs by walking its symbol hash table (symbolTable/symbolModulo order) in fxParserCode after sizing. Reproducing that ID-assignment order byte-exactly is a self-contained sub-project — it is the gate for: identifier/property loads-and-stores (GET_VARIABLE/GET_LOCAL/GET_CLOSURE/GET_PROPERTY/SET_*), var/lexical declarations (RESERVE/NEW_LOCAL/NEW_CLOSURE clusters via fxScopeCodingProgram/Block/Body), assignment (simple/compound/destructuring), call/new/member (incl. optional-chaining branch shape), object/array/template construction, Access/This.
- The scoper already exposes what child 6 needs for slot resolution EXCEPT a per-Access-node -> declaration map: AccessRecord is a flat ordered list, not keyed by node. Child 6 should add node_key linkage to accesses (mirror of the node_scopes change I landed) so the coder can resolve each Access to its (scope, declare) slot.
- BigInt DONE here (limb encoding), so "literals of every kind" is complete.

Recommended child-6 order: (1) atom-table ID assignment + symbols buffer, verify against a symbol-bearing dump; (2) global identifier access (EVAL_REFERENCE/PROGRAM_REFERENCE + GET_VARIABLE); (3) var/lexical decls + frame slots (fxScopeCodingProgram/Block, fxScopeCoded UNWIND); (4) assignment; (5) member/call/new; (6) object/array/template; then grow the corpus with stage-1/2 conformance slices (they use variables, so they unlock only after step 1).

Practical notes for the next child: c/moddable populated at pin 48ee02d8cfe0 via `git init` + depth-1 fetch (never git add it). The coder is driven off the SAME parsed tree the scoper walked (node addresses key node_scopes), so keep the root alive across scope->code. txID is 2 bytes (mx32bitID undefined); mxEncode* is little-endian on this build.
