from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T03:17:57Z
---
Stage-5 child 6 (coder, second half) — handler report from xs2rust-endor-stage5-coder-decl.

LANDED (byte-identical vs oracle, cargo test --workspace --test-threads=1 EXIT=0, pushed to xs2rust-endor, PR #600 kept DRAFT):
- Slice 9 — variable declarations (var/let/const) at program/eval scope:
  full fxScopeCodingEval (sloppy var/Define hoist + direct-eval WITH publish;
  strict reserve + block coding), fxScopeCodingBlock/fxScopeCoded slot
  allocation + UNWIND teardown, binding/declare/access resolution choosing
  NEW_LOCAL/LET_LOCAL/CONST_LOCAL/VAR_LOCAL + GET_LOCAL/SET_LOCAL over the
  symbol path. Scoper extended to expose a per-node access-resolution map;
  program node stamped mxEvalFlag so the top scope is EVAL (eval-program
  lexicals are plain LOCALs, not the program-scope CLOSURE marking). Block
  lexicals code + unwind. 3 new byte-identity tests.
- Slice 10 — with statement (fxWithNodeCode: TO_INSTANCE/WITH, body under a
  forced eval flag, WITHOUT); sloppy-only. 1 new byte-identity test.
- README updated to record slices 9-10 and name the remaining folds.

Key finding (hard-won, avoid rediscovering): the oracle compiles the Script
goal with mxProgramFlag|mxEvalFlag, so the top scope must be XS_TOKEN_EVAL,
not PROGRAM. fxScopeBound marks PROGRAM-scope declares closure|useClosure but
an EVAL scope leaves them plain LOCAL. Before this fix the Rust scoper built
a PROGRAM scope and every let/const emitted the *_CLOSURE opcode variant
(one byte off). compile_with now stamps mxEvalFlag on the program node before
scoping. scope_program (parser/scoper tests) intentionally stays PROGRAM.

FOLDED (still panic! in the coder — the headline child-6/7 surface, NOT done
this handler; each is a large transliteration of xsCode.c):
- Functions in every flavor (plain/generator/async/async-generator), param
  lists (defaults/destructuring/rest), arguments machinery, closures, arrows,
  home-object/super. (Define nodes at eval scope assert.)
- Classes (constructor/derived, methods/accessors, static members, private
  fields/methods/brands, static blocks).
- for-in/for-of/for-await-of and for(let ...) refresh (scope_code_refresh is
  still the non-declaring no-op/assert).
- Generators/async yield/await emission.
- Modules: import/export linkage + module-body wrapper.
- Destructuring binding targets (ArrayBinding/ObjectBinding/RestBinding).

Suggested next slice order: (1) plain function bodies + Define (unblocks the
most), (2) for-of/for-in with refresh, (3) generators/async, (4) classes,
(5) modules. The declaration + scope-coding machinery landed here (decl_index
map, scope_coding_block/scope_coded, resolution_of) is the foundation the
function/param slices build on.
