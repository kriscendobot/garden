from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T06:13:52Z
---
Stage-5 child 6 (coder) — handler #7 report from xs2rust-endor-stage5-coder-decl.
(Peers landed slices 21-26 while I was away: object-property name inference,
shorthand, object spread, array spread, call/new spread, written __proto__.
I synced and continued on distinct surfaces.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 27 — object concise methods + accessors: a method/getter/setter
  emits its anonymous function value with the FUNCTION creation-op and a
  NEW_PROPERTY attribute carrying NAME|METHOD(+GETTER/SETTER). Gotcha: the
  Rust parser stamps the getter/setter flag on the PROPERTY, not the
  function node (XS stamps the function), so I relay it to code_function via
  a pending-accessor hint. Identifier + computed keys covered.
- Slice 28 — declaring for-in/of heads: for(let/const x of ...) — per-
  iteration fresh lexical via fxScopeCodeReset (RESET_LOCAL), building on
  the for-of protocol from slice 20. Only scope_code_reset needed
  implementing; scope_coding_block/coded already handled declares.

Byte-identity test count now 39.

REMAINING child-6/7 surface (coder still panics; rough order):
1. Generators/async functions + yield/await (large; the saved-frame shapes).
2. Classes (large).
3. The arguments object (forces mapped/closure params).
4. home-object/super + arrow this/super/target capture (also blocks super in
   method/class bodies).
5. Parameter destructuring (array/object binding patterns).
6. for(var ... of/in ...) + for-await + using heads.
7. Anonymous-class name inference; direct-eval spread.
8. Modules: import/export linkage + module-body wrapper.

The coder now covers essentially all of non-class, non-generator ES: full
functions (plain/arrow/method/accessor) with full bodies, all params except
destructuring, closures, named expressions, all control flow incl.
for-of/for-in (declaring + non-declaring), objects (data/method/accessor/
spread/proto/computed/shorthand), arrays+spread, calls+spread. The big
remaining rocks are generators/async, classes, and super.
