from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T09:40:42Z
---
Stage-5 child 6 (coder) — handler #15 report from xs2rust-endor-stage5-coder-decl.
(Peers landed slices 46-49 while I was away: anonymous-class name inference,
static class fields, instance fields for base + derived classes.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 46b — cross-construct INTEGRATION corpus: a 12-case byte-identity
  test of real-world programs that combine many strata at once
  (functions/closures + destructuring + generators/async + classes/super +
  control flow, deeply nested). Confirms the interactions between all the
  slices are byte-identical — slot numbering carried across a closure
  through a destructured param, super() inside a destructuring method, a
  for-await over a defaulted rest param, etc. All 12 pass. This is the
  strongest regression guard; per-construct tests don't cover interactions.

Byte-identity test count now 61.

HONEST ASSESSMENT of the remaining surface — I investigated all of it and
did NOT force a risky/colliding slice:
1. MODULES — genuinely BLOCKED ON ORACLE INFRA. endor_oracle::run() only
   compiles the SCRIPT goal (fxParseScript with mxProgramFlag|mxEvalFlag);
   import/export are syntax errors there, so modules aren't testable via the
   current harness. Testing modules needs a NEW oracle C entry that parses
   as a module goal and returns codeBuffer WITHOUT running (a module can't
   fxRunScript without a linker) — a change to the SHARED test oracle
   (endor_shim.c + FFI + build.rs), plus fxModuleNodeCode (102 lines) +
   fxImport/ExportNodeCode. Recommend a dedicated slice that first adds the
   oracle module-compile entry, guarded so it can't break the script path.
2. CLASS TAIL (private members, computed-key fields, static blocks) — needs
   SCOPER class-hoisting the field slices (47-49) are actively building: the
   class scope's per-member CONST_CLOSURE declares, the private brand/method
   closures, the atAccess for computed keys, and the constructorInit fold
   for static blocks. The scoper currently emits NO class-scope declares for
   private members (verified). This overlaps the in-flight field work, so a
   concurrent slice here risks a semantic collision; best done by whoever
   owns the field-declare scoper extension.

Coder now covers ALL of non-module ES except the class private/computed/
static-block tail: functions (every flavor) + params + closures +
destructuring + arguments + super/this, all control flow incl. for-of/in/
await + try/finally, objects/arrays/spread, generators/async/yield*/await,
and classes (base/named/derived + methods/accessors/static/computed-keys +
data fields + super()). Remaining = modules (infra) + class private/fields
tail (peer scoper work).
