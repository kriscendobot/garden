from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T05:23:52Z
---
Stage-5 child 6 (coder) — handler #5 report from xs2rust-endor-stage5-coder-decl.
(NOTE: a peer gardener landed slices 16 defaults + 17 rest while I was away;
I synced and continued.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 18 — captured closures: the closure slot contract. A captured
  variable is promoted to a closure slot in its defining scope (NEW_CLOSURE/
  VAR_CLOSURE); the capturing inner function's fxScopeCodeRetrieve emits
  RETRIEVE_1 to pull closures into its frame (accesses resolve to
  GET_CLOSURE), and fxScopeCodeStore emits STORE_1 of the defining scope's
  slot into the new function's environment on creation. Params/locals,
  nested/multi-capture, arrow closures, mutation, capture-in-control-flow
  all covered.
- Slice 19 — named function expressions: (function g(){...}) binds its own
  name g in a const slot (CURRENT), so the body recurses by name. The Rust
  scoper folds XS's symbolScope into the function scope as a Define, so this
  is a targeted code_function_name emission (NEW_LOCAL in coding_params +
  CURRENT/CONST_LOCAL/POP). A name captured by an inner function stays
  deferred.

Byte-identity test count now 30.

REMAINING child-6/7 surface (coder still panics; rough order):
1. for-of/for-in/for-await-of + for(let) refresh — fxForInForOfNodeCode is
   LARGE (iterator protocol + a catch/finally for iterator .return cleanup,
   6 temporaries, target aliasing, fxScopeCodeReset/UsedReverse). Budget a
   full slice for it.
2. Generators/async + yield/await.
3. Classes (all).
4. The arguments object (forces mapped params).
5. home-object/super + arrow this/super/target capture.
6. Object-method/property name inference (the coder NAME-op path).
7. Modules: import/export linkage + module-body wrapper.

Coder now covers: declarations, with, functions (plain/arrow) with FULL
bodies (control flow + declarations), positional + default + rest params,
name inference (identifier targets), catch-params, captured closures, and
named function expressions. The scoper's slot contract (CLOSURE/USE_CLOSURE/
alias) is now fully consumed by the coder.
