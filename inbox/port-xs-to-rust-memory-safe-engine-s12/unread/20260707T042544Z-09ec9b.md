from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T04:25:46Z
---
Stage-5 child 6 (coder) — handler #4 report from xs2rust-endor-stage5-coder-decl.

Landed the BIG unblocker this handler. Byte-identical vs oracle, cargo test
--workspace --test-threads=1 EXIT=0, pushed to xs2rust-endor, PR #600 DRAFT.

LANDED THIS HANDLER:
- Slice 14 — full branch optimizer + non-trivial function bodies. Ported
  fxCoderOptimize's full FOUR peephole passes (previously only the last was
  ported): (1) a BRANCH_1 whose target reaches an END* over placeholders/
  unwinds becomes that END* inline; (2) an UNWIND_1 before an END* is
  dropped; (3) a dead END* before the same END* is dropped; (4)
  branch-to-next. Plus the fxStatementNodeCode store-and-pop fusion
  (trailing SET_LOCAL/SET_CLOSURE + POP => PULL_LOCAL/PULL_CLOSURE) and XS's
  mxExpressionNoValue optimization (threaded via a no_value coder flag
  through code_node to postfix/compound; set on statement + for-iteration
  expressions).

  Impact: function bodies with CONTROL FLOW (loops, if/else, switch, labeled
  break, try-finally, return threaded to END) and with DECLARATIONS
  (var/let/const, nested blocks) now code byte-identically. The prior
  simple-body and declaring-body GUARDS are removed. Two new byte-identity
  tests (function_control_flow_bodies, function_declaring_bodies). Test count
  now 25.

Key finding: the optimizer was the keystone the prior handler flagged. It
also (via pass 2, unwind-before-end removal) resolved the function-body-var
returnTarget/UNWIND interaction that looked like a separate scoper problem —
it was the missing optimizer passes all along.

STILL FOLDED (coder panics; remaining child-6/7 surface):
- Catch-parameter bindings (catch(e){...}) — declaring catch, deferred.
- Named function expressions (function scope binds its name to CURRENT as a
  CONST; the Rust scoper folds XS's symbolScope into the function scope as a
  Define with no CURRENT-initializer node, so it needs a coder special-case
  rather than a straight port).
- Captured closures (fxScopeCodeRetrieve/Store, NEW_CLOSURE, alias slots) +
  home-object/super.
- Parameter defaults/destructuring/rest + the arguments object.
- Generators/async + yield/await.
- Classes (all).
- for-in/for-of/for-await-of + for(let) refresh.
- Modules: import/export linkage + module-body wrapper.
- Object-method/property name inference (the coder NAME-op path).

Coder now covers: declarations, with, functions (plain/arrow) with FULL
bodies (control flow + declarations) + positional params + name inference.
Recommended next: catch-parameter (small), then closures, then
generators/classes/modules.
