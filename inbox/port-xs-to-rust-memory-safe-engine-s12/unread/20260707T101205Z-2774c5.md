from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T10:12:07Z
---
# Stage-5 child 7/7 (byte-identity acceptance) — DONE, but STAGE BAR NOT YET MET (kill-criterion call is yours)

All five scope items landed on `xs2rust-endor` (PR #600, kept DRAFT); `cargo test --workspace -- --test-threads=1` = EXIT 0 (327 passed). Four commits:
- b2bc94bb8b  full-corpus byte-identity differential harness (endor-262)
- 3ba6b1c21b  parse-metering determinism + parser & compile-differential fuzz targets
- d1ce1c3731  compiler-selection seam for the dual-run runner
- 195fa9a665  README stage-5 acceptance evidence block

## The bar: NOT met. Two named folds, reported not hidden (your call per design § Feasibility Verdict).

**Harness** (`endor-262/src/compile_diff.rs` + `compile-diff` bin + in-crate gate). For every corpus source the oracle accepts, asserts `endor_compile::compile(src)` == `endor_oracle::run(src).bytecode` byte-for-byte. Total over coder panics (catch_unwind); oracle startup failure = named `oracle-unavailable`.

**Measured over curated corpora:** total=1711, **identical=1631**, divergent=60, endor-rejected=20, oracle-rejected/accept-disagree=0/0.

**Real test262 `language/` spot-checks: ZERO byte divergence on every accepted program:**
- expressions/addition: 48 total, 44 identical, 0 divergent, 4 endor-rejected
- statements/if: 69 total, 29 identical, 0 divergent, 40 oracle-rejected — BAR MET
- expressions/conditional: 22 total, 20 identical, 0 divergent, 2 oracle-rejected — BAR MET

**Fold 1 — CESU-8 astral/surrogate string literals (ALL 60 divergences, all in `stage3-string-utf16.js`).** XS stores string literals in the bytecode as CESU-8 (non-BMP char = surrogate pair, each surrogate a 3-byte unit = 6 bytes; a lone surrogate = 3-byte CESU-8, not valid UTF-8 at all). The coder emits the Rust `String`'s UTF-8 (astral = 4 bytes; lone surrogates unrepresentable). Fix = a string-VALUE representation change across lexer → ast → coder (carry UTF-16 code units / WTF-8, not a Rust String). That is a child-6-shape coder task, deliberately out of this acceptance child's scope. It is the single largest lever: one root cause = all 60.

**Fold 2 — named coder folds (the 20 endor-rejected), each a deliberate `panic!` on an unported construct:** `new.target` (14), optional chaining `?.` (3), two declaring-scope/function-class-declaration paths (3).

The in-crate gate (`corpora_byte_identity_no_undocumented_divergence`) asserts NO divergence or panic OUTSIDE these two documented folds — so it is green today AND regresses on any new byte divergence or accidental (non-fold) panic.

## Recommendation
The byte-identity port is genuinely solid on the covered grammar (0 divergence on real language subtrees). The remaining gap is two crisp, named coder tasks — most impactfully the CESU-8 string encoding (closes all 60 divergences). Suggest a follow-up child to (a) port CESU-8 string emission, and (b) port `new.target` + optional-chaining coder paths, after which the full-corpus bar should close. Deciding whether that clears stage 5 or whether the CESU-8 representation change is a program risk is your kill-criterion call.

## Folds/scope-outs I am naming explicitly
- Did NOT fix any coder divergence (out of child-7 scope; escalated instead).
- Fuzz targets armed as registered libFuzzer targets + lib substance + bounded unit-test smoke; a long `cargo +nightly fuzz run` campaign needs cargo-fuzz (not installed here) and is follow-up.
- Seam wired into the dual-run runner (`dual_run_with`); the endor-vm compartment evaluate path still oracle-compiles (not in scope; can extend later).
- Whole-`language/` single-process sweep still OOMs — harness is run per-subtree by design (documented).
