from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T08:11:08Z
---
Stage-5 child 6 (coder) — handler #13 report from xs2rust-endor-stage5-coder-decl.
(Peer landed slice 41, direct-eval parameters, while I was away.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 42 — BASE CLASSES (the biggest remaining rock, first slice). Ported
  fxClassNodeCode for an anonymous class with no heritage: fresh prototype
  (NULL/OBJECT), the base constructor (code_function now handles the BASE
  flag: BEGIN_STRICT_BASE / END_BASE), the CLASS opcode binding the
  prototype/constructor pair, and concise method/accessor/static members
  (NEW_PROPERTY with DONT_ENUM + method/getter/setter bits; static on the
  constructor via DUB, instance on the prototype). Also added the SCOPER's
  fxClassNodeBind (push_variables(2)) so the enclosing frame's RESERVE
  includes the two class temporaries — the scoper had deferred class
  hoisting entirely. Covers generator/async/super-using methods, explicit
  and synthesized constructors, static getters. 13-case harness green.

Byte-identity test count now 53. Full scoper suite still green.

REMAINING child-6/7 surface (coder still panics):
1. CLASSES tail (each needs more scoper class-hoisting work):
   - Named classes (class C{}) — the symbolScope binds the name as a
     CONST_CLOSURE; scoper must create the symbolScope.
   - extends / derived constructors + super(...) — BEGIN_STRICT_DERIVED /
     END_DERIVED, the CHECK_INSTANCE super() shape.
   - Instance/static FIELDS + PRIVATE members/brands — the class scope's
     declares (CONST_CLOSURE per member), instanceInit/constructorInit, and
     the base-constructor instance-field-init block in fxFunctionNodeCode.
   - Static blocks; computed method keys.
   - Anonymous-class name inference (var C=class{}).
2. MODULES: import/export linkage + the module-body wrapper.

The base-class scaffolding (code_class + code_function BASE handling +
scoper fxClassNodeBind) is now in place, so the class tail builds on it.
Next recommended: named classes (small scoper symbolScope add), then
extends, then fields/private (the largest, needs the class scope declares).
