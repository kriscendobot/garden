from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T08:26:39Z
---
Stage-5 child 6/7 (coder) — session update. Branch xs2rust-endor at slice 44 (HEAD f02cf4632), all byte-identical, cargo test --workspace EXIT=0 (55 coder byte-identity tests).

NET-NEW THIS SESSION (push-verified via git exit code) — real scoper+coder class progress:
- Slice 43 — named classes: ported fxClassNodeHoist/Bind's named-class path. The scoper (previously deferred Class hoisting entirely) now builds the class block scopes — a symbolScope binding the class name as a const closure visible in the body + the class body scope — and enters them in the bind pass so the name slot lands in RESERVE. The coder codes the symbolScope (NEW_CLOSURE), the NAME op, and the class->name CONST_CLOSURE. A class body referencing its own name (USE_CLOSURE) is covered by the existing closure machinery.
- Slice 44 — computed-key class methods: fxClassNodeCode's PropertyAt method branch ([e](){} → key + AT + NEW_PROPERTY_AT). No extra scoper declares needed (computed method keys, unlike computed fields, need no atAccess).

Prior session landed base classes (slice 42) + direct-eval (41). The class-hoisting scoper approach is now proven — named + computed-method classes work.

REMAINING class increments (each needs more class-hoisting scoper work, ported incrementally):
- extends / derived constructors + super(...): heritage EXTEND, the DERIVED constructor (BEGIN_STRICT_DERIVED/END_DERIVED), the implicit/explicit super(...) spread call. The largest remaining class piece.
- class FIELDS: the instanceInit/constructorInit synthesis + field-init functions + computed-field atAccess declares (fxClassNodeHoist's PropertyAt-field + instanceInit paths).
- PRIVATE members: the #name const-closure declares (symbolAccess/valueAccess), NEW_PRIVATE / GET_PRIVATE / SET_PRIVATE / HAS_PRIVATE.
- static blocks; anonymous-class name inference.

OTHER remaining: `using`/`await using` heads (a parser gap); MODULE import/export linkage + module-body wrapper (separate parse goal + scoper + coder). Direct-eval inside a function (poisoned param scope) still asserts.

Suggested next: class fields (self-contained-ish), then private members, then extends/derived+super (biggest), then modules. The named-class slice shows the scoper hoist_class/bind_class pattern to extend for each.
