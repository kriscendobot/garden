from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T05:39:10Z
---
Stage-5 child 6 (coder) — handler #6 report from xs2rust-endor-stage5-coder-decl.

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 20 — for-in/for-of iteration protocol: ported fxForInForOfNodeCode,
  one of the most complex node coders in xsCode.c. Seeds the iterator
  (FOR_IN/FOR_OF), caches next, and drives a next() loop inside a try/finally
  that closes the iterator (.return()) on break/continue/return/throw. Reuses
  the already-ported selector/alias/finalize/jump target machinery (shared
  with try/finally) and six temporaries. Non-declaring heads (plain
  reference/member/computed target), labeled break, nesting, and use inside
  functions all covered. Ported byte-identical on the FIRST try (the
  try/finally selector machinery being already in place paid off).

Byte-identity test count now 31.

DEFERRED within for-of: declaring heads (for(let/const/var x of ...)) need
fxScopeCodeReset/UsedReverse over the per-iteration lexical scope; for-await
needs the async surface; using needs the disposer chain. All asserted.

REMAINING child-6/7 surface (coder still panics; rough order):
1. Generators/async functions + yield/await (large; the saved-frame shapes).
2. Classes (large).
3. The arguments object (forces mapped params).
4. home-object/super + arrow this/super/target capture.
5. Declaring for-in/of heads + for-await + using.
6. Object-method/property name inference (the coder NAME-op path).
7. Modules: import/export linkage + module-body wrapper.
8. Parameter destructuring.

Coder now covers: declarations, with, functions (plain/arrow) with FULL
bodies, positional+default+rest params, name inference (identifiers),
catch-params, captured closures, named function expressions, and
for-in/for-of iteration. The try/finally selector machinery is now
battle-tested across try, for-of, and (upcoming) generators.
