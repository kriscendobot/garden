from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T06:34:17Z
---
Stage-5 child 6 (coder) — handler #8 report from xs2rust-endor-stage5-coder-decl.

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 29 — the arguments object: a function referencing arguments carries
  a synthetic arguments Var; scope header slots it, and a code_arguments_object
  prelude builds it (ARGUMENTS_SLOPPY mapped / ARGUMENTS_STRICT, operand =
  param count) and stores it. Also pinned for(var x of/in ...) coverage (var
  hoists out -> loop block non-declaring -> already coded correctly).
- Slice 30 — mapped arguments closure-marks parameters (SCOPER change):
  ported fxParamsBindingNodeBind — a sloppy function with arguments + a
  simple param list promotes each param to a closure slot so the mapped
  object can alias it. Key insight: marking a declare CLOSURE changes NO
  frame counts (closure_count only tracks alias declares, not marked
  existing ones), so this was low-risk. Completes the arguments surface:
  function(a){...arguments...} now byte-identical.

Byte-identity test count now 41. Full scoper test suite still green.

REMAINING child-6/7 surface (coder still panics; rough order):
1. Generators/async functions + yield/await (large; the saved-frame shapes).
2. Classes (large: constructor/derived, methods, static, private, fields).
3. Parameter destructuring (array/object binding patterns).
4. home-object/super + arrow this/super/target capture (blocks super in
   method/class bodies too).
5. for await + using heads.
6. Anonymous-class name inference; direct-eval spread.
7. Modules: import/export linkage + module-body wrapper.

The coder now covers essentially ALL of non-class, non-generator ES,
including the full arguments object. The three big remaining rocks are
generators/async, classes, and modules; super is the cross-cutting
prerequisite for class/method bodies that use it.
