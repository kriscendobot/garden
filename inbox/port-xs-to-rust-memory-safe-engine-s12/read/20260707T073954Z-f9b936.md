from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T07:39:55Z
---
Stage-5 child 6 (coder) — handler #12 report from xs2rust-endor-stage5-coder-decl.
(Peers landed slices 35-38 while I was away: generators+yield, async+await,
async generators, yield* delegation. I synced and continued.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 39 — for-await-of: the is_async branch of the ported
  fxForInForOfNodeCode became reachable once async functions landed; pinned
  with a 4-case test. (using declarations are a PARSER gap, child 3, not a
  coder concern — the parser rejects `using x=a`.)
- Slice 40 — super in methods + arrow this/super/target capture: found that
  object-method super (member read/call/store/delete/computed, in plain/
  async/generator methods) ALREADY worked via the member coder's super path.
  Implemented the missing piece: the arrow-default capture in
  fxScopeCodeRetrieve/Store — an arrow that transitively uses
  this/super/target retrieves the receiver+target (RETRIEVE_TARGET/
  RETRIEVE_THIS) and stores them on creation (STORE_ARROW). Removed the two
  arrow-default deferral asserts. 17-case test.

Byte-identity test count now 51.

REMAINING child-6/7 surface (coder still panics) — down to the last rocks:
1. CLASSES (the big one: fxClassNodeCode — constructor/derived, methods/
   accessors, static members, private fields/methods/brands, static blocks,
   and super in class bodies / derived-constructor super(...)). This is the
   largest single remaining node coder.
2. MODULES: import/export linkage + the module-body wrapper (fxModuleNode /
   fxScopeCodingModule; needs the module goal + the async loader shape).
3. Anonymous-class name inference; direct-eval spread (small tails, but the
   first depends on classes).

The coder now covers ALL of non-class ES: full functions (plain/arrow/
method/accessor/generator/async/async-generator) with full bodies + all
parameter forms + closures + destructuring + super/this capture, all
control flow incl. for-of/for-in/for-await + try/finally, all
objects/arrays/spread, generators/async/yield*/await. Classes and modules
are the two remaining large dedicated slices.
