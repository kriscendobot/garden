from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T00:11:46Z
---
Stage-5 child 4/7 (scoper/hoisting pass) — COMPLETE, pushed to xs2rust-endor (PR #600, kept DRAFT).

Landed 3 green increments (each `cargo test --workspace -- --test-threads=1` EXIT=0):
  1. 3ea14b3dce  endor-compile/src/scoper.rs — port of xsScope.c's two passes (hoist + bind).
  2. 9cf45e066a  module records: imports (immutable indirect let bindings) + local exports.
  3. f00f2204ce  tests/corpus_scope_smoke.rs — 1711 corpus programs scoped, 0 panics.

WHAT'S DONE (faithful to xsScope.c @ 48ee02d8cfe0):
- Scope tree: PROGRAM/EVAL/FUNCTION/MODULE/BLOCK/WITH + catch param+statement scopes, with XS's
  exact kinds/boundaries. Body-vs-function scope split (vars -> bodyScope, args -> functionScope).
- Hoisting: var + function-decl hoisting to the body scope (incl. the NoToken intermediate-block
  placeholders and their fxScopeHoisted removal; the PROGRAM/EVAL var/define declareCount discount);
  let/const/using lexicals; duplicate-declaration early errors (fxDeclareNodeHoist/fxDefineNodeHoist),
  duplicate-argument (strict/arrow/async/method/non-simple), duplicate-variable, duplicate-import/export.
- Closure capture + SLOT NUMBERING: the per-scope ordered declare list IS the coder's slot order
  (coder does index = scopeLevel++ over firstDeclareNode; verified xsCode.c fxScopeCoded). Function-scope
  closure aliases (NoToken, closure|useClosure, forwarding to the ancestor) with closureNodeCount, and the
  per-function scopeCount = binder scopeMaximum (fxBinderPush/PopVariables transliterated across Array/
  Object/Spread/Template/Try/Delegate/Postfix/For-in-of/bindings). scopeCount is the frame slot count.
- this/target/super arrow marking (fxScopeArrow -> mxDefaultFlag). Direct eval + with scope poisoning
  (fxScopeEval up the chain; eval de-opt flags on function/environment nodes). eval/with lookup shadowing.
- Module records: imports as closure|useClosure indirect lets; local exports mark the declaration
  closure|useClosure; module top-level functions resolve lexically (not global, unlike scripts).
- Output surface: ScopeTree::dump renders scope tree + declare order/flags + scopeCount + access
  resolutions; 21 unit fixtures pin the numbering contract for the coder children, values derived by
  reading the C.

REPRESENTATION NOTE for the coder child: the endor AST is an immutable value tree, so the scoper keeps
a scope arena and keys node->scope / node-extra-flags by node address instead of mutating nodes.
Declare-list ORDER + counts + closure flags + scopeCount are the contract; slot indices themselves are
assigned by the coder (confirmed: xsScope.c never sets declareNode->index; xsCode.c does).

FOLDS (explicitly scoped OUT, named for the coder child — byte-identity is ITS bar, not this pass's):
- CLASSES: fxClassNodeHoist/Bind not ported — the class-name symbolScope, private-member/computed-key
  slot synthesis (atAccess/symbolAccess/valueAccess), instanceInit/constructorInit init-record slots, and
  base/derived instanceInitAccess wiring. Class nodes currently fall through to default distribute (method
  functions ARE scoped; class-specific scope records are NOT). This is the largest remaining scoper piece
  and pairs with the parser's already-deferred class-field -> init-function desugaring.
- MODULES partial: `export ... from` re-export indirection (synthesized indirect lets + specifier links),
  `export default`, and export-link back-references (firstExportSpecifier chains) are folded; named/default
  import + `export {local}` + `export const/let/var/function` are done.
- HOST (@) nodes: fxHostNodeHoist define synthesis folded (no @ in the safe corpus).
- fxParamsBindingNodeBind arguments-object mapping (mapped/getter/setter arity) left to the coder; params
  are distributed for scope resolution.
- using/await-using: parser-folded at the pin already (mxExplicitResourceManagement==0); the disposable
  const slot machinery IS implemented for when it lands.

Recommend the next scoper follow-up (if desired) be a dedicated CLASS-scoping child; it's self-contained
and testable the same dump-fixture way.
