from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T07:07:57Z
---
Stage-5 child 6 (coder) — handler #11 report from xs2rust-endor-stage5-coder-decl.

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 34 — object rest + computed-key destructuring: completed
  fxObjectBindingNodeCodeAssign. Object rest ({...r}) collects the source's
  own enumerable properties minus the explicitly-bound keys via COPY_OBJECT
  (each bound key pushed as an exclusion arg via SYMBOL/AT); computed keys
  ({[k]:v}) read through GET_PROPERTY_AT with an at-temporary. Nested
  patterns already recurse.

  DESTRUCTURING IS NOW COMPLETE: array + object, assignment + let/var/const
  binding + parameters, with holes / rest / defaults / computed / nested.

Byte-identity test count now 45.

REMAINING child-6/7 surface (coder still panics; the big rocks):
1. Generators/async functions + yield/await (large; START_GENERATOR/
   START_ASYNC, the saved-frame shapes from stage-3/4).
2. Classes (large: constructor/derived, methods, static, private, fields,
   static blocks).
3. home-object/super + arrow this/super/target capture (cross-cutting
   prerequisite for class/method bodies that use super).
4. for await + using heads.
5. Anonymous-class name inference; direct-eval spread.
6. Modules: import/export linkage + module-body wrapper.

The coder now covers ALL of non-class, non-generator ES. The three remaining
rocks are generators/async, classes, and modules; super is the cross-cutting
prerequisite for class/method bodies. Each of those is a large dedicated
slice (100+ lines of xsCode.c apiece).

Minor note: avoid backticks in double-quoted git -m commit messages — bash
command-substitutes them (one word got dropped from slice 34's commit body;
harmless, subject/code correct). Use single-quoted -m.
