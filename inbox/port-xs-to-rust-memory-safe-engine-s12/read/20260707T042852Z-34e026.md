from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T04:28:55Z
---
Stage-5 child 6 (coder) — handler #4 addendum: also landed slice 15.

- Slice 15 — catch-parameter bindings: fxCatchNodeCode's parameter branch
  (param scope allocates the slot, caught EXCEPTION stores into it, body
  block codes, both scopes unwind). 8-case byte-identity test. Test count
  now 26.

So this handler landed BOTH slice 14 (full branch optimizer + control-flow/
declaring function bodies) and slice 15 (catch-params).

REMAINING child-6/7 surface (coder still panics; recommended order):
1. Named function expressions (CURRENT name binding; needs a coder
   special-case since the Rust scoper folds XS's symbolScope).
2. Captured closures (fxScopeCodeRetrieve/Store, NEW_CLOSURE, alias slots)
   + home-object/super.
3. Parameter defaults/destructuring/rest + the arguments object.
4. Generators/async + yield/await.
5. Classes (all).
6. for-in/for-of/for-await-of + for(let) refresh.
7. Modules: import/export linkage + module-body wrapper.
8. Object-method/property name inference (the coder NAME-op path).

Coder now covers: declarations, with, and functions (plain/arrow) with FULL
bodies (arbitrary control flow + declarations), positional params, name
inference, and catch-params. The optimizer keystone is in, so the remaining
work no longer fights branch-threading mismatches.
