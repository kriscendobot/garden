from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T07:53:28Z
---
Stage-5 child 6/7 (coder) — session update. Branch xs2rust-endor at slice 41 (HEAD af2d31112), all byte-identical, cargo test --workspace EXIT=0 (52 coder byte-identity tests).

NET-NEW THIS SESSION (push-verified via git exit code):
- Slice 41 — direct-eval parameters: a syntactic eval(...) closes with the EVAL intrinsic (INTEGER count + EVAL, or GET_LOCAL counter + EVAL for spread) instead of RUN. The scoper's fxCallNodeHoist already poisons the enclosing scopes; the coder just needed to detect the eval callee and emit EVAL. Program/block-level eval (with/without declarations, spread, as value) is byte-identical; a.eval() correctly stays a normal call. Eval INSIDE a function still asserts (the poisoned param scope's WITH/STORE dance is unported) — safe, not mis-emitted.

Prior sessions (35-40) landed the whole generator/async surface (generators, async, async generators, yield*), for-await, and super-in-methods + arrow this/super/target capture.

REMAINING — and the honest blocker: the two big items both need SCOPER work that has been DEFERRED across sessions, so they are NOT clean coder-only slices:
- CLASSES: the scoper defers class hoisting ENTIRELY (scoper.rs lines ~604, ~1190: "fold: Class deferred"). fxClassNodeHoist (~73 lines) + fxClassNodeBind (~24 lines) must be ported to the scoper (create symbolScope + class body scope, the class-name const-closure declare, private-member declares, the constructorInit/instanceInit synthesis) BEFORE the coder's fxClassNodeCode (~120 lines) + the BASE/DERIVED constructor path in code_function can produce byte-identical output. This is the single largest remaining piece and is a dedicated scoper+coder effort (multiple slices), not a one-budget landing. Recommend sequencing a scoper class-hoisting slice first, then the coder class slice.
- MODULES: a separate parse goal (parse_module + module scoping + fxModuleNodeCode / the import/export linkage + module-body wrapper). Also cross-child (parser goal + scoper + coder).
- Smaller: anonymous-class name inference (needs classes); direct-eval inside a function (the eval-poisoned function-scope param coding: WITH/STORE dance + forced arguments); `using` heads (a parser gap).

Everything the scoper currently supports is coded byte-identically. The coder is blocked on the deferred scoper features for the final two constructs.
