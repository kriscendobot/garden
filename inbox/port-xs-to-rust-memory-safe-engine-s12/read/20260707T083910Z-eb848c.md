from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T08:39:12Z
---
Stage-5 child 6 (coder) — handler #14 report from xs2rust-endor-stage5-coder-decl.
(Peers landed slices 43-44, named classes + computed-key class methods,
while I was away.)

LANDED THIS HANDLER (byte-identical, cargo test --workspace EXIT=0, pushed):
- Slice 45 — DERIVED CLASSES + super(): class extends E derives the
  prototype from E (code the heritage then EXTEND); the derived constructor
  uses BEGIN_STRICT_DERIVED / END_DERIVED (code_function now handles the
  DERIVED flag), and the new code_super (fxSuperNodeCode) invokes the parent
  constructor (SUPER + the argument list) and installs the result as this
  (SET_THIS). Covers the synthesized default constructor
  (constructor(...args){super(...args)}), explicit constructors,
  static/instance members, and an expression heritage. 10-case harness green.

Byte-identity test count now 56.

REMAINING child-6/7 surface (coder still panics):
1. CLASS FIELDS + PRIVATE members + STATIC BLOCKS — the largest remaining
   class piece. Needs scoper class-hoisting: the class scope's per-member
   CONST_CLOSURE declares, the instanceInit/constructorInit define nodes,
   and the base/derived-constructor instance-field-init block in
   fxFunctionNodeCode (THIS/GET_CLOSURE/CALL/RUN, and after super() in
   code_super). Also private-member access (GET/SET/HAS_PRIVATE).
2. Anonymous-class name inference (var C=class{}).
3. MODULES: import/export linkage + the module-body wrapper.

Classes are now mostly done: base + named + derived + methods/accessors/
static/computed-keys, all constructors, super() and super.x. Only fields/
private/static-blocks remain on the class side, plus modules. The class
scaffolding (code_class, code_function base/derived, code_super, scoper
bind_class) is all in place for the fields work to build on.
