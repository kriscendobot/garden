from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-07T09:26:43Z
---
# Fold report — xs2rust-endor-stage5-coder-decl (class fields)

Job: stage-5 child 6/7 of the XS→Rust coder port, PR endojs/endo-but-for-bots#600
(branch xs2rust-endor, base llm, DRAFT). Three byte-identical slices landed +
pushed this session; `cargo test --workspace -- --test-threads=1` EXIT=0 after each.

## Slices landed (byte-identical vs endor_oracle::run)

- **Slice 47 — static class fields** (a0a79f80e). `code_class` collects non-method
  members as fields; static data fields synthesize the `constructorInit` field
  function (`CONSTRUCTOR_FUNCTION` / `BEGIN_STRICT_FIELD` body running
  `fxFieldNodeCode` per field), called inline with the constructor as this/home
  (`GET_LOCAL ctor` / `SET_HOME` / `CALL` / `RUN_1 0` / `POP`). No scoper change
  (the static init is called inline, not via a closure).

- **Slice 48 — instance fields, base classes** (2805a4377). The parser leaves
  Class children[3]/[4] (constructorInit/instanceInit) NULL — the synthesis is
  FOLDED TO THE CODER. New coordinated scoper+coder work:
  - Scoper `hoist_class`: `class_has_instance_field` detects instance data fields;
    adds an anonymous `const` closure declare (`Sym::Anon`, XS's instanceInit,
    ID=-1) to the class body scope. Exposed via `ScopeTree.class_instance_init`.
  - Scoper `bind_function`: a BASE constructor creates a use-closure alias in its
    scope targeting that declare (XS's mxBaseFlag lookup).
  - Coder: stores the instanceInit field function into the closure, homed on the
    prototype (`SET_HOME` + `CONST_CLOSURE`); the base constructor calls it on
    entry (`THIS` + `GET_CLOSURE` + `CALL` + `RUN_1 0` + `POP`), inserted after
    START_ASYNC / before the params.
  - **Anon-symbol threading**: `declares_of` now returns `Option<Sym>` (was
    `Option<String>`, which collapsed Anon→None). NEW_CLOSURE for an anon slot
    serializes the null symbol via `add_symbol_null` (id 0); RETRIEVE/STORE count
    it because `sym.is_some()` is true for Anon. Added `Coder::sym_name` helper.

- **Slice 49 — instance fields, derived classes** (746bf42f6). A `super(...)` node
  captures the instanceInit closure via `bind_super` (a `scope_lookup` on the anon
  symbol from the super's scope — creates the function-boundary alias; exposed via
  `ScopeTree.super_instance_init`), and `code_super` calls the initializer after
  SET_THIS installs `this` (`GET_THIS` + `GET_CLOSURE` + `CALL` + `RUN_1 0` +
  `POP`, fxSuperNodeCode). Shares slice 48's declare + field function.

## Corpus proven
Static: single/multiple/bare, mixed with methods, expression/anon-fn values, named
classes. Instance (base+derived): single/multiple/bare, interleaved with methods and
static fields, name-inferring/expression/this values, synthesized and explicit
super(...) constructors, named and anonymous classes. Derived-no-field + base-only
regressions hold. Fixtures: `class_static_fields`, `class_instance_fields`,
`class_instance_fields_derived` in tests/coder_byte_identity.rs (60 coder tests pass).

## Still folded (coder asserts/panics, never mis-emits) — the remaining child-6/7 surface
- **Private members** (#x): need NEW_/GET_/SET_/HAS_PRIVATE + private-property
  declares in the class scope (xsScope.c ~505-513: symbolAccess/valueAccess closures).
- **Computed-key fields** (`[e]=v`): need a per-field `atAccess` closure declare in
  the class scope; `code_field` currently handles only Token::Property (asserts on
  PropertyAt).
- **Static blocks** (`static {}`): a static-init block form.
- **`using`** heads — a parser gap.
- **Modules** — import/export linkage + module-body wrapper. NOTE: untestable via
  endor_oracle::run (it compiles the Script goal as an eval program where
  import/export are syntax errors); needs a different harness.

Ground rules held: explicit-pathspec commits, never staged c/moddable, rebase-CAS
pushes (exit-code checked), PR kept DRAFT, no maintainer/PR contact. Job NOT
complete — the folded surface above remains.
