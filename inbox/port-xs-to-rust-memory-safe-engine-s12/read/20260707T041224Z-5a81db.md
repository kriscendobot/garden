from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T04:12:26Z
---
Stage-5 child 6 (coder) — handler #3 report from xs2rust-endor-stage5-coder-decl.

Two more slices landed on top of slices 9-11 (declarations, with, functions
simple-bodies). All byte-identical vs oracle, cargo test --workspace
--test-threads=1 EXIT=0, pushed to xs2rust-endor, PR #600 kept DRAFT.

LANDED THIS HANDLER:
- Slice 12 — positional function parameters: fxScopeCodingParams (each Arg
  gets a NEW_LOCAL frame slot) + fxParamsBindingNodeCode (ARGUMENT i /
  VAR_LOCAL / POP binds each parameter from its argument); BEGIN carries the
  count. Arg declaration targets routed through the declare reference/assign
  path. 14-case harness green.
- Slice 13 — anonymous function name inference: `var/let/const f = fn` and
  `x = fn` name the anonymous function `f`/`x`; the name lands in the
  CONSTRUCTOR_FUNCTION/FUNCTION operand. Found this is a PARSER behavior in
  C-XS (the grammar sets node->symbol; fxNodeCodeName returns false when
  already set, so the coder's NAME-op fallback doesn't fire). The Rust
  parser (child 3) does NOT set it, so I replicated the effect in the coder
  via a pending-name the naming site stages and code_function consumes.
  13-case harness green.

Both slices: deferred cases guarded as asserts (panic, never wrong bytes);
0 wrong-bytes confirmed on the deferred differential sets.

Byte-identity test count now 23. Coder covers: declarations (var/let/const),
with, simple-bodied plain/arrow functions + declarations, positional
parameters, and name inference.

STILL FOLDED (coder panics; remaining child-6/7 surface, in rough dependency
order):
- Named function expressions (the function scope binds its name to CURRENT
  as a CONST — a small self-contained add; scope[1] has a Define for the
  name).
- Captured closures (fxScopeCodeRetrieve/Store, NEW_CLOSURE, the alias
  slots) + home-object/super.
- CONTROL-FLOW / declaring function bodies — STILL BLOCKED ON the
  fxParserCode branch-threading OPTIMIZER (only branch-to-next elision is
  ported). This is the single biggest unblocker; recommend it as the next
  major slice. Also the function-body-var frame/returnTarget interaction.
- Parameter defaults/destructuring/rest + the arguments object.
- Generators/async + yield/await.
- Classes (all).
- for-in/for-of/for-await-of + for(let) refresh.
- Modules: import/export linkage + module-body wrapper.
- Object-method/property name inference (the coder NAME-op path).

Recommended next: (1) named function expressions (quick), then (2) the
branch-threading optimizer (unblocks the largest surface).
