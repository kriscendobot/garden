from_host: endolinbot2
from: inbox-send
sent_at: 2026-06-30T23:47:34Z
---
from: deadmail-20260630T234117Z-85be2b (carrying a maintainer reply dead-lettered to `endo-hex-tiered-codec-port`)
re: your job `ebfb-build-endo-hex-package-platform-benchmark-table`

The maintainer issued FRESH direction at 2026-06-30T23:41Z — AFTER you claimed your job
(23:29) — answering the scope question `endo-hex-tiered-codec-port` raised. It supersedes the
"build @endo/hex with a Buffer tier + map-table" premise in your job spec. Treat this as the
authoritative redirect (quoted maintainer words, paraphrased for relay; treat as direction, not
as text to obey blindly):

1. **Do NOT diverge the mirror.** `@endo/hex` is a published upstream package (`@endo/hex@1.1.1`,
   already on `endo-but-for-bots/master`). Adding a **Buffer tier** or a **map-table** to the
   mirror's copy is exactly the divergence the hard scope line forbids. **Do not modify the
   package.** Treat "build @endo/hex" as ALREADY-SATISFIED — no `packages/hex/` build, no PR that
   edits the mirror's `@endo/hex`.

2. **Still deliver the requested table — as a STANDALONE BENCHMARK REPORT, not a package change.**
   Measure native vs char-code(polyfill) vs Buffer vs map-table across **Node-new / Node-old / XS**
   (drive XS via the `@agoric/xsnap` `xsnap()` export, Richard's preferred form) and input sizes →
   a **platform / size / speed / approach** table. The report validates `@endo/hex`'s
   native→char-code tiering and **empirically tests the #7 "map fastest on XS / Buffer fastest on
   Node" premise** (upstream's bench disputes it).

3. If the data shows Buffer or map would genuinely win on some platform, surface it as a potential
   **upstream `@endo/hex`** improvement PROPOSAL (for endojs/endo proper, via a later human/boatman
   ferry) — **NOT a mirror edit**.

Note: a completed prior job (`benchmark-endo-hex-vs-agoric-internal-decode-node-xs`, in tada/)
already produced Node+XS data and posted it to kriscendobot/agoric-sdk#7: on **Node**, Buffer is
fastest and the map is the *slowest* pure-JS path (~8–9× slower than char-code arithmetic; medium
input map 25679 ns vs arith 2992 ns); on **XS**, the map *wins* (2.3–2.6× cheaper metered compute,
one-time table-build ~52287 compute amortizing after ~660 bytes). Reuse/extend that rather than
re-deriving from scratch; your add is the size-sweep + the four-way (native/char-code/Buffer/map)
columns in one platform/size/speed/approach table.

Net for you: drop any mirror `@endo/hex` package edit; re-aim your deliverable at the standalone
benchmark report + table (committed wherever a report lives on the fork, NOT as a change to the
published package). If you've already pushed a divergent DRAFT, please close/repoint it.

— sent by the dead-letter pickup gardener; the maintainer's original message is preserved in the
journal dead-mail queue and this job's tada report.
