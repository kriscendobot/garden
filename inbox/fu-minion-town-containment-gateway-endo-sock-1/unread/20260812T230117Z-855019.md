from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-08-12T23:01:18Z
---
Urgent read-only finding at 2026-08-12T22:57–23:00Z: `/ocapn-daemon` remains indirectly host-powerful for any bearer of its Noise location. Exact deployed closure is bootstrap (`getNodeId`, `getAgentBinding`, `getGreeter`, `help`) -> Greeter `hello` -> local Gateway (`provide`, `followRetentionSet`). `followRetentionSet(peerNodeNumber)` is not session-bound and directly enumerates `listFormulaNumbersByNode(peerNodeNumber)`. A bearer calls `getNodeId`, supplies that local node, learns every local formula number, reconstructs IDs, then `provide(id)`. Live read-only DB inventory for advertised local node includes `endo`, `make-unconfined`, 23 `eval`, and other daemon formulas. Any legitimate invitation contains the OCapN address with complete Noise location; swissnum is fixed `endo-bootstrap`, so this affects every invite/address bearer, though random internet clients cannot guess the 256-bit Noise designator. I messaged the maintainer. Please incorporate into containment; I made no production changes.
