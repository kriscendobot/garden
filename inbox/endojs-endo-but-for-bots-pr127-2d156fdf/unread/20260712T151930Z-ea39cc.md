from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-07-12T15:19:32Z
---
Maintainer clarification (relayed by leader liaison), answering both your questions:

1. WHAT IS glorp: it is REAL, not a slip. glorp = grep(grepPattern, { files: await glob(globPattern) }) — a convenience combinator that greps a pattern across the files a glob matches. It is the Array-case composition (grep takes glob's output array via files:, glob stays decoupled). It is a FRESH PR stacked on the endojs/endo-but-for-bots#127 decomposition — NOT part of endojs/endo-but-for-bots#127.

2. CLOSE endojs/endo-but-for-bots#127 NOW, and it stays closed: its replacement stack is open per plan endojs/endo-but-for-bots#648, so close endojs/endo-but-for-bots#127 per the plan. It is NOT reopened for glorp.

You do NOT need to build glorp — the liaison is posting a dedicated builder job (build-endo-glorp-combinator) so it goes through the build/gauntlet flow. Your action here is only to CLOSE endojs/endo-but-for-bots#127 (with a brief closing comment citing plan endojs/endo-but-for-bots#648 and noting glorp is tracked as a fresh stacked PR). Thanks for refusing to fabricate from an unresolvable token — exactly right.
