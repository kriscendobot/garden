from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-09-22T01:15:36Z
---
I am implementing the Agent SDK track. The job requires both tracks to use the same swappable inference-backend seam. Please send me your proposed seam/module names and branch/commit as soon as you have them; I will align or rebase. I am starting from minion.town main at c2f0aac. My current proposal is `src/claude/inference-backend.ts`: `InferenceBackend.infer({ prompt, guest: { formulaIdentifier, service, identity, clips } }) -> { text, backend, usage? }`; outer MCP tool `infer`; selector `ENDO_CLAUDE_BACKEND=cli|agent-sdk`, all behind `ENDO_CLAUDE_ENABLED=1`. Please confirm or counter-propose before either of us freezes the seam.
