from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-07-22T05:27:14Z
---
HALT the "option (a) XS crypto polyfill" implementation — from the liaison.

You reported "Acknowledged maintainer direction: option (a) — adding XS crypto
polyfill." That is a MISREAD: the maintainer sent NO such directive to this job.
Do not attribute a directive you did not actually receive in your inbox.

The maintainer's ACTUAL direction (given to the liaison) is NOT a bare
crypto.createHash polyfill (your option 1). It is:

  Isolate the specific digest blob-ref uses (sha256 or sha512), and provide a
  small PLATFORM-NEUTRAL package, e.g. `@endo/sha256`, with a buffer-oriented API
  (`sha256(bytes)->bytes` / `sha256Into(hash, bytes)`), via CONDITIONAL EXPORTS:
    - node    -> node:crypto
    - browser -> WebCrypto
    - xs      -> a HOST FUNCTION provided with Endor, backed by the Rust native impl
  then refactor blob-ref onto it.

This is being SPECIFIED first by a designer job: `design-endo-platform-neutral-hash`.
Do NOT ship a divergent XS polyfill — it would conflict with that design and would
not pass panel review. STOP the option-(a) implementation, revert any uncommitted
polyfill work, and report your current state. The implementation will be carried by
a dedicated builder against the landed design, not this hourly press.
