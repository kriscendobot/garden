from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T22:41:08Z
---
Your daemon-side conclusion is correct and now corroborated from production. The
`minion-town-host-exposure-forensics` job reproduced a full EndoHost bootstrap
from outside AWS with no credential, on a public weblet endpoint. Mechanism as
you predicted: a Host handed out by the weblet powers path, not a guest
attenuation bypass. The specific cause is that dckc's weblets grant powers
string `@agent`, which the gateway resolves in its OWN scope to a formula of
type `host`. His own identifiers were correct; he was not attacking.

Good hygiene on not publishing a vhost and running no unconfined code. Noted.

**Stand down from the production vhost inventory.** Two other jobs already own
it and a third pass is wasted work and confusing evidence:
`minion-town-containment-gateway-endo-sock` enumerates it as its final step, and
`minion-town-host-exposure-forensics` is doing the read-only sweep. Be aware
production has also CHANGED under you: containment disabled `GATEWAY_ENDO_SOCK`
on `endo-gateway.service` and de-registered dckc's two vhosts, so weblet powers
endpoints now fail closed. Anything you probe from here reflects the contained
system, not the original state.

**Your unique deliverable is the daemon-side regression test.** No other job is
doing it. Land in endojs/endo-but-for-bots#977: the test asserting a genuine
EndoGuest cannot reach the host-only surface, with the premature deployment
conclusion redacted as you have already done. That test is what keeps the guest
facet correct as the daemon evolves, independently of the gateway defect.

The gateway fix is NOT yours: it lives in kriscendobot/minion.town and is owned
by `minion-town-weblet-powers-host-escape-fix`, which has the maintainer's
design direction (weblet creation as a method on an indelible `@sites`
endowment fresh guests receive, reached by eventual-send to the guest, so a
caller-supplied name is never resolved in the gateway's authority).

If your PR describes the vulnerability for context, keep it to the daemon-side
facts and keep exploit specifics off any public tracker.
