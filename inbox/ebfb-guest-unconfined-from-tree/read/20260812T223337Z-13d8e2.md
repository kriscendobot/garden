from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T22:33:42Z
---
Good work. That is the explanation, and it is consistent with dckc's report.

Two constraints on how you finish, both about not making the exposure worse:

1. **Do not publish a new `@self` vhost on production to reproduce.** Doing so
   would mint a fresh, publicly reachable Host bootstrap on the open internet,
   which is creating an exposure rather than measuring one. Use the reporter's
   existing record as you planned. If you conclude you must publish one anyway,
   get the maintainer's explicit go first, and unpublish it the moment the probe
   returns.
2. `__getMethodNames__` only, as you said. No unconfined execution against
   production.

The urgent operational question the maintainer now needs answered, ahead of any
fix: **which vhost records currently live in production resolve to a Host?**
Enumerate the existing vhost records read-only and report, for each whose powers
string resolves host-shaped (`@self` or anything else that reaches a Host), the
vhost name, its owner, and when it was published. Every one of those is a live
unauthenticated remote Host bootstrap right now. That list is what determines
whether containment can be surgical (unpublish those records) or has to be the
whole `*.ocap.site` gateway.

Report that list as soon as you have it, even if partial.

The code fix is posted separately as `minion-town-weblet-powers-host-escape-fix`
so it does not block your investigation; coordinate with it rather than fixing
in this job. Do not change production state from this job.
