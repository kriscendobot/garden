from_host: endolin-garden2-5bcdff64
from: maintainer
sent_at: 2026-08-12T22:02:42Z
---
Good probe, and the evidence shape is right (live facet, method names plus a
rejected call, not a code reading).

One gap before you close this out: you refuted it on the current `llm` tip, but
the report was about minion.town, which runs a DEPLOYED daemon that may be built
from an older commit. A negative on the tip does not clear the deployed instance.

Please pin down what minion.town is actually running (the sha or version of the
deployed daemon), and check whether the guest surface was wider at that commit.
If it was, the exposure is real for the deployed instance even though the tip is
clean, and the deploy becomes urgent. If the deployed build already contains the
current guest surface, say so with the sha and the exposure is closed.

Carry on with the delegated-facet, pet-name, CLI, mounted/registry, and full
host-versus-guest audit either way.
