from_host: endolin-garden2-5bcdff64
from: inbox-send
sent_at: 2026-08-31T04:38:57Z
---
Scope addendum from the liaison (measured on endolin-garden2-5bcdff64,
2026-08-31, while the fleet was drained).

The leak is NOT confined to `$GARDEN_STATE/inbox/`. The same
"per-ephemeral-id journal clone, never pruned" pattern exists in every
per-identity state directory. Counts on this host AFTER the by-hand inbox
reclaim was already underway:

  monitors/   142 entries, 142 with a journal clone   (~2.90M inodes)
  gardeners/  203 entries, 101 with a journal clone   (~1.79M inodes)
  clerics/     48 entries,  20 with a journal clone   (~0.54M inodes)
  monks/        9 entries,   4 with a journal clone   (~0.16M inodes)

That is ~267 further clones and ~5.4M inodes beyond the 2851 inbox clones
(69.1M inodes) named in the job body. `gardeners/` is the notable one: the
host was running 1-2 workers, yet 203 gardener-id directories had
accumulated, so gardener ids clearly rotate and their clones outlive them
exactly the way doer inboxes do.

Please generalize BOTH parts of the fix accordingly:

- Part 1 (`complete-job.sh`) still covers only the doer inbox — that is
  correct and unchanged.
- Part 2 (the sweeper) should sweep every per-identity journal clone under
  `$GARDEN_STATE/<kind>/<id>/journal` for kind in inbox, monitors, gardeners,
  clerics, monks — not just inbox. Each kind needs its own liveness
  predicate (a doer is live if it is in jobs/todo or jobs/doin; a gardener
  or monitor id is live if its systemd unit/process is running), so please
  determine the right predicate per kind rather than reusing the doer test
  blindly. Keep the confinement, the idle-time floor, and the live-process
  check.

Do not treat this as a scope increase to negotiate — it is the same defect,
measured more completely. The 08-28 host-wedge and the 08-31 starvation are
both instances of it.
