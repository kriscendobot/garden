from_host: endolinbot
from: inbox-send
sent_at: 2026-06-24T17:55:27Z
---
SCOPE EXTENSION from the maintainer (decided just now): the proxy is now the
SINGLE consolidated maintainer-reaction agent. Fold this into your build.

## Two response channels, by whether a gardener is still live

1. **Live gardener gating question** (a maintainer-inbox message whose `reply_to`
   doer is still live) → respond by **messaging that gardener's inbox** with a
   tentative, progress-favoring reply. (Already in your spec — keep it.)

2. **Completion report** (a `jobs/tada/<base>.md` report from a FINISHED doer —
   no live inbox) → respond by **posting jobs** (the gardener is gone; you cannot
   message it). For each report:
   - **Execute its explicit `## Follow-ups`** as one-time jobs (`post-job.sh`),
     recurring schedules (`set-schedule.sh`), or one-time future schedules
     (`set-schedule-once.sh`).
   - **Make progress-favoring judgment calls on open/undecided items** the report
     surfaces (the same proxy preferences: favor progress, tolerate throw-away
     work, tentative, explore a direction) — post jobs to carry them forward.
   - **Maintainer-authority / irreversible / scope items** (ferry, identity-switch,
     anything touching agoric-sdk, destructive ops, closing/merging where not
     already authorized) → do NOT act; post a note to the maintainer inbox per
     your boundary.

## Retire garden-follow-up (it is now redundant — the proxy subsumes it)

`garden-follow-up` already does the completion-report → jobs function (wearing the
liaison role). The maintainer chose to consolidate it into the proxy. As part of
this build, **retire it**:
- Delete `scripts/jobs/follow-up.sh`, `scripts/jobs/handlers/follow-up-claude.sh`,
  and `scripts/systemd/garden-follow-up.{service,timer}`.
- Remove its registration from `install-units.sh` (install path + `enable_services`
  + the summary log line), and ensure the timer is disabled/stopped in the enable
  path (or document the migration step).
- Move the documentation: remove the **"Autonomous follow-up surface"** section now
  in `roles/liaison/AGENT.md` and re-express it on the **proxy** role
  (`roles/proxy/AGENT.md`), adjusted for the consolidated proxy.
- **Reuse its proven machinery** in the proxy rather than reinventing: the
  `set-schedule-once.sh` one-shot scheduler it added, and its **cold-start
  seen-marker** for tada reports (mark all existing reports seen on first run so
  the proxy only acts on reports produced after it starts).

## What this means for your service

The proxy now watches BOTH surfaces each tick: the maintainer inbox (live
questions, past the grace window) AND `jobs/tada/` (new completion reports, via a
durable seen-marker in `GARDEN_STATE/proxy/`). Same `claude -p` proxy role, same
boundary, same "always report proxied actions to the maintainer inbox." Idempotent
job basenames derived from the source report + item identity so re-ticks never
duplicate. Update your tests to cover the completion-report path (explicit
follow-up → job; open item → progress-favoring job; authority item → deferred note;
idempotent on re-tick) and the garden-follow-up retirement.

If you have already largely finished the live-question-only version, that is fine —
incorporate this extension and the retirement before completing; if it is too large
to fold in cleanly, say so in your report and I will post a focused follow-on job.
