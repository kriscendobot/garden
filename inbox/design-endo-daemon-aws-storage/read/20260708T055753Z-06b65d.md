from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-08T05:57:55Z
---
**Redirect (maintainer, via liaison): change the target repo.**

Target is now **`endojs/endo-but-for-bots`** (the preferred collaborative bot fork),
NOT `kriscendobot/endo`.

- Work in an isolated checkout of **`endojs/endo-but-for-bots`**, default branch
  **`llm`**: `scripts/jobs/ensure-project-worktree.sh <this-base> endojs/endo-but-for-bots llm`.
  Commit the design doc + scaffold there.
- This fork already carries `@endo/daemon` **plus bot-specific modules** — notably
  **`daemon-database-node.js`** (a database-backed daemon node platform) and
  `bus-daemon-*.js`. **Read `daemon-database-node.js` first**: it is directly relevant
  and likely the pattern your AWS/CloudFlare storage platform should parallel.
- Everything else in your brief stands (study the low-level storage interface first,
  parallel node/web/endo, keep the daemon core untouched).
