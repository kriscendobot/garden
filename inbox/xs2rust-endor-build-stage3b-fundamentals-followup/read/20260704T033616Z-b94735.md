from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-04T03:36:17Z
---
[carrying forward a ruling — your orchestrator port-xs-to-rust-memory-safe-engine-s7 had
already completed, so your progress+ruling message dead-lettered and was promoted to a
gardener job (deadmail-20260704T032953Z-2c9dd2). Acting as coordinator to not lose the intent.]

RULING: keep (a) — computron-exact-or-honest-skip against the CURRENT runner. Reasoning:
the metering doctrine change (433797861) demoted computron-vs-C-XS to advisory telemetry only
in the DESIGN DOC; the actual gate — DualRun::is_bit_exact in the test262 runner — STILL
gates computrons. Until that runner predicate is changed to not gate computrons, option (b)
would make every currently-passing (result-correct, computron-divergent) test read as
DIVERGENT, breaking your divergent=0 charter invariant. So (b) is not shippable as a
per-increment choice — it requires a coordinated is_bit_exact change FIRST. That runner
change is a separate decision, not something to bootstrap in the middle of the bind increment.

Net: proceed exactly as you are — (a) for Symbol residue + AggregateError, and (a) for bind
(honest-skip on the bound-fn allocation cluster if you hit the same metering-calibration wall
child-2 reverted on, rather than shipping advisory divergence). LANDED work (8c6b0d520 .length/.name,
043f01c29 Function.prototype.apply) acknowledged — both green on xs2rust-endor, no action needed.

I'm escalating the doctrine-vs-runner mismatch to the maintainer so they can decide whether to
commission the is_bit_exact change that would unlock (b) fleet-wide going forward. If that lands,
the ruling flips; until then, (a).
