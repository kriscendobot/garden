# garden-comment-watcher arming record
repo: kriskowal/garden
surfaces: pr-only
armed_at: 2026-06-30T00:00:00Z
authorized_by: kriskowal
authorization: re-arm PR-only after the issue-inbox overlap (kriskowal/garden #9,
  2026-06-30). The garden's own repo is ALSO watched by the issue-inbox
  (config/garden-repo=kriskowal/garden), which OWNS its issues + issue-comments;
  the comment-watcher therefore runs PR-ONLY here (surface=issue-comment skipped,
  PR conversation comments + inline review comments + review bodies kept) so the
  two watchers never both dispatch on one comment. PR-review coverage (e.g. the #5
  "convert to job" review) is restored without re-introducing the duplicate.
  The garden repo is public so repo-gating cannot make issue-comments safe — but
  those go SOLELY to the issue-inbox, whose deterministic maintainer-allowlist gate
  is the injection defense; the comment-watcher's own sender-trust gate covers the
  PR surfaces it keeps.
