# Journal config/budget-pools. Both Anthropic pools are now CALIBRATED from the
# maintainer's account-specific Claude Code /usage figures; re-derive whenever a
# subscription or promo changes the weekly ceiling.
#   endolin-garden-ece02cb4  : 385M/wk, raised 2026-09-01 to match the sibling
#     pool. This is a DELIBERATE UNBLOCK, not a calibration: on 2026-09-01 the
#     meter read 120.1M/149M = 80.5% and the budget-level controller had throttled
#     that host's gardeners 2 -> 1, while Claude Code /usage on that host reported
#     0% of the week consumed. Those cannot both be right, and the host was being
#     held at half capacity on the disputed number. Job
#     `diagnose-budget-meter-overreport-ece02cb4` owns explaining the gap and
#     replacing this with a real calibration. Until then 385M restores capacity
#     while keeping a real 85% high-water (327M) rather than removing protection.
#     NB the two hosts are on DIFFERENT Anthropic accounts: /usage shows the +50%
#     weekly promo ending Aug 31 on garden2 and Sep 13 on ece02cb4, so parity here
#     is an interim convenience, not evidence the ceilings match.
#   endolin-garden2-5bcdff64 : 385M/wk, calibrated 2026-09-01. Derived from
#     /usage reporting 3% of the weekly limit consumed at a metered window spend
#     of 13,437,641 tokens => ~448M point estimate, band 385M-538M across the
#     display rounding of "3%". The LOW end is taken deliberately: it cannot
#     over-grant, and it is still 77x the 5M seed it replaced. Re-derive from a
#     second (spend, percent) sample for a tighter figure.
#   NOTE the bases differ: this meter counts input + output + cache_creation and
#   EXCLUDES cache_read (see usage-meter.sh), while /usage reports Anthropic's own
#   weighted limit. The calibration above is a ratio over one shared window, so it
#   holds only while the workload mix stays broadly similar.
# Columns, tab-separated:
# pool_id	provider	account/host	ceiling_kind	ceiling
anthropic:endolin-garden-ece02cb4	anthropic	endolin-garden-ece02cb4	weekly-tokens	385000000
anthropic:endolin-garden2-5bcdff64	anthropic	endolin-garden2-5bcdff64	weekly-tokens	385000000
