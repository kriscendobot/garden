# Garden designs

Meta-designs for the garden itself: architectural proposals for how the garden's roles, skills, journal, and host infrastructure should evolve. Each design is one Markdown file in this directory, named after the design's short slug.

Project-specific designs (e.g. for `endojs/endo-but-for-bots`) do not live here; they live under that project's upstream repo (typically `designs/<slug>.md` on a roadmap branch like `llm`). The garden's own infrastructure is the subject of this directory.

## Convention

Each design carries a metadata table at the top:

| Created | YYYY-MM-DD |
| Author  | <role-or-name> |
| Status  | Proposed \| Accepted \| Implemented \| Withdrawn |

Status semantics:

- **Proposed**: the design has a PR open against the garden and is under maintainer review. The PR is the discussion venue.
- **Accepted**: the maintainer has approved the design; implementation is in flight or queued. The merged design document is the canonical statement of the agreed approach.
- **Implemented**: the roles / skills / scripts described by the design exist on `main` and are in active use. The design document remains as historical context.
- **Withdrawn**: the design was opened but not adopted. The document remains as the record of what was considered and why it was not pursued.

## PR-against-garden exception

The garden's `CLAUDE.md` § Conventions states that the garden does not generally open pull requests against itself. Garden designs are the deliberate exception: a substantial architectural change is opened as a PR so the maintainer can review and comment in GitHub's PR interface, rather than landing directly on `main`. Smaller changes (single role edits, skill additions, notes-from-the-field rows) continue to land directly on `main` per the existing convention.

## Index

| Design | Status | Summary |
| --- | --- | --- |
| [dependabot-auto-merge.md](dependabot-auto-merge.md) | Implemented | A botanist MERGE-NOW explicitly auto-conducts a live Dependabot-authored PR on a bot-owned repo without a maintainer signature, while CI, CHANGES_REQUESTED, botanist diligence, repository scope, unfreeze/shared-stack, branch retention, and post-merge verification remain enforced. |
| [multibot-leader-follower.md](multibot-leader-follower.md) | Implemented | Gardeners run on every host; singletons run only on the leader host named by the journal `leader` marker, via the `GARDEN` knob and the `is-main-host` predicate. |
| [root-repo-guard.md](root-repo-guard.md) | Implemented | Harden the deployed root repo (`$GARDEN_ROOT/.git`, shared with the `journal/` worktree) against job git-escapes (2026-07-17/07-21: a HEAD moved onto a fixture `feature` branch; an origin rewritten to an unrelated project remote). Three layers: `GIT_CEILING_DIRECTORIES=$GARDEN_ROOT` in the worker spine so an un-inited dir FATALs instead of ascending; a "never run git in `$GARDEN_ROOT`" rule in every worker prompt; and an every-host `garden-root-repo-guard.timer` (~30m) that asserts + losslessly repairs the invariants (canonical origin from an escape-proof per-instance-clone source; HEAD detached at a `main2` ancestor, re-detached onto the recorded deploy point with a `root-guard-backup/*` ref), deferring while draining, plus a once-per-window stalled-deploy alert. |
| [gardener-bid-accept-market.md](gardener-bid-accept-market.md) | Proposed | Generalize the straight claim-race into a bid/accept market: gardeners differentiated by role and model, bid on jobs; a pluggable broker awards; a per-kind reputation ledger and the AMiX objective/subjective acceptance oracle feed selection. Race stays the default; market is opt-in, shadow-first, permanently dual-mode. |
| [gardener-reputation-bootstrapping.md](gardener-reputation-bootstrapping.md) | Proposed | Companion to the bid/accept market: effectiveness is the acceptance gate, cost is the free variable (notional dollars and duration, unified as cost per accepted job). Bootstrap reputation from journal `todo`/`tada` history plus synthetic replay; explore/exploit via Thompson sampling over per-arm cost posteriors; a reputation-driven role refiner mints fresh-prior bidders, bounded by a cap plus a consolidator (hierarchy as the follow-on). |
| [bot-email-dedicated-domain.md](bot-email-dedicated-domain.md) | Proposed | Counter-plan to FastMail masking: run bot email on a dedicated domain (`minion.town`) the bot fully controls. Compares AWS SES, Hetzner self-hosted, Cloudflare Email Routing, ImprovMX/ForwardEmail/Migadu, and Postmark/Mailgun on inbound/read-path/catch-all/outbound/deliverability/cost/ops for a receive-mostly bot. Recommends Cloudflare Email Routing (free catch-all inbound + Email Workers) with a managed sender deferred for rare outbound; carries the ToS/abuse and identity-blast-radius caveats. |
| [review-retrospective-loop.md](review-retrospective-loop.md) | Proposed | The double loop: every substantive PR comment also indicts the review process. The comment-watcher mints a second, deferred `-retro` job (deterministic verb-class gate, no LLM); a `prosecutor` role discriminates real review misses from new direction, records them with grounds in a journal `review-misses/` store, clusters them by a failure taxonomy mapped to juror seats, and past a floored subjective threshold dispatches a dual-goal `review-improve-*` job: prevent the error at the producing role AND make the panel catch it next time (seat-brief line plus panel-hints probe, or a deterministic gate). |
| [streamlined-onboarding.md](streamlined-onboarding.md) | Proposed | Collapse onboarding to clone → `./garden` → "help": the launcher auto-builds, seeds `.claude/settings.json` (SessionStart container-guard hook) like `.garden`, and execs an interactive `claude --dangerously-skip-permissions` liaison on enter (`./garden shell` is the escape hatch; CMD stays systemd). A first-class `help` verb drives an ask-before-acting first-run tutorial whose script *is* the new on-demand `context/` tree on `main2` (`first-run/` + `operations/`, context-library discipline), and a migration map moves the operational command detail out of README.md and CLAUDE.md § Job system into it. |
| [tada-token-accounting.md](tada-token-accounting.md) | Accepted | Deterministic per-engagement token-spend accounting: `gardener.sh` measures each handler invocation's billable tokens as a before/after delta over the job's own session JSONL (per-base worktree cwd → per-job `~/.claude/projects` dirs; handler-written `GARDEN_USAGE_FILE` primary, gardener-side snapshot fallback), appends one row per engagement to the journal-backed append-only ledger `usage/<base>.tsv` (host-additive, sum-on-read total), and `complete-job.sh` re-stamps a delimited machine footer onto the tada report on every doin→tada transition. No LLM in the capture/annotation path; fail-open everywhere — accounting can never block completion. |
| [token-cost-ledger.md](token-cost-ledger.md) | Accepted | Attributed per-job cost ledger, on unum's pattern (journal `library/` concept `cost-ledger`): widens the accepted tada-token-accounting record into a per-engagement JSONL CostRecord on `journal2` `usage/<base>.jsonl` (four token classes, CLI-computed notional dollars, wall-clock, rusage; tagged base/role/model/host/session/outcome), captured code-only from the `claude -p` result envelope with the session-log delta as fallback. Read-time-only aggregation (`cost.sh --by job\|role\|model\|day\|host`, dollars-descending), three surfaces (on-demand table, machine-stamped `## Cost` stanza on the tada report, leader-only bulletin chip), and the journal-as-shared-ledger answer to multi-host aggregation, which also feeds `usage-meter.sh`'s quota gate its missing multi-host sum (ledger measures, gate caps). |
| [provider-model-catalog.md](provider-model-catalog.md) | Reference | Cross-provider model catalog (Claude via `claude`, Codex via `codex`): concrete ids, tiers, per-model reasoning-effort support, context windows, rate-card prices, and the unified `(provider, model, thoughtfulness)` axis (`minimal < low < medium < high < xhigh < max < ultra`) that lets reputation arms be keyed uniformly across backends. Flags the Codex ChatGPT-plan no-per-token-dollar gap and the stale `sonnet` tier binding. |
| [cleric-worker-bid-auction-reputation.md](cleric-worker-bid-auction-reputation.md) | Proposed | The cleric (a codex-backed worker kind) on a single factored worker spine (kind registry, one loop, one unit-template source, handler contract extended with normalized usage output); a decentralized bid auction over the unchanged push-CAS (bounded bid window, per-bidder bid files, deterministic job-seeded Thompson award rank, staged eligibility degrading to the race — no central auctioneer, no double-award); reputation as journal data keyed `(kind, provider, model, thoughtfulness) × work_class × target`, valued in aggregate dollars (measured agentic tokens × rate card + inferred human review at ~$125/hr, measured signal swaps in without churn), the bid = expected aggregate dollars to merge-worthy. Gates the two build children of `orch-cleric-worker-system`. |
| [anthropic-worker-kind-monk.md](anthropic-worker-kind-monk.md) | Accepted | Reserves gardener for every generic job-doing agent and renames the Anthropic worker kind to monk, with a schema-aware legacy reader, no-overlap per-host unit cutover, dual reputation projections, explicit rollback gates, and a three-provider fleet acceptance test. |
| [transcript-journal-capture.md](transcript-journal-capture.md) | Proposed (reference spec; build owned by `supervise-transcript-durability`) | Durable fleet transcript archive plus fleet-wide deletion disable. Lean reference spec — storage decision made (dedicated `transcripts2` orphan branch, mirroring `journal2`; **not** committed to `journal2`). Every host spools finished session transcripts (a completion hook in `gardener-claude.sh` before its transcript `rm`, plus an hourly every-host `garden-transcript-capture` sweep of idle liaison/subagent/ad-hoc sessions), gzips and lightly redacts them, and CAS-pushes them to `transcripts2` on a configurable transcripts remote (`config/transcripts-remote`, inert until armed; a private repo is recommended because the origin is public). The same tick idempotently reconciles `cleanupPeriodDays: 36500` into gitignored `~/.claude/settings.json`, which the `garden` launcher also seeds for new instances. Adds bring-up arming surfacing and a liaison broadcast-reader (liaisons drain `role/liaison` + `broadcast` on bring-up — they read no broadcast today) so the durability rollout reaches every liaison. |
| [turnkey-garden-host.md](turnkey-garden-host.md) | Proposed | A credential-free, ARM64 EC2 garden-host AMI and launch-template path: private and smoke-tested first, then Marketplace only if the distribution/support commitment is wanted. Claude auth remains interactive; a bot PAT is interactive or fetched once from a narrowly scoped Secrets Manager secret. |
| [evaluation-epochs-panel-calibration.md](evaluation-epochs-panel-calibration.md) | Proposed | Applies RQGM's controlled-utility-evolution to the PR-review panel only: a versioned `journal/evaluation-epochs/pr-review.md` registry freezes the seat composition plus disposition rubric per epoch and tags every verdict with its epoch ID; a maintainer-curated, held-out anchor corpus (drawn from review-misses, CI escapes, and adjudicated findings) is the ground truth; a scheduled checkpoint scores challenger rubrics on conservative lower-bound recall (paired McNemar) with false-accept/reject, cost, and adversarial-resistance gates, and emits a proposal only. Promotion is maintainer-gated (no autonomous rubric self-mutation); on approval only open-PR verdicts materially dependent on changed seats or rules recompute. Phase 0 is registry plus epoch tagging (pure observability). |
| [watchdog-notice-dedup.md](watchdog-notice-dedup.md) | Implemented | One maintainer inbox entry per watchdog CONDITION instead of one message per occurrence: `alert_maintainer` (the sole `watchdog:*` writer) now delivers through the coalescing `watchdog-notice.sh` (`notice_count`/`first_seen`/`last_seen`, amend-in-place — `doom-notice.sh` generalized), counts throttle-suppressed occurrences so the count is real, re-keys a provider quota/usage-cap refusal to ONE fleet-level `provider-quota` notice carrying its reset time, and closes the loop with a recovery notice when the condition clears. Also classifies triager fetch failures (upstream-gone → immediate, with the disarm remedy; transient → quiet until it persists; other → immediate with git's own words) instead of paging identically for all three. |
| [omega-task-rank-and-foreman-retirement.md](omega-task-rank-and-foreman-retirement.md) | Proposed | Budget 5/5. Order dispatch by an omega rank (task's depth in a tree of plans; leaves worked first; a job too big for its window creates a plan and self-promotes its rank — the garden's orchestration jobs already half-implement this) and retire the foreman. Grounds the scheme honestly: NO omega/rank definition exists in the reachable library or live unum source, so the interpretation is flagged provisional and a grounding question is posted to jcorbin. Per child 4, human review dominates machine cost ~50–190×, so the rank's dominant term is human-review-blocked work (worked/surfaced first), not tokens. Isolates the foreman's objected-to half — the undirected, unmetered `claude -p` generative pump that mints top-of-tree work — from its still-needed half, the deterministic no-LLM rank-ordered promoter that is the ONLY place dispatch honors any rank (gardeners claim `todo/` in arbitrary id-offset order). Staged retirement: neuter the pump, extract a standalone `garden-promoter`, add an `omega:` field, order by it, formalize self-promotion on overrun; the 95 deferred jobs stay as the promoter's pool and `gate: deferred` survives. Stopping the unit needs a sysop `unit` op with maintainer attestation; changes no dispatch behavior. |
| [kimi-k3-takes-opus-work-with-opus-fallback.md](kimi-k3-takes-opus-work-with-opus-fallback.md) | Proposed | Let kimi-k3 take builder work away from opus for evaluation, gated on an automatic opus fallback. A per-instance journal flag `config/kimi-takes-opus-work` (default off) is the reversible enablement; when on, a mystic may claim a `role: builder` job pinned `model: kimi-k3` only if it carries a `fallback-model:` chain (safety net mechanically required); designer stays barred (graduated). On a genuine failure the reaper — the single requeue writer — advances `model:` to the chain head (opus claims the same base, fresh session + fresh worktree by construction), pops the burned model to `model-burned:`, resets the reap counter, and records the kimi arm `accepted: false` so the evaluation charges fallbacks to kimi. One hop, never a cycle. Kill switch: `set-kimi-fallback.sh off` (finer) or `set-mystics.sh 0` (blunt). Phase 2 (deferred): a completed-but-bad panel-rejection trigger, auto-stamping a share of builder jobs, and designer graduation; cost-ranking waits on `build-token-cost-ledger`. |
