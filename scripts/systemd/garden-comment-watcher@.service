[Unit]
Description=Garden PR/issue comment watcher for repo %i
# Producer: watch one gated repo's PR/issue comments and post jobs for verb-table
# directives + @-mentions. Driven by its .timer. Monitoring-safety gated: armed
# only for repos in the journal's comment-repos/ set (see comment-watcher.sh).

[Service]
Type=oneshot
# A systemd stop mid-tick SIGTERMs the run; a signalled shutdown is a CLEAN
# exit, not a failure. The self-heal wrapper already normalizes 143/130 to 0;
# this is the belt-and-suspenders so any future bypass of the wrapper still
# doesn't mark a SIGTERM stop as Failed.
SuccessExitStatus=143 130 SIGTERM SIGINT
# Graceful drain on stop/restart. The default KillMode=control-group would SIGTERM
# the WHOLE cgroup the instant systemd stops this unit — but a git credential
# helper forked by the source's `gh --paginate` may ignore/outrace that TERM and
# linger in the cgroup, so the next start flags "Found left-over process (git) in
# control group while starting unit". KillMode=mixed instead sends the initial
# SIGTERM ONLY to the main process (self-heal-run.sh, which forwards it to
# comment-watcher.sh); the watcher's EXIT/INT/TERM trap then reaps the source's
# whole process group (gh + every git child) deterministically — signalling the
# negated PGID and then WAITING for the group to drain so the cgroup is empty
# before the main process exits — and the cgroup-wide SIGKILL backstop at
# TimeoutStopSec catches anything the process-group reap cannot reach (e.g. a gh
# child placed in its OWN process group). Mirrors garden-gardener@.service
# (commit b3074e154).
KillMode=mixed
# Short stop timeout: the comment poll holds NO in-flight work worth preserving (a
# killed tick simply re-polls next firing — the cursor only advances on a verified
# post), so unlike garden-gardener@.service (TimeoutStopSec=2700, which waits for
# an in-flight job to finish) we do NOT wait minutes on stop. The 20:57:54 leak
# was a straggler git surviving the stop and lingering into the next start: the
# default 90s DefaultTimeoutStopSec let the cgroup-wide SIGKILL backstop fire only
# at ~90s, racing the 90s timer that fires the next instance. A SHORT bound makes
# the backstop SIGKILL any straggler the process-group reap missed WELL BEFORE the
# next firing. 20s is ample for the trap's TERM→drain (itself bounded by the
# source `timeout`'s 10s --kill-after) yet far below the 90s cadence.
#
# Every BLOCKING fetch a stop can land mid-flight must have a worst case
# (timeout + kill-after) that fits inside this 20s with margin. The main source
# fetch is backgrounded and trap-reaped, so its stop-drain is bounded by its 10s
# --kill-after (not its 180s timeout). The one FOREGROUND fetch — the throttled
# source-path self-test in comment-watcher.sh — cannot be trap-interrupted until
# it returns, so ITS budget is a hard floor on stop latency; it is capped at
# 10s+5s=15s (was 40s, which overran this 20s and SIGKILLed the stop at 09:40:35
# during a GitHub outage — the orphaned-git-in-cgroup failure KillMode=mixed
# exists to prevent).
TimeoutStopSec=20s
Environment=GARDEN_ROOT=@GARDEN_ROOT@
# Leader-only singleton: run only on the host whose GARDEN identity matches the
# journal `leader` marker. On a follower the timer still fires but this
# ExecCondition fails, so the tick is SKIPPED CLEANLY (condition-failed, never
# marked Failed) — that IS follower mode. Each firing re-evaluates, so a
# leader change (promotion/demotion) is picked up next tick with no restart.
# See issue kriskowal/garden#11 (Multibot) and designs/multibot-leader-follower.md.
ExecCondition=/bin/bash @GARDEN_ROOT@/scripts/jobs/is-main-host.sh
ExecStart=/bin/bash @GARDEN_ROOT@/scripts/jobs/self-heal-run.sh garden-comment-watcher@%i -- @GARDEN_ROOT@/scripts/jobs/comment-watcher.sh %i
# The self-heal responder runs synchronously on a failed tick before the
# wrapper returns the failing rc; give it headroom beyond the implicit 90s
# oneshot start timeout so a diagnosis is not cut off (responder itself is
# hard-bounded by SELF_HEAL_RESPONDER_TIMEOUT).
TimeoutStartSec=900
