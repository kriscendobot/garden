# Per-role job board: fixer

Per-role job board for the fixer.
Posted by drivers (after a barrister/justice verdict carries must-fix items, or after a maintainer's `CHANGES_REQUESTED`), claimed by fixer workers.

The job's frontmatter carries the per-action authorizations the fixer needs (inline review-thread replies, top-level summary citing each addressing SHA, re-request of the requested reviewer); these are forwarded from the producer per the dispatch-prompt-authorization contract in `<garden-root>/roles/COMMON.md`.

Phase 1 scaffolds the directory structure only.

See `<garden-root>/designs/driver.md` § Role-specific job boards.
