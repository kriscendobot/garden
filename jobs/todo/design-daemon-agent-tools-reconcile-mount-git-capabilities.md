---
role: designer
---

Design work on endojs/endo-but-for-bots: reconcile the stale `daemon-agent-tools` sketch (Not Started, its own 2026-05-18 revision note defers to the later mount/git designs) against the now-landed `daemon-mount-capabilities`, `daemon-git-capability`, and `daemon-git-remotes` designs, producing a buildable, phased spec for the endoclaw filesystem/shell/git agent-tool surface (`Dir`/`Shell`/`Git` capabilities scoped through mount descriptors) and correcting the design record's status, so a subsequent builder can deliver M3's "Claw-like coding capabilities available to agents" pillar.
