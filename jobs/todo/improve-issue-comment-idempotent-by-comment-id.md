Make the issue-comment dispatch path in `scripts/jobs/issue-inbox-watcher.sh` idempotent by GitHub comment id, matching the idempotency the `issue` path already has via `verify_posted "$spine"`. Today the issue-comment branch (around lines 304-319) calls `inbox-send.sh "$spine" "$mb"`, and `inbox-send.sh` always mints a *random* `msgid` (`msgid="$(date ...)-$(od .../dev/urandom)"`), so a re-poll of the same comment — coldstart, lost/reset cursor, or an `updated_at`-driven re-surface from the `repos/$REPO/issues/comments?since=` feed (issue-source-gh.sh line 82) — re-delivers it, producing a duplicate dead-letter, a duplicate `deadmail-<msgid>` promoted job, a duplicate gardener run, and a duplicate reply on the maintainer's issue thread. The source row already exposes a stable comment id (`cid`, field 3 of the issue-comment row); thread it through. Concretely: (1) teach `inbox-send.sh` to accept a caller-supplied deterministic message id via an env var (e.g. `GARDEN_MSG_ID`), falling back to the current random id when unset, so both the live-inbox filename and the dead-letter filename become `inbox/dead/<stable-id>.md`; (2) in issue-inbox-watcher.sh's issue-comment branch, pass `GARDEN_MSG_ID="issue-comment-<cid>"` so a re-poll dead-letters to the same path — `git add` of an identical path is a no-op and `deadmail.sh`'s promoted basename `deadmail-issue-comment-<cid>` is already idempotent by basename in `post-job.sh`; (3) for the live-inbox fast path, skip the write if `inbox/$doer/unread/<stable-id>.md` (or a read/processed marker) already exists, so a re-poll while the doer is still alive doesn't double-deliver. Add a run-test.sh subtest that polls the same comment twice and asserts exactly one promoted job / one delivery. Files: scripts/jobs/issue-inbox-watcher.sh, scripts/jobs/inbox-send.sh, scripts/jobs/test/run-test.sh.
