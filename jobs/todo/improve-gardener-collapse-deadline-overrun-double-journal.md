In `scripts/jobs/gardener.sh`, a handler killed at its own wall-clock budget (`deadline_overrun=1`) emits TWO progress entries: the generic transient note at line ~498 ("transient handler outage … no escalation") always fires, then the deadline-overrun note at line ~523 ("DETERMINISTIC deadline overrun … stamping for early poison") also fires. The first is misleading and is contradicted by the second (it says "no escalation" while the code proceeds to stamp an early-poison hint). Gate the generic transient `printf` (the block starting `printf 'gardener-%s on %s: job %s handler exited rc=%s (signal-kill/timeout/empty/transient-signature output)...'`) behind `if [ "${deadline_overrun:-0}" -ne 1 ]` so that a deadline overrun emits ONLY the accurate deadline-overrun entry, and every other transient path emits ONLY the generic entry (unchanged). Keep the `log` line (local, not journal). Net: one accurate journal entry per event instead of two contradictory ones. Update `scripts/jobs/test/handler-budget-test.sh` (and/or `timeout-classifier-test.sh`) to assert exactly one progress entry is produced on the deadline-overrun path.
