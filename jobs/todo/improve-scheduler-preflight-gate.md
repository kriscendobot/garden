Add an optional deterministic preflight gate to `scripts/jobs/scheduler.sh` so a recurring schedule can skip posting its job when a script proves there is no work — moving the idle/active decision off the dispatched agent into plain code. In `scheduler.sh`, parse an optional `preflight:` field from the schedule frontmatter (alongside `cadence:`/`last_dispatched:`/`job_basename_prefix:`); when present and the cadence has elapsed, run it (resolved relative to `scripts/jobs/`, passed the schedule name) inside the CAS loop. Exit 0 = work present → post job + stamp `last_dispatched` as today. Exit 2 = no work → stamp `last_dispatched` only (advance the clock, post nothing) and log a "preflight gated: no work" line. Any other exit = treat as work-present (fail open) so a broken gate never silently starves a schedule. Then add `scripts/jobs/scholar-preflight.sh` that returns 0 iff the scholar role inbox is non-empty, OR a claimable `scholar-*` job exists in `jobs/todo/`, OR a `role/scholar` broadcast is newer than the schedule's `last_dispatched`; else 2. Wire `preflight: scholar-preflight.sh` into `schedules/scholar-library-cycle.md` via `set-schedule.sh`. This eliminates the three-in-a-row (and growing) idle LLM scholar dispatches while preserving instant pickup when real work arrives, and gives every future schedule the same cheap gate. Reuse the deterministic standing-scan helpers in `scripts/jobs/common.sh` (`list_jobs`, clone/sync) rather than reimplementing board reads.
