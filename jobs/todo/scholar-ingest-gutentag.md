role: scholar

Ingest https://github.com/gutentags/gutentag and its component packages into the
cross-cutting reference library under journal/library/, per roles/scholar/AGENT.md.

Scope:
- Survey the repo shape (root README, package layout, source, docs) and ingest the
  primary source documents first pass.
- gutentag is a monorepo — ingest its component/sub packages as well (each package's
  README and any design/doc material). If the full set exceeds one cycle's budget
  (3-5 sources / ~25 section writes), file a faithful first pass and post a follow-on
  scholar-ingest-gutentag job naming exactly which packages/docs remain.
- This is a new domain relative to the endo-centric corpus: add new topics for it
  rather than bending the existing taxonomy (AGENT.md § Do not overfit).

No fork or upstream actions — library curation only.
