Harden `scripts/jobs/clone-keeper.sh` (`keep_clone`, line ~78) so a tracked clone that is missing / not a git repo is **restored** rather than warn-and-skipped forever. Today the endo bare clone (`worktrees/endojs-endo.git`, the default in `GARDEN_TRACKED_CLONES`) is absent on endolinbot2 and the keeper logs `WARN: tracked clone ... is missing ... skipping` and returns 0 every ~30m tick, so the clone never comes back and the library fleet's upstream-freshness guarantee silently fails — the exact 6-week-stale hazard the script's header warns about. On a missing clone, `git clone --bare` it from a URL derived from the `<owner>-<repo>.git` dir-name convention (`endojs-endo.git` → `https://github.com/endojs/endo.git`), honoring the existing special case that the endo `origin` carries no fetch refspec (set up the explicit refspec the header documents), then fall through to the normal fast-forward path. Keep every failure path returning 0 (self-contained, one bad clone never aborts the rest); if the clone/create itself fails, log and escalate once rather than silently re-warning each tick. Extend `scripts/jobs/test/clone-keeper-test.sh` with a missing-clone-gets-restored subtest.
