---
role: shepherd
---

Shepherd endojs/endo-but-for-bots PR #615 (`feat(daemon): Shell capability — provideShell + makeShellTool`, the M3 daemon-agent-tools phase-2 build) to green: its build landed but CI is red with two failing `lint` checks and a failing `test` check; drive them to green, escalating to a code fix if the failures are substantive.
