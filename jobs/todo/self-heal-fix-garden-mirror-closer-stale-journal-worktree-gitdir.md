Harden the standing-journal-worktree self-heal against a **stale worktree gitdir pointer** left behind by a garden relocation/deploy. Failure signature (garden-mirror-closer, exit 1, 2026-07-03 11:07:58): `fatal: not a git repository: /home/kris/garden2/.git/worktrees/journal` followed by `FATAL: no JOURNAL_REMOTE set and no origin on /home/kris/journal`. Root cause: `/home/kris/journal/.git` pointed at a prior checkout location (`/home/kris/garden2/.git/worktrees/journal`) that no longer exists after the root moved to `/home/kris`, so `journal_remote()` in `scripts/jobs/common.sh` (~lines 490-495) `die`d and every consumer that derives the remote from `$GARDEN_ROOT/journal` (mirror-closer here; also any board reader) failed until a manual `git worktree repair` fixed it at 11:08:42.

Two scoped changes:
1. `scripts/jobs/journal-worktree-keeper.sh` — in `keep_journal_worktree`, replace the do-nothing skip at lines 234-236. When `git -C "$JW" rev-parse --git-dir` fails but `$JW` exists as a directory, first attempt a repair: `git -C "$GARDEN_ROOT" worktree prune` then `git -C "$GARDEN_ROOT" worktree repair "$JW"` (idempotent; rewrites both the worktree `.git` gitdir pointer and the admin `gitdir` file to current absolute paths), then re-test `rev-parse --git-dir`. On success `log` the repair and continue the tick; only `log WARN … skipping` (and, if you page, `alert_maintainer` with a `journal-worktree-broken-$GARDEN` idempotency key) when the repair itself fails. Do not `worktree add` a fresh checkout — repair preserves the existing tree.
2. `scripts/jobs/common.sh` — make `journal_remote()` defensive so a momentarily-broken journal worktree cannot starve every producer/consumer before the keeper's next tick: when `git -C "$GARDEN_ROOT/journal" config --get remote.origin.url` fails, fall back to `git -C "$GARDEN_ROOT" config --get remote.origin.url` (the garden root repo shares the same origin) before the final `die`.

Cover with a test alongside `scripts/jobs/test/journal-worktree-keeper-test.sh`: construct a worktree whose `.git` gitdir points at a removed path, run the keeper, assert it repairs and reconciles rather than skipping.
