---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
`scripts/jobs/receipt-watcher.sh` lines 83-91: when the `( ensure_clone "$DIR"; sync_clone "$DIR" )` subshell exits non-zero with an **empty** `$PREREQ_ERR`, the subsequent `die "receipt journal prerequisite failed ... rc=$prereq_rc; see prerequisite stderr above"` is misleading — there is no stderr above, because the failure was a raw `set -e` exit from a bare command inside `ensure_clone`/`sync_clone` that never routed through `log()`/`die()` (every real `die()` call in those two functions logs to stderr first, so it would have appeared). This exact zero-content signature recurred twice on `garden-receipt-watcher@kriscendobot-endo-but-for-bots` within 3 hours (2026-09-21 22:22:54 and 2026-09-22 00:33:49), and both times self-healed on the next tick — but gave the self-heal responder nothing to diagnose. Fix: after the `sed` prerequisite-stderr dump, check whether `$PREREQ_ERR` was empty and if so append an explicit marker to the die message, e.g. `die "... rc=$prereq_rc; NO prerequisite stderr captured — likely a bare command failure under set -e inside ensure_clone/sync_clone with no log() call; re-run with bash -x to localize"`, so a recurrence is actually actionable instead of a dead end. Keep the fix scoped to this diagnostic message; do not change retry/classification behavior.
