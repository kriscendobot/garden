---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
scripts/jobs/receipt-watcher.sh: `GARDEN_RECEIPT_WATCH_CLONE` (line 45) defaults to the un-slugged `$GARDEN_STATE/receipt-watcher/journal`, shared by all `garden-receipt-watcher@<slug>` instances (16 currently: one per watched repo). Every instance runs `ensure_clone`+`sync_clone` against this one clone unconditionally on every ~5min tick (line 83), serializing on one `clone_lock` flock with only a 180s total wait budget (`GARDEN_LOCK_WAIT=60s` × `GARDEN_LOCK_RETRIES=3`). With 16 contending instances this routinely exceeds the budget, producing "FATAL: receipt journal prerequisite failed ... rc=1" restart-loops fleet-wide (63 FATALs across receipt-watcher instances in the last 2 hours vs. 0 for ci-watcher, 3 for comment-watcher, per `journalctl --user -u "garden-receipt-watcher@*"`). The clone at `$DIR` is read-only in this script (only `ls`/`grep`/`-d` reads against `$DIR/panel-runs`, `$DIR/jobs/*`, `$DIR/receipts/$slug`, confirmed via grep — no push ever happens through it), so there is no correctness reason to share it across repos. Fix: make the default `GARDEN_RECEIPT_WATCH_CLONE` per-slug, e.g. `$GARDEN_STATE/receipt-watcher/journal-$slug` (mirroring how other per-repo watcher state is namespaced), so each instance's read-only sync no longer contends with its 15 siblings for one lock. Update `scripts/jobs/test/receipt-watcher-test.sh` if it hardcodes the shared path.
