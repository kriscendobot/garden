---
role: designer
---

Designer job on endojs/endo-but-for-bots: redraft the `endoclaw-network-fetch` design (M3 "confined outbound HTTP" exit-criterion capability, currently Not Started with a stale 2026-03-03 daemon-formula shape) as an `@endo` confined-fetch plugin aligned with the just-landed `@endo/reminder` message-scheduler pattern (design PR #682, implementation PR #721), so the outbound-HTTP capability follows the maintainer's unconfined-plugin direction rather than the superseded daemon-formula approach.
