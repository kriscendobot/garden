---
role: builder
---

Build the `daemon-docker-self-hosting` design in endojs/endo-but-for-bots (plan/designs/endo-but-for-bots/daemon-docker-selfhost.md, status Not Started, M3): produce the Docker image bundling the daemon/worker/CLI with headless defaults, state-directory persistence across restarts, and remote-authenticated gateway network exposure (bearer-token auth already landed), delivering a mergeable draft PR that auto-runs the gauntlet.
