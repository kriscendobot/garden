repo: kriscendobot/garden
role: gardener
Fix the leader-only garden Mentor service so its inner-agent inference uses the garden standard availability-aware provider/model sequence instead of being hard-wired to Claude. Reuse or extract the proven Foreman provider-order machinery: try configured available Codex/OpenAI and other standard backends, fall through on missing credentials, quota, or transient provider failure, and retain Claude as a candidate when available. Preserve Mentor digest inspection, strict JOB block parsing, stable mentor:<path> identities, transient marker semantics, and fail closed on malformed semantic output rather than asking a second model to make a conflicting decision. Add focused regressions for Claude quota -> Codex success, unavailable-provider traversal, all providers unavailable, malformed output, injected test handlers, and no duplicate job posting. Update the operational provider-order documentation, land directly on main2 per garden convention, and report that a deliberate deploy is required.
