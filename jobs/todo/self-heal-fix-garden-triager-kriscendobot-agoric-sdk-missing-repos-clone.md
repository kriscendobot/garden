The armed `garden-triager@<slug>` units for the garden's own forks FATAL every tick with `no bare clone at $GARDEN_REPOS/<slug>.git (clone the repo first)` (`scripts/jobs/triager.sh:32`). Root cause: `scripts/jobs/fork-watch-provisioner.sh` is **not tracked on main2 / not deployed to the root checkout** — it lives only in scratch PR worktrees. A scratch gardener ran it and landed the 8 own-fork watch rows into the shared journal (`journal/repos/{kriscendobot-agoric-sdk,endo,cosgov,finbot,ocapn,vattr97,agoric-3-proposals,minion.town}`, armed_at 2026-07-10T06:01:51Z), so `repo-watcher.sh` armed 8 `garden-triager@<slug>` units — but the provisioner's leader-gated MATERIALIZE step (clone `worktrees/<owner>-<name>.git` → `$GARDEN_REPOS/<slug>.git`) never ran on this host, so none of the `repos/<slug>.git` clones exist (the `repos/` dir is absent). The standing clones DO exist under `worktrees/<slug>.git`.

Fix (deploy the provisioner so arming and materialize ship together, which is the invariant that was broken):
1. Land `scripts/jobs/fork-watch-provisioner.sh` and its test `scripts/jobs/test/fork-watch-provisioner-test.sh` into `main2` from the PR branch that authored it (e.g. `scratch/gardener-wt-kriscendobot-agoric-sdk-pr14-shepherd/scripts/jobs/fork-watch-provisioner.sh`; also see `designs/auto-provision-fork-watchers.md` and `config/fork-owners` seeding). Verify the DISCOVER + MATERIALIZE + escalate paths are the final reviewed version, and wire its invocation into the tick path its header names (top of the `repo-watcher.sh` reconcile / a dedicated leader-only timer) so it runs on the deployed leader, not only in scratch worktrees.
2. Confirm the MATERIALIZE step's clone source (`$GARDEN_FORK_CLONE_URL_BASE/<owner>/<name>.git`, atomic `mv -T` staging, `alert_maintainer` on unreachable) is sound, and that it (re)creates `repos/<slug>.git` for every armed own-fork slug. Prefer materializing from the already-present `worktrees/<slug>.git` where cheaper than a fresh network clone.
3. Deploy to the root via the normal drained `deploy-garden.sh`, then confirm all 8 `repos/<slug>.git` clones exist and `garden-triager@kriscendobot-agoric-sdk` (and its 7 siblings) reach a clean tick (`no change` / `triaged …`) instead of the `no bare clone` FATAL.

If deploying the full provisioner is out of scope for this fix, the minimum stopgap is to materialize the 8 missing `repos/<slug>.git` clones on the leader from the existing `worktrees/<slug>.git` shelf — but that leaves the arming/materialize split unrepaired and the next auto-armed fork will re-break, so the provisioner deploy is the durable fix.
