---
role: builder
---

Build the `daemon-docker-selfhost` design (endojs/endo-but-for-bots): a headless Docker image bundling the daemon, its bundled worker, and CLI with state-directory persistence and gateway network exposure accepting authenticated remote (non-localhost) connections per the Complete `gateway-bearer-token-auth` design — open a DRAFT PR. Design record: journal/plan/designs/endo-but-for-bots/daemon-docker-selfhost.md.
