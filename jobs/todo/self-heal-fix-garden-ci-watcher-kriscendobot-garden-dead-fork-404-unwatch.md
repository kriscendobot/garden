The ci-watcher (and previously the triager, twice) FATAL-flaps on `kriscendobot-garden` because the fork `kriscendobot/garden` no longer exists on GitHub (`gh api repos/kriscendobot/garden` → 404), yet a stale bare clone `worktrees/kriscendobot-garden.git` (origin `ssh://git@github.com/kriscendobot/garden.git`) causes `scripts/jobs/fork-watch-provisioner.sh` to auto-arm it into `comment-repos/kriscendobot-garden` and `repos/kriscendobot-garden`. All three per-repo watchers then target a 404 repo and die loud each tick. Do both, so the class closes rather than recurring on the next watcher:

1. **Durably unwatch the dead fork.** Add a journal tombstone `watch-optout/kriscendobot-garden` (the design's required durable-unwatch signal so the reconciler can't re-add it), and delete `comment-repos/kriscendobot-garden` and `repos/kriscendobot-garden` on `journal2` in one CAS-landed commit. Then remove the stale bare clone `worktrees/kriscendobot-garden.git` on this host (its upstream is gone). Reconcile the systemd units so `garden-ci-watcher@kriscendobot-garden`, `garden-comment-watcher@kriscendobot-garden`, and `garden-triager@kriscendobot-garden` are stopped/disabled.

2. **Harden the provisioner against dead-fork re-arming.** In `scripts/jobs/fork-watch-provisioner.sh`, before CAS-landing an own-fork arming record, add a cheap upstream existence check (`gh api repos/<owner>/<name>` — read-only, bot identity) and skip (and ideally auto-tombstone via `watch-optout/<slug>`) any bare clone whose upstream 404s. This prevents an orphaned/leftover bare clone of a deleted fork from re-arming watchers that will only ever FATAL. Verify with the provisioner's test harness that a 404-upstream clone is not armed, and that `endojs/endo-but-for-bots` plus a live bot fork still arm normally.
