`scripts/jobs/triager.sh` dies fatally (`triager.sh:32`, "no bare clone at .../repos/<slug>.git") because the bare-clone default `GARDEN_REPOS=$GARDEN_ROOT/repos` (`triager.sh:25`) points at the repo-watcher's watch-set config directory, not where standing bare clones actually live. The canonical location is `$GARDEN_ROOT/worktrees/<owner>-<name>.git` (see `common.sh:444` and `ensure-project-worktree.sh:63`), and the clone for this slug exists and is healthy there (`worktrees/kriscendobot-agoric-3-proposals.git`, HEAD=main). Fix: change the `GARDEN_REPOS` default in `triager.sh:25` from `$GARDEN_ROOT/repos` to `$GARDEN_ROOT/worktrees` so `BARE="$GARDEN_REPOS/$slug.git"` resolves to the real clone; apply the same default change in `comment-watcher.sh:179` for consistency (there it is non-fatal — it degrades to `gh` polling — but it is still pointing at the wrong dir and needlessly skipping the local clone). Do NOT touch the test harness, which passes `GARDEN_REPOS` explicitly (`comment-watcher-test.sh`), and keep `repos/`/`comment-repos/` as the watch-set config dirs. Verify by running `triager.sh kriscendobot-agoric-3-proposals` and confirming it fetches the worktrees bare clone and resolves `refs/remotes/origin/main` instead of dying. Consider whether the same `repos`→`worktrees` default is wrong anywhere else that reads `GARDEN_REPOS`.
