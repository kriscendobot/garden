# Botanist: refresh the board
Role: botanist. **Refresh the job board** per your board-tending role — reconcile board state, prune/close
stale or superseded jobs, re-surface anything wrongly stranded, and update the dependency/board views so
the board reflects reality. Report what you refreshed (jobs pruned, stranded jobs recovered, state
reconciled). Read `roles/botanist/AGENT.md` for the exact procedure.
