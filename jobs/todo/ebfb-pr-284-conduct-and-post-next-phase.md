# PR #284 — conduct (merge) the approved retention-paths Phase 1, then post the next-phase job

**Repo:** endojs/endo-but-for-bots (bot direct push; bot identity). **PR #284** —
*feat(daemon,cli): retention-paths Phase 1 (host API + endo paths CLI)* —
https://github.com/endojs/endo-but-for-bots/pull/284 — **APPROVED** by kriskowal
(review 4604615118, 2026-06-30T22:57Z), base **`llm`**, MERGEABLE/CLEAN.

**Maintainer directive (compound — execute BOTH, reliably; not a queued option):**
> Please conduct and post a job for the next phase of development as described.

1. **Conduct (merge)** #284 into `llm` (conductor role — do NOT name the merge method; the
   conductor's canonical norm chooses it). It is APPROVED and MERGEABLE/CLEAN. (A "CI green,
   ready for merge" status comment was already posted — proceed to the merge.)
2. **Post the next-phase job — "as described".** This is **retention-paths Phase 1**; read the
   PR description / the retention-paths design's roadmap for the **next phase (Phase 2)** as
   described there, and post a build job for it, grounded in that described scope. Deterministic
   basename (e.g. `ebfb-retention-paths-phase-2-...`).

**Report BOTH:** the merged SHA of #284 and the basename of the posted next-phase job.
Scope: bot fork; no upstream-of-endo contact.
