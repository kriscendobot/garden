`scripts/jobs/handlers/ci-rollup-gh.sh` line 42 runs `gh pr view … 2>/dev/null` and then reports only `exit 1` on failure. During the 2026-07-02 window this fired 354 times with rc=1 and no operator can tell whether the cause is a rate-limit, an expired token, or a network blip — exactly the swallowed-stderr trap the file's own header calls out ("the 2026-06-24 jq-outage lesson"). Change the call to capture stderr to a tempfile (or a var), and on failure emit the first line(s) of that stderr into the `log` message (e.g. `gh pr view $repo#$pr failed: <gh stderr> — cannot read CI state (skip, never guess)`) before `exit 1`. Keep the never-guess exit-1 contract; only make the *reason* visible so a systemic outage is diagnosable instead of 354 opaque rc=1s.
