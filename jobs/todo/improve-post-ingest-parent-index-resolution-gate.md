A library ingest (job `ingest-ocap-kernel`, commit `069d42b1`) committed 11 child section files, the source page, and the `sections/README.md` rows but silently dropped the parent `kind: index` section file `sections/metamask-ocap-kernel--docs-kernel-guide-md.md`, leaving both the source-page section table and the README `(index)` row pointing at a nonexistent file — a real dead-end that survived until a later scan happened to cover that axis (`090317Z-result-scholar-99178f92.md` § "Defect found and fixed"). Add a deterministic post-ingest integrity gate to the ingest job path so that, before an ingest is reported complete, every section-table target on each committed source page and every `sections/README.md` (index) row it touched is verified to resolve to a committed file; a missing parent-index (or any unresolved row) fails the ingest loudly at write time instead of lurking until a future integrity cycle. This complements the standing scan (improve-deterministic-section-link-integrity-scan) by catching the omission at the producer that created it rather than hours later downstream.
