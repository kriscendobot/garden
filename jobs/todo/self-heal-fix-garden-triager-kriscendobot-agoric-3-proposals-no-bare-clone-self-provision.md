scripts/jobs/triager.sh
Failure signature (every tick, exit 1): `FATAL: no bare clone at $GARDEN_REPOS/<slug>.git (clone the repo first)` from triager.sh:32, e.g. for slug `kriscendobot-agoric-3-proposals` (also affects `kriscendobot-cosgov`, `kriscendobot-ocapn`, and any watched repo whose clone this host has never held).

Root cause: triager.sh hard-`die`s when the bare clone is absent, but common.sh (§ standing bare-clone provisioning helpers) and clone-keeper.sh both document that triager is supposed to SELF-PROVISION a never-held clone with the shared `derive_clone_url` + `bounded_clone` helpers. That wiring was never added; the `die` at line 32 is the gap.

Two problems to fix together:

1. Wrong clone root. `GARDEN_REPOS` defaults to `$GARDEN_ROOT/repos`, which is empty — the canonical standing bare clones live under `worktrees/<owner>-<name>.git` (CLAUDE.md § Layout; clone-keeper.sh:121 tracked defaults; clone-keeper self-repairs clones there). Point triager's bare-clone lookup at the canonical `worktrees/` location (align the `GARDEN_REPOS` default to `$GARDEN_ROOT/worktrees`, matching clone-keeper) so triager finds and shares the clone clone-keeper already maintains, instead of pointing at an empty dir and provisioning a duplicate. Note comment-watcher.sh:179 shares the same `GARDEN_REPOS` default; keep the two consistent (comment-watcher already tolerates absence, so aligning it is safe and lets it reuse the real clone).

2. Self-provision instead of die. When the bare clone at the resolved canonical path is still missing (genuinely never cloned, e.g. agoric-3-proposals/cosgov/ocapn), replace the `die` with the clone-keeper self-provision path: `src="$(derive_clone_url "$BARE")"` then `bounded_clone "$src" "$BARE"`, logging a `provisioned missing clone` line, and `die` only if provisioning itself fails (mirror clone-keeper.sh:180-222). Verified: `derive_clone_url` on basename `kriscendobot-agoric-3-proposals.git` yields `https://github.com/kriscendobot/agoric-3-proposals.git` (first-`-` split → owner=kriscendobot, name=agoric-3-proposals), which is the real fork.

Guardrails: keep the change scoped to triager.sh (plus the shared GARDEN_REPOS default consistency for comment-watcher). Do NOT alter the monitoring safety posture — repos are added to the watch set (`repos/`) only under the existing maintainer-authorization rule; this fix only provisions clones for repos already in the set. Add/extend the triager test to cover (a) an existing clone under worktrees/ being found, and (b) a missing clone being self-provisioned via a stubbed `GARDEN_CLONE_URL_BASE`/bounded_clone (same override pattern clone-keeper's tests use) rather than dying.
