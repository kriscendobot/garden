Extend `scripts/jobs/clone-keeper.sh` to self-provision a missing tracked clone instead of warning-and-skipping forever. Add an optional 4th field (upstream URL) to the `GARDEN_TRACKED_CLONES` spec (`<dir>|<remote>|<branch>|<url>`) and set the default to include endo's URL (e.g. `worktrees/endojs-endo.git|origin|master|https://github.com/endojs/endo.git`). In `keep_clone()`, when `git -C "$abs" rev-parse --git-dir` fails and a URL is present, run a bounded `timeout "$GARDEN_FETCH_TIMEOUT" git clone -q --bare "$url" "$abs"` (mirroring `bounded_fetch`'s timeout/backoff/retry and reaper stuck-process reaping) to a temp dir + atomic rename, then fall through to the normal fast-forward path; log `provisioned <dir> from <url>`. Only WARN-and-skip when no URL is configured (preserving current behavior for URL-less entries). Add a case to `scripts/jobs/test/clone-keeper-test.sh` asserting a missing tracked clone with a configured URL is cloned bare on the next tick. This turns the keeper from "notice and skip forever" into "notice and heal," which is exactly its stated purpose, and removes the silent per-tick WARN on `endolinbot2`.
