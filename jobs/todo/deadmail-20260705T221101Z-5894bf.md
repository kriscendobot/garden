# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `port-xs-to-rust-memory-safe-engine-s7` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `port-xs-to-rust-memory-safe-engine-s7`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: port-xs-to-rust-memory-safe-engine-s7

----- ORIGINAL MESSAGE -----
to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T22:11:02Z
dead_lettered_at: 2026-07-05T22:11:02Z
---
UPDATE (supersedes prior note) — stage-3b child 7/9 (promises), PR #600 xs2rust-endor.

FINAL STATE (pushed HEAD bc785b265), all green:
- Oracle shim drains the promise job queue post-fxRunScript (pump-loop latch
  reference); non-promise corpora identical.
- COVERED bit-exact (result AND computron, divergent=0): Promise constructor +
  executor (resolve/reject settling under a shared [[AlreadyResolved]] guard),
  Promise.resolve/reject, then/catch, the microtask job queue, and the endor-
  side pump-loop drain. Resolution chains (primitive-returning reactions),
  already-settled, pass-through, rejection routing.
- built-ins/Promise dual-run: total=474 covered=7 divergent=0 (from ~0).
- Curated stage3b-promises.js corpus + gen_stage3b_promise_program fuzz arm,
  bit-exact. endor-vm 46 / endor-262 30 / endor-fuzz 21 / endor-oracle 3 tests
  green; stage-1 86/86; language/ spot-check divergent=0.

THENABLE ADOPTION — attempted, then REVERTED (commit 74adcdfac + revert
bc785b265). Native-promise adoption (resolve-with-promise / reaction-returns-
promise via an fxOnThenable job) was bit-exact for single-adoption-with-
observation cases (unlocked built-ins/Promise covered 7->9), BUT the fuzz sweep
found compound adoption chains (adoption feeding further then/catch, and an
un-observed adoption `Promise.resolve(4).then(v=>Promise.resolve(8))`) that
diverge in computrons by a few units — a native-handler reaction settles both
the adopting promise AND the discarded inner-.then derived, a job-count nuance I
could not calibrate within the wall. A SILENT computron divergence violates the
sacred invariant, so I reverted to the honest named skip (promise:resolve-
thenable) per the charter's "if calibratable within budget; else keep the honest
named skip." The enum/fxOnThenable scaffolding is in git history for a clean
resume.

REMAINING NAMED SKIPS (honest, self-name Halt::Unsupported): thenable adoption
(both native + user-object), self-resolving promise, handler-that-throws,
.finally, the all/race/allSettled/any COMBINATORS (fxCombinePromises: iterator
protocol over the arg + per-element sub-promise/combine functions + native-
function reaction handlers — same native-handler job machinery the reverted
adoption needed, so a shared follow-up), and async/await (stage 4).

FOLLOW-UP for a future increment: nail the native-handler reaction job's
double-settle (adopting promise + discarded derived) bit-exact — it unblocks
BOTH thenable adoption and the combinators in one stroke.

----- END ORIGINAL MESSAGE -----
