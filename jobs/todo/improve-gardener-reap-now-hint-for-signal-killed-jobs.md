In `scripts/jobs/gardener.sh`, the transient branch (around lines 206–218, where a signal-kill/empty/transient-signature handler exit is classified `transient=1` and the job is "left in doin for TTL requeue") takes no action to shorten the requeue — the job waits up to `GARDEN_CLAIM_TTL` (3600s) for `reaper.sh` to notice its `claimed_at` is stale, even though the gardener knows at exit time the claim is dead. The 2026-06-27 23:03Z double `rc=143` on the two Wayback-fetch scholar jobs (`scholar-ingest-source-combex-capdesk-polaris-primaries`, `scholar-ingest-source-hpl-techreports-polaris`) is the live instance: both died ~4 min into a 1-hour TTL and will idle ~56 min before any retry.

Change: when (and only when) `transient=1`, have the gardener emit a deterministic "reap-now" hint on the `doin` claim — preferred shape is a marker the reaper honors (e.g. `<!-- garden-reap-now -->` in the claim body, or back-date `claimed_at` so `age ≥ GARDEN_CLAIM_TTL`), so `reaper.sh`'s stale-set detection (around lines 172–180) requeues it on its next ≤10-min tick instead of after the full hour. Keep the **reaper as the single writer** of the requeue and the `<!-- garden-reaped: N -->` poison-cycle counter — do NOT have the gardener requeue `doin→todo` directly, or it would bypass `GARDEN_REAP_POISON_THRESHOLD` (5) and let a transient-looking poison loop (a genuinely wedged Wayback fetch that is SIGTERM'd every cycle — the exact risk the gardener.sh comment near line 214 already flags) requeue forever without ever escalating. The reap-now hint must still flow through the poison counter so such a job surfaces to the maintainer inbox after the threshold. Leave the deterministic-real-failure branch (non-signal rc) on the existing reaper-TTL path unchanged. Add a case to `scripts/jobs/test/signal-kill-classifier-test.sh` asserting that a signal-kill exit stamps the reap-now hint and that the reaper requeues a hinted claim before TTL while still incrementing the poison cycle.
