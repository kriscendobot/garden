---
role: builder
---

Run the gauntlet on endojs/endo-but-for-bots PR #655 ("feat(daemon): mount grep (PR C of #127)"): clean → panel review → fix-loop → un-draft. It is the next rung of the #127 daemon-mount split, stacked on the already-un-drafted glob base (#653) and CI-green from its shepherd pass, so it is unblocked and advances the split toward its final JSON rung (#657, which is blocked on it).
