---
role: shepherd
---

Shepherd `endojs/endo-but-for-bots` PR #713 ("feat(daemon): EndoMount.glorp — fused glob+grep search extension, G′ of #127", daemon-mount / M3) to green: its `lint` check is failing (run 29199437573) while the full Node 22.x/24.x matrix passes; drive the CI-to-green loop so the ready-for-review PR becomes mergeable.
