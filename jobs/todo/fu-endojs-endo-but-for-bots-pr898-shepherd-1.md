---
tier: minion
model-burned: mentor
fallback-tier: 
dispatch: automatic
---
endojs/endo-but-for-bots: the `/* global setTimeout */` lint-fix directive that PR #898 needed also exists as a gap on `upstream/llm` itself, so `llm` is red on lint by default. Land the equivalent fix directly on `llm`.

<!-- garden-reaped: 0 -->
