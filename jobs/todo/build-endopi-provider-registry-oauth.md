---
role: builder
---

Build the `endopi-provider-registry-and-oauth` design (M3) in endojs/endo-but-for-bots as a DRAFT PR, scoped to the genuinely-missing, unblocked subscription-OAuth path — the authorization-code-with-PKCE flow and per-provider encrypted auth-storage exo described in the design — and explicitly leaving the Lal-vs-Genie registry-consolidation policy question out of scope.
