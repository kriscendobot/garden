In `/home/kris/scripts/jobs/handlers/proxy-claude.sh`, the `answer_question()` helper aborts with `printf: - : invalid option` (line 56) because three `printf` format strings begin with a literal `- `, which bash's `printf` builtin treats as an option flag. This makes the proxy handler FATAL (exit 1) on every proxied question. Fix lines 56–58 so the dash is not parsed as an option — either guard each with the end-of-options marker, e.g. `printf -- '- gardener: %s\n' "$doer"` (and likewise for `'- question (msgid %s)\n' "$msgid"` and `'- tentative answer: %s\n' "$reply"`), or move the literal text into the argument: `printf '%s\n' "- gardener: $doer"`. Prefer the latter for the lines with no format conversions, the former where `%s` is needed. After the fix, verify by running the handler down the `answer_question` path (or a `printf` smoke test) so the report block renders the three bullet lines instead of dying. Scope is these three lines only; no other handler has a leading-dash format.
