On endojs/endo-but-for-bots PR #397 (branch `design/gateway-package-phase-9`), dispatch a fixer to land the three inherited breakages directly on the base: the makeClient stale import, the bytes SECURITY.md drift, and the composite-tsconfig drift, so the base's own CI goes green.
