---
tier: mentor
handler-timeout: 14000
---
<!-- garden-promoted-from-plan: gate=orchestrated priority=normal at=2026-08-14T22:43:05Z cleared=none -->

---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
handler-timeout: 14000
# with-cluster child A (FOUNDATIONAL): the environment-chain model + real `with (obj)` statement

This is the foundational child; every sibling depends on it landing first. Implement the XS environment-chain model in ironhorse-vm and make the real `with (expression) statement` execute against the XS oracle.

Scope: the `language/statements/with` subtree (~140 in-scope `unsupported-opcode:with` cases in the slice). Deliver that subtree fully COVERED with exact metering.

Do:
- Add a per-frame `mxEnvironment` register + environment chain to the interpreter (an env instance is a real slot object; save/restore the head across call entry/return exactly as XS resets it at frame setup, xsRun.c ~L856).
- Implement XS_CODE_WITH and XS_CODE_WITHOUT (see MECHANISM CONTEXT). WITH must leave the env-instance reference on the stack (so the compiled POP balances it, and so a later STORE — child B — can target it), allocate exactly 2 slots (meter 2*SLOT_ALLOCATION_METERING) and wire behavior-slot kind/value from the top-of-stack `with` value and prototype from the prior head.
- Implement the env-walk in XS_CODE_EVAL_REFERENCE and XS_CODE_PROGRAM_REFERENCE for OBJECT environments, including fxIsScopableSlot (HasProperty + @@unscopables). Make GET_VARIABLE, GET_THIS_VARIABLE, and SET_VARIABLE resolve against the object the reference op selected, falling through to today's resolve_get/resolve_set when the chain does not bind the name.
- Keep the empty-chain path byte-identical (no baseline regression).
- Calibrate EVAL_REFERENCE scopable-walk and GET/SET-VARIABLE property metering exactly against the oracle on the with slice.

Add focused Rust tests under rust/engine/ironhorse-262/tests/ for: with(obj) read/write, @@unscopables blocking, shadowing/outer-scope fallthrough, nested with, with over a primitive-box boundary (defer the primitive-box ToObject to its existing owner if out of scope — do NOT regress to_instance:primitive-box), and env-instance allocation metering.

COMMON (every child): Repository endojs/endo-but-for-bots. Continue the EXISTING shared branch feat/ironhorse-262-language-completion and draft PR https://github.com/endojs/endo-but-for-bots/pull/970 — keep it OPEN and DRAFT, do NOT merge. Use an ISOLATED checkout keyed by THIS child's base (scripts/jobs/ensure-project-worktree.sh <child-base> endojs/endo-but-for-bots feat/ironhorse-262-language-completion), fetch+rebase the latest shared head, PRESERVE every prior commit, stack bounded commits, and CAS-push (git push origin HEAD:feat/ironhorse-262-language-completion) with a rebase loop. Pins: engine head measured b3c3ae93b81c0f266458cd25f2f27d94ef80cc5b; test262 tc39/test262@be13516fb6441b950ba8a3df97eb34062c186972; Moddable XS oracle 23b4d6b0a65f35209d9118c4c13c6c9b3e68784d (git submodule update --init --depth 1 c/moddable). Rust: prepend $HOME/.cargo/bin to PATH; set TMPDIR off any noexec mount. Acceptance bar (non-negotiable, identical to the js-XX arc): convert the child's cases from unsupported/failure to COVERED via REAL execution against the official XS differential oracle (rust/engine/ironhorse-262/scripts/full-run.sh --subtree <PREFIX> --test262-dir <pinned checkout>), except a specifically justified standards-grounded host-only/proposal exclusion. Add focused Rust unit/regression tests under rust/engine/ironhorse-262/tests/. Do NOT relabel, suppress, skip-list, or add expectation files. Zero generic ironhorse-aborted / parse-or-decode / unsupported-opcode:* / abort-value-differs / non-primitive-completion reasons may remain in the child's scope. Regression invariant: no baseline-covered case (rust/engine/ironhorse-262/baseline/baseline.json) or earlier-child case may regress; no new ironhorse-failure/infrastructure result; every exact-metering case under rust/engine/ironhorse-262/cases/** stays passing with its computron expectation unchanged. Before EVERY push run: the affected official slice, cargo test --workspace --release, and ironhorse-xst --gate-meter-exact. Report commands, before/after totals+reasons for the slice, head SHA, PR URL. If gates fail, signal orchestration failure honestly rather than papering over.

MECHANISM CONTEXT (shared finding from the cb-with-statement analysis): The parser already parses `with` (ironhorse-compile/src/parser/stmt.rs) and strict `with` is already a parse SyntaxError; the coder already EMITS XS_CODE_WITH/XS_CODE_WITHOUT (ironhorse-compile/src/coder.rs: code_with ~L1489, scope_coding_body_eval ~L4007, params-eval branch ~L3514). The gap is the VM: ironhorse-vm/src/interp.rs has NO handler for XS_CODE_WITH/WITHOUT, so they fall to the default Halt::Unsupported(op.name()) -> reason "unsupported-opcode:with" (the entire 949-case cluster). XS semantics to port (c/moddable/xs/sources/xsRun.c): XS_CODE_WITH (~L4429) sets variable=mxEnvironment; slot=fxNewEnvironmentInstance(the,variable); mxEnvironment=slot. fxNewEnvironmentInstance (xsType.c:1156) allocates TWO slots (instance + behavior slot ID=XS_ENVIRONMENT_BEHAVIOR whose kind/value = top-of-stack `with` value: a Reference for with(obj), NULL/UNDEFINED for the eval prelude), prototype=prior env, and REPLACES top-of-stack with the env-instance reference (the following POP removes it). XS_CODE_WITHOUT (~L4438) pops mxEnvironment to its prototype. Name resolution: XS_CODE_EVAL_REFERENCE (~L4346) / PROGRAM_REFERENCE walk the mxEnvironment prototype chain; for an object env it calls fxIsScopableSlot (xsRun.c:4872 = HasProperty(obj,id) AND NOT (obj[@@unscopables]?.id is truthy)) and on success pushes the object reference; for a closure env it calls mxBehaviorHasProperty on the env instance; the fallthrough pushes the realm global. GET_VARIABLE/GET_THIS_VARIABLE (xsRun.c:2331) and SET_VARIABLE (2514) then do an ordinary property get/set on whatever object the *_REFERENCE resolved. Metering: each WITH = one dispatch + 2 x SLOT_ALLOCATION_METERING (2*256 raw); the EVAL_REFERENCE scopable-walk cost and GET/SET-VARIABLE property costs MUST be calibrated exactly against the oracle. In ironhorse-vm today, EVAL_REFERENCE pushes a sentinel (Kind::EnvReference) and GET_VARIABLE/SET_VARIABLE ignore it and call resolve_get/resolve_set over id_map/global_props; STORE (store_closure, ~L24764) already appends a closure cell to a top-of-stack Reference environment. Keep the EMPTY-with-chain path byte-identical to today so the large covered baseline never regresses — make the env-chain resolution additive (consulted only when a with/eval environment is active).
