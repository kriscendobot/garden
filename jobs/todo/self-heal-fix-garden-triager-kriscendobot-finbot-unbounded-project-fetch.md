Bound the steady-state project-clone fetch in `scripts/jobs/triager.sh:117`. The line `git --git-dir="$BARE" fetch -q --all --prune || die "fetch failed for $slug"` is the fleet's only unbounded `git fetch` — a half-open GitHub connection hangs it forever until an external SIGTERM reaps it, producing the observed `Terminated` + `FATAL: fetch failed for kriscendobot-finbot` (exit 1) on a mere network blip (the repo is reachable; the same fetch succeeds on retry). Wrap it in the same `timeout --kill-after="$GARDEN_FETCH_KILL_AFTER" "$GARDEN_FETCH_TIMEOUT"` discipline every other fetch uses (see `_journal_git_fetch` / `bounded_clone` in `common.sh`, and the `GARDEN_FETCH_TIMEOUT`/`GARDEN_FETCH_KILL_AFTER`/`GARDEN_FETCH_RETRIES` knobs it already sources), ideally with the bounded retry loop so a transient stall (timeout exit 124/137) retries a couple of times before it `die`s, rather than hard-failing the whole service on the first blip. Keep the existing `die "fetch failed for $slug"` as the terminal outcome only after retries are exhausted.
