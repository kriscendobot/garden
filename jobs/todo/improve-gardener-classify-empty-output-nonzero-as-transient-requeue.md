Both failed jobs (improve-classify-offline-as-tempfail-in-journal-fetch, improve-self-heal-treat-offline-as-clean-exit) failed identically with empty output seconds apart, the signature of a transient `claude -p` outage (API overload / rate-limit / 5xx), not a deterministic job defect. Currently scripts/jobs/gardener.sh:75-103 records every such case as a `kind: error` journal entry and leaves the job in doin, where the reaper requeues it after GARDEN_CLAIM_TTL — but it does so via the loud FAILED/escalation path, spamming the gardener inbox and error journal for a self-resolving blip. Mirror the offline→tempfail classification just added to scripts/jobs/self-heal-run.sh (the GARDEN_OFFLINE_RC=75 / connectivity-signature short-circuit): in gardener.sh, when the handler exits non-zero AND the capture is empty AND the handler tail matches a transient-claude signature (e.g. `overloaded`, `rate limit`, `Connection error`, `5\d\d`, or a bare empty-output-nonzero from gardener-claude.sh), emit a single `kind: progress` "transient handler outage; left in doin for TTL requeue" note instead of a `kind: error` escalation, and skip the inbox append. Reserve the error/escalation path for handlers that fail with real diagnostic output. This depends on improve-gardener-fold-report-and-rc-into-failure-capture landing first so the rc and report tail are available to classify on.
