Add `scripts/jobs/check-source-children.sh <hub-url-or-mirror-path>` (with a matching `scripts/jobs/test/check-source-children-test.sh`) that moves the per-cycle manual reachability probe off the scholar agent into deterministic code. It should: (1) fetch the hub/nav page via the existing `scripts/jobs/fetch-source.sh` fetch+mirror+Wayback-`id_` fallback path; (2) extract the child hrefs from the hub's nav map; (3) probe each child through the same `fetch-source.sh` logic; and (4) emit a one-line-per-child manifest classifying each as `reachable` (mirror or wayback) or `dead` (404 on both mirror and Archive, www/no-www tried), so a scholar gets a deterministic reachable/dead list instead of hand-probing each link. Why: the two scholar results in this capture each re-derive this probe in prose ("verify reachability via fetch-source.sh before planning sections"), and a missed probe wastes a whole ingest cycle chasing a never-written page (`elang/guarding/style.html`); making the manifest a scripted precondition removes that failure mode and the recurring "verify reachability" caution hand-carried in every follow-on job body. Cite the new script from `roles/scholar/AGENT.md` (the reachability step) and `journal/library/conventions.md` § source acquisition the same way `fetch-source.sh` is cited, so the scholar reads the script path in its always-read procedure docs rather than re-deriving the recipe.
