Harden the standing bare-clone fetch in `scripts/jobs/triager.sh:117`. Today it is a raw `git --git-dir="$BARE" fetch -q --all --prune || die "fetch failed for $slug"` with no timeout wrapper and no offline classification, so a half-open/stalled fetch that is SIGTERM-reaped (the captured tail: `Terminated` immediately followed by `FATAL: fetch failed for kriscendobot-ymax-e2e`, exit 1 via `die()` at `common.sh:285`) crash-fails the `garden-triager@` unit instead of skipping the tick.

Bring this fetch under the same bounded discipline the journal path already uses (`journal_fetch`/`sync_clone` at `common.sh:1662`/`:2483`, and `bounded_clone` at `:717`): wrap it in `timeout --kill-after="$GARDEN_FETCH_KILL_AFTER" "$GARDEN_FETCH_TIMEOUT"`, capture stderr, and on a wall-clock kill (rc 124/137) or a stderr matching `_fetch_stderr_is_offline` (`common.sh:1722`), log and clean-skip the tick (`exit "$GARDEN_OFFLINE_RC"`, mirroring `sync_clone`) rather than `die`. Reserve the hard `die "fetch failed for $slug"` for a genuinely non-transient fetch error. Prefer factoring a small reusable bounded-bare-fetch helper in `common.sh` over inlining, since the same raw pattern may exist in sibling watchers (grep `fetch -q --all --prune` / `fetch --all` under `scripts/jobs/`). Keep it a net behavior change only on the transient path; the success path and cursor/diff logic below line 117 stay identical.
