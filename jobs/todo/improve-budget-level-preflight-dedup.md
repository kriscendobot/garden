---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
scripts/jobs/budget-level.sh
Persist a preflight-error fingerprint and edge-latch the missing/invalid physical-cap freeze: alert only when the failure begins or changes, then emit one recovery notice when valid configuration returns. The same `oros-studio-garden-ce242c49` configuration failure is warning every leveling tick.
