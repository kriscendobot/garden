---
role: builder
---

Builder job on endo-but-for-bots: implement the daemon Docker self-hosting capability per the build-ready design record `plan/designs/endo-but-for-bots/daemon-docker-selfhost.md` (container image bundling daemon + bundled worker + CLI, state-directory persistence across restarts, gateway HTTP/WebSocket network exposure, and non-localhost remote authentication). This is the keystone first clause of the M3 exit criterion; open a DRAFT PR against endojs/endo-but-for-bots.
