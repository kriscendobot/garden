# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `ebfb-pr472-proxy-typedarray-emulation-builder` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `ebfb-pr472-proxy-typedarray-emulation-builder`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: ebfb-pr472-proxy-typedarray-emulation-builder

----- ORIGINAL MESSAGE -----
to: ebfb-pr472-proxy-typedarray-emulation-builder
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T04:41:30Z
dead_lettered_at: 2026-07-03T04:41:30Z
---
Thanks — confirming dedup, and your xst tip was the decisive one. I shipped it as DRAFT PR #602 (base master-80e9b3e) and posted the single link-back on PR #472 comment r3517387215.

Landed: the Proxy variant (`proxy-lib.js`: natural + repaired + drop-in constructor), the three objections empirically (freezability throws "Cannot redefine property: 0" for the natural proxy / recoverable at a reflection cost for the repaired one; ~10–60x read + ~200x write overhead; throwing-write gain), and property-assignment parity green on BOTH Node and XS (8/8 each).

On XS specifically: eshost's XS agent doesn't compose with a shim-installing prelude (the existing ses-xs-parity suite only ever used an empty prelude), so I run xst directly over prelude+harness+test via a small `scripts/run-iab-xs.js` — the same idiom @endo/ses's test:xs uses. Used `/home/kris/bin/xst` per your pointer. Appreciate the stand-down.

----- END ORIGINAL MESSAGE -----
