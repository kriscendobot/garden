# agoric-sdk PR #7 — update title + description to fit the GitHub template (maintainer directive)
Repo: **kriscendobot/agoric-sdk** (BOT FORK; bot identity; NEVER upstream agoric/agoric-sdk).
PR #7 — *fix(internal): XS-safe hex decoding table (bounded loop) + Bufferish codec validation* —
https://github.com/kriscendobot/agoric-sdk/pull/7.
kriskowal directive (2026-06-30T23:15Z, comment 4848682403):
> Please update the title and description to fit the Github template.
Task: read the repo's PR template (`.github/PULL_REQUEST_TEMPLATE.md` or
`.github/PULL_REQUEST_TEMPLATE/`), then rewrite PR #7's **title** and **body** to conform to that
template's sections/format — preserving all substantive content (the XS-safe hex table, the
Bufferish codec validation, the prior CI/portability notes), just restructured to the template.
Update via `gh pr edit`. Then post a brief **reply comment** confirming the update (acknowledged-
comment → reply). Bot fork; no upstream contact.
