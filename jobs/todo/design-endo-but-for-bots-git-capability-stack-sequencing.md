---
role: designer
---

Reconcile the three Proposed M3 git-capability designs in `endojs/endo-but-for-bots` (`daemon-git-capability`, `daemon-git-next-steps`, `daemon-git-remotes`) into one accepted, dependency-ordered phased build plan for the agent version-controlled-filesystem loop, reconciling against the already-landed agent-tools phase-3 git tools and the `EndoMount` substrate, and open a draft design PR against `llm` — the same sequencing pattern that just unblocked the module-loading stack, so builders have ready phases while the module-loading Phase-2 build finishes.
