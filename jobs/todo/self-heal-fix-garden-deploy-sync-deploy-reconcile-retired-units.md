`scripts/jobs/deploy-garden.sh` advances the root checkout to origin/main2 and restarts the fleet but never reconciles systemd units, so retirements landed by a deploy (here: `garden-deploy-sync.{service,timer}`, whose `deploy-sync.sh` was deleted in fe2d9f296 and which are listed in `RETIRED_UNITS` in `scripts/jobs/install-units.sh`) stay `enabled`+`active` on the deployed host and fire every timer tick, exec'ing the missing `deploy-sync.sh` → rc 127 (signature: `self-heal-run.sh: line 95: /home/kris/scripts/jobs/deploy-sync.sh: No such file or directory`). Fix two things: (1) Make `deploy-garden.sh` run `scripts/jobs/install-units.sh enable-services` (or at minimum the `RETIRED_UNITS` disable loop) as a post-merge step so a retirement landed by a deploy actually disables the stale unit on every host. (2) Strengthen the `RETIRED_UNITS` reconciliation in `install-units.sh enable_services()` so that after `unit_ctl disable --now "$u"` it also `rm -f`s the corresponding `~/.config/systemd/user/$u` files and runs `daemon-reload`, so a retired unit's files don't linger and can't be re-triggered. As part of executing this job, also run `install-units.sh enable-services` once on endolinbot to clear the currently-firing stale `garden-deploy-sync.timer`/`.service` and stop the rc-127 loop. Verify with `systemctl --user is-enabled garden-deploy-sync.timer` → not-found/disabled.
