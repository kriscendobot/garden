model: gpt-5.6-terra
role: fixer
Fix Kimi text-renderer completion-marker normalization in scripts/jobs/handlers/mystic-kimi.sh. Live Kimi Code 0.29.1 output renders the first report line as UTF-8 bullet+space but continuation lines, including the final completion marker, with two leading ASCII spaces; exact bytes observed: `20 20 3c3c3c...` plus trailing blank. The current normalizer only accepts bullet+space+marker, so a genuinely completed tool canary exits 0 without a sentinel. Normalize only the LAST nonblank line when it is the exact marker with renderer decoration limited to optional leading horizontal whitespace or one bullet plus horizontal whitespace; keep anchoring and exact marker text so mid-report mentions cannot forge completion. Add fixtures for the real two-space form, bullet form, trailing blanks, and rejection of embedded/suffixed markers. Run Mystic harness and completion tests, push main2 directly, report deploy readiness. Do not use live credentials or requeue canaries.
