Fix `scripts/jobs/triager.sh` so its per-repo tick stops dying with `FATAL: no bare clone at $GARDEN_ROOT/repos/<slug>.git` (triager.sh:32). Two compounding defects, both in the deployed root:

1. **Path mismatch.** `triager.sh:25` and `comment-watcher.sh:179` default `GARDEN_REPOS=$GARDEN_ROOT/repos`, but the garden's standing bare clones live under `worktrees/<owner>-<repo>.git` (see `clone-keeper.sh` header, `common.sh` clone-provisioning helpers, and `CLAUDE.md` § Layout). `repos/` does not exist on this host, so `$BARE` is never found and *every* triager instance (agoric-sdk, cosgov, endo, finbot, ocapn, …) fails, not just ocapn. Point the `GARDEN_REPOS` default at `$GARDEN_ROOT/worktrees` (align triager.sh and comment-watcher.sh, and their tests under `scripts/jobs/test/`).

2. **Incomplete self-provision refactor.** The uncommitted working diffs to `scripts/jobs/common.sh` and `scripts/jobs/clone-keeper.sh` add shared helpers `derive_clone_url` + `bounded_clone` (plus `is_own_git_repo`/`is_remote_location`) whose header comment states *"triager.sh self-provisions a watched repo whose clone this host has never held"* — but `triager.sh:32` still just `die`s. Convert it: when the standing bare clone is absent, self-provision via the shared helpers instead of dying, staging into a sibling temp path and atomic `mv -T` exactly as `clone-keeper.sh`/`bounded_clone` prescribe. Gate self-provision to repos the garden is authorized/tracked to watch (§ Monitoring safety constraint) — do NOT blindly clone an arbitrary derived URL.

3. **Reconcile the instance set.** `kriscendobot-ocapn`, `-cosgov`, and `-agoric-3-proposals` have triager instances enabled but are in neither `clone-keeper.sh`'s tracked-clone set nor (as far as this diagnosis can tell) the authorized watch set. Confirm whether each is meant to be watched; if not, disable those `garden-triager@<slug>.{service,timer}` instances rather than provisioning clones for them.

Verify by running the triager for a tracked repo (e.g. `worktrees/kriscendobot-vattr97.git` exists) end-to-end and confirming it resolves the bare clone, and by running the triager/comment-watcher test suites. Also note the deployed root is carrying uncommitted changes to `common.sh`/`clone-keeper.sh`; ensure the completed refactor is committed together so the deployed tree is clean.
