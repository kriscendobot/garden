Move journal2 *content-file* edit landing off agent judgment into a script, eliminating the manual-rebase-of-the-live-worktree hazard that bit the scholar on 2026-06-27 (`entries/2026/06/27/095956Z-result-scholar-e213f5.md`: rebasing the stale, WIP-laden `/home/kris/journal` worktree replayed upstream commits into a destructive conflict, requiring abort + re-land). Add `scripts/jobs/land-journal-edit.sh` that generalizes the producer-clone discipline already in `scripts/jobs/journal-entry.sh`: take a relative journal2 path under `library/` (or a small allowlist of editable trees) plus the new file body (from a body-file or stdin), then inside the isolated `${GARDEN_PRODUCER_CLONE:-$GARDEN_STATE/producer/journal}` clone run `sync_clone` (fetch + hard-reset to `origin/journal2` tip), write the file, `git add <path>`, and `commit_and_push` with the CAS retry/backoff loop and `_verify_pushed` silent-loss guard already in `scripts/jobs/common.sh`. It must refuse any path outside the allowlisted trees and must never operate on `$GARDEN_ROOT/journal` (the live worktree), mirroring the read-side guarantee documented in `library-link-check.sh`. Then point `roles/scholar/AGENT.md` and `roles/librarian/AGENT.md` (and `skills/journalism`/`skills/library-lookup`) at this script as the only sanctioned way to land a library content edit, replacing the manual `git` instructions that let an agent pick the live worktree. Add a test under `scripts/jobs/test/` (sibling to `library-link-check-test.sh`) pointing `JOURNAL_REMOTE` at a throwaway bare repo, asserting an edit lands on `journal2` and that an out-of-allowlist path and the live-worktree path are both rejected.
