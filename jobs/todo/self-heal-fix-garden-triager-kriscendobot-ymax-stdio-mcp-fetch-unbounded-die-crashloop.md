In `scripts/jobs/triager.sh:117`, the own-repo fetch `git --git-dir="$BARE" fetch -q --all --prune || die "fetch failed for $slug"` is unbounded and hard-dies, which crash-loops the systemd unit on any transient network condition. Observed live for `kriscendobot-ymax-stdio-mcp`: the failure blob is just `Terminated` (the fetch got SIGTERM'd on a hung/half-open connection) followed by `FATAL: fetch failed for kriscendobot-ymax-stdio-mcp`, exit 1 → systemd restart. The clone is valid and the fetch succeeds on retry, so this is a transient blip that should never have flapped the unit.

Fix, mirroring the patterns already in this file and `common.sh`:
1. Bound the fetch with `timeout --kill-after="$GARDEN_FETCH_KILL_AFTER" "$GARDEN_FETCH_TIMEOUT"` (as `bounded_clone` and `journal_fetch` already do — git has no IO timeout of its own, so a half-open connection hangs forever, which is what produced the `Terminated`).
2. On fetch failure, do a **clean skip** (`log "WARN: ..."; exit 0`) so systemd retries next tick instead of `die` (exit 1) flapping the unit — matching the self-provision-failed branch directly above (lines ~95–105) and the ci-watcher circuit-breaker discipline referenced in the header comment.
3. Escalate only on persistence: throttled `alert_maintainer "triager-fetch-failed-${slug//[^A-Za-z0-9._-]/_}" "..."` (as the provision-failed path does) rather than crash-looping.
Preserve the existing behavior on success (cursor advance, ref resolution below stay unchanged). Add/extend a test guard alongside the existing clone-provision cases asserting that a failing fetch resolves to a clean exit 0, not a die.
