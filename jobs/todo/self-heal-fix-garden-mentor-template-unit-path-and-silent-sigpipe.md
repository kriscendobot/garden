---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
scripts/jobs/handlers/mentor-claude.sh

Two deterministic defects in the mentor handler, observed together at 2026-08-11 20:50:51 (garden-mentor rc=1, blob 4c04fd762e669b13d4e9f26b2b85abf2fd78ddee: a blank line followed by the bare `FATAL: improve handler failed; leaving markers so the next tick retries`, with no diagnostic).

(a) `validate_mentor_response`'s first-body-line path regex `^[A-Za-z0-9_./-]+\.(sh|py|js|ts|md|service|timer)$` omits `@` from its character class, so a systemd TEMPLATE unit path can never validate even though `.service`/`.timer` are allowed extensions. The provider emitted a correct, well-formed block naming `scripts/systemd/garden-ci-watcher@.timer` (see `.garden-state/mentor/rejected/20260811T205051.652966613Z-openai.txt`) and it was rejected fail-closed with reason "first body line is not a repo-relative script/unit/brief path". 11 template units exist under `scripts/systemd/` (garden-{ci,comment,dependabot}-watcher@, garden-triager@, garden-approval-reconciler@, garden-watcher@), so every mentor proposal touching a watcher template fails identically and permanently. Fix: add `@` to the character class in the regex at mentor-claude.sh:145, and to the matching `[[ "$path" =~ ^[A-Za-z0-9_./-]+$ ]]` guard in `post_mentor_job` so the accept check and the directive-identity derivation stay in agreement (the file's own comment requires this). Note `already_fixed_pending_deploy`'s `grep -oE '[A-Za-z0-9._/-]+\.(sh|md|py|js|ts|service|timer)'` has the same omission and should be widened too, or a template-unit job will silently skip its pending-deploy pre-filter. Add a regression test covering a template-unit path.

(b) `record_malformed_reply` exits silently before it can log, which is what made (a) undiagnosable. Its excerpt line — `excerpt="$( { head -n 3 "$raw"; printf '  …  '; tail -n 3 "$raw"; } 2>/dev/null | head -c 400 | tr '\n' ' ')"` — SIGPIPEs the upstream group whenever the raw reply exceeds 400 bytes (this reply: 420). Under the file's `set -euo pipefail` the command substitution returns 141 and `set -e` terminates the handler immediately, so neither the function's own `log "WARN: provider ... returned malformed output"` nor the caller's `die "mentor provider ... returned malformed semantic output ($mentor_reject_reason)"` ever runs. The handler exits rc=141 with EMPTY stdout+stderr; mentor.sh's `is_transient_empty_failure` does not list 141 (only 143|130|137|75), so it takes the real-failure branch and prints an empty `$out` — the blank line in the blob. Reproduced deterministically. Fix: make the excerpt computation SIGPIPE-proof and non-fatal, e.g. bound the input before the byte cap and neutralize the exit status (`excerpt="$( { ... ; } 2>/dev/null | tr '\n' ' ' | cut -c1-400 || true )"`, or wrap the existing pipeline with a trailing `|| true`). The durable rejected/ capture must never be the only surviving evidence of a rejection — the WARN and the FATAL reason both need to reach stderr. Add a regression test with a >400-byte malformed reply asserting the handler emits its reject reason and exits via `die` rather than dying silently.
