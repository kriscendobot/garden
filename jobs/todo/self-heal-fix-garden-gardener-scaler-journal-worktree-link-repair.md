Add a lossless self-heal for a BROKEN journal-worktree link to `scripts/jobs/journal-worktree-keeper.sh` in `keep_journal_worktree()`. Today its preflight (`if ! git -C "$JW" rev-parse --git-dir …` → `log "WARN: … not a git repo …; skipping"; return 0`) leaves a broken worktree link unfixed, wedging every consumer of `journal_remote()` (`scripts/jobs/common.sh:493`) fleet-wide with the signature `fatal: not a git repository: <stale>/.git/worktrees/journal` followed by `FATAL: no JOURNAL_REMOTE set and no origin on $GARDEN_ROOT/journal`. This is exactly what took down `garden-gardener-scaler` (and, per a live scratch worktree, `garden-orchestrate`) after the checkout's worktrees were left registered under a stale `/home/kris/garden2/…` path prefix while the live checkout is `/home/kris`.

Change: in that preflight, before giving up, when the worktree directory exists and the owning repo admin (`$GARDEN_ROOT/.git/worktrees/<name>`) exists but `rev-parse --git-dir` fails, attempt `git -C "$GARDEN_ROOT" worktree repair "$JW"` (idempotent and lossless — it only rewrites the `.git` gitdir file and the admin `gitdir` file; it never touches tracked content or WIP), then re-run the `rev-parse --git-dir` check. Log a one-line `REPAIRED: relinked $JW to $GARDEN_ROOT/.git/worktrees/…` on success and proceed with the normal fetch/reconcile; only fall through to the existing `WARN … skipping` (and leave the tree untouched) if the repair does not restore a valid git dir. Keep the function returning 0 on every path so a tick is never marked Failed, consistent with the keeper's existing self-heal posture. No change to `common.sh` is needed; the immediate wedge has already been repaired by hand via `git -C /home/kris worktree repair /home/kris/journal`, so this job is purely the recurrence guard.
