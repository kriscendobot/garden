`scripts/jobs/clone-keeper.sh:79` handles a missing/not-a-git-repo tracked bare clone with a `WARN … skipping` that returns 0, so a vanished clone (observed 11:30:06Z: `worktrees/endojs-endo.git` missing) is re-warned every ~30m tick forever and never restored — a log line nobody reads, and downstream library-fleet reads of that clone silently fail. Harden it: extend the `GARDEN_TRACKED_CLONES` format (`clone-keeper.sh:47`) to carry the clone URL (`<dir>|<remote>|<branch>|<url>`, url optional for back-compat) and, on the missing-clone branch, attempt a bounded `git clone --bare <url> <abs>` to self-heal; if the URL is absent or the re-clone fails, escalate ONCE to the maintainer inbox (dedup on a stamped "already-reported-missing" marker so it does not re-post every tick) instead of silently repeating the WARN. This closes the "missing clone silently blocks the fleet" gap the script's own six-week-stale-clone rationale (`clone-keeper.sh:10`) was written to prevent.
