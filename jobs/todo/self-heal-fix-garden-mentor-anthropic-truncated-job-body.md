---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
scripts/jobs/handlers/mentor-claude.sh
The prompt built at ~line 27-52 (before `for provider in $(provider_order); do ... mentor_anthropic_attempt`) has no length constraint on the model's reply, so the anthropic provider's `claude -p` call repeatedly produces a JOB block long enough to get cut off mid-generation before the closing `ENDJOB`, tripping the deliberate fail-closed FATAL at line 287 (`die "mentor provider '$provider' returned malformed semantic output ($mentor_reject_reason); refusing fallback..."`) and exiting the `garden-mentor` service with rc=1. Evidence: `.garden-state/mentor/rejected/*-anthropic.txt` shows the `EOF reached inside JOB '<slug>' with no closing ENDJOB (unterminated block)` reason 4 times since 2026-08-19 (2026-08-19T08:22Z, 2026-09-17T18:22Z, and twice on 2026-09-24 at 08:21Z and 15:21Z — the latter two both proposing near-identical `budget-level.sh` `pool_failure()` fixes under different slugs, each truncated before completion; the 15:21 capture cuts off mid-word, "the better-fitt", confirming a hard output-length cutoff rather than a stylistic omission). Fix: tighten the prompt to (a) forbid preamble/framing text before the first `JOB` line (the current replies sometimes open with "Now writing the job." before the block, burning budget), and (b) cap each JOB body to a short, bounded size (e.g. "state the defect as file:line plus the fix in at most 4-6 sentences; do not enumerate multiple precedents in prose, cite file:line instead") so a reply reliably completes with its closing `ENDJOB` inside the turn's output budget. Add a regression subtest in `scripts/jobs/test/mentor-provider-order-test.sh` asserting the prompt text contains the new brevity/no-preamble constraint (the existing SUBTEST 15 already covers the fail-closed *handling* of an unterminated block; this closes the gap on *preventing* the recurring cause).
