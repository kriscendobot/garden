In `scripts/jobs/triager.sh` at line 117, the source-clone fetch `git --git-dir="$BARE" fetch -q --all --prune || die "fetch failed for $slug"` hard-dies (exit 1) on any transient/offline fetch failure, producing the failure signature `FATAL: fetch failed for <slug>` and triggering a systemd failure + self-heal on every network blip. This is inconsistent with the rest of the script: the missing-clone provision path (a few lines above) treats a failed fetch as "skip and retry next tick," and `sync_clone`/`journal_fetch` in `scripts/jobs/common.sh` already classify offline shapes (`_fetch_stderr_is_offline`, timeout rc 124/137) and take a clean-skip path (`exit "$GARDEN_OFFLINE_RC"`) rather than dying. Harden line 117 to mirror that: wrap the fetch in the same bounded-timeout + offline-classification handling used by `sync_clone` (or minimally, capture stderr and, when `_fetch_stderr_is_offline` matches or the timeout rc is 124/137, `log` and `exit "$GARDEN_OFFLINE_RC"` to skip and retry next tick instead of `die`). Keep `die` only for a genuinely non-transient failure (e.g. a fetch error that is not an offline signature). Verified transient: the repo exists, the bare clone/origin are intact, and re-running the exact fetch now returns rc=0.
