In `scripts/jobs/triager.sh`, wire in the self-provisioning path that `common.sh` (§ standing bare-clone provisioning helpers, ~line 442) and `clone-keeper.sh` (~line 153) already document as existing but which was never implemented. Failure signature: `garden-triager@<slug>` dies with `FATAL: no bare clone at <GARDEN_REPOS>/<slug>.git (clone the repo first)` on every tick for any repo armed in the journal `repos/` set whose bare clone this host has never held (observed for `kriscendobot-agoric-3-proposals`; the `repos/` dir is empty on the leader host, so every never-cloned armed repo fatal-loops).

Change: replace the hard `die` at `triager.sh:32` (`[ -d "$BARE" ] || die "no bare clone at $BARE (clone the repo first)"`) with self-provisioning that mirrors `clone-keeper.sh`'s missing-clone repair, reusing the shared helpers already in `common.sh` (do not copy logic):
- When `is_own_git_repo "$BARE"` is false, derive the source with `src="$(derive_clone_url "$BARE")"` (basename `kriscendobot-agoric-3-proposals.git` → `https://github.com/kriscendobot/agoric-3-proposals.git`, confirmed reachable), then `bounded_clone "$src" "$BARE"` (atomic sibling-temp + `mv -T`, as clone-keeper uses). Set the fetch refspec exactly as clone-keeper/`ensure-project-worktree.sh` prescribe so the subsequent `git fetch --all` and `refs/remotes/origin/<ref>` resolution work. Log a `provisioned missing clone <slug> from <src>` line.
- Preserve the fatal only for the genuinely-unrecoverable cases, matching clone-keeper's guards: if `derive_clone_url` cannot derive a URL (basename not `<owner>-<name>.git`) OR the derived source is unreachable, `die` (or escalate) rather than looping — a transient unreachable source self-heals next tick, a persistently bad one surfaces to a human.

Verify with `scripts/jobs/test/comment-watcher-test.sh`'s sibling triager test harness (add/extend a case that arms a never-cloned slug and asserts the triager provisions rather than dies), keeping `GARDEN_CLONE_URL_BASE` overridable for offline tests as the existing helpers already support.
