Add `scripts/jobs/regenerate-topics-counts.sh`, a sibling to the existing `scripts/jobs/regenerate-sections-index.sh`, that makes the per-topic Sections-count column in `library/topics/README.md`'s Index table a pure projection of the committed corpus instead of a hand-maintained number that drifts silently every scholar cycle. For each topic, the authoritative count is the number of section rows in that topic page's (`library/topics/<topic>.md`) sections table; topics whose index count is the literal `(meta)` (e.g. `spec-to-implementation`, `references`) are passed through verbatim and never recounted. Mirror the established script's contract exactly: `--check` (regenerate the Index counts in-memory and diff against the committed README; exit 0 if current, 1 if stale, 2 on setup/anchor error — CI/gate form), `--print` (emit to stdout, no network/write), and `--land` (default; sync the dedicated producer clone via `common.sh` `ensure_clone`/`sync_clone`, regenerate, land via `land-journal-edit.sh library/topics/README.md`, idempotent no-op when current, exit 75 on transient sync outage). Wire `--land` as the final step of the scholar/library landing gate alongside the sections-index regeneration, and `--check` into the integrity gate (step 8), so the count stays a usable signal rather than decorative. Why: two independent 2026-06-28 cycles (results 173121Z and 174820Z) each had to hand-correct multi-dozen-row count drifts and both flagged the need for exactly this deterministic reconciliation; whole-corpus hand-recounting is the same "impractical and risky" hand-edit that `regenerate-sections-index.sh` was created to eliminate. Add a paired test under `scripts/jobs/test/` following `regenerate-sections-index-test.sh`. Build in an isolated worktree off `origin/main2` per the garden-infra-jobs norm.
