---
role: shepherd
---

Run the gauntlet (clean → panel review → fix-loop → un-draft) on `endojs/endo-but-for-bots` DRAFT PR #643 `refactor(exo-git): use platform filesystem path types` (base `llm`, head `build/mount-types-daemon-local`), which the just-completed weave restored to MERGEABLE with all CI green; drive this M3 exo-git/mount path-type consolidation from green-draft to reviewed-and-un-drafted, treating the known repo-wide lint projectService ceiling (#594) as pre-existing and out of scope.

<!-- garden-reaped: 2 -->
