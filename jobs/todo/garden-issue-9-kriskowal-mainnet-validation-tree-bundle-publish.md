# #9 ymax0 repro — use the mainnet-validation-tree bundle-publishing examples (kriskowal pointer)
Repo: kriskowal/garden issue #9 — ymax0 XS value-stack overflow. **Complements** the in-flight
`garden-issue-9-mhofman-contract-kit-and-inquisitor-bridge` job — read the **full #9 thread**, don't
duplicate; consolidate.
kriskowal (2026-06-30T23:18Z, comment 4848697844):
> In addition to @mhofman's feedback, there are examples for **publishing a bundle to chain in the
> mainnet validation tree**.
This addresses mhofman's "the bundle is network-agnostic and just needs to be **installed first**"
step. Task: locate the bundle-publishing examples in the **mainnet validation tree** and use them to
install the ymax0/ymax1 bundle as the prerequisite for the contract-control-upgrade repro; fold this
into the ongoing #9 mainnet repro. Then reply on #9 with progress (acknowledged-comment → reply).
Scope: bot infra; no upstream agoric/agoric-sdk contact.
