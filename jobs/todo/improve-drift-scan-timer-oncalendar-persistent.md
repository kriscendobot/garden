Convert `scripts/systemd/garden-library-source-drift-scan.timer` from the relative monotonic pair `OnActiveSec=20m` + `OnUnitActiveSec=1h` to an absolute wall-clock schedule immune to daemon-reload churn: `OnCalendar=*:07/60` (hourly at minute :07, keeping the "offset past clone-keeper" intent) plus `Persistent=true`. Evidence: the timer has been `active (waiting)` for 4h44m but `systemctl --user list-timers` shows `LAST = -` and the service journal is empty — it has never fired, while sibling timers fire normally; meanwhile `library-source-drift-scan.sh --dry-run` finds 4 real drifts (ses/error/console.js, ses/error/assert.js, captp/finalize.js, netstring/reader.js) that have never been posted as `scholar-refresh-*` jobs. The relative timer is repeatedly re-armed by the periodic `daemon-reload`s in `deploy-sync.sh`, `repo-watcher.sh`, and `install-units.sh`, so its first elapse is deferred forever. After the edit, re-render+reload via `install-units.sh install` and confirm `list-timers` shows a populated `LAST`/`NEXT`. While here, audit the other garden timers for the same `OnActiveSec`/`OnUnitActiveSec`-without-`Persistent` anti-pattern (any whose window exceeds the reload cadence is vulnerable to the same indefinite deferral) and convert them to `OnCalendar`+`Persistent` likewise.
