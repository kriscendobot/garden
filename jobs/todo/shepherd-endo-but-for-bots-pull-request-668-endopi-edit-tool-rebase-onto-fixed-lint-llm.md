---
role: shepherd
---

Shepherd `endojs/endo-but-for-bots` PR #668 (`feat(agentry): LLM edit tool for Lal and Fae (endopi-edit-tool)`, branch `endopi-edit-tool`, base frozen `llm-08f5acc`) to green: its sole failing check is `lint` while all other checks pass — rebase it onto current `origin/llm` (which now carries the eslint projectService lint-ceiling fix from #596/#597), the same resume treatment #660/#661/#667 received, so this ready draft M3 endopi PR becomes CI-green and can proceed to its review gauntlet; re-escalate to a fixer only for a genuinely different, out-of-scope red.
