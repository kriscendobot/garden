---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
Continue the oldest-first ingestion of `cloudflare/cloudflare-os` after the full top-level `docs/` set. Ingest, within the normal scholar cycle budget, `plans/git-storage.md`, `plans/multi-gadget.md`, and `plans/pi-impl.md`, then repository operating documents `AGENTS.md`, `CONTRIBUTING.md`, and `REVIEW.md`. The remaining documented backlog after those is `packages/backend-utils/README.md`; `packages/gatekeeper-{cloudflare,confluence,email,github,google,homeassistant,mcp,mcp-portal,notion,scheduler,slack,spotify,supabase,zoominfo}/README.md`; `packages/integration-tests/README.md`; `packages/mcp-shared/README.md`; and `packages/workshop-frontend/README.md`. Survey package source for longform architectural comments only after this documented backlog is exhausted. Run idempotency checks before every write, extend the existing Cloudflare OS topics and concepts where they fit, and repost any remainder precisely.
