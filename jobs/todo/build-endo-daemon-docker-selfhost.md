---
role: builder
---

Build the `daemon-docker-selfhost` design in endojs/endo-but-for-bots (M3 exit-criterion centerpiece): a Docker image bundling the daemon/worker/CLI for headless operation, persistent state directory across container restarts, external gateway HTTP/WebSocket exposure, and acceptance of authenticated remote (non-localhost) connections per the Complete gateway-bearer-token-auth design. Open a mergeable PR.
