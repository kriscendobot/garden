`garden-comment-watcher@.service` already carries `KillMode=mixed` plus an EXIT/INT/TERM trap meant to reap the source's whole process group (gh + git children), yet the 20:57:54 tick still logged "Found left-over process 355210/355211/355212 (git) in control group while starting unit" — three git children survived the prior run's stop and lingered into the next start. The existing trap-based process-group reap in `scripts/jobs/comment-watcher.sh` is therefore not catching `gh --paginate`'s forked git/credential-helper children (likely they sit in a different process group than the one the trap signals, or are mid-network-syscall and outrace the SIGTERM before the cgroup SIGKILL backstop fires at TimeoutStopSec). Investigate the actual process-group topology under `gh --paginate` and harden the reap: signal the source's whole process group (e.g. `kill -- -$pgid` on the negated pgid, or run the source under `setsid` and TERM that session), and/or shorten `TimeoutStopSec` so the cgroup-wide SIGKILL backstop reaps stragglers before the next timer firing. This is a warning, not a unit failure today, but it is the "unclean termination" systemd flags and the kind of leak that compounds across the leader's many comment-watcher instances.
