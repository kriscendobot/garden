`scripts/jobs/triager.sh:32` hard-`die`s with "no bare clone at $BARE (clone the repo first)" when the watched repo's bare clone is absent under `$GARDEN_REPOS/<slug>.git`, so `garden-triager@kriscendobot-finbot` fails exit 1 on every tick (the clone at `repos/kriscendobot-finbot.git` was never created and nothing creates it). This contradicts the documented design: `common.sh:445-446` and the `clone-keeper.sh` header both state "triager.sh self-provisions a never-held clone with the same derive-URL + bounded-atomic-clone logic" — but the extraction of `is_own_git_repo`/`is_remote_location`/`derive_clone_url`/`bounded_clone` into `common.sh` (currently uncommitted, alongside modified `clone-keeper.sh`) was never wired into `triager.sh`. Fix: replace the `[ -d "$BARE" ] || die ...` guard with self-provisioning that mirrors `clone-keeper.sh`'s `keep_clone` missing-dir branch — when `! is_own_git_repo "$BARE"` and `$BARE` does not already exist as a non-repo dir, derive the source with `derive_clone_url "$BARE"` (a `repos/` clone is named `<owner>-<name>.git`, e.g. `kriscendobot-finbot.git` → `https://github.com/kriscendobot/finbot.git`), `bounded_clone` it into place, set `git -C "$BARE" config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'`, and log a `provisioned` line; on a non-derivable/unreachable source, log and `exit 0` (skip this tick to retry) rather than `die` so systemd does not churn. Then fall through to the existing fetch. Guard against clobbering a present-but-corrupt dir (surface, don't re-clone) exactly as `keep_clone` does. Confirm the corresponding triager test (if any under the jobs test suite) covers the never-held-clone path.
