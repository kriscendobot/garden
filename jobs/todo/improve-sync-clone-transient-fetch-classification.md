Broaden `sync_clone`'s transient-outage classification in `scripts/jobs/common.sh` so the fleet stops recording `garden-gardener@N.service: Failed with result 'exit-code'` (and stops burning self-heal responders) on ordinary git-fetch flakiness under ~100-gardener contention. Changes: (1) In `sync_clone` (around line 440-460), treat a `journal_fetch` timeout (`rc==124`) as a transient outage — `exit "$GARDEN_OFFLINE_RC"` — exactly as the `rc==128 && _fetch_stderr_is_offline` path already does; `journal_fetch` already logs the timeout, so this is just promoting it to the clean-skip path instead of `die "fetch failed"`. (2) Broaden `_fetch_stderr_is_offline` (around line 425-437) to also match the common transient remote/transport signatures: "the remote end hung up unexpectedly", "early EOF", "RPC failed", "fetch-pack: unexpected disconnect", "Connection reset by peer", "unable to access", "Failed to connect", "Operation timed out", and "TLS"/"SSL" handshake errors — and drop the hard `rc==128` gate so any rc whose stderr matches a known outage signature is normalized (these errors surface under several exit codes, not just 128). (3) De-duplicate the classifier: make `self-heal-run.sh`'s belt-and-suspenders grep (which currently carries a narrower, divergent copy missing "Connection timed out") source and call `_fetch_stderr_is_offline` from common.sh instead of its own inline regex, so the wrapper and the library can never disagree on what counts as transient. Keep all existing tests green and add a `run-test.sh` subtest that drives `sync_clone` with an injected `GARDEN_FETCH_CMD` returning rc=124 (and rc=128 with a "remote end hung up" stderr) and asserts the process exits `GARDEN_OFFLINE_RC` (normalized to 0 by self-heal), not 1.
