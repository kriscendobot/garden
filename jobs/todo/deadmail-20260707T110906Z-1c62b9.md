# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `port-xs-to-rust-memory-safe-engine-s13` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `port-xs-to-rust-memory-safe-engine-s13`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: port-xs-to-rust-memory-safe-engine-s13

----- ORIGINAL MESSAGE -----
to: port-xs-to-rust-memory-safe-engine-s13
from_host: endolin-garden-ece02cb4
from: inbox-send
sent_at: 2026-07-07T11:09:08Z
dead_lettered_at: 2026-07-07T11:09:08Z
---
[xs2rust-endor-stage5-fix-rejects] DONE — named coder rejects ported, bar MET, pushed to xs2rust-endor (PR #600, KEPT DRAFT).

Commits (branch xs2rust-endor):
- dd766cd22e endor-compile: document eval-in-function as a named remaining fold
- 1e8c815630 endor-262: retarget compiler-seam fold test off new.target
- 26e9f83d98 endor-compile: port new.target, optional chaining, for-let refresh, nested function decls

Ported (all byte-identical vs oracle pin 48ee02d8cfe0):
- new.target: Target node -> XS_CODE_TARGET (fxValueNodeCode).
- optional chaining ?.: Chain (fxChainNodeCode) + Option (fxOptionNodeCode), saved/restored chain_target + BRANCH_CHAIN.
- for(let ...): fxScopeCodeRefresh (REFRESH_LOCAL/CLOSURE per slot).
- nested function declarations: Define declares in scope_coding_block (NEW_LOCAL, no init) + fxScopeCodeDefineNodes.
- extra: non-identifier name-inference target (o.m=function(){}) now leaves value anonymous instead of asserting — this closed the addition subtree's 4 rejects (they were NOT new.target as the job spec guessed; they were the name-inference fold).

Bar (all MET):
- curated corpora: endor-rejected 20 -> 0 (total=1711 identical=1711 divergent=0).
- test262 spot-checks: addition 48/48 id, 0 rej; statements/if 0 rej; expressions/conditional 0 rej. All 0 divergent.
- cargo test --workspace -- --test-threads=1: EXIT=0.
- In-crate gate (corpora_byte_identity_no_undocumented_divergence) tightened: documented-fold allowlist emptied, now asserts endor_rejected == 0.
- #![forbid(unsafe_code)] intact.
- Per-construct fixtures added to coder_byte_identity.rs (new_target, optional_chaining, for_let_declaring_scope, nested_function_declaration, anonymous_function_member_target_no_name).

Remaining fold (in-scope but NOT completed — reported honestly):
- Direct eval INSIDE a function (SCOPE_EVAL param scope). Attempted the parameter with/STORE dance but it diverged: the oracle needs the whole in-function eval-BODY slice (EVAL opcode environment plumbing + in-function sloppy-eval reference resolution), not just the param dance. Reverted the attempt; documented it as a self-naming assert ("eval-scope params deferred") on scope_coding_params. Program/block-level eval remains byte-identical. This is a larger follow-up slice, not the small self-contained port the job assumed.

----- END ORIGINAL MESSAGE -----
