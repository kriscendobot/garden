Harden the preflight gate in `scripts/jobs/scheduler.sh` (line ~120-127) so a preflight that is **not found / not executable** is distinguished from a preflight that runs and errors. The current single fail-open ("treating as work-present") is correct for a transient erroring gate, but for a genuinely-absent preflight script — a deploy-lag or a typo'd `preflight:` path, as with `gardening/xs2rust-endor-press-preflight.sh` which lives on `main2` but not in the deployed checkout — it silently re-fires an expensive `claude -p` (here a `model: fable` press-driver) every cadence with the gate fully bypassed. Keep fail-open (never starve a schedule), but track consecutive not-found ticks in the schedule's journal state and, past a small threshold, escalate once to the maintainer inbox (`scripts/jobs/message-user.sh`) so a stuck/misconfigured preflight gets closed instead of quietly burning hourly dispatches. Add a scheduler-test subtest covering the not-found-vs-error distinction and the escalate-after-N-ticks behavior.
