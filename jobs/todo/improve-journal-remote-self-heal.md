Harden `journal_remote()` in `scripts/jobs/common.sh` (lines 490-494) so a transient inability to read the journal origin cannot FATAL the entire fleet. Root evidence: at 2026-07-03 11:06-11:11Z ~every `garden-*` unit on this host died repeatedly with `FATAL: no JOURNAL_REMOTE set and no origin on /home/kris/journal`, yet the origin (`git@github.com:kriskowal/garden.git`) is intact and identical to `$GARDEN_ROOT`'s own origin — the read was momentarily empty (config lock / worktree-keeper / deploy window). Change: (1) on every successful resolution, cache the value to a per-host `$GARDEN_STATE/config/journal-remote` file (GARDEN_STATE lives outside any reset-prone worktree, common.sh:28); (2) when both `$JOURNAL_REMOTE` and the journal worktree's `remote.origin.url` are empty, fall back in order to the cached file, then to `git -C "$GARDEN_ROOT" config --get remote.origin.url` (the shared repo config), and only `die` if all fail — logging a single WARN, not a per-tick FATAL. This converts a fleet-wide FATAL storm into a self-healing transient and stops the log flood that buries real signal. Keep the test hook intact (JOURNAL_REMOTE override, common.sh:20-22) so a bare-repo test fixture still wins.
