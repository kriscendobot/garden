`scripts/jobs/clone-keeper.sh` (`keep_clone`) warns "tracked clone worktrees/endojs-endo.git is missing or not a git repo; skipping" on every ~30m tick and freshens nothing — the tracked default points at a path that does not exist on this host (on disk only `worktrees/endojs-endo-but-for-bots.git` is present). Two changes: (a) correct/validate the `GARDEN_TRACKED_CLONES` default so it names a clone that actually exists (confirm whether upstream `endojs/endo` should be tracked here at all, and drop or repoint the row rather than warning forever); (b) since the script only fast-forwards and never clones, a missing tracked clone has no recovery path — make `keep_clone` reconcile a genuinely-missing-but-still-wanted clone by cloning it bare from a known remote URL (bounded/timeboxed like `bounded_fetch`, logged, returning 0 on failure so one clone never aborts the rest) instead of skipping indefinitely. Otherwise the exact stale-clone hazard the script exists to prevent recurs silently.
