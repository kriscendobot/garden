---
tier: minion
model-burned: mentor
fallback-tier: 
dispatch: automatic
---
endojs/endo-but-for-bots: `this.self` inside an exo's object literal was found suppressing TypeScript's inference across the whole literal on PR #713's daemon code. Investigate whether other daemon exos still using `this.self` are masking type errors the same way.

<!-- garden-reaped: 0 -->
