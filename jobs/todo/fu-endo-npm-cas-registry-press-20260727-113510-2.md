In endojs/endo-but-for-bots, write a design note (designer job, no code) on the npm-CAS registry's handling of nanoid's `node:crypto`/webcrypto need — the choice between a builtin shim, a browser-condition policy, and a node-shims default — so the default-condition-set policy question raised across the 07-27/07-28 press reports has a written proposal for the maintainer to rule on.
