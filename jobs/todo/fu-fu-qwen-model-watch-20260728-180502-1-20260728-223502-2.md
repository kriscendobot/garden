In the garden's own repo (kriscendobot/garden, `main2`), reconcile the ollama-unit naming in `scripts/jobs/handlers/codex-provider-common.sh`: its operator-facing handler messaging tells the reader to check/start `garden-ollama.service`, but on this host that unit is installed-but-disabled (it is hermit-count-gated by `install-units.sh`) while the endpoint actually serving local inference is a separate `ollama.service`. Determine which unit the guidance should name in each case and fix the messaging (and any sibling text in `context/operations/local-inference-amd.md` that inherits the same assumption) so an operator following it reaches the running endpoint; do not enable, disable, or otherwise change any systemd unit — this is a text/diagnostics fix only.
