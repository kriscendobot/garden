`scripts/jobs/journal-entry.sh` mints a malformed journal entry when invoked with `--help` (observed: `entries/2026/06/27/104604Z---help-gardener-f5074e.md`, `kind: --help`, empty body). At line 18 the first positional is taken verbatim as `<kind>` with no help handling and no flag-shape validation, so any flag-looking argument is committed and pushed as a real journal entry — polluting the add-only journal and the message bus. Harden it: (1) before consuming the positional, intercept `-h`/`--help` and print the usage block (lines 2-6) then exit 0; (2) reject a `<kind>` that begins with `-` (and ideally constrain to a known set / `[a-z]` charset) with a usage error and nonzero exit, so a flag typo can never become a committed entry. Apply the same `-h`/`--help` + flag-shape guard to the sibling producer scripts that take a leading positional (`post-job.sh`, `post-plan.sh`) since none currently handle help (confirmed: no `usage()`/`--help` convention in `common.sh`). Build in an isolated worktree off `origin/main2` per the garden-infra-jobs norm.
