`journal:schedules/xs2rust-endor-press.md` declares `preflight: gardening/xs2rust-endor-press-preflight.sh`, but no such file exists in `scripts/jobs/gardening/`. Per `scripts/jobs/scheduler.sh:126`, a missing/non-executable preflight fails open ("treating as work-present") and only logs a per-tick WARN, so the gate is silently a no-op forever and the schedule dispatches unconditionally every cadence. Harden `scripts/jobs/set-schedule.sh` to validate at write time that a supplied/preserved `preflight:` path resolves to an executable file under `scripts/jobs/` (die with a clear message on a dangling reference) so an authoring typo cannot be committed. Separately, in `scripts/jobs/scheduler.sh`, distinguish a *configured-but-absent* preflight (a config error that should escalate to the journal once, dedup'd) from a transient gate error — a config error that repeats the same WARN every 30m indefinitely is invisible noise, not a signal.
