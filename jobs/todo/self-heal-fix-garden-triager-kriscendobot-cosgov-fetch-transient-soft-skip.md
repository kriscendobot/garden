In `scripts/jobs/triager.sh` line 117, the steady-state fetch of an existing bare clone `git --git-dir="$BARE" fetch -q --all --prune || die "fetch failed for $slug"` hard-`die`s (exit 1) on any transient fetch failure, marking the systemd unit `garden-triager@<slug>` failed and triggering the self-heal responder on every network blip. Observed signature: blob shows `Terminated` (fetch subprocess) then `FATAL: fetch failed for kriscendobot-cosgov`, exit 1. This contradicts the file's own stated principle (line 50: "a missing clone must never be a hard die") and the self-provision path (lines 94–104), which on a transient failure logs a WARN, calls throttled `alert_maintainer` with a per-slug dedup key, and `exit 0` to retry next tick. Change line 117 so a fetch failure on an existing clone follows the same soft-skip pattern: on failure, `log "WARN: fetch failed for $slug (transient? retrying next tick)"`, call `alert_maintainer "triager-fetch-failed-${slug//[^A-Za-z0-9._-]/_}" "<message noting a persistent failure means the remote is unreachable/gone>"`, and `exit 0` instead of `die`. This keeps a genuinely-broken remote escalating (throttled, once per window) while stopping every transient fetch blip from crash-looping the unit and spuriously invoking self-heal. Keep the existing `set -euo pipefail` intact and ensure the `|| { ...; exit 0; }` block doesn't trip `errexit`.
