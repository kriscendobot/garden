Fleet-wide triager outage: every `garden-triager@<slug>` instance dies each tick with `FATAL: no bare clone at $GARDEN_ROOT/repos/<slug>.git`, because `scripts/jobs/triager.sh:25` defaults `GARDEN_REPOS` to `$GARDEN_ROOT/repos`, but the garden's standing bare clones live under `$GARDEN_ROOT/worktrees/<owner>-<repo>.git` (see `common.sh:442-448` "standing bare-clone provisioning helpers" and CLAUDE.md § Layout). `repos/` does not exist on this host; the clones the triager needs already exist under `worktrees/` and are kept fresh by clone-keeper.sh. All 8 triagers (agoric-3-proposals, agoric-sdk, cosgov, endo, finbot, minion.town, ocapn, vattr97) are affected.

Two coordinated changes to `scripts/jobs/triager.sh`:

1. Point the bare-clone lookup at the standing clone directory. Change the default from `: "${GARDEN_REPOS:=$GARDEN_ROOT/repos}"` to `$GARDEN_ROOT/worktrees` (and update the header comment at line 7 accordingly). Verify no other consumer relies on the old `repos/` path in a way this breaks — `comment-watcher.sh:179` has the identical stale default and only warns (`:311-312`), so update it in the same commit for consistency (it will then find the same worktrees clone instead of falling back to gh-only polling).

2. Replace the hard `die` at `triager.sh:31-32` with the self-provisioning the design already documents. Instead of dying when `$BARE` is absent, use the shared `common.sh` helpers to self-heal: if `is_own_git_repo "$BARE"` is false and the dir is genuinely missing, `derive_clone_url "$BARE"` → `bounded_clone <src> "$BARE"`, set `remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'`, and proceed; only `die`/escalate if the source is unreachable or underivable (mirror clone-keeper.sh's `keep_clone` missing-clone branch, including its transient-vs-persistent handling). This is exactly what `common.sh:444-446` and `clone-keeper.sh` already claim triager does.

Validate: after the change, `triager.sh kriscendobot-minion.town` must resolve the existing `worktrees/kriscendobot-minion.town.git` clone (HEAD `main`) and reach the fetch/cursor path instead of dying, and a slug whose worktrees clone is absent must self-provision from the derived `https://github.com/<owner>/<name>.git` rather than FATAL-storming every tick. Add/extend a triager test covering both the found-clone and self-provision paths. Do not create a `repos/` directory as a workaround — the fix is to use the canonical `worktrees/` location.
