The live garden checkout at /home/kris (the tree the garden-gardener@N systemd units exec scripts/jobs/gardener.sh from) is pinned at commit 2e3372fbe, which is 9 commits behind origin/main2 (6a6e21b36) and is MISSING the already-merged rc==128 claim-absorb fix (commit bd65630d1; job self-heal-fix-garden-gardener-claim-transient-git-128-not-fatal, in jobs/tada). Deployed gardener.sh still does the bare `[ "$rc" -ne 0 ] && die "claim failed (rc=$rc)"` with no `rc==128 || rc==75` skip branch, so a transient git rc=128 during claim-job.sh's sync_clone crashes the worker (exit 1) and trips the systemd restart + self-heal responder — the 08:40:47 `[gardener/22] FATAL: claim failed (rc=128)` crash, recurring fleet-wide on every connectivity blip. Fix: fast-forward the /home/kris main2 working tree to origin/main2 so the deployed gardener.sh carries the rc==128/75 absorb-and-retry branch (origin/main2:scripts/jobs/gardener.sh:80), then let systemd cycle the fleet onto the updated script. Do this safely: the live tree has concurrent gardener WIP (uncommitted `M scripts/jobs/gardener.sh` and untracked bundle-ymax0.json / wt-507/ / test/claim-transient-test.sh) — stash or preserve that WIP rather than `git reset --hard`, fast-forward only if HEAD is a clean ancestor of origin/main2 (it is: rev-list left-right count is 0  9), and confirm `grep -n 'rc.*128' scripts/jobs/gardener.sh` shows the skip branch before the die afterward. Consider following up by pinning the units to a deployed/released checkout so the fleet never executes a half-merged live tree.
