In `scripts/jobs/handlers/follow-up-claude.sh`, the inner `claude -p` failure path (the `if [ "$rc" -ne 0 ]; then ... die "claude -p failed (rc=$rc)..."` block) treats EVERY non-zero claude exit as a blind tick failure that `follow-up.sh` retries against the same digest indefinitely — the 2026-06-27 07:53–08:44 outage re-rolled the same prompt ~6 times. Mirror the gardener's inner-claude classifier (`gardener.sh:170`: `grep -qiE 'overloaded|rate[ _-]?limit|connection error|\b(429|5[0-9][0-9])\b|api[ _-]?error|econnreset|etimedout'`) instead of inlining it twice: factor that regex into a shared `is_transient_claude_signature()` helper in `scripts/jobs/common.sh` (next to `is_offline_signature`) and call it from BOTH gardener.sh and follow-up-claude.sh. On a transient signature, keep current behavior (die → retry next cadence). On a NON-transient claude failure (genuine crash/malformed-prompt/auth), route the captured stderr+stdout to the maintainer inbox (reuse the existing `route_rejected`/`inbox-send.sh maintainer` path) and exit 0 so the seen-marker advances and the bad digest does not wedge the service.
