In scripts/jobs/gardener.sh + scripts/jobs/common.sh: a handler killed by its own `timeout --signal=TERM "$GARDEN_HANDLER_TIMEOUT"` wrapper exits rc=124, which is conceptually an EXTERNAL kill (the wall-clock wrapper terminated it) but is masked by timeout's 124 code instead of surfacing as 143. Today the classifier (gardener.sh:215–240) only folds 143/130/137 into the transient branch via is_external_kill_rc (common.sh:831), so rc=124 falls through to the loud real-failure path and escalates kind:error to the gardener inbox on EVERY reaper requeue. This false-escalates inherently-long handlers (the shepherd job shepherd-kriscendobot-agoric-sdk-pr7 timed out at the 2400s window driving CI to green and was escalated as a defect). Fix: introduce a dedicated wall-clock-timeout classification — either add 124 to is_external_kill_rc (with its comment updated to name the timeout-wrapper kill as a fourth external-kill source) or add a sibling is_handler_timeout_rc and branch it into the transient path in gardener.sh. Treat it like the signal-kill transients: emit ONE kind:progress note, no inbox kind:error, leave in doin, and let the existing reaper poison-counter (reap_count / `<!-- garden-reaped: N -->`) escalate it as poison after the threshold — so a genuinely DEADLOCKED handler still surfaces, just after N cycles instead of spamming a real-error every cycle. Update the deliberate-route comment at gardener.sh:137–142 accordingly. Add a timeout-classifier unit case alongside the existing scripts/jobs/test/timeout-classifier-test.sh.
