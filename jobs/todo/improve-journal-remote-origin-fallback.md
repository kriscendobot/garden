Harden `journal_remote()` in `/home/kris/scripts/jobs/common.sh` (lines 490–494) against the fleet-wide FATAL cascade seen at 15:50–15:51Z, where a momentary loss of `remote.origin.url` on the `$GARDEN_ROOT/journal` worktree made every gardener, gardener-monitor, and ci-watcher `die` on claim simultaneously. The garden **root** checkout `$GARDEN_ROOT` is the same repo pointing at the same remote and had its origin intact throughout. Change `journal_remote()` to try, in order: `$JOURNAL_REMOTE` (unchanged) → `git -C "$GARDEN_ROOT/journal" config --get remote.origin.url` → **fallback** `git -C "$GARDEN_ROOT" config --get remote.origin.url` → only then `die`. This single fallback would have prevented the entire host's worker fleet from going down on a transient one-worktree config gap. Consider also having `journal-worktree-keeper.sh` verify `remote.origin.url` exists on `$JW` at the top of `keep_journal_worktree()` and re-add it (from `$GARDEN_ROOT`'s origin) if missing, so the worktree self-heals its remote the same way it already self-heals its branch.
