Add a deterministic regenerator script (e.g. `scripts/jobs/regenerate-sections-index.sh`) that rebuilds `library/sections/README.md` from the committed `library/sources/*.md` section tables, lands it through the producer clone (reusing `land-journal-edit.sh`'s sync+CAS path), and is invoked as the final step of the scholar/library landing gate. Today no script regenerates this auto-generated 40 KB+ index: `library-link-check.sh` requires its `### <slug>` blocks to resolve (it's a "backstop"), yet scholars handle it inconsistently — the 2026-06-28 erights ingest had one scholar skip it entirely ("a regeneration pass will pick up the new sections", entry 155751Z) and another hand-edit it (+5 blocks, entry 161137Z). Whole-file hand-editing a 5561-entry generated file is exactly the "impractical and risky" judgment call that belongs in a script. Wire the regenerator into the gate so the backstop is never left stale and no agent ever whole-file-replaces it by hand.
