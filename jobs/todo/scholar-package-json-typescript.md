---
role: scholar
---
# Scholar: how TypeScript reads package.json (types, exports, moduleResolution)

Follow-on to scholar-package-json-schema-and-tooling. Ingest the TypeScript handbook's module-resolution + publishing material as library sources (pinned commits): types/typings, typesVersions, the `types` export condition, and moduleResolution `bundler`/`node16`/`nodenext`; the correct dual-`types`-condition publishing recipe. Add sections under topic `package-manifest`; back the project's TypeScript matrix row and the condition-order inconsistency; note remaining synthesis.
