---
role: shepherd
---

Shepherd endo-but-for-bots PR #660 (`refactor: repoint @endo/marshal, @endo/captp, @endo/patterns consumers and deprecate their plain re-exports`, branch `refactor/inter-package-reexports-marshal-captp-patterns`) to green: its sole failing check is `lint` while all 24 other checks pass — diagnose and fix the lint failure (rebasing onto the fixed-lint baseline as #661 did, if that is the cause), so this ready non-draft M3 substrate PR becomes clean and mergeable.
