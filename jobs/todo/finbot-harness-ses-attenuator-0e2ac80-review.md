Repository: kriscendobot/finbot (worktree slug kriscendobot-finbot), default branch now at 0e2ac80.
A security-critical change just landed on the default branch: packages/harness gained a real SES compartment attenuator for subagent sandboxing (2f14eed), the harness default was flipped from permissive to compartment attenuation (7ac18d4), and the SES dependency was declared (3b74dfe). This replaces prompt-level ("the LLM follows the prompt") isolation with actual SES enforcement, and the v1 compartment path is now the production default.
Task: run a security-focused review of packages/harness/sandbox/permissive.js (the new attenuator), spawn.js/index.js/schemas/spawn.js (how attenuation is selected and applied), and test/spawn.test.js. Verify the per-role globals policy construction and capability-based tool filtering actually deny ungranted globals/tools, that there is no compartment escape (no leakage of host intrinsics, no bypass through endowments or the powerful references SES lets through), and that flipping the default to compartment attenuation does not silently break existing spawn flows. Confirm the declared SES dependency version matches what the code uses. Report findings; if a defect or escape is found, open a fix or flag it to the maintainer rather than merging over it.
