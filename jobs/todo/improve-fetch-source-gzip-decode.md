scripts/jobs/fetch-source.sh
After a successful fetch, inspect the dumped response headers (`$hdrs`) for `Content-Encoding: gzip` (case-insensitive) and, when present, gzip-decode `$out` in place *before* computing `sha256`/`bytes` at line 243. Today `_curl` (lines 152–159) saves the raw response body and hashes it as-is; the Wayback Machine `id_` original-bytes form replays the originally-captured payload including its stored `Content-Encoding: gzip`, so the saved file is gzip-compressed HTML, the citable `source_content_sha256` idempotency anchor is taken over the *compressed* bytes, and the ingesting scholar must manually `gzip -d` before the file is readable (it looks like binary). This surfaced live in the OpenAI Symphony ingest (openai.com 403 → wayback `id_` fallback, gzip bytes). Two failure modes to close: (1) an ingester reading undecoded binary, and (2) a future curl that negotiates a *different* transfer encoding shifting the SHA anchor and breaking idempotency for the same logical source. Normalize the stored bytes to decoded content and compute the SHA over the decoded form so the anchor is transfer-encoding-invariant; record the decode in the log line and keep `source_fetched_via` unchanged. Guard the decode (only when the gzip header is present and `gzip -t` validates) so identity-encoded responses are untouched, and add a test-harness case covering a gzip-encoded stub response.
