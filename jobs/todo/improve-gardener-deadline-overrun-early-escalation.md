Act on the deadline-overrun signal the gardener already computes instead of only logging it. In `scripts/jobs/gardener.sh`, in the transient branch (around lines 285-340), when `is_handler_timeout_rc "$rc"` is true AND `elapsed >= GARDEN_HANDLER_TIMEOUT - epsilon` (a small epsilon, e.g. GARDEN_HANDLER_KILL_AFTER or ~30s — meaning the handler hit its OWN wall rather than getting an external SIGTERM/OOM), stamp a distinct marker on the still-in-doin claim (e.g. `<!-- garden-deadline-overrun: N -->`, incremented alongside the existing reap-now hint) instead of treating it as an ordinary transient blip. Then in `scripts/jobs/reaper.sh` (poison decision around lines 320-333), escalate a job carrying that overrun marker to POISON after a much lower threshold (new knob `GARDEN_REAP_OVERRUN_THRESHOLD:=2`) rather than the full `GARDEN_REAP_POISON_THRESHOLD=5`. Rationale: a handler killed at its own 2400s wall will be killed identically every requeue, so requeuing it 5× (~200 min of gardener wall-clock) before surfacing it is pure waste; two identical deadline hits is already conclusive. The poison alert body should name the deterministic-overrun signature (rc=124, elapsed≈GARDEN_HANDLER_TIMEOUT) so the maintainer sees "this job exceeds the handler budget" rather than a generic "poison after 5 cycles." Keep the external-signal-kill path (143/130/137 via is_external_kill_rc) on the existing 5-cycle threshold — those genuinely are transient blips. Extend the timeout-classifier tests under `scripts/jobs/test/` (timeout-classifier-test.sh) to cover the elapsed-at-the-wall branch.
