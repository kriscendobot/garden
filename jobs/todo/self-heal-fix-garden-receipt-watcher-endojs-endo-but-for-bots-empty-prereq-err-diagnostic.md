---
tier: mentor
fallback-tier: minion
dispatch: automatic
---
In scripts/jobs/receipt-watcher.sh (around line 84-91), the prerequisite-failure path assumes `$PREREQ_ERR` has diagnostic content to show ("see prerequisite stderr above"), but a failed tick on garden-receipt-watcher@endojs-endo-but-for-bots (2026-09-22 05:19:05, rc=1) produced a completely empty PREREQ_ERR — `ensure_clone`/`sync_clone` exited non-zero without writing anything to stderr, so the final FATAL line referenced diagnostic text that didn't exist, and the self-heal capture blob (596edc4341ef...) contained only that one uninformative line. Add a branch so when `$PREREQ_ERR` is empty (`[ -s "$PREREQ_ERR" ]` false) after a non-transient `prereq_rc != 0`, the die message says so explicitly (e.g. "receipt journal prerequisite failed for $repo (rc=$prereq_rc) with NO captured stderr — likely signal-killed or a silent tool failure mid-write") instead of pointing at absent "stderr above". This is a pure diagnosability improvement (no behavior change to the retry/exit path) so the next occurrence of this same silent-failure shape is actually debuggable instead of opaque. The clone dir is deliberately SHARED across all `garden-receipt-watcher@*` template instances (same pattern as ci-watcher/comment-watcher's shared verify clones) — do not "fix" that by namespacing it per-slug; that is not the bug here.
