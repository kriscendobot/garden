Add a standing `scripts/jobs/library-source-drift-scan.sh` (modeled on the existing `library-link-scan.sh` standing-scan + `clone-keeper.sh` cadence-keeper shape, with a paired `scripts/systemd/garden-library-source-drift-scan.{service,timer}` auto-discovered by `install-units.sh`'s `garden-*` glob). Each tick: read the committed `library/sources/README.md` from the current `origin/journal2` tip (via a dedicated read-only clone synced tip-first, exactly like `library-link-scan.sh` does — never the live `journal/` worktree); parse each source row's `source-repo` + `source-path` + recorded inline `file-commit \`<sha>\``; for every row whose repo has a local bare clone under `worktrees/<owner>-<repo>.git` (skip-and-log rows without one, e.g. MetaMask/ocap-kernel, so the scan never fails on a missing clone and never reaches the network), resolve the upstream path's latest commit with `git -C <bare-clone> log -1 --format=%H -- <source-path>` and compare to the recorded sha; for each drifted source, idempotently post a `scholar-refresh-<source-slug>` job via `post-job.sh` (low priority, skipping any already-open/in-flight refresh for that slug). This moves the per-source freshness audit off the scholar — who today only catches drift on the 2–4 sources a cycle happens to touch — onto a deterministic corpus-wide scan, the same way `library-link-scan.sh` moved tip-stale section-link auditing out of the in-context LLM. It reuses the bare clones clone-keeper already keeps fresh, so it stays offline-safe and read-only, and it does not widen scope (read-only reads of already-ingested upstream repos; no agoric-sdk).

---
claim:
  host: endolinbot
  gardener: 100
  claimed_at: 2026-06-27T15:08:52Z
