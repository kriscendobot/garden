Make `scripts/jobs/clone-keeper.sh` self-provision a missing tracked bare clone instead of warning forever. Today `keep_clone()` (lines 78-81) logs `WARN: tracked clone ... is missing` and returns 0 every tick, so a host that was never seeded with `worktrees/endojs-endo.git` (this host, endolinbot2, is one) emits the warning on every ~30m timer firing indefinitely and never recovers without a manual `git clone --bare`. Move provisioning onto the deterministic cadence, mirroring the keeper's own freshness ethos: when `git rev-parse --git-dir` fails, `git clone --bare <url> <abs>` then set the fetch refspec (`git -C <abs> config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'`) — the same two steps `ensure-project-worktree.sh:66-69` prints as manual instructions — and then fall through to the normal fetch/fast-forward. Because deriving the URL by splitting `endojs-endo` into owner/name is ambiguous (hyphens appear in both owner and repo, e.g. `endojs-endo-but-for-bots`), extend the `GARDEN_TRACKED_CLONES` line format from `<dir>|<remote>|<branch>` to an optional fourth field `<dir>|<remote>|<branch>|<clone-url>` (default the existing entry to `https://github.com/endojs/endo.git`), and clone from that explicit URL. Wrap the clone in `timeout GARDEN_FETCH_TIMEOUT` with the same offline-tolerant logging so an unreachable upstream leaves the host un-provisioned-but-quiet-next-tick rather than aborting the loop. Add a case to `scripts/jobs/test/clone-keeper-test.sh` covering the missing-clone → provisioned path.

<!-- garden-reap-now -->
---
claim:
  host: endolinbot2
  gardener: 19
  claimed_at: 2026-07-02T20:51:58Z
