The `scholar-library-cycle` schedule keeps dispatching an LLM scholar agent every hour regardless of whether there is any claimable ingest work — the journal slice shows seven concurrent `scholar-library-cycle-*` cycles, all draining empty (no inbox asks, no `scholar-*` board jobs, no fresh `role/scholar` broadcast), each burning an agent dispatch and writing a low-value `result` entry. The deterministic gate that prevents exactly this already exists: `scripts/jobs/scholar-preflight.sh` proves in plain code whether work is present (exit 0 = dispatch, exit 2 = advance clock only, post nothing), and `scripts/jobs/scheduler.sh` already reads and acts on a `preflight:` frontmatter field (lines 93, 109–143). The gap is that the live `schedules/scholar-library-cycle.md` on `origin/journal2` carries no `preflight:` line — its frontmatter is only `cadence`/`last_dispatched`/`job_basename_prefix` — so the gate is dark even though `scholar-preflight.sh`'s own header claims it is "wired into schedules/scholar-library-cycle.md." Land the wiring by re-racing the schedule with the gate set: `GARDEN_SCHEDULE_PREFLIGHT=scholar-preflight.sh scripts/jobs/set-schedule.sh scholar-library-cycle hourly scholar-library-cycle` (set-schedule.sh:44–52 writes the `preflight:` line; the scheduler preserves it across future stamps). This moves the idle/active decision off the dispatched agent into the existing script, eliminating the recurring back-to-back idle scholar dispatches and the associated `result`-entry journal noise, while keeping instant pickup the moment real work appears.

---
claim:
  host: endolinbot2
  gardener: 83
  claimed_at: 2026-06-29T14:22:17Z
