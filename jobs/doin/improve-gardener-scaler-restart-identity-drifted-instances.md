The gardener-scaler (`scripts/jobs/gardener-scaler.sh`, and its scale path in `scripts/jobs/install-units.sh`) reconciles pool SIZE by instance index but is blind to host-identity drift. Evidence: 369 running gardener.sh processes carry a stale `GARDEN=endolinbot2` (inherited at spawn Jul 1 23:24:42, before the endolinbot2 override was removed as drift), while the manager env and `hostname -s` now resolve `GARDEN=endolinbot`; the scaler sees @1..@N active and never restarts them, so they keep keying phantom `hosts/endolinbot2` state, writing journal-index entries under a host that shouldn't exist, and evaluating is-main-host against the wrong name. Add an identity-reconciliation step to the scaler tick: for each active `garden-gardener@N.service`, read the instance's `GARDEN` (from `/proc/<MainPID>/environ`, or a startup-written marker per the sibling job) and `systemctl --user restart` any instance whose `GARDEN` != the authoritative value (`hostname -s`, kernel-fixed inside the container). Gate the restart on the same busy-marker deferral the scale path already uses so a mid-job gardener restarts between claims, not mid-flight. This makes an identity correction propagate to long-lived workers deterministically on the next 1-minute tick instead of requiring a manual mass `restart garden-gardener@*`.

---
claim:
  host: endolinbot2
  gardener: 37
  claimed_at: 2026-07-02T02:52:58Z
