Harden `scripts/jobs/handlers/ci-rollup-gh.sh` against transient GitHub network blips. Today it does one bare `gh pr view "$pr" -R "$repo" --json state,statusCheckRollup` (line 47) and `exit 1` on ANY failure. This window it hit `net/http: TLS handshake timeout` twice (#394, #306), each time skipping the PR and logging a WARN — delaying auto-shepherd-on-red detection. `common.sh` already has `gh_api_retry` (line 860) with a full-jitter bounded retry loop, and `GARDEN_TRANSIENT_GH_API_SIGNATURES` (line 848) already classifies `net/http: TLS handshake timeout` as transient — but that helper wraps `gh api`, not the `gh pr view` shape this handler uses. Fix: factor the retry loop in `gh_api_retry` so the `gh pr view` call can reuse it — either add a sibling `gh_cmd_retry`/`gh_pr_view_retry` helper in `common.sh` that runs an arbitrary `gh` subcommand under the same `_gh_api_stderr_is_transient` + `backoff "$attempt"` loop (up to `GARDEN_GH_API_ATTEMPTS`), or convert the `gh pr view` to the equivalent `gh api graphql`/`gh api repos/.../pulls/...` call and route it through the existing `gh_api_retry`. Preserve the "never guess a state" discipline exactly: a definitive (non-transient) failure still exits 1 without retry; only transient signatures retry; on exhaustion still exit 1 (never a false green). Extend `scripts/jobs/test/ci-watcher-test.sh` / `gh-api-retry-test.sh` with a case that a stubbed transient-then-success `gh pr view` returns a settled verdict rather than skipping.

---
claim:
  host: endolinbot2
  gardener: 13
  claimed_at: 2026-07-03T03:22:27Z
