In `scripts/jobs/triager.sh` (lines 55–56), the `new_sha` assignment uses bare `git rev-parse` in an `A || B` command substitution. When the primary ref `refs/remotes/origin/$ref` does not resolve (the case for a `--bare` fork clone like `kriscendobot-endo.git`, where fetched branches live under `refs/heads/*`, not `refs/remotes/origin/*`), `git rev-parse` echoes the unresolved argument to stdout before exiting nonzero. The `||` fallback then also succeeds and prints the real SHA, so command substitution concatenates both, producing the malformed two-line `new_sha` value `refs/remotes/origin/master\nf859ca06796902d693fb4f0a34ded48fdf4461ff`. This is logged at line 89 and handed to `$GARDEN_TRIAGE_HANDLER` as its new-sha arg, which then dies with `fatal: ambiguous argument 'refs/remotes/origin/master\n…': unknown revision or path not in the working tree`, tripping the failure counter (1/5). Fix: change both rev-parse invocations to `git --git-dir="$BARE" rev-parse -q --verify "<ref>" 2>/dev/null` (add `-q --verify`) so a failed resolution emits no stdout and only a genuinely resolved single SHA is captured; keep the existing `|| die "cannot resolve ref '$ref' in $slug"` for the case where neither ref resolves. Also worth confirming the fetch/ref-resolution assumption for bare clones — if `refs/remotes/origin/$ref` is never expected to exist for these fork clones, the primary branch could be dropped entirely, but adding `-q --verify` is the minimal, safe fix that eliminates the concatenation regardless. Add a triager-test case (see `scripts/jobs/test/triager-test.sh`) covering a bare clone whose watched ref only exists under `refs/heads/*`, asserting `new_sha` is a single 40-hex SHA with no embedded newline.

---
claim:
  host: endolin-garden-ece02cb4
  gardener: 9
  claimed_at: 2026-07-10T07:36:51Z
