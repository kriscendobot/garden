In `scripts/jobs/handlers/issue-source-gh.sh`, the two HARD-FAIL enumeration pipelines — the NEW ISSUES query (line ~76, `gh_api_retry --paginate "repos/$repo/issues?…" 2>/dev/null | jq … || die "issues enumeration … failed"`) and the NEW ISSUE-COMMENTS query (line ~100, `gh_api_retry --paginate "repos/$repo/issues/comments?…" 2>/dev/null | … || die "issue-comments enumeration/join …"`) — redirect `gh_api_retry`'s stderr to `/dev/null`. That discards the `WARN: gh api … failed (definitive/exhausted, rc=…): <stderr>` diagnostic that `gh_api_retry` (scripts/jobs/common.sh:1796) emits to stderr on the definitive/exhausted path, which is the ONLY record of *why* the call failed (401/403, secondary rate-limit, DNS/TLS reset, 5xx). Result: when the tick dies (`issue-source-gh.sh:82`), the death report carries a bare `rc=1` with zero root-cause — the exact failure just observed on garden-issue-inbox, undiagnosable.

Fix: drop `2>/dev/null` on the gh_api_retry call in BOTH top-level enumeration pipelines so gh_api_retry's own WARN/log lines flow to stderr, where the watcher already captures the source's stderr into ERRF and surfaces it prefixed `source:` (issue-inbox-watcher.sh:459-476). This is safe: gh_api_retry keeps the payload strictly on stdout and only routes its controlled `log` diagnostics to stderr (it never leaks gh's raw stderr except through those WARN lines), so jq's stdin stays clean and no "expected-empty 404/idle" noise appears on success. Do NOT touch the `issue_meta` per-comment lookup (line ~94, `gh_api_retry "repos/$repo/issues/$n" 2>/dev/null | … || true`): that failure is deliberately tolerated, so suppressing its WARN noise is legitimate. Mirror the watcher's own "do NOT 2>/dev/null it" rationale (issue-inbox-watcher.sh:460, the 2026-06-24 silent-empty outage) in a brief updated comment on the handler's stderr policy block (lines ~59-63), since the current comment justifies suppressing gh_api_retry's WARN lines and would otherwise invite re-adding the redirect. If a regression/handler test exists for this source, add a case asserting a forced definitive gh failure (stubbed `GARDEN_GH`) surfaces its stderr in the tick's death output.

---
claim:
  host: endolin-garden-ece02cb4
  gardener: 5
  worker_kind: cleric
  claimed_at: 2026-07-16T22:55:24Z
