# reconcile-v1-migration

Reconcile the v1→v2 garden migration and run the supervised runthrough before
the first `main2` commit. Deferred from the build session of 2026-06-24.

## Scope / checklist

- **Manifest coverage.** Cross-check `designs/v1-migration-manifest.md` against
  what landed: every TRANSLATE role (18) and skill (~39) present exactly once in
  `roles/` / `skills/`; no LEAVE_BEHIND item leaked in; no CARRY_VERBATIM juror
  altered (33 in `roles/jurors/`).
- **No overwrites.** Confirm the translators did not clobber the hand-built v2
  files (`skills/{job-board,message-bus,schedule,panel,dispatch-worktree,worktree-per-pr}`,
  `roles/{gardener,triager,watchman,liaison,improver}`).
- **Link integrity.** Resolve every `[name](path)` link in roles/skills; the
  remaining plain `[name]` refs should only be not-yet-present targets.
- **Flagged overlaps / anomalies:**
  - `panel-review` vs the new `panel` skill — decide consolidate vs keep-scoped.
  - `em-dash-style`, `no-latin-shorthand`, `relative-paths`,
    `test-title-spec-spelling` exist as skills despite a LEAVE_BEHIND manifest
    mark (folded into `COMMON.md`) — confirm intended.
  - COMMON.md still has a pre-existing `## Style` section alongside the new
    `## House style` — decide whether to merge.
  - Translated schedules name v1 roles `scholar` / `journalist`
    (`journal/schedules/{scholar-library-cycle,daily-progress-summary}`) — confirm
    those roles' v2 disposition so the dispatched jobs resolve.
- **Supervised runthrough.** `bash -n` all `scripts/jobs/*.sh`; run
  `scripts/jobs/test/run-test.sh` (expect 12 subtests / 40 checks green); spot-read
  a few translated roles/skills for posture correctness (no steward/driver/gamut
  residue beyond explicit migration notes).

## Acceptance

A short reconciliation report (carried/translated/left-behind counts reconciled,
anomalies resolved or ticketed), the test suite green, and a recommended scope
for the first `main2` commit (new v2 files only, vs a broader cut).

## Context

Design: `designs/job-board.md`, `designs/gardening-state-machine.md`,
`designs/judicial-workflow.md`. Scripts: `scripts/jobs/`. The journal (this
branch, `journal2`) carries the migrated v1 record; `journal-v1` retains the
original.

---
claim:
  host: endolinbot
  gardener: 1
  claimed_at: 2026-06-24T08:22:11Z
