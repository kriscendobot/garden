Harden `scripts/jobs/scholar-preflight.sh` condition #3 (the `role/scholar` broadcast freshness check, lines ~52-67) so a *fresh* broadcast counts as work-present only when its body carries an actionable scholar marker — i.e. a `library_action: ingest-source` (or other ingest `library_action:`) ask, or a writeback-review request — not merely a `sent_at` newer than the schedule's `last_dispatched`. Today the timestamp-only test lets purely informational broadcasts (deterministic-projection notices, deploy/leadership broadcasts — e.g. `msgs/role/scholar/20260629T154517Z-423c15.md`, which has no `library_action:` field) trip the gate, dispatching an idle scholar LLM cycle that drains and exits with nothing, defeating the gate's stated purpose ("moves the scholar's idle/active decision off the dispatched LLM agent ... eliminating back-to-back idle scholar dispatches"). Verified live: the 2026-06-29T16:35:14Z scholar-library-cycle was such a leaked idle dispatch (journal entry `entries/2026/06/29/163807Z-result-gardener-ef7244.md`). Fix: in the condition-#3 loop, after confirming `sts > last`, grep the message file for the actionable marker(s) before `exit 0`; if a fresh broadcast lacks any actionable marker, `continue` instead of dispatching. Derive the exact set of actionable markers from `roles/scholar/AGENT.md`'s inbox/topic-handling predicate so the gate and the role agree on what "ingest work" means; keep the gate read-only and fail-open on parse ambiguity. Add a case to `scripts/jobs/test/` (mirroring the existing scholar-preflight test if present) covering: fresh informational broadcast → exit 2, fresh `library_action: ingest-source` broadcast → exit 0.

---
claim:
  host: endolinbot2
  gardener: 18
  claimed_at: 2026-06-29T16:52:15Z
