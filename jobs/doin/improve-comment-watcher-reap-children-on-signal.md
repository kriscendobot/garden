Harden `scripts/jobs/comment-watcher.sh` against the recurring `garden-comment-watcher@*.service: Found left-over process <pid> (git) in control group while starting unit` warning (seen this cycle on `@endojs-endo-but-for-bots`, three orphaned git pids). Cause: the only trap is `trap 'rm -f "$SRC" "$ERRF"' EXIT` at line 583 — it has no `INT`/`TERM` arm, so a systemd stop/restart that SIGTERMs the oneshot mid-tick never runs cleanup and never reaps the `gh`/`git` descendants spawned by `handlers/comment-source-gh.sh` (`gh_api_retry --paginate`, which forks git credential helpers); they orphan into the unit cgroup and are flagged "left-over" on the next start. Fix, mirroring the already-landed gardener pattern (commit b3074e154, KillMode=mixed + SIGTERM trap): (1) extend the trap to fire on signals and reap the process group, e.g. `trap 'rm -f "$SRC" "$ERRF"; kill -- -$$ 2>/dev/null || true' EXIT INT TERM` (run the script in its own process group, or use `pkill -P $$`), so a SIGTERM both cleans temp files and kills child gh/git; and (2) bound the source invocation with `timeout --signal=TERM --kill-after=10s <N>s "$GARDEN_COMMENT_SOURCE" …` at line 587 (and the self-test `gh` call in `source_path_healthy`) so a hung gh/git fetch can never outlive the tick even absent a stop. Add `KillMode=mixed` to `scripts/systemd/garden-comment-watcher@.service` to match. Extend `scripts/jobs/test/comment-watcher-test.sh` with a case asserting no child processes survive after a SIGTERM to the watcher. Build in an isolated worktree off origin/main2 per the garden-infra-jobs convention.

---
claim:
  host: endolinbot2
  gardener: 25
  claimed_at: 2026-06-29T20:51:56Z
