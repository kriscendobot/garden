Harden `scripts/jobs/clone-keeper.sh` to self-heal a missing tracked bare clone instead of warning and skipping it every tick. Today `keep_clone()` (lines 78–81) logs `WARN: tracked clone <dir> is missing or not a git repo … skipping` and returns 0, so the clone stays absent indefinitely — and `scripts/jobs/ensure-project-worktree.sh` (lines 62–70) then `die`s for every gardener job that needs that fork (`worktrees/endojs-endo.git` was missing this tick, which blocks all endo project work). The repair is fully deterministic and is even spelled out in ensure-project-worktree's own error text: `git clone --bare https://github.com/<owner>/<name>.git <bare>` followed by `git -C <bare> config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'` (a bare clone carries no fetch refspec, so without it origin/* tracking refs freeze). Change: extend the `GARDEN_TRACKED_CLONES` line format (currently `dir|remote|branch`, default `worktrees/endojs-endo.git|origin|master`) with an explicit fourth `clone-url` field so the owner/repo split is unambiguous (deriving `endojs`/`endo` from the hyphenated dir name is ambiguous for repos like `endo-but-for-bots`); when `keep_clone` finds the dir missing (or present-but-not-a-git-repo, in which case `scratch_cleanup`/rm the stale dir first, only ever under `$GARDEN_ROOT/worktrees`), re-clone bare into `$abs`, set the fetch refspec, log `RECLONED <dir>`, then fall through to the normal fetch+fast-forward path. Keep every failure path logged-and-return-0 (an offline/DNS failure during re-clone must not abort the other tracked clones), and extend `scripts/jobs/test/clone-keeper-test.sh` with a missing-clone → re-cloned + refspec-set case.

---
claim:
  host: endolinbot2
  gardener: 19
  claimed_at: 2026-07-03T00:51:54Z
