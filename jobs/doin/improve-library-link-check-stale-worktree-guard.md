Harden `/home/kris/scripts/jobs/library-link-check.sh` so it cannot emit a phantom must-resolve FAIL when run against a stale live worktree — the exact failure the scholar hit on 2026-06-27 (job `scholar-library-cycle-20260627-175523`), where `--all` against `$GARDEN_ROOT/journal/library` reported "FAIL — 12 must-resolve dangling links" purely because that worktree was missing commit `9840fa1db` (the endoclaw fix). Root cause: the whole-library validating scopes (`--all` and `--nav`) are meaningful only against the `origin/journal2` tip, but the script's default `--library` resolves to `$GARDEN_ROOT/journal/library` (the long-lived, arbitrarily-stale live worktree), and there is no staleness check (grep confirms no `origin/journal2`/`merge-base`/`behind` logic in the resolver path). Fix: after `GIT_ROOT` is computed (line 131), when the scope is `--all` or `--nav` and the library lives in a git repo, do a no-network local check — `git -C "$GIT_ROOT" merge-base --is-ancestor origin/journal2 HEAD` (or compare `rev-parse HEAD` vs `rev-parse origin/journal2`). If the worktree is behind the local `origin/journal2` ref, print a loud one-line warning naming `library-link-scan.sh` (the wrapper that fetch+resets a dedicated clone to tip) as the correct tool and exit 2 (setup error) rather than running a validating scan that produces phantom FAILs. Leave `--changed`/`--source-slug`/`--files` unaffected — those run against the producer's own fresh clone or are explicitly scoped. This moves the "never point `--library` at the live worktree" rule out of agent memory and into deterministic code; keep the existing `# This script makes NO writes and NO network calls` invariant by comparing only against the already-fetched local `origin/journal2` ref (no `git fetch`).

---
claim:
  host: endolinbot
  gardener: 37
  claimed_at: 2026-06-27T18:09:13Z
