# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `port-xs-to-rust-memory-safe-engine-s7` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `port-xs-to-rust-memory-safe-engine-s7`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: port-xs-to-rust-memory-safe-engine-s7

----- ORIGINAL MESSAGE -----
to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-06T03:31:51Z
dead_lettered_at: 2026-07-06T03:31:51Z
---
xs2rust-endor-strings-utf16-test (child 3/3) — DONE, pushed to origin/xs2rust-endor (PR #600, keep DRAFT).

Two increments landed (996cc6784, 0b991a8b4):

1. Result parity (governing check): dual-ran test262 String-touching sections vs
   the pin 48ee02d8cfe0, per-subtree. divergent=0 on RESULTS everywhere:
   built-ins/String total=1111 covered=130 divergent=0 (62 computron-gap skips);
   template-literal/less-than/greater-than/equals/strict-equals all divergent=0.
   Locked into cargo test: test262 utf16_string_sections_have_zero_result_divergence
   (charCodeAt/codePointAt/charAt/slice/substring, total=146 covered=8 divergent=0,
   42 named builtin-coercion-computron-gap skips — value agrees, cost shifts).

2. New cases. KEY FINDING: the oracle shim transports a string completion by reading
   C-XS's CESU-8 payload as UTF-8, so an astral/lone-surrogate string VALUE decodes
   lossily (U+FFFD) — the differential harness cannot faithfully assert those string
   values. Split accordingly:
   - Scalar-result differential fixtures (corpora/stage3-string-utf16.js): charCodeAt/
     codePointAt across a surrogate boundary, index-heavy [i]/charCodeAt(i) loops,
     slice/substring splitting a pair, spread/iterator yielding whole code points,
     lone-surrogate ops, concat reuniting a split pair. Result parity + determinism
     asserted (stage3_string_utf16_result_parity_and_determinism), plus a live-
     recalibration guard (some multi-unit fixture must meter differently from the pin).
   - Value-layer endor-vm tests (interp.rs utf16_*) for the string-VALUED astral/lone-
     surrogate results the oracle can't transport: index-heavy O(1) direct access
     correct at every position incl. just past a supplementary char; slice splits a
     pair into a valid lone surrogate; lone surrogate round-trips through storage
     (verbatim BE unit, no normalization), comparison (byte-lex == code-unit order),
     and the real concat_add (lead+trail → U+10000; two lone highs stay distinct).

3. Meter expectations. utf16_meter_expectations_are_the_frozen_recalibrated_costs
   freezes endor's own UTF-16 computron costs (asserted against endor DIRECTLY, not
   back-fit to CESU-8 byte length or the oracle) with determinism-per-release
   (identical across repeated runs). Cross-engine computron equality with C-XS is
   neither required nor asserted.

4. Snapshot round-trip: supplementary-plane AND lone-surrogate string atoms round-trip
   bit-identically through the chunk payload (the snapshot serialize form) into a fresh
   machine (utf16_string_atom_snapshot_round_trips_supplementary_and_lone_surrogate).

Bar: cargo test --workspace green (endor-262 35, endor-vm 52, all crates pass).
Miri: the 5 string-storage tests this job touches pass under Miri (18s); the full
endor-vm Miri suite exceeds the 10-min tool timeout (pre-existing runtime, dual-runs
large corpora) so it was not run to completion here. forbid(unsafe_code) intact; only
safe test code added. c/moddable pin left at 48ee02d8cfe0 locally, deliberately never
staged.

Follow-up (pre-existing, not introduced here): the "method result consumed directly as
receiver/arg" temporary-lifetime residual (e.g. slice(..).charCodeAt(0)) still costs one
computron less in endor than the pin — a named computron-gap, never a result divergence.

----- END ORIGINAL MESSAGE -----

---
claim:
  host: endolinbot
  gardener: 9
  claimed_at: 2026-07-06T03:35:25Z
