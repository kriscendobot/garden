Add the Claude Code session/usage-limit signature to the canonical transient-`claude -p` set `GARDEN_TRANSIENT_CLAUDE_SIGNATURES` in `scripts/jobs/common.sh:852` (consumed by `is_transient_claude_signature`, line 860). On 2026-07-01T00:26–00:50Z, 9 unrelated gardener jobs on endolinbot2 all failed with byte-identical captured output "You've hit your session limit · resets 1:10am (UTC)" (blob 35b3d8ab…); because that wording matches none of the current alternatives (`overloaded|rate[ _-]?limit|connection error|429|5xx|api[ _-]?error|econnreset|etimedout`), every one fell through to the real-failure branch in `gardener.sh:357` — escalated to the gardener inbox as lane-0 `handler-nonzero` defects and left in `doin`, where the reaper requeues them to re-fail identically until the cap resets and poison-counts toward a false maintainer escalation. A session/usage cap that names its own reset time is the definitive self-resolving transient. Extend the regex to also match, case-insensitively, the Claude Code cap wording, e.g. add alternatives `hit your (session|usage) limit|(session|usage|5-hour) limit (reached|reset)|resets [0-9].*\(utc\)`. Because the var is the single source of truth, both the gardener inner-claude classifier and `follow-up-claude.sh` inherit the fix; add a case to `scripts/jobs/test/` (alongside the existing timeout/signal-kill classifier tests) asserting the exact captured string classifies transient (no inbox escalation, reaper-requeue path). Optional follow-on worth a code comment but not required for this job: when the signature carries an explicit reset time, back off the requeue until that time instead of re-failing every TTL cycle.

---
claim:
  host: endolinbot2
  gardener: 29
  claimed_at: 2026-07-01T01:23:01Z
