In `scripts/jobs/common.sh:732`, `GARDEN_OFFLINE_SIGNATURES` enumerates curl/git transient phrasings (`Connection timed out`, `Operation timed out`, `Connection reset by peer`, …) but omits the Go `net`-package phrasings that the `gh` binary actually emits on a transport blip. On 2026-06-29 21:14:03 `gh` printed `Get "https://api.github.com/...": dial tcp 140.82.116.5:443: i/o timeout`; `_gh_api_stderr_is_transient` (line 786) returned false, so `gh_api_retry` (line 794) logged "definitive, rc=1); not retrying" and returned the rc instead of riding the blip out under backoff. `mirror-pr-state-gh.sh`'s `die` then fired, `mirror-closer.sh` exited 1, and `garden-mirror-closer.service` was marked Failed — a contradiction with its own "will retry next tick" intent, and a self-heal/start-limit noise source on every transient network hiccup. Add the Go-net transient phrasings to `GARDEN_OFFLINE_SIGNATURES` (the shared source of truth, also used on the git offline path): `i/o timeout`, `dial tcp`, `TLS handshake timeout`, `context deadline exceeded`, `connection refused`, `network is unreachable`, `no route to host`. These are unambiguously transport-level transients, so they belong in the same retry class as the existing curl/git phrasings. Verify with a unit check that the captured `endojs/endo#3137` stderr string now matches the pattern, so the same blip retries instead of killing the unit.

---
claim:
  host: endolinbot2
  gardener: 71
  claimed_at: 2026-06-29T21:22:38Z
