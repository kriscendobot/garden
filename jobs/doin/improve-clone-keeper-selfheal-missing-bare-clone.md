Harden `scripts/jobs/clone-keeper.sh` so a *missing* tracked bare clone self-heals instead of warning-and-skipping forever. Today `keep_clone()` hits the `git -C "$abs" rev-parse --git-dir` guard and just logs `WARN: tracked clone <dir> is missing … skipping` on every :00/:30 tick — which currently fires for `worktrees/endojs-endo.git` (genuinely absent on this host), leaving endo upstream re-ingestion fully blocked, the exact stale-clone failure this keeper's header says it was built to make impossible. Change: extend the `GARDEN_TRACKED_CLONES` spec from `<dir>|<remote>|<branch>` to `<dir>|<remote>|<branch>|<url>` (default the endo row to `https://github.com/endojs/endo.git`, matching the recipe already documented at `ensure-project-worktree.sh:66`); when the clone dir is absent and a `<url>` is present, deterministically re-establish it with a bounded `timeout "$GARDEN_FETCH_TIMEOUT" git clone --bare <url>` into a temp path + atomic `mv` into place (so a partial/racing clone never clobbers or half-populates the tracked path), then fall through to the normal fetch/fast-forward. On clone failure (offline/DNS) keep the current warn-and-skip so one unreachable clone never aborts the loop; if no `<url>` is configured for a row, preserve today's warn-and-skip. Keep the existing "not a git repo but dir exists" case a loud warn (don't auto-clobber a non-empty non-repo dir). This moves the recreate judgement off any agent and onto the cadence timer, so a deleted bare clone can never silently wedge re-ingestion.

---
claim:
  host: endolinbot2
  gardener: 9
  claimed_at: 2026-07-03T02:51:58Z
