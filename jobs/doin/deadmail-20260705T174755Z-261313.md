# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `port-xs-to-rust-memory-safe-engine-s7` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `port-xs-to-rust-memory-safe-engine-s7`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: port-xs-to-rust-memory-safe-engine-s7

----- ORIGINAL MESSAGE -----
to: port-xs-to-rust-memory-safe-engine-s7
from_host: endolinbot
from: inbox-send
sent_at: 2026-07-05T17:47:56Z
dead_lettered_at: 2026-07-05T17:47:56Z
---
stage-3b object-statics/intern (child 5/9), PR #600 — progress, NOT yet complete (requeuing for Object.defineProperty).

LANDED & PUSHED to xs2rust-endor (3 commits, all zero-divergence, corpus+fuzz+CI-gate green):
1. 10888be66 — interning AT/AT_2 opcode: computed string member access o[k] now resolves ANY string key
   through the global intern table. Program symbol → resolves as its o.name static access does; genuinely-novel
   name → interns one fxNewSlot slot, reads bit-exact undefined; index-string → XS's 2× code metering. SOUNDNESS
   GUARD: a boot default-key name the program never referenced (o["hasOwnProperty"]) self-names Halt::Unsupported
   rather than risk a wrong undefined (endor's %Object.prototype% carries a method only for program-referenced
   names — the dual-run CAUGHT this, fixed before landing).
2. 46e6c2f36 — sound `in` false-answers: XS_CODE_IN reworked (fxRunIn→fxHasAt=fxAt+fxHasAll). Novel key → sound
   false + one slot; default-key-non-symbol → self-name (same guard). Metered the fxOrdinaryHasProperty recursion
   exactly: +1 XS_CODE_METERING per prototype level descended (own-hit 0, ordinary-miss/immediate-proto 1).
3. 0bb42b19c — README evidence block.

EVIDENCE: built-ins/Object dual-run grew covered=48→53 divergent=0 (+1 at/at_2, +4 in). covered-grammar CI gate
green; endor-vm/262/fuzz suites green. Curated corpus (stage3b-object-statics.js) + differential fuzz arm both
extended with computed-access and in arms.

REMAINING (the named bar item): Object.defineProperty. NOT started — it needs the attribute-aware property model
(the Slot.flag byte already exists: XS_DONT_DELETE=2/DONT_ENUM=4/DONT_SET=8; flag==0 today = writable/enumerable/
configurable all true) rippling through keys (own_enumerable_ids currently rejects flag!=0) and gopd (currently
hardcodes true/true/true for flag==0). The blocker is METERING: XS fx_Object_defineProperty → fxDescriptorToSlot
reads SIX fields {enumerable,configurable,value,writable,get,set} in that order, EACH a mxHasID (fxHasAll chain
walk, same shape as my `in` metering) + a mxGetID when present + fxToBoolean per present flag, then
mxBehaviorDefineOwnProperty. So the cost is per-field-present, not a single residual — needs careful isolate-the-
gap calibration against the pin (oracle raw for `Object.defineProperty(o,"x",{value:1})`=3378280; all-4-attrs
=3968880). This is a full dedicated increment; a hasty single-constant residual would be a fitted meter (forbidden).
Recommend the next handler owns defineProperty end-to-end. Not emitting the completion signal — job requeues.

----- END ORIGINAL MESSAGE -----

---
claim:
  host: endolinbot
  gardener: 39
  claimed_at: 2026-07-05T17:50:14Z
