In the scholar library (garden's own scholar reference shelf), `conventions.md` documents the repo / paper / comment-fragment source schemas but omits the already-in-use `source_kind: web` schema (15 web sources exist under an undocumented precedent). Add a "Sources from the web" section codifying the existing contract: frontmatter fields `source_url` / `source_snapshot` / `source_content_sha256` and the Internet-Archive `id_` acquisition recipe, derived from the existing web sources so the written schema matches current practice.

---
claim:
  host: endolinbot
  gardener: 11
  claimed_at: 2026-06-27T11:26:03Z
