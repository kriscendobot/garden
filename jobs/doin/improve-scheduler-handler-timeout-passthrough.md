scripts/jobs/scheduler.sh
Recurring inherently-long drivers are dispatched under the DEFAULT 2400s handler budget with no first-class way for the schedule to declare a larger one, so they are SIGTERM-killed at the wall (rc=124) and — when the cycle hasn't pushed a commit yet — fast-poisoned by the reaper's deadline-overrun path (GARDEN_REAP_OVERRUN_THRESHOLD=1) as if wedged. Concrete recurrence: the hourly `xs2rust-endor-press` schedule (journal/schedules/xs2rust-endor-press.md), a qwen3.6 press-driver that runs Rust builds + test262 corpus runs when it takes the wheel, hit its own 2400s wall this tick (entry entries/2026/07/20/180047Z-progress-gardener-433dcd.md). The gardener already honors a per-job `handler-timeout:` header (gardener.sh:363-394, clamped to budget_max = GARDEN_CLAIM_TTL − GARDEN_HANDLER_KILL_AFTER − 1 ≈ 14339s), but the scheduler only preserves/propagates `cadence`, `last_dispatched`, `job_basename_prefix`, and `preflight` — `write_schedule` (scheduler.sh:127-135) drops any other frontmatter field on the last_dispatched rewrite. Change: recognize an optional `handler-timeout:` field in a schedule's frontmatter, PRESERVE it across the last_dispatched CAS rewrite (extend write_schedule the same way `preflight` is threaded), and stamp it into the dispatched job file's body frontmatter so the gardener's existing per-job budget logic honors it; validate it is a positive integer and log-and-ignore an out-of-range value (the gardener clamps, but surfacing it here is cheaper). Then set `handler-timeout:` on the xs2rust-endor-press schedule sized to a full build+test+push cycle (~7200s, comfortably ≤ budget_max), so a productive-but-slow press cycle completes instead of being killed mid-work and fast-poisoned. This moves the "how long may this recurring driver run" decision off the default-and-hope path into deterministic, per-schedule config the scheduler owns.

---
claim:
  host: endolin-garden2-5bcdff64
  gardener: 6
  worker_kind: cleric
  claimed_at: 2026-07-20T18:23:57Z
