Harden `scripts/jobs/journal-worktree-keeper.sh` to REPAIR a broken `$GARDEN_ROOT/journal` worktree instead of warn-and-skip. At the guard in `keep_journal_worktree()` (currently lines 234–237: `if ! git -C "$JW" rev-parse --git-dir …; then log "WARN: … skipping"; return 0; fi`), before giving up, detect the "dangling gitfile" case (`$JW/.git` exists as a gitfile whose `gitdir:` target is missing) and run `git -C "$GARDEN_ROOT" worktree repair "$JW"`; re-check `git -C "$JW" rev-parse --git-dir`, and only if it still fails, page the maintainer (or recreate the worktree via `git worktree add`). This is the failure captured in self-heal blob cc3c77b: after the garden tree moved from `/home/kris` to `/home/kris/garden2`, the journal worktree's links still pointed at `/home/kris/.git/worktrees/journal` and `/home/kris/journal/.git` (both gone), so `git -C $GARDEN_ROOT/journal …` returned "not a git repository", `common.sh:journal_remote()` mis-reported it as "no origin", and `unblock.sh` (via `ensure_clone`/`sync_clone`) exited 1 — a failure shared by every journal-touching service, recurring every tick. Secondary hardening: in `common.sh:journal_remote()` (line 492), distinguish a broken worktree (git exit 128, "not a repository") from a genuinely missing `origin` remote (exit 1) so the `die` message is actionable rather than misleading. A `git worktree repair` on the live tree already confirms the fix (repair succeeds and `origin.url` resolves), so the keeper can perform it deterministically and losslessly.

---
claim:
  host: endolinbot2
  gardener: 20
  claimed_at: 2026-07-03T15:51:42Z
