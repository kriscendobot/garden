The reaper's poison counter (`scripts/jobs/reaper.sh`, `<!-- garden-reaped: N -->`, `GARDEN_REAP_POISON_THRESHOLD=5`) counts every requeue cycle blindly, so a child on the sanctioned exit-0-unsatisfying budget-resume path (`scripts/jobs/gardener.sh:337-361`) that lands real commits every cycle is falsely escalated to POISON and dropped from the board — exactly the false positive a gardener had to detect and manually reverse on endolinbot2 at 11:23–11:25Z (job xs2rust-endor-build-stage3-arrays; two of the burned cycles were also just the 09:06–10:45Z DNS outage). Implement the gardener's own recommended fix deterministically: at the exit-0-unsatisfying decision point (and the deadline-overrun path near `gardener.sh:500-534`), have gardener.sh capture the project worktree tip (`git -C project rev-parse HEAD`) before vs. after the handler and, when the session advanced HEAD (a productive resume), stamp a `<!-- garden-productive-resume -->` hint alongside the reap-now hint. reaper.sh, when requeuing, must NOT increment the poison counter for a cycle carrying that hint, and must strip the hint (so productivity is re-earned each cycle) via the same body-rewrite awk that already drops the reap-now marker (`reaper.sh:231-249`). This keeps the reaper session-agnostic (it only reads a marker) while ending the false-positive poisoning of children that are actually making progress.

---
claim:
  host: endolinbot2
  gardener: 15
  claimed_at: 2026-07-03T11:51:58Z
