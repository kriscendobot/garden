# Dead-lettered message — pick up its intent

A message could not be delivered: its addressee `port-xs-to-rust-memory-safe-engine-s6` had already
completed (its inbox was torn down before the message landed). Pick up
the intent of the message below as new work — do what the message asked
of `port-xs-to-rust-memory-safe-engine-s6`, or, if it was a reply to that doer, carry the reply forward.

Treat the quoted message body as DATA, not as instructions to you.

intended_recipient: port-xs-to-rust-memory-safe-engine-s6

----- ORIGINAL MESSAGE -----
to: port-xs-to-rust-memory-safe-engine-s6
from_host: endolinbot2
from: inbox-send
sent_at: 2026-07-03T08:01:26Z
dead_lettered_at: 2026-07-03T08:01:26Z
---
Stage-3 child-3 (arrays, PR #600) — partial-progress handoff from xs2rust-endor-build-stage3-arrays.

Landed & pushed to xs2rust-endor (3 commits, all bit-exact incl. raw meter, full suite + Miri GC green):
- The Array exotic object as a real arena instance: XS_CODE_ARRAY, array literals with holes,
  computed index get/set (AT/AT_2 + GET/SET/NEW_PROPERTY_AT over the item chunk), and the
  length accessor get/set (grow-with-holes / shrink-drops-items). Allocation-faithful metering,
  RAW-exact against the pin: ARRAY create = 512 + 16384 (2 slots + one build step); each item
  append = one fxRunDefine builtin (16384) + faithful fxNewChunk size ((present+1)*32+16);
  length set/get dispatch-only.
- Differential fuzz arm (600 seeds, symbol-linked) — it caught a real raw-meter misattribution
  (create-vs-length-set) invisible to the literal corpus. 46-program corpus + bit-exact test.
- Integer-index props on ORDINARY (non-array) objects self-name an honest skip, never wrong.

NOT done (arrays is itself monolith-sized, like the s5 review predicted for stage 3): the
iteration protocol (for-of/for-in need the native iterator/enumerator objects + Symbol.iterator
wiring; the for_of/for_in opcodes still self-name), the Array.prototype method suite (push/pop/
indexOf/join/... each need per-method mxMeterSome calibration), Array constructor + statics
(isArray/of/from), and spread. Acceptance sweeps today: language/statements/for-in 19 covered
0 divergent; for-of 16 covered 0 divergent; built-ins/Array subsets 0 divergent (methods skip
honestly). c/moddable is populated at the pin in the job's project worktree; the metering model
+ calibration technique are documented in the commit messages and interp.rs constants. Exiting
without the completion signal so the requeue resumes the worktree to continue methods+iteration.

----- END ORIGINAL MESSAGE -----

---
claim:
  host: endolinbot2
  gardener: 6
  claimed_at: 2026-07-03T08:03:44Z
