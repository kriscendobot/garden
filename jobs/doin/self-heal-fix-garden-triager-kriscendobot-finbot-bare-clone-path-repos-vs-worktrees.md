Fleet-wide triager failure: `scripts/jobs/triager.sh` dies with `FATAL: no bare clone at $GARDEN_ROOT/repos/<slug>.git (clone the repo first)` for every watched repo (finbot, agoric-3-proposals, minion.town, ocapn, vattr97 all confirmed failed with this signature). Root cause: `triager.sh:24` defaults `GARDEN_REPOS` to `$GARDEN_ROOT/repos`, but the standing bare clones live under `$GARDEN_ROOT/worktrees/<owner>-<name>.git` (maintained by `clone-keeper.sh`, per WORKTREES.md). `repos/` is only the journal watch set, never a filesystem clone pool, so `$GARDEN_REPOS/$slug.git` never resolves and `triager.sh:32` `die`s.

Fix: point the bare-clone lookup at where clones actually live. Change the default in `scripts/jobs/triager.sh:24` (and the identical `scripts/jobs/comment-watcher.sh:179`) from `: "${GARDEN_REPOS:=$GARDEN_ROOT/repos}"` to `: "${GARDEN_REPOS:=$GARDEN_ROOT/worktrees}"` — since `slug == <owner>-<name>`, `$GARDEN_REPOS/$slug.git` then resolves to the existing `worktrees/<owner>-<name>.git` (verified: `worktrees/kriscendobot-finbot.git` is a valid bare repo with `refs/remotes/origin/main`). Preserve the env-override contract — the comment-watcher tests pass an explicit `GARDEN_REPOS=$TR/norepos`, so only the default may change; if a canonical worktrees-dir variable exists in `common.sh`, prefer reusing it over a raw `$GARDEN_ROOT/worktrees` literal. Consider whether `triager.sh` should also degrade like `comment-watcher.sh:312` (log note) rather than FATAL when a clone is genuinely absent, but the primary fix is the path default. Verify with the existing `scripts/jobs/test/comment-watcher-test.sh` suite and by confirming a triager tick against finbot advances its cursor instead of dying.

---
claim:
  host: endolin-garden2-5bcdff64
  gardener: 12
  claimed_at: 2026-07-11T01:55:00Z
