---
role: shepherd
---

Run the gauntlet (clean → panel review → fix-loop → un-draft) on `endojs/endo-but-for-bots` DRAFT PR #691 `design: accept and sequence the git-capability stack for the version-controlled-filesystem loop (M3)` (base `llm`), driving this design from green-draft to reviewed-and-un-drafted so the accepted, dependency-ordered git-capability build plan unblocks the remaining M3 git builds (#645, #646) following the just-landed exo-git path-type consolidation (#643). Treat the known repo-wide lint `projectService` ceiling (#594) as pre-existing and out of scope.

<!-- garden-reap-now -->
---
claim:
  host: endolin-garden-ece02cb4
  gardener: 9
  claimed_at: 2026-07-12T06:52:49Z
