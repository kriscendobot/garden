In scripts/jobs/gardener.sh, the handler-failure classifier only treats signal-kill exit codes (143 SIGTERM / 137 SIGKILL/OOM / 130 SIGINT) as transient on the empty-$capture branch (via is_transient_empty_failure in common.sh). A gardener killed mid-job that already flushed partial output to $capture (progress lines, or the folded tail of $report) skips that branch and is falsely escalated to the gardener inbox as a kind:error real failure — exactly what happened on 2026-06-27 to job garden-deliberate-deploy-no-shared-tree-development (rc=143, escalated). An external signal-kill is never a deterministic job defect (it is a deploy-window restart, drain-fleet, OOM, shutdown, or claim-TTL kill); the reaper already requeues the job after GARDEN_CLAIM_TTL. Fix: classify a signal-kill rc as transient BEFORE the `[ ! -s "$capture" ]` empty/non-empty split, so capture content is irrelevant for these codes. Add a small `is_external_kill_rc()` helper to common.sh covering 143|130|137 (NOT the offline rc, which should stay gated on its existing paths) and consult it first in the gardener.sh classification block: set transient=1 when is_external_kill_rc "$rc"; only then fall through to the existing empty-capture (is_transient_empty_failure) and claude-signature (is_transient_claude_signature) tests for non-signal rcs. Keep the existing transient-branch journal note (it already records rc and reap_count, so a job that signal-dies the same way every cycle stays greppable). Net effect: a mid-job kill with partial output is logged as a transient handler outage and left in doin for the TTL requeue, instead of spamming the error journal and gardener inbox. Add a test under scripts/jobs/test covering rc=143 with non-empty capture asserting no inbox escalation, mirroring the existing empty-capture signal-code coverage.

---
claim:
  host: endolinbot
  gardener: 29
  claimed_at: 2026-06-27T19:39:20Z
