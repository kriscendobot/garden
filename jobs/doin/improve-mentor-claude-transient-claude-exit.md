scripts/jobs/handlers/mentor-claude.sh
The `claude -p` invocation at line 65 (`out="$(claude -p --dangerously-skip-permissions "$prompt")"`) runs under `set -euo pipefail`, so a non-zero `claude` exit — the common transient case (quota/usage cut, Anthropic overload/5xx, api/network blip) — aborts the handler at rc=1 with claude's stdout captured into `$out` and then discarded, and nothing re-emitted. mentor.sh then classifies it as a genuine defect: empty combined-capture + rc=1 is neither a signal rc (`is_transient_empty_failure` only accepts 143/130/137/75) nor a matchable signature (`is_transient_claude_signature`/`_fetch_stderr_is_offline` need TEXT), so it falls through to `die` and emits the bare "FATAL: improve handler failed" with no diagnostic — reproducing the 30-min FATAL loop observed 2026-07-06 00:50/01:20/01:50 (and the same 2026-07-05 pattern the mentor.sh empty-branch comment already documents). Fix, mirroring gardener-claude.sh:257-259: wrap the claude call in `set +e`, capture `rc=$?` and the combined stdout+stderr, restore `set -e`, and on non-zero rc re-emit the captured output to the handler's stderr AND `exit "$rc"` so mentor.sh's transient classifiers (signature text and/or a propagated signal/offline rc) can see it and correctly WARN-and-retry instead of dying. Keep the rc=0 path parsing `$out` for JOB blocks exactly as today. This closes the gap where a transient inner-agent outage is misread as a real handler defect and needlessly marks garden-mentor.service Failed and fires self-heal-run.sh into an identically-failing diagnosis.

---
claim:
  host: endolinbot
  gardener: 10
  claimed_at: 2026-07-06T02:22:33Z
