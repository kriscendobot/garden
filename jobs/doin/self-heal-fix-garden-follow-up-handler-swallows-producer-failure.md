In `scripts/jobs/handlers/follow-up-claude.sh`, the parse loop runs each emitted action block as `printf '%s' "$body" | "$HERE/../post-job.sh" "$name"` (and the set-schedule / set-schedule-once / inbox-send variants) under `set -euo pipefail` with no capture, so any non-zero downstream exit (e.g. `post-job.sh`/`set-schedule.sh` `die` on an illegal `fu-<base>-<n>` name via `case … -*|*/*|.*|'')`, or `die "could not post … after retries"` on push contention) kills the entire tick. `follow-up.sh:115` then emits a bare `FATAL: follow-up handler failed`, never advances `$GARDEN_STATE/follow-up/seen`, and the same digest is retried every 10 min — re-rolling the non-deterministic `claude -p` output and re-failing whenever the roll produces a bad block (observed 07:53–08:25 on 2026-06-27, signature "follow-up handler failed; leaving markers so the next tick retries", with no underlying cause in the captured blob). Fix, scoped to this one handler: (1) capture `claude -p`'s exit status and stderr explicitly (`out="$(claude -p … 2>err)"; st=$?`) and `die` with the stderr tail when non-zero, so a future inner-agent failure is visible in the self-heal blob instead of vanishing; (2) in the parse loop, run each downstream producer capturing combined output+status (don't let the pipe's non-zero propagate under pipefail) and, on failure, `log` the offending block's state+name, the command, and its output tail, then continue to the next block rather than aborting the tick; (3) distinguish a deterministic per-block rejection (illegal name / malformed block) — which must NOT fail the tick, since re-rolling the same digest just wedges the seen-marker and self-heal forever — from a genuinely transient producer failure (push-retry exhaustion), returning non-zero only for the latter so the retry semantics still apply; optionally route an un-actionable/rejected block to the maintainer inbox so it isn't silently dropped. Add or extend a stub-handler test under `scripts/jobs/test/` asserting that a block whose producer exits non-zero is logged-and-skipped (tick succeeds, marker advances) while a simulated transient push failure still fails the tick for retry.

---
claim:
  host: endolinbot
  gardener: 57
  claimed_at: 2026-06-27T08:29:07Z
