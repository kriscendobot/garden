In `scripts/jobs/gardener.sh`, implement the elapsed-constancy early-escalation that the transient-branch comments (lines ~326–334) describe but never code. Today the branch computes `cycle="$(reap_count "$jobfile")"` and `elapsed` and only writes them into a kind:progress note; a job that deterministically overruns and dies with a transient-claude-signature (not an external-kill/timeout rc) — e.g. `garden-issue-9-run-contract-control-upgrade-test-to-completion` dying rc=1 at ~472s every cycle — silently requeues through all `GARDEN_REAP_POISON_THRESHOLD` (5) cycles before surfacing. Add a deterministic check, read-only of existing state (no new CAS, keep the reaper the sole requeue writer): (1) recover the prior cycles' `elapsed` for this `$base` by grepping this gardener-clone's prior gardener progress journal entries for the note's `elapsed=([0-9]+)s` on the same job base; (2) when `cycle >= 2` AND the last N elapsed values fall within a tolerance band (e.g. ±15%) AND the rc is NOT `is_external_kill_rc`/`is_handler_timeout_rc` (i.e. it's a transient-claude-signature or claude-CLI failure, which — unlike a legitimately-long shepherd hitting the wall-clock bound — should not repeat deterministically), emit ONE gardener-inbox kind:error via `skills/gardener-inbox-error-reporting/report-error.sh` flagging "likely deterministic overrun classified transient, not a blip" with the elapsed series, while still leaving the job in `doing` for the reaper (do not change requeue ownership). This moves the "is this stuck?" judgment off a human/watchman grep and into the script, surfacing a genuinely-stuck transient-classified job in ~2 cycles instead of ~5. Gate the whole check behind a new tunable (e.g. `GARDEN_ELAPSED_CONSTANCY_CYCLES`, default 2) so it can be disabled.

---
claim:
  host: endolinbot2
  gardener: 41
  claimed_at: 2026-07-01T05:52:03Z
