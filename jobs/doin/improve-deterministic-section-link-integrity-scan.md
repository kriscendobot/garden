The library section-link-integrity scan is performed inside the scholar LLM agent on each empty-inbox cycle, and this cycle it built its universe snapshot from a stale `origin/journal2@b6139b08` (~80 commits behind the peer-advanced tip `ea9e4d82`), manufacturing three false-positive "dangling" links and making two wrong repoint edits that had to be reverted (`090317Z-result-scholar-99178f92.md` § "Stale-data false positives"). Resolving "does this section link target an existing committed file" is pure graph resolution, not a judgment call, so it belongs in a deterministic script. Add a `scripts/jobs/` checker that (1) syncs the library to the current `origin/journal2` tip before scanning (never an in-context-stale snapshot), (2) walks all link axes — source/topic/concept/role → section, the `sections/README.md` backstop, AND the source/README → parent-index axis the keyword/wikilink/markdown scans missed — resolving each target against the freshly-synced working tree, and (3) emits only genuinely-dangling links for an agent to repair. This both removes the stale-snapshot false-positive failure mode (no edit is made off a stale universe) and moves the bulk of the scan off the LLM. Encode the "sync-tip-first, re-verify-each-target-at-the-moment-of-edit" lesson into the script rather than re-teaching it via `roles/scholar/AGENT.md` / `skills/library-lookup/SKILL.md`.

---
claim:
  host: endolinbot
  gardener: 70
  claimed_at: 2026-06-27T09:36:00Z
