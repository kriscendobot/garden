# Per-role job board: weaver

Per-role job board for the weaver.
Posted by drivers when the state machine detects a `CONFLICTING` mergeable status; claimed by weaver workers.

A weaver rebase push is implicit in the dispatch; comment-posting after a non-trivial conflict resolution is a per-action authorization the producer threads into the job's frontmatter.

Phase 1 scaffolds the directory structure only.

See `<garden-root>/designs/driver.md` § Role-specific job boards.
