I now have the complete picture, and the correct action is verification rather than mutation. Here's the reasoning:

**What the dead-lettered message was:** child 5/5 (`xs2rust-endor-stage4-ses-conformance`) of the `xs2rust-endor-build-stage4b` serial orchestration reporting its scope-fold findings *back to* its supervisor `port-xs-to-rust-memory-safe-engine-s10`. It is a **status report / reply**, not an action request — s10 was asked to do nothing by it.

**Why its intent is already preserved (three independent guarantees):**

1. The identical content lives durably in `jobs/tada/xs2rust-endor-stage4-ses-conformance.md`, which s10's own spec **explicitly instructs it to read** ("read those five children's tada reports"). That report even carries a dedicated **"Key finding for s10"** section and openly documents that its inbox report was dead-lettered — so s10 cannot be misled into thinking the child didn't report.
2. The orchestration `xs2rust-endor-build-stage4b` reached `jobs/tada/` with `orchestration-status: complete` (all 5 children succeeded).
3. s10 (`gate: blocked`, `blocked_on: xs2rust-endor-build-stage4b`) will be **deterministically auto-promoted** plan→todo: I confirmed `unblock.sh`'s exact promotion condition is met (`jobs/tada/xs2rust-endor-build-stage4b.md` exists for a job-basename blocker), and this worktree is on the **leader host** where that watcher runs.

**Why re-sending would be wrong:** s10 is not a live inbox until it is promoted and claimed. Re-issuing the message would simply re-dead-letter into another fresh job — an infinite loop the originating child already recognized and deliberately avoided.

No garden files needed changing and no journal mutation is warranted — manually promoting s10 would race the deterministic watcher. My worktree has no changes to commit.

---

## Completion report

**Job:** `deadmail-20260706T204713Z-8ae72e` — pick up the intent of a dead-lettered message addressed to `port-xs-to-rust-memory-safe-engine-s10`.

**Finding:** The dead-lettered message was child 5/5's completion/status report to the parked Fable supervisor `s10`, not an action request. Its intent was to convey the stage-4 closure fold findings to s10.

**Disposition — intent already preserved, no action required:**
- The full findings are durable in `jobs/tada/xs2rust-endor-stage4-ses-conformance.md` (includes a "Key finding for s10" section and self-documents the dead-lettering); s10's spec mandates reading each child's tada report.
- Orchestration `xs2rust-endor-build-stage4b` is complete in `jobs/tada/` (all 5 children succeeded).
- `s10` is parked `blocked_on: xs2rust-endor-build-stage4b`; that blocker is in `tada/`, so `unblock.sh` (leader-only, deterministic) will promote `s10` plan→todo on its next tick. Confirmed the exact promotion predicate is satisfied and this is the leader host.

**Changes:** none (no code/journal edit; re-sending would only re-dead-letter since s10 isn't a live inbox until promoted+claimed).

**Follow-ups:** none owned here. Natural next work belongs to `s10` when it wakes: the whole-stage-4 acceptance review, then the `globalThis` live global-object binding that the boot-bundle chain is ledgered against. No maintainer inbox touched; PR #600 remains DRAFT.
