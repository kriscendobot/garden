Rebased PR #807 onto `llm` and force-with-lease pushed head `640059855`.

Validated targeted Prettier and diff checks. `yarn install --immutable` was blocked by a `better-sqlite3` native-build failure; CI was running at handoff. Posted the required PR summary comment.
