PR #253 is open but is a stale mirror: all six head commits were already present in upstream `endojs/endo` master. I rebased its head to upstream SHA `46d4edf`, force-pushed it, and confirmed the `master-46d4edf` reflection exists. GitHub rejected the base edit because there are no commits between that reflection and the now-identical head; the PR still targets `master`. Maintainer follow-up: close or otherwise retire this empty, long-idle PR.
