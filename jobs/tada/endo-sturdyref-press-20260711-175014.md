The realign sub-job was already claimed by a fleet gardener (it's in `jobs/doin/`). All press artifacts for this tick are in place — completion report follows.

## Completion report — endo-sturdyref-press-20260711-175014 (tick 1 of the hourly press)

**Assessment (state as found).** The effort had been stalled since 2026-07-02 with no live sturdyref workers (verified via `inbox-list.sh` and `jobs/doin/`). The settled design is #539 (`designs/sturdy-refs-ocapn-enlivenment.md`, all five maintainer inline directives addressed 6/26); #541 is on hold since 6/26 for building the abandoned endor-syscall retention edges; **#521 contradicts the settled design** — its pass-style exports a `makeSturdyRef` maker and holds locators in a module-private WeakMap, where the maintainer's #539 directives require shape-only pass-style ("pass-style should define its shape but not construct them; that is the role of the CapTP session manager"), and it still carries the `getStudyRefLocator` typo.

**Pressed this tick (two artifacts):**

1. **Design refinement on #539 (inline):** the doc claimed a confined guest holds "an opaque SturdyRef" while also giving SturdyRef a readable `location` accessor — a direct contradiction of the binding Distributed Confinement charter, since a location-bearing value in a confined guest's hands *is* the location leak. Landed commit `4537e4a5c` on `design/sturdy-refs-endor-syscall-followup` (verified as #539's head via `gh pr view`, output `4537e4a5c…`): a new "Distributed confinement (binding invariants)" section establishing the two-tier split (location-bearing SturdyRef = trusted/wire tier; fresh, unlinkable, opaque guest-scoped token = confined tier, resolved only by the trusted mediator holding the closely-held OCapN network capability), the three invariants (no location / no identification / opaque & unforgeable), four confinement tests in the test plan (cannot read a locator, cannot correlate two tokens, mediated convergence, forged-token rejection), a confinement acceptance criterion, and one scoped open question (the token's concrete representation). Explanatory comment posted: PR comment `4948199929`. PR remains DRAFT.
2. **Posted builder sub-job** `ebfb-realign-521-passstyle-shape-only` (directive identity `endojs/endo-but-for-bots#521:realign-shape-only`) to realign #521 to design cuts 1–2: shape-only pass-style (`location` + optional `type` accessors, no maker, typo'd export dropped), ocapn session-manager construction replacing the tagged-WeakMap shim, real-execution test bar including a swiss-number-never-readable confinement test, additive commits only, PR stays DRAFT. **The job was claimed by a fleet gardener within a minute** (observed in `jobs/doin/`).

**Progress recorded:** journal entry `entries/2026/07/11/180228Z-progress-gardener-5760f4.md` with the full assessment and the next-tick queue (watch the realign job → re-scope #541 to cuts 3–4 → guest-token representation → agent provide/accept surface → base hygiene for #539/#511 on stale `llm-65b0abe`).

**Confinement property statement (required):** this tick's landed artifact strengthens both **no-location** and **no-identification** — it makes the guest tier's opacity and per-grant unlinkability binding in the design and adds the tests that enforce them. Nothing widened sturdyref reach.

**Verification status:** design-only edits landed this tick, so no test suite applies — reported "not verified" by that bar deliberately; the realign sub-job carries the real-execution requirement (install + affected suites with observed output). Pushes verified by observed `git push` acceptance and `gh pr view` head SHA.

**Follow-ups:** next hourly tick should check the realign job's outcome without racing its branch; #541 re-scope is the cut after that.
