Design job complete. Draft PR **endojs/endo-but-for-bots#613** carries the design; here is the report.

**What I did.** Wrote `designs/buffered-channel-exo-stream-consolidation.md` against the `llm` roadmap branch (tip 1132289 at checkout), committed on `design/buffered-channel-exo-stream-consolidation` (commit 2daae55a4, includes the required `designs/README.md` summary row and totals note), and opened draft PR #613 against `llm`, citing kumavis review 4633245769 on PR #486.

**What the design says.** Diffed the two copies: identical logic, already diverged at the exo boundary (floot uses `Far`, claude-sandbox uses `makeExo` + a bespoke guard); named `packages/chat`'s `makeTextFeed` as a third informal copy. Answered the job's central question: no existing `@endo/exo-stream` export covers the semantics — everything is pull-based and backpressured — and the obvious composition (`@endo/stream` `makeQueue` + `readerFromIterator`) fails semantic 3: `makeReaderPump`'s sequential loop cannot observe the initiator's early close while parked on an idle producer, so `onClose` (the `claude -p` kill) would wait for the next push and never fire for a hung turn. The proposal is a new push-fed `@endo/exo-stream/buffered-channel.js` export keeping the `makeBufferedReader` name and kit shape: eager ack-chain resolution (fire-and-forget push, terminal auto-finalize), a live syn-chain close watcher (prompt `onClose` and discard-on-return), plus a deprecated legacy `next`/`return`/`throw` surface so both copies delete in one PR before consumers flip wire-by-wire to `iterateReader`. Migration is phased to work whichever of #486 and the build lands first; tracking anchor named as branch `build/buffered-channel-exo-stream-consolidation`, tracking issue to be filed. Five open questions surfaced (upstream endo#3036 divergence, `@endo/stream` layer split, `makeTextFeed`, `readPattern` timing, dual-surface lifetime).

**Side effects and follow-ups.**
- Library writeback: appended a "Gap: no non-backpressured push side" section to `journal/library/concepts/exo-stream.md` (landed via `land-journal-edit.sh`).
- Infra blocker found and routed: `ensure-project-worktree.sh` deterministically fails for `endojs/endo-but-for-bots@llm` because `refs/heads/llm` is checked out at the standing monitor worktree, so its fetch is refused and the new stale-tree guard dies. I worked around it with a manually created, correctly-keyed worktree at the verified remote tip, and posted job `fix-ensure-project-worktree-checked-out-branch` with root cause and a suggested remote-tracking-ref fix. Until that lands, every `llm`-checkout job on this host hits the same wall.
- Job worktree torn down; inbox drained (empty); no comments were posted to #486 (untouched per the job spec).

Not verified (design-stage by nature): the proposed push pump's interoperation with `iterateReader` is argued from the source of `iterate-reader.js`/`reader-pump.js`, not from an executed prototype; the build job should prove it with the tests named in Phase 1.

Self-improvement: posted `fix-ensure-project-worktree-checked-out-branch` so the worktree-provisioning failure is fixed in the script rather than re-worked-around by every future `llm` job.
