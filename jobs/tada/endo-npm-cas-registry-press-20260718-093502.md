Press tick complete for the npm-via-CAS registry-proxy arc.

**Assessment.** The stack stands at #276 (Phase 2 fetch) → #772 (MVS resolver) → #782 (offline/.npmrc) → #785 (assembly) → #787 (XS execution) → #791 (exec hardening), all draft, no new review feedback since the last tick, no live peer on any of these branches, inbox empty. The finish line (cold-fetch `endor run` over XS) was already reached; the top recorded gap was conditional/subpath `exports` maps — that is what this tick pressed.

**Pressed: new draft PR #795** (`feat/endor-npm-exports-resolution`, stacked on #791). Real Node-semantics `exports` resolution, implemented at run time in the archive bootstrap where dynamic `require('pkg/sub')` needs it anyway: subpath keys, single-`*` wildcard patterns with specificity ordering, nested condition objects in key order, array fallbacks, `null` blocks, and subpath encapsulation (unlisted subpath → clean named error, not a file fallback). Packages without `exports` keep Node-style file access; `"."` falls back to `main`/`index.js`; scoped names split correctly. `run_map.rs` sheds its host-side `default_entry` and `"."`-edge rewrite. Two documented deviations: `exports` now beats `main` (the old binding had Node's precedence backwards), and conditions resolve import-pass-then-require-pass because every load bottoms out in `importNow`. One regression I introduced and fixed before pushing: the zip-archive convention registers a literal `"."` source key, which the resolver now honors before consulting the manifest.

**Real-execution evidence.** From a cold CAS against the live registry: `endor run main.js` importing `{ nanoid, customAlphabet } from 'nanoid/non-secure'` — a real conditional subpath export — fetched `nanoid@3.3.16`, executed over XS, printed generated IDs, exit 0; `--offline` replay identical with zero network; importing the unexported-but-present `nanoid/non-secure/index.cjs` failed exit 1 with the named encapsulation error. Suites: endo 137/137, xsnap 118/118 (7 new run_map tests). Build note: the previous tick's surviving worktree held only 66-byte stub boot bundles; real ones recovered from the xs2rust stage-9 worktree.

**Follow-ups for a future tick.** Remaining recorded gaps: `"module"`/`"browser"` manifest fields, self-referential package imports (`"imports"`/own-name), cyclic `require`. The registry-capability edge (#671/#403) remains the byte-array arc's; nothing duplicated here.
