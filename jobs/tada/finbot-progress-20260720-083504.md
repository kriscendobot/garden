Inbox empty. Cycle complete.

## Completion report

**Cycle:** finbot-progress-20260720-083504 (builder). One increment, landed and green.

**Assessed state.** No open PRs on `kriscendobot/finbot` (work lands directly on `main`, was at `f32bc19`); every prior `finbot-*` job is in `tada/`, none in flight — nothing to duplicate or compete with. The stale `worktrees/kriscendobot-finbot` checkout points at the garden repo, so I used an isolated project worktree via `ensure-project-worktree.sh`. The last cycle's handoff named the deepest unblocked step: *decide whether Diebold-Mariano significance becomes a selector gate alongside the parsimony margin* — I took it.

**Advanced** — `kriscendobot/finbot` main `f32bc19 → 45470c4` (two commits):
- **`refactor(simulator)`** — `walkForwardQlike` now returns the per-observation QLIKE loss arrays next to the scalar means (so a selector can run a paired test without re-walking the suffix), and `dieboldMariano` moved to the leaf `vol-selection.js` (re-exported from `vol-eval.js` for its public path) so `garch.js` can reach it without an import cycle.
- **`feat(simulator)`** — an **opt-in `significanceAlpha`** on `autoGarchFamilyMleFromPriceHistory` / `conditionalVolFromPriceHistory` / `--adaptive-vol=auto-family`. When set, an asymmetric branch that already cleared the fixed-nat parsimony margin must *also* beat GARCH by a significant paired DM QLIKE test before acceptance; otherwise GARCH is retained and the reason is recorded (`oos-qlike-significant` / `oos-qlike-insignificant` / `oos-qlike-unverifiable`). The DM verdict surfaces in `stats()`/the regime read as `qlikeSignificance` only when the gate ran. **Default `null` → live behavior and every proposal hash byte-for-byte unchanged.** Plus 3 new deterministic tests (noisy-edge-declined, consistent-edge-accepted, gate-reaches-fitted-selector) and a design field note.

**Verified.** Full workspace suite **598 pass / 0 fail** (+7; fresh worktree needed the known one-time `npm install` relink — pre-existing `package-lock.json`-not-committed gap). Seeded dry-run OODA on the default and `auto-family` paths both **auditor-APPROVED, WALLET TOUCHED: false** (default proposal hash `d54b517e…` stable). Tree left green.

**Next unblocked step (handed off to maintainer inbox):** decide whether `significanceAlpha` should become the *default* for the live `auto-family` path — that would change proposal hashes, so it needs a maintainer call plus a re-baselined fixture. Secondary: make the walk-forward report's DM comparison test best-asymmetric-vs-GARCH (the selector's actual contest) instead of winner-vs-runner-up.

**Maintainer decision pending (unchanged):** live execution stays blocked on explicit paper-wallet/test-net authorization and a chosen CapTP transport. Reported to the maintainer inbox.
