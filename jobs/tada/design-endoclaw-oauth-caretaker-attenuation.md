Both asks are complete and pushed to PR #621. The inbox is empty, and the PR state is untouched (it is currently not a draft — evidently the maintainer's doing, and I left that alone). Final report:

Completed designer job `design-endoclaw-oauth-caretaker-attenuation`: another design round on `designs/endoclaw-oauth.md` per kriskowal's review of endojs/endo-but-for-bots#621, plus the named reusable pattern doc, both pushed to the existing PR head branch `design/endoclaw-oauth-foundation` (commits `72cf84eada`, `6e1de440c4`; PR #621 head now `6e1de440c4`).

**Ask 1 — endoclaw-oauth revision (`designs/endoclaw-oauth.md`, Updated 2026-07-10).** Added holder-driven recursive partition composed with the existing caretaker controls: a new `partition(opts)` method on the `OAuth` facet mints a narrowed child `OAuth`/`OAuthControl` pair without the parent's controller in the loop, recursively, making the partitioner the caretaker of its delegate. A new `## Partition and Delegation` section (with a mermaid delegation-tree diagram) pins the semantics: the monotonicity invariant is enforced by per-request conjunction along the ancestor chain (allowed paths intersect, `readOnly` ORs, `baseUrl`/`scopes` inherit), never by a partition-time snapshot intersection, so a live child can never out-scope a parent that later narrows via its caretaker; partition therefore needs no subset validation (surplus authority is inert). Facet revocation is now subtree-wise (structural under conjunction); token revocation is unchanged. Durability is child-to-parent formula references with O(depth) enforcement. Synced sections: interface comments, the changes list, Token/Facets/Refresh, Connector Contract item 7 (partition is for exo holders; closed-over fetch consumers layer their own attenuation), Endo Idiom example, Phase 3, Design Decisions 8–9, Dependencies, and Prompt.

**Ask 2 — named pattern captured.** New Reference-status `designs/caretaker-attenuation.md`: the composite of the caretaker pattern with attenuation by recursive partition, stated as five normative invariants (dynamic monotonicity, conjunction-not-snapshot, layer-local controls, subtree-wise revocation, legible-not-merely-possible delegation), with a when-to-apply note, one considered-and-rejected shape (partition via the control facet), an instances table cross-linking endoclaw-oauth, daemon-capability-filesystem, exo-google-sheets, daemon-capability-persona, and endoclaw-timer, and the review body quoted under `## Prompt`. Chose the `designs/` reference-doc vehicle (the repo's precedent: forge-gap-analysis) over a `.claude/skills/` skill. Cross-linked in both directions.

**README sync (`designs/README.md`).** Last-updated line, recently-revised prose, summary-table rows (endoclaw-oauth → 2026-07-10; new caretaker-attenuation Reference row), totals (8 Reference, 146 designs), and the M7 per-design estimate description.

**Open questions surfaced for the maintainer** (in the design, per the ambiguity in the review): (3) whether `mint`/`partition` should offer a `delegable: false` legibility knob, given it cannot actually prevent proxy-based delegation; (4) GC policy for revoked subtrees (eager cascade-delete vs. tombstone-and-sweep vs. general formula GC; lazy-plus-sweep suggested as presumptive).

**Notes.** PR #621 is currently **not** draft — that was its state before this job (the job asked only that I not un-draft it, and I made no state change and posted no PR comments; no authorization was granted or used). Follow-up if desired: a fixer/panel round on the revised design, and the maintainer's answers to open questions 3–4.
