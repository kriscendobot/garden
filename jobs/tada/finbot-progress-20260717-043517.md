One increment landed, verified, pushed, and reported.

## Completion report

**Cycle: the volatility regime read goes asymmetric — one increment.**

**Assessment.** All prior `finbot-*` jobs are in `tada/`; no finbot work is in-flight and there are no open PRs (work lands directly on `main`, at `df2a164`). The previous cycle's own hand-off named the deepest unblocked next step: `conditionalVolFromPriceHistory` rolled the *symmetric* GARCH surface forward even when the world is GJR, so the analyzer's risk denominator and the auditor's tail floor could only see *magnitude*-conditional vol, never *down-move*-conditional vol.

**Advanced.** `main` `df2a164 → abf643b`:
- `conditionalVolFromPriceHistory` (`packages/simulator/garch.js`) now accepts `kind: 'gjr-garch'` and builds the GJR (leverage) surface — fixed split or per-asset MLE per `estimate` — for the roll-forward. `GjrGarch11Surface` exposes the identical `initialVariance`/`nextVariance`/`stats` interface, so **only the fit changed**; the roll-forward recursion is untouched. A `gjr-garch` read carries `gamma` in each per-asset entry (absent from a symmetric read).
- No pipeline code change was needed: the analyzer passes `config.regimeVol` straight through as these opts, so a caller feeds the down-move-conditional read into the sizing denominator and auditor tail floor by setting `{ estimate: 'mle', kind: 'gjr-garch' }` — the leverage read reaches the decision boundary through the existing config seam.
- +3 tests in `packages/simulator/test/garch.test.js`: a drawdown-ending window reads hotter under GJR than symmetric at identical `(alpha, beta)`; a drawdown reads hotter than its sign-mirror rally under the same GJR params (sign-driven, not magnitude-only); and the `gjr-garch` MLE read is deterministic with a non-negative estimated gamma. Design doc updated with a dated field note.

**Verified.** Full suite **541 pass / 0 fail** (was 538); `finbot-ooda --seed=7` green with **WALLET TOUCHED: false**. Repo has no CI workflows, so nothing to shepherd; tree left green. Reported to the maintainer inbox.

**Next unblocked step (handed off).** Auto-select `gjr-garch` for the regime read when the fitted gamma is materially positive (an asymmetry-gated switch), rather than leaving it to config. Deferred behind that: EGARCH, implied-vol surfaces, PNG rasterization, far-ref vending.

**Maintainer decision pending (unchanged).** Live execution remains blocked on explicit paper-wallet/test-net authorization and a chosen CapTP transport.
