Garden worktree clean; project work committed and pushed; maintainer notified; inbox drained clean.

## Completion report — `finbot-progress-20260713-080513`

**Assessed state.** No open PRs on `kriscendobot/finbot` (direct-push convention, `main` at `6acf142`); no in-flight `finbot-*` jobs (all in `tada/`), so no competing work. The last two cycles landed the per-instrument GARCH MLE (`14fcccf`) and the analyzer regime blend (`6acf142`), and both explicitly handed off the same gap: **the live OODA cycle's short window (warmup 10 → 9 returns) forces the MLE into its fixed-split fallback, so the per-asset α/β only ever fires in unit tests on long synthetic windows.** I took it.

**Advanced this cycle** (direct-push `6acf142..b77f31e`): **a separable vol-fit window — the per-instrument MLE now engages on a live cycle.**
- **`packages/pipeline/ooda-cycle.js`** — `config.fitWindowTicks` (default = `windowTicks`) draws a *longer* rolling `fitReadings` from the same history, ending at the same current tick, threaded into the analyzer and forecaster. The oracle deviation read and the realized-vol denominator keep the short `readings`. Unset or ≤ `windowTicks` → `fitReadings === readings` and the cycle is **byte-identical** to before.
- **`analyzer.js`** — `input.fitReadings` feeds the `regimeVol` GARCH fit only; realized vol and the deviation stay on the short window.
- **`forecaster.js`** — `input.fitReadings` feeds the adaptive vol fit only.
- **`bin/finbot-ooda`** — `--fit-window=N` drives it, warming `max(warmup, fitWindow)` ticks so the longer window has history while the oracle read keeps its short window.

**Verification — GREEN.** Full workspace suite **511 pass / 0 fail** (+6 pipeline tests: fitReadings drives the regime; realized vol unaffected by the longer window; a fitReadings ≤ readings is byte-identical; the forecaster reads the longer window; the live MLE engages through the ooda-cycle; and `fitWindowTicks ≤ windowTicks` is byte-identical to omitting it). End-to-end proof: `--multi --adaptive-vol=mle --fit-window=16` recovers **distinct per-asset persistence** (ATOM 0.58 / OSMO 0.68 / stATOM 0.03) where the fixed split reports a uniform 0.98 — the MLE is now measuring per-instrument regime on the live path. Safety gate `finbot-ooda --seed=7` across every mode (plain, `--adaptive-vol`, `--adaptive-vol=mle`, `--multi`, `--multi --adaptive-vol=mle --fit-window=16`): **WALLET TOUCHED: false** throughout; auditor APPROVED on the dry-run path. (`package-lock.json` left untracked — the repo does not track it; `npm install` was needed to link the `@finbot/*` workspaces.)

**Next unblocked step (handed off):** feed the now-live per-instrument regime persistence into a *downstream decision* — either the forecaster horizon (a high-persistence regime argues for a longer projection) or the auditor's tail floor (persistent high vol should tighten the tail-risk gate). That closes the loop from "we measure the regime per instrument" to "the regime changes what we do."

**Needs a maintainer decision (standing, unchanged):** the only *blocked* axis remains live execution — cap-attenuation Phase 2 + a first paper-wallet/test-net run behind an explicit `live_authorized: true`. Everything else is unblocked and moving. Maintainer inbox notified; my inbox drained clean; garden worktree clean.
