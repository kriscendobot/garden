Confirmed the compatibility fix is already on `main2`: prompt-mode Kimi invocations omit `--yolo`/`--auto`, including resume. The fake CLI regression rejects both incompatible combinations.

Passed Mystic harness (29), worker-spine, completion-signal (17), and model-routing (29) tests. No credentials printed or live canaries requeued.

Deployment readiness: `main2` is ready for the deliberate deployment procedure; deployment itself was not performed.

Self-improvement: nothing this time.
