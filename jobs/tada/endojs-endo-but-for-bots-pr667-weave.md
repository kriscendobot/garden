Rebased PR #667 onto `llm` and force-pushed `9e4437cfa`.

Resolved the agentry exports-test conflict by retaining both eval and RPC export coverage. Posted the PR summary comment.

Verification: 38 focused tests passed; TypeScript passed; ESLint completed with 0 errors (31 existing warnings). Project worktree removed.
