The job is genuinely complete. Here is my report.

---

## Completion report — dead-letter pickup `deadmail-20260812T225323Z-c7db45`

**Nature of the job.** A dead-lettered maintainer message (2026-08-12T22:53Z) addressed to the completed `minion-town-host-exposure-forensics`. Its intent: walk the closure of the minion.town OCapN bootstrap and decide whether `getGreeter().hello`/`getAgentBinding` reach anything host-shaped, plus characterize the "valid nonce locator" gate and the separate `/ocapn` demo. Read-only.

**Key finding on arrival: the intent was already fulfilled by an earlier run of this same job base** (host `endolin-garden-ece02cb4`, 23:00:46Z) before it was requeued to me, and **the maintainer already accepted it** (23:07:14Z: *"Outstanding work… The closure argument stands on its own"*). Both the delivery and the ack are durably in the journal. So my task reduced to independently re-verifying the delivered conclusion from the deployed source and confirming nothing remains open.

**Independent source verification (deployed pins).** I read the exact deployed source: `/.well-known/ocapn-cbor-np` + `/ocapn-daemon` → the `endo-pet-daemon` container built from `endojs/endo-but-for-bots@a371b007`; `/ocapn` → `endo-ocapn-daemon.service@b155b339` (demo). The prior run's conclusion is **correct**, and the decisive mechanism holds:

- The `EndoOcapnBootstrap` (`packages/daemon/src/networks/ocapn.js`) exposes `getNodeId/getAgentBinding/getGreeter/help`. `getAgentBinding` returns a pass-by-copy `{agentPublicKey, signature}` (no methods); `getGreeter().hello(...)` returns the daemon's `localGateway` with `provide` + `followRetentionSet`.
- **The break is `followRetentionSet`**: it calls `listFormulaNumbersByNode(peerNodeNumber)` — documented as *"return all formula numbers whose node matches"* — with **no peer/session authorization** and no check that the argument isn't the local node. Since `getNodeId()` publicly reveals the local node, a locator-bearer enumerates the entire local formula index, rebuilds each `number:node` id, and calls `provide(id)` (which only checks `assertValidId` + `isLocalId`). This defeats the `randomHex256`/`deriveId(secret rootNonce)` unguessability that would otherwise bound `provide`, reaching `endo`, `eval`, `make-unconfined`, etc. → **host-equivalent authority. Not bounded-safe.** (My own first-pass reasoning had called it bounded; that was wrong precisely because it overlooked the `followRetentionSet` index leak.)
- **Gate:** `Noise_IK_25519_ChaChaPoly_BLAKE2s` — the initiator must possess the responder's 256-bit Ed25519 designator, which lives in the container's `/data/ocapn-daemon-location.json` and is **not** Caddy-served. So it's capability-gated (not internet-open), but broader than a single random formula: every legitimate invitation/address embeds the full location in its `loc=`, so any invitation recipient (or a leaked address) becomes a bearer of the dangerous bootstrap.
- **`/ocapn` demo is bounded-safe:** its locator holds only swissnum `greeter` → `hello(string)→string` and `getNodeId()→string`; no gateway, no daemon behind it.

**Containment context (unchanged by this):** the ~22:37–22:45Z containment (unset `GATEWAY_ENDO_SOCK`, de-register the two `@agent` vhosts) was on the *weblet powers plane* (`endo-gateway.service`), a different unit from these OCapN daemons. The recommended follow-up already stated to the maintainer stands: retire/rotate the public `/ocapn-daemon` listener's Noise location until the Gateway API is fixed to bind `followRetentionSet` to the authenticated peer and stop leaking the local formula index.

**Changes made:** none — read-only forensic job. No garden `main2` commit, no project mutation. Created and cleaned up one scratch worktree at the deployed endo pin for source reading. Did not send a redundant maintainer message (the loop was explicitly closed by the maintainer's 23:07 sign-off).

**Follow-ups (already surfaced to the maintainer, not new work for this job):** the code-level fix in `@endo/daemon` (authenticated-peer binding for `followRetentionSet`; no local-index enumeration via the gateway) and the operational retire/rotate of the `/ocapn-daemon` Noise location.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/deadmail-20260812T225323Z-c7db45.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 2 on 2 host(s) (1 unmetered)
- Input: 110 tokens (4561633 cached reads)
- Output: 45163 tokens
- Cost: $4.5804255000000005 (1 engagement(s) unpriced)
- Wall-clock: 1225s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
