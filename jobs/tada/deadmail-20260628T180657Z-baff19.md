Both jobs were posted (with the `.md` extension my first check omitted) and have **already been claimed** by the fleet — they're in `jobs/doin/` with inbox + work records created. The work is dispatched and in progress. My producer job is complete.

---

## Completion report — `deadmail-20260628T180657Z-baff19` (intent of `classify-lint-endo-master`)

**Intent picked up:** A maintainer reply to the completed `classify-lint-endo-master` doer was dead-lettered: *"classify the warnings and post jobs to ratchet each of these warnings up to error and fix resulting defects."* This is a stronger follow-up than the original doer's output (which only proposed *clearing* the 5 jsdoc warnings). I carried it forward as producer work.

**Context reconstructed (no re-lint needed):** The prior classification (2026-06-27) found endo master (`endojs/endo-but-for-bots@master`, `364d69ba1`) **lint-clean — 0 errors**, with **5 jsdoc warnings in 2 rule classes**. Master sha is unchanged since, so the findings still hold exactly. I traced both rules to their config home: `packages/eslint-plugin/lib/configs/style.js` (the shared `@endo/eslint-plugin` `style` config), inherited as `warn` from `plugin:jsdoc/recommended-typescript-flavor`.

**What I did — posted one ratchet job per warning rule class** (each a `fixer` → small PR on `endojs/endo-but-for-bots`, with exact config location, defect list, fix approach, and definition of done):
1. **`ratchet-jsdoc-require-param-error-endo`** — set `jsdoc/require-param: 'error'` in `style.js`; autofix-then-fill the 4 `packages/daemon` `@param` defects (directory.js:129/174, pet-sitter.js:71, pet-store.js:159). All autofixable.
2. **`ratchet-jsdoc-check-tag-names-error-endo`** — set `jsdoc/check-tag-names: 'error'`; resolve the 1 `compartment-mapper/src/types/policy-schema.ts:64` `@remarks` defect (allow the TSDoc tag in config, or rewrite — house-style judgment, flagged in the job).

Both posted to `todo/` as `gate: ready` (the maintainer directive is the go-ahead — not parked). **Both have already been claimed by the fleet** (now in `jobs/doin/`).

**Also:** Superseded the now-retired consolidated plan `fix-lint-jsdoc-warnings-endo-master` (the weaker "just clear warnings" approach) — converted it to a `status: superseded` tombstone pointing at the two ratchet jobs, so no future foreman/fixer duplicates the weaker work. CAS-pushed to `journal2`.

**Verification:** Both jobs present in `jobs/doin/` on `origin/journal2`; old plan shows `status: superseded`. Temp files cleaned. No worktree was created (pure producer work).

**Follow-ups for the maintainer:**
- The two ratchet PRs will land separately on the bot fork. The `check-tag-names` one carries a house-style decision (allow `@remarks` repo-wide vs. rewrite the one block) — the fixer will ask if unsure.
- Unrelated open item from the original classification (untouched, identity call): bot master diverged from upstream `endojs/endo@master` (64 ahead / 8 behind) — whether to force-sync remains your call.
