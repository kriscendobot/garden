Inbox empty, garden worktree clean (all work was in the project repo, as it should be). Done.

## Completion report

**Job:** Dead-lettered message pickup — the designer `design-endo-thesis-translation`'s reply to `translate-distributed-confinement-to-docs` couldn't be delivered (recipient already completed). I carried the reply forward.

**What I found.** The reply answered three questions the translator had asked, then completed *before the answer landed*. The translator's output already exists as **draft PR endojs/endo-but-for-bots#629** ("docs: Distributed Confinement", `docs/distributed-confinement.md`), whose own body explicitly flagged that the convention hadn't landed yet and needed reconciliation. The design it should conform to landed as `designs/thesis-translation.md` (design PR #631). Carrying the reply forward = reconciling #629 with that now-landed design's paper-specific guidance.

**What I did.** Edited the doc on the PR head branch (isolated project worktree, keyed to this job base) and pushed to `endojs/endo-but-for-bots:docs-distributed-confinement` (`46da36e1b5..a85cabf176`):
- **Category `Concepts` → `Annex`** — the design's placement for the research edition; dropped the invented `Concepts` category (no `typedoc.json` change needed, `Annex` already in `categoryOrder`).
- **Protocol name discipline** — first mention now reads *"Pluribus (whose living descendant is OCapN)"*, OCapN thereafter.
- **Unreachable erights.org links → archive.org** with the original URL kept in the link `title` attribute (two occurrences), per the convention's dead-link rule.
- **Refreshed the now-stale "convention hadn't landed" note** to state conformance and the single-paper `docs/<slug>.md` shape (not nested under `docs/thesis/`).

Prettier passes (`proseWrap: preserve`, no re-wrap risk). Posted a reconciliation comment on #629 cross-linking design #631 (fixed a first-pass escaping glitch via a body-file patch).

**What I deliberately did *not* change.** #629 already took the copyright-safe route the reply blesses — an original exposition with an original diagram (no author permission required, given Miller's public-domain text dedication) — and is already **draft** with the licensing decision surfaced. I preserved that; the collapsed *Original E* / caption code-flagging machinery doesn't apply here since this prose note has no E code blocks.

**Follow-ups (unchanged, for the maintainer).** PR #629 stays draft pending the maintainer's call on whether to seek explicit author confirmation before publishing. I did not notify the designer peer — `design-endo-thesis-translation` is no longer alive on the bus (a message would only dead-letter into fresh-job noise); the PR comment is the durable record.
