**Press tick complete — VFS tool-call-surface parity arc (endojs/endo-but-for-bots, base `llm`).**

**Assessed:** All six live arc PRs re-verified. #656, #657, #655, #788, #790 — all open, mergeable, full CI green, no maintainer activity to answer (the #655 close-as-superseded question from 2026-07-17 remains unanswered; did not re-ping). No merges since the last tick, so no weaves were needed.

**Fixed:** #713 (mount glob+grep+glorp) had one red check on head 454b2b97db — `test (22.x, macos-15)` failing on an unrelated `@endo/agentry` "failed to exit" hang (rootfs-form / sandbox-slice-mint tests; 105 agentry tests passed before the hang). #713 is daemon-only, so I re-ran the failed job; the rerun passed and **#713's full matrix is now verified green**. Noted the flake signature in the schedule body so future ticks rerun before diagnosing.

**Built:** Opened draft **PR #796 — feat(daemon): hashline edit-format pure core** (`feat/hashline-core`), the last unopened finish-line surface (`cli-edit-verb.md`) that isn't blocked on the mount stack. It lands the design's prescribed shared pure module `packages/daemon/src/hashline.js`: CRC32 per-line anchors with the pinned normalization/width rules, SHA-256 file-rev CAS with injected `sha256Hex` power, the textual hashline parser and hashline-json validator, hashline-annotated read rendering, the deterministic bottom-up splice with same-line composition, and the bounded reapply relocation search with `ambiguous-reapply` reporting. Deliberately no mount/CLI wiring, so it cannot conflict with the review-blocked mount PRs. Evidence: 38 new ava tests pass locally (worked example, CRLF/trailing-newline round-trips, width crossover, reapply bounds, render→author→apply round trip), eslint and tsc clean, and the **full 24-check CI matrix verified green** (no silent pull_request-skip). PR #204's closed sketch was consulted; this is a fresh implementation of the since-pinned normative spec, with the two design-unpinned surfaces (the `ambiguities` failure field, the same-line composition realization) flagged in the PR body for review.

**State propagated:** Updated the `endo-vfs-parity-press` schedule body on `journal2` (post-12:20 state: #713 green with flake note, #796 opened and green, remaining surface = lal glob/grep and the mount/CLI hashline wiring, both blocked on the mount stack landing).

**Follow-ups (for future ticks, recorded in the schedule):** lal glob/grep stays blocked on #713/#655; `EndoMount.edit`/`EndoGuest.edit` + `endo edit` CLI wiring builds on #796 once the mount stack lands; seven arc PRs now await maintainer review.
