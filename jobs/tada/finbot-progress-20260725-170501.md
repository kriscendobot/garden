Cycle complete. All work is committed and pushed to the PR branch; my main2 worktree has no changes (this increment was project code). Report follows.

---

**finbot progress cycle — `finbot-progress-20260725-170501`**

**State assessed.** PR `kriscendobot/finbot#4` (SES compartment role programs) was the sole open PR — draft, MERGEABLE, CI green — blocked on the mandated panel gate. The prior panel-rerun job `finbot-pr4-panel-rerun-20260725` was **poisoned** (requeue-exhausted, 5 cycles). No competing finbot jobs in todo/doing. Merged so far: PRs #1–#3.

**Increment advanced (one unit).** Ran the `locksmith` jury seat by hand from the PR worktree and got a substantive verdict that flagged a real bypass in this attenuation PR. Confirmed it against the code and fixed the deepest one:
- `runCompartmentLlm` re-derived a role program's ambient globals from `CAPABILITY_MAP`, so a caller-supplied `params.attenuator` narrowed only the *tool* slice, not ambient authority — the declared narrowing point was not the only one. `spawn()` now threads the attenuated `globals` policy into the compartment, which builds from exactly that; direct callers keep the role-default fallback.
- Added a regression test proving a narrowing attenuator's globals are honored by the executing compartment. Verified it **fails against the pre-fix path** and passes after — a live guard, not a tautology.
- Recorded a field note in `designs/cap-attenuation.md`.

**Pushed & green.** Commit `f5d1f91` on `feat/harness-compartment-role-program`; full `npm test` green locally (all packages), CI `test` SUCCESS on the new head. PR left **DRAFT — not merged** (governance honored; the press never runs `gh pr merge`).

**What changed:** `packages/harness/sandbox/permissive.js`, `packages/harness/spawn.js`, `packages/harness/test/spawn.test.js`, `designs/cap-attenuation.md`.

**Reported to maintainer** (`message-user.sh`, delivered) with two decisions the chain is actually blocked on — not more building:
1. The panel gate is structurally stuck (poisoned 5×). Root-caused the "empty seat output" to seats needing the PR worktree as cwd (proven by the manual locksmith run). Offered three options: supervise a full panel long-run next cycle, a fail-closed `panel.sh` fix (empty seat = FAIL) as a main2 job, or an authorized reduced seat set.
2. Two locksmith findings I deliberately deferred (documented as follow-ups) need a design call before a thorough panel can pass: the vended `fetch` is unbounded where the capability map specifies a **pinned** fetch, and `bounded` is currently identical to `full`. Needs the pin-set for `oracle-watcher` and the concrete `bounded` surface for `steward`.

**Follow-ups / next unblocked step:** awaiting maintainer answers to (1)/(2). PR `kriscendobot/finbot#4` is in correct "awaiting panel + Fable sign-off" state. Did **not** re-post an identical panel job (would re-poison). The stale poisoned `finbot-pr4-panel-rerun-20260725` in `plan/` can be GC'd by the liaison. No merge performed.
