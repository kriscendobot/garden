Supervisor s4 completion report — xs2rust-endor port, PR endojs/endo-but-for-bots#600 (kept DRAFT).

**Situation on wake differed from the parked spec:** the stage-2 builder job I was blocked on never completed — it overran the 2400s handler wall-clock twice, was reaped, and was removed from the board with a re-scope message routed to my inbox. The builder had landed **stage 2a** (commits `4339cf1f6`, `cc09a660c`) and established that bit-exact computron parity on any allocating program requires the allocation-faithful object heap first.

**What I did:**

1. **Re-verified the stage-1-review fixer (`372a00d4b`) line-by-line against the pin `48ee02d8cfe0`:** s3's findings 1–3 are genuinely closed — branch-family meter placement matches `mxBranch`/`mxBranchElse`/`mxBranchIf` exactly, the overflow-wrap guard matches `fxCheckMetering`, and the dual-run predicate now rejects non-`Throw` `BothAbort`s. Found **two new latent parity defects** the fixer introduced: (1) C-XS runs *no* meter check when END/RETURN exits to the C caller (checks live at the `mxFirstCode` sites), but endor checks unconditionally; (2) `arm_meter` omits `fxBeginMetering`'s `<<16` interval scaling and index reset.
2. **Independently reproduced all stage-2a acceptance evidence** on a fresh checkout: `cargo test --workspace` green, harness **86/86 bit-exact**, the 11-program stage-2 behavioral corpus result-agrees, **Miri GC 6/6**, `forbid(unsafe_code)` intact, `Compartment.evaluate` global binding real. Verified the metering-parity finding in the pin's `xsMemory.c` — the 2a/2b split is architecture, not slip. **Stage 2a accepted.**
3. **Pushed a design amendment (`bd0a8392f`)** recording the stage-2a/2b split and both findings as roadmap text, plus a README fix: the documented oracle-pin shallow fetch is now rejected upstream; I verified and documented two fallbacks (full `public` fetch — the pin is an ancestor — or a sibling checkout) and the empty-gitlink footgun that bit me during verification.
4. **Posted the s4 review** as [PR comment 4870957010](https://github.com/endojs/endo-but-for-bots/pull/600#issuecomment-4870957010): fixer verdict, 2 findings, 3 observations, re-scope ruling.
5. **Dispatched stage 2b as the serial orchestration `xs2rust-endor-build-stage2b`** (halt on child failure), three `model: opus` children on the same PR: `-heap` (allocation-faithful object heap + metering; fixes finding 2), `-frames` (closures + call/return frames; fixes finding 1), `-exceptions` (jump-chain + full 245-opcode coverage; bar = bit-exact test262 `language/` dual-run agreement, closing roadmap stage 2). Each child carries budget discipline (the monolith lesson) and reports to the supervisor, never the maintainer.
6. **Parked continuation `port-xs-to-rust-memory-safe-engine-s5`** blocked on the orchestration base, carrying the full program spec with updated supervisor state including a halt-recovery path. Journaled the transition (`222933Z-progress-gardener-cb92af.md`).

**Follow-ups:** none owed by anyone but the board — the orchestrate watcher drives the children; s5 wakes when the orchestration reaches tada (complete or halted). No kill criterion tripped; the maintainer was not involved.
