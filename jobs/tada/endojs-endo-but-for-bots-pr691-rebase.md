Rebased endojs/endo-but-for-bots#691 onto `llm` (`e8edeb2b23`) and force-with-lease pushed `983f51e73b`.

Resolved the README overlap preserving both sequencing and newer eval notes. `git diff --check` passed; Prettier could not run because dependencies are absent. Posted PR summary and removed the isolated worktree.
