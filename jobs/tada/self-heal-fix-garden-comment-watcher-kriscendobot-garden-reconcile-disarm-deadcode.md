Implemented and pushed `55feb33e49` to `main2`.

- Reconcile now discovers armed watcher instances via `list-units --all`.
- Mock distinguishes templates from instances.
- Regression test verifies unwatch issues `disable --now`.
- Disabled all three requested local `kriscendobot-garden` timers.
- Verified with `scripts/jobs/test/run-test.sh`.

Self-improvement: nothing this time.
