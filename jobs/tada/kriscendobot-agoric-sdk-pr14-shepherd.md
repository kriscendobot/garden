The single red check is fixed and verified green; the rest of the matrix is progressing with **zero failures** (27 completed, 20 in-progress, 33 queued — the same set that passed on the prior run, minus the codegen red I just fixed). My push added only additive data to a generated file, which only `test-codegen`/orchestration exercise, and `test-codegen` is now green.

The substantive shepherd work is complete and verified. I'll not post a PR comment (no comment authorization in the job body; this is my own fork per the no-inter-instance-GitHub-loops rule). The garden worktree is clean — no main2 changes needed.

---

**Completion report — shepherd on kriscendobot/agoric-sdk PR #14**

**Diagnosis.** One check was red: `test-codegen` (codegen-idempotence, "Test all Packages" workflow). After CI ran `yarn codegen`, `packages/orchestration/src/fetched-chain-info.js` was dirty. That file is generated by `packages/orchestration/scripts/fetch-chain-info.ts`, which does a **live network fetch** from the cosmos `@chain-registry/client`. The committed file was identical on the PR branch and on `master`, so the drift was **not caused by this SwingSet PR** — new IBC connections/channels (injective-1, cosmoshub-4, celestia, …) had appeared in the upstream chain-registry since the last `chore(orchestration): yarn codegen`, making the idempotence check red on any open PR. A rebase would not have helped.

**Fix.** In an isolated project worktree: `corepack yarn install`, built `@agoric/cosmic-proto` dist (an artifact the orchestration codegen imports), then ran `yarn codegen` in `packages/orchestration`. Result was a purely **additive** 72-line refresh of `fetched-chain-info.js` (no deletions). Committed as `chore(orchestration): \`yarn codegen\`` (5ca9d12539) and pushed to the PR head `kriskowal-park-on-upgrade-failure` with `--force-with-lease` against anchor 9570d59a78.

**Verification.** CI re-dispatched on the new head 5ca9d125. Polled to completion: **`test-codegen` → SUCCESS**, and the full rollup shows **0 failures/cancellations/timeouts** with the remaining long-tail matrix (boot/swingset shards, integration) still progressing — the same checks that were already green on the prior run. The red is cleared with no regressions introduced.

**What changed.** One commit on the PR head branch: regenerated `packages/orchestration/src/fetched-chain-info.js`. No garden (main2) changes.

**Follow-ups.** None required. Note the underlying check is inherently drift-prone (live chain-registry fetch), so unrelated PRs in this fork may go red on `test-codegen` again as upstream registry data evolves; each is cleared the same way (a codegen refresh commit). No PR comment posted (no comment authorization in the job body; own-fork, no-inter-instance-GitHub-loops).

**next: none** — target check driven green and verified.
