Done. Summary of the work:

## Completion report

**Intent recovered.** The dead-lettered message was addressed to `--help` with an empty body — the fingerprint of `inbox-send.sh --help` being run to read usage, where `--help` was consumed as the `<doer>` positional, found no live inbox, and dead-lettered an empty message (which garden-deadmail promoted into *this* job). The intent behind the message was "show me help," and the actionable defect it exposed was the `--help`-as-positional misfire.

**Fix landed on `main2`** (commit `33dde773be`):
- `scripts/jobs/inbox-send.sh` — added a `usage()` and a `case "${1:-}" in -h|--help) usage; exit 0;;` interceptor *before* the positional is consumed, plus a `-*` guard so any other option-like doer typo fails fast instead of dead-lettering. This mirrors the identical, precedented guard already in `post-job.sh` (lines 70-72) and `journal-entry.sh` (lines 59-62), which document this exact bug class.
- `scripts/jobs/message-user.sh` — the delegating sender got the same `-h|--help` guard so `--help` there no longer posts an empty `reply_to=--help` message to the maintainer inbox.

Verified: `--help`/`-h` now print usage and exit 0 with no journal side effects; `bash -n` clean.

**Process note / self-inflicted noise (disclosed to maintainer, msgid `073747Z-651a0a`).** During verification I ran the still-unpatched *deployed*-root scripts with `--help`, reproducing the misfire and creating disposable noise before I caught it. I reverted the stray deployed-root working-tree edits (no git run in the root — reversed via editor), then re-did the work correctly in my job worktree. The noise, all safe to discard at muster: 2 duplicate deadmail jobs (`073340Z-0229ab`, `073348Z-2bd99c`) and 1 empty maintainer message (`073358Z-5ec034`).

**Follow-ups (optional):** the remaining inbox-family scripts (`inbox-read.sh`, `inbox-list.sh`, `block-job.sh`, `unblock.sh`) still lack `--help` guards, but none have destructive side effects on a `--help` typo, so I left them out of scope. The two duplicate deadmail jobs are self-resolving (a claimant will find the fix already merged).
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/deadmail-20260822T072116Z-cf8821.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 54 tokens (1649602 cached reads)
- Output: 19452 tokens
- Cost: $1.8498919999999999
- Wall-clock: 364s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
