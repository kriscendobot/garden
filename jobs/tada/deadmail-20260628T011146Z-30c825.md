Done. Completion report follows.

---

## Completion report — dead-lettered job `deadmail-20260628T011146Z-30c825`

**Intent picked up:** the dead-lettered message carried kriskowal's new comment on `kriskowal/garden#9` — *"Perhaps we can instrument XS to report the trace where it overflows."* (the ymax0 v320 XS stack-overflow investigation). I executed that ask and replied on the issue thread.

**What I did:**
1. Reconstructed the on-host repro harness (`/tmp/xs6` driver + `~/.cache/garden-scratch/exp6-ymax0` from-source xsnap tree + the beta3 ymax0 bundle), and confirmed the baseline overflow with the prebuilt 0.14.2 worker.
2. **Instrumented XS:** edited `xsnapPlatform.c`'s `fxAbort(XS_STACK_OVERFLOW_EXIT)` arm to dump, before the uncatchable host exit — the XS frame chain with per-frame value-stack slot consumption, the value-stack fill level, and a native C backtrace (`backtrace_symbols_fd`, `-rdynamic`). Built only the worker with gcc 13 and ran the real ymax0 `importBundle` under it.
3. **Validated faithfulness:** trivial-bundle control imports OK; a genuine deep recursion overflows at **394 frames** (~10 slots each), matching the prebuilt 0.14.2 budget — so the from-source worker is not an artifact.

**Key finding (corrects every prior synthetic estimate on the issue):** the overflow is **value-stack exhaustion, not deep recursion**. Value stack full at **4096/4096 slots but only 9 XS frames deep**, aborting from `mxOverflow` inside `fxRunID`. Two adjacent frames during contract module evaluation (`portfolio.contract.bundle.js:21`) hold ~2,588 + ~1,238 = **93% of the 4,096-slot stack** — one *wide* activation, not a deep `passStyleOf` descent. XS's value stack is fixed at `stackCount=4096` (no CLI override; same on-chain), so the production exit-12 crash is this same exhaustion. Fix direction: shrink that one wide frame below 4,096 slots; raising `stackCount` is last-resort.

**Posted:** https://github.com/kriskowal/garden/issues/9#issuecomment-4823848562 — full trace, methodology, and the reappliable instrumentation patch. Issue left **open** (submitter closes).

**Memory:** extended `reference_xsnap_xs_repro_without_agoric_build` with the instrumentation/build recipe (incl. the `build.config.env`-must-exist and symlink-clobber gotchas), the corrected mechanism, and a note that `/tmp` + `~/.cache/garden-scratch` are wiped by redeploys.

**Scope:** read-only analysis plus an on-host build of the open-source XS engine and public Endo packages — no agoric-sdk push, PR, or merge.

**Follow-ups (left for the maintainer, noted in the comment):** the slot-hog is an anonymous frame at minified bundle line 21; naming the exact contract definition needs a sourcemap re-bundle or a stub-bisection — offered but not run (it edits the contract build, agoric-sdk territory).
