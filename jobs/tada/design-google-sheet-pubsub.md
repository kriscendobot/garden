Everything is done: design written, README synced, parent cross-linked, branch pushed, draft PR opened, provenance comment posted, inbox empty. Final report:

Design job `design-google-sheet-pubsub` complete. The design slug is **google-sheet-pubsub**; the draft PR is **endojs/endo-but-for-bots#622**.

**What I did.** Wrote `designs/google-sheet-pubsub.md` (the push phase that `designs/exo-google-sheets.md` deferred as its Resolved Question 2 / Phase 5), on branch `design/google-sheet-pubsub`, opened as draft PR #622 based on `design/exo-google-sheets` (PR #612's head) so the diff shows only this design and GitHub retargets to `llm` when the parent merges. Facts about the Drive push protocol (default 3600s / max 86400s channel TTL, no renewal API, empty notification bodies, token-header spoof check, retry-on-5xx-only delivery) were verified against Google's current push guide before drafting.

**What the design decides.** (1) Placement: pubsub is its own package, `@endo/google-drive-watch` — a plain, no-`exo`-prefix substrate owning the channel lifecycle (fresh-UUID arming with per-channel secrets, replacement-before-expiry with overlap, `channels.stop`, validation, message-number dedupe, durable records reconciled on restart) shared by all Drive-family watchers; not a mode of exo-google-sheets, not a gateway feature. (2) Notifications are unauthenticated nudges only — they trigger the same coalesced authenticated read-and-diff step polling performs, so forged webhooks can at most cause throttled re-reads. (3) Fan-out is one channel per file, one batched read per nudge, per-follower diff locally; `changes.watch` account mode deferred for many-file hosts. (4) Polling survives as a long-interval backstop since Google delivery is best-effort. (5) The sheet-as-queue recipe (appendOnly producers, follow consumers, at-least-once with idempotency keys and consumer checkpoints) is worked end to end. Four open questions are surfaced, notably a proposed handler-capability machine-delivery refinement to endoclaw-webhooks (with an inbox-follow adapter as the unblocked fallback) and the narrowest Drive scope for `files.watch`.

**What changed.** New `designs/google-sheet-pubsub.md`; `designs/README.md` synced on top of #612's edits (summary table row, M7 bucket and count 12 → 13, dependency graph edges `esheets --> gsheetpub` and `ewebhook --> gsheetpub`, per-design estimate S-M/3 days, totals 58 → 59, recently-added and last-updated lines); `designs/exo-google-sheets.md` Phase 5 now cross-links the filed design. One commit, `3d89ebf2b`, pushed under the bot identity. A provenance comment linking #622 was posted on #612 (standing repo authorization).

**Follow-ups.** The endoclaw-webhooks machine-delivery refinement job should be filed if the maintainer accepts Open Question 1's proposal; the Drive-scope and domain-verification questions are pinned to Phase 3 implementation, not design. When #612 merges, #622 retargets to `llm` automatically; if #612 is instead force-pushed, #622 needs a rebase.

Self-improvement: the designer role's PR norm does not cover a follow-up design whose parent design PR is still open; basing on the parent's `design/<slug>` branch (auto-retarget on merge) kept the diff and README counts coherent and may be worth encoding in `roles/designer/AGENT.md`.
