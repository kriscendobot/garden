I now have the complete picture. Here's what I found:

**The dead-lettered message is a status report** from child 2/3 (`xs2rust-endor-strings-utf16-build`) to the supervisor `port-xs-to-rust-memory-safe-engine-s7` — i.e. a *reply/report* to that doer, not a request asking it to do work. The instruction is therefore to "carry the reply forward."

Verifying the intent is preserved and the successor path is intact:

1. **Content is durably on the board.** Every substantive point in the dead-lettered message (UTF-16 big-endian storage, code-unit metering with ASCII-unchanged computrons, deleted O(1)-index machinery, C-FFI transcoding, the "xsnap boundary doesn't apply" note, `cargo test --workspace` green with total=953/covered=322/divergent=0, Miri-clean, PR #600 DRAFT) is already recorded verbatim in `jobs/tada/xs2rust-endor-strings-utf16-build.md`. That report even explicitly anticipates this: *"Supervisor report to port-xs-…-s7 was dead-lettered → will be promoted to a fresh job, intent preserved."* This job is that promotion.

2. **The orchestration is advancing correctly on its own.** `xs2rust-endor-strings-utf16` is serial (design→build→test), `state: running`. Build is in `tada/`; child 3 `xs2rust-endor-strings-utf16-test` is **live right now** — the deterministic `orchestrate.sh` watcher already promoted it after build landed. Promotion is owned by that watcher, not by any inbox message, so the message never gated the mechanics.

3. **The successor consumer reads the board, not the inbox.** s7's role continues in `port-xs-to-rust-memory-safe-engine-s8`, parked `blocked_on: xs2rust-endor-strings-utf16`. Its spec explicitly instructs it to *"read the three children's tada reports (xs2rust-endor-strings-utf16-{design,build,test}) FIRST"* when the orchestration reaches `tada/` and unblocks it. So s8 receives this exact intent from the durable `-build` tada record.

No board mutation, PR change, or message is warranted:
- Messaging the live **test** child would be duplicate noise — it was promoted with the build tada report already available to it.
- Messaging **s8** (parked/blocked, not a live agent) would just dead-letter again and spawn another redundant job — a noise loop — while s8 already gets the content from the board when it wakes.

The reply is fully carried forward by durable board state and the deterministic orchestrator; the correct action is to confirm that and close, which itself leaves a `tada/` trace for this dead-letter reconciliation.

---

## Completion report

**Job:** `deadmail-20260706T030053Z-a1f859` — pick up the intent of a dead-lettered message addressed to `port-xs-to-rust-memory-safe-engine-s7`.

**Classification:** The message was a **status/completion report** (child 2/3 `xs2rust-endor-strings-utf16-build` → supervisor s7), i.e. a *reply to* the completed doer — not a request. Action required: carry the reply forward, not perform new build work.

**What I did:** Traced the intent to confirm it is preserved and reaches its successor consumer. Verified against `origin/journal2`:
- The full message content is durably recorded in `jobs/tada/xs2rust-endor-strings-utf16-build.md` (the build child's own completion report), which already flags the dead-letter and that this promoted job preserves the intent.
- The `xs2rust-endor-strings-utf16` orchestration (serial, `running`) is progressing correctly: build is in `tada/`; child 3 (`-test`) is live now, promoted by the deterministic `orchestrate.sh` watcher — the mechanics never depended on the inbox message.
- The successor supervisor `port-xs-to-rust-memory-safe-engine-s8` (parked `blocked_on: xs2rust-endor-strings-utf16`) is specced to read all three children's tada reports on unblock, so it receives this exact status from the board.

**What changed:** Nothing on `main2`, no PR/project change, no board mutation. No inbox messages sent — the two candidate recipients (live `-test` child; parked `s8`) already receive the content via the board, so any message would be duplicate noise or a dead-letter loop.

**Follow-ups:** None dangling. The strings-utf16 orchestration will complete when the live `-test` child reaches `tada/`, deterministically unblocking `s8`, which then verifies the fixer + utf16 and dispatches stage 4 — all owned by the board.
