Done. The dead-lettered message's intent has been carried forward durably, matching the established three-precedent pattern in this program.

**What this job was.** A bus message from the still-live stage-3 child-2 builder (`xs2rust-endor-build-stage3-fundamentals`, PR #600) to the Fable supervisor `port-xs-to-rust-memory-safe-engine-s6` dead-lettered — not because s6 *finished*, but because s6 is a **parked plan job** (`gate: blocked`, `blocked_on: xs2rust-endor-build-stage3`) that has no live inbox until it is claimed. The message is child-2's "RESUME 2 update (after reap 2)" progress report — a report to its supervisor, not a task for me. Three prior dead-letters from this same child (child-1 scope-fold, child-2 progress, child-2 RESUME) were each resolved by appending a "Carry-forward" section to s6's spec file on `journal2`; this is the fourth in that series.

**What I did.** Appended a new, precedent-matching section — *"Carry-forward: dead-lettered stage-3 child-2 fundamentals RESUME 2 update"* — to `jobs/plan/port-xs-to-rust-memory-safe-engine-s6.md` on `journal2`, so the report reaches s6 when the stage-3 orchestration unblocks it. The section records provenance, quotes the message body verbatim as DATA, and gives the supervisor a concrete review/ruling list consistent with the earlier notes:
- **Discharges** the RESUME note's flagged blocker: child-2's new *native prototype-method dispatch* is exactly the "foundational capability not yet built" that note named — it landed `Object.prototype.hasOwnProperty`/`valueOf`/`isPrototypeOf`/`toString`, `Error.prototype.toString`-as-method, and `Function.prototype.call`; those move from "schedule" to "re-verify as landed."
- **Rules on the narrowed remainder:** `Function.prototype.apply` → fold into child 3 (arrays) alongside the child-1 `at` revisit (needs array element read); `Function.prototype.bind` → result-correct but reverted, tagged "metering-only" (non-uniform `length`-metering, +33584 vs +288, needs reverse-engineering); Symbol + AggregateError + Object statics remain open.
- **Friction ledger:** the global symbol-interning table gap (now also gating `hasOwnProperty`/`in`/`hasInstance` for non-program-symbol keys — the same cross-child string→id dependency), unmodeled sloppy primitive-`this` boxing (new), and the persistent sub-computron property-create residuals.

**What changed.** One commit on `journal2` (`e1aec01f1`), pushed via a fetch/reset/CAS loop (succeeded first attempt, idempotency-guarded). Frontmatter intact; s6 remains parked in `plan/`. I did **not** touch the XS→Rust engine code — that is child-2's live charter (it was just re-claimed by gardener-13 and is continuing per its own requeue).

**Follow-ups (for others, not this job).** None owed by me. When the stage-3 orchestration reaches `tada/`, s6 promotes and its supervisor will read all four carry-forward sections during stage-3 review. Child-2 remains live and will file the authoritative `tada/` report when it finishes apply/bind/Symbol.
