## Completion report — job `dependabot-botany-rollout`

**Phase 1 — botanist role updated and pushed.**
`roles/botanist/AGENT.md` rewritten and pushed to `origin/main2` at SHA **`495e5531`** (frontmatter `updated: 2026-06-24`, `author: gardener, liaison`, with the one-line note that the role now carries autonomous accept/close/defer authority for dependabot PRs on bot-owned repos). The safety posture is preserved intact; the additions are:

- **CI-shepherd workflow step** (step 6): drive CI to green before MERGE-NOW by reusing `roles/shepherd/AGENT.md`, classify each failure flake-vs-real explicitly, and cross-check (not trust) the existing rollup against the head SHA; real-red routes via `next: fixer`/`weaver`/`liaison`.
- **Strengthened transitive security investigation** (steps 2 + 5): enumerate every moved lockfile version, flag newly-introduced packages and any version <24h old, and check each moved version against GHSA / `npm audit` / OSV — one advisory on any transitive version blocks MERGE-NOW.
- **Maturity/compromise** kept (7-day embargo; yank-republish / disowned / no-adoption → REJECT or EMBARGO).
- **Autonomous disposition section** (the new authority): MERGE-NOW → conduct onto main (conductor discipline, no merge method named); REJECT → `gh pr close` with the structured verdict comment; EMBARGO → record maturity date in the dependabotany ledger **and** ensure an idempotent daily `dependabotany-recheck-<project>` schedule via `set-schedule.sh`. I chose the recurring-daily-sweep-over-ledger primitive over a per-PR one-shot and documented *why* in the role: the scheduler has no one-shot mode (a fresh schedule's empty `last_dispatched` reads as immediately-due and re-fires every cadence), so the ledger date is the gate and the daily schedule is the rot-proof heartbeat.
- **Explicit gate** next to the authority: auto-conduct fires only when CI green AND (maturity satisfied OR real CVE closed) AND source-read clean AND transitive set benign; green CI alone is never sufficient. Authority is scoped to bot-owned repos; on non-owned upstreams the verdict is a recommendation only.

**Phase 2 — fanned out after Phase 1 was confirmed on the remote.**
Fresh enumeration found **10** open `dependabot[bot]` PRs on `endojs/endo-but-for-bots`: **#512, #275, #274, #273, #271, #270, #269, #268, #267, #197** (matched the posting-time list). Posted one `botany-ebfb-pr<N>` job per PR; each names the repo and PR, directs the claimer to wear the botanist role per the updated file, carries explicit per-action authorization for comment / merge(conduct) / close / schedule-defer, and instructs shepherd-CI + transitive read + verdict + execute. **All 10 verified present on `origin/journal2`** (9 already claimed into `doin/` by the fleet, #197 in `todo/`).

**Bug found and fixed mid-job (not just claimed-done).** The first `post-job.sh` pass reported "posted" for all 10 but only **4** actually reached `origin/journal2`. Root cause: the shared producer clone (`$GARDEN_STATE/producer/journal`) gets `reset --hard origin/journal2` by concurrent producers (triager/scheduler) between a local commit and its push, clobbering the commit; the no-op push still exits 0, so `post-job.sh` falsely reports success. I re-posted the missing 6 through a **private** producer clone and verified each landed. Filed a self-improvement message to `role/liaison` (`20260624T103423Z-7dd227`) proposing a post-push ref-advance check / default private clones / flock on `set-schedule.sh` and `post-job.sh`.

**Deliberate scope exclusion (needs maintainer authorization to widen).** Fan-out was confined to `endojs/endo-but-for-bots`. I did **not** post botany jobs for **`endojs/endo`** (30 open) or **`agoric/agoric-sdk`** (30 open): the bot cannot autonomously merge into those upstreams, and widening botanist surveillance to them is gated by the standing monitoring-safety constraint in `CLAUDE.md` (requires explicit maintainer authorization recorded in a journal `message`). The maintainer can widen deliberately once that authorization is on record.

Self-improvement: filed the `post-job.sh` shared-clone race lesson to the liaison; recommend batch posters use a private `GARDEN_PRODUCER_CLONE` and verify each basename on `origin/journal2` until the script gains a post-push verification.
