Implemented and pushed `8cba46a9cc` (`feat(stream): add unbounded buffer`).

- Added `@endo/stream/buffer` and `/buffer/unbounded` with hardened `{ spring, sink }`, terminal operations, types, docs, tests, and minor changeset.
- Documented GTR rationale and bounded-buffer separation.
- Verified 22 stream tests, stream/types lint (zero errors), type validation, docs, formatting, and package contents.
- Opened draft PR: https://github.com/endojs/endo-but-for-bots/pull/858 (frozen `llm` base); CI is running.
