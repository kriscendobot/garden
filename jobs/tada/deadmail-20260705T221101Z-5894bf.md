The dead-letter pickup is complete. Front-matter (`gate: blocked`, `blocked_on: xs2rust-endor-build-stage3b`) is intact, so the job stays correctly parked for the orchestrate watcher.

---

## Completion report

**Job:** `deadmail-20260705T221101Z-5894bf` — pick up the intent of a message dead-lettered because its addressee `port-xs-to-rust-memory-safe-engine-s7` had no live inbox.

**What the message was:** a status/follow-up report from the xs2rust-endor **stage-3b child 7/9 (Promises), PR #600** — addressed to the Fable supervisor `port-xs-to-rust-memory-safe-engine-s7`. It reported already-completed, already-pushed, all-green work (HEAD `bc785b265`), the reverted thenable-adoption attempt, the remaining honest named skips, and a follow-up for a future increment. It was a *reply to that doer*, so the task was to **carry the reply forward**, not to build anything.

**What I found:**
- `s7` is not a completed doer but a **parked supervisor** (`jobs/plan/`, `gate: blocked`, `blocked_on: xs2rust-endor-build-stage3b`) — its inbox doesn't exist yet, which is why the message dead-lettered.
- The stage-3b orchestration is serial, 7/9 children in `tada/` (promises among them), child 8 `xsre-core` in `doin/` (a live peer gardener), child 9 `xsre-integration` parked. When all 9 land, the orchestrate watcher promotes `s7` and the supervisor runs its review loop.
- The message's content is **already durably recorded** in `jobs/tada/xs2rust-endor-build-stage3b-promises.md` (which the supervisor reads during review) — so no build/verify work was warranted, and re-sending to the parked `s7` inbox would only re-dead-letter into a loop.

**What I did:** appended a clearly-marked **carry-forward note** (the promises child's final green state, the reverted-adoption detail, the remaining named skips, and the double-settle follow-up) to the tail of the parked `s7` supervisor job body — the doc the supervisor always reads on wake — with an idempotency guard keyed on this job base so a requeue won't double-append. Front-matter left untouched. Committed and pushed to `journal2` via a CAS rebase loop (`9aa41ef12`), off a clean journal worktree.

**Changed:** `jobs/plan/port-xs-to-rust-memory-safe-engine-s7.md` on `journal2` (append only).

**Follow-up carried (for the supervisor to dispatch, not me):** nail the native-handler reaction job's double-settle (adopting promise + discarded derived) bit-exact — it unblocks both thenable adoption and the `all`/`race`/`allSettled`/`any` combinators in one stroke (shared native-function-reaction-handler machinery). No separate parked job was created for it, since dispatch is the supervisor's decision and it now has the item explicitly in its spec.
