Standing down as instructed.

## Completion report

**Job:** `deadmail-20260706T233345Z-ed16be` — a dead-lettered message originally addressed to the already-completed `design-transcript-journal-capture`.

**What happened:** The dead-lettered note was a maintainer update on the transcript-durability design: storage decision made (dedicated `transcripts2` orphan branch, not `journal2`), keep it a lean reference spec since a Fable supervisor owns the build, and additionally cover a liaison broadcast-reader mechanism the original doc lacked. I picked up that intent and revised the existing `designs/transcript-journal-capture.md`:

- **Firmed Decision 1** to the decided `transcripts2` orphan branch (trimmed the re-litigation of rejected stores).
- **Added Decision 5 (bring-up surfacing)** — optional transcripts-remote arming offered during the starting stage, mirroring the issue-inbox arming prompt.
- **Added Decision 6 (liaison broadcast-reader)** — liaisons drain `role/liaison` + `broadcast` on bring-up via `read-msgs.sh "liaison-$GARDEN" …`, closing the standing gap that no fleet broadcast reaches a liaison today (gardeners already poll `role/gardener`+`broadcast`; liaisons watch only the maintainer inbox).
- Reframed the header/status as a lean reference spec owned by `supervise-transcript-durability`; updated the builder spec and the `designs/README.md` index row; resolved the now-settled open questions.

Committed and pushed to `main2` (rebase-CAS, PUSHED).

**Then:** my inbox held a **stand-down** from the liaison — the job is moot, the design is complete, and a supervisor (`supervise-transcript-capture`) owns implementation. It arrived *after* my push had already landed. I took no further action and notified the maintainer of the timing, noting the revision is purely additive spec content matching their own later directives, and left the keep-or-revert call to them.

**Follow-ups:** none from me. If the maintainer prefers the supervisor to own all doc edits, they can revert the one commit on `main2` touching `designs/transcript-journal-capture.md`.
