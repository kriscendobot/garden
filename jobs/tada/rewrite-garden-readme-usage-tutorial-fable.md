The garden README is rewritten as a graduated usage tutorial and landed on `main2`.

**What I did.** Fanned out four research agents over the tree (getting-started plane, control surfaces, internals, vocabulary/services) so every claim in the new README is grounded in a verified file, script, or design doc — then wrote the tutorial and verified every relative link and `scripts/jobs/*.sh` path resolves before committing.

**What changed.** `README.md` (391 insertions, 282 deletions), landed as `7241a43f7` plus amendment `a6461b9d7` on `main2`:

- **Introduction** opens with the slogan ("the garden mostly grows by itself, but to get what you want, you have to pull weeds") and frames design→build as an OODA loop that runs until the machine anticipates no further maintainer feedback, with the appellate tuning the tail (small in-context deferrals promoted into the same change) and the proxy answering readily-anticipated gating calls.
- **Getting started**: Docker + `./garden build`/`./garden`/`reset`; bot SSH keys under the bind-mounted `.ssh/` (with the launcher's deliberate non-forwarding of `SSH_AUTH_SOCK`); `gh auth login` as the bot; claude subscription login in-container (the practiced path) with the `ANTHROPIC_API_KEY` alternative; fleet bring-up including leader designation; the bulletin PAT pointer to `docs/bulletin/SETUP.md`; and the liaison verb table (gauntlet-era, with the deterministically-recognized verbs starred).
- **Five control surfaces**, each with common interactions mined from the roles/skills/scripts: the liaison CLI, the garden's own GitHub issues (allowlist gate), other repos (fork model, comment verbs, @-mentions, reactji ack), the permissioned ferry path (host preconditions, `GARDEN_GH_IDENTITY` override, attribution discipline), and the GitHub Pages bulletin (PAT reply round-trip onto `journal2`).
- **How it works, last**: the life of a pull request (separate design and build PRs; the gauntlet's assayer → cleaner → 26-seat panel → fixer loop → appellate → un-draft) and of an issue; self-healing (self-heal-run wrapper, reaper, deadmail, follow-up, mentor, watchman); the context library's token-efficiency disciplines; plan/velocity/milestone tooling with the weekly recalibration; and the bid/accept market with reputation ledger as the next metamorphosis — accurately marked as Proposed (issue #15), with today's claim race as its degenerate case.

Mid-job, a peer landed the `GARDEN_SHARD` durable-identity change to the launcher; the amendment commit folds that into the getting-started step so the tutorial matches the launcher it shipped beside.

**Follow-ups.** Research exposed `CLAUDE.md` drift the README couldn't fix in scope: the stale "gamut" idiom, dead pointers to `§ Vocabulary` sections and `roles/steward/`, and six skills named in its inventory that don't exist under `skills/`. I parked a deferred plan job `reconcile-claude-md-with-v2-tree` naming each finding. Also noted for the record: the bot-account SSH-key provisioning procedure existed nowhere in the repo before this README — the tutorial now documents the location and steps, but a dedicated setup doc could go deeper. This supersedes the standing "refresh the garden README once services settle" note.
