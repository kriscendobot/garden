Cycle complete. Writing my report.

---

## Completion report — finbot progress cycle `20260729-012002`

**Headline: the finbot gate is blocked on a stalled DEPLOY, not on missing code.** This overturns the premise of the last week of cycles.

**Assessment.** Three finbot PRs (`kriscendobot/finbot#4/#5/#6`) are CI-green, draft, mergeable — all blocked on the merge-governance panel. Every panel job (five of them: #4, #4-rerun, #5×2, #6) is sitting **poisoned+parked** in `jobs/plan/` on `deadline-overrun` / `requeue-exhausted`. Prior cycles read this as "the panels are structurally broken" and kept re-posting them (or building new increments on stuck PRs).

**Root cause (the real finding).** The panels aren't broken anymore. `origin/main2` **already contains the fix**: bounded-parallel seat fan-out (`GARDEN_PANEL_CONCURRENCY`, default 8, with its own `panel-parallel-fanout-test.sh`) plus "stage the gauntlet into claim-sized stages" + panel single-round mode — a 28-seat panel now fits one claim budget by construction. But the **deploy is stalled**: the deployed root is pinned at `3f44c0c` (last advanced Jul 28 08:25, ~17h ago) while `origin/main2` is **53 commits ahead**. `garden-upgrade-monitor` fires every ~5 min emitting "Upgrade ready: main2 ahead by 53 commit(s)" and writing `.garden-state/deploy/upgrade-ready`, but nothing consumes it — the deploy-on-upgrade Monitor isn't running the deliberate `deploy-garden.sh`. So the fleet still runs the 50-commit-stale **serial** `panel.sh`, which overruns the claim on every finbot panel.

**Action taken.** Diagnosed and reported. I did **not** build a new increment (main2 already holds the one that matters; a 4th PR atop three gate-stuck ones is the named sprawl anti-pattern) and did **not** re-post panels (pre-deploy they'd just overrun again). Messaged the maintainer (`message-user.sh`) with the reframing, the exact evidence, and the recommended sequence: run the deliberate `deploy-garden.sh` (drains the fleet — a maintainer/liaison call, not a gardener's), then re-post fresh un-poisoned panels for #4/#5/#6 → Fable sign-off → gate drains.

**Correction (disclosed).** Early on I read and edited the *deployed root's* stale `panel.sh`/`SKILL.md` by mistake (reinventing the parallelization that main2 already has) instead of my worktree. I **restored both files byte-identical to their committed `3f44c0c` state** and removed the two scratch test files I'd created there — verified the live tree is clean and unmodified. No finbot source touched, nothing merged, no `gh pr merge`. Governance intact.

**Next unblocked step (for the maintainer/liaison, not a gardener).** Deploy `main2` to the root; is the deploy-on-upgrade Monitor down or intentionally held? Nothing on the finbot gate can move until that deploy lands — after which the previously-poisoned panels clear on the default budget.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/finbot-progress-20260729-012002.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 97 tokens (4848069 cached reads)
- Output: 55184 tokens
- Cost: $5.083878499999998
- Wall-clock: 944s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
