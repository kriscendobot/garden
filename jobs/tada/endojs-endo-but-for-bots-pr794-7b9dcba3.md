Updated PR #794 with static responder mapping and graceful `SIGHUP` reload semantics.

Pushed `128bb78ea` and posted the PR completion summary. Validation: Prettier and `git diff --check` passed.

Follow-ups: none.
