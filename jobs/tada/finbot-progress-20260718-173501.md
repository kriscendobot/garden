Done. This cycle's increment is landed and green.

## Completion report

**Cycle:** finbot-progress-20260718-173501 (builder)

**Assessed state:** No open PRs on `kriscendobot/finbot` — work lands directly on `main`. The prior cycle (finbot-progress-20260718-112002) landed `auto-egarch` and named the next step: "walk-forward OOS evaluation harness with QLIKE and naive baselines." No `finbot-*` job was in flight on that step, so I took it (the single deepest unblocked increment toward the ensemble-forecasting design).

**Advanced this cycle** — landed `kriscendobot/finbot` main `4d0e902 → 49430db`:
- New `packages/simulator/vol-eval.js`: a walk-forward, strictly out-of-sample volatility-forecast evaluation. Splits each series at a train/test boundary, fits every GARCH-family model (garch/gjr/egarch/auto-egarch MLE) on the training prefix only, then rolls each forward one step at a time through the held-out suffix — forecasting `h_t` before observing `r_t` — and scores the conditional-variance forecasts against the `r²` proxy with **QLIKE** (proxy-robust, asymmetric) and **MSE**.
- Three **naive baselines** (constant train variance, RiskMetrics EWMA λ=0.94, rolling window) as the honesty check. The output bears out the contract: GARCH wins on structured cyclic/synthesis presets; a naive baseline wins on i.i.d. constant-vol `gbm-flat-lowvol` — exactly where GARCH shouldn't earn its complexity.
- Surfaced as a section of `bin/finbot-eval` (`--vol-length`, `--vol-train-fraction`), exported from `index.js`, added to `package.json` exports, documented in the simulator README.
- 9 new tests. Full suite green: **585 ok, 0 fail, exit 0** (the initial `@finbot/harness` failure was a missing-workspace-link in the fresh worktree, resolved by `npm install`; unrelated to the change).

**Next unblocked step (handed off, not done here):** turn the OOS QLIKE ranking into a *selection signal* — let auto-egarch / the regime read pick its surface by walk-forward QLIKE rather than only in-sample gamma evidence, validating model choice out of sample.

**Maintainer decision needed:** none. Notified the maintainer inbox.
