CI is queued/running on all three new heads and the inbox is empty. This tick's work is complete — writing the report.

# SturdyRef press tick — completion report (endo-sturdyref-press-20260726-112002)

**What I did:** Took the wheel (no live peer or cascade on the stack — `inbox-list`/`jobs/doin` showed only xs2rust and google-sheets work) and executed the previous tick's first check: CI on the restacked bridge cuts came back **#698/#700/#701 fully green** but **#702/#703/#704 red on `lint`**, with #704 also red on `test (24.x, ubuntu-latest)`.

**Root cause & fix (the lint red, deterministic):** the lint job's `build API docs` step failed with `TS2339: Property 'SturdyRef' does not exist on type 'typeof globalThis'` at `packages/sturdyref/src/sturdyref-shim.js:146`. The shim blob is byte-identical on the lint-green PRs below, so I traced the trigger to cut 4's new daemon imports of `@endo/ocapn/{cryptography,syrup}`, which pull the shim source into per-package typedoc programs that don't load the ambient `shim.types.d.ts` global declaration. Fixed at the point of manifestation — `build/sturdyref-bridge-4-ocapn-singleton` (#702), sparing the six green PRs below a restack: the read now goes through an explicit `/** @type {{ SturdyRef?: SturdyRefNamespace }} */` cast, typing-only, no runtime change. Pushed as a fast-forward append 9cda67148 → **137673219**, then restacked **#703** (5eef14afa → **bb3906aad**) and **#704** (c51aca470 → **e32b01f2a**) conflict-free with `--force-with-lease`; all PRs remain DRAFT; the line #774→#737→#541→#698→#700→#701→#702→#703→#704 has zero skew.

**Real-execution evidence:** reproduced the failure locally (`typedoc` → `Found 2 errors`, exit 3), and after the fix `typedoc` → **`Found 0 errors`, exit 0**; `eslint` + `prettier --check` on the file clean; `@endo/sturdyref` ses-ava suite **8/8**; on the new #704 head, daemon `ava --serial three-party-roundtrip ocapn` → **14 passed** including the armed cross-peer enliven over tcp-test-only. CI observed queued/in-progress on all three new heads — full-rollup green is **not yet verified** (in flight at report time; next tick's first check).

**#704's Node-24 failure:** an ava global timeout in `@endo/marshal` — rankOrder property tests running 6–9 s each on a slow runner, every completed test passing, and #703's Node-24 job green on the same marshal tree. Assessed as runner-speed flake; if the fresh run reproduces it, next tick should trim the sturdyref arbitraries' cost or raise the package timeout rather than re-roll.

**Confinement property preserved:** the change is typing-only; the suite re-exercised all three invariants — *no-location* (a sturdyref is passStyleOf-opaque and leaks no locator), *no-identification* (the same locator mints distinct, unlinkable sturdyrefs), *opaque/withheld* (a child compartment does not see the `SturdyRef` global).

**Recorded:** evidence comments on [#702](https://github.com/endojs/endo-but-for-bots/pull/702#issuecomment-5083321159), [#703](https://github.com/endojs/endo-but-for-bots/pull/703#issuecomment-5083321790), [#704](https://github.com/endojs/endo-but-for-bots/pull/704#issuecomment-5083321836); journal progress entry `entries/2026/07/26/114431Z-progress-gardener-8947a3.md`. Inbox drained three times; no maintainer or peer messages.

**Follow-ups for the next tick:** check CI on 137673219 / bb3906aad / e32b01f2a — lint should now be green everywhere; watch whether the marshal Node-24 timeout recurs. Once the stack is fully green, finish-line bar 1 (OCapN sturdyref support) is CI-complete end to end, and the frontier is bar 2 — the agent provide/accept surface (design #695: Lal/Fae/Genie + `@endo/agent-tools`) — best posted as a builder sub-job.
