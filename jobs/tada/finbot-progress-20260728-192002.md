Cycle complete. Summary follows.

## Completion report — finbot progress cycle `20260728-192002`

**Assessment.** Three open finbot draft PRs (`kriscendobot/finbot#4/#5/#6`), all CI-green, all blocked on the merge-governance gate. No finbot PR has passed the gate since it was created 2026-07-22 — the panels keep dying on `deadline-overrun`/`requeue-exhausted` (#5/#6 panels are poisoned+parked; #4's rerun panel has been running ~2h20m). Critically, PR #4's panel has a **real standing must-fix** that no one had implemented: the untrusted role program ran as `await program(...)` **on the host event-loop thread**, so a non-yielding program (`while(true){}`) blocked the loop and `timeoutMs` could never fire — the harness wedged, un-preemptible. Prior fixers scoped a Worker-based runner but explicitly punted it.

**Increment advanced (the deepest unblocked unit).** Built the preemptible worker-thread role-program runner for `kriscendobot/finbot#4`:
- Each role-program turn now runs in a dedicated **worker thread** (`packages/harness/sandbox/role-worker.js`); the host stays free to enforce the deadline and `worker.terminate()`, which interrupts even a tight synchronous loop.
- **JSON-only transport** both directions; host performs the authoritative assistant-message validation; worker rejects accessors/proxies/symbols on its side.
- Ambient globals distilled to a **token descriptor** the worker rebuilds from shared materializers; plain-data globals ship verbatim; unknown function-globals rejected loudly — the attenuator stays the sole narrowing point.
- Boundary primitives extracted to `packages/harness/sandbox/boundary.js` so host and worker share one hardened path.
- **Regression test**: a `while(true)` program with a 250 ms timeout rejects near the deadline (it *hangs forever* against the pre-fix path) + a worker-isolation assertion.
- Design doc note added to `designs/cap-attenuation.md`.

**Verification.** Root `npm test` (CI-equivalent, all packages) **green — 63/63** in the harness suite incl. the 2 new tests; bin dry-run healthy (`WALLET TOUCHED: false`).

**Landed / handed off.** Committed as `b58b125` and pushed to `feat/harness-compartment-role-program`. **Not merged, not un-drafted, no `gh pr merge`** — governance intact. Messaged the in-flight `finbot-pr4-panel-rerun-20260728` to re-review against the new head `b58b125` (it was on stale `a99c87b`), and reported the cycle to the maintainer.

**Next unblocked step.** `kriscendobot/finbot#4` awaits a panel on `b58b125` → Fable sign-off; `#5`/`#6` need fresh un-poisoned panels.

**Flagged to maintainer (recurring, non-blocking):** the true bottleneck is **panel execution** — full multi-seat panels keep timing out against the throttled 2-gardener pool, so correct increments land in a queue that never drains. Worth a decision (raise panel deadline / cap seats for small PRs / lift the pool throttle).
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/finbot-progress-20260728-192002.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 99 tokens (5078179 cached reads)
- Output: 58692 tokens
- Cost: $5.1852885
- Wall-clock: 875s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
