Retcon of endojs/endo-but-for-bots PR #600 (`feat(ironhorse): JavaScript engine in Rust, based on XS`) is complete.

## What I did
- Set up an isolated project worktree keyed by this job base and confirmed branch state. Saved the pre-retcon head `bc1fcc39` (tagged `pre-retcon-pr600`).
- **Base-currency finding:** the branch **lags its base `llm` by 3 commits** — it is *not* current with `llm`. Rather than fold those 3 commits in (that's the separate rebase/weave step the directive's orchestration calls for), I retconned against the branch's actual fork point / three-dot merge-base `75c3b0b7` — which is exactly the base GitHub uses to render the PR diff, so the displayed diff is preserved untouched.
- `git reset --mixed` to the merge-base, then restaged the net diff into **14 coherent conventional commits** (down from 353).
- Force-pushed with `--force-with-lease` to `origin` (endojs/endo-but-for-bots — the PR head lives there, not the fork; my first push to the wrong `kriscendobot` remote was correctly refused by the lease). New head `975edf72`.
- Posted the required PR completion summary comment (issuecomment-5201712941).

## Proof of invariance
- Post-retcon tree `92232d22…` is **byte-identical** to the pre-retcon head tree (`git diff pre..post` empty).
- Net diff vs base unchanged before and after: `1828 files, +83399, -17`. PR still `MERGEABLE`.

## Grouping (this is a Rust-workspace branch, not a JS-package one — adapted accordingly)
One commit per Rust crate (`ironhorse-vm`, `-compile`, `-regexp`, `-snapshot`, `-fuzz`, `xs-oracle`, `endor`), plus JS package commits (`test262-runner`, `daemon`), a `docs`, and a `build` commit.

**Documented departures from one-commit-per-package:**
- The 1713-file test262 **corpus** (`cases/`) split into its own `test(ironhorse-262)` commit, separate from the crate's hand-written source — it's a vendored data import.
- The **`c/moddable` submodule pin bump** (→ 8.3.1, `23b4d6b0`) grouped into `feat(xs-oracle)`, its consumer, not a standalone chore.
- **`chore: Update Cargo.lock`** carries both root and `rust/engine` lockfiles, kept separate from all code per the lockfile-separation discipline.

## Follow-ups (for the orchestration)
- **Rebase/weave onto current `llm`** (the 3-commit lag) remains outstanding — the directive's "at least one pressed engagement" for merge-conflict resolution. Then shepherd CI on the new head shape and merge.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/ebfb-pr600-retcon-5201229349.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 58 tokens (1874049 cached reads)
- Output: 26304 tokens
- Cost: $2.2480974999999996
- Wall-clock: 433s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
