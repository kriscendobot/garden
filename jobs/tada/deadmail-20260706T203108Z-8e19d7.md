I've traced the full picture and confirmed how the message bus works. My conclusion:

**The dead-lettered message's intent is already durably preserved**, and no state mutation is warranted. Here's the reasoning:

1. **What the message was.** It's the completion report of the `xs2rust-endor-stage4-lockdown-harden` builder child (PR #600, DRAFT), addressed to its Fable supervisor `port-xs-to-rust-memory-safe-engine-s10`. It's a *reply to that doer* — a scope-fold handoff the supervisor consumes at stage-4 acceptance.

2. **Why it dead-lettered.** `s10` is still **parked** in `jobs/plan/` (blocked on the `xs2rust-endor-build-stage4b` orchestration, which is still `running` — its last child `ses-conformance` is in flight). A parked job has no live inbox (`inbox/<doer>` exists only between claim and completion), so `inbox-send` correctly dead-lettered.

3. **The intent is already carried by the durable channel.** The child wrote the *same* report into its **tada report** (`jobs/tada/xs2rust-endor-stage4-lockdown-harden.md`), which carries every material item from the dead-lettered message: the `harden`/`petrify`/oracle-shim work, the full green-bar evidence (30-program result-gated corpus, 3 unit tests, `cargo test --workspace` green, Miri-clean, built-ins/Object no-regression), the accuracy-over-parity metering note, and all four scope folds (`lockdown()`, `mutabilities`, exotic-receiver, accessor-freeze). It even annotates the fold explicitly: *"the supervisor inbox … was gone — my completion report was dead-lettered … (intent preserved)."* The supervisor's own spec instructs it to **read all five children's tada reports** at acceptance, so it is guaranteed to see this.

4. **Why re-sending or pre-seeding would be wrong.** `inbox-send port-xs-to-rust-memory-safe-engine-s10` now would just re-dead-letter (no inbox dir → another deadmail job → loop). Pre-creating an inbox for a not-yet-claimed doer is unsupported (the inbox is created fresh at claim). And **creating the follow-up children** for the folded scopes is the supervisor's own ownership at stage-4 acceptance (its spec: audit every fold, post findings/fixer jobs, then decompose stage 5) — doing it here would usurp its decomposition and risk double-dispatch.

I drained my inbox (empty), synced the journal, and verified the above. The faithful resolution of this dead-mail is a no-op on state: the reply is already durably delivered to where the supervisor reads it.

---

**Completion report**

- **Job:** Pick up the intent of a dead-lettered message addressed to `port-xs-to-rust-memory-safe-engine-s10`.
- **Finding:** The message was the `xs2rust-endor-stage4-lockdown-harden` child's completion report (a *reply* to its still-parked Fable supervisor `s10`). It dead-lettered only because `s10` is parked (blocked on the `xs2rust-endor-build-stage4b` orchestration) and has no live inbox.
- **Intent preserved — no action needed:** The identical report, including all four scope folds (`lockdown()`, `mutabilities`, exotic-receiver, accessor-freeze) and evidence, is already in `jobs/tada/xs2rust-endor-stage4-lockdown-harden.md`, which `s10`'s spec has it read at stage-4 acceptance. The tada report even explains the dead-letter, so the supervisor won't be confused by an empty inbox.
- **What I did NOT do (deliberately):** did not re-send to `s10` (would re-dead-letter into an infinite loop); did not pre-seed an inbox (unsupported; claim creates it fresh); did not create follow-up children for the folded scopes (that decomposition is the supervisor's own ownership at acceptance — acting would usurp/double-dispatch it).
- **Changes:** none to the garden repo or journal (verification-only; the dead-mail's intent was already durably delivered by a redundant channel).
- **Follow-ups (owned elsewhere, not by me):** when the `stage4b` orchestration reaches `tada`, `s10` is promoted and will, per its spec, audit these four folds and decide whether they become a `stage4c` child (or fold into stage-5 planning). That is the supervisor's job, already correctly queued.
