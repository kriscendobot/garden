Maintainer notified, inbox clean. Job complete.

## Completion report — `finbot-progress-20260713-202002`

**Assessed state.** No open PRs on `kriscendobot/finbot` (direct-push convention, `main` at `554d2fd`); every `finbot-*` job in `tada/`, no in-flight or competing work. The last four cycles built the per-instrument GARCH regime machinery and then closed the first decision loop (regime tightens the auditor tail floor). Every recent report handed off the same next lever: **feed regime persistence into the forecaster horizon**. I took it.

**Advanced this cycle** (direct-push `554d2fd..7a57fc7`): **a persistent vol regime now stretches the forecaster's projection horizon** — the paired companion to the regime-tail-floor, applying the same measure-then-decide loop to projection *depth* instead of the audit gate.
- **`packages/pipeline/forecaster.js`** — new `regimeHorizon()`: `horizon = min(cap, round(baseHorizon·(1 + regimeHorizonStretch·stress)))`, where `stress` ramps the **worst asset's** GARCH persistence from `regimePersistenceLo` (0.70) to `regimePersistenceHi` (0.98), capped at `regimeHorizonCap` (60 ticks). Rationale: a persistent regime holds elevated conditional variance for many ticks, so a shock this cycle compounds — a horizon that truncates mid-shock understates the drawdown-and-recovery the auditor's `pathStats` read. `regimeHorizonStretch` defaults to **0** → base horizon, no `horizonRegime`, byte-identical artifact + hash.
- **Reuse win:** extracted the worst-asset scan + persistence→stress ramp (previously inline in the auditor) into shared `worstAssetPersistence()` / `persistenceStress()` helpers now imported by both levers, so the horizon and the tail floor key off the **same** worst instrument the **same** way by construction, not coincidence.
- **`ooda-cycle.js`** defaults the stretch to 0.5 only when `forecaster.adaptiveVol` is set (mirroring the `regimeTailBump` default); **`bin/finbot-ooda`** exposes `--regime-horizon-stretch=F`.

**Verification — GREEN.** Full workspace suite **528 pass / 0 fail** (+8 in a new `regime-horizon.test.js`: the two shared helpers, the `regimeHorizon` unit incl. cap-bounded and too-small-to-round cases, a `project()` integration on the DIP window showing horizon 10→15 with a genuinely different terminal distribution and byte-identical-when-off, and the ooda-cycle default-on/off). Safety gate `finbot-ooda --seed=7` across every mode (plain, `--adaptive-vol[=mle]`, `--multi`, `--fit-window=16`, `--regime-horizon-stretch=0.8`): **WALLET TOUCHED: false** throughout. Design note appended to `designs/ensemble-forecasting.md`. `package-lock.json` left untracked (the repo does not track it).

**Live note:** the GBM demo world fits worst-asset persistence ~0.68 (just shy of the 0.70 engage threshold), so the stretch is inert on the live demo cycle today and engages once a genuinely clustered regime is observed — same inert-until-clustered pattern the tail floor has.

**Next unblocked step (handed off):** feed persistence into the analyzer's **risk appetite / position sizing** directly — the last regime lever not yet wired (gate ✓, horizon ✓, sizing ⟵ next).

**Needs a maintainer decision (standing, unchanged):** live execution remains the only *blocked* axis — cap-attenuation Phase 2 + a first paper-wallet/test-net run behind an explicit `live_authorized: true`. Everything else is unblocked and moving. Maintainer inbox notified; my inbox drained clean; garden worktree carries no changes (all work landed in the project fork).
