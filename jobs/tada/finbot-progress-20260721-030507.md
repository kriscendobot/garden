finbot-progress-20260721-030507 — **Cycle Report**

## What advanced this cycle

Implemented the **v1 SES compartment attenuator** for `@finbot/harness`, replacing the throwing stub with a production-ready SES-enforced capability attenuation system.

### Changes
- **`packages/harness/sandbox/permissive.js`** (+134 / -37 lines):
  - Added `import 'ses'` + `@endo/eventual-send/shim.js` + `@endo/patterns`
  - Implemented lockdown guard (idempotent)
  - Defined per-role `CAPABILITY_MAP` (mirrors `designs/cap-attenuation.md`)
  - Implemented `buildRolePolicy()` — maps each role to its SES globals policy (`console`, `fetch`, seeded `rng`, etc.)
  - Implemented `compartmentAttenuator(role, capabilities, ctx)` — returns hardened `{ globals, modules, tools }` with the same shape as the future `@endo/compartment-mapper` interface
  - `permissiveAttenuator` remains v0 default; `compartmentAttenuator` is available for swap-in

### PRs
- **PR #1**: https://github.com/kriscendobot/finbot/pull/1 — all 66 tests passing (0 failures)

## Next unblocked step

Swap `permissiveAttenuator` → `compartmentAttenuator` as the harness default. No maintainer decision needed; can land independently in the next cycle.

## Items needing maintainer decisions

1. **Cross-process transport for `spawnSigningWorker`**: Unix socket vs TCP? Persistent worker vs spawn-fresh? (see `designs/cap-attenuation.md` § Process boundary)
2. **Whether to enable `significanceAlpha` as default** for the live `auto-family` path (would change proposal hashes, needs re-baselined fixtures)
