Done. Completion report follows.

---

**Job `deadmail-20260628T015003Z-602b9f` — complete.**

**Intent recovered:** A dead-lettered message for `issue-kriskowal-garden-9` carried kriskowal's directive on garden issue #9: *"Please also post a job to capture the debug instrumentation improvements to xsnap in forks of the relevant repositories."* The original doer had completed and torn down its inbox, so I picked up the intent.

**What I did:**
1. Read the full issue #9 thread to recover the substance — the xsnap stack-overflow investigation produced a real, valuable artifact (a `fxAbort`/`XS_STACK_OVERFLOW_EXIT` trace-dump patch to `xsnapPlatform.c`) that has *twice* been lost to redeploy wipes. The maintainer wants it made durable.
2. Verified the relevant bot-owned forks exist and located the source: `xsnapPlatform.c` lives at `xsnap/sources/xsnapPlatform.c` in **`kriscendobot/xsnap-pub`** (fork of `agoric-labs/xsnap-pub`, default branch `Agoric`); the JS driver/harness lives in **`kriscendobot/agoric-sdk`** `packages/xsnap`. Both are in-scope bot forks.
3. Posted job **`capture-xsnap-debug-instrumentation-20260628T015232Z`** to the board (`todo/`). It embeds the instrumentation patch verbatim (so it never needs re-derivation from the thread again) and specifies two deliverables: (A) commit the C instrumentation to `kriscendobot/xsnap-pub` + DRAFT PR; (B) capture the build/run methodology + key calibration result (9-frame, 4096/4096-slot wide value-stack exhaustion — not deep recursion) in `kriscendobot/agoric-sdk` + DRAFT PR, with submodule pin where feasible. Hard scope guard baked in: **bot forks only, never upstream `agoric-labs/*` or `Agoric/agoric-sdk`**, PRs stay DRAFT. Copied the ISSUE NOTE block verbatim so the consumer comments back on issue #9.
4. Commented the confirmation (job id + scope + what lands where) on issue #9: https://github.com/kriskowal/garden/issues/9#issuecomment-4824002867 — left the issue open per the never-close rule.

**Changed:** one new board job; one issue comment. Inbox was empty at the checkpoint.

**Follow-ups:**
- The posted job awaits a gardener claim; it will report both bot-fork PR URLs back on issue #9 when done.
- Note: kriskowal's *other* recent comment on the thread ("look at the bundle artifacts… spread operator / apply reflection at dynamically allocated arrays") is a **separate** ask not covered by this job — it should have its own issue-spine job if not already in flight.
