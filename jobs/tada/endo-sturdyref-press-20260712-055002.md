## Completion report — SturdyRef press tick (job endo-sturdyref-press-20260712-055002)

**What I did: unstalled the last red artifact of bar 1.** PR #704 (bridge cut 6, three-party round-trip) had 4 red CI legs and its auto-shepherd had been poisoned (requeue-exhausted, parked in `jobs/plan/` at 04:53Z) — no lane owned it, and no other live sturdyref worker existed. Per the charter's take-the-wheel-when-stalled rule, I fixed it under my own job base in an isolated worktree.

**Diagnosis (real execution).** All 4 legs fail in the cut-5 confinement test "the ocapn capability and netlayer handles never cross a facet boundary": the guest's `@host` lookup resolves to the host's bare mail handle, which has no `identify` method (run 29179035963). I reproduced the identical TypeError locally at **cut 5's own green head** `a67769b07` (twice, including with fresh daemon state) — where CI's log shows the same test passing in 733ms. Conclusion: the guest's host-view facet shape is environment-sensitive, not a cut-6 regression (cut 6's only daemon-source change is the netlayer-arming block, inert when unarmed). The test encoded one shape and flapped.

**Fix pushed** to `build/sturdyref-bridge-6-three-party-roundtrip` (forced update `4e7292c9a` → `0dd7f34ad`): rebased onto cut 5's fixed head (picking up the armed-netlayer `shutdown()` teardown), plus one commit making the assertion shape-agnostic — the guest's probe for `ocapn` must either throw `target has no method "identify"` (bare handle, the stronger posture) or return `undefined` (namespace miss). #704 remains DRAFT.

**Verified locally** (node 22.23.1, at `0dd7f34ad`): facet-boundary test ✔ 245ms; three-party round-trip 4/4; daemon ocapn tests 10/10 (incl. armed cross-peer enliven); full `@endo/ocapn` lockdown suite 547/547; all `*SturdyRef*` endo tests 5/5. **CI on the new head: not yet verified** — matrix takes ~20 minutes; the next hourly tick checks it.

**Confinement property preserved: no-identification, strengthened.** The test now accepts the stronger posture (no `identify` reachable at all) while still requiring that a confined guest cannot recover the ocapn identity through its host-view; no location or correlation surface was added, and all prior confinement tests re-ran green.

**Follow-ups** (recorded in journal entry `entries/2026/07/12/060916Z-progress-gardener-7dbe3b.md`): (1) next tick verifies #704's matrix — if green, bar 1 is test-green end to end; (2) the poisoned `pr704-shepherd` job parked in `jobs/plan/` is then moot — liaison should drop, not promote it; (3) the #695 go/no-go on agent-surface cuts A–F (bar 2) is still unread — nudge after ~21:00Z; (4) the CI-vs-local flip in guest `@host` facet shape at the same commit deserves a small designer probe eventually, non-blocking.
