Conclusion is clear. Both `@earendil-works/pi-ai` and `@earendil-works/pi-agent-core` publish latest `0.80.9`, with no version at or above `0.81.0` under any dist-tag. The repo pins `^0.80.3` (semver range `>=0.80.3 <0.81.0`) uniformly across all four consumers (agent-tools, agentry, genie, lal), so the latest releases fall **within** the pinned range — the only releases newer than the pinned floor (0.80.5–0.80.9) are within-range patch bumps.

This is a no-op week. No job posted, no changes committed.

---

**Pi-release watch — 2026-07-16**

- **Pinned in fork (`endojs/endo-but-for-bots` @ `origin/llm`, read-only):** `@earendil-works/pi-agent-core` and `@earendil-works/pi-ai` both `^0.80.3` (range `>=0.80.3 <0.81.0`), consistent across `packages/{agent-tools,agentry,genie,lal}/package.json`.
- **Upstream latest (npm):** both packages `0.80.9` (`latest` dist-tag); no published version `>= 0.81.0` on any tag (other tag `legacy-node20` = `0.74.2`, older).
- **Comparison:** latest `0.80.9` is **within** the pinned `^0.80.3` range. Releases newer than the pinned floor are all within-range patches (0.80.5–0.80.9); no new minor/major that would escape the caret and warrant a migration.
- **Action:** none. No new migration-relevant `@earendil-works/pi-*` releases since pinned `^0.80.3` (latest 0.80.9, in range).

Follow-up: next dispatch should watch for a `0.81.x` (or higher) release, which would fall outside `^0.80.3` and trigger a `propose-pi-bump-<version>` job.
