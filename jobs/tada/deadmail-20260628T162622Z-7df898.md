Completion report:

**Job: deadmail-20260628T162622Z-7df898 — dead-lettered maintainer message to `ingest-ocap-library-sections`**

The intended recipient (`ingest-ocap-library-sections`) had already completed and been torn down. The maintainer's message asked it to verify that the erights.org-mirror work is already handled and to thank them. I picked up that intent and did the verification myself (gardener-93, endolinbot).

**What I verified — the maintainer was correct on all three points:**
1. **The mirror note lives in the always-read doc.** `journal/library/conventions.md` § PDF acquisition guidance (lines ~314–325) records that `erights.org`/`caplet.com` refuse the bot sandbox and the canonical substitute is the GitHub Pages mirror `https://erights.github.io/erights-org-website/<path>`, with the Internet Archive `id_` fallback for PDFs/talk files the mirror lacks. This is exactly the "encode the directive in the role's always-read procedure doc" fix from prior feedback.
2. **The tool enforces it.** `scripts/jobs/fetch-source.sh` rewrites erights.org/caplet.com URLs to that mirror automatically (confirmed at lines 13/35/126), so the convention isn't just prose.
3. **The ingestion continued.** `scholar-ingest-erights-3` completed (tada, gardener-69) and `scholar-ingest-ocap-kernel-comment-fragments` is claimed/in-flight (gardener-56). The library carries the `erights--` elang/elib sources and ocap concept files (object-capability, object-sameness, passable-equality, etc.).

**What changed:** No code or library changes were needed — the work was genuinely already done. I sent the maintainer a verification-and-thanks reply via `message-user.sh` (delivered to the maintainer inbox).

**Follow-ups:** None. Closing as already-handled.
