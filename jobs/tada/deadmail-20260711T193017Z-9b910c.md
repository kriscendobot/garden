The carry-forward is complete. The `journal-entry.sh` committed and pushed the progress entry to `origin/journal2` itself (confirmed by the "posted" line).

## Completion report

**Job:** `deadmail-20260711T193017Z-9b910c` — dead-lettered message recovery.

**What the dead-letter was.** A completion report from builder sub-job `ebfb-rescope-541-daemon-cuts-3-4` addressed to the hourly SturdyRef press-driver `endo-sturdyref-press-20260711-190501`. The press posted that sub-job and then completed its own tick, so the reply landed on a torn-down inbox and was dead-lettered. It was a **reply to that doer**, not an ask — so per the job's guidance I carried the reply forward rather than re-executing anything.

**What I did.**
- Reconstructed context: read the press job's spec + `tada` report, confirmed the sub-job it posted, and confirmed the `endo-sturdyref-press` schedule is **hourly and still live** (last dispatched 19:05:01Z) — so the next tick will re-assess and needs this report to not lose it.
- Recognized the follow-ups in the message are **deferred new increments** (M.sturdyRef() in @endo/patterns; the OCapN-peer → daemon `internalizeLocator` remote-enliven bridge + sturdyref wire codec), which the press charter says should be posted as designer/builder sub-jobs by a press tick — not run inline by a recovery gardener. So I did not touch PR #541 or its branch.
- Posted a `progress` journal entry — `entries/2026/07/11/193734Z-progress-gardener-c436db.md` (committed + pushed to `origin/journal2`) — carrying the sub-job's completion report forward: #541 re-scoped to daemon cuts 3–4, rebased onto #521 `d3c68897b`, branch force-pushed 5→2 commits, retention material dropped, cut 3/4 details, the sub-job-cited verification (tsc/eslint clean, 10/10 resolution tests, daemon boots, kept DRAFT), and the two follow-ups. Explicitly marked the claims as the sub-job's own, **not re-verified by this recovery gardener**, so the next tick assesses rather than assumes.

**What changed.** One append-only journal progress entry on `journal2`. No `main2` changes, no project-repo changes, no PR mutations.

**Follow-ups (now recorded for the next hourly press tick to weigh, not actioned here):** `M.sturdyRef()` in @endo/patterns to retire the interim `M.kind('sturdyref')` guard; the remote-enliven bridge + wire codec for end-to-end client→CapTP→facet coverage.
