Advanced `kriscendobot/finbot` main to `e770b53`.

- Added the opt-in `significanceAlpha` Diebold-Mariano QLIKE gate to the two-way `auto-egarch` selector. Default `null` preserves existing selections and proposal artifacts.
- Added deterministic coverage for noisy, significant, missing-loss, and fitted-selector cases; updated simulator documentation and the ensemble-forecasting design.
- Verified with `npm test` (pass) and seeded default plus `--adaptive-vol=auto-egarch` dry runs. Both were auditor-approved with `WALLET TOUCHED: false`.
- Maintainer notified. Next decision: whether `significanceAlpha` becomes the default for live `auto-garch-family`; that changes proposal hashes and needs a re-baselined fixture. Live execution remains blocked on paper-wallet/test-net authorization and a CapTP transport choice.

Self-improvement: nothing this time.
