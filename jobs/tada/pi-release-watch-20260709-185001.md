The proposal is posted and has **already been claimed** by a peer gardener (`endolin-garden2-5bcdff64/gardener-18`) — it's now in `jobs/doin/`, actively being worked. Since it's under another gardener's claim, I won't race-edit the file for the one cosmetic stray character (meaning is unambiguous: "Open/update the PR").

The watch engagement is complete.

## Report

**Weekly Pi-release watch — 2026-07-09**

**Pinned (endojs/endo-but-for-bots @ `origin/llm`, read-only):** `@earendil-works/pi-agent-core` and `@earendil-works/pi-ai` both pinned `^0.79.0` (yarn.lock resolves 0.79.9) across packages agent-tools, agentry, genie, lal. (No pi dependency in a `fae` package; the four above are the only consumers.)

**Upstream latest (npm + earendil-works/pi GitHub releases, read-only):** both at **0.80.3** (published 2026-06-30). The 0.80.x line is **outside** the pinned `^0.79.0` range.

**Judgment — migration-relevant, YES:** the 0.80.0 → 0.80.3 range carries breaking API changes our harnesses depend on:
- **v0.80.0:** pi-ai's global API (`getModel`/`getProviders`/`registerApiProvider`/`stream`/… ) moved off the `@earendil-works/pi-ai` root to `/compat`. The root→compat loader alias only covers pi *extensions*; our packages import pi-ai directly, so they don't get the alias and will break. Also removed the `/base` selective-provider entrypoints.
- **v0.80.2:** renamed agent-core `ExecutionEnvExecOptions` → `ShellExecOptions`; `ApiKeyCredential` discriminator `type:"api-key"` → `type:"api_key"`.
- Confirmed real consumer import sites of the moved runtime values (`getModel`, `getProviders`, `registerBuiltInApiProviders`, `registerFauxProvider`, `createAssistantMessageEventStream`) in agentry/genie/lal.

**Note on dependabot PR #607:** it mechanically bumps the four ranges `^0.79.0`→`^0.80.3` + lockfile, bundled with 21 other updates, but does **no** code migration — so it will fail/break without the code work.

**Action taken:** posted job **`propose-pi-bump-0.80.3`** (`scripts/jobs/post-job.sh`) proposing the migration — deterministic basename keyed to the target version so a later week won't duplicate it. The body scopes the exact import repoints (→ `/compat` or `createModels()` factory API), the type/credential renames, the consumer import sites, and frames #607 as the version-bump half to co-opt rather than re-do. The job was immediately claimed by a peer gardener and is now in `jobs/doin/`.

**Follow-ups:** none from the watch itself (read-only; experiment/PR happen on a bot fork under the claimed proposal job). Next weekly dispatch should re-post only for a version newer than 0.80.3.
