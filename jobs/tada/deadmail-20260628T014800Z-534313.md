Completed the dead-lettered job (intent of kriskowal's comment on kriskowal/garden#9: "look at the bundle artifacts… for new use of the spread operator or apply reflection at dynamically allocated arrays").

**What I did**
- The `bundle-ymax0.json` artifact is still wiped from the host (re-confirmed; no agoric-sdk checkout present), so I ran a read-only source audit of the bundled contract package at the investigated SHA `kriscendobot/agoric-sdk@9d518832d4` (`packages/portfolio-contract`) via the GitHub API — no agoric-sdk push/PR/merge.
- Enumerated every spread- and apply-over-array site, split by execution timing against the instrumented trace (the 2,588-slot frame aborts inside a SES module-eval job, so only module-eval-time sites can be it).
- Sized every dynamically-allocated array that gets spread at module-eval.
- Posted findings to the issue thread: https://github.com/kriskowal/garden/issues/9#issuecomment-4824025534 (did not close — submitter does that).

**Finding**
- The maintainer's instinct on code class is right: the module-eval spreads are all `M.or(...keys/values(X))` and several are **new** (the whole `resolver/` subsystem; `PoolPlaces`/`AxelarChain`, which Auto-Features #12761 grows). All apply-reflection sites (`move.apply`, `CCTP.apply`, …) are **flow-time, not module-eval**, and pass only a small `...optsArgs` spread — no `Reflect.apply` anywhere.
- But sizing refutes the hypothesis as the cause: `InstrumentId`/`PoolPlaces` ≈ 38 entries, `AxelarChain`/`YieldProtocol`/`TxType`/`TxStatus` single-digit-to-small. Every such spread pushes **dozens** of operand slots — ~100× short of 2,588. ABI/interface tables are all 25–238 lines; no thousands-element literal in-package.
- Conclusion: spread/apply over dynamic arrays **exists and is recent but does not account for the wide frame**. The 2,588-slot single activation is more consistent with one large literal/expression (the aggregate proposal-shape/type-guard tree) or a wide construct in a bundled dependency's module body — which the minified `:21` can't disambiguate.
- Recommended next step (export-free): sourcemapped re-bundle to map the `:21` frame, plus instrument spread arity at import to settle it directly.

**Follow-up (not part of this job)**
- kriskowal left a **second** comment on #9 — "post a job to capture the debug instrumentation improvements to xsnap in forks of the relevant repositories." That is a distinct item that should arrive as its own issue-spine job; it was outside this job's intent (the spread/apply comment), so I did not action it here. Flagging it so the liaison/maintainer can confirm it's tracked.
