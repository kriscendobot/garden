Inbox empty. Work complete.

---

## Completion report

**Job:** dead-lettered message for `issue-kriskowal-garden-9` — maintainer (kriskowal) asked on kriskowal/garden issue #9 to "post a job to make sure that `@endo/hex` does not suffer the same defect or patch accordingly."

**The defect guarded against:** a metered XS stack overflow from building a hex-codec table with `.flatMap` over a large spread array — the `@agoric/internal/src/hex.js` `decodings` builder (the `flatMap`→loop fix established earlier in the thread). The trigger is the `flatMap`/large-array-spread construction, not validation strictness.

**What I did:** Verified `@endo/hex` against fresh bot-fork source (`endojs/endo-but-for-bots`, sparse-checkout of `packages/hex`). Read every runtime file (`index.js`, `decode.js`, `encode.js`, `src/decode.js`, `src/encode.js`) and ran a pattern scan for `flatMap` / large-array spread / `fromCharCode` / `push.apply` / `concat(...)` across runtime code (excluding tests/benches).

**Finding — `@endo/hex` is immune by construction; no patch needed:**
- It deliberately uses **no decode lookup table** (its own comment: "computes nibble values directly from character codes rather than indexing a lookup table"), so there is no `decodings`-style array to build via `flatMap`/spread.
- Both codecs are **bounded `for` loops** over pre-sized arrays (O(1) stack depth in input size); no recursion.
- **Zero hits** for any offending pattern in runtime code. The only `apply` is `Reflect.apply(intrinsic, recv, [oneString])` — fixed arity, not an argument spread.
- On XS it dispatches to the native `Uint8Array.fromHex`/`toHex` intrinsics anyway.

**What changed:** Nothing in code — verification confirmed no patch is warranted. Posted the result + reproducible methodology as a comment on issue #9 (https://github.com/kriskowal/garden/issues/9#issuecomment-4825052906); left the issue open for the submitter to close.

**Follow-up (not mine to resolve here):** The maintainer's adjacent comment notes that bot-fork PR #7's validation-consistency patch addresses a *different* issue than the `flatMap`-under-XS defect they flagged — re-aiming that agoric-sdk hex patch at the `flatMap`→loop fix is the live thread on `issue-kriskowal-garden-9`'s other work, separate from this `@endo/hex` check.
