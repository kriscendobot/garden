Cycle complete. Everything landed and verified.

## Completion report

**Cycle:** finbot-progress-20260719-115001 (builder). One increment, landed and green.

**Assessed state.** No open PRs on `kriscendobot/finbot` (work lands directly on `main`, was at `f804aa3`); all prior `finbot-*` jobs are in `tada/`, none in flight — nothing to duplicate. The visual-projection, path-stats, and bootstrap axes of the ensemble-forecasting design already exist. The last cycle's handoff named the deepest unblocked step: "add a pre-specified QLIKE improvement margin," and I took it.

**Advanced** — `kriscendobot/finbot` main `f804aa3 → 53cfcfb`:
- **`packages/simulator/garch.js`** — a **parsimony margin** on the `AutoGarchFamilySurface` chooser. It was selecting an asymmetric branch (GJR/EGARCH) on *any* held-out QLIKE edge over symmetric GARCH. I measured the flaw empirically: on a symmetric GARCH DGP the spurious asymmetric "improvement" runs ~0.01 nats and flips the selection, while a genuine leverage signal clears ~0.07. Now the best asymmetric candidate must beat the GARCH baseline by `selectionMargin` (default `0.02`, overridable) before its extra parameter is accepted; the two equally-complex asymmetric branches still race on raw QLIKE between themselves. A best-but-within-margin outcome retains GARCH and records `selection: 'oos-qlike-within-margin'`, making the near-miss visible in the fit artifact. Threaded through `autoGarchFamilyMleFromPriceHistory` and the `conditionalVolFromPriceHistory` auto-garch-family path.
- **Tests** — 2 new `garch.test.js` cases (margin gates a noise-level edge, a real leverage signal clears it, `selectionMargin=0` restores raw argmin, wide margin refuses all, negative throws; plus a real symmetric-DGP case where the margin correctly retains GARCH). Updated the pipeline `forecaster-adaptive-vol` test to reflect the honest new reason.
- **README** — documented the margin and the threshold rationale.

**Verified.** Full workspace suite **591 pass / 0 fail** (+6 vs. 585; fresh worktree needed the known one-time `npm install` relink — pre-existing gap, `package-lock.json` deliberately not committed). Seeded dry-run OODA on `--adaptive-vol=auto-family` and on the default path both **auditor-APPROVED, WALLET TOUCHED: false**. Tree left green.

**Next unblocked step (handed off):** apply the same parsimony margin to the two-way `AutoEgarchSurface` (still raw argmin, identical over-selection flaw); then a Diebold-Mariano forecast-comparison test for whether the QLIKE gap is statistically significant, not just above a fixed nat threshold.

**Maintainer decision pending (unchanged):** live execution stays blocked on explicit paper-wallet/test-net authorization and a chosen CapTP transport. Reported to the maintainer inbox.
