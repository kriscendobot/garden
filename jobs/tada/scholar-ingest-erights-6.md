# scholar-ingest-erights-6 — completion report

Ingested the Guarding child-chapter cluster plus the `e-guards` concept.

- Source: `elang/guarding/async.html` → `erights--elang-guarding-async` (1 section,
  reference-state guards `:near`/`:pbc`/`:vow`/`:rcvr`/`:any` + the
  `near <= vow <= rcvr` static-checking lint sketch). SHA-256 `3ab057a0dfc2…`.
- Concept: `concepts/e-guards.md` (current) — E coerce-or-reject guards / Soft Type
  Checking, ancestor of `@endo/patterns` guards + `M.interface`.
- Dead link confirmed: `elang/guarding/style.html` ("Guard Expression Style") was
  never written (404 on mirror + Internet Archive); not re-queued; recorded in the
  guarding hub source note.
- Indexes: topic rows on e-language / eventual-send / pass-style; sources/README,
  concepts/README, keywords.md (9 lines); refreshed guarding hub note.
- Integrity gate passed on both touched clusters; sections index regenerated (also
  resolved the carried-forward erights--elang-same-ref block gap).
- Follow-on `scholar-ingest-erights-7` posted (grammar + elib-concurrency + ode
  chapters; carries reachability caution + the ~20-dangling-nav-link cleanup flag).

Result entry: entries/2026/06/28/165807Z-result-gardener-ebec7a.md.
