## Press report — npm-via-CAS registry proxy (endojs/endo-but-for-bots, base `llm`)

**Assessment.** Phases 1 (SQLite `RegistryTable`) and 3 (MVS semver) remain solid at `llm` HEAD. The next unblocked increment was Phase 2: PR **#276** (npm-registry HTTP fetch layer) had sat CONFLICTING and untouched since May 17, 636 commits behind `llm`, with a 16-seat panel review leaving 8 unaddressed should-fix items. No live peer held the branch. The shared registry-capability edge (#403 MERGEABLE, #671 active yesterday under the byte-array arc, #563 draft/conflicting) was left alone per the no-duplication instruction.

**What I did.**
- **Rebased #276 onto `llm` @ 25978ee499.** Conflicts were confined to `rust/endo/Cargo.toml` (additive dep merge) and `Cargo.lock` (regenerated minimally — only the new deps `tar`, `ureq`, `filetime`, `webpki-roots`, `xattr` enter the lock). PR went from CONFLICTING to **MERGEABLE**.
- **Addressed all 8 panel should-fix items** in a new commit `a48979f39b`: tarballs containing links/devices/FIFOs now fail with a new `FetchError::UnsupportedEntry` instead of silent truncation; paths with `..`/absolute components are rejected before becoming tree-manifest keys; the dead `Dist::shasum` field is removed; and the four implicit contracts the panel wanted named (standard-base64 SRI, single-`ureq::Agent` reuse, registry-table trust boundary, write-once metadata cache) are now doc'd at their enforcement sites. Two new regression tests cover the symlink and traversal rejection paths.
- **Verified with real execution.** `cargo test -p endo --lib` on the rebased branch: fetch 24/24, registry 8/8, semver 14/14, cas 15/15. Live-network evidence: `ENDOR_REGISTRY_LIVE_TEST=1 cargo test … live_registry` passed in 0.23s — a real HTTPS fetch of `is-odd@3.0.1` from registry.npmjs.org, SHA-512-verified, extracted into a fresh CAS, `package.json` read back from the CAS tree.
- Force-pushed with lease to `feat/endor-npm-registry-fetch` and posted the full summary + evidence as a PR comment (endojs/endo-but-for-bots#276, comment 4996690322).

**Follow-ups surfaced (not pressed this tick).**
1. **Base build gap:** at `llm` HEAD, `rust/endo` cannot compile from a fresh checkout — `xsnap/src/lib.rs` `include_str!`s three gitignored generated bundles, but generators for `ses_boot.js`/`worker_bootstrap.js` exist only on the unmerged slot-machine branch (45bb0e96a9), which also carries the missing `rust-endor.yml` CI workflow. This blocked the panel judge before me and blocks any full `cargo test` (I stubbed the bundles; the verified suites never boot XS). Worth a designer/shepherd job, converging with the panel's "no rust CI" observation.
2. **Phase 4 (compartment-mapper integration)** is the next phase once #276 lands; Phase 5 (offline/`.npmrc`) after that. #276 is now review-ready rather than rotting.
