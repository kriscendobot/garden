## Completion report — `ocapn-noise-press-20260718-002002`

**Assessment.** M1–M5 all remain proven. This dispatch had one inherited pending item — the 18:20Z dispatch died of a wall-clock overrun after rebasing the stack over `llm`'s daemon→manager rename, leaving "CI pending; next dispatch confirms" — plus one fresh regression discovered during the steady-state probe.

**CI confirmed green on the whole restacked stack.** #340 `ceba59e6fe` 25/25, #684 `eecc1246e7` 23/23, #688 `7c9a0e212f` 23/23, #693 `97b8499e95` 23/23; #683 `64b6dac549` also MERGEABLE/CLEAN 24/24. #684's lone initial failure was the known `@endo/chat` token-autocomplete macOS flake (a UI test outside the stack's diff; identical code passed on #688/#693) — classified from the job log, rerun via `gh run rerun --failed`, now green.

**Regression found and fixed: minion.town's OCapN endpoints had fallen behind the OAuth gate.** `kriscendobot/minion.town` gained continuous deployment on 2026-07-17 (first run 17:21Z) that renders `deploy/aws/caddy/` onto the box wholesale — clobbering the hand-installed ungated `/ocapn` and `/ocapn-daemon` routes, so both endpoints (and WebSocket upgrades) 302'd into `/oauth2/sign_in`, unreachable for Noise dialers. SSM inspection confirmed both listeners survived (container `endo-pet-daemon` on :8931, `endo-ocapn-daemon.service` on :8930); only routing was lost. I landed both routes durably in the repo Caddyfile (kriscendobot/minion.town#9, merged — closing the follow-up the original deploy job had left open), the CD run validate-gated the reload, both endpoints answer 426 again, and I re-proved a **real cross-host Noise IK capability round-trip** through the restored route (`RESULT {"ok":true, nodeId 810b996c…}` from the garden container at the rebased #693 head — which incidentally shows the restacked client interops with the 07-11 toy server, no locator-format break on this path). Evidence comment: endojs/endo-but-for-bots#693 issuecomment-5008903173.

**Recorded.** Maintainer notified via the bus (msg `20260718T003113Z-f2c074`) including the standing hazard: box-local state overlapping CD's surface (app/oauth2-proxy/caddy/www) now dies on any push to main unless landed in the repo. Memory `ocapn-over-noise-goal` updated. No garden-repo (main2) changes were needed.

**Follow-ups for the next dispatch:** (1) cross-host TCP+CBOR still blocked on the maintainer's non-443-port answer (msg `20260717T001224Z-3483f4`, still unread — do not re-send); (2) mutual bidirectional pairing blocked on a public address for the garden container; (3) `ocapn-noise-session-reconnect` design still Proposed/unimplemented; (4) `daemon-agent-network-identity` closure; (5) consider a clearer mixed-version locator error.
