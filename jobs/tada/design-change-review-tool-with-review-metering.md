Added and pushed `designs/change-review-tool-with-review-metering.md` (781078592d).

It specifies a minion.town GitHub-linked review app, conservative active-time metering, target-bar isolation (`llm` vs `master`), privacy limits, append-only review records, and reputation-reducer integration.

Validated Mermaid parsing, style checks, and `git diff --check`.

Follow-ups: maintainer decisions on target-policy ownership, export authorization, disputed intervals, and the idle threshold.

Self-improvement: nothing this time.
