Confirmed. The two remaining folds are durably recorded in **three** independent places at the class-tail commit `1cb4529c1`. No intent is lost. Here is my assessment and completion.

## What this job was

The dead-lettered message was a **child completion report** from `xs2rust-endor-stage5-fix-class-tail` addressed to the Fable supervisor `port-xs-to-rust-memory-safe-engine-s13`. That supervisor is **not gone** — it is parked in `journal/jobs/plan/port-xs-to-rust-memory-safe-engine-s13.md`, `gate: blocked`, `blocked_on: xs2rust-endor-build-stage5-fix` (the serial 5-child stage-5 fix orchestration, currently mid-run: cesu8✓ → rejects✓ → class-tail✓ → **modules[running]** → fix-verify[plan]). Its inbox does not exist yet because it isn't alive, so the child's report dead-lettered. The message is a **reply to that doer**, so the correct action is to carry the reply forward — i.e. ensure the supervisor receives it.

## Finding: the intent is already preserved via the designed channel

Delivery to a parked supervisor's inbox is inherently unreliable — **every** stage-5-fix child report dead-letters the same way, not just this one. That is exactly why supervisor s12 wrote s13's spec to read child reports from `journal/jobs/tada/xs2rust-endor-stage5-fix-*.md` (its FIRST instruction), not from the inbox. That channel is intact:

- **`jobs/tada/xs2rust-endor-stage5-fix-class-tail.md`** — the complete completion report, including the route-2 decision and both remaining folds. (The child itself noted the report was dead-lettered and "will be promoted (intent preserved).")
- **`rust/engine/README.md` "Still folded" list** at `1cb4529c1` (the tree s13 reviews at acceptance) — explicitly carries both folds: a private-member **read** in a body (`#x in o`, `this.#x`, `this.#m()` — GET/SET/HAS_PRIVATE) and a static block with local lexical declarations (needs the field function's own frame count).
- **Commit message of `1cb4529c1`** (pushed to `origin/xs2rust-endor`, verified present in the bare clone) — same "Still folded" note.

## What I did / did not do

- **Did:** diagnosed the dead-letter, confirmed s13 is parked (not completed), and verified — thoroughly, in three independent places — that the child's report and its two flagged follow-up folds are durably captured on the exact channel s13's protocol consumes. Nothing is on the floor.
- **Did not re-send** to the parked s13 inbox: it would re-dead-letter into a loop.
- **Did not edit s13's parked spec:** the tada-record channel it's designed to read already carries everything; a class-tail-only annotation would be both redundant and inconsistent (all four sibling reports dead-letter identically).
- **Did not build the two folds myself:** deciding and dispatching stage-5 follow-up fixers is the Fable supervisor's exclusive authority (opus children, one-invocation sizing, gated by its whole-stage-5 acceptance review, which judges which folds are acceptable). They are self-contained follow-ups already on the supervisor's ledger; doing them here would overstep and risk racing the acceptance decision.

## Follow-ups (owned by s13, not this job)

When `xs2rust-endor-build-stage5-fix` reaches terminal state, s13 promotes and runs its whole-stage-5 acceptance review. At that point the two class-tail folds — private-member reads and static-block lexicals — are visible to it in the tada record and the README, to schedule as fixer children or judge acceptable. No action needed from me.

No garden files changed; worktree is clean, nothing to commit.
