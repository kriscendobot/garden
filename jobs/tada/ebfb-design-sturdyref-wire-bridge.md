Completion report:

**Job `ebfb-design-sturdyref-wire-bridge` — done.** DRAFT PR **endojs/endo-but-for-bots#697** (`design/sturdy-refs-cross-peer-bridge` @ `5aee6e0b4`, base `llm`, one file: `designs/sturdy-refs-cross-peer-bridge.md`, 496 lines).

**What I did.** Verified the live state first: #521 (`d3c68897b9`), #541 (`fab626e84a`), #539 (`22923949b2`), #695 (`619493db4d`) all OPEN/DRAFT at the job's stated SHAs; read the enlivenment design, the daemon locator reference, the #521 codec/tracker code, #541's `sturdyref-resolution.js`, the daemon's peer/gateway machinery, goblin-chat's OCapN wiring, and the journal library's sturdyref and three-party-handoff concepts. None of the four in-flight branches was touched.

**What the design settles.** (1) Wire codec both directions: what #521 already builds versus needs-promotion versus missing — including two drift/gap findings from code (the read path's fatal-ASCII decode breaks non-ASCII swiss-nums; the `ocapn://` URI parse *and* format exist only in goblin-chat), plus the whole daemon mint/export story: a persistent `sturdyref-store` swiss-num table behind `locator.get`/bootstrap `fetch`, fresh 256-bit mints, hash-keyed grant handles, revocation by forgetting, and an explicit rejection of mint-on-serialize. (2) Foreign-locator internalization: a closely-held `ocapn` singleton, `ocapn-peer`/`ocapn-sturdyref` formulas mirroring `formulatePeer`/`makePeer`, a `known-sturdyrefs-store` dedup index, what the formulaIdentifier denotes, and enliven-per-use lifetime semantics with session-loss teardown. (3) Three-party handoff: the sturdyref pass *is* the handoff (B dials C directly); explicit contrast/composition table against `desc:handoff-give`, including the grant tracker's `handoff -> sturdy-ref` upgrade; the daemon specified as each of A, B, C. (4) A six-cut table, each cut with a confinement test, and the required two-daemon mint→pass→enliven round-trip in cut 6.

**Confinement property per artifact** (stated in the design's per-artifact table): Syrup wire form — opaque-and-unforgeable, secret off-band in the session manager, tier gate at the facet seam; `ocapn://` URI — location+secret confined to closely-held host-tier emission, never in logs or errors; `sturdyref-store` — unforgeable random mints, daemon-private, revocation-by-forgetting; `ocapn` capability — no-location, sole holder of dial authority, never crosses the worker boundary; `ocapn-sturdyref` formula + dedup index — no-identification toward guests (random location-free ids, daemon-private body, guest grants ride #695's fresh unlinkable tokens), with the value-convergence caveat stated openly rather than hidden.

**Verification evidence.** Both mermaid fences parse (`mermaid.parse` under jsdom: `flowchart-v2` OK, `sequence` OK — one semicolon-in-Note parse failure caught and fixed before commit); em-dash sweep clean; Latin-shorthand sweep clean; typist-code-point sweep clean.

**Follow-ups.** #539's design was deliberately not amended: its one open question (worker-held enlivened-presence lifetime) is narrowed daemon-side but not resolved, per the job's only-if-resolved rule. Builder cuts are not posted (press-driver's job). Two open questions await the maintainer on #697: OCapN identity reuse versus distinct-by-default, and default netlayer arming. One noted drift for the record: #539's "no enlivenment cache" prose versus #521's per-instance memo with rejection eviction; the design adopts the code's behavior as normative.
