Fixed PR #802’s stale zizmor action pins in commit `9a87cf1056` and pushed it to the PR branch. Posted the required PR summary.

CI reached terminal green: 23 checks completed, 0 failures on `9a87cf1056962072ee1b47b20db02ec0354abbbe`.

Local full verification was not runnable because dependency installation could not execute `better-sqlite3` helpers due to permission-denied `prebuild-install`/`node-gyp`; workflow diff checks passed. Removed the isolated project worktree.
