Fixed and pushed:
- `44b64b128`: formatted the two CI-reported daemon files.
- `70c65e2bc`: corrected three stale `actions/checkout` v6 pin comments causing zizmor failures.

Verified Prettier across `.github` and `packages`. CI cannot dispatch because PR #811 is now `CONFLICTING` / `dirty` with an empty check rollup.

Posted follow-up job `endojs-endo-but-for-bots-pr811-weave`.

next: weaver
