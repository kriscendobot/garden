Rebased PR #357 onto current `llm`, regenerated the Markdown format commit, and force-with-lease pushed head `45e38da5d`.

Verified Prettier check and `git diff --check` passed. Full local harness could not execute Yarn script binaries; runtime/type tests were not run. Posted PR summary comment.

Self-improvement: nothing this time.
