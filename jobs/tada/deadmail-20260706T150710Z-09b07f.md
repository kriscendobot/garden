The work is complete. Here is my report.

---

**Job `deadmail-20260706T150710Z-09b07f` — dead-lettered message, intent carried forward.**

**What the message was.** A stage-4 async/await child (`xs2rust-endor-stage4-async-await`, PR #600 `xs2rust-endor`) sent a **scope-fold report** to its supervisor `port-xs-to-rust-memory-safe-engine-s9` via `inbox-send`. It dead-lettered because **s9 is parked** in `jobs/plan/` (`blocked_on: xs2rust-endor-build-stage4`) and therefore has no live inbox. This was a child-reporting-up message, so the correct handling was to **carry the reply forward** to the supervisor, not to undertake the XS→Rust port myself (which the message itself says needs its own fresh full-budget child).

**What I did.**
1. Diagnosed the mechanics: re-sending to s9 would just dead-letter again (parked → no inbox → loop), so that path was closed. s9's own body instructs it to read children's scope-fold reports on wake — but this one was neither in its inbox nor in a `tada/` report (the async-await job is stuck in `jobs/doin/`, never reached `tada/`, and the serial orchestration has already moved on to child 5 `modules`). Left alone, the intent would have been lost — exactly what dead-mail promotion exists to prevent.
2. **Verified the child's durable claims against the actual branch:** keystone commit `49e27a89b` present; handoff commit `86bb59fe6` is the current tip of `xs2rust-endor` ("engine(stage-4 async): async-function surface implementation handoff"); `rust/engine/ASYNC-AWAIT-HANDOFF.md` exists in that tree. All real.
3. **Delivered the scope-fold durably to s9** by appending a clearly-marked `## DEAD-LETTERED SCOPE-FOLD` section (verbatim original message as data + a verified-facts header + the actionable sizing recommendation) to `jobs/plan/port-xs-to-rust-memory-safe-engine-s9.md` — the one channel a parked supervisor is guaranteed to read on wake. Committed and CAS-pushed to `origin/journal2` (`6cc2bf1`), idempotency-guarded by a marker.

**What changed.** One file in `journal2`: `jobs/plan/port-xs-to-rust-memory-safe-engine-s9.md` gained the dead-lettered scope-fold section. No garden code (main2) changes were needed, so my job worktree is untouched. Temp journal clone cleaned up.

**Follow-ups (for supervisor s9 to action on wake — not for me to schedule):**
- Schedule the **async-function surface** (`XS_CODE_ASYNC_FUNCTION`/`AWAIT`, `Promise.prototype.finally`, `all/race/allSettled/any`) as a **distinct fresh full-budget child** starting from `rust/engine/ASYNC-AWAIT-HANDOFF.md` — do not re-queue the exhausted async-await job.
- The async-await job lingering in `jobs/doin/` (with `garden-reaped: 1`) never reported to `tada/`; s9 (or the reaper) may want to reconcile it, since its keystone half is landed and its async half is an honest fold.
