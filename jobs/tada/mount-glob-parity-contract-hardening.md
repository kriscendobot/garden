Done. The commit is on `origin/feat/mount-glob` and the summary comment is posted.

## Completion report

**Job:** Harden the endo-but-for-bots mount-glob shared cross-language parity contract so the #654 Rust/XS runner can't pass while diverging from the Node reference.

**Context resolved first:** #653 (`feat/mount-glob`) is the shared-contract PR; #654 wires a Rust-side runner over its `packages/daemon/test/` JSON artifacts — so the parity direction is settled and the "revise the shared fixture once for both PRs" precondition is met. Work landed on `feat/mount-glob` (#653 head), where the fixture/cases live. Commit `d18ac75fd`, pushed via rebase-CAS to `origin/feat/mount-glob`.

**All 5 required items delivered (additive; mount.js glob behavior unchanged):**
1. **UTF-16 astral sort probe** — added `sort-<U+1F4C4>` (astral) and `sort-<U+E000>` (high-BMP private-use; I switched off U+F900 because it NFC-normalizes to U+8C48, which caused a real byte-mismatch the test caught). `sort-*` is pinned to UTF-16 code-unit order; a natural code-point/UTF-8 `Ord` port yields the reverse and fails. Rippled `*`/`**`/`**/*` root-enumeration expectations regenerated by hand and verified against the real `mount.js`.
2. **Escape-symlink target in the manifest** — new `outsideRoot` section; `_mount-fixture.js` now materializes the above-root `escape-target/secret.txt` from data instead of imperatively, so a manifest-only runner builds a real sibling (no vacuous confinement pass). Refactored into a shared `materializeRecord` helper.
3. **Full deny set as shared artifact** — new `test/mount-glob-contract.json` encodes all 14 `defaultDeniedSegments` + the case-fold rule; every denied name is now physically present with a `[]` case, plus an uppercase `.SSH` case-fold probe. Node runner asserts `defaultDeniedSegments`/`GLOB_MAX_RESULTS` equal the contract (code↔data drift guard). Empty-deny test extended to prove the new names are load-bearing on the filter.
4. **Contract constants as shared data** — `GLOB_MAX_RESULTS`, post-sort truncation, UTF-16 sort, and record-once symlink-cycle semantics in the contract file, bound to exports.
5. **Matcher literal gaps** — cases for `?` literal (not a wildcard), `a**b` embedded-globstar collapse, and `{`/`}`/`,` (no brace expansion), in a new `probes/` subtree.

**Optional Node-side polish:** clarified across `types.d.ts`, `help.md`, `help-text-data.js` that a trailing `**` also matches file descendants; dropped the two redundant mid-walk `names.sort()` calls (final sort governs output). Left the "caller-visible truncation signal" deferred to the streaming variants, as the spec allows.

**Verification (real execution):**
- `yarn ava test/mount-glob.test.js` → **9 tests pass**, including the new contract-drift test and the expanded case table (ran ≥30 cases). First run correctly caught the U+F900 normalization mismatch, which I fixed and re-verified.
- `helpdown.test.js` → 18 pass; `tsc` → exit 0, 0 errors; `eslint` → 0 errors (only pre-existing style warnings); `prettier --check` → clean.
- Confirmed only `mount-glob.test.js` consumes the shared fixture/glob, so no other suite is affected.

**Follow-ups (noted on #653):**
- #654's Rust runner is on a frozen snapshot of #653's old head — it needs a rebase onto `d18ac75fd` and its glob mirror updated to honor the new probes (UTF-16 collation, literal `?`/`{`/`}`, `outsideRoot` materialization, full deny set from the contract). These are exactly the divergences the hardened contract now catches — expected and intended.
- `help-text-data.js` is auto-generated but its committed copy is already stale vs. its generator (400+ lines of pre-existing drift, no enforcing test); I made only the surgical clause edit rather than sweep in that unrelated churn.
