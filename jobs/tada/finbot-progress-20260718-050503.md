Increment landed, tree green, maintainer notified. Cycle complete.

## Completion report

**Cycle: EGARCH MLE — reading the log-variance asymmetry from data. One increment.**

**Assessment.** All prior `finbot-*` jobs are in `tada/`; no finbot work in flight, no open PRs (work lands directly on `main`, was at `dbfa3bd`). The last cycle's hand-off named the deepest unblocked next step: a light variance-targeting **EGARCH MLE** (`egarchMleFromPriceHistory`) reading `(alpha, gamma, beta)` from the data. No competing work to duplicate.

**Advanced.** `kriscendobot/finbot main` `dbfa3bd → 29d4683`:
- **`packages/simulator/egarch.js`** — new `egarchMleFromPriceHistory` + internal `egarchNegLogLik` / `estimateEgarchParams`. Same deterministic nested-grid refinement the symmetric and GJR fitters use (no optimizer, no RNG, byte-identical per input). Variance targeting removes omega from the search — in the log-variance form `omega = ln(s²)·(1−beta)` depends on **beta alone** (not all three, as GJR's does). The one structural difference from GJR: the gamma box **straddles zero** (`[-0.4, 0.4]`), because EGARCH's gamma multiplies the *signed* shock — so the fit recovers the leverage sign (`gamma < 0`) or a reverse-leverage series. Short (`< 12` returns) and degenerate constant-price windows fall back to fixed defaults.
- Routed through `makeVolSurface({ kind: 'egarch', history, estimate: 'mle' })` (mirroring the garch/gjr `estimate: 'mle'` flag) in `world.js`; exported from the simulator index.
- Design field note added to `designs/ensemble-forecasting.md`.

**Verified.** New `packages/simulator/test/egarch-mle.test.js` (9 cases): determinism, variance-targeting preservation, **leverage-sign recovery** (a `gamma=-0.2` DGP recovers clearly negative gamma while symmetric GARCH fits near zero), **reverse-leverage recovery** (a `gamma=+0.2` DGP the fixed path cannot represent), short-window + custom fallbacks, constant-price degeneracy, and factory routing that differs from the fixed fit. Full workspace suite **572 pass / 0 fail** (was 563). `finbot-ooda --seed=7` green, **WALLET TOUCHED: false**, auditor APPROVED. (Fresh worktree needed the known one-time `npm install` to relink the `@finbot/harness` workspace symlink — a pre-existing gap, unrelated to this change; the untracked `package-lock.json` artifact was deliberately not committed.) Tree left green.

**Next unblocked step (handed off):** route EGARCH into the **live regime read / adaptive-vol selector** — extend `conditionalVolFromPriceHistory` / the `auto-gjr-garch` per-asset chooser into a three-way GARCH/GJR/EGARCH (or `auto-egarch`) selector on fitted-gamma evidence, so the forecaster and auditor can key off a log-variance asymmetry. Deferred behind that: implied-vol surfaces, PNG rasterization, far-reference vending.

**Maintainer decision pending (unchanged):** live execution remains blocked on explicit paper-wallet/test-net authorization and a chosen CapTP transport.
