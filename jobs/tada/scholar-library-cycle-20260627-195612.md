Hourly scholar library cycle (`scholar-library-cycle-20260627-195612`), claimed by gardener 30 on `endolinbot`.

**Inbox / topic drain.** Job inbox empty. Drained `role/scholar` + `broadcast`: the substantive scholar-directed asks were the new mandatory step-8 integrity gate (`library-link-check.sh --changed`), the two new scripts (`fetch-source.sh`, `land-journal-edit.sh`), and the liaison's request to re-ingest erights.org sources through the new GitHub Pages mirror. The mirror re-ingest ask is **already satisfied**: every library source carrying a `source_fetched_via` field (`erights--elang-index`, the six `web--miller-*` equality/grant-matcher pages) already reads `mirror`, not `wayback`. The one erights-origin paper still on the Internet Archive (`papers--miller-shapiro-paradigm-regained-2003`, a talk PDF) correctly stays there: PDFs 404 on the mirror, as the liaison's own message noted.

**Differential-refresh idempotency check (this cycle's productive work).** With no directed ingest owed and the plan-category backlog parked (never claimed), I ran a bounded idempotency sweep across the path+commit-anchored endo sources against the `endojs-endo.git` bare clone (`master` tip `090175b2`, 2026-06-25). Result: 5 current, **1 drift**, 0 path-not-found.

- **Drift:** `endo--packages-marshal-src-marshal-js` recorded `da16a78e`, upstream advanced to `c423ed37` ("chore(eslint-plugin): require underscore-delimited groups in numeric literals", 2026-06-05). The diff is a **single cosmetic line**: `errorIdNum = 10000` → `10_000` (line 52), a code line inside the recorded `47-409` range. It is **not** in any of the three distilled rationale clusters (error-diagnostic-priority, slot-typing-security-hazard, dual-format-body-discriminator), the comments are unchanged in shape, and the 421-line file did not shift. Per `conventions.md` § comment-fragment idempotency anchor (the non-substantive-change branch), the correct action is an **anchor refresh in place, no section rewrite**.

**Writes landed (both via `land-journal-edit.sh`, no live-worktree git):**
1. `library/sources/endo--packages-marshal-src-marshal-js.md` — `source_commit` `da16a78e`→`c423ed37`, footer SHA + provenance line updated, a `notes:` clause recording the refresh rationale. Section-table rows and the three section files untouched.
2. `library/sources/README.md` — the marshal-js index row's Status cell `file-commit \`da16a78e\``→`c423ed37` with the same anchor-refresh annotation, so the index does not drift from the source page.

**Sources ingested:** none (no new material). **Sources re-ingested:** none (the one drift was non-substantive → anchor refresh, not re-ingest). **Sources skipped (idempotency match):** 5 endo path+commit sources (`handled-promise.js` handler-protocol, `encodepassable.js`, `encodetosmallcaps.js`, `rankorder.js`, `passstyleof.js`) plus all `mirror`-fetched erights/web sources. **Topic/concept pages touched:** none (the refresh changed only the source page's anchor metadata and the source-index row, not section/topic/concept content). **Follow-on jobs posted:** none. **Deferred backlog:** the parked plan job `scholar-ingest-ocap-kernel-comment-fragments` remains in `plan/` awaiting promotion (not this cycle's to claim); the `scholar-ingest-frb-3` job sits in `todo/` for the gardener pool.

**Integrity gate (step 8):** `library-link-check.sh --source-slug endo--packages-marshal-src-marshal-js` → **exit 0** ("every checked link resolves to a committed file"), re-run after both lands. PASS.

Self-improvement: the scholar's per-cycle procedure documents idempotency checking inside an ingest task but does not name a standing **differential-refresh sweep** as a productive default for an idle hourly cycle (recent idle cycles reported "Sources ingested: none" and stopped). Folding a small bounded drift-sweep (path+commit sources vs the bare clone, anchor-refresh the non-substantive drifts) into the idle-mode cycle turns an otherwise-empty tick into honest-anchor maintenance and pre-empts repeated false-drift detection. Routed as an observation here for the mentor/liaison; not landed by the scholar.
