Press tick complete: this dispatch landed the **XS execution half of Phase 4** — the arc's finish line now runs end to end.

**Assessment.** The stack #276 (Phase 2 fetch) → #772 (Phase 4 resolver) → #782 (Phase 5 offline/.npmrc) → #785 (Phase 4 acquisition) was green, mergeable, and draft; the sole remaining gap was executing the assembled compartment map. #785 had recorded that gap as blocked on the xs2rust arc's boot-bundle generators, but on inspection that gate only covers the daemon/worker manager bundles at build time — the standalone native XS archive runner (`run_xs_archive_loaded` + `install_archive`) was already in-tree and whole. What was genuinely missing was the CAS-to-runner join and commonjs support (which real npm packages are made of). No live peer held any of these branches.

**What I built** (PR **#787**, draft, stacked on #785, commit `add76a4408`):
- `rust/endo/src/run_map.rs` (new): materialises the assembled map from the CAS into the runner's `LoadedArchive` — sources walked out of each compartment's `cas:sha256:` tree, parsers classified by Node semantics (`.js` by the package's `"type"`), and `"."` dependency edges bound to each target's default entry via `main`/`exports`/`index.js`.
- `rust/endo/xsnap/src/archive.rs`: the native archive runtime grew cjs (esm-functor wrap with `module`/`exports`/`__filename`/`__dirname` and a synchronous `require` over the link graph), json modules, referrer-relative specifier resolution, Node-style `.js`//`index.js` lookup, and shebang stripping.
- `rust/endo/src/bin/endor.rs`: `endor run <entry.js>` now executes instead of honestly stopping; design-doc status advanced to all-phases-implemented with the residual gaps recorded (conditional `exports` maps, `module`/`browser` fields, cyclic `require`, named esm imports from cjs).

**Real-execution evidence** (cited in full on #787): `cargo test -p endo --lib` 129/129 and `cargo test -p xsnap --lib` 117/117 (6 new tests, including an XS machine test of the esm→cjs→cjs is-odd→is-number shape); then the built binary from a **cold CAS** against the real registry — `endor run /tmp/endor-exec-demo/src/main.js` fetched is-odd@3.0.1 + is-number@6.0.0, assembled, and printed `is-odd(7) = true` / `is-odd(42) = false` over XS, exit 0; the `--offline` replay reproduced identical hashes and output with zero network. Package tree hashes match #772/#785's independent runs exactly.

**Follow-ups for the next tick:** watch CI on #787 (a local-build caveat: the gitignored xsnap boot bundles were borrowed from a peer xs2rust worktree since this branch lacks the worker/ses generators — the xs2rust stage-8 work); consider a richer multi-file/scoped-package demo and the recorded cjs-interop gaps; the stack (#276→#772→#782→#785→#787) is now feature-complete against the design and ready to be judged as a unit when the maintainer wants it un-drafted.
