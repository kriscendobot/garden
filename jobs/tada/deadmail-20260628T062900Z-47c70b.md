Inbox empty. Work complete.

---

## Completion report

**Job:** dead-lettered issue-inbox notification for kriskowal/garden#9 comment 4825122191 (intended recipient `issue-kriskowal-garden-9` had completed). Picked up its intent: continue the ymax0 v320 XS stack-overflow investigation and **verify whether `@agoric/internal/src/hex.js` changed between the beta versions, or is a red herring.**

**What I did**

1. **Verified the hex.js red-herring question — decisively, read-only via the GitHub API.** Between `ymax-v0.3.2606-beta2` (`776528eb`) and `ymax-v0.3.2606-beta3` (`9d518832`):
   - The file's blob SHA is **identical** at both tags (`791b4d953bb…`).
   - It is **not** in the `compare/beta2...beta3` changed-file set.
   - Its last-touching commit `5b3f4420c` (2025-04-09) is an **ancestor of beta2** (`ahead_by=3856, behind_by=0`).
   - **Verdict:** the `flatMap` decode-table build kriskowal flagged predates beta2 by 3,856 commits and is unchanged across the window → it **cannot be the beta2→beta3 regression cause**. Red herring confirmed. Added a secondary correctness note: `new Map(encodings.flatMap(...))` passes a single array argument (not an argument spread), so its native stack depth is O(1) in table size — it isn't even the XS-stack-overflow *mechanism* (that's the deep `passStyleOf`/`checkMatches` copyRecord recursion during import). So PR #7's bounded-loop refactor is harmless hardening but addresses a non-cause.

2. **Posted the verification as a comment** on issue #9 (https://github.com/kriskowal/garden/issues/9#issuecomment-4825135120). Did not close the issue (submitter's job).

3. **Continued the investigation per the directive** ("Agoric SDK is equally germane"). Posted a follow-on job `investigate-ymax0-v320-xs-depth-harness-20260628T0630Z` to run methodology step-6's export-free synthetic XS depth harness on the in-scope `kriscendobot/agoric-sdk` fork (bisect overflow N for passStyleOf/marshal/checkMatches through the cached `xsnap-worker` v0.14.2), carrying the ISSUE NOTE verbatim so its result routes back to the thread.

**What changed:** one issue comment posted; one follow-on board job queued. No code/garden-repo edits. No upstream `Agoric/agoric-sdk` interaction.

**Follow-ups:** The synthetic-harness job will report the empirical threshold curve back on issue #9. My scope memory was already current (a peer updated it for the parallel "revise standing instructions" comment 4825117452) — no action needed; that separate revision request is being handled on its own job.
