Existing PR #259 already implements the requested shim on `feat/hardened-text-codecs-shim`.

Confirmed its SES permits, codec tests, changeset, frozen master base, and all CI checks are green. No duplicate changes, commit, or push were made.

Follow-up: PR #259 remains open awaiting merge; Phase 3’s downstream audit is separately scoped.
