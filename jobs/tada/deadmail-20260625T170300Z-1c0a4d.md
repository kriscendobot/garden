## Completion report — dead-letter pickup `deadmail-20260625T170300Z-1c0a4d`

**Disposition: intent already discharged; one real gap found and closed with a new job.**

### What the dead-letter was
A coordination ACK from `reconcile-pr96-general-case` to the (already-completed) #96 doer `endojs-endo-but-for-bots-pr96-d9e3df0b`, saying it owned the 16:51Z directive, was folding in the specific transitive-dependency fixture+test (auxiliary `package.json` in a plain subdirectory of a dep-of-dep, no intermediate `node_modules`), and asking the recipient to stand down. It was one of a cluster of three #96 dead-letters (164749Z = maintainer RECONCILE directive to `finish-ebfb-pr96`; my 170300Z; 170305Z = the same stand-down to `pr96-rebase`), all converging on the same reconcile.

### Verification that the intent landed
The sender `reconcile-pr96-general-case` is in `tada` (completed) — it executed and pushed the work. I confirmed on the live #96 branch (`design/compartment-mapper-auxiliary-package-json`):
- Reconcile-onto-the-lazy-walk landed in commit `16ce3b08d` (17:05Z); the entry-only precompute was removed; `languageForExtensionByPrefix` is now populated lazily for every compartment.
- The transitive fixture exists exactly as promised: `packages/compartment-mapper/test/fixtures-auxiliary-transitive/` (`app → dirdep → transdep`, plain-subdir `transdep/aux/` auxiliary, nested `flip-back/`, `deep/buried.js`) plus `auxiliary-transitive-language.test.js` and the `language-for-extension-by-prefix.js` helper.
- CI is fully green; the 17:06Z PR comment already posted the coherent-diff summary **and named the fixture path** — so even the message's "will confirm the fixture path when pushed" promise was delivered.

Nothing of the message's intent was left to carry forward — the work and its confirmation were both already on the PR.

### The gap I found and fixed
The later 2026-06-25T17:55Z `CHANGES_REQUESTED` review carried three inline asks. One (consolidate descriptor types) landed via job `pr96-0105506f`. That job's report claimed the other two were "tracked under their own jobs," but no such job exists anywhere on the board (todo/doin/plan/tada). The two orphaned asks:
1. Move the design doc into `packages/compartment-mapper/designs/`, rewritten to state only the implemented design.
2. Account for `ts`/`mts`/`cts` in `language-for-extension-by-prefix.js`, with Node.js-parity test fixtures.

Per the standing proactive-fix directive (in-scope bot-fork PR, reversible), I posted one job **`pr-ebfb-96-review-followup`** to the board capturing both items with full provenance, DoD (green tests, tsc/eslint clean, regression evidence, inline replies + one top-level summary comment), and the review text quoted as data.

### Follow-ups for the liaison
- **Apparent watcher gap:** a 3-comment `CHANGES_REQUESTED` review produced only one tracking job (`pr96-0105506f`); the other two comments silently produced none. Worth checking whether the comment/review-directive watcher posts per-review rather than per-comment, since that would systematically drop sibling asks. (Diagnosing the watcher was out of scope for this dead-letter job.)
- The two sibling dead-letters (`...164749Z-5d8697`, `...170305Z-ce5467`) are still in `doin`; they share this cluster's now-discharged intent and should resolve to the same conclusion.
