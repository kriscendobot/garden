Both index jobs are confirmed live on the board (idempotent no-op on re-post). The audit is complete.

## Completion report

**Librarian library audit** of `journal/library/` (on `journal2`): 6674 sections, 215 concepts, 73 topics, 904 sources.

**Oversize / concept-mixing audit — no violations found; nothing posted.**
- The largest topic files (`capability-security.md` 312L, `hardened-javascript.md` 169L) are section-index **tables** — scanned against a clear abstract, not read top-to-bottom; they satisfy the abstract-at-the-top exit criterion.
- The largest concept files (`passable-equality.md` 151L, `space.md` 150L) are rich but **single-concept**, well-abstracted pages.
- Section files are leaf digests, single-source-section by construction (largest body ~416L); splitting further is not the ingest model.

**Index-gap audit — 2 genuine gaps found; 2 index jobs posted.**
1. `librarian-index-sources-compartment-class-20260726` — `sources/tc39-module-harmony--compartments-compartment-class.md` (status `current`, ingested 2026-07-21) is missing from `sources/README.md` while all its cluster siblings are listed.
2. `librarian-index-exo-stream-aliases-20260726` — `concepts/exo-stream.md` is the only concept lacking `aliases:` frontmatter, making it unreachable by term-based library-lookup search.

**Checked and cleared (no job needed):**
- All 215 concepts listed in `concepts/README.md`; all 73 topics in `topics/README.md`. Fully indexed.
- The other "missing" source, `erights-org--elang-intro`, is a **superseded tombstone** redirecting to canonical `erights--elang-intro` (which is indexed) — correctly omitted, not a gap.
- `keywords.md` is partial by design (concept `aliases:` frontmatter is the real keyword index), so no concepts-missing-from-keywords gap beyond the exo-stream aliases fix above.

**Follow-up (not posted — self-healing, low value):** 22 sub-section files of the retired v1 `garden--designs-driver-md--…482b40e2` source are absent from the auto-generated `sections/README.md`. That index is regenerated by `scripts/jobs/regenerate-sections-index.sh` (+ the `garden-regenerate-sections-index` systemd unit), so this is not a hand-editable index job; it looks like a possible truncation/edge-case in the generator for that source's 240-char filenames. Worth a glance by whoever owns the regenerator, but the source is a retired design and the sections themselves exist.

Inbox drained (empty). No live jobs pre-existed for either gap.
