No code changes were warranted — the work was verification and carrying the stranded intent forward. Report:

## Report

**Dead-letter intent: moot on its face, but its tail was still live.**

The dead-lettered message warned `endojs-endo-but-for-bots-pr671-conduct` that the deployed approval gate would stall its merge, and suggested re-parking. That warning was already stale when sent: **endojs/endo-but-for-bots#671 merged at 2026-07-29T02:33:47Z** (merge commit `50972e791d`), 69 seconds *before* the message was dispatched. Nothing to carry forward on #671 itself.

What was still live was the message's closing paragraph — three PRs (#656, #671, #691) stranded on the un-deployed gate. I re-verified that whole strand.

**Verified (real execution, on this host `endolin-garden-ece02cb4`):**

- The gate fix **is deployed**. `/home/kris/garden/scripts/jobs/handlers/pr-maintainer-approval-gh.sh` is byte-identical to the `main2` copy (`diff` clean), i.e. carries `c510ec1b4f`. Running it from the *deployed root* against #656 emits the post-fix message — `no current APPROVED review on head d74caef78c…` — not the pre-fix `reviewDecision=none` short-circuit. The `main2`-worktree work-around is no longer needed by anyone.
- #671 MERGED 2026-07-29; **#691 MERGED 2026-07-30**. Two of three landed unaided.
- **#656 is still OPEN and now blocked for a different, legitimate reason:** head moved `76e6800ee5` → `d74caef78c`; kriskowal's `APPROVED` review still carries `commit_id 76e6800ee5`, so it is stale by design (kriscendobot's review on the new head is `COMMENTED`, which does not approve). `mergeStateStatus: UNSTABLE`; 23/24 checks pass, `test (24.x, macos-15)` pending.

**What changed:**

- Annotated `jobs/plan/endojs-endo-but-for-bots-pr656-conduct.md` (key `pr656-gate-deployed-approval-stale-20260801`) with the corrected facts. Its recorded rationale — "promote only after `main2` is deployed" — was obsolete and would have misled the gate-holder into thinking a deploy was the remaining blocker. I did **not** promote it; promotion is the gate-holder's call, as that job's own text states.
- Messaged the maintainer (`inbox/maintainer`, `20260801T095615Z-067488`) that the deploy strand is closed and #656 needs a re-approval on `d74caef78c`.

**No garden commits.** No defect in `main2` surfaced — the fix was already correct and already deployed; nothing to push.

**Follow-up (owner: kriskowal):** re-approve endojs/endo-but-for-bots#656 head `d74caef78c`, then promote the parked conduct job. It should merge cleanly once the pending macOS check finishes.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/deadmail-20260729T023454Z-34a94e.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 19 tokens (457641 cached reads)
- Output: 8083 tokens
- Cost: $0.8496814999999999
- Wall-clock: 146s

<!-- garden-usage-end -->
