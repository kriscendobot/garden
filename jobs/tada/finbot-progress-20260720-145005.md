Advanced finbot `main` to `55c7c02`: DM reporting now tests the live selector’s best-asymmetric-vs-GARCH contest. Added regression coverage and updated docs/design.

Verified: `npm test` passed (599 tests); default and auto-family dry runs were auditor-approved with wallet untouched.

Maintainer notified. Next: decide whether `significanceAlpha` becomes the live default.

Self-improvement: nothing this time.
