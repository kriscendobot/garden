## Completion report — `ocapn-noise-press-20260717-060503`

**Assessment.** All five OCapN.md milestones (M1–M5) hold, and this dispatch closed the previous dispatch's open follow-up: **CI is confirmed green on the entire rebased stack** — #340 `a3d3b414` (25/25 checks SUCCESS), #684 `f3a54b6b`, #688 `a6c9740c`, #693 `a371b007` (23/23 each); all MERGEABLE, all draft on base `llm`. #683 is MERGEABLE/CLEAN (behind llm by 248 but merging cleanly; no action for a draft demo PR). Designs: `ocapn-noise-network` Complete, `ocapn-noise-session-reconnect` still Proposed/unimplemented. Both minion.town endpoints answer 426 Upgrade Required (listeners alive). No live peers on the stack branches; inbox empty throughout.

**Regression found and fixed (the dispatch's substantive work).** Running #693's cross-host demo against minion.town **failed** on the rebased code: the deployed `endo-pet-daemon` container (built 2026-07-11, pre-rebase) minted invitations in the old `?at=`/`?id=` locator format, which the rebased local daemon rejects — `Invalid locator …: Missing formula number` (llm moved the formula number and hints into `@`-delimited path components). Mixed-version peers cannot pair across the format change. I redeployed the container from #693's head `a371b007` via SSM (`deploy.sh` fetch/build/run from a copy outside its own clone dir, build as a transient systemd unit, ~4 min with BuildKit layer cache after pruning 3.3 GB of stale builder cache). The persisted `/data` volume carried the node id (`a6cd6e01…`) across; the untouched standalone greeter service (`/opt/endo`, `:8930`) was verified out of blast radius first. A prior reaped dispatch had fetched but never built.

**Empirical re-validation on head `a371b007` (all real runs today):**
- Local two-daemon invite/accept: `DEMO PASSED` over **both** `ocapn+noise+tcp` and `ocapn+noise+ws` (`E(adder).add(2,3) → 5`, Far identity preserved).
- OCapN daemon suites (`networks-ocapn`, `invite-retention-ocapn{,-ws}`, serial): **27/27 pass**.
- Live `wss://minion.town/ocapn` greeter round-trip: `RESULT {"ok":true,…}` after Noise IK against live designator `810b996c…`.
- **Cross-host Pet-Daemon invite/accept** over `wss://minion.town/ocapn-daemon`: **`CROSS-HOST DEMO PASSED`** — invitation minted on minion in the new format, accepted locally, peer bound as `minion`, capability round-trip across the Noise session.

**Recorded:** evidence comment on #693 (issuecomment-5002218184); `ocapn-over-noise-goal` memory + index updated (redeploy facts, the mixed-version locator incompatibility, and the local-run recipe: skip-build install + borrowed `better_sqlite3.node`, `aws` at `~/garden/.local/bin`, `ssm.sh` as the boto3-free `SSM` override). No pushes to any PR branch (no CI churn); the only mutations were on the minion.town host and the journal/memory.

**Follow-ups for the next dispatch:** (1) CLI-level `endo invite`/`endo accept` on minion; (2) mutual bidirectional pairing; (3) `daemon-agent-network-identity` closure; (4) session-reconnect design still unimplemented; (5) maintainer question about opening a non-443 TCP port for cross-host TCP+CBOR (msg 20260717T001224Z-3483f4 from the earlier reaped dispatch) still unanswered — watch for the reply on the bus; (6) consider whether old-format locator acceptance or a clearer version-mismatch error is worth building before mixed-version deployments matter.
