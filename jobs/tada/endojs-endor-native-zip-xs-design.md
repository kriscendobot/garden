Created and pushed draft design PR #828: native Rust raw-DEFLATE host functions selected by `-C xs`, including bounded inflate, snapshot ABI migration, and XS test plan.

Changed:
- `designs/endor-native-zip-xs.md`
- `designs/README.md`

Committed: `8a857b7aa`. Verified clean diff with `git diff --check`. Prettier was not run because dependencies are not installed in the fresh worktree.

Follow-up: review and implement PR #828; PR #160 was not modified.

Self-improvement: nothing this time.
