# scholar-ingest-erights-7 — done

Ingested the E grammar child-chapter cluster: 7 new web sources (each 1
consolidated section) off the `erights--elang-grammar` hub map, all verified
reachable on the erights.org GitHub Pages mirror first.

Sources: `erights--elang-grammar-{expr,prim-expr,patterns,quasi-overview,quasi-xml,dispatchee,lexical}`.
`quasi-xml` ingested at status: stale (upstream-flagged obsolete; kept for the
child-page map).

Indexes: +7 rows topics/e-language, +3 rows topics/patterns, +7 rows sources/README;
refreshed the erights--elang-grammar hub note. Integrity gate PASSED on all 7
slugs + hub + --changed. Sections index regenerated and landed.

Follow-on `scholar-ingest-erights-8` posted (ELib concurrency child chapters).
Grammar chapter is now fully ingested; no further grammar child page exists.

Self-improvement: nothing this time.
