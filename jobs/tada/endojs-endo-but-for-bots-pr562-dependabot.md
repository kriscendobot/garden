## Report

**Verdict: REJECT (superseded), executed.** [endojs/endo-but-for-bots#562](https://github.com/endojs/endo-but-for-bots/pull/562) (happy-dom 15.11.7 → 20.10.6) is **CLOSED** (`closedAt=2026-07-28T07:35:04Z`, verified via `gh pr view`), with the structured verdict comment posted first ([issuecomment-5101282899](https://github.com/endojs/endo-but-for-bots/pull/562#issuecomment-5101282899)) plus an addendum ([5101329947](https://github.com/endojs/endo-but-for-bots/pull/562#issuecomment-5101329947)).

**Why REJECT rather than MERGE-NOW.** On the merits the bump is MERGE-NOW-class — the review found nothing against it. The *vehicle* failed: [#869](https://github.com/endojs/endo-but-for-bots/pull/869) (opened 2026-07-26) moves the same dependency in the same three `package.json` files against the same `llm` base to the newer 20.11.0, from a current base with fresh green CI. #562 was untouched since 2026-06-28, its head a month behind `llm`, so its green CI attested to a base shape that no longer exists. Both cannot land.

**Diligence completed (all 11 workflow steps).**
- **Pre-flight:** diff touches only 3 `package.json` + `yarn.lock`. Clean. `direct:development`.
- **Transitive set (54 added / 18 removed):** newly introduced `@types/whatwg-mimetype@3.0.2`, `buffer-image-size@0.6.4`, `undici-types@8.3.0`; new `@types/node@26.0.1` resolution; new major `entities@7.0.1` (4.x retained); `webidl-conversions@7.0.0` removed. **`ws` does not move** (happy-dom's `^8.21.0` merges into existing `ws@8.21.0`). No version 24h-fresh (newest 34 days). No license change. Named call-out: `buffer-image-size@0.6.4` published 2018-03-01, still `latest` — an unmaintained 8-year-old package entering the tree, dev-only, advisory-free.
- **Advisories:** the version **in the tree today** (15.11.7) carries three open advisories — **GHSA-37j7-fg3j-429f CRITICAL** (VM context escape → RCE, `>=0 <20.0.0`), GHSA-6q6h-j7hj-3r64 HIGH, GHSA-w4gp-fjgq-3q4g HIGH. Both 20.10.6 and 20.11.0 query clean. `yarn npm audit --all --recursive`: **zero hits on any package the PR moves** (its 175 lines are the pre-existing dev-toolchain backlog).
- **Install with scripts disabled:** `YARN_ENABLE_SCRIPTS=false corepack yarn install --immutable` exit 0, lockfile immutable-consistent.
- **Source read:** no install/preinstall/postinstall script, no `bin`. `child_process` in exactly one file (`lib/fetch/SyncFetch.js`, `execFileSync(process.argv[0], ...)` — the long-standing sync-fetch design, not a new spawn surface). `eval`/`new Function` only in the `window.eval` surface. No telemetry egress. Regression evidence for GHSA-6q6h: the fix is *readable* as `VALID_VARIABLE_NAME_REGEXP` gating in `src/module/ECMAScriptModuleCompiler.ts`, not merely claimed in release notes.
- **Local verification (real execution):** ran the three consumer suites against the head tree — `markmdown` 15, `space-file-explorer` 80, `chat` 762 = **857 tests passed, zero failures**. The 15.x→20.x major does not break consumers.
- **Maturity:** 20.10.6 published 2026-06-17, 41 days — window satisfied, no embargo. Terminal verdict, so **no ledger row opened and no recheck schedule placed**.

**Two things I verified rather than assumed, and one claim I narrowed:**
1. **CI rollup cross-check:** all 22 check-runs at head are `success`, but `/commits/<sha>/status` reports `state: pending` — **vacuous**, `total_count: 0`, because the repo posts no legacy commit statuses. #869's head shows the identical artifact, so it is repo-wide, not a per-PR defect. I initially flagged this to the maintainer as actionable, then checked and **narrowed it**: `ci-wait-merge.sh` and `ci-rollup-gh.sh` both read `statusCheckRollup`, so no fleet gate is affected. Sent a correcting follow-up so nobody chases a non-bug.
2. **Supersession ancestry.** A peer objected that a higher version number does not prove containment. `compare/v20.10.6...v20.11.0` → `status: ahead, behind_by: 0` (reverse: `behind_by: 4`). v20.10.6 is a **strict ancestor**, linear superset, nothing lost. The close stands on verified ground.

**Open follow-up — not mine to close, and worth attention.** #869 is `MERGEABLE`/`CLEAN` with green CI but `reviewDecision` is empty and it carries **no reviews**, so the conductor spine's maintainer-approval gate will refuse it (`merge blocked: no maintainer approval`). **The CRITICAL advisory stays unrepaired in the tree until a journal maintainer approves #869.** I surfaced this to the maintainer inbox and handed the full diligence to the gardener holding `endojs-endo-but-for-bots-pr869-dependabot` so it is not redone.

**Journal:** two `result` entries tagged `project: endo-but-for-bots` (`073552Z-result-botanist-94e416`, `074116Z-result-botanist-c7663f`).

**Incidental, outside my scope:** `git gc` in the shared garden repo fails with `fatal: unable to read 9ad05cc3563a7ba4b8f3a0b3e7941090e4d427d6 / failed to run repack`, leaving a stale `gc.log`. Pushes and fetches work fine, so this is a maintenance-path issue, not a broken repo — but someone should look, and I did not run git maintenance in the root.

**Self-improvement:** `roles/botanist/AGENT.md` (pushed to `main2` as `9321496acb`, confirmed at `refs/heads/main2`); added the pre-flight supersession check, a `REJECT (superseded)` verdict clause insisting the comment not read as a defect finding, a correction to step 6's CI cross-check (name `check-runs`, not the legacy `status`), and three notes from the field. My first push hit a conflict: a peer had already landed a **richer** version of the same check (it catches that the stale PR's *branch name* keeps the old target while its title rebases forward — a detail I lacked). I kept theirs and folded in only what mine added, rather than overwriting the better text.
