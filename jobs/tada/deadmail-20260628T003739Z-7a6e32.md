Done. The work is complete and the result is posted to the issue.

## Completion report

**Job:** dead-lettered message intended for `issue-kriskowal-garden-9` — maintainer comment on kriskowal/garden#9: *"Consider using separate worktrees for the two betas with separate builds."* I picked up its intent and ran the experiment it suggested.

**What I did:** Set up two separate agoric-sdk worktrees off the bot fork (`kriscendobot/agoric-sdk@9d518832d`, the #12761 tree), one pinned to the **beta2** Endo set, one to **beta3**, each with its own `yarn install`. Built a contract bundle in each (esbuild inlines that worktree's Endo; only cosmic-proto `dist/` needed pre-building), bundled both host `@endo/init` SES realms (ses 1.14 vs 2.2), and imported each bundle under its matching host on the cached XS worker.

**Decisive result — it reverses the prior conclusion.** Same contract source, identical V8 exports; only the Endo set differs:
- beta3 Endo libs → **XS STACK_OVERFLOW**
- beta2 Endo libs → **imports cleanly on XS**
- Cross-control (beta3 libs under the **beta2** host) → still overflows → isolates the cause to the **contract-side `@endo/{pass-style,marshal,patterns}` libraries**, not the host `ses` version and **not the contract change (#12761)**.

This corrects the prior sessions' repeated "Endo ruled out" finding (that was a synthetic-probe artifact). A concrete lead surfaced: pass-style 1.8.1 pulls a new `hideAndHardenFunction` from `@endo/errors` (absent in beta2), a hardening wrapper that plausibly adds the frames pushing import over XS's fixed budget — also the reason single-package bisection won't build (the libs are version-coupled).

**Posted:** https://github.com/kriskowal/garden/issues/9#issuecomment-4823597582 — full result matrix, reproducible methodology, revised fix direction (contract-side flatten to unblock now; a now-warranted upstream Endo XS-repro issue, no longer merely informational). Issue left open per protocol.

**Artifacts left on host for re-run:** worktrees `~/.cache/garden-scratch/exp6-2worktree/{beta2,beta3}`; bundles/driver/host scripts under `/tmp/xs6/`. Incomplete `mix` worktree removed.

**Scope:** read-only/repro on the bot fork + public npm only — no agoric-sdk push/PR/merge. Inbox drained empty throughout.

**Follow-ups for the maintainer to consider:** (1) the upstream Endo issue with the minimal XS repro; (2) contract-side guard/exo flattening as the immediate chain unblock. Both are agoric-sdk/chain-side decisions, out of bounds for autonomous garden action.
