The work is complete. Final report:

---

## Completion report — `deadmail-20260628T060057Z-400273`

**Intent picked up:** A dead-lettered message for the (torn-down) doer of `kriskowal/garden#9`. It carried kriskowal's comment `4825045739`: *"This is odd considering the problem we are trying to fix pertains to use of stack overflow when using flatMap under XS with large arrays spread."* — i.e. **DRAFT PR `kriscendobot/agoric-sdk#7` was misaimed.** It fixed hex-codec *validation consistency*, but missed the actual XS defect. A later gardener had *promised* the re-aim "is being handled on that thread," but PR #7's head was still the validation-only commit. I carried the intent forward and did the re-aim.

**What I did:**
- Diagnosed the real defect in `packages/internal/src/hex.js`: the portable codec's `decodings` map was built by feeding a 1024-element pair array from `encodings.flatMap(...)` into `new Map(...)` — the large-intermediate-array/spread pattern that overflows the metered XS stack.
- Replaced it with a **bounded `for` loop** that inserts the four case permutations per byte incrementally (stack depth O(1) in table size; no `flatMap`, no spread).
- **Verified behavior-preserving:** new table is byte-identical to the old (484 entries) and all 48 `hex.test.js` accept/reject/round-trip/cross-codec checks pass against the edited source via a standalone harness (the file has no `@agoric` deps).
- Committed `47b701c` and pushed to the PR branch (fork-internal, DRAFT, `kriscendobot/agoric-sdk`; nothing touched upstream `Agoric/agoric-sdk`).
- Updated PR #7's title/body to lead with the XS fix while keeping the genuine validation-consistency commit (`f36fd16`).
- Replied on the issue thread (`#issuecomment-4825085942`); left the issue open for the submitter to close, per protocol.

**Changed:** `kriscendobot/agoric-sdk` PR #7 — +1 commit (`47b701c`), retitled/re-bodied. One issue comment on `kriskowal/garden#9`.

**Scope:** fork-internal DRAFT on a bot-owned fork under direct maintainer direction — pushed with the active `kriscendobot` token. No upstream push, no merge, no close.

**Follow-ups / honest limits:**
- I did **not** run the full `@agoric/internal` ava suite under a real install (heavy monorepo install); the standalone harness mirrors `hex.test.js` exactly, but a CI run on the PR would confirm the broader package.
- PR #7 remains DRAFT awaiting kriskowal's review.
- A standalone clone remains at `/home/kris/scratch/hexfix` (independent clone, not a garden worktree); the sandbox guard blocked `rm` of it — harmless ephemeral scratch, safe to reap later.
