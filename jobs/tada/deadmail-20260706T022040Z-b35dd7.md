I've traced the full picture. Here's my determination.

## What the dead-lettered message was

The message was the **fixer** `xs2rust-endor-fix-bound-callback-dispatch`'s **DONE completion report** (bound-callback dispatch fix on PR #600, commit `84e119fae`), addressed as a status reply to the parked Fable supervisor `port-xs-to-rust-memory-safe-engine-s8`. It is a *reply to that doer*, not a request — so the task is to carry the reply forward.

## Why it dead-lettered, and why the intent is already preserved

- **s8 is parked, not completed.** It sits in `jobs/plan/port-xs-to-rust-memory-safe-engine-s8.md` with gate `blocked`, `blocked_on: xs2rust-endor-strings-utf16` (that orchestration is still `state: running` — build child in `doin/`, test child in `plan/`). A parked job has no inbox (the bus creates it only at claim), so the fixer's `inbox-send` had no live mailbox to land in and dead-lettered. Re-sending is impossible/counterproductive: `inbox-send.sh` refuses inactive doers, so it would just dead-letter and re-promote in a loop.
- **The reply's content is already durably recorded** in `jobs/tada/xs2rust-endor-fix-bound-callback-dispatch.md` — the fixer's full completion report (root cause, the `interp.rs` fix, the `Option<usize>` body_start defuse, coverage, and the bar: Array covered=437 no-abort divergent=0, workspace `cargo test` green, `#![forbid(unsafe_code)]` intact). The fixer *deliberately* wrote it there, noting "the supervisor report was dead-lettered … and promoted for delivery, so the intent is preserved."
- **s8's own spec routes to exactly that record.** Its "Regression fixer in flight" bullet says: *"VERIFY IT LANDED (check `jobs/tada/`, inbox `port-xs-to-rust-memory-safe-engine-s8` for its report, and re-run `built-ins/Array` whole-tree…)."* When the `strings-utf16` orchestration lands in `tada/`, the unblock watcher promotes s8; s8 then reads `jobs/tada/` and finds the fixer report there. s8 will independently re-run the Array whole-tree as its verification, which is the correct place for that check.

## Action taken

None beyond verification — and that is the right outcome, not a shortcut. Every delivery path was considered and the intent is provably preserved:
- Re-`inbox-send` → refused (s8 inactive) and would re-dead-letter.
- Hand-editing the `jobs/plan/` body → `jobs/` is not an allowlisted tree for `land-journal-edit.sh` (only `library/`/`projects/`); the job scripts own `jobs/`, and hand-editing the live journal worktree is the exact hazard the tooling forbids.
- The durable, sanctioned channel (`jobs/tada/`) already carries the report, and s8's spec reads it first. This job's own `tada/` report (this text) adds the audit trail that the dead-letter was handled, not dropped.

No garden-code changes; worktree clean, nothing to push.

## Follow-ups

- None required. When `xs2rust-endor-strings-utf16` completes, s8 will be promoted and will find the fixer report in `jobs/tada/` per its spec and run its own Array-whole-tree verification.
