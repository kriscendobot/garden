Inbox empty. The carry-forward is complete.

---

## Completion report — `deadmail-20260627T151020Z-5f405e` (intent of `cognito-mcp-metadata-bridge`)

**Carried forward a dead-lettered maintainer reply** to the already-completed `cognito-mcp-metadata-bridge` doer.

### Context
The original doer built DRAFT PR **#2** on `kriscendobot/minion.town` (branch `cognito-mcp-metadata-bridge`) — the Cognito↔MCP OAuth bridge (RFC 8414 metadata + RFC 7591 `/register` + RFC 8707 audience). It proceeded on its *recommended defaults* but left the PR **draft pending maintainer confirmation** of two design Open Questions, and posed them via the liaison. The doer completed before the maintainer's reply landed, so the reply was dead-lettered into this job.

### What the reply said vs. what was built
The maintainer concurred on both points — *proceed as planned, no redirect*:
- **#1 (IdP):** Stay with Cognito + API Gateway + Lambda bridge → **matches the build** (issuer kept = Cognito; reversible to an MCP-native IdP later via config).
- **#2 (DCR):** Ship all three with `/register` behind the default-on stack toggle → **matches the build** (`enableDcr` default-on; `-c enableDcr=false` drops `/register`).
- Scope: bot repo, `cdk synth` + local handler unit tests, no live AWS → **already satisfied** (22 vitest tests pass; synth passes both DCR modes).

The approval matched the implemented defaults exactly, so **no code change was required**.

### What I did
1. Verified PR #2 state (OPEN, DRAFT, MERGEABLE) and repo access (gh as `kriscendobot`, admin/push).
2. Posted a top-level **maintainer-resolution summary comment** (`#issuecomment-4818660986`) mapping each Open Question to the build and listing remaining non-blocking follow-ups.
3. **Un-drafted PR #2** — now `isDraft:false`, OPEN, ready for review.

### Follow-ups (non-blocking, recorded on the PR)
- Deploy follow-on `synth-and-deploy-minion-town-aws`: confirm Cognito emits `aud` or falls back to the `mcp_aud` mirror claim.
- Optional later reversibility knob: expose issuer/JWKS as config for a true config-only IdP swap.
- Open Question #3 (scope-naming `mcp:tools` vs `mcp/tools`): pre-existing, untouched, tracked separately.
- PR #2 is now ready for the maintainer (or a conductor merge) — no further action owed by this job.
