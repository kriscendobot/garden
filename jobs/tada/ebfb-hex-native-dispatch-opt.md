Inbox is empty and the PR is verified (draft, base `llm`, exactly the two design files). The job is complete: the design is authored, integrated into the roadmap README, and the draft PR opens it into the standard design→PR pipeline (once merged to `llm`, the design-to-PR poller will detect it as uncovered and post the build job; the panel/fix/un-draft chain follows the normal flow from there — deliberately not run from inside this claimed job, per the pipeline skill's no-nesting rule).

## Completion report

**Job:** `ebfb-hex-native-dispatch-opt` (designer) — optimize `@endo/hex` implementation dispatch across platforms, follow-up from kriskowal's approving review of endojs/endo-but-for-bots#580.

**What I did:**
- Authored `designs/hex-native-dispatch-opt.md` on branch `design/hex-native-dispatch-opt` (off `origin/llm`) in an isolated project worktree, following the project's design conventions (metadata table, absolute dates, Design Decisions, Known Gaps, Prompt section).
- The design specifies: (1) native TC39 `Uint8Array.fromHex`/`toHex` stays first-choice on every platform via the existing pre-lockdown intrinsic capture; (2) the char-code codec (`jsDecodeHex`/`jsEncodeHex`) remains the default pure-JS fallback for Node/web — the #580 benchmarks show it is the fastest portable decoder on V8, and the default build stays byte-for-byte identical to what ships today; (3) a legacy XS variant behind a package.json `exports` `"xs"` condition (precedent: `ses`, `module-source`, `compartment-mapper`; selected by Node `--conditions xs` or bundle-source `-C xs`) whose non-native decode fallback is the char-pair `Map` decoder from agoric-sdk#7 — 2.2–2.6× cheaper metered compute on XS per #580, no `flatMap`, table built eagerly at module load only when native is absent so there is no post-lockdown mutable module state. Error paths re-run `jsDecodeHex` so diagnostics stay identical across variants. The XS *encode* fallback is gated on a metered-xsnap measurement (#580 measured decode only). A Node `Buffer` tier is considered and rejected with reasons (silent truncation semantics, shrinking no-native window, directive names char-code). Test plan: a second `@endo/hex-test` ava pass under `--conditions=xs` plus a fuzz equivalence test.
- Synced `designs/README.md` per convention: last-updated prose, summary table row (Proposed), totals recount (146 designs, 30 Proposed), Milestone 2 table row (marked as post-completion follow-up that does not reopen the exit criterion), per-design estimate (S, 1–2 days, timeline-neutral).
- Opened **draft PR endojs/endo-but-for-bots#664** against the `llm` roadmap branch, body citing the originating review (4668982725) on #580.

**Follow-ups (owned by the normal pipeline, not this job):** maintainer review/merge of #664; once on `llm`, the design-to-PR poller posts the `build` job (implementation PR roots at `master` since `packages/hex` exists on both branches), then panel → fix-loop → un-draft. Known gaps flagged in the design for the builder: XS encode numbers (measurement gate), confirming when Moddable/xsnap ship `fromHex`, and confirming which Agoric build paths pass `-C xs`.
