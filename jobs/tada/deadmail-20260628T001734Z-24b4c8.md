Posted to issue #9: https://github.com/kriskowal/garden/issues/9#issuecomment-4823364518

---

# Completion report — dead-lettered job deadmail-20260628T001734Z-24b4c8

**Intent picked up:** A maintainer (kriskowal) comment on kriskowal/garden issue #9 ("Please try experiment 6") was dead-lettered because its doer `issue-kriskowal-garden-9` had already completed. I carried it forward as new work: ran experiment 6 of the ymax0 v320 XS stack-overflow investigation and replied on the issue thread.

**What I did:**
- Inventoried host artifacts. The cached **xsnap-worker v0.14.2 (XS 13.3.0)** is present and execs (sandbox must be disabled). But **`bundle-ymax0.json` and the agoric-sdk checkout were wiped by a redeploy** — so experiment 6's *primary* form (real `importBundle` through the xsnap supervisor) is no longer reproducible on this host, and re-sourcing the bundle is chain/out-of-scope.
- Built and ran the **synthetic fallback**: drove the real `@agoric/xsnap@0.15.0` controller against the cached worker (symlinked path, protocol confirmed compatible), bundled SES + `@endo/pass-style` + `@endo/patterns` into XS via esbuild, and binary-searched the nesting depth where `passStyleOf` and `mustMatch`/`checkMatches` trip XS's stack — with V8 controls.

**Results (largest-survive / smallest-fail):**
- `passStyleOf`: **XS ~51**, V8 ~1999 (~39×). Array variant XS ~52, V8 ~2124.
- `mustMatch`: **XS ~50** across `splitRecord`/`recordOf`/`M.any()`; V8 500–3921.
- XS trip is an **uncatchable process abort** (`exited: stack overflow`), confirming the slog's uncatchability; it's the native C-stack limit (`-l` independent).

**What changed in the conclusions:** Confirms the qualitative model and the V8 ~2047 prediction (~1999 measured). **Corrects** two absolute XS numbers on this worker — passStyleOf ~51 not ~115, and checkMatches ~50 not ~15 (checkMatches is *not* markedly shallower than passStyleOf here). Most likely cause of the gap: this prebuilt worker's fixed stack is smaller than the build the earlier estimates assumed — so the *ratio/mechanism* is portable, the absolute ~50 is a calibration of this binary.

**Caveats reported on the issue:** no `lockdown()` inside XS (deep-freeze `harden` shim used; V8 control used real lockdown); absolute depths are worker-stack-specific; the synthetic test calibrates the budget but does not by itself fire the "clean import overflows → no export needed" decision branch.

**Follow-ups (left for the maintainer):** The decisive next artifact is still agoric-sdk/chain-side — either re-source `bundle-ymax0.json` so the real import runs on this worker, or the v320 swing-store export. Reusable harness left at `~/.cache/garden-scratch/exp6-ymax0/`. Issue left open (submitter closes).

No git pushes, PRs, or agoric-sdk writes — all work was local, read-only experimentation. Inbox drained (empty), no peer/maintainer messages arrived mid-job.
