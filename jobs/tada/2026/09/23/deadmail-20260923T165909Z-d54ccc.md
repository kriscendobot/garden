## Completion report

**Job:** `deadmail-20260923T165909Z-d54ccc` — pick up the intent of a dead-lettered message originally addressed to the completed design job `design-sysop-attested-exec-op-20260916`.

**The message (maintainer, kriskowal, 2026-09-23 muster):** "harden the launcher. Posted builder job `harden-garden-container-no-privileged-no-sudo` (drop --privileged, remove NOPASSWD sudo, acceptance probe, staged per-host recreate)." It was a reply/FYI to the design doer, closing the ship gate in `designs/sysop-attested-exec.md` (§ "Boatman and ferry boundary", ~lines 286-297) that refuses to ship attested `exec` while the launcher runs `--privileged` and the bot user has passwordless sudo.

**What I did:**
1. Traced the intent to its concrete ship gate in `designs/sysop-attested-exec.md` and confirmed both hardening targets exist in the tree today: `garden:307` (`--privileged`) and `Dockerfile:314` (`${USERNAME} ALL=(ALL) NOPASSWD:ALL`, plus `-G sudo` at :312).
2. Searched the deployed journal worktree for the named builder job and initially found nothing — but the deployed `/home/kris/garden2/journal` worktree was **stale** (HEAD `9da22c4b`, while `origin/journal2` was at `a7ec8345`).
3. Ran `post-job.sh harden-garden-container-no-privileged-no-sudo`, which reported **"already present in lifecycle (plan/todo/doin); nothing to do"** (a clean idempotent no-op by basename).
4. Verified against `origin/journal2` in an isolated scratch repo (outside `$GARDEN_ROOT`): the job lives at **`jobs/doin/harden-garden-container-no-privileged-no-sudo.md`** — it has been **claimed and is actively in progress**. Read its body and confirmed it is genuinely the launcher-hardening build (posted by the liaison, `role: builder`, same `--privileged`/NOPASSWD gate, with the acceptance-probe and staged per-host-recreate deliverables the maintainer named).

**Outcome:** The dead-lettered intent is already durably captured and in flight. The maintainer's builder job exists on the board (`doin/`) and a gardener is working it now. No new job was needed and none was created (the post correctly no-op'd); no garden-dev changes to land on `main2`.

**Changed:** nothing committed (intent already owned; my scratch temp dirs cleaned up).

**Follow-ups:** none from this job. The launcher-hardening work — and updating `designs/sysop-attested-exec.md` to record the prerequisite as addressed (pending per-host container recreate) — is owned by the in-progress `harden-garden-container-no-privileged-no-sudo` job. Note for operators: the deployed root's `journal/` worktree at the time of this job was several commits behind `origin/journal2`; the fleet's per-id clones were current, so job posting was unaffected, but a manual reader of the deployed worktree would see a stale board.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/deadmail-20260923T165909Z-d54ccc.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 28 tokens (817313 cached reads)
- Output: 12637 tokens
- Cost: $1.3586815
- Wall-clock: 200s
- Model(s): claude-opus-4-8 ×1

<!-- garden-usage-end -->
