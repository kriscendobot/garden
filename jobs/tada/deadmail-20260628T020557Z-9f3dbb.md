Reply posted to issue #9. The job is complete.

---

## Completion report

**Job:** dead-lettered issue-inbox message for `issue-kriskowal-garden-9` — maintainer asked to evolve the XS stack-overflow instrumentation to show JavaScript function names interleaved with the C stack frames, corroborated against sources.

**What I did:**
1. **Evolved the engine instrumentation** (`xsnap/sources/xsnapPlatform.c`, `XS_STACK_OVERFLOW_EXIT` arm of `fxAbort`). The prior walk only had each frame's source path, so the widest frame printed `(anonymous):0`. It now names every frame via XS's own `fxBufferFrameName` (the routine the engine uses for `Error.stack`), tags each frame `J` (JS bytecode) vs `C` (host builtin, named by its C symbol via `dladdr` — the literal JS↔C interleave point), and names the per-wide-frame slot breakdown. Allocation-free, so still safe at exhaustion.
2. **Rebuilt** the instrumented release worker on-host and **re-ran** the real ymax0 `importBundle`.
3. **New finding:** the 2,588-slot wide frame that was previously anonymous is now named **`Array.prototype.flatMap`** (a host/C builtin), invoked from the SES `compartmentImportNow → execute` module-link path. The native C backtrace corroborates (the flatMap builtin between two `fxRunID` frames; `fxOnResolvedPromise` appears in both stacks). The frame is closure-dominated (~1,986 closures) — so the mechanism is a single `flatMap` materializing the bundle's module-linking accessor closures all at once, sharpening the prior "~2,000 module-scope closures" read. Controls held: trivial bundle imports clean (no spurious overflow).

**What changed (durable, on bot forks only):**
- `kriscendobot/xsnap-pub#2` branch `debug/xs-stack-overflow-trace` → HEAD `a8fb4ce` (the named/interleave instrumentation; also folded in the kind-histogram round that had never been committed back). Stays DRAFT.
- `kriscendobot/agoric-sdk#6` branch `debug/xs-stack-overflow-methodology` → `de03ef7` (new §4 documenting the capability + named result). Stays DRAFT.
- Replied on the issue: https://github.com/kriskowal/garden/issues/9#issuecomment-4824208254 (issue left open for the submitter to close).

**Scope:** read-only analysis + on-host build of the open-source XS engine and public npm packages; commits only to bot-owned forks. No upstream agoric-sdk push/PR/merge.

**Follow-ups (left for the maintainer):** the contract bundle is minified onto a couple of lines, so the exact `flatMap` call site needs a **sourcemapped re-bundle** (or stubbing the bundler's link helper) to pin — that's the remaining lever, and it points the fix at bundler output shape (emit sub-modules as their own functors) over contract/Endo source.
