# Completion report — endo-npm-cas-registry-press-20260725-045004

**Assessment (re-verified against live state, not the job spec's snapshot):** the design `designs/endor-npm-registry-proxy.md` now records **all five phases implemented** on `llm` HEAD (`e2c6ff853c`). Phase 2 (#276) merged 07-18; the Phase 4 resolver/assembly/execution PRs (#799, #800), Phase 5 offline+.npmrc (#803), and the follow-on execution work (#805 top-level await, #812 relative resolution, #818 CJS require linkage, #854 CJS named-export facades) are all merged. The registry-capability edge (#403/#671/#563) remains open under the byte-array arc but does not block this arc — Phases 1–5 landed without it.

**Finish-line verification (real execution, this tick):** built `endor` from `llm` HEAD (needed `git submodule update --init c/moddable` plus local placeholder files for the three xsnap JS bundles whose generators are absent from the tree — a pre-existing gap recorded in `rust/endo/README.md`; the `endor run` archive path never evaluates them). With a cold CAS + empty registry table and entry deps `p-limit@^4.0.0` + `semver@7.5.4`, `endor run main.js` resolved and fetched **5 packages** live from registry.npmjs.org (including the transitive CJS chain `semver`→`lru-cache`→`yallist` and transitive `yocto-queue`), stored them content-addressed, recorded them in `registry.db`, and executed in XS printing `p-limit squares: [1,4,9]` / `semver check: true 7` — no npm CLI, no node_modules, no lockfile. `endor run --offline` replayed identically with zero network, and a cold-state offline run failed with the clean typed refusal. The finish line **stands verified** for well-typed entries.

**Regression found and fixed (the press):** an **ambiguous `.js` entry** — package manifest without `"type": "module"`, the shape of every quick-start demo and of PR #800's own recorded evidence — was misrouted through the CJS wrapper and died with `SyntaxError: invalid import` at `__loadCjs`. This regressed with the CJS-linkage work (#818/#854 era). I implemented Node-style module-syntax detection (`detect_esm_syntax` in `rust/endo/src/cjs_lexer.rs`, reusing its tokenizer; wired into `normalize_to_esm` for ambiguous `.js` only), added 6 tests (176 lib tests pass, including the regression shape executed in an XS machine), verified the exact failing repro now passes live plus offline-replays, and opened **DRAFT PR #856** (`fix/endor-ambiguous-entry-esm-detection` → `llm`) with before/after evidence and the design-doc status note.

**Follow-ups for future ticks:**
- Shepherd #856 through review; it is draft per the press mandate.
- Remaining design gaps (unchanged, listed in the doc): `peerDependencies`/`optionalDependencies` handling, workspace-protocol resolution, private-registry auth beyond `.npmrc` tokens — `peerDependencies` is the most consequential next increment (real packages like `react` plugins need it).
- Top-level-await-only ambiguous entries (no import/export marker) are not detected — noted as deliberate scope in #856.
- The absent worker/SES-boot bundle generators (`rust/endo/README.md` gap 2) force every builder to stub three files before `cargo build` compiles; worth a small tracked fix on the daemon arc.
