Diagnosis complete and surfaced. No push was made — correctly, since the reds are not the PR's and pushing would pollute the draft or churn CI. Nothing to commit to the garden worktree (this was a diagnosis job). Writing the report.

---

## Shepherd report — kriscendobot/agoric-sdk PR #9

**PR:** #9 "prototype: promote ymax contract vat to critical at chain upgrade (garden#29)" — DRAFT, `MERGEABLE`/`UNSTABLE`, head `34c3a82`.

### What I found

Three red checks, **all base-staleness / environmental — none caused by the PR diff**:

| Check | Failure | Verdict |
|---|---|---|
| `test-codegen` | `packages/orchestration/src/fetched-chain-info.js` dirty after `yarn codegen` | Environmental. That file is generated by fetching the **live chain registry** (`@chain-registry/client`), so it's non-deterministic; it flip-flops pass/fail across sibling upstream PRs. Unchanged by this PR. |
| `test-quick (node-old)` | `# fail 3` in `packages/orchestration`, `# fail 1` ("upgrade not faked") in `packages/portfolio-deploy` | Both packages are **byte-identical to master** here — the PR doesn't touch them. |
| `test-quick (node-new)` | cancelled | Concurrency cancellation, not a real failure. |

### Root cause

PR #9's base is **503 commits behind upstream/master**. Upstream landed `88a5a67322 chore(orchestration): refresh fetched chain info` **after** this PR's merge-base — that commit is exactly the `test-codegen` drift fix, and it's absent from this PR's base. The orchestration/portfolio-deploy test-quick failures likewise reflect the stale base (those packages evolved substantially over the 503 commits).

Verified: `git merge-base HEAD origin/master` == current master, and `git diff merge-base..HEAD` touches **none** of orchestration / portfolio-deploy / deploy-script-support. The PR's actual diff is confined to SwingSet, cosmic-swingset, golang, and a3p-integration.

### PR-scope checks not yet in

The checks that actually exercise this PR's changes — `test-swingset`, `test-boot`, `test-cosmic-swingset`, `test-portfolio-contract` — were **still pending** at read time (run ~17 min in). Their verdict on the SwingSet/cosmic-swingset upgrade code is not yet available; a later CI-watcher tick will re-evaluate.

### Action taken

- **No push / no nudge commit / no cherry-pick** — the reds aren't the PR's, and touching the draft would pollute a SwingSet-focused prototype with an unrelated chain-info regeneration (that would re-drift anyway) or churn CI.
- Surfaced the diagnosis to the maintainer via the message bus (delivered to `inbox/maintainer`), with the rebase recommendation.

### Classification / next step

**`next: weaver`** (contingent on maintainer intent — `next: liaison`): the technical remedy is to **weave/rebase #9 onto current upstream/master**, which picks up the `fetched-chain-info` refresh and the evolved orchestration/portfolio packages. This is out of shepherd scope (a 503-commit rebase with likely conflicts in the churned SwingSet upgrade code) and design-consequential on a prototype draft, so it needs a maintainer decision rather than an auto-advance: rebase it green, or leave it as a known-stale-base prototype red.

**Follow-ups:** none from me. If the maintainer chooses to weave, the PR-scope checks (test-swingset/test-boot/test-cosmic-swingset) will re-run against fresh master and reveal whether the actual ymax-critical-promotion changes are sound.
