Added and pushed `designs/ai-sdk-garden-integration.md` (`a521665dad`).

It compares pi-ai, Pi harness, AI SDK Core, and HarnessAgent options; recommends a bounded read-only AI SDK Core pilot alongside pi-ai, with a future Endo adapter path. It identifies garden components, strict migration boundaries, phases, and open questions.

Verified: Mermaid diagram parses; `git diff --check` passed; working tree clean.

Follow-up: resolve AI SDK subscription-OAuth parity and hardened-JS compatibility before implementation.
