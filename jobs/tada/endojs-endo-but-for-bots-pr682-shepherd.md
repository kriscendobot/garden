Fixed PR #682’s zizmor failure by updating `actions/setup-node` pins to v6.5.0 in `5402b0554`.

All 27 CI checks are green on the new head. Posted the required PR summary comment.

Local install was blocked by `better-sqlite3` build failure; CI provided full successful verification.

Self-improvement: nothing this time.
