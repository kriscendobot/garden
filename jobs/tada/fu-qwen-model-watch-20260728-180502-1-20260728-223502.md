Weekly Qwen model watch complete. Report delivered to the maintainer (`inbox/maintainer` `20260728T223932Z-07a3b0`). No garden or host changes made — nothing to commit.

## What I did

Read the live state rather than any baseline, then probed the primary sources.

**Live local state** (as the job required, not hardcoded):
- Effective routing table via `set-model-routing.sh --show`: no journal `config/model-routing` override exists, so the tracked seed governs — `local  qwen*  qwen3:0.6b`.
- `ollama list` / `GET /api/tags`: **empty**. Daemon pid 210 (v0.31.2, restarted 2026-07-28T06:19:49Z) runs `User=ollama` with `OLLAMA_MODELS=/usr/share/ollama/.ollama/models`, which holds 12K.
- Cross-checked against fleet state before calling that a fault: `hermits: 0` on this host, no `garden-hermit@` units — the lane is deliberately parked under the 2026-07-27 maintainer directive. Known standing condition, so I raised no alarm.

**Sources:**
- `https://ollama.com/library/qwen3.7` → still **404, no flip**. Also 404: `qwen4`, `qwen3.7-preview`, `qwen3.6.5`, `qwen3.6-coder`, `qwen3.6-vl`, `qwen3-asr`.
- Newest official Qwen on Ollama remains **qwen3.6** (~1 month old; 35b/27b, 256K ctx).
- `https://huggingface.co/Qwen` — new since the 07-21 watch: **Qwen3-ASR-0.6B-hf**, **Qwen3-ASR-1.7B-hf** (~07-22). Speech recognition, no Ollama tag, not hermit-lane candidates. `Qwen-AgentWorld-35B-A3B` (Jun 25) unchanged and still tagless — already reported last week.
- Only "Qwen3.7" on HF is third-party `RscriptSQwen/Qwen3.7-plus`, not the Qwen org.

## Delta

**Upstream: nothing worth harnessing this week.** No routing change warranted.

**Local: the `qwen3:0.6b` pin is deliberate, not drift** — commit `c090912036` ("fix: use the exact local qwen tag", 20h ago) matches the leader's 07-25 workaround `ollama pull qwen3:0.6b && ollama cp qwen3:0.6b qwen3.6`. I verified that before reporting it, so it is not flagged as an error.

**The one likely-unknown finding:** a full **23 GB `qwen3.6:latest` store already exists on this box** at `/home/kris/garden2/.ollama/models` (pulled Jul 14, kris-owned) — the daemon simply doesn't read that path. So the prescribed re-pull may be unnecessary; an `ollama.service` drop-in pointing `OLLAMA_MODELS` there would restore real qwen3.6 (35B MoE, 256K ctx) instead of qwen3:0.6b (~9 months old, ~40× smaller). Both tags verified live upstream (registry manifest HEAD 200).

## Follow-ups (not acted on — maintainer's call)

1. Re-enabling the hermit lane and any `systemd` change are consequential host ops outside a watch job; left to the maintainer, with the models-dir option laid out.
2. Handler messaging in `codex-provider-common.sh` references `garden-ollama.service`, which **does not exist on this host** — the running unit is `ollama.service`. Operator guidance points at a non-existent unit.
3. The schedule body (`journal/schedules/qwen-model-watch.md`) still names `qwen3.6` as the served model and `qwen.ai/blog` as the source — both superseded by this job's prompt. Worth refreshing so next week's run doesn't inherit a stale baseline. I left it alone since the job scoped me to reporting.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/fu-qwen-model-watch-20260728-180502-1-20260728-223502.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 389 tokens (1385510 cached reads)
- Output: 17443 tokens
- Cost: $1.7378419999999994
- Wall-clock: 269s

<!-- garden-usage-end -->
