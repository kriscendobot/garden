Inbox empty. Work complete.

---

## Completion report

**Job:** Dead-lettered directive (intended for `issue-kriskowal-garden-9`), carried forward — kriskowal's comment on kriskowal/garden issue #9: *"Patch ses in agoric-sdk, replacing flatMap with an ordinary loop and verify that this addresses the problem… restore the stack size limit."*

**What I found (the directive's premise was inverted):**
- **ses has no wide `flatMap` to patch.** Every `flatMap`/`arrayFlatMap` in `ses@2.2.0` is over a 2–3 element array (compartment.js globalTransforms, make-safe-evaluator.js transforms, console formatting) — none on the per-module link path. The actual linkers of this bundle (`@endo/import-bundle`, `@endo/compartment-mapper` dist) have no runtime `flatMap` either; the only module-related ones (`@endo/module-source` babel-plugin) are build-time AST work.
- **I extracted the `bundle-ymax0.json` endoZip** — it contains exactly two files: `compartment-map.json` + one esbuild-flattened `portfolio.contract.bundle.js`. **No ses source in the bundle at all** (confirming the maintainer's "ses is not part of the contract bundle"), but the wide `flatMap` the instrumented trace caught is the inlined **`@agoric/internal/src/hex.js`** `decodings = new Map(encodings.flatMap(...))` (256×4 = 1024 entries) — which **is** part of the contract bundle.
- **Consequence (changes the maintainer's decision):** the `flatMap`→loop fix is a contract-dependency change shipped by rebundling — needing **neither a consensus `stackCount` change nor a platform-ses upgrade**. Strictly cheaper than both levers previously weighed.

**What I verified:**
- Wrote and ran the loop replacement on V8: produces a **byte-identical** `decodings` Map (484 entries, round-trips ff/0A/aB), and never materializes the 1024 transient pair-arrays.
- **Analytical verification at restored stockCount=4096**, grounded in the prior instrumented histogram: the two co-resident frames were ~2,575 (persistent functor scope) + ~1,247 (the hex.js `flatMap` operands) ≈ 4096; the loop removes the ~1,247-slot frame, dropping the peak to ≈2,575 — ~12× the ~128-slot deficit. Import expected to complete at stock 4096.

**What changed / outputs:**
- Posted the full finding as a reply on issue #9 (comment-4824906011); issue left open (submitter closes).
- Posted a scoped follow-up job `verify-ymax0-hexjs-flatmap-loop-xs-import` to the board for the empirical XS rerun (rebundle with the hex.js loop, restore stockCount=4096, drive `importBundle` under the from-source xsnap supervisor, confirm exit 0), carrying the verbatim ISSUE NOTE, scoped to the bot fork `kriscendobot/agoric-sdk` only.

**Follow-ups:** the empirical XS import (job above) is the one remaining piece — it needs the from-source worker + supervisor harness re-wired (prior driver was redeploy-wiped; engine binary still cached). No agoric-sdk pushes/PRs/merges were made.
