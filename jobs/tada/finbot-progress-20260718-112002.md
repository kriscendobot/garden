Advanced `kriscendobot/finbot` main: `29d4683 -> 4d0e902`.

Implemented `auto-egarch`: evidence-gated per-asset GARCH/EGARCH selection, routed through forecasts, analyzer/auditor regime reads, and `finbot-ooda --adaptive-vol=auto-egarch`.

Verified with `npm test` (exit 0) and single/multi dry-run OODA cycles, both auditor-approved with `WALLET TOUCHED: false`.

Maintainer notified. Next step: walk-forward OOS evaluation harness with QLIKE and naive baselines.
