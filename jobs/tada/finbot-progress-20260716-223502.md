Inbox empty. Work is complete, pushed, verified green, and reported.

## Completion report

**Cycle: asymmetric (leverage) MLE — one increment.**

**Assessment.** All prior `finbot-*` jobs are in `tada/`; no finbot work is in-flight and there are no open PRs (work lands directly on `main`, currently was `efa6454`). The volatility-regime *decision* loop is already closed (analyzer scoring, forecaster horizon, auditor tail floor, position sizing all key off fitted GARCH persistence). The deepest **unblocked** deferral on the forecasting-model axis — named as "the natural next refinement" in the code and deferred every cycle as *gamma/asymmetric MLE* — was live estimation of the GJR-GARCH leverage parameter.

**Advanced.** `main` `efa6454 → df2a164`:
- Added `gjrGarchMleFromPriceHistory` (`packages/simulator/gjr-garch.js`): fits **(alpha, gamma, beta)** per asset from the realized down/up asymmetry via the same deterministic nested-grid MLE as the symmetric fitter, variance-targeting `omega` out of the search (no RNG, no optimizer lib, byte-identical for identical input). gamma is bounded non-negative; short-window/constant-price inputs fall back to the fixed split.
- Routed `makeVolSurface({ kind: 'gjr-garch', history, estimate: 'mle' })` to it (previously a "deferred" throw) and exported it from the simulator index.
- New `packages/simulator/test/gjr-garch-mle.test.js` proves recovery (leverage DGP → gamma > 0.05 with downWeight > upWeight; symmetric DGP through the same seed → gamma < 0.05), determinism, fallbacks, and factory routing; replaced the old "gjr MLE throws" case with a routing assertion; updated the design doc.

**Verified.** Full suite **538 pass / 0 fail** (was 528); `finbot-ooda --seed=7` green with **WALLET TOUCHED: false**. Repo has no CI workflows, so nothing to shepherd; tree left green. Reported to the maintainer inbox.

**Next unblocked step (handed off).** `conditionalVolFromPriceHistory` still rolls the *symmetric* surface even when the world is GJR — a `gjr-mle` roll-forward would let the auditor's tail floor and analyzer's sizing read *down-move-conditional* vol. Deferred behind that: EGARCH, implied-vol surfaces, PNG rasterization, far-ref vending.

**Maintainer decision pending (unchanged).** Live execution remains blocked on explicit paper-wallet/test-net authorization and a chosen CapTP transport.
