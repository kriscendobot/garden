Inbox empty. Both increments are landed, verified, and the PR remains DRAFT with no comments.

## Completion report

**Job:** stage10e child 2/3 — live daemon worker-evaluate round trip on the Rust engine (PR #600, `endojs/endo-but-for-bots`, branch `xs2rust-endor`, kept DRAFT, no PR comments).

**Binding precondition gate: RED → job DEGRADED to a worker-bundle gap round (as the spec directs).** At my tip (`be4a8af13b`, the real remote head, ahead of the cut `cc158e5ff3`), the frontier marker `boot_drives_the_real_chain_to_the_worker_bundle_frontier` showed `halted_at == Some(("worker_bootstrap", Unsupported("instantiate")))`, `handle_command_registered == false`. The bundle still halts at a named frontier, so per the spec I did **not** attempt the daemon round trip — I continued push-per-gap from the exact frontier. (Seeded `rust/engine/target`, root `target`, generated bundles, and `c/moddable` @ pin `23b4d6b0` via `cp -al` from the same-commit sibling `stage10e-worker-gaps`; confirmed sha + clean status first.)

**Closed 2 worker-bundle frontier gaps, push-per-gap** (each: oracle-grounded dual-run snippets → implement → gate tests → promote marker → full bars → push):

1. **`98333bf528` — the `instantiate` opcode (`XS_CODE_INSTANTIATE` / `fxRunInstantiate`).** Not the module machinery an earlier note guessed — it is the object-literal `{ __proto__: v, ... }` prototype initializer the SES/@endo bundle uses for its `{ __proto__: null }` records. `op_instantiate` replaces the stack-top `__proto__` value with a fresh instance whose `[[Prototype]]` is `null` / the object reference / %Object.prototype% (primitive ignored). Per-branch built-in metering calibrated **bit-exact** against the C-XS oracle (reference path 0 `mxMeterOne`, null/primitive paths 2). Gate: `object_proto_instantiate.rs` (6 tests, result + computron agreement). Frontier promoted `instantiate` → `harden:exotic-object`.

2. **`5e26986bd3` — `harden` a RegExp in the primordial graph.** SES `harden`'s deep-freeze reached a RegExp, which endor honest-skipped. A RegExp is the one exotic shape whose freeze reduces exactly to the ordinary integrity treatment with a **referent-complete** traversal (its `lastIndex` is an ordinary slot; no out-of-band reference-bearing state — unlike arrays/collections). `harden_freeze_and_traverse` now treats a RegExp as ordinary; every other exotic class keeps the honest skip. Gate: `hardened_javascript.rs § harden_freezes_a_regexp_in_the_graph` (result agreement vs native `harden`: identity, `isFrozen`, effective non-writable `lastIndex`, transitive graph freeze, `RegExp.prototype` reachable). Frontier promoted `harden:exotic-object` → `fill:non-dense-array`.

**Bars green before every push** (outputs to files, `$?` checked): engine workspace `cargo test --workspace` EXIT=0, 0-failed (769 passed / 54 result lines at final); compile-diff 1909/1909 + SYMB 1909/1909; boot gate 30/30; ROOT `cargo test -p endo --lib` 86/0; zero new Rust warnings (only pre-existing C-oracle `-Wclobbered`/`-Wstrict-aliasing` in `c/moddable`); forbid intact (7 roots, endor-oracle exempt); VARIANT_COUNT 35 (no new side table); `c/moddable` @ pin `23b4d6b0`, clean, never staged; no committed bundles. `local == remote` verified at `5e26986bd3` by git exit code. PR #600 still DRAFT/OPEN, no comments.

**Why I stopped at two (honest increment over deadline-death):** the new frontier `Unsupported("fill:non-dense-array")` is `Array.prototype.fill` over a holey array — an array-internals gap with data-dependent chunk-realloc metering (`fxSetIndexSize` + `mxMeterSome(5)` per element), materially more involved and higher fidelity-risk than the two clean opcode/exotic closes. Three prior live-captp children died at deadline over-reaching; two verified, landed capability increments is honest success.

**Resume point (next child):** frontier `Unsupported("fill:non-dense-array")` — `Array.prototype.fill` over a non-dense array (endor's `dense_array_this` returns `None`). The gate remains RED (bundle still halts before registering `handleCommand`), so the daemon `error-trace.test.js` round trip stays blocked until the bundle boots to completion; the next child should continue push-per-gap from this frontier.

**Deliberately not gated (accuracy over parity; endor runs both correctly by value):** a multi-own-property object with a custom prototype (`{ __proto__: p, b, c }`) has a tiny pre-existing 8-raw property-slot meter delta unrelated to `instantiate`; `Object.isExtensible`/`harden` over other exotic classes (array, collection) remain separate named frontiers.
