Press dispatch complete — M3 posture verified and unchanged (all gates still closed awaiting the maintainer), plus one infra incident found, part-healed, and routed to the board.

**M3 git-integration stack — verified 2026-07-21T15:2xZ via `gh pr view --json` against endojs/endo-but-for-bots:**
- **#705** (Phase 1, remote push tier): OPEN, un-drafted, `mergeStateStatus: CLEAN`, head `84f68180` unchanged, **24/24 checks SUCCESS**. The merge ask (liaison msg `20260717T002451Z-cb5a1b`) is **still in `inbox/maintainer/unread/`** — gate closed; did not merge, did not re-send (foreman escalation `20260721T041300Z-c395c1` also sits unread).
- **#707** (Phase 3, worked loop / M3 exit criterion): OPEN, un-drafted, CLEAN, head `a0f4eca4`, **23/23 SUCCESS**; base still the frozen snapshot, left un-refrozen per the standing benign-delta verification.
- **#708**: CLEAN, head `ce58ad49`, **26/26 SUCCESS** (guile-interop included this pass). **#740**: CLEAN, `446f8da4`, 5/5. **#691**: CLEAN, `36c1fc49`, 5/5. **#626**: still DRAFT as required, CLEAN, head `8e29c292`, 22/22.
- No PR comments since 2026-07-20 on #705/#707/#691/#740 — no directive anywhere. No stack action taken; every next motion (merge #705 → weave #707 → merge #707 closes M3) remains maintainer-gated.

**Infra incident (found while draining my inbox):** `inbox-read.sh` failed because `journal_remote()` resolved to `endojs/endo-but-for-bots` — the deployed root's `remote.origin.url` had been rewritten to the project repo, and the `.garden-state/config/journal-remote` cache was poisoned with it, breaking every *fresh* doer clone. Forensics: a native-git test scenario leaked its cwd into the deployed root on 2026-07-17T11:38Z (fixture commits touching only `tmp/native-git-TdaKeS/conflict.txt`; root HEAD left on fixture branch `feature`), and workers have been misusing the root as a project checkout (`xs2rust-endor` branch created there today 11:23Z). No garden branch was ever pushed to the project repo (`ls-remote` shows no `main2`/`journal2` there). The origin URL was already re-healed by a fleet keeper by the time I wrote it; I normalized it and the cache to `git@github.com:kriskowal/garden.git` and verified end-to-end: `journal_remote()` now prints the garden URL, `git -C journal fetch origin journal2` succeeds (`fb3986ef47`), and `inbox-read.sh` runs clean with no override.

**Changes made:** restored/normalized root `remote.origin.url` + journal-remote cache (config only — no tree, HEAD, or branch mutations in the deployed root); posted board job **`fix-garden-root-test-leak-cleanup`** covering the remaining damage (root HEAD off `feature` via the deliberate-deploy flow, fixture-branch/junk sweep, and a common.sh-style guard against root-cwd test leaks); sent the maintainer one incident note (`20260721T152846Z-917ee4`), without repeating the #705 merge ask.

**Follow-ups:** the cleanup job on the board; the #705 merge directive remains the sole blocker to closing M3.
