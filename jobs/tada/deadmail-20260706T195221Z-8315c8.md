Durably landed on `journal2` and pushed. The carry-forward is complete — no `main2` code change is involved (this is a message-bus delivery, not garden development).

---

**Completion report**

**Job:** `deadmail-20260706T195221Z-8315c8` — pick up the intent of a dead-lettered message.

**What the message was:** A completion/scope-fold *report* from the still-live stage-4b child `xs2rust-endor-stage4-async-surface` (child 2/5 of the serial `xs2rust-endor-build-stage4b` orchestration), addressed to the supervisor `port-xs-to-rust-memory-safe-engine-s10`. It reports the async-function surface LANDED bit-exact on PR #600 (branch `xs2rust-endor`, kept DRAFT) and carries three honest scope folds — most importantly recommending **Promise.prototype.finally + the combinators (all/race/allSettled/any)** as "the single highest-value remaining child" on the now-built 5-slot native-reaction substrate.

**Why it dead-lettered:** `s10` is a *parked* supervisor (`gate: blocked`, `blocked_on: xs2rust-endor-build-stage4b`), so it has no live `inbox/` — `inbox-send` correctly dead-lettered the child's report rather than dropping it. This was a **reply to that doer**, so the task was to carry the reply forward, not to execute a request.

**What I did:** Deposited the report on `s10`'s **durable** message topic via `send-msg.sh job/port-xs-to-rust-memory-safe-engine-s10` → `journal/msgs/job/port-xs-to-rust-memory-safe-engine-s10/20260706T200110Z-2d4fae.md` (verified landed and pushed to `origin/journal2`). This is the same durable directed channel the liaison used to reach supervisor `s6` (the `msgs/job/port-xs-to-rust-memory-safe-engine-s6/` precedent); unlike the live-only `inbox/`, it survives the parked→live transition, so when `s10` is promoted (once stage4b reaches `tada/`) it drains this report and receives the scope-fold intelligence. I prefixed a clear framing note so `s10` knows it's a carried-forward dead-letter and must verify the child's claims at stage-4 acceptance per its own spec.

**What I deliberately did NOT do:**
- Did **not** re-`inbox-send` to `s10` (still parked → would just dead-letter again in a loop).
- Did **not** post the recommended Promise.finally/combinators child myself — the `s10` spec is explicit that the supervisor owns decomposition/sequencing; stage4b is serial and still running (compartment, lockdown-harden, ses-conformance pending). Carrying the recommendation forward to `s10` is correct; usurping its dispatch would create an unowned, unorchestrated child.
- Did **not** touch the child's `tada/` report (the child is still live on gardener 3 and owns it) or edit the `s10` plan body.
- No `main2` change — nothing to commit; this is a message-bus carry-forward.

**Follow-ups / notes:** When `s10` wakes it will read both the async-surface child's eventual `tada/` report and this durable topic message; any overlap is harmless redundancy. The actionable item for `s10` is the folded **Promise.finally + combinators** next-child recommendation.
