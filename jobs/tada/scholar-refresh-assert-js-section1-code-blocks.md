Refreshed the five stale quoted `assert.js` blocks: `declassifiers`, `canBeBare`, `bare`, `redactedDetails`, and `unredactedDetails` now match `endojs/endo` commit `0594e99f` verbatim.

Corrected the `redactedDetails` explanation to describe its deferred `{ template, args }` storage and rendering. Kept all in-text line citations and `source_lines` frontmatter unchanged.

Landed the section through `land-journal-edit.sh`. Verified with `library-link-check.sh --changed` (passed); topic counts and both projected indexes were already current. No follow-ups.

Self-improvement: supplied line ranges support a focused verbatim refresh plus the standard integrity gate.
<<<GARDEN-JOB-COMPLETE>>>
