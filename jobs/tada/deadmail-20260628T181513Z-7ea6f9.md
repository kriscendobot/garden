The deploy is now running detached: it has engaged the drain and is waiting for the fleet to quiesce. It will survive my exit and complete once the mid-job gardeners (including me, once this job ends) finish. I'll finish here so my own busy marker clears and the deploy can proceed.

---

**Completion report**

**Intent:** A dead-lettered message from the maintainer (from_host `github-pages`) addressed to a since-completed `liaison` said simply "Please redeploy." I carried that intent forward as the deliberate redeploy of this host (`endolinbot`).

**What I found:**
- Root checkout HEAD was `fe2d9f2` (deployed version); `origin/main2` had advanced to `86157f3` — so a redeploy was meaningful (root was behind by real development).
- The deployed-sha marker (`.garden-state/deploy/deployed-sha`) was **absent** (no baseline ever recorded), and the `.garden-state/deploy/` directory **did not exist**. Consequently the `garden-upgrade-monitor`'s "Upgrade ready" signal was silent — with no baseline it had nothing to compare, so the liaison's deploy-on-upgrade Monitor would never have auto-fired. The maintainer's manual "please redeploy" was effectively the only path until a baseline exists.

**What I did:**
- Created the missing `.garden-state/deploy/` directory.
- Launched `scripts/jobs/deploy-garden.sh` detached (background, `nohup`) rather than foreground — I am gardener 47 holding my own busy marker, so a synchronous deploy would have blocked forever waiting for me to quiesce. It immediately engaged the drain and is waiting on 3 mid-job gardeners; it logs to `.garden-state/deploy/last-deploy.log`.
- The deploy will: drain → quiesce → fast-forward root `fe2d9f2 → 86157f3` → **record the first deployed-sha** → lift drain → restart the long-running services and gardener fleet onto the new code.

**Follow-ups:**
1. **Verify the deploy finished cleanly** — check `.garden-state/deploy/last-deploy.log` for the "deployed sha recorded / drain lifted / fleet restarted" lines once the fleet quiesces (it can wait up to the 600s timeout for gardeners 1/14/42 plus me to exit).
2. **Missing `deploy/` dir + absent deployed-sha baseline is a real gap** — fresh hosts apparently start with no `.garden-state/deploy/` directory, so the upgrade monitor stays silent and auto-deploy never arms until a first manual deploy. Worth hardening: have install/bootstrap (or `deploy-garden.sh` itself, which now does) `mkdir -p` the deploy dir and the upgrade monitor seed a baseline. I'd post this as a garden-infra job if it isn't already tracked.
