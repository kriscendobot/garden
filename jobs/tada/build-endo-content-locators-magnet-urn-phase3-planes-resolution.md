Implemented and pushed Phase 3. Draft PR: #789.

- Added per-agent empty `@planes`, content-plane registry/resolution plumbing, and source-hint tests.
- Added `@endo/daemon` changeset.
- Verified: TypeScript check, ESLint (no errors), focused registry AVA test.
- Daemon-backed test is included but not runnable here because the worktree path exceeds Unix socket limits.

Self-improvement: nothing this time.
