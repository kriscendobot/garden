Verified the final state. Here's my completion report.

---

## Completion report — deadmail-20260706T133039Z-fa81df

**Job:** A dead-lettered message — child-3 (`xs2rust-endor-stage4-generators`) of the `xs2rust-endor-build-stage4` orchestration sent its COMPLETE + scope-fold report to supervisor `port-xs-to-rust-memory-safe-engine-s9`, which dead-lettered. I was to carry that intent forward.

**Diagnosis (root cause):** `s9` had not "completed" — it is **parked** (`jobs/plan/port-xs-to-rust-memory-safe-engine-s9.md`, `blocked_on: xs2rust-endor-build-stage4`). A parked supervisor has no live inbox, so `inbox-send` correctly dead-lettered. This is structural: all eight stage-4 children reporting scope folds to a parked supervisor will dead-letter the same way. The supervisor's own spec accounts for this — when promoted (once the orchestration reaches `tada/`), it is directed to read `jobs/tada/xs2rust-endor-build-stage4.md` **and each of the eight children's tada reports** before draining its inbox. The **tada report is the durable channel**, not the inbox.

**What I found / verified:** By the time I acted, the generators child had completed. Its tada report `jobs/tada/xs2rust-endor-stage4-generators.md` **already carries the full ledger** from the dead-lettered message verbatim in substance: landed surface (SavedFrame side table, GENERATOR_FUNCTION/START_GENERATOR/YIELD/BRANCH_STATUS_{1,2,4}, %GeneratorPrototype% next/return, for-of/spread), the frozen metering constants (FUNCTION_EXTRA=24, START=1136, YIELD=32616, RESULT=66304, RESUME=65536), dual-run tallies (divergent=0), the complete **scope fold** (yield\*, throw/return-into-suspended + finally unwinding, yield-in-live-try, new-constructed generators, async generators→child 4), and the advisory yield-metering residual note. The child itself recorded: *"Supervisor inbox was gone; the completion message was dead-lettered... (intent preserved)."*

**Conclusion:** The intent is **already fully preserved** through the exact channel `s9` reads on promotion. No re-delivery was needed; pre-seeding the parked supervisor's inbox would have been a non-standard hack (and would have altered the other children's dead-letter behavior), so I did not do it.

**Changes made:**
- No garden-dev (main2) code changes — this was a coordination/investigation job.
- Journal: retired one redundant dead-letter I generated. (Mid-investigation I sent an informational relay to the generators child to ensure its tada report carried the ledger; it had just completed, so my relay also dead-lettered. Since its content was already moot — the tada report was complete — I CAS-retired that queued dead-letter (`inbox/dead/20260706T133852Z-764cda.md`) on `journal2` to avoid spawning a needless job. Retirement is race-safe: losing to the deadmail service would simply have promoted it, same as inaction.)

**Follow-up (advisory, for the maintainer/liaison — not blocking):** Children instructed to report to a *parked* supervisor's inbox will always dead-letter, generating a deadmail job per child that resolves to "the tada report already has it." Two clean options if the churn is unwanted: (a) have children report scope folds **only** via their tada reports (which s9 already reads), dropping the inbox-send; or (b) have deadmail recognize an addressee that exists as a parked `plan/` job and re-queue the message to land when that job is promoted, rather than promoting a standalone job. Today the redundancy is benign because s9's spec reads the tada reports regardless.
