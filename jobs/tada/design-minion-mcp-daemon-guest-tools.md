Draft PR #13 is confirmed open against `main`, inbox is empty. The job's definition of done is met.

# Completion report

**Design slug:** `designs/mcp-daemon-guest-tools.md` · **Branch:** `design/mcp-daemon-guest-tools` · **Draft PR:** [kriscendobot/minion.town#13](https://github.com/kriscendobot/minion.town/pull/13), citing the kriskowal/garden#58 directive comment.

**What I did.** Grounded live against both repos before drafting: the toy server (`src/server.ts`), the per-session `McpServer` construction in `src/http.ts`, the existing `src/endo/` module (identity derivation, root control, the still-throwing Gate-2 stub in `root-ctl.ts`), the deployed daemon topology (`deploy/aws/daemon/README.md`), and `endojs/endo-but-for-bots @ llm` HEAD (`f6650503`) — the real `EndoGuest` method surface, `provideGuest`/`provideHost`, `makeEndoClient` → `E(bootstrap).host()`, `@endo/gateway` still phase-1, no `@endo/mcp`. Then wrote the design as the build-organizing successor to `mcp-endo-guest.md` (cross-linked; carries forward rather than duplicates), matching the repo's design conventions; both mermaid diagrams parse-validated (`OK flowchart-v2`, `OK sequence`).

**Key decisions the design lands.** (1) Gate re-ordering — the stall's root cause: Gate 1 (Claude-as-OAuth-client, needs a human) no longer blocks daemon work; every increment validates with the repo's own PKCE client against the deployed edge. (2) Daemon substrate: a new `endo-daemon.service` at a pinned `llm` commit — not the OCapN demo container, whose build commit I verified is **not an ancestor of `llm`**. (3) Client dependency: the daemon's `workspace:^` manifest blocks a git dep, so a thin ported netstring-CapTP client first, with `@endo/mcp` extraction as the maintainer-gated destination; the `@endo/*` vs. config-layer split is stated. (4) Tool surface: the carried `guest_*` table verified method-by-method (including switching `guest_read_text` to `maybeReadText`), per-session facet-derived tool mounting, and a new load-bearing session-identity-pinning requirement. (5) Build decomposition B0–B6, serial, halt-on-failure, each with a named validation hook — executable by a builder without further design clarification.

**Follow-ups (for the liaison/maintainer, not blockers):** review the draft PR; after merge, an orchestration job parking B1–B6 as sequenced children is the intended next step (the design specifies the order and failure policy); open question #1 asks the maintainer to ack `@endo/mcp` extraction timing (B6) when B5 lands.
