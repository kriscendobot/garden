Updated PR #722 at `a4859f575c` with the requested base/unconfined `@endo/fetch` and stateful/confined `@endo/confined-fetch` split. Rebasing, related design/index updates, PR body/title, review-specific summary, and re-request of `kriskowal` review are complete.

Validation: Prettier and `git diff --check` passed; all six PR CI checks are green.

Follow-up: PR #723 still implements the old single-plugin design and must be aligned separately; it was not modified.
