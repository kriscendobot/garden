Advanced https://github.com/kriscendobot/finbot/pull/1 to commit `3b74dfe`.

- `spawn()` now defaults to the hardened SES role policy. The permissive attenuator is an explicit opt-in, and `@finbot/harness` now declares its direct `ses` dependency.
- Added coverage for the hardened policy and requested tool slice. The PR description now accurately distinguishes the current policy from a future archive-backed Compartment loader.
- Verification run: `npm test` passed. `npm run finbot-ooda -- --seed=7` completed an auditor-approved dry run with `WALLET TOUCHED: false`.

Next unblocked step: review and merge https://github.com/kriscendobot/finbot/pull/1. The subsequent design increment is an archive-backed `@endo/compartment-mapper` loader for locally executed role code. No live-wallet decision is needed for this change.

Maintainer notified through the inbox.

Self-improvement: nothing this time.
