I now have the complete picture. Let me record my analysis.

The dead-lettered message was child 1/5's **completion report** (`xs2rust-endor-stage5-fix-cesu8`) addressed to the supervisor `port-xs-to-rust-memory-safe-engine-s13`. It dead-lettered because s13 has no live inbox — but not because s13 *finished*: s13 is **parked in `jobs/plan/`, `blocked_on: xs2rust-endor-build-stage5-fix`**, waiting to be promoted when that orchestration terminates. It was simply never alive to receive the message.

The message is a **reply/report to a doer**, not a request. Carrying it forward means ensuring its intent — informing s13 that child 1 is done — reaches s13. That intent is **already durably preserved** and needs no re-delivery:

- The full report content lives in `jobs/tada/xs2rust-endor-stage5-fix-cesu8.md` (commit `8c9c86924`).
- s13's own spec directs it to read exactly that: *"read the orchestration's completion/progress record and `journal/jobs/tada/xs2rust-endor-stage5-fix-*.md` … and drain your inbox."* The `tada/` records are the authoritative source; the inbox message was a best-effort courtesy.
- The orchestration is healthy: `state: running`, serial; child 1 is in `tada/`, and the deterministic leader-only `garden-orchestrate` watcher will promote child 2 (`-stage5-fix-rejects`) next.

Re-sending to s13's inbox would be **actively harmful**: s13 is parked with no inbox, so the send would dead-letter again into an infinite loop. Manually promoting children or interfering with the running orchestration would risk double-promotion against the watcher. Independently verifying the pushed commit is s13's acceptance-review job, not this pickup's scope.

So the correct disposition is: confirm the intent is carried, take no re-delivery or orchestration action, and complete. No garden files change; nothing to commit.

<<<REPORT>>>

**Dead-mail pickup — `deadmail-20260707T104253Z-8c2c23`**

**Intended recipient:** `port-xs-to-rust-memory-safe-engine-s13` (Fable supervisor, XS→Rust Endor port).
**Message:** child 1/5 `xs2rust-endor-stage5-fix-cesu8`'s DONE report (CESU-8 string-literal fold closed; commit `23ca8ac79c` on `xs2rust-endor`; gate `divergent 60→0`; workspace green; PR #600 still DRAFT).

**What I did:** Investigated the board/journal state rather than blindly re-delivering. Findings:
- s13 has **not completed** — it is parked in `jobs/plan/`, `blocked_on: xs2rust-endor-build-stage5-fix`. It had no inbox yet, which is why the report dead-lettered.
- The report's intent is **already durably preserved** in `jobs/tada/xs2rust-endor-stage5-fix-cesu8.md`, the authoritative record s13's own spec instructs it to read when promoted. Nothing is lost.
- The orchestration `xs2rust-endor-build-stage5-fix` is `state: running`, serial/halt; child 1 in `tada/`, children 2–5 parked, awaiting the deterministic `garden-orchestrate` watcher to promote child 2. Healthy and progressing.

**Action taken:** None beyond verification, deliberately. Re-sending to s13's (nonexistent) inbox would dead-letter again into a loop; manually promoting children would race the orchestrate watcher; independent verification of the commit is s13's acceptance-review job. The intent is carried forward by the existing `tada/` record.

**What changed:** Nothing — no garden files, no commits, no pushes. This was a status-report reply whose payload is already at its destination.

**Follow-ups:**
- Expect **4 more dead-mail jobs of this exact shape** — children 2–5 will each report to s13's inbox while s13 is still parked, so each report will dead-letter identically. Each is a harmless no-op for the same reason (payload lands in `jobs/tada/`). If this recurring dead-lettering is noise the maintainer wants suppressed, the fix would be in the orchestration/messaging design (e.g. children skip the inbox courtesy-send when the supervisor is parked, or the dead-mail promoter recognizes "recipient parked in plan/, payload in tada/" and self-resolves) — flagging for the liaison/maintainer, not actioning it here.
- No action needed on the port program itself; s13 will run stage-5 acceptance when the orchestration terminates.
