Revised `designs/mcp-endo-guest.md` on `kriscendobot/minion.town` and pushed direct-to-`main` as commit `35e9b4a` (revising `c92a66c`'s design per the 2026-07-09 maintainer directive). Design-only, no PR, per the repo convention; 38 insertions / 19 deletions, all confined to the one design file.

What changed:

1. **Directive of record added to the front matter** — OAuth scopes are not the access-control surface; real authorization is the object capabilities behind the tool calls, with the live 2026-07-09 validation (fresh baseline identity limited to `minion_status`) cited as the placeholder-grade auth layer working as intended.
2. **§ 4.4 rewritten as the two-layer model** ("The tool surface, and where authorization lives"): a thin outer auth layer (the single `mcp/guest` scope ∩ policy — authenticates and admits, authorizes nothing per-action) versus the authoritative per-action control (the capability **facet** composed at provisioning, exercised by tool implementations; denial is the absence of the capability). Added an explicit decision-by-decision reconciliation table (who's calling / admitted at all / act-as-this-guest / this-action-on-this-object / resource spend → which layer and mechanism). The tool table's "Scope" column became "Exercises (cap in the granted facet)", and the proposed `mcp/guest:eval` scope is explicitly retired unminted — `guest_eval` mounts only on an explicit capability grant, and the store-ladder invariant is restated in stronger form (dangerous authority is never scope-shaped at all).
3. **Gate 3 (§ 5) gained a "Grant — provisioning is the authorization act" bullet**: the one site that `provideGuest`s also composes the facet (baseline = intrinsic guest powers only; evaluator/`@fs`/worker widenings are explicit grants there and nowhere else), and suspension is reframed as correctly thin-auth (an admission question, no capability surgery).
4. **§ 8 metering seam reshaped ocap**: payment attenuation arrives as membranes wrapped around the same caps the § 5 grant site composes — the attenuator `debit()`s the reserved ledger shape and refuses by throwing when credits run dry; the earlier per-tool `authorize()` wrapper is subsumed (authorization to spend *is* the funded cap). Still deferred pending X7 data.
5. **§ 9 open question resolved**: `mcp/guest` survives as the single coarse admission scope (baseline grant is safe because it authorizes nothing per-action); the `guest_eval` question is restated in capability terms (recommend: evaluator not in the baseline facet, explicit admin grants for the § 7 exercises until metering prices it).

Everything else — daemon unit, deploy script, CapTP-over-UDS control path, `iss+sub` guest keying, gates 1/4, impedance exercises — is intact, with only two consistency touches (V4's parenthetical now notes Gate-4 denials are capability-shaped, and the § 4.3 authority-model bullet notes tool implementations close over the granted facet, never raw host powers).

Follow-ups: none required by this job. The referenced memories (`minion-town-access-control-ocap`, `endo-gateway-mcp-direction`) were not present in this instance's memory store; the job spec carried the directive in full, so nothing blocked. Inbox drained (empty).
