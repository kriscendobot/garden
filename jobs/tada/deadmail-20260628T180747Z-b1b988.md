Inbox empty. Work complete.

---

## Completion report

**Job:** dead-lettered message for `issue-kriskowal-garden-10` — maintainer kriskowal commented on garden issue #10: *"Consider using exponential back-off with full jitter, generally."*

**What I did**

Read "generally" as a directive to apply the canonical AWS exponential-backoff-with-full-jitter recipe everywhere the garden retries a contended compare-and-swap, not only the one spot the maintainer hit. Developed in an isolated worktree off `origin/main2`; landed as commit **`63b1837ab`** on `main2`.

**What changed (27 files)**
- `scripts/jobs/common.sh` `backoff()` — was a flat `~50–300ms` randomized sleep; now `backoff [attempt]` sleeps a fresh uniform draw in `[0, min(2000ms, 50ms·2^(attempt-1))]`, tunable via `GARDEN_BACKOFF_BASE_MS`/`_CAP_MS`, overflow-guarded. A bare `backoff` stays a small attempt-1 pause (back-compatible).
- ~25 retry-loop call sites now pass their 1-based attempt counter (`post-job`, `claim-job`, `complete-job`, `inbox-send`/`-read`, `send-msg`, `scheduler`, `reaper`, `proxy`, `set-main-host`, `clone-keeper`, `mirror-closer`, the journal-fetch + clone-lock + reap-now loops in `common.sh`, and the rest).
- `docs/bulletin/github.js` `commitReply()` — the reply-CAS retry the maintainer saw throw `422` now uses the same full-jitter growth keyed on its (0-based) attempt; `docs/bulletin/DESIGN.md` §3 updated.

**Verification**
- Full suite: `237 passed, 4 failed`. The 4 failures are **pre-existing on the clean baseline** (`86157f371`, verified by re-running it) — an unrelated comment-source PR-exclusion test emitting empty output; my change adds zero new failures.
- Retry-path suites green: `fetch-timeout` (10/10), `clone-keeper` (13/13), `main-host` (39/39), `land-journal-edit` (18/18).
- `bash -n` clean on all 25 modified scripts; `node --check` clean on `github.js`; functional check confirmed the window grows and caps correctly in both shell and JS.
- Note: the 2 failures in my first test run were purely environmental — the default worktree landed on `/tmp` (`noexec`), so child scripts couldn't exec; re-running from an exec-capable home worktree gave all-green.

**Replied** on the issue thread: https://github.com/kriskowal/garden/issues/10#issuecomment-4826993633 (left the issue open for the submitter to close). The bulletin change deploys with the next Pages publish from `main2`/`docs`.

**Follow-ups**
- Pre-existing, unrelated: the comment-source "PR-exclusion / comment row-join" subtests (`run-test.sh` ~L2101–2103) fail with empty output despite `jq` being present — possibly a sandbox `gh`/fixture artifact. Not diagnosed here; worth a dedicated look since it predates this change.
