Checked nodejs/cjs-module-lexer upstream main against 2.2.0.

Result: no new commits. Both upstream main and tag 2.2.0 resolve to cb772facaab04a64deced6161b5c0efec683189f (2026-01-02).

Reported this result on https://github.com/kriskowal/garden/issues/55#issuecomment-4996466888. The issue remains open. No upstream issue or pull request was opened or modified.

Verification: cloned upstream and ran `git log 2.2.0..origin/main`, which produced no commits; `git show -s` returned the same SHA for both refs.

Self-improvement: nothing this time.
