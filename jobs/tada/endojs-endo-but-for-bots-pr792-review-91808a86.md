Implemented all three review items in `453a9ffbf6` and pushed `build/endo-content-locators-web-seed-review`.

- Replaced inline type import with `@import`.
- Split web-seed provisioning into `provideBlob` and `provideTree`.
- Extracted streaming ustar output into `@endo/tar/writer.js`, with round-trip coverage.

Replied to every inline thread and posted a verified summary on PR #792. Tests, type checks, and targeted HTTP tests passed. Follow-up: open/land a successor PR from the pushed branch, since #792 is merged.

Self-improvement: nothing this time.
