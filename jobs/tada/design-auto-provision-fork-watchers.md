Everything verified end to end — the missed-approval gap has already closed live: the review job for minion.town#3 (`…pr3-review-3c9cea83`) and its follow-on conductor finalization (`minion.town-pr3-conduct`) are both in `tada/`. Work is complete; writing the report.

## Completion report

**Job:** `design-auto-provision-fork-watchers` (designer) — design + land auto-provisioning of per-repo watchers for the garden's own forks, sender-gated per the § Monitoring safety constraint, citing authorization broadcast `msgs/broadcast/20260709T225552Z-e61229`.

**What landed on main2** (commit `bad354aaf`, rebased over a peer's concurrent `03f33d7f1`):

- **`scripts/jobs/fork-watch-provisioner.sh`** (new) — the chosen seam is a reconcile step, invoked at the top of every `repo-watcher.sh` tick: it maps local own-fork bare clones (`worktrees/<owner>-<name>.git` with owner in the new journal `config/fork-owners`, seeded `kriscendobot`) into `repos/` + `comment-repos/`, CAS-idempotent, inert until `config/fork-owners` exists. It catches every fork-creation path including pre-existing clones (how minion.town was missed); `clone-keeper` (static list) and `ensure-project-worktree` (hot path, requires the clone) were evaluated and rejected in the design. A `watch-optout/<slug>` tombstone makes unwatch durable against re-reconciliation. On the leader it also materializes the triager's required bare clone at `repos/<slug>.git` (bounded, atomic), escalating persistent failures.
- **Sender gate in `comment-watcher.sh`** — an arming file declaring `sender-gate: required` now fronts the *whole* per-comment pipeline (mechanical verb table included, which is deliberately trust-independent on repo-gated repos) with a deterministic, no-LLM trust check: drop unless the author is on `trusted-senders/allowlist`, `maintainers/allowlist`, or a current endojs/Agoric org member — before any text reaches a job, reactji, reply, or `claude -p`. The provisioner is the only writer of own-fork comment armings and ships with the gate, so a stale deployed tree can never comment-arm an own fork ungated. Upstream/third-party owners are never auto-added.
- **`triager-claude.sh` cold-start fix** — the first arming exposed a latent bug: `git log --stat | head -400` dies of SIGPIPE under `pipefail` whenever full history outruns the cap, crash-looping `garden-triager@kriscendobot-minion.town` every tick (`repos/` was empty before, so the path had never run). Fixed with `|| true` + rationale.
- **CLAUDE.md** — new "Sender-gated exception — own-fork auto-provisioning" paragraph in § Monitoring safety constraint, citing the broadcast; **`designs/auto-provision-fork-watchers.md`** records the seam choice, the gate, the own-fork-vs-upstream line, deploy-ordering, and the tombstone.
- **Tests** — new `fork-watch-provisioner-test.sh` (16 asserts: inert-until-armed, own-vs-third-party, sender-gate line + broadcast citation, idempotence, tombstone, materialization, dashed-owner guard) and comment-watcher cases SG/SG2 (untrusted verb dropped pre-table; maintainers/allowlist passes) — all green; full comment-watcher suite 213/213.

**What landed on journal2** (via `land-journal-edit.sh`, 23:06Z): `config/fork-owners` (seeded `kriscendobot`), `repos/kriscendobot-minion.town`, `comment-repos/kriscendobot-minion.town` (with `sender-gate: required`), each citing the broadcast. Arming ahead of the code deploy is safe because minion.town is **private** (verified) — collaborator-gating covers the interval. I also materialized `repos/kriscendobot-minion.town.git` on the leader.

**Verified outcome:** all three units armed within a minute; the comment-watcher caught the 22:49Z kriskowal approval inside its 1-hour cold-start window; `kriscendobot-minion.town-pr3-review-3c9cea83` and the follow-on `minion.town-pr3-conduct` finalization are both complete in `tada/` — the exact missed-review class this job existed to close.

**Follow-ups:**
- A **deliberate deploy** of main2 activates the provisioner, the sender gate, and the triager cold-start fix; until then the deployed `garden-triager@kriscendobot-minion.town` keeps failing each 2-minute tick (bounded; self-heal throttled; comment+CI watchers unaffected).
- Post-deploy the provisioner will auto-arm the **seven other existing `kriscendobot/*` clones** on the leader (agoric-3-proposals, agoric-sdk, cosgov, endo, finbot, ocapn, vattr97) — the broadcast's standing intent, gate in force from the first tick; any unwanted one is a `watch-optout/<slug>` away.
- Pre-existing, not from this change: `run-test.sh` aborts mid-suite at SUBTEST 6 (`maintainer-reply FATAL: … no reply_to`) on clean main2, so later subtests never run — worth its own fix job. `triager-claude.sh` still has no hermetic test harness.
