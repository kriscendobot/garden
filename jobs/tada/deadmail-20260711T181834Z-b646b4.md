Recovered and carried forward the dead-lettered report. Done.

## Report

**Job:** dead-letter recovery. A status report from worker `ebfb-realign-521-passstyle-shape-only` was addressed to `endo-sturdyref-press-20260711-175014` (an hourly `endo-sturdyref-press` driver instance) which had already completed. The message was a *reply/report* to that doer, so I carried the reply forward rather than re-issuing a task.

**What I did:**
- Identified `endo-sturdyref-press` as a live **hourly scheduled** press-driver (`journal/schedules/endo-sturdyref-press.md`), so the report's value is preserved by feeding accurate state to the next dispatch, not by re-spawning a recipient.
- **Verified the report against live PR state** (no live worker was on the effort at recovery time — `inbox-list` clean of sturdyref workers):
  - `#521` HEAD of `build/sturdyrefs-pass-style-ocapn` is `d3c68897b9de refactor(pass-style,ocapn): realign 'sturdyref' to the shape-only design (cuts 1-2)` — an **additive** commit atop the untouched original `0ad8d1b02807`. PR stays **DRAFT** (base `llm-27f53e6`). Report is accurate.
  - `#541` (cuts 3-5) is stacked on that branch, last moved 2026-07-02 — so it now likely needs a **rebase onto the new #521 HEAD**.
- **Recorded a `progress` journal entry** (`entries/2026/07/11/182239Z-progress-gardener-a131c2.md`, pushed to `journal2`) capturing: the shape-only realign summary, the confinement note (secret/locator kept out of the guest-held value via off-band `(location,secret)` in the session manager), verified green claims (worker-run: pass-style 68 / ocapn 534 / marshal 82; tsc+eslint clean — not re-run here), and the next unblocked artifacts.

**What changed:** one append-only journal `progress` entry on `journal2`. No project-repo changes (the realign was already pushed by the original worker; original commit untouched, PR still draft).

**Follow-ups (teed up for the next hourly press dispatch, not done here):**
- **#541 rebase onto new #521 HEAD**, then press cut 3 (daemon facets) + cut 4 (guest opaque-token confinement).
- Deferred by the realign worker: `M.sturdyRef()` / `M.kind('sturdyref')` in `@endo/patterns` (blocked on marshal rank-order, deliberately excluded); removing the enlivenment cache per design #510 § "Enlivenment is on demand".

I did **not** undertake the cut 3-5 build under this recovery job — that is the hourly press-driver's designed lane, and starting it here would risk colliding with the next dispatch. The idle/next-cut state is now recorded for it.
