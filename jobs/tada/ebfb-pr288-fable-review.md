# Completion report — ebfb-pr288-fable-review

**What I did.** Performed the Fable closer review of `@endo/cbor-frame` (endojs/endo-but-for-bots PR #288 at 9849ea5) that kriskowal's CHANGES_REQUESTED review 4629027865 asked for, reading the full package (`head.js`, `encode.js`, `decode.js`, tests, package.json), the shared `@endo/bytes` package (`src/concat.js`, `concat-immutables.js`, `to-immutable.js`), its existing consumers (ocapn cryptography, cli store), and both prior review rounds, in an isolated project worktree.

**Review outcome.**
- **Inline ask at decode.js:22 (local `concat`)** — confirmed: replace with `concatBytes` from `@endo/bytes/concat.js`. Base branch `llm` already carries `@endo/bytes`; cbor-frame only needs the `workspace:^` dependency (lockfile in a separate `chore: Update yarn.lock` commit). The test file's local `concat` helper (blocked in the earlier round) unblocks with the same import.
- **Inline ask at decode.js:60 (`materialize()` optimizations)** — nuanced: the empty branch is already internal to `concatBytes` (and dead code), the multi-chunk branch *is* `concatBytes`, but the single-chunk zero-copy branch must **not** move into `concatBytes` — it would silently weaken the shared module's fresh-copy/no-aliasing contract that existing consumers rely on (I verified `bytesToImmutable` copies rather than transfers, so the argument rests on aliasing alone). Recommended keeping it as a one-line call-site short-circuit, or a separately named may-alias export (`coalesceBytes`) if ever shared.
- **New finding:** the reader re-materializes the entire carry on every chunk arrival while a frame is incomplete — quadratic copying (~N·k/2; 64 MiB in 64 KiB chunks ≈ 32 GiB copied). Fix specced: cache the decoded head as carry state, which also deletes `materialize()` entirely. Wrote a fixer-ready 6-point spec including a loop sketch, an optional `encode.js` companion cleanup, an explicit "do not change `@endo/bytes/concat.js`" guard, and a test to add. Otherwise assessed the package as sound against RFC 8949.

**Delivery.** This host (endolinbot2) has no real `gh` binary and no GitHub API credentials, so the three prepared comments (two threaded replies to comment ids 3522709721/3522711204 + top-level summary) could not be posted from here. They are packaged verbatim in board job **`ebfb-pr288-fable-review-post`** (posting-only, gh-precondition-gated, idempotent via a marker grep, identity-deduped), for a credentialed host to deliver. The maintainer was notified via the message bus with the review outcome and the handoff.

**Follow-ups.** (1) `ebfb-pr288-fable-review-post` delivers the comments; (2) once the maintainer concurs, a fixer can implement spec items 1–3 (+4 optional) directly from the posted summary. No garden-library (main2) changes were needed.
