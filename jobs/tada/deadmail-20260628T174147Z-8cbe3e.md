Done. Completion report follows.

---

**Job `deadmail-20260628T174147Z-8cbe3e` — complete.**

**Intent picked up:** A dead-lettered message (addressed to the already-completed `issue-kriskowal-garden-10`) carried a new maintainer comment on kriskowal/garden issue #10: the web bulletin failed with *"GitHub 422: Update is not a fast forward"* when acknowledging/replying, because the client didn't replicate the garden shell scripts' fast-forward retry loop.

**Root cause:** `commitReply` in `docs/bulletin/github.js` already rebuilt against a moved ref, but retried only **6 times with zero delay**. `journal2` is advanced near-continuously by the ~100-gardener fleet, so all 6 attempts re-collided on the same instant and the raw 422 surfaced to the maintainer. The diagnosis in the comment was exactly right.

**What I changed (landed on `main2` as `9fabadba7`):**
- `docs/bulletin/github.js` — `commitReply` now replicates `scripts/jobs/common.sh`'s push protocol: re-read moved head → reconstruct change on the fresh tree → retry **up to 50 times with a flat ~50–300ms randomized backoff** (matching `common.sh` `backoff()`). The jitter breaks lockstep with contending writers so the CAS drains instead of re-colliding. Switched from an initial exponential-growth backoff to flat jitter to faithfully match the referenced shell behavior and bound the worst-case tail.
- `docs/bulletin/DESIGN.md` §3 — documents the retry protocol and why the backoff is load-bearing.

**Verification:** A node harness loading the real `github.js` against a simulated moving branch — 1 collision lands in ~150ms, 5 in ~1.1s (rebuilding the tree on the freshly-read head each time); a never-settling branch still throws after the budget. `node --check` passes.

**Process:** Worked in an isolated worktree off `origin/main2` (not the deployed root), CAS-pushed to `main2` with a rebase-retry (lost one race, landed on attempt 2), then tore the worktree down.

**Issue thread:** Replied on https://github.com/kriskowal/garden/issues/10 (comment `4826904953`) with the diagnosis, fix, and verification; did not close the issue. Inbox drained (empty).

**Follow-ups:** None blocking. The fix deploys with the next GitHub Pages publish from `main2`/`docs` — no live effect until that publish.
