Done. Both actions the maintainer asked for are complete.

## Completion report

**Job:** `deadmail-20260628T052047Z-d6fc2a` — a dead-lettered message (intended for the already-completed `issue-kriskowal-garden-9`) carrying a new maintainer comment on garden issue #9: *"post a job to build a PR that patches Agoric internal hex. Follow up here with the PR link when it's up."*

**What I did:**
1. **Identified the target.** "Agoric internal hex" = `packages/internal/src/hex.js` in the Agoric monorepo (a real, self-contained hex codec). Read the source: it exposes a portable codec and a Node `Buffer`-backed codec. Found the obvious defect — the two codecs **validate differently**: the portable `decodeHex` throws on odd-length/non-hex input, but the Bufferish one delegates to `Buffer.from(hex,'hex')`, which silently drops bad input.
2. **Posted the build job** `build-agoric-internal-hex-20260628` to the board (body fed by file, per the producer-hang rule; `timeout`-wrapped). It instructs a gardener to patch hex.js (leading candidate: fix the validation divergence + add parity tests), **scoped to the bot fork `kriscendobot/agoric-sdk` as a fork-internal DRAFT PR** — explicitly *not* upstream `agoric/agoric-sdk`. It carries the ISSUE NOTE verbatim so the builder comments the PR link on issue #9.
3. **Followed up on issue #9** (https://github.com/kriskowal/garden/issues/9#issuecomment-4824982128): job posted, the builder will post the DRAFT PR link, plus my scope note and my interpretation of the intended patch, inviting correction before the build lands.

**Scope judgment:** agoric-sdk is a standing hard-no for *autonomous* garden action, but this was an explicit, authenticated directive from the owner (kriskowal, gated through the maintainer allowlist) for a narrow, well-defined patch — so I honored it while keeping the hard boundary intact: bot-fork DRAFT only, no upstream-agoric push or PR.

**Follow-ups / open items:**
- The exact intended patch was unspecified; I made the validation-divergence interpretation explicit on the issue and in the job, with a request to redirect if a different fix was meant. The DRAFT keeps it correctable.
- If the maintainer wants this upstream in `agoric/agoric-sdk`, that's a separate boatman/ferry step requiring the kriskowal-credentialed host — flagged on the issue.
- Inbox drained at both checkpoints (empty). No worktree was created (work delegated via the board).
