Reply posted (`#issuecomment-4826671944`); inbox clean. Done.

---

## Completion report

**Job:** `deadmail-20260628T160726Z-bef943` — dead-lettered message for `issue-kriskowal-garden-10` (already torn down). Picked up its intent: a new maintainer comment on kriskowal/garden **#10** to fold into the in-flight bulletin work and reply on the issue thread.

**What the directive said (kriskowal, untrusted data, treated as such):** "Committing to main2 is off limits. It should be possible to set up a CI workflow in main2 that is triggered by changes landed on journal2."

**Context I reconstructed:** The directive (16:06:21) and the first builder's "built and pushed to `main2` (`6454719ec`, `docs/bulletin/`)" report (16:06:46) **crossed in flight, 25s apart** — so the shipped approach (rendered site committed to `main2`) is the one the maintainer just ruled out. Verified: default branch is `main2`; `docs/bulletin/` (8 files) is committed on `main2`; only workflow on `main2` is `driver-tests.yml`; **no** workflows on `journal2`.

**What I did:**
1. **Posted a re-architecture job** `bulletin-rearch-ci-from-journal2` to the board (carries the ISSUE NOTE verbatim). Spec: generate+publish the bulletin via Pages-from-Actions (`configure-pages`→`upload-pages-artifact`→`deploy-pages`) so nothing is committed; keep the CI workflow file on `main2` (the endorsed part); preserve the client-side reply-to-`journal2` feature reusing the prior JS as workflow input; remove the superseded `docs/bulletin/` content from `main2`. The spec requires a short revised design note resolving two real constraints first.
2. **Replied on the issue** (`#issuecomment-4826671944`): acknowledged the crossed-in-flight miss, confirmed the new no-committed-content direction, and surfaced the key GitHub mechanics constraint.

**Notable technical constraint I surfaced** (the reason the directive can't be taken 100% literally): for `push` events GitHub runs the workflow from the *pushed branch's* ref, so a `main2` workflow won't fire on a `journal2` push unless the YAML also lives on `journal2` (a large orphan branch). Since `main2` is the default branch, the working path is a `main2` workflow triggered by `repository_dispatch` (`journal2-updated`, fired by the journal2-push machinery) + `workflow_dispatch` + a `schedule` safety net. Offered the maintainer the alternative of a tiny dispatch shim on `journal2`.

**Follow-ups (owned by the new job + maintainer):**
- A builder claims `bulletin-rearch-ci-from-journal2`, writes the revised design note, implements, removes `docs/bulletin/` from `main2`, and reports on the issue.
- Maintainer decisions pending: confirm the trigger path (repository_dispatch vs. journal2 shim) and flip Settings → Pages → Source = GitHub Actions; one-time PAT for the client-side reply box still required.
- I did **not** revert `6454719ec` myself (reverting is itself a `main2` commit, and the directive flags `main2` commits as sensitive); removal is scoped into the build job to be done deliberately via worktree.
