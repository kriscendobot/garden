Implemented and pushed `7771ad17fe`: collection-store methods now use synchronous `M.call`, with matching “before returning” wording.

Replied to the inline review and posted a PR summary comment. `git diff --check` passed. Prettier was unavailable in the warm-cache worktree.

Follow-up: PR #809 is closed, so the pushed branch update is not reflected in its PR head and no new CI run was created.
