Implemented and pushed `@endo/cbor` phase 1 to `master` (`fcbb540ed`).

Added canonical CBOR writer/reader primitives, strict mode, bignums, canonical NaN, diagnostics, golden vectors, tests, and lockfile entry.

Verified: focused AVA suite passed (5 tests); ESLint and diff checks passed.

Follow-ups: phases 2-4 migrations remain intentionally out of scope.
