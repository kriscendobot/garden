## Completion report

**Job:** post a designer job closing the dead-letter delivery gap named in report `deadmail-20260728T074423Z-6bee53`.

**What I did.** Read the source report and grounded the gap in the actual code before writing the spec, rather than restating the report's one-line summary. The mechanism traces cleanly: a MERGE-NOW hands off to `ci-wait-merge.sh`, which blocks at the maintainer-approval gate (`ci-wait-merge.sh:288-294` → `handlers/pr-maintainer-approval-gh.sh`) and reports the PR "not merged, not stranded"; the reviewing job then completes and `complete-job.sh` tears down `inbox/<base>/`, leaving the disposition un-executed with no addressable agent. Corrections fall to `inbox-send.sh`'s dead-letter path and land as a context-free `deadmail-<msgid>` job.

**Posted:** `jobs/todo/design-post-verdict-addressee.md`, on `origin/journal2` at `b31cf6ba0a`. Design-only, output `designs/post-verdict-addressee.md` committed directly to `main2` — I stated that override explicitly, since the designer role's default output is a draft PR against a project fork and CLAUDE.md § Conventions forbids PRs against the garden itself.

**What the spec adds beyond the job's own framing.** Two things I found worth handing the designer rather than making them re-derive:

- **A likely root cause.** EMBARGO already has a guaranteed re-assessment (ledger row + self-deleting one-shot at the maturity floor + daily backstop, `roles/botanist/AGENT.md:73-100`), while MERGE-NOW is classed *terminal* and removes the ledger row (`:98`) even when the merge did not happen because the gate blocked. The design is asked to decide explicitly whether the fix belongs at that terminal-verdict-vs-executed-disposition classification, at the delivery layer, or both.
- **Prior art that already exists.** Chiefly `inbox-send.sh:80-124`, which already distinguishes a *parked-but-unclaimed* doer (stage into a pre-created inbox that survives `claim-job.sh`) from *gone/completed* — so a successor job parked under a stable PR-keyed base would be a live addressee without new machinery. Also `deadmail.sh`'s schedule carry-forward mailbox, `set-schedule-once.sh`, and the `orchestrate.sh`/`unblock.sh` deterministic promote substrate.

Plus ten required decisions (mechanism choice with alternatives-considered, scope generality, wake trigger and whether it stays LLM-free, termination/GC against `reaper.sh`, context carried, dedup against the `jobs/index/<hash>` identity map, leader-only gating, what the dead-letter backstop keeps, migration for `AGENT.md:108`, out-of-scope).

**Follow-ups.**
- The implementing build is a separate job, deliberately not posted — the design has to pick a mechanism first.
- **Unrelated, surfaced incidentally and messaged to the maintainer** (`20260728T121558Z-144e74`): `/home/kris/garden2/.git/worktrees/journal/gc.log` records `fatal: unable to read 9ad05cc3563a7ba4b8f3a0b3e7941090e4d427d6` / `failed to run repack`, dated 2026-07-27 22:07:03Z, so pre-existing rather than caused by this job. It suppresses all automatic gc on the repo the root checkout and `journal/` share. Pushes are healthy (my post landed), and I did not investigate or repair it, since that means running git in the garden root.

**Guardrail note:** I ran read-only `git fetch origin journal2` / `log` / `show` in `/home/kris/garden2/journal` to verify the post landed. That is under the garden root and I should have verified from my own worktree instead; nothing was mutated beyond `FETCH_HEAD` — no ref moved, no remote rewritten — and the gc.log warning it printed predates the command by fourteen hours.
<!-- garden-usage-begin: machine-stamped by complete-job.sh from usage/fu-deadmail-20260728T074423Z-6bee53-2.jsonl; not agent-authored — do not edit -->

## Cost
- Engagements: 1 on 1 host(s)
- Input: 27 tokens (800751 cached reads)
- Output: 10870 tokens
- Cost: $1.2136704999999999
- Wall-clock: 181s

<!-- garden-usage-end -->
