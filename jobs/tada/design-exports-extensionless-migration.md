Design job complete: **draft PR endojs/endo-but-for-bots#663** (`design/exports-extensionless-migration` → `llm`) carries `designs/exports-extensionless-migration.md` plus its `designs/README.md` registration.

**What I did**
- Surveyed the monorepo for ground truth before drafting: 235 explicit `.js` exports keys across 97 packages with exports maps; 43 already-extensionless keys with **zero** `./K.js`/`./K` collisions; 4 wildcard pattern keys (3 test-condition-gated, 1 public passthrough); 70 conditional-object subpath values (60 with `types`); ~992 cross-package `.js` specifier sites, of which 967 are pass-2 eligible by explicit-key lookup, 22 wildcard-resolved, 3 anomalies (`ses/console-tools.js`, `@endo/ocapn/syrup/{encode,decode}.js`) matching no key.
- Wrote the design resolving all five mandated questions: (1) pass-2 eligibility as one mechanical rule — bare workspace specifier whose post-pass-1 exports carry the exact extensionless key — which excludes intra-package relative imports, wildcards, and anomalies by construction; (2) conditional exports copied wholesale, `.`/`./package.json`/`.css` untouched, wildcards out of scope with the dual-pattern alternative documented and rejected; (3) `types` parity by construction (deep-copied values; TS NodeNext declaration lookup follows the target, not the key; repo already builds NodeNext, no consumer tsconfig changes); (4) two idempotent codemods (`scripts/codemod-exports-extensionless.mjs`, `scripts/codemod-extensionless-specifiers.mjs`) with `--check` gates A/B armed in CI by end-of-pass arming children, gate C holding structurally; (5) full build/tsc/ava/xs/hermes verification per pass plus an `import.meta.resolve` dual-resolution probe and a compartment-mapper `importLocation` probe (verified `infer-exports.js` interprets non-pattern keys literally, so extensionless keys are supported there). Changeset shapes specified: pass-1 minors with the standing compatibility note, pass-2 patches. Orchestration: two parallel per-package-tranche orchestrations, pass 2 parked `blocked_on` pass 1.
- Registered the design in `designs/README.md` per the project's plan-sync convention: summary table, M2 (Project Hygiene) row — reopening M2 with this single additive row while noting its original exit criterion remains met — per-design estimate (M, 4–6 days), milestone totals (58→59), timeline, and the last-updated layer note. Dependency graph untouched (no design-graph dependencies).

**Verification evidence:** all survey numbers come from scripts run against the worktree at `llm` HEAD `7870da1d9` (outputs observed in-session); the collision check ran over all 97 exports maps and found none. The design itself is unimplemented by intent — Status: Not Started.

**Follow-ups (named in the doc):** the two migration orchestrations execute from this doc after maintainer review; `release-automation-major-bump-exports-trigger` design and a docs/markdown specifier sweep remain to be filed; two open questions for the maintainer (extensionless support for `@endo/platform`'s public `./fs/extended/*` wildcard; ESLint-rule vs script form for the gates).
