Inbox is empty. The job is complete — final report:

Designed and built the Stripe credit-purchase system for minion.town in test/simulation mode, with everything landed on `main` (commit `328e7e1`) and the deploy follow-on parked for the maintainer's go-ahead.

**Design** — `designs/stripe-credits.md`: the full purchase→credit loop with a sequence diagram, threat model, and exact existing-account maintainer setup steps (switch to test mode → copy `sk_test_`/`pk_test_` keys → create the webhook endpoint at `https://minion.town/billing/webhook` for `checkout.session.completed` + `checkout.session.async_payment_succeeded` → store all three as Secrets Manager `minion/stripe`). Key decisions: credits as an atomic `ADD` on the existing `minion-town-accounts` row; a `minion-town-billing-events` marker table giving exactly-once semantics via one two-action `TransactWriteItems` (marker put conditioned on absence + balance ADD, so dedupe and credit commit atomically); webhook ungated at Caddy but secured by Stripe signature verification over raw bytes; identity bound by server-written `iss`+`sub` session metadata; the rate as one config value (`CREDIT_UNIT_CENTS=1`, 1 credit = 1¢) pinned per session so config changes never reprice in-flight purchases; live keys refused outright with no override flag. The debit/metering seam is shaped (a conditional negative ADD refusing to go negative) but deliberately not built, per mandate.

**Toy** — `src/billing/` (config, rate math, ledger with in-memory + lazily-loaded DynamoDB adapters, checkout, webhook handler, Express router with gated identity via shared-secret proxy headers) plus `dev/billing-demo.ts` (`npm run billing:demo`). Grounded against stripe-node v22.3.0 (API `2026-06-24.dahlia`) by reading the installed SDK's type declarations, not memory. Signature verification uses the real SDK code path (`constructEvent`, offline HMAC); in mock mode only the checkout creation and the event's origin are simulated — deliveries are signed with the SDK's own `generateTestHeaderString`. With `sk_test_` keys set, the demo creates a real test-mode Checkout Session and polls for the `stripe listen`-forwarded webhook.

**Verified by execution:** `test/billing.test.ts` — 27/27 green (all 68 repo tests pass; the two "failed" vitest suites are pre-existing `node --test` SIWE thunk tests that pass under their own runner, unrelated to this change). The demo run proved the loop end-to-end: checkout created with identity metadata → signed webhook credited 500 credits once → duplicate delivery no-oped (HTTP 200, balance unchanged) → forged signature rejected (HTTP 400). Typecheck clean.

**Follow-ups:** parked `--go-ahead` job `deploy-stripe-credits-minion-town` on the plan queue (builder role), naming the Stripe-test-keys precondition and the seven binding steps (events table, attribute-scoped IAM so the serving path still can't touch role/status, router mount before `express.json()`, secrets/EnvironmentFile, Caddy routes, buy control, end-to-end verification with evidence). Minor observed nit, not acted on: the root `npm test` glob picks up the SIWE thunk's `node --test` files and reports them as failed suites — a vitest exclude would clean up the signal.

Self-improvement: nothing this time.
