Delivered. The dead-lettered message's intent has been carried forward to the live author via the durable channel. No garden-code change is warranted for this job — it's a message-bus coordination recovery, and creating roadmap follow-up jobs would usurp the supervisor's scoping call.

---

**Completion report — job `deadmail-20260705T215940Z-4e2ea4`**

**What the dead-letter was:** A supervisor-status report from the still-live promises child (`xs2rust-endor-build-stage3b-promises`, child 7/9 of the serial `xs2rust-endor-build-stage3b` orchestration, PR #600) addressed to `port-xs-to-rust-memory-safe-engine-s7`. It could not be delivered because **s7 is parked** in `jobs/plan/` (`gate: blocked_on xs2rust-endor-build-stage3b`), not a live agent — so the bus had no inbox and dead-lettered it.

**Diagnosis:** The message was informational (a report/reply), not a request for s7 to act. Critically, s7's own brief states (line 44) that the supervisor learns child outcomes by **reading each child's tada report**, not from inbox messages. So the dead-lettered inbox note was supplementary to the authoritative channel — the promises child's own `jobs/tada/xs2rust-endor-build-stage3b-promises.md`, which s7 reads when promoted on stage3b completion.

**What I did:** Routed the intent to the one live agent that owns the durable channel — the promises child itself (confirmed alive: gardener 12, claimed 21:07, still in `doin/`, tada not yet written). Sent it a relay (delivered `20260705T220248Z-a4d070`) telling it (a) its s7-addressed report dead-lettered because s7 is parked, (b) the authoritative channel is its forthcoming tada report, and (c) the full content to ensure lands there so nothing is lost: the COVERED surface, the honest named skips (thenable adoption, throwing reaction handler, .finally + all/race/allSettled/any combinators), and the KNOWN metering bound (multi-unhandled-rejection list walk). Timing is ideal — the child hasn't written its tada yet, so it can fold this in before completing.

**What changed:** Nothing in garden code or on `main2` (none warranted). One message delivered on the bus.

**Follow-ups:** The named skips (thenable adoption, throwing reaction handler, promise combinators/finally) and the multi-unhandled-rejection metering bound are follow-up increments for **supervisor s7 to scope** when it wakes on stage3b completion — I deliberately did **not** create roadmap jobs for them, as that scoping is s7's call and pre-creating them could collide with its planning. Belt-and-suspenders: if the promises child completes before reading my relay, the bus promotes it to a fresh recovery job, so the intent still cannot be lost.
