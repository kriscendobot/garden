Press tick complete for the npm-via-CAS registry-proxy arc (endojs/endo-but-for-bots, base `llm`).

**Assessment (state moved since the job spec was written):** Phase 2 (#276, HTTP fetch) has **merged** into `llm`. The remaining phases are all implemented on a six-PR stack: #799 (Phase 4 resolver) → #800 (Phase 4 assembly + XS execution) → #801 (exec-hardening reland) → #802 (exports-resolution reland) → #803 (Phase 5 offline + `.npmrc`) → #805 (top-level-await support, draft). Every PR in the stack is CI-green and mergeable. The design doc at the tip marks all five phases implemented, with only out-of-scope enhancements left in Known gaps. No live peer held any of these branches (last pushes were prior press ticks; the board shows no other worker on this arc).

**What I pressed this tick:** the one increment lacking independent verification was #805 (pushed 08:53Z today, closing the last recorded execution gap — top-level `await` failing under XS's synchronous `importNow`). In an isolated worktree at its head `ae965fff60` I rebuilt `rust/endo` from scratch (carried-over bootstrap bundles + prebuilt `libxs.a`, per the known CI-doesn't-compile-this-crate caveat) and re-ran the full evidence sequence with a fresh `ENDO_STATE_PATH`:

- `cargo test --release -p xsnap --lib`: 118 passed, 0 failed; `-p endo --lib`: 140 passed, 0 failed — both match the PR's claims.
- Cold `--offline` → typed refusal, exit 1. Live cold run against registry.npmjs.org → `p-limit@4.0.0` + `yocto-queue@1.2.2` fetched into the CAS and the **top-level-await entry executed in XS** (`tla verify demo: squares=1,4,9,16`, exit 0), package hashes byte-identical to the #800/#803 evidence. `--offline` replay → identical output, exit 0, zero network. A TLA entry throwing after its `await` → clean `ERROR: boom after await` + stack, exit 1, no crash. Demo dir held only `package.json` + `main.js` throughout — no npm CLI, no `node_modules`, no lockfile.
- Recorded the verification as a comment on #805: https://github.com/endojs/endo-but-for-bots/pull/805#issuecomment-5023698268

**Arc status:** the stack is **finish-line complete** — `endor run <entry.js>` resolves (MVS), fetches, CAS-caches, and executes npm dependencies end to end, offline-replayable, TLA included, now verified by two independent ticks. The arc is no longer blocked on any unbuilt phase; what remains is review/merge of the stack (starting at #799 into `llm`) plus recorded follow-up gaps (daemon-side synchronous archive install, private-registry auth, workspaces, peer/optional deps).

**Follow-ups:** none posted — merge shepherding is review-gated maintainer/conductor territory and the arc's daily status feed already tracks it; the next press tick should re-verify stack state and press a follow-up gap only if the stack has landed or review feedback has arrived.
