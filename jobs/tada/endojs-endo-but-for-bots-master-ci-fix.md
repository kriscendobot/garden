Landed master fixes through `6ee3fda77b`.

Verified green: lint, zizmor, all Node 22/24 Ubuntu/macOS tests, cover, TypeDoc CI build, browser tests, package uniformity, and action-pin checks.

Remaining failures are repository configuration only: configure `RELEASE_TOKEN` for Release and enable GitHub Pages for deployment.
