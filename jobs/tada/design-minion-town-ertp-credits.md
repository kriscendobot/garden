Job complete. Design PR opened: **https://github.com/kriscendobot/minion.town/pull/8**

## Completion report

**What I did.** Wrote `designs/ertp-credits.md` (202 lines, the repo's own design shape: `# Design:` title, bold Status/Mandate/Grounded-against/Companion header block, numbered sections, mermaid) and delivered it as design PR #8 against `main` on `kriscendobot/minion.town` per the 2026-07-10 maintainer directive, commit subject carrying "spec only, no live change". Grounded against the full corpus before drafting: `stripe-credits.md`, `mcp-endo-guest.md`, `mcp-oauth.md`, plus garden-library lookups (mint-purse-money / Miller-Morningstar-Frantz 2000, caretaker pattern, Karp/Stiegler/Close six-aspects, the `@endo/gateway` Decision 8 resource-ledger sections).

**Key design decisions.** ERTP is the **settlement layer between parties**, not a rewrite or mirror of the Stripe stack: the DynamoDB balance stays the fiat-boundary ledger of record, and bearer credits exist only between an explicit **reflect** (escrow debit + treasury mint into the caller's guest purse, consuming the stripe-credits § 2 debit seam) and **redeem** (issuer burn + account credit), with a `minion-town-reflect-events` marker table mirroring the billing-events idempotency discipline and a stated conservation invariant. The **attenuated charge account** is a caretaker-split facet (`draw`/`pay` action facet, `revoke`/`tighten`/`getSpent` control facet) with limit, expiry, single-use, rate-per-period, and payee-lock terms that compose narrow-only, and the ocap hazards (revocation, double-spend, rights amplification, confinement) are addressed one by one. minion.town keeps only the fiat bridge; the ERTP mechanics go to a proposed `@endo/exo-credits` companion design (to be filed on `endojs/endo-but-for-bots` @ `llm`), converging with gateway Decision 8 (Stripe billing as the operator-supplied `PaymentProcessor`).

**Use cases covered** (first two with sequence diagrams): peering arrangement (metered resource facets, continuous per-draw settlement, fail-safe revocation both directions); third-party file transfer paid in credits one party issued itself (brand generality, charge facet passed inside the transfer invocation); metered relay/network egress; storage rental; delegated compute.

**Key open questions** (§ 8 of the design): sub-brands versus payee-locking for purpose scope; bearer-final versus escrow-shaped draws; the Endo package's name and home (`@endo/exo-credits` versus inside `@endo/gateway`); expiry's clock in a confined facet; reflect/redeem fees and rate limits; refund interplay with already-reflected credits; bearer-credit custody on the single box (daemon disk loss).

**Verification.** All three mermaid diagrams validated with real execution: `mermaid.parse` under jsdom answered `OK flowchart-v2`, `OK sequence`, `OK sequence` (mmdc/Chromium cannot run in the container). Em-dash and Latin-shorthand sweeps ran clean. The live minion.town deployment was not touched (design-only; nothing to verify against runtime).

**Follow-ups and side effects.** (1) Landed a `verifyPaymentProof` keyword shortcut on the journal library index and notified the scholar (`20260710T222856Z-919ce8`). (2) Self-improvement: landed `skills/mermaid-validation/SKILL.md` (parse-only mermaid checking without a browser) on `main2` at `f9575310e`, referenced from the designer role's diagram norm. (3) The Endo-side companion design (`@endo/exo-credits`) is named in the PR as to-be-filed; the maintainer's PR review gates any build.
