Press tick complete — this dispatch landed **Phase 5 (offline mode + `.npmrc`)**, the last unimplemented phase of the npm-via-CAS registry-proxy design.

**Assessment.** Phase 2 (PR #276, HTTP fetch) merged 2026-07-18. The previous ticks consolidated everything onto one new-generation stack: #276 (merged) ← #799 (transitive MVS resolver) ← #800 (assembly + XS execution — the finish-line `endor run` capability, proven) ← #801 (exec hardening reland) ← #802 (Node `exports`-map resolution reland). The only remaining design phase was Phase 5, whose prior-generation PR #782 sat on a superseded base. No live peer held this ground.

**What I pressed.** Opened **PR #803** (DRAFT, stacked on #802, branch `reland/endor-npm-offline-npmrc`): a reland of #782's substance ported to the new stack — new `npmrc.rs` (`NpmConfig`: `registry`, `@scope:registry`, nerf-dart `_authToken` lines, layered `~/.npmrc` → project `.npmrc` → `NPM_CONFIG_REGISTRY` → `--registry`), `FetchError::Offline` + `OfflineClient` in `fetch.rs` with Bearer-token attachment in `UreqClient`, scoped-registry routing in `npm_resolve.rs`/`assemble.rs` (`*_with_config` variants, no churn to existing callers), and CLI flags `endor run --offline` / `endor npm-resolve --offline`. Also updated the design doc's Status section (all five phases now recorded) in the same PR.

**Real-execution evidence** (fresh state, live registry.npmjs.org, `p-limit@^4.0.0` demo): cold-state `--offline` refuses with the typed offline error (exit 1); the online run fetches p-limit + yocto-queue transitively into the CAS and executes in XS (`squares=1,4,9,16`); the `--offline` replay produces identical hashes and output with zero network by construction; `npm-resolve --offline` serves from cache; and `.npmrc` → env → flag precedence was observed live via the consulted-registry URL in error output. `cargo test -p endo --lib`: **139 passed, 0 failed** (11 new).

**Follow-ups:** the stack (#799→#803) is feature-complete against the design and ready for review/gauntlet from the bottom up; the old-generation drafts (#772, #785, #787, #791, #795, #798, #782) remain open only as superseded references (supersession comments posted on #782 and #800) and could be closed. Remaining design known-gaps: full CommonJS `require` linkage, nested directory-relative resolution, `_auth`/password auth forms, `${VAR}` expansion.
