---
host: endolinbot
lane: 1
kind: driver-self-improvement-log
---

# driver lane 1 self-improvement log

Per-tick agent analysis of driver loop iterations. Each section names
the tick's transcript SHA (inspect via `git -C journal cat-file blob <sha>`)
and the agent's suggestions for self-improvement.


## tick at 2026-06-03T04:35:36Z -- state=build rc=0 sha=6e3bdf95db525b9e3363facf7e93c0f0ac8bdad2

PR: (none)
Workflow: design-only-pr

### agent analysis

I read the transcript. It captures one tick that advanced `initial → build`: `DRIVER_PR` was empty, the workflow's `initial` branch posted a `builder:build-design-only` job to the board, and `write_state build builder:build-design-only` persisted the new state. `rc=0`.

The transcript is short (42 lines, all `set -x`); the analysis below is about what the *next* ticks in `build` will do, given how we left the lane.

## Self-improvement suggestions

**1. The `initial → build` branch without `DRIVER_PR` strands the lane.** The `build` predicate calls `get_pr_json`, which short-circuits to `{}` when `DRIVER_PR` is empty (driver.sh:189-191). The predicate's `pr_state` and `pr_draft` extractions both come up empty, so the directive is `wait` on every subsequent tick — forever — even after the builder opens the PR. Phase 2's note on driver.sh:214-216 ("test harness exercises the path with `DRIVER_PR` already set") confirms this is a known gap, but the production driver should not silently spin. Two reasonable fixes:
   - Discovery: when in `build` with empty `DRIVER_PR`, read the builder job's result entry (or scan kriscendobot's recent PRs on the configured repo) for the matching `build-design-only` slug, then write `DRIVER_PR` back into the state file's frontmatter and re-tick.
   - Refuse: have the `initial` branch escalate (or remain in `initial`) when `DRIVER_PR` is empty, instead of advancing through a one-way door.

**2. `post_job` output is fully suppressed (driver.sh:529).** `if post_job "$role" "$slug" >/dev/null 2>&1; then` collapses any real error into "post_job failed; staying in $state". When self-improvement reads this transcript, it can't see whether the job board rejected the post (duplicate slug, missing eligibility, push failure), so it can't suggest a real fix. Suggest teeing post_job's stderr to a per-tick log appended to `$tick_capture` so the agent sees the actual failure mode.

**3. No structured progress lines in the transcript.** Everything in the blob is `set -x` trace; there is no human-readable line like `driver: advancing initial→build (posting builder:build-design-only)`. The trace is dense and the self-improvement agent has to reconstruct intent from shell-internal variable assignments. A few targeted `echo "driver: ..."` statements at decision points (per branch in the dispatcher, around `post_job`, after `write_state`) would dramatically improve transcript readability without bloating it.

**4. The `build` predicate should distinguish "no PR known yet" from "PR open but still DRAFT".** Right now both look identical — `wait` — but they have different remedies (discovery vs. genuine waiting on builder). Have the predicate emit something like `escalate:no-driver-pr-bound` when `pr_json == {}` after a configurable grace window (e.g. 3 ticks in `build`), so the gardener inbox sees the stuck lane instead of the lane silently spinning.

**5. Minor: the `advance:` directive parser is positional.** `advance:build:builder:build-design-only` works because the workflow happens to use a single-token role name. If a future role or slug ever contains a colon (`solicitor:design-panel` already appears in the codebase as an `awaits:` value), the parser at driver.sh:519-526 will misroute. Worth a delimiter-quoting comment in the dispatcher, or moving to `|` as the directive separator now.
