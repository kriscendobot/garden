elapsed-constancy overrun-suspect: ironhorse-js-00-report-harness-foundation-gauntlet-panel-2

gardener-2 on endolin-garden2-5bcdff64: job ironhorse-js-00-report-harness-foundation-gauntlet-panel-2 was classified TRANSIENT (rc=1) but its
handler has now died at a near-CONSTANT elapsed across the last 2 requeue
cycles (elapsed window, oldest->newest: 4,3s; requeue cycle 2; tolerance +/-15%).

That is the signature of a DETERMINISTIC overrun MISclassified as a
self-resolving blip (a Claude Code session/usage cap tripping at the same
point every run, or a prompt that drives the CLI to the same failure), NOT
an external signal-kill or wall-clock timeout (those vary in elapsed and
are excluded). Left in doin for the reaper (requeue ownership UNCHANGED),
with the early-doom overrun counter stamped so it surfaces after
GARDEN_REAP_OVERRUN_THRESHOLD cycles instead of the full 5. Triage the
job spec / handler rather than waiting out the doom threshold.
