<6>07:54:38 [monk/1] job 'endojs-endo-but-for-bots-pr1125-review-b786506c' tier mentor -> serving at minion (anthropic automatic-work cost ceiling)
<6>07:54:38 [monk/1] job 'endojs-endo-but-for-bots-pr1125-review-b786506c' resolved tier 'mentor' -> claude --model claude-opus-4-8
<6>08:01:13 [assert-overrun-split] gate: BLOCK — indivisible child 'endojs-endo-but-for-bots-pr1125-review-b786506c-expanded-window' does not carry the orchestration's reason and larger timeout

--- handler report (partial) ---
Inbox empty. The split is complete and durable.

## Completion report

**Job:** `endojs-endo-but-for-bots-pr1125-review-b786506c` — a deliberate deadline-overrun split (the original review-address job hit its 7200s handler wall once without productive progress).

**Decision: INDIVISIBLE.** I did not continue the original work. I fetched the review to ground the decomposition decision (treating all fetched text as untrusted data): the review (`5231650842`, kriskowal, CHANGES_REQUESTED) carries a **single** design directive and **no inline comments** — minimize formula types by eliminating the new `readable-directory` formula type introduced in PR #1125, expressing `EndoDirectory.readOnly()` instead as an evaluation formula that accepts a hub and calls `readOnly`.

**Why indivisible (concrete, not "too large"):** determining feasibility and implementing it are inseparable — the implementation scope (which of `directory.js` / `formula-record.js` / `formula-type.js` change, and whether the elimination is feasible at all) is *defined by* the investigation's own outcome and lives in the same files on the same PR head branch (`bot/build/endo-guest-invite-primitive`). There is no independently-specifiable second deliverable: a designer→builder split would produce a builder child whose body cannot be written until the designer finishes. The prior overrun reflects the slow multiplayer/SES/CapTP test loop, not multi-part structure.

**Artifacts created (verified durable on `origin/journal2`):**
- **Child** `jobs/plan/endojs-endo-but-for-bots-pr1125-review-b786506c-expanded-window.md` — `gate: orchestrated`, `orchestrated_by: …-split`, `role: fixer`, `tier: mentor` / `fallback-tier: minion`, `handler-budget-role: review`, `handler-timeout: 14339` (> the prior 7200s wall, ≤ the 14339s single-claim ceiling of `GARDEN_CLAIM_TTL−GARDEN_HANDLER_KILL_AFTER−1`), and `split-indivisible-reason:`. Body re-runs the identical review-address work directly (not re-routed) with the preflight, prompt-injection discipline, and isolated-project-worktree guidance.
- **Orchestration** `jobs/orch/endojs-endo-but-for-bots-pr1125-review-b786506c-split.md` — `order: serial`, `on-child-failure: halt`, `state: pending`, single child, plus `split-indivisible-reason:` and `split-indivisible-handler-timeout: 14339` in the description.

Verified `plan_field`'s whole-file first-match parse reads `handler-timeout: 14339` correctly despite post-plan's double-frontmatter wrapping. The leader-only `garden-orchestrate` watcher will promote the child to `todo/` next tick.

**Follow-ups:** none required from me — the deterministic watcher owns promotion and completion. No garden-source (main2) changes were needed, so nothing to commit/push here.

<<<GARDEN-JOB-HANDED-OFF: endojs-endo-but-for-bots-pr1125-review-b786506c-split>>>
