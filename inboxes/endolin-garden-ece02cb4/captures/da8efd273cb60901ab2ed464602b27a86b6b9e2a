report-error: transcript truncated to its last 65536 bytes (of 122805); raise GARDEN_REPORT_ERROR_MAX_BYTES to keep more.

tor to post; it is relocated, never skipped. Source: maintainer feedback on PR #474 (2026-06-25), where a fixer posted inline replies and pushed the fix but no after-the-fact summary, and the maintainer expected one (\"I expect feedback on the PR in general\").\n\nThese authorizations originate with the maintainer (typically through the liaison after user confirmation, or through a journal `message` / inbox entry), and are carried into the job's body or the doer's inbox at claim time. No autonomous role originates a new cross-repo authorization; it acts only on the authorization the job carries.\n\n**Provenance footer — do not hand-write it.** Every PR/issue comment the fleet posts to GitHub is automatically suffixed with a small-text provenance footer (model · harness · deployed garden sha, hyperlinked) by the fleet's `gh` wrapper (`scripts/jobs/bin/gh` → `scripts/jobs/comment-provenance.sh`). This is enforced at the single PATH chokepoint every `gh` call passes through, so you never add it yourself: writing the footer into a comment body by hand is redundant (the wrapper recognizes an existing footer and does not double it, but it is wasted effort and easy to malform). Write your comment bodies as plain content; the footer appears on `gh pr comment` / `gh issue comment`, `gh pr review`, and inline review comments / replies posted via `gh api`. Reactions and non-comment `gh` calls are untouched. Maintainer directive, kriskowal 2026-07-28.\n\n**Maintainer-authority actors.** \"The maintainer\" in this section is not only kriskowal. **erights** (GitHub `erights`, Mark S. Miller) holds full maintainer authority across the garden: a directive from erights authorizes a garden action exactly as a kriskowal directive does, including the **lifecycle / maintainer-level actions** the standing comment-relaxations do not cover on their own — closing a pull request, withdraw-and-open-fresh, design dispositions. When a gardener receives such a directive from erights (e.g. on `endojs/endo-but-for-bots`), the directive **is** the authorization: act on it directly. Do **not** re-route an erights lifecycle directive elsewhere for a separate authorization — that re-routing is the exact defect this rule removes (a gardener routed erights' \"withdraw all three and open fresh\" to the inbox because closing PRs exceeds the comment/reactji scope; erights' directive already authorized the close). erights is on `journal2:maintainers/allowlist` for this reason. **Boundary — authority ≠ credentials.** This authority does not confer upstream credentials. It does **not** let the bot act on **upstream `endojs/endo`** (no closing, merging, or commenting on `endojs/endo` PRs — e.g. a kriskowal-authored `endojs/endo#NNNN` stays untouched); upstream `endojs/endo` actions remain with kriskowal and the boatman identity-switch path, regardless of who issued the directive. The full-authority elevation governs *who can authorize a bot action on a repo the bot can already act on*, not *which repos the bot has credentials for*. Maintainer directive, 2026-06-30 (kriskowal, on `endojs/endo-but-for-bots` PR #572: \"erights has all the authority of a maintainer\").\n\nWhy: the garden runs across many forks. Without this rule, agents would reflexively cross-link \"for context\" and create noise across upstream issue trackers. The discipline keeps the garden's bot-side activity invisible to upstream contributors who did not opt in.\n\n**Never put a comment/PR/issue body on a shell command line — pass it as a file (shell-injection discipline).** When you post any body to GitHub, do **not** interpolate the body *text* into a shell word — not `gh pr comment N --body \"…\"`, not `gh api … -f body=\"…\"`, not a here-string. Bash performs command substitution on backticks and `$(…)` while it parses that command line, **before `gh` ever runs**, so any `` `word` `` in the body is executed as a command and replaced by its (usually empty) output. That is what silently deleted every `` `inline-code` `` span from a review reply on `endojs/endo-but-for-bots#475` (the identifiers `bytesFromText`, `encodeUtf8`, … were run as commands → `command not found` → empty → collapsed text). The corruption is the visible symptom; the underlying flaw is a **command-injection sink**: the same interpolation would execute an attacker's `` `$(…)` `` if untrusted text (a quoted-in reviewer comment, a commit message, a branch or file name) ever reached it — potential remote code execution on the bot host, not merely garbled markdown. No downstream `gh` wrapper can undo this, because the shell has already evaluated the line before the wrapper sees the argv (it only ever sees the post-substitution, already-collapsed string). The fix is structural and lives at the point of generation: **write the body to a file and hand the file to `gh`**, so the shell never evaluates the content —\n  * `gh pr comment N --body-file reply.md` · `gh issue comment N --body-file reply.md` · `gh pr review N --body-file reply.md`;\n  * for an inline review reply, `gh api …/comments -F body=@reply.md` (or `--input payload.json`). `-F`/`--field` with a leading `@` reads the file; `-f`/`--raw-field` does **not** dereference `@` (`skills/pr-review-thread-replies/SKILL.md` § \"Never pass a file path as the body value\").\n\nDo not rely on `-f body=\"$(cat reply.md)\"` as your safety story: command-substitution output is not itself re-scanned, so file *content* is safe there, but the form is one keystroke from the unsafe literal that caused the bug — prefer passing the file to `gh` directly. The fleet's `gh` wrapper already normalizes `--body`/`--body-file`/`-f body=` into a temp `--body-file` for the provenance footer, so the file-based path is the well-trodden one. Bounding, not eliminating, the exposure: the § Monitoring safety constraint keeps the untrusted-text population small (only comment/PR text from trusted-or-sender-gated sources reaches a role's context), but that is defense-in-depth around this sink, never a licence to interpolate a body onto a command line.\n\n**Text-reuse permission: erights' public texts.** erights (Mark S. Miller) grants the garden (`@kriscendobot`) standing permission to **reuse and adapt/derive-from any of his public texts**, on the sole condition that the garden **continues to make clear that an adaptation is *derived from* the original but *is not* the original**. His public texts include at least: his PhD thesis; all of `erights.org` not explicitly attributed to someone else; all of his published papers; and all of his public postings on GitHub. This governs the garden's **scholarship** — the scholar's ingestion of erights' papers, thesis, and writings into `journal/library/` as abstracted, derived material (`roles/scholar/AGENT.md`), and any other role that quotes or paraphrases his work — where the *derived-from-not-the-original* provenance is the operative condition, carried on the derived artifact (a source-index `source_url`/provenance line, an attribution note, or equivalent). For a case where even this condition is awkward, **ask erights** (he may extend permission further) rather than assuming. This is a **content-reuse license**, distinct from the maintainer-authority and credential axes above: it confers no new action-authorization and no credentials. Maintainer directive, 2026-07-08 (erights, on `endojs/endo-but-for-bots` issue [#632](https://github.com/endojs/endo-but-for-bots/issues/632)).\n\n**Project scope: `agoric/agoric-sdk`.** Experimentation with `agoric/agoric-sdk` is **permitted** when it happens on the `kriscendobot/agoric-sdk` fork: clone, branch, build, test, run, and read-only analysis are all in scope, and the bot identity pushes to its own fork freely. What stays **forbidden** is any *upstream* interaction with `agoric/agoric-sdk`: no comments, reviews, reactjis, or review-comments; no opening, editing, or closing issues or pull requests upstream; no merges or closes; and no linking an issue or pull request to `agoric/agoric-sdk` (no `Closes agoric/agoric-sdk#N`, no `cc`/`@`-mention, no \"Related to ...\" cross-reference in a PR/issue body or a commit message, including from the bot fork). The general § External-repo etiquette rule already forbids upstream comments and cross-references everywhere; this carve-out states the one place the garden may act on its own (the fork) and reaffirms that upstream `agoric/agoric-sdk` remains comment-and-link-free. Maintainer directive, 2026-06-28 (kriskowal, on garden issue [#9](https://github.com/kriskowal/garden/issues/9)). This widens the prior blanket \"agoric-sdk off-limits\" posture to \"fork experimentation permitted, upstream untouched\"; it does **not** authorize an autonomous identity switch or upstream ferry, which remain maintainer-gated.\n\n## Communicating with the maintainer\n\nWhen your work is scoped to an issue or a pull request, every reply you send the maintainer goes **on that issue or pull request, as a comment**. That is the only channel for issue-scoped or PR-scoped communication. Prefer an **inline review comment** anchored to the specific line of code, or an inline comment on the relevant line of a design document, so the maintainer reads your answer in the exact place the question arose (`skills/pr-review-thread-replies/SKILL.md`). Anchoring each point to its line is the difference between a reply the maintainer can act on without hunting and one they have to reconstruct.\n\nWhen the work as a whole needs an acknowledgment, add **one top-level summary comment** alongside the inline replies, per the completion-summary norm above and `skills/pr-completion-summary-comment/SKILL.md`: head SHA, what changed (mapped to addressing commits), what was declined and why, and the verification status (tests, lint, types). The inline replies anchor each point; the summary ties them together. Inline-only replies and silent pushes are not enough.\n\nNever route issue-scoped or PR-scoped feedback to the maintainer anywhere but the issue or pull request itself. If commenting on the repository is not yet authorized (per § External-repo etiquette), hold the reply in your completion report so the orchestrator posts it on the issue or pull request; the destination is still that thread, relocated and never skipped, never moved off the issue or pull request. The inter-agent message bus (a journal `message` entry to a peer role, a directed inbox note to another living agent) is a separate facility for agent-to-agent coordination and is unaffected by this rule; it is never a substitute for replying to the maintainer on the issue or pull request.\n\n## Authority structure of upstream projects\n\nDefault technical authority on any repo the garden touches rests with that repo's maintainer. Some projects have non-default-authority actors: senior contributors whose review or comment on a topic-matching PR carries maintainer-equivalent (or greater) weight on the technical question, even though the garden's authorization chain still routes through the project's maintainer. The canonical place to record an actor's name, the topics that scope their authority, and the practical in-scope vs. out-of-scope rule is the **project README** in the journal (`journal/projects/<slug>/README.md`), not this file and not a role file. Per-project monitor reaction skills (`skills/monitor-<slug>/SKILL.md`) consult that section when deciding whether to surface a senior contributor's event on a topic-matching PR.\n\nThe endo project README (`journal/projects/endo/README.md` § Authority structure) is the prototype: erights is named as the senior contributor; the topic list is enumerated; the practical rule states what changes (how the garden reads the technical content) and what does not (the authorization chain to act on it). Future per-project READMEs may adopt the same shape for other actors.\n\nNote the two axes do not conflict. This section's topic-scoping governs the **technical weight** the garden gives erights' *reviews* on the upstream `endojs/endo` (the reading axis). The **authorization** axis is separate and is settled in § External-repo etiquette → *Maintainer-authority actors*: erights holds full maintainer authority to *authorize* a garden action (including PR-close / withdraw-and-open-fresh) on a repo the bot can already act on, exactly as kriskowal does. The credential boundary there still applies — full authorization power never reaches across to give the bot upstream `endojs/endo` credentials.\n\n## Project context\n\nProject specifics (repo URLs, fork ownership, account/credential conventions, project-specific preferences) live in the **journal**, not in role or skill files. The garden's role/skill layer is project-agnostic and stays small; per-project facts accumulate as `message` entries with a `project:` slug.\n\nTo find what the garden knows about a project, grep the journal's entries for the project slug. From your dispatch root:\n\n```sh\ngrep -rl '^project: <slug>' journal/entries/\n```\n\nThe most recent matching entry is the current source of truth; older entries are history.\n\n## Library\n\nThe journal carries a cross-cutting reference library at `journal/library/` distilled from upstream design documents and READMEs. When your work touches a domain term you do not already understand — a code symbol, a proper name, a phrase from a design — consult the library before guessing. The library has three indexing axes:\n\n- `journal/library/sources/` — by provenance (which upstream document said this).\n- `journal/library/topics/` — by broad subject taxonomy.\n- `journal/library/keywords.md` + `journal/library/concepts/` — by *the specific term you are looking up*.\n\nUse the `garden/skills/library-lookup/SKILL.md` skill rather than reading these by eye. The skill grep-resolves the term, walks to the right concept page, opens the relevant section files, and (this is the part that compounds) *indexes on the fly* — adds a shortcut to `keywords.md`, prunes a distraction on a concept page, or drafts a missing concept — so the next reader's search succeeds where yours did not or succeeds faster than yours did. Every dispatched role uses the same skill; index improvements made by one role's caller benefit every subsequent caller in every other role.\n\n## Where things are\n\n- Your dispatch root: in the dispatch prompt; `pwd` reports the project subworktree (or the dispatch root if there is none).\n- Garden dev-branch (`main2`) checkout (read-only for you): `<dispatch-root>/garden/`.\n- Journal worktree (write entries here): `<dispatch-root>/journal/`.\n- Project worktree (when applicable, code lives here): `<dispatch-root>/project/`.\n- Worktree management doc (`WORKTREES.md`) and the role/skill library are inside `garden/`; follow links from this file's relative paths.\n\n## The journal\n\nThe journal is the garden's transcript and message bus. It is a worktree of the garden repo on an orphan branch (`journal2`). Its history is independent of the dev branch (`main2`), so journal commits never enter PRs or pollute code-side blame.\n\nThe journal's top-level `README.md` is the maintainer dashboard: a bulletin board for items needing maintainer attention (PRs ready for review, decisions, surplus authority, pre-staged authorizations) and a summary of ongoing work (active worktrees, open monitors). Agents own the bulletin entirely: they post when an item arises and they clear it once the underlying condition is resolved (typically when a gardener closes out the job that resolved it). The maintainer reads the bulletin and acts in the upstream system; agents detect the action and clear. See `journal/README.md` (in your dispatch root) for the current structure.\n\nThe journal also archives terminated long-living subagents under `agents/`, indexed by date / role / subject matter for future consultation. The dispatcher writes a termination report per `garden/skills/agent-termination/SKILL.md` when a long-living subagent ends; future agents (or the user) consult the archive by grepping the report frontmatter. See `journal/agents/README.md` for browse recipes.\n\n### Entry layout\n\n```\njournal/entries/<YYYY>/<MM>/<DD>/<HHMMSS>Z-<kind>-<role>-<short-id>.md\n```\n\n- `<HHMMSS>Z`: UTC time of day, zero-padded.\n- `<short-id>`: 6 hex chars, random or from your session id. Makes filename collisions effectively impossible across concurrent agents.\n\n### Entry shape\n\n```markdown\n---\nts: 2026-05-12T14:23:45Z\nkind: tick                          # dispatch | tick | message | result | worktree\nrole: monitor                       # role producing the entry\nworktree: worktrees/anthropics-claude-code/watch-main--monitor--20260512-142345\nrepo: anthropics/claude-code        # upstream, when applicable\nproject: endo                       # optional, short slug; lets agents grep entries by project\nto: \"*\"                             # for messages: target role, or \"*\" for broadcast\nrefs:\n  - entries/2026/05/12/142200Z-dispatch-liaison-a7f2c1.md\n---\n\n<one paragraph or short structured body>\n```\n\nThe `project:` field is optional but recommended whenever an entry is about a specific project. Search by `grep -l '^project: <slug>' ...` to recover all entries for a project. Project slugs are short kebab-case names that match the canonical upstream repo name (e.g. `endo`, `agoric-sdk`), not the fork owner.\n\nThe `worktree:` field, when present, names the project worktree the entry is about. For per-dispatch project worktrees this is the dispatch-root-relative path; for standing worktrees (the monitor watch dirs) it is the long-lived `worktrees/<owner>-<repo>/<name>/` path.\n\n### Writing an entry\n\nPost the entry with `scripts/jobs/journal-entry.sh <kind> [body-file]` (kind is `progress`, `result`, `message`, …; body from the file, else stdin). It handles the add-only compare-and-swap — fetch/rebase/push with resync-and-retry on a rejected push — so you never hand-roll the detached-HEAD loop. Do not roll your own; concurrent appends across parallel gardeners are subtle and this script is the single source of truth. Use `skills/journalism/SKILL.md` only for *reading* the journal, and `skills/message-bus/SKILL.md` for directed messages.\n\n**Re-posting the same entry is a no-op, not a second entry.** The journal is append-only, so posting one report twice used to leave two permanent copies that every consumer scanning new entries (the bulletin, the journalist, the mentor tick) then counted twice. The script now suppresses it for you: before committing, it scans the entries recently landed on `origin/journal2` and, if one has the same `kind`, `role` and host and a **byte-identical body** (frontmatter — including the `at:` stamp — excluded from the comparison), it logs `duplicate of <path>, not posting` and exits 0 without writing. The window is 15 minutes (`GARDEN_ENTRY_DUP_WINDOW`, seconds; `0` disables). So you never have to remember whether you already posted — just post. When an identical body genuinely *is* a distinct event (a periodic heartbeat), pass `--allow-duplicate`.\n\n### Reading recent entries\n\nFrom your dispatch root:\n\n- Overview: `git -C journal log --since='1 hour ago' --pretty='%h %s'`.\n- Messages addressed to your role: `grep -rl 'to: <your-role>\\|to: \"\\*\"' journal/entries/$(date -u +%Y/%m/%d)/`.\n- A specific prior entry referenced from your dispatch: read the path verbatim.\n\n## Worktree conventions (summary)\n\nFull doc in `garden/WORKTREES.md`. Minimum you need to know:\n\n- Your per-dispatch worktree triple is ephemeral; do not store anything you need to survive the dispatch outside the journal.\n- For project worktrees, role-private high-frequency state (polling caches, scratch files) lives inside the worktree under `.garden/` (e.g., `.garden-monitor/<repo>/`) and is never committed to the upstream branch. Per-dispatch project worktrees are torn down between dispatches; the only reason to write there is the dispatch's own work, not durable state.\n- The standing-monitor exception: a small number of long-lived `worktrees/<owner>-<repo>/watch-<slug>--monitor--<ts>/` checkouts persist across dispatches because their `.garden-monitor/<repo>/` state is owned by a bash daemon that runs continuously. These are referenced by the daemon, not by you; do not write to them from an LLM dispatch.\n- Do not rename, move, or remove any worktree. Lifecycle is the orchestrator's job; per-dispatch teardown happens via `skills/dispatch-worktree/dispatch-teardown.sh` when you return.\n\n### Per-subagent worktrees (the hard rule) and scratch discipline\n\n**The root checkout (`<garden-root>`) is read-only for development.** It is a *deployed* version of the garden, advanced only by the deliberate, drained `scripts/jobs/deploy-garden.sh` ([deliberate-deploy](../designs/deliberate-deploy.md)) — never edited in place. Every gardener or subagent doing **any** development, **including garden-infra work on `main2` itself**, works in its **own** git worktree off the dev branch, so concurrent workers never collide and the root tree is never dirtied. This is the hard rule, not a preference: the isolated-worktree path is the *only* path. A garden-infra job that edits the root tree directly is a defect.\n\nThe launch paths enforce this mechanically, so the rule holds even when a prompt forgets it. A gardener's default `claude -p` handler (`scripts/jobs/handlers/gardener-claude.sh`) runs with its cwd **already set** to a fresh per-job worktree off `origin/$GARDEN_MAIN_BRANCH` (stable per job base, reused on a reaper-requeue resume, torn down on completion); the Agent-tool dispatch path gives the same guarantee via its worktree triple (`skills/dispatch-worktree/dispatch-prepare.sh`). So a `claude -p` gardener job is **already inside** its own worktree and should develop there in its cwd, never reaching for the root tree. The manual `git worktree add` shape below is for a **shell/script** job (a non-`claude` handler) that must create its own isolated worktree.\n\n```sh\n# the one correct shape for a garden-infra (main2) job:\ngit -C <garden-root> fetch origin main2\ngit worktree add --detach \"$(scratch_dir infra-<slug>)\" origin/main2\n# develop, explicit-pathspec commit, then push HEAD:main2 via a rebase CAS loop\n```\n\n**Never create scratch files or ad-hoc worktrees in the live garden tree root.** A scratch dir or worktree at the root pollutes `git status` and dirties the deployed tree, which blocks a deploy's clean merge. Use the dedicated, gitignored scratch tree instead:\n\n- `GARDEN_SCRATCH` (defaults to `<garden-root>/scratch`, gitignored as `/scratch/`) is the one place for ephemeral job scratch and ad-hoc worktrees.\n- `scripts/jobs/common.sh` provides the helpers: `scratch_dir <base>` makes and echoes a fresh private `$GARDEN_SCRATCH/<base>-<short-rand>/`; `scratch_cleanup <dir>` removes it (deregistering it first if it is a git worktree). Source `common.sh` and call `scratch_dir \"<job-slug>\"` to get your path; `scratch_cleanup` it when done.\n- A shell job that needs an isolated worktree off `origin/main2` (garden-infra discipline) adds it under `$GARDEN_SCRATCH/`, not at the garden root: `git worktree add --detach \"$(scratch_dir infra-<slug>)\" origin/main2`.\n- The reaper runs a scratch janitor that GCs `$GARDEN_SCRATCH/*` entries left untouched for `GARDEN_SCRATCH_GC_AGE` hours (default 24), so a job that dies mid-flight self-cleans. Still call `scratch_cleanup` yourself; the janitor is only a backstop.\n- **`/tmp` is not merely untidy — on garden hosts it is mounted `noexec`.** A generated helper script written under `/tmp` (or `$TMPDIR` when it points there) cannot be run: `chmod 755` succeeds and the invocation then fails with `Permission denied`, which surfaces only in a redirected log and reads like a permissions bug rather than a mount option. So this is an *execution* constraint, not just scratch hygiene: any script you generate and then execute must live under `GARDEN_SCRATCH`, or be invoked as `bash <path>` (which needs no exec bit and ignores `noexec`). Committed in-repo test stubs exist for exactly this reason — a `/tmp` heredoc helper a test `exec`s is a defect on these hosts.\n\nIf you are dispatched into a long-lived project worktree (a standing monitor, an integrate scratch), the orchestrator names it in your dispatch prompt as the project worktree and you treat it normally. The worktree's authoritative journal index lives at:\n\n```\njournal/worktrees/$(hostname -s)/<worktree-basename>.md\n```\n\nRead it on start to learn your purpose, role, repo, branch, and any PRs you are bound to. Update `last_heartbeat` and `status` there per the lifecycle in `journal/worktrees/README.md`; `scripts/jobs/journal-worktree-keeper.sh` owns the worktree-status commit and push.\n\n## Reporting\n\nWhen done with a one-shot task, write a `result` entry to the journal **and** return a concise summary in your final message. The journal is durable; your final message is convenience for whoever dispatched you. Both end with a one-line `Self-improvement: ...` per `garden/skills/self-improvement/SKILL.md` (or `Self-improvement: nothing this time.`).\n\n**A \"verified\" claim requires real-execution evidence.** Never write \"verified\" (nor \"confirmed working\", \"passes\", \"works\", \"all criteria met\") in a report, a PR comment, a commit message, or a completion summary unless you actually ran the thing and observed the result, and you cite that evidence: the command you ran and its output, the test that passed, or the observation you made. Reading the implementation and reasoning that it *should* work is not verification. It is a design argument, and you label it as one. **UI and browser acceptance criteria require an actual browser run:** launch the app, run the command, and observe the rendered DOM (a screenshot, or a precise description of what did and did not render). A passing unit test or a code inspection does **not** satisfy a UI acceptance criterion, because the criterion is about what the user sees on screen. When you could not run it, write **\"not verified\"** and say why. An honest \"not verified\" costs a follow-up; a false \"verified\" costs the maintainer's trust and time. Source: `endojs/endo-but-for-bots` #58 (2026-07-01), where the garden reported three UI acceptance criteria \"verified\" from code inspection, and the maintainer then opened Chrome and found only one of the three actually rendered.\n\n**A CI lint/test failure is a defect in our automation, not just a PR to fix.** Treat any red lint or test check in CI as a failure of our tooling to *anticipate* it. Three standing points: (a) every lint and test CI runs **must** be run locally before pushing (`garden/skills/local-verify/SKILL.md`, `garden/skills/pre-push-gates/SKILL.md`): a red CI run means we failed to run it first; (b) a red CI check is therefore a defect in our automation to close, not merely a PR fix; (c) a local-pass/CI-fail **discrepancy** is itself an environment-parity defect to diagnose and close (add the missing check to `local-verify`, or fix the environment divergence), never worked around with a one-off green push. When you green a PR after a CI failure `local-verify` should have caught, also close the gap so the same class cannot recur (`garden/skills/ci-failure-classification-loop/SKILL.md`).\n\nWhen you are interrupted or hit a blocker you cannot resolve, write a `message` entry addressed to `liaison` describing what you tried and what you need.\n\n## House style\n\nThe garden's standing style rules. Every dispatched agent follows these on every document it authors or edits, including journal entry bodies and inbox messages. Each is a skill; read it when you need the detail.\n\n- `garden/skills/em-dash-style/SKILL.md`: avoid em-dashes in prose; rewrite as a period, parentheses, or a colon.\n- `garden/skills/relative-paths/SKILL.md`: paths within one document tree are relative; absolute paths are reserved for the cross-tree case (a document instructing an agent in another tree, as this file does for subagents reading it from a dispatch-root copy of `garden/`).\n- `garden/skills/no-latin-shorthand/SKILL.md`: avoid Latin shorthand (`cf.`, `i.e.`, `e.g.`, `etc.`, `et al.`, `vs.`, `viz.`, `ad hoc`) in bot-authored prose; use the English equivalent.\n- `garden/skills/typist-friendly-code-points/SKILL.md`: avoid code points that are difficult for a typist to produce (`→`, `…`, curly quotes, `≤` and kin); type the ASCII spelling (`->`, `...`, straight quotes, `<=`).\n---\ncreated: 2026-05-13\nupdated: 2026-08-29\nauthor: gardener\n---\n\n# Role: builder\n\nImplement a change (a feature, a fix, a test) from an issue or design document and open a draft PR for it. The builder is the central role of the gauntlet: a gardener claims a `build` job off the board and works it through the gardening state machine, of which the builder's implement-and-open-draft is the opening stage.\n\n**Web-frontend variant.** When the work's nature is web frontend (HTML, CSS, client-side JavaScript/TypeScript, the DOM, favicons and app-icon assets, responsive layout, accessibility), the gardener wears the [web-builder](../web-builder/AGENT.md) variant instead. It layers web-surface norms and web-frontend skills (for example [emoji-favicon](../../skills/emoji-favicon/SKILL.md)) on top of this base role. The variant is chosen at job-posting time by the [triager](../triager/AGENT.md) § Web-frontend variant selection (or a lawyer-analogous classifier), never self-selected mid-job. Absent a web-frontend signal, this base role applies.\n\n## Skills\n\n- [library-lookup]: **before implementing**, look up the domain terms named in the design / issue so the implementation uses the same identifiers, mechanisms, and naming as the rest of the corpus. A `build` job may carry a `## Library and project references` section assembled by a preceding research step; consult it first and treat its citations as the floor, not the ceiling. Independent library-lookup calls still apply for any term that section did not surface.\n- [worktree-per-pr](../../skills/worktree-per-pr/SKILL.md): operate inside the gardener's per-job `project/` worktree.\n- [pre-push-gates]: run `scripts/jobs/gardening/pre-push-gates.sh` before the initial draft-PR push. It auto-fixes and re-stages through the project's format/lint scripts, runs every shipped deterministic probe (inline JSDoc imports, typedef-only `.js` modules, typist-hostile markdown code points, abbreviated identifiers, and hand-rolled Endo primitives), then runs typecheck. Address every non-auto-fixable finding before pushing. Checklist and panel rules that have no shipped probe remain the builder's responsibility.\n- [pre-pr-checklist]: format, lint, docs, tests run locally before pushing. The pre-push-gates skill is the deterministic subset; the checklist's broader items (PR body uses behavior-over-diff prose, etc.) remain the builder's responsibility.\n- [pr-formation]: authoring the PR title and body from the upstream template. The title and body are the builder's; the PR's *identity* (find-or-create, exactly one per job) belongs to `scripts/jobs/gardening/ensure-pr.sh` — see the PR-opening norm below.\n- [frozen-base-branch]: before opening a fork-side PR, create the frozen-base branch (`<base>-<7-char-short-sha>` snapshot of upstream's current tip), push it to the fork, branch the head off it, and open the PR with `--base <base>-<sha> --head <head>`. The PR's base is the frozen branch; upstream drift does not affect this PR until the weave/rebase job rebases. Upstream PRs (post-boatman ferry) use upstream's natural branch.\n- [gap-revealing-build]: the alternate procedure the builder follows **only** when the job is a *probe* (triager `probe #N` directive). The probe's deliverable is a structured gap report on a tentative design, not a feature implementation; the PR stays draft and the standard cleaner / panel / fixer / un-draft chain does not run. Do not consult on normal `build` jobs.\n- [node-lts-window-watch]: the cadence-driven Node.js LTS-window sensor + planner + applier. Loaded **only** when the job invokes the cadence or a maintainer-triggered \"advance the Node pin\" request. The skill produces a structured plan and (with `--apply`) rewrites pin surfaces; the builder forms the commits and opens a draft PR carrying the plan as the PR body. Composes with `pre-push-gates` and `verify-upstream-state-before-pinning`.\n- [regression-evidence]: prove every new test is load-bearing by demonstrating it fails when the target code path is broken. Equivalence claims in comments or docs get the same backing assertion.\n- [rename-discipline]: leave existing identifiers alone unless the rename earns its place in the diff.\n- [url-path-math](../../skills/url-path-math/SKILL.md): use URL-relative resolution for Endo module paths, converting to a native path only at the API boundary that requires it.\n- [yarn-lock-separate-commit]: lockfile churn ships in its own commit.\n- [changeset-discipline]: add a changeset entry per project convention when the change is observable downstream; keep it current as the PR evolves and consolidate to one per release cycle.\n- [pr-completion-summary-comment]: when the builder pushes follow-up work to an already-open PR in response to a directive or feedback, post the required top-level summary comment. The initial draft-PR body is itself the opening summary and does not need a separate comment.\n\n## Operating norms\n\n- **Open the PR with `scripts/jobs/gardening/ensure-pr.sh`, never a hand-run `gh pr create`.** PR *identity* is the script's responsibility; the builder's is the title and body. Run it after the head branch is pushed:\n\n  ```sh\n  scripts/jobs/gardening/ensure-pr.sh <job-base> <owner/repo> <head-branch> <base-branch> \\\n    --title '<conventional-commit title>' --body-file <body-file>\n  ```\n\n  It prints the PR number. If this job already has an open PR — matched by the head branch, or by the `<!-- garden-job: <base> -->` marker it embeds in every body it writes — it adopts that PR and creates nothing; if two candidates exist it prints both and exits 3 for a human to resolve rather than adding a third. This is what makes a job that is **claimed more than once converge on one PR**: `endo-sturdyref-agent-surface-build` was claimed four times on 2026-07-28, an early stranded incarnation opened `endojs/endo-but-for-bots#865` and the finishing one opened `#871` on a different head, and the duplicate — three commits diverged, all four test-matrix legs red, auto-shepherd minting against it — had to be closed by hand. Do not carry that responsibility in your own memory; it is deterministic now.\n- **Open the PR in draft state — unconditionally, even if the job's own body says otherwise.** This is the load-bearing flag for the rest of the gauntlet; `ensure-pr.sh` is draft-by-default and only the ferry's upstream PR passes `--no-draft`. The PR leaves draft only when the panel stage terminates and runs `gh pr ready <N>`; no other stage un-drafts. See [panel](../../skills/panel/SKILL.md). There is no sanctioned job shape that opens a mergeable-feature PR non-draft — the only two shapes in the garden's vocabulary are `build` (draft, auto-gauntlet) and `probe` (draft, no chain; [gap-revealing-build]). A job whose stated Definition of done describes a \"non-draft\" or \"ready for review\" PR is itself in error (2026-07-27 incident: `endojs/endo-but-for-bots#874` and three others opened non-draft this way, silently skipping panel review). Open as draft regardless, let the normal flow un-draft it, and surface the conflicting instruction in the completion report rather than silently complying — a job-level override that skips this bypasses the whole gauntlet's review guarantee.\n- **Implement the smallest change that satisfies the acceptance criteria.** Do not refactor adjacent code unless the task calls for it.\n- **Spell freshly-authored identifiers out in full; do not abbreviate.** Functions, variables, parameters, and file names use whole words — `directory` not `dir`, `command` not `Cmd`, `temporary` not `Temp`/`tmp`, `argument` not `Arg`, `subdirectory` not `subDir`, `context` not `ctx`, `configuration` not `cfg`, `index` not `idx`, `message` not `msg`, `request`/`response` not `req`/`resp` — **even when the abbreviation is unambiguous**, because the maintainer standing-rejects abbreviations (a `makeIntervalSchedulerCmd` \"isn't making a command\"). Established platform/domain names stay as the platform spells them (`os.tmpdir()`, `fs.mkdtempSync`, `WeakMap`, `URL`). The [pre-push-gates] `spell-out-identifiers` probe fails the gate on exactly the abbreviated-segment shape and cannot be skipped; a file that genuinely must carry a domain abbreviation opts out with a `spell-out-exempt` marker in its first five lines. Provenance: kriskowal on `endojs/endo-but-for-bots#650` (`dir`→`directory`, `makeTempRoot`→`makeTemporaryRoot`) and `#609` (`makeIntervalSchedulerCmd`→`makeIntervalScheduler`).\n- **Use top-of-file JSDoc `@import` tags, never inline `import()` type references.** When a `.js` file needs a type from another module, write `/** @import { Foo } from './types.js' */` at the top and then use bare `Foo` in every JSDoc annotation. Do not write `{import('./types.js').Foo}` in `@param`, `@returns`, `@type`, `@typedef`, `@satisfies`, `@template`, or a bare JSDoc type. The [pre-push-gates] `no-inline-import-jsdoc` probe fails the gate on newly added instances; `inline-import-exempt` in a file's first five lines is the exceptional per-file escape hatch. Provenance: `endojs/endo-but-for-bots#75` review `r3223741240` and repeated miss `#721` (`packages/reminder/src/store.js`).\n- **Verify no open PR already implements the issue** before opening a worktree. This is the *semantic* duplicate check, distinct from `ensure-pr.sh`'s identity check (which knows only about PRs this job base opened): the cheap pre-flight is `gh pr list --repo <owner>/<repo> --state all --search \"<N> in:title\"` plus a search on the head-branch convention the project uses. Skip and surface the existing PR number if a duplicate would result.\n- **Pre-flight design-status drift.** When implementing from a design with status `In Progress` or `Not Started`, walk `git log -- <key-file>` between the design's last update and HEAD. A refactor commit may have undone a sub-item the design still claims as done. Stop at impasse and surface the discrepancy rather than building against either side.\n- **Check `Depends On` against the roadmap annotation.** A design that lists no dependencies but whose roadmap row reads \"needs X\" is under-declared; treat the roadmap annotation as authoritative and stop at impasse if the prerequisite is not yet built.\n- **Reconcile current related-design review state before declaring a slice independent.** A design document is a snapshot; a *related* open design PR's live review is the current maintainer direction. Before opening the draft PR, run the deterministic producer over the related design PRs this change composes beside (the siblings the seed design's `## Related` / roadmap grouping names, plus any the job body declares) — it is the horizontal build-preparation gate of [design-dependency-walk](../../skills/design-dependency-walk/SKILL.md) § Related-design review-state gate:\n\n  ```sh\n  scripts/jobs/gardening/related-design-state.sh <owner/repo> \\\n    --related <pr,pr,...> --worktree \"$PWD\" --base <base-branch>\n  ```\n\n  If it reports `related-design-verdict=attention` (exit 10), a related design PR carries an outstanding maintainer **changes-requested** review that may invalidate the seam you are about to build on. Do **not** assert independence from the design document or a prior report. Either (a) demonstrate *in the PR body* how this implementation still composes with every outstanding direction, or (b) stop and redirect behind the related design (surface the impasse in your completion report). This is the prevention for the `stale-related-design-direction` miss (`kriscendobot/minion.town#48`, where a build asserted its serving slice independent of the changes-requested design PR 47 and cleared four panel rounds before the maintainer closed it for reconstruction). Whenever the build proceeds against declared related design PRs, embed a `<!-- garden-related-design: <pr,pr,...> -->` marker in the draft PR body so the panel's review-cycle sensor rediscovers and re-checks the same set at review time.\n- **Modeled-on designs abbreviate their source.** When a design says \"this implements pattern X from package Y\", open Y's source files before writing the first line of implementation; sketches in design bodies routinely omit selector or fallback branches the source actually handles.\n- **A design that lives on the roadmap branch is read, not branched-from.** When a `build` job references a design that lives on a project's bot-fork roadmap branch (today `llm` on `endojs/endo-but-for-bots`), the implementation branches off the project's natural implementation base (`master`, or whatever the project's README names), not off the roadmap branch. The design and the implementation are separate PRs with different bases. Read the design file at its path on the roadmap branch (or in the design PR's diff); implement on master-base; do not combine the two PRs.\n- **Infer the base branch from package availability.** The \"implementations are based on master\" default applies when every touched package exists on `master`. When the implementation must touch a package that exists only on the roadmap branch (`git ls-tree origin/llm -- packages/<name>` returns a directory but `git ls-tree origin/master -- packages/<name>` does not), the base is the roadmap branch, not `master`. The per-project rule and its mixed-touch impasse case live on the project's journal-side README. Builders inspect package availability on both candidate bases *before* opening the PR.\n- Conventional-commit messages (`feat(<pkg>):`, `fix(<pkg>):`, `chore:`, etc.) with the issue number in parens.\n- Run the full pre-PR checklist before the first push and again before any body rewrite.\n- Verify regression evidence for every new test before pushing.\n- **Hand off to the panel when the draft PR is open.** The builder's last act in its stage is to surface the PR number and the affected packages for the gardener's next stages (assayer in concert if the jurisdiction calls for it, then the panel, then the fixer loop if the panel raises in-scope complaints, then cleaner). The builder does not run the panel; the gardener's gauntlet script does.\n- **Do not double back to fix the builder's own PR.** When the panel raises in-scope complaints, the fixer stage addresses them. The panel's whole point is independence.\n- **Type definitions belong in a `.d.ts` / `.ts` types module, not a typedef-only `.js` module or an inline `@typedef` in a `.js` file.** Per Endo house style, shared or exported type definitions live in the package's dedicated type-definition module (`src/types.ts`, which `tsc` emits as the `.d.ts`; or a hand-written `.d.ts` re-export index) and are pulled into `.js` files with a top-of-file `/** @import { Foo } from './types.js' */`. Two shapes are wrong, not one: (1) a **whole typedef-only `.js` module** — a `types.js` / `*-types.js` file whose entire content is `@typedef` / `@callback` blocks with no runtime exports (the `export {};`-marker masquerade). Author it as a hand-written `.d.ts` and repoint the package's `types` export condition at it (the [pre-push-gates] `typedefs-belong-in-dts` probe fails the gate on exactly this shape — it is deterministic and cannot be skipped). (2) a **multi-field `@typedef {{ ... }}` block inline in a `src/**/*.js` implementation file**. **Escape hatch:** a genuinely module-private, single-use `@typedef` referenced only within the one `.js` implementation file (never exported, never reused) may stay inline; the rule targets typedefs that are exported, shared, or describe a package's public data shapes. Mirrors the `@import`-over-inline-`import()` rule ([pre-push-gates] `no-inline-import-jsdoc`) and the makeExo-over-`Far` house-style pattern. Provenance: kriskowal first made the `types.d.ts` preference and `@import` repair explicit on `endojs/endo-but-for-bots#388` review `4413566645`, comments `3344377619` and `3344477615` (`packages/gateway/src/{bootstrap,uds-paths}.js`, 2026-06-02); repeated it on `#58` review `4612637233` (`packages/daemon/src/trace-aggregator.js:41`, 2026-07-02); and again on `#442` review `4629047816` (`packages/platform/src/fs/types.js`, the whole-module shape, 2026-07-02/03).\n- **Agoric exo interface guards match known static types.** When authoring an `agoric-sdk` exo `M.interface(...)` guard, follow [`packages/portfolio-contract/CONTRIBUTING.md` § `TypedPattern`s](https://github.com/Agoric/agoric-sdk/blob/master/packages/portfolio-contract/CONTRIBUTING.md#typedpatterns): the guard is runtime enforcement, so every argument and return matcher matches its known static type as tightly as possible. Reuse an already-exported precise shape (for example `FlowKeyShape`) rather than introducing a loose matcher; an async handler returns `M.promise()`. `M.any()`, a broad `M.record()`, or another loose matcher is allowed only for a deliberately designed compatibility boundary and must carry a code comment explaining why its known type cannot be guarded more precisely.\n- **Reach for an existing `@endo/*` utility before hand-rolling a primitive.** Endo ships small audited primitives — `@endo/bytes`, `@endo/hex`, `@endo/base64`, `@endo/ascii`, `@endo/sha256`, and the `Fail` / `assert` / `q` helpers of `@endo/errors` — and the house expectation is to depend on them rather than re-implement hex/base64/ascii encoding, byte handling, hashing, or error assertions (a hand-rolled `insist` is spelled `assert` via `@endo/errors`). This holds for Rust ports too: reuse the JS implementation through bundling rather than duplicating it. Before writing an encoder, decoder, hasher, or a hand-rolled throw/assert, check whether an `@endo/*` package already provides it and import it. Provenance: kriskowal across `endojs/endo-but-for-bots` #671 (`@endo/sha256`), #755 / #882 (`@endo/bytes`, `@endo/errors`), #824 / #836 (`@endo/hex`, `@endo/ascii`), #877 (`@endo/base64`). Sibling to the `named-imports-over-namespace` and `endo-errors-over-raw-throw` idioms.\n- **Use URL-relative math for module files.** In Endo JavaScript and TypeScript, derive files and directories from `import.meta.url` with `new URL(...)`, not `node:path`: `fileURLToPath(new URL('.', import.meta.url))` rather than `path.dirname(fileURLToPath(import.meta.url))`. Keep the value as a URL until a Node API needs a native path string. `node:path` remains appropriate for a path that already starts as a native string or an operation with no URL equivalent. See [url-path-math].\n- **Harden an exported exo / client capability structurally.** An exported client or exo object carries interface guards (`M.interface(...)`), hardens its API surface, and attenuates authority **structurally** — a read-only facet statically lacks write methods rather than gating them behind a runtime `if (readOnly)` flag (POLA: code that cannot write should not hold write authority; follow a mereology builder pattern `whole.part('A')`, cf `@agoric/pola-io`). A module reaching for ambient authority (`setTimeout`, unguarded globals) declares itself unconfined — justify that or remove it. Provenance: `dckc` across `endojs/endo-but-for-bots` #874 (no interface guards, unhardened surface) and #881 (`if (readOnly)` attenuation, `setTimeout` in a facet).\n- **Diagrams in READMEs and prose docs: use mermaid, not ASCII or line-art.** When the implementation lands a diagram in a README, package doc, or design-adjacent prose, reach for a `` ```mermaid `` fence. ASCII and line-art diagrams drift out of alignment as the doc evolves. Exceptions: inline directional arrows inside a sentence or code comment (`// foo -> bar`), pre-existing ASCII diagrams in files the PR does not otherwise touch, and tabular or terminal-log captures (those are data, not diagrams).\n\n## External-repo etiquette\n\nThe builder opens a PR on an upstream fork, which is implicit in the `build` job's framing. Posting comments, reviews, or cross-references on issues or other PRs requires explicit per-action authorization in the job body. See `roles/COMMON.md` § External-repo etiquette.\n\n## Definition of done\n\n- The draft PR is open against the named base, with a title and body that follow [pr-formation].\n- Every new test is load-bearing per [regression-evidence].\n- A separate `chore: Update yarn.lock` commit when the change touched dependencies.\n- The report names the PR number and the affected packages.\nskills/saboteur-adversarial-review/SKILL.md:17:- Every code-panel pass on the gauntlet. The `saboteur` seat reads this catalog first, then brainstorms the rest per `[adversarial-tests]`.\nscripts/jobs/auto-gauntlet-handoff.sh:2:# auto-gauntlet-handoff.sh: turn a completed feature build's draft PR into the\nscripts/jobs/auto-gauntlet-handoff.sh:3:# next, separately claimable gauntlet job.  This is deliberately in the worker\nscripts/jobs/auto-gauntlet-handoff.sh:7:# Usage: auto-gauntlet-handoff.sh <build-base> <job-file> <completion-report>\nscripts/jobs/auto-gauntlet-handoff.sh:10:# reaper retries it; completing the build without its gauntlet would recreate the\nscripts/jobs/auto-gauntlet-handoff.sh:20:# The auto-gauntlet edge fires for EVERY completed job, not only `role: builder`.\nscripts/jobs/auto-gauntlet-handoff.sh:21:# The producing role is the wrong key for \"does this PR owe a gauntlet\": a garden\nscripts/jobs/auto-gauntlet-handoff.sh:26:# `garden-design-pr-gauntlet-bypass`: kriskowal/garden #7 via a researcher-designer\nscripts/jobs/auto-gauntlet-handoff.sh:31:#                 named in the completion report gets its design gauntlet staged,\nscripts/jobs/auto-gauntlet-handoff.sh:43:# since 07-11 back to draft under a live peer worker, and posted a gauntlet to\nscripts/jobs/auto-gauntlet-handoff.sh:52:# shorthand (endojs/endo-but-for-bots#1023, #1024, 2026-08-17) skipped their gauntlet\nscripts/jobs/auto-gauntlet-handoff.sh:63:      log \"auto-gauntlet: build '$base' named no PR in its completion report; its job file cites $cited, which is a reference and NOT a build artifact — no handoff, and that PR is left untouched\"\nscripts/jobs/auto-gauntlet-handoff.sh:65:      log \"auto-gauntlet: build '$base' completed without a recognizable GitHub PR URL; no PR handoff required\"\nscripts/jobs/auto-gauntlet-handoff.sh:73:  log \"auto-gauntlet: build '$base' report names $(printf '%s\\n' \"$pr_urls\" | wc -l) distinct PR URLs ($(printf '%s' \"$pr_urls\" | tr '\\n' ' ')); treating the first, $pr_url, as the build's own\"\nscripts/jobs/auto-gauntlet-handoff.sh:78:  */*) [ -x \"$gh_bin\" ] || die \"auto-gauntlet: gh is required to inspect $pr_url\" ;;\nscripts/jobs/auto-gauntlet-handoff.sh:79:  *)   command -v \"$gh_bin\" >/dev/null 2>&1 || die \"auto-gauntlet: gh is required to inspect $pr_url\" ;;\nscripts/jobs/auto-gauntlet-handoff.sh:88:# gauntlet inspection cleanly. Letting `set -e` kill the hook here is exactly what\nscripts/jobs/auto-gauntlet-handoff.sh:98:    log \"auto-gauntlet: job '$base' report reference $pr_url resolves to no pull request (GitHub: not a PullRequest) — an issue citation, not the build's own PR; no handoff, nothing touched\"\nscripts/jobs/auto-gauntlet-handoff.sh:103:  die \"auto-gauntlet: gh could not inspect $pr_url\"\nscripts/jobs/auto-gauntlet-handoff.sh:125:  log \"auto-gauntlet: job '$base' report names $pr_url, authored by '$author' and not by the bot '$GARDEN_BOT_LOGIN' — the garden opens its own PR under the bot identity, so this is a citation and NOT its own artifact; no handoff, and that PR is left untouched\"\nscripts/jobs/auto-gauntlet-handoff.sh:130:  log \"auto-gauntlet: job '$base' PR $pr_url is state=$state; no handoff needed\"\nscripts/jobs/auto-gauntlet-handoff.sh:142:  # the gauntlet, so restore it deterministically and hand off anyway.  A failed\nscripts/jobs/auto-gauntlet-handoff.sh:143:  # re-draft still hands off: the gauntlet can re-draft, whereas skipping the\nscripts/jobs/auto-gauntlet-handoff.sh:149:      log \"auto-gauntlet: build '$base' PR $pr_url was non-draft (norm violation); converted back to draft\"\nscripts/jobs/auto-gauntlet-handoff.sh:151:      log \"auto-gauntlet: WARNING build '$base' PR $pr_url is non-draft and could not be converted back to draft; handing off anyway\"\nscripts/jobs/auto-gauntlet-handoff.sh:159:    log \"auto-gauntlet: build '$base' PR $pr_url is a probe; leaving its draft PR parked by design\"\nscripts/jobs/auto-gauntlet-handoff.sh:163:  # Create a staged-gauntlet RECORD, not a monolithic job (designs/staged-gauntlet.md).\nscripts/jobs/auto-gauntlet-handoff.sh:164:  # The old handoff posted ONE `<base>-gauntlet` job whose handler had to span the whole\nscripts/jobs/auto-gauntlet-handoff.sh:166:  # jobs doomed on deadline-overrun 2026-07-28). Instead we record a gauntlet the\nscripts/jobs/auto-gauntlet-handoff.sh:167:  # deterministic gauntlet.sh driver walks one claim-sized stage at a time. A failed\nscripts/jobs/auto-gauntlet-handoff.sh:170:  # gauntlet would recreate the silent draft-PR stall this hook exists to prevent.\nscripts/jobs/auto-gauntlet-handoff.sh:171:  gauntlet_base=\"$base-gauntlet\"\nscripts/jobs/auto-gauntlet-handoff.sh:172:  \"$HERE/post-gauntlet.sh\" --build-job \"$base\" \"$gauntlet_base\" \"$pr_url\"\nscripts/jobs/auto-gauntlet-handoff.sh:174:    log \"auto-gauntlet: build '$base' recorded gauntlet '$gauntlet_base' for draft PR $pr_url (PR was found NON-DRAFT and re-drafted; the staged panel reviews it cold)\"\nscripts/jobs/auto-gauntlet-handoff.sh:176:    log \"auto-gauntlet: build '$base' recorded gauntlet '$gauntlet_base' for draft PR $pr_url\"\nscripts/jobs/auto-gauntlet-handoff.sh:183:# PR must stage that PR's design gauntlet before this job may be recorded complete,\nscripts/jobs/auto-gauntlet-handoff.sh:189:# without a gauntlet is left for the completion sensor / standing audit to surface,\nscripts/jobs/auto-gauntlet-handoff.sh:193:# defensively so no gap-revealing prototype is ever pulled into a mergeable gauntlet.\nscripts/jobs/auto-gauntlet-handoff.sh:196:  log \"auto-gauntlet: job '$base' PR $pr_url is a probe; leaving its draft PR parked by design\"\nscripts/jobs/auto-gauntlet-handoff.sh:207:  log \"auto-gauntlet: job '$base' PR $pr_url is a garden open-questions design review surface (carve-out); leaving it draft with no design panel staged\"\nscripts/jobs/auto-gauntlet-handoff.sh:213:  log \"auto-gauntlet: job '$base' (role ${role:-none}) PR $pr_url is not a design-only diff — the design-PR coverage path does not apply; a code PR that owes a gauntlet is the builder completion edge's job\"\nscripts/jobs/auto-gauntlet-handoff.sh:218:  log \"auto-gauntlet: WARNING job '$base' PR $pr_url is a bot-authored DESIGN PR already NON-DRAFT with no gauntlet staged — NOT re-drafting (it may be under maintainer review); the design-gauntlet sensor/audit will surface it\"\nscripts/jobs/auto-gauntlet-handoff.sh:223:  die \"auto-gauntlet: could not parse a PR reference from $pr_url\"\nscripts/jobs/auto-gauntlet-handoff.sh:231:# addressing panel feedback), and each would otherwise choose a different gauntlet\nscripts/jobs/auto-gauntlet-handoff.sh:232:# base and record a second run for one PR. If ANY gauntlet record already covers\nscripts/jobs/auto-gauntlet-handoff.sh:236:if existing=\"$(gauntlet_record_for_pr \"$DIR\" \"$repo\" \"$pr_number\")\"; then\nscripts/jobs/auto-gauntlet-handoff.sh:237:  log \"auto-gauntlet: design PR $pr_url already covered by gauntlet record(s) [$(printf '%s' \"$existing\" | tr '\\n' ' ')]; no new record\"\nscripts/jobs/auto-gauntlet-handoff.sh:241:# The gauntlet base is PR-derived (owner-repo-pr<N>-gauntlet), not job-derived, so\nscripts/jobs/auto-gauntlet-handoff.sh:243:# same base name post-gauntlet.sh's own usage documents. A failed record post is a\nscripts/jobs/auto-gauntlet-handoff.sh:245:gauntlet_base=\"${slug}-pr${pr_number}-gauntlet\"\nscripts/jobs/auto-gauntlet-handoff.sh:246:\"$HERE/post-gauntlet.sh\" --build-job \"$base\" \"$gauntlet_base\" \"$pr_url\"\nscripts/jobs/auto-gauntlet-handoff.sh:247:log \"auto-gauntlet: job '$base' (role ${role:-none}) recorded design gauntlet '$gauntlet_base' for draft design PR $pr_url — panel.sh senses the design panel; it runs before maintainer review\"\nscripts/jobs/post-gauntlet.sh:2:# post-gauntlet.sh — producer primitive: record a staged GAUNTLET run over a PR, so\nscripts/jobs/post-gauntlet.sh:3:# the deterministic gauntlet.sh driver walks it one claim-sized stage at a time\nscripts/jobs/post-gauntlet.sh:5:# must span the whole chain (designs/staged-gauntlet.md).\nscripts/jobs/post-gauntlet.sh:7:# The gauntlet ran as ONE claimed job whose wall-clock was the SUM of every stage,\nscripts/jobs/post-gauntlet.sh:16:#   post-gauntlet.sh [--build-job <base>] [--probe] [--kind feature|probe]\nscripts/jobs/post-gauntlet.sh:19:#   <g>                 the gauntlet base (deterministic, e.g. <owner>-<repo>-pr<N>-gauntlet).\nscripts/jobs/post-gauntlet.sh:20:#   <pr-url>            the PR the gauntlet runs on (full github.com …/pull/<N> URL,\nscripts/jobs/post-gauntlet.sh:23:#                       (empty for a `run the gauntlet` origin).\nscripts/jobs/post-gauntlet.sh:35:# is a no-op success — a re-triggered gauntlet on the same PR reuses the same record\nscripts/jobs/post-gauntlet.sh:43:GARDEN_TAG=\"post-gauntlet\"\nscripts/jobs/post-gauntlet.sh:47:post-gauntlet.sh — record a staged gauntlet run over a PR.\nscripts/jobs/post-gauntlet.sh:50:  post-gauntlet.sh [--build-job <base>] [--probe] [--kind feature|probe]\nscripts/jobs/post-gauntlet.sh:53:  <g>                 the gauntlet base (deterministic, stable across re-triggers).\nscripts/jobs/post-gauntlet.sh:87:base=\"${1:?usage: post-gauntlet.sh [--build-job <base>] [--probe] <g> <pr-url>}\"\nscripts/jobs/post-gauntlet.sh:88:pr_url=\"${2:?usage: post-gauntlet.sh <g> <pr-url> (a PR URL is required)}\"\nscripts/jobs/post-gauntlet.sh:91:  -*)        die \"illegal gauntlet base: '$base' (names must not start with '-')\";;\nscripts/jobs/post-gauntlet.sh:92:  */*|.*|'') die \"illegal gauntlet base: '$base'\";;\nscripts/jobs/post-gauntlet.sh:105:# proxy / unblock watcher already use, so a gauntlet identifies a PR identically.\nscripts/jobs/post-gauntlet.sh:136:  printf '# gauntlet %s\\n\\n' \"$base\"\nscripts/jobs/post-gauntlet.sh:137:  printf 'Staged gauntlet run over %s (%s).\\n' \"$pr_canonical\" \"$kind\"\nscripts/jobs/post-gauntlet.sh:139:  printf '\\nThe deterministic gauntlet.sh driver walks this PR one claim-sized stage at\\n'\nscripts/jobs/post-gauntlet.sh:147:    log \"gauntlet '$base' already recorded; nothing to do\"\nscripts/jobs/post-gauntlet.sh:153:  if commit_and_push \"$DIR\" \"gauntlet($base) recorded [$kind, pr #$pr_number] by $GARDEN\"; then\nscripts/jobs/post-gauntlet.sh:154:    log \"recorded gauntlet '$base' ($kind, $repo#$pr_number, max_iterations=$max_iterations, max_resumes=$max_resumes)\"\nscripts/jobs/post-gauntlet.sh:157:  log \"post-gauntlet of '$base' lost a push race (attempt $attempt); re-syncing\"\nscripts/jobs/post-gauntlet.sh:160:die \"could not record gauntlet '$base' after retries\"\nscripts/jobs/gardener.sh:616:  # draft PR must receive a gauntlet job before this build is allowed into tada.\nscripts/jobs/gardener.sh:624:    \"$HERE/auto-gauntlet-handoff.sh\" \"$base\" \"$jobfile\" \"$report\" >>\"$capture\" 2>&1\nscripts/jobs/gardener.sh:629:      log \"auto-gauntlet handoff FAILED for '$base' (rc=$hrc); leaving build in doin for retry\"\nscripts/jobs/gardener.sh:633:  # SENSING half of the design-PR gauntlet-bypass fix (auto-gauntlet-handoff is the\nscripts/jobs/gardener.sh:636:  # design gauntlet — the evaluator's absence. On the ordinary path the stager just\nscripts/jobs/gardener.sh:638:  # with no gauntlet (the three-miss bypass shape, or any future producer that opens\nscripts/jobs/gardener.sh:641:  # scripts/jobs/assert-design-pr-gauntlet.sh (deterministic, no LLM).\nscripts/jobs/gardener.sh:644:    \"$HERE/assert-design-pr-gauntlet.sh\" \"$base\" \"$jobfile\" \"$report\" >>\"$capture\" 2>&1\nscripts/jobs/gardener.sh:649:      log \"design-PR gauntlet SENSOR blocked completion of '$base' (rc=$hrc): a garden-owned design PR named in the report has no staged gauntlet; leaving in doin for retry\"\nroles/liaison/AGENT.md:46:- **A `build` job auto-runs the gauntlet. Never tell the maintainer to gauntlet a\nroles/liaison/AGENT.md:48:  *opening stage* of the gauntlet: the supervising gardener carries its draft PR\nroles/liaison/AGENT.md:51:  the gauntlet #N* dispatch is needed, and you must never say a build \"won't\nroles/liaison/AGENT.md:52:  auto-run the gauntlet.\" Use **run the gauntlet #N** only for a PR that did **not**\nroles/liaison/AGENT.md:58:  So the auto-gauntlet invariant is for **mergeable-feature** builds, never probes.\nroles/liaison/AGENT.md:307:- **Doom and halt.** Reaper-parked jobs and non-converging gauntlets sitting in\nskills/panel/SKILL.md:123:   staged-gauntlet driver reads. This is the seat-gate pattern the\nskills/panel/SKILL.md:195:  **gauntlet**. The script and this skill use \"gauntlet\" / \"panel\" throughout.\nskills/panel/SKILL.md:206:  running the gauntlet on `endojs/endo-but-for-bots#847`, a diagnostic baseline PR\nscripts/jobs/reputation-reduce.sh:13:#      whose acceptance was unknown at completion (a PR still in gauntlet/review).\nscripts/jobs/assert-followup-posted.sh:21:# assert-design-pr-gauntlet.sh: gardener.sh calls it right before a job may be\nscripts/jobs/gardening/garden-pr.sh:124:# The gauntlet's follow-up pushes only ever ADD commits on top of the live head,\nscripts/jobs/gardening/garden-pr.sh:128:# branch-rewind hazard (endojs/endo-but-for-bots #792): a gauntlet supervised from\n","exit_code":0,"status":"completed"}}
{"type":"item.started","item":{"id":"item_3","type":"command_execution","command":"/bin/bash -lc \"sed -n '1,260p' roles/builder/AGENT.md && rg -n '\"'^## House style|''^## Improving'\"' roles/COMMON.md && scripts/jobs/ensure-project-worktree.sh ironhorse-fuzz-c9eaa7b5ae02437a-repair endojs/endo-but-for-bots ironhorse-fuzz-findings\"","aggregated_output":"","exit_code":null,"status":"in_progress"}}
{"type":"item.completed","item":{"id":"item_3","type":"command_execution","command":"/bin/bash -lc \"sed -n '1,260p' roles/builder/AGENT.md && rg -n '\"'^## House style|''^## Improving'\"' roles/COMMON.md && scripts/jobs/ensure-project-worktree.sh ironhorse-fuzz-c9eaa7b5ae02437a-repair endojs/endo-but-for-bots ironhorse-fuzz-findings\"","aggregated_output":"<6>12:51:15 [jobs] ensure-project-worktree: endojs/endo-but-for-bots@ironhorse-fuzz-findings absent; resolving from kriscendobot/endo-but-for-bots (garden fork)\nPreparing worktree (detached HEAD 938ff59f1)\n<6>12:51:42 [jobs] WARM-CACHE hit: endojs-endo-but-for-bots@183d36e1b883 [native-builds] populated 120 node_modules tree(s) into /home/kris/garden/scratch/project-wt-ironhor-191acbcc798c-dc6c292b in 24s (of which 15s link-state reconcile 'yarn install --immutable' rc=0; cold install was 5s)\n/home/kris/garden/scratch/project-wt-ironhor-191acbcc798c-dc6c292b\n","exit_code":0,"status":"completed"}}
{"type":"item.started","item":{"id":"item_4","type":"command_execution","command":"/bin/bash -lc 'scripts/jobs/ensure-project-worktree.sh ironhorse-fuzz-c9eaa7b5ae02437a-repair endojs/endo-but-for-bots ironhorse-fuzz-findings'","aggregated_output":"","exit_code":null,"status":"in_progress"}}
{"type":"item.completed","item":{"id":"item_4","type":"command_execution","command":"/bin/bash -lc 'scripts/jobs/ensure-project-worktree.sh ironhorse-fuzz-c9eaa7b5ae02437a-repair endojs/endo-but-for-bots ironhorse-fuzz-findings'","aggregated_output":"<6>12:51:54 [jobs] ensure-project-worktree: endojs/endo-but-for-bots@ironhorse-fuzz-findings absent; resolving from kriscendobot/endo-but-for-bots (garden fork)\nPreparing worktree (detached HEAD 938ff59f1)\nUpdating files:  99% (45275/45398)\rUpdating files: 100% (45398/45398)\rUpdating files: 100% (45398/45398), done.\n<6>12:52:15 [jobs] WARM-CACHE hit: endojs-endo-but-for-bots@183d36e1b883 [native-builds] populated 120 node_modules tree(s) into /home/kris/garden/scratch/project-wt-ironhor-191acbcc798c-dc6c292b in 19s (of which 12s link-state reconcile 'yarn install --immutable' rc=0; cold install was 5s)\n/home/kris/garden/scratch/project-wt-ironhor-191acbcc798c-dc6c292b\n","exit_code":0,"status":"completed"}}
{"type":"error","message":"This content was flagged for possible cybersecurity risk. If this seems wrong, try rephrasing your request. To get authorized for security work, join the Trusted Access for Cyber program: https://chatgpt.com/cyber"}
{"type":"turn.failed","error":{"message":"This content was flagged for possible cybersecurity risk. If this seems wrong, try rephrasing your request. To get authorized for security work, join the Trusted Access for Cyber program: https://chatgpt.com/cyber"}}
