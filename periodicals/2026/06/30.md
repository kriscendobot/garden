---
date: 2026-06-30
window_start: 2026-06-30T07:00:00Z
window_end: 2026-07-01T07:00:00Z
created: 2026-07-01
author: journalist
purpose: daily-progress-summary
---

# Daily progress summary: 2026-06-30

Another high-throughput window — roughly 789 journal commits, 103 jobs posted
against 103 completed. The headline continued the prior day's XS value-stack
thread but shifted from proving the fix to *consolidating and reusing* it: the
`hex` codec work converged on the already-published `@endo/hex` package, and the
ymax0 contract-control upgrade was driven end to end against a real mainnet
swing-store, confirming the one-line `flatMap`->loop change carries the upgrade
past the overflow at the on-chain `stackCount=4096`. Around that, the
endo-but-for-bots PR pipeline merged **#284** (retention paths) and genuinely
fixed **#58** (error rendering, verified in real Chrome this time), the
kriskowal/garden **#16** bid/accept-market design merged, a run of three
lesson-encoding jobs hardened the fleet's reporting and comment discipline from
the #58 fallout, and a real worktree-isolation collision was root-caused and
fixed. No load-bearing failures; the day's errors were transient handler retries
that all recovered.

## The hex codec: consolidation onto `@endo/hex` (the headline)

The overflow arc from 2026-06-29 moved from "fix shipped" to "reuse the right
package and prove it on-chain":

- **Richard Gibson feedback applied on agoric-sdk#7.** The bot-fork benchmark
  (`packages/internal/benchmark/hex-decode-bench-xs.mjs`) was rewritten to drive
  XS through the real `@agoric/xsnap` `xsnap()` export in Richard's "best of all"
  form, deleting the hand-rolled netstring/worker recapitulation it had carried;
  `@agoric/xsnap` is now a `workspace:*` devDependency of `@agoric/internal`, with
  a deterministic `yarn.lock` patch to clear the immutable-install CI gate.
  Richard's second, architectural point (move to a shared `@endo/hex`) was
  captured and queued as a follow-on job.
- **`@endo/hex` already exists — and is already correct.** The queued
  `endo-hex-tiered-codec-port` job found the package published upstream as
  `@endo/hex@1.1.1` and mirrored on the bot fork: already tiered (native
  `Uint8Array.fromHex`/`toHex` preferred, pure-JS floor), already XS-safe by the
  job's own criterion (bounded `for` loops with char-code arithmetic, no
  `flatMap`, no large-array spread, no module-scope decode `Map`), and already
  pin-complete on correctness. No bot-fork change was made (that would diverge the
  mirror from a published package); the maintainer was asked to close the port as
  already-satisfied and re-scope the real remaining work to slimming agoric-sdk#7
  to consume `@endo/hex` and drop its in-tree `hex.js` body.
- **agoric-sdk#7 shepherded through the slim-down.** A run of #7 jobs
  (`slim-to-consume-endo-hex`, `shepherd`, `shepherd-after-slim-down`,
  `shepherd-fixer-swingset-regression`, `update-title-description-template`)
  advanced the fork PR and worked a swingset regression surfaced by the slim.
  Scope line held throughout: base and head stay on the bot fork, zero upstream
  Agoric contact.

## kriskowal/garden#9: contract-control upgrade run to completion

`garden-issue-9-run-contract-control-upgrade-test-to-completion` drove mhofman's
three-step contract-control-upgrade protocol through inquisitor against the cached
real mainnet swing-store (`agoric-26146641`), and posted the evidence to
kriskowal/garden#9 (comment 4851105447). Driving ymax0's delegated `ContractControl`
`upgrade({bundleId})` on the live ZCF vat `v290`: the baseline (beta2) upgrade
succeeded, the unpatched beta3 (`flatMap` hex) failed with `"Stack meter exceeded"`
/ XS exit-12, and the patched beta3 (`flatMap`->loop) succeeded — clean import
span, no overflow, at the on-chain default `stackCount=4096`. Two gotchas were
recorded to the `agoric-chain-snapshot` skill on main2 (`5ff0e97e1`): the upgrade
`bundleId` is the *contract* bundle, not v290's ZCF source bundle, and
`await EV(cc).upgrade()` deadlocks (the overlay can't service post-upgrade
wallet/vstorage traffic), so a concurrent-observer driver that polls the
incarnation span is required. Companion #9 jobs answered mhofman's
mainnet-repro-clarification, contract-kit/inquisitor-bridge, and functor-stack
questions, and `inquisitor-ymax0-hex-repro` refreshed the offline repro.

## endo-but-for-bots PR pipeline

The bot fork's queue was again the busiest project, spanning roughly a dozen PRs:

- **#284 merged** (`feat(daemon,cli): retention-paths Phase 1`) into `llm` at
  `9432b796`, on kriskowal's APPROVED "conduct and post the next phase" review.
  The claiming gardener read the design, posted the Phase 4 chat-panel build job
  (`ebfb-retention-paths-chat-panel`, already claimed), and dispatched the
  conductor to merge.
- **#58 actually fixed and verified in real Chrome.** After a prior "verified"
  claim proved false in the browser, this cycle reproduced the failure in real
  Chromium first, then diagnosed three end-to-end gaps (inline-`/js` render path,
  the WebSocket gateway missing the `marshalSaveError` hook the CLI path already
  had, and client errorId recovery under SES `errorTaming: 'safe'`). Commit
  `c613814a2` threads `marshalSaveError` into the WS gateway and captures the wire
  errorId at decode time; the hard gate was met with a real Playwright/Chromium
  run rendering message, stack trace, and worker chip, plus the reverted-commit
  counterfactual. Two gardeners worked the requeued job concurrently and converged
  cleanly via git-push CAS.
- **#442 -> #585.** kriskowal's line-33 review (use `@endo/platform` for the
  hand-rolled file/crypto powers) was discharged by opening **#585**
  (`feat(platform): add content-store powers for node fs`, un-drafted, on a frozen
  `llm` base), which adds `makeContentStoreFilePowers` / `makeContentStoreCryptoPowers`;
  the #442-local test adoption (net -130/+27) is prepared and held until #585
  merges and #442 rebases. Platform suite 276/276, adoption 9/9.
- **#578 opened** — DRAFT `@endo/gateway` Feature 2 (virtual hosting) Phase 1, on
  the #343 `AppsNameHub` skeleton: a `makeWebletResolver` hardened exo routing
  `Host` through `@apps`, a content-addressed read-through cache, MIME
  inference/override, and 73 passing tests, with SSR and OCapN-CBOR seams left as
  named follow-ons.
- **#475 designated the fresh bytearray-view implementation PR.** The erights
  directive asking about a "fresh view-based implementation PR" was resolved by
  recognizing #475 (`feat/narrow-bytearray-to-uint8`) already *is* that PR (all 17
  checks green); the #572 design doc was synced to name it and an anchor comment
  designated it as the PR of record, collapsing #57/#429/#3226 into it. A
  companion protocol-versioning question from erights on #475 was moved to a
  separate issue per the directive.
- Also refreshed/shepherded/conducted: **#277** (macOS shepherd, retcon, conduct),
  **#343** (conduct and post next phase), **#472**, **#544**, **#570/#571**, the
  **js-extension** exports-map migration and lint enforcement
  (`enforce-js-extension-lint-endo-but-for-bots`, `builder-ebfb-enforce-js-extension-jsdoc-import-lint`),
  the **namehub interface unification** build, and the dependabot **#197** refresh.

## kriskowal/garden#16: bid/accept-market design merged

The `design/gardener-bid-accept-market` design PR merged (`result: conductor
merged kriskowal/garden#16`). A `build-bid-accept-market-phase0-1` job started the
implementation, and a five-comment CHANGES_REQUESTED review fanned out into ~5
concurrent jobs (`308f53fe`, `3c8d784d`, `40e01695`, `eabd1e1d`, and a
`review-*` consolidator) all editing the same design doc — each scoped to its own
comment, reconciled through CAS rebases and one manual same-region conflict
resolution. The recurring lesson (an N-inline-comment review fans out into N
same-file jobs) was flagged to the liaison: emit one job per review, or make the
`review-*` job the sole consolidator.

## Garden governance and fleet hardening

Three lesson-encoding jobs turned the #58 fallout into durable norms, each landed
in the always-read surfaces (not one ephemeral place):

- **verification-claims-require-real-evidence** (`7655539bb`) — a "verified" claim
  must cite real-execution evidence; UI/browser criteria require an actual browser
  run; code inspection is a design argument, not verification. Landed in
  `roles/COMMON.md`, the gardener role, and the saboteur/skeptic juror seats (which
  now reject an unbacked "verified" as must-fix).
- **acknowledged-comment-needs-reply** (`0b6929e3a`) — a single `post_reply`
  chokepoint in the comment-watcher so a trusted non-actionable comment gets a
  reply, not just a silent reactji, and an actionable comment gets a reply naming
  the posted job.
- **erights-maintainer-authority** — encoded in `roles/COMMON.md` and the
  endo-but-for-bots project README that erights holds full maintainer authority
  (lifecycle directives are self-authorizing; do not re-route to the inbox); the
  credential boundary against upstream endojs/endo is unaffected.

Two maintainer issues were discharged: **#18** granted maintainers comment/push
access across the bot forks (deleted `contributors_only` interaction-limits on six
forks, created the one missing `write` invite so all eleven forks now invite
kriskowal — pending his acceptance), and **#20** posted the
`agoric-3-proposals-316-missing-proposals` fork job (already claimed).

The **worktree-isolation collision** behind the #58 double-work was root-caused and
fixed (`0ec35c1a8`): two gardeners on the same PR had improvised the same
repo+PR-keyed project path into one shared tree, bleeding edits across each other.
The new `ensure-project-worktree.sh` keys the project checkout by the gardener's
unique job base (deterministic under requeue, distinct per concurrent job), wired
into the launch prompt, the gardener role, and the `worktree-per-pr` skill, with a
12/12 isolation test. A further run of `improve-*` jobs hardened handler lifecycle
and the comment path: per-job handler budget, elapsed-constancy early escalation,
classify-session-limit-as-transient, deterministic-requeue-on-incomplete-exit,
comment-source-exclude-bot-own-comments, drop-directive-on-merged-PR,
dependabotany-self-schedule-precise-recheck, a CLAUDE.md inventory-drift gate, and
a fully-deterministic watcher observe-to-post-job path.

The scholar pool ingested `scholar-ingest-tailscale-oauth-apps-and-oauth-skills`
into the library (16 library edits in the window).

## Health

The board stayed balanced: 103 posted against 103 completed, with #284 and
garden#16 merged inside the window. The 16 **deadmail** promotions were the bus
working as designed — peers messaging already-torn-down issue/PR agents, each
dead-letter promoted to a fresh job so no intent is lost (the agoric-sdk#7 and #58
coordination are the clearest examples). Nine gardener **handler errors** (rc=1)
were captured, escalated to the gardener inbox, and left in `doin` for the reaper;
the affected jobs (`issue-kriskowal-garden-20`,
`enforce-js-extension-lint-endo-but-for-bots`, `build-bid-accept-market-phase0-1`,
and others) all completed on requeue, so no work was lost. No stuck `doin` jobs and
no load-bearing failures at window close.
