# Daily progress summary: 2026-09-21 (Pacific)

Window `[2026-09-21T07:00:00Z, 2026-09-22T07:00:00Z)`.

## Abstract

The Claude-on-minion.town arc (garden issue 89) advanced hard. Its sole standing
blocker cleared when endo-but-for-bots PR #1310, guest-native invitation
acceptance, merged at 21:36Z; that immediately unblocked and ran the
invitation-onboarding build, which handed to a successor orchestration that
completed clean. Overnight the arc kept moving: minion.town PRs #87 and #98
merged, two Claude inference-track draft PRs (#105 CLI, #106 Agent-SDK) opened
green, and #79, #96, #97, #104, and #107 all saw review, rebase, conduct, or
shepherd work. On endo-but-for-bots, Dependabot PR #1317 (a 19-update
all-minor-patch group that had been orphaned by a mis-routed merge job) was
re-verified and merged at 05:26Z, leaving zero open Dependabot PRs, and a wide PR
front (#1089, #1097, #1226 through #1228, #256, #1285, #1286, #1289, #1290,
#1293, #1303, #1309) churned through review, weave, and conduct stages. The
principal open concern remained the follower deploy gate on
`endolin-garden2-5bcdff64`, which rejected fifteen distinct `main2` candidates
across twenty-seven rejections, every one naming the same trio of test suites
(`signal-kill-classifier-test.sh`, `retry-narrowing-test.sh`, and
`provider-cooldown-test.sh`); the fix job for that regression was itself wedged.
A cluster of roughly six requeue-exhausted dooms landed on the sibling host
`endolin-garden-ece02cb4` around 23:23Z, including the arc's critical-path
`pr1015-refresh` job, which a press tick escalated to the maintainer as a
host-level issue rather than a defect in the fix. The window recorded 46 journal
entries: 29 errors (27 deploy-gate, 2 gardener), 13 progress notes, 2 messages,
and 2 results.

## endo-but-for-bots

### Results and completions

The headline was PR #1310, guest-native invitation acceptance, merging into `llm`
at 21:36Z after a full gauntlet chain (conduct, receipt, and the
`review-2d8eec89` pass). That merge cleared the last live blocker on the
minion.town arc. See
`entries/2026/09/21/222734Z-progress-gardener-b5b939.md`.

Dependabot PR #1317, an `all-minor-patch` group of nineteen updates, was
recovered and merged. It had carried a standing MERGE-NOW verdict from 09-20 and
a maintainer "Please conduct," but the follow-up had run as a plain conductor
merge job that stalled on the maintainer-approval gate because it never opted into
the botanist `--dependabot-auto-merge` path; its review job was doomed and its
merge orchestration halted, leaving the PR orphaned and open. The recheck sweep
re-verified the maturity floor, re-queried advisories on all incoming versions
including `happy-dom@20.14.5`, confirmed CI green on the head (27 success, 8
skipped, 0 failed), and merged into `llm` at 05:26Z as `2d0f7fb7f0`. After the
merge there were zero open Dependabot PRs. The gardener flagged the mis-routing
as a pattern worth guarding: a Dependabot MERGE-NOW should be conducted by a
botanist-role job, not a generic merge. See
`entries/2026/09/22/052747Z-message-gardener-9f60a4.md` and
`jobs/tada/2026/09/22/endojs-endo-but-for-bots-pr1317-receipt.md`.

A broad PR front reached terminal or intermediate stages across the window. PR
#1285 completed weave, review, shepherd, receipt, and finalize. PR #1286 ran
review, shepherd, and conduct. PR #1290 ran a conduct plus a maintainer-review
chain (below). PRs #1089 (weave, review, conduct chain), #1097 (review), #1226
through #1228 (revise plus reviews and a receipt), #256 (hashline phase-2 build
and review), #1289, #1293, #1303, and #1309 (review, conduct, receipt) all
recorded completions. See `jobs/tada/2026/09/21/` and `jobs/tada/2026/09/22/`.

### The PR #1290 maintainer-review chain

A gardener fetched maintainer review 5271710675 and its inline thread and found
the requested work incomplete: the current head carried per-package commits plus
a `chore: Update browser-test lockfile`, while the directive called for an exact
separate `chore: Update yarn.lock`, and two macOS checks were still in progress.
Rather than merge ahead, it posted a serial orchestration
`pr1290-review-5271710675-chain` owning three children (a mentor-tier feedback
re-evaluation, a net-diff-preserving retcon with the exact lockfile commit, and a
green-and-mergeable conduct) and warned the older active conduct job not to merge
ahead. A later result corroborated that follow-up PR #1326 deletes the
extraneous `browser-entry.js` fixture and routes its imports through an in-memory
virtual entry, satisfying the earlier feedback with no further source change
needed. See `entries/2026/09/21/234744Z-result-gardener-4f607b.md` and
`entries/2026/09/22/013459Z-result-gardener-9f93b1.md`.

### Dispatches, dooms, and requeue churn

Several endo-but-for-bots PR jobs exhausted their single backed-off retry without
a completion signal and were left in `doin` for the reaper: `pr1293-receipt`,
`pr1309-conduct-20260921` (twice), `pr1015-refresh-for-review-20260919` (twice),
`pr1286-conduct-20260921`, and `pr1286-review-cc7d78b9`. Two of these escalated a
kind-error to the gardener inbox as suspected wedged children rather than working
ones, on the grounds that elapsed time was near-constant across requeue cycles
(the `pr1309-conduct` job at 30 and 31 seconds, the `pr1286-receipt` job at 210
and 178 seconds). The `merge-pr1317-20260921` handler separately hit its own
2,400-second wall-clock budget and stamped a deadline-overrun for the reaper. See
`entries/2026/09/21/222924Z-progress-gardener-21dd94.md`,
`entries/2026/09/21/232547Z-error-gardener-bcc2a0.md`,
`entries/2026/09/22/005328Z-error-gardener-40c86f.md`, and
`entries/2026/09/22/013658Z-progress-gardener-8e0cb6.md`.

### Messages

A conductor working PR #1290 observed that `ci-wait-merge.sh` binds its wait to
`post_rebase_head` and, when a concurrent authorized force-push moves the live
head, only reports the mismatch until timeout rather than adopting or rebasing
onto the new head; in this run that forced a manual interrupt after a full CI
walk. The message asked the liaison to consider teaching the spine to detect a
changed live head and either restart from it or fail immediately, instead of
waiting ninety minutes on an unreachable object id. See
`entries/2026/09/21/225927Z-message-conductor-1a16ea.md`.

## minion.town

### Arc completion and outward presses (garden issue 89)

The arc's governance presses ran on their roughly six-hour cadence throughout the
window and recorded the arc turning from decision-gated to advancing. The 09:53Z
and 16:09Z ticks found the arc nominal but still gated on the maintainer's #1310
merge or review call, with three then two clean in-window completions and no
dooms, refusals, or stalls. The 22:27Z tick recorded the turn: #1310 merged at
21:36Z, which unblocked the invitation-onboarding build (completed via a verified
honest handoff to the successor orchestration
`minion-town-invitation-onboarding-build-20260921`), and the maintainer began
actively steering by requesting changes on the #1226 design and promoting the
#1015 refresh. See
`entries/2026/09/21/095331Z-progress-gardener-30e8b6.md`,
`entries/2026/09/21/160925Z-progress-gardener-6cc371.md`, and
`entries/2026/09/21/222734Z-progress-gardener-b5b939.md`.

The 04:29Z tick reported the arc advancing hard with one critical-path doom.
minion.town PRs #87 and #98 both merged in-window (#87 at 03:14Z as merge commit
`287af35`, resolving an inline fixture ask through
`fix-minion-town-pr87-wiring-test-fixtures` and a production-gate job); both
Claude inference-track draft PRs opened with green CI (#105 CLI with 34 new
tests, #106 Agent-SDK); and the invitation-onboarding successor orchestration
completed clean over both of its children (endo-daemon-pin-refresh and
guest-web-invite-accept-fallback-fix). Roughly twenty arc jobs completed clean in
that window, including two merges, two draft PRs, four design reviews, and an
orchestration. See `entries/2026/09/22/042944Z-progress-gardener-4edfd7.md`.

### The critical-path doom and its escalation

The single in-window arc doom was `pr1015-refresh-for-review-20260919`, doomed
requeue-exhausted at 23:23Z on `endolin-garden-ece02cb4`. The press tick noted it
sat inside a cluster of roughly six requeue-exhausted dooms on that host at the
same moment (also `pr1286-receipt`, `pr1286-review-cc7d78b9`, `pr1293-receipt`,
`pr1309-conduct`, and `fix-subscription-model-deploy-gate-regression`), all
classified transient with a repeated-plain-exit split reason, which points at a
host-level issue on that box rather than a defect in the #1015 fix. Because PR
#87's provider seam stays `makeUnavailableProvider` until #1015 lands, the tick
messaged the maintainer inbox about the critical-path doom and about a stale
production-gate resume job that still called the Track B inference work failed
even though PR #106 had landed green. The #87 production gate itself was parked
awaiting a maintainer answer to a question posted on the PR. See
`entries/2026/09/22/042944Z-progress-gardener-4edfd7.md`.

### Other completions

The daily containment-gateway follow-up ran again (the
`fu-minion-town-containment-gateway-endo-sock-1` job on both 09-21 and 09-22
cadences). Overnight the arc also completed a vitest migration, a repo-wide
vitest holdout fix, an endo-pin bump on main, PR reviews and conducts across #79,
#96, #97, and #104, shepherds on #105 through #108, and the
`orchestrate-claude-cli-signal-upgrade-20260922` and
`minion-town-claude-inference-exploration-20260922` orchestrations (the latter
reported complete-with-failures, but both of its deliverables landed, so the
failure was a transient handler-timeout latch rather than a lost deliverable).
See `jobs/tada/2026/09/21/` and `jobs/tada/2026/09/22/`.

## Garden meta

### The follower deploy gate (principal open concern)

The candidate test gate on `endolin-garden2-5bcdff64` rejected `main2` deploy
candidates twenty-seven times across the window (one further rejection on
`endolin-garden-ece02cb4`), spanning fifteen distinct candidate commits
(including `913e1d0d1d`, `7070fc7e9c`, `85ae2f878c`, `9bc8c5682e`,
`3dc215a57d`, and `ebfff9b84e`). Every rejection named the same three failing
suites: `signal-kill-classifier-test.sh`, `retry-narrowing-test.sh`, and
`provider-cooldown-test.sh`, with two rejections also naming
`codex-policy-refusal-resume-test.sh`. The deployed tree was left in place each
time, so the follower did not advance across the window. This is the same trio
the prior day's summary flagged, and it is consistent with the recent quota and
termination-signal work on `main2`. The dedicated fix job
`fix-subscription-model-deploy-gate-regression` was itself wedged, exiting rc=143
after its sole retry, and sat among the ece02cb4 doom cluster. See
`entries/2026/09/22/045434Z-error-deploy-garden-944510.md` and
`entries/2026/09/21/215434Z-progress-gardener-8aa9bc.md`.

### Self-healing and infrastructure work

A large volume of self-heal and improvement jobs completed. The receipt-watcher
self-heal family cleared many prerequisite-stderr, empty-diagnostic, shared-clone
lock-contention, and silent sync-clone reset cases across several repos, and
triager self-heals fixed cursor-set and cgroup-reap edges. Standalone
improvements landed for `ci-wait-head-change`, `issue-inbox-tick-bounds`,
`repo-watcher-provisioner-offline`, `worktree-sweeper-time-budget`, and
`transcript-capture-bounded-tick`; a `fix-cursor-set-local-clone-race` and an
`apply-garden-arm64-bash32-patch` also completed. Library and registry work
included `incorporate-typesafe-agent-skill`,
`typesafe-api-key-handoff-allowlist`, and
`register-gpt-6-astra-mentat-tier`. Two garden-repo issue-inbox jobs (issues 105
and 106) and several dead-lettered bus messages promoted to jobs also completed.
See `jobs/tada/2026/09/21/` and `jobs/tada/2026/09/22/`.

### Board lifecycle

The board saw heavy reaper churn concentrated around the 23:23Z ece02cb4 doom
cluster and the long-running PR conduct and receipt jobs, but completions kept
landing: the two dated `tada` directories together hold well over a hundred
completion files, including the arc merges, the Dependabot merge, the wide
endo-but-for-bots PR front, and the self-heal fleet. The scheduled daily summary,
the arc presses, the Dependabot recheck, the containment-gateway follow-up, and a
deploy canary probe all completed within the window. No worktree-lifecycle entry
was recorded.
