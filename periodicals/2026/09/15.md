# Daily progress summary: 2026-09-15 (Pacific)

Window `[2026-09-15T07:00:00Z, 2026-09-16T07:00:00Z)`.

## Abstract

A busy, healthy day dominated by two forks and a burst of garden self-care. On
endojs/endo-but-for-bots the OCapN CapTP invitation PR #1125 (arc item 7) ran a
tight review-and-fix loop through the night: three kriskowal reviews were
processed and four fixer commits landed (unified host and guest agent options,
restored the over-stripped pins and wake-on-message machinery, and an invitation
result-name retention fix), all with CI green. A newest review briefly went
orphaned in `doin` after a usage failure and was surfaced to the maintainer, then
recovered on its own and completed. The same repo also opened and fully ran a
gauntlet on the SES Node 26 lockdown-permits PR #1281 (with the useful finding
that the unpermitted-intrinsic noise is not new in Node 26 at all), and landed the
Iron Horse computron-benchmark-baseline stream through design, build, and a
complete gauntlet, opening PRs #1282 and #1283 and demolishing the XS-computron
parity myth. On kriscendobot/minion.town the Claude-on-minion.town press kept its
cadence (four completion-press ticks and seven outward-press dispatches, all
clean) while a fresh line of reminder-store work landed on `main`: a
digest-pinned deployment compatibility store, a daemon redeploy, and a live
@pins revival verification. The garden worked on itself the hardest: an
investigation proved the fleet foreman was deliberately quiesced since
2026-07-14 rather than broken, a partial unquiesce to target 2 landed, a new
credit-controls orchestration began pumping four budget and gauntlet-gating
children, and a rolling deploy exercised canary probes across hosts. Two deploy
candidates were rejected by a failing policy-refusal-quarantine suite on the
oros-studio host, which drew a diagnose, a verify, and a fix job in response. The
window held eleven journal entries (six progress notes, two results, three
errors) and the `jobs/` board recorded 79 completions, 96 claims, 61 posts, 9
promotions, 13 requeues, one deadline-overrun hint, and zero dooms.

## endojs/endo-but-for-bots

The day's center of gravity. Three concurrent work streams: the #1125 CapTP
invitation review loop, the SES Node 26 lockdown-permits gauntlet, and the Iron
Horse computron-benchmark-baseline design-to-gauntlet chain.

### PR #1125 (OCapN CapTP guest invitation, arc item 7) review-and-fix loop

The Claude-on-minion.town completion press ticks tracked this closely, since the
arc's `build-minion-town-invitation-onboarding` job stays blocked behind #1125.
Across the window the loop processed three maintainer reviews and landed four
fixer commits, all reported with CI green:

- `review-b58d5a3f` completed and processed a `CHANGES_REQUESTED` review
  (host and guest option parity plus dropping `listIdentifiers` in the mailbox
  reincarnation), posting a single fixer job carrying both directives
  (`entries/2026/09/15/190811Z-progress-gardener-bd6cfe.md`).
- `fix-agent-option-parity-20260915` completed at commit `97891bb3`: unified the
  host and guest agent options and swapped `listIdentifiers` for `listValues`,
  with the full 259-test daemon suite and CI green
  (`entries/2026/09/16/010925Z-progress-gardener-6a92df.md`).
- `7ec0d5f0` restored the `hostPins`, `guestPins`, and wake-on-message machinery
  that an earlier commit had over-stripped, per a kriskowal comment.
- `3193517b` landed an invitation result-name retention fix at `bf272ccf`, CI
  green.

The newest review, `review-a74698d6` (kriskowal review `5215956390`), was the
one blemish. Its handler failed with a usage error at 22:02Z and the claim was
left in `doin` for the reaper (`entries/2026/09/15/220244Z-error-gardener-060cd3.md`).
By tick 30 it had sat orphaned for about three and a half hours past the review
handler timeout without a requeue while the reaper actively requeued other jobs,
so the press surfaced it to the maintainer as a real finding
(`entries/2026/09/16/010925Z-progress-gardener-6a92df.md`). It then recovered on
its own: re-claimed at 01:43Z and completed at 01:45Z, so the review-as-a-unit
work is no longer stuck.

### PR #1281 (SES Node 26 lockdown permits)

`ses-node26-lockdown-permits` completed its build and opened draft PR #1281, then
ran a complete gauntlet to its budget: the clean stage, six panel rounds, and six
fix rounds all reached `tada`. The build carried a useful correction to its own
premise: the unpermitted-intrinsic lockdown noise it silences is identical on Node
22, 24, and 26, so it is not a Node 26 regression, and `permits.js` simply never
covered those intrinsics. A later `pr1281-review-b373c832` review job also
completed.

### Iron Horse computron benchmark baseline

The `ironhorse-computron-benchmark-baseline` orchestration ran end to end: design,
build, and a full gauntlet (clean, two panel rounds, one fix round) all reached
`tada`, and the orchestration recorded itself finished. The work opened draft PRs
#1282 and #1283 and reconciles a `computrons_agree` metering-claim rule. Alongside
it, `ironhorse-demolish-xs-computron-parity-myth` completed, arguing against any
implementation site chasing XS instruction counts at a cost to CPU-time fidelity,
and leaving follow-ups (regenerate the test262 tree to drop about 1,444 inert
`ironhorse-meter-exact` tags) reported but not done. A `pr1282-d101dbfb` job also
completed.

## kriscendobot/minion.town

### Claude-on-minion.town press (garden issue #89 arc)

The completion press held its six-hour cadence with ticks 27 through 30, all
observe-and-report only, no board writes:

- Tick 27 at 07:08Z: roster stable, two clean outward-press dispatches, no
  qualifying event, no maintainer message
  (`entries/2026/09/15/070851Z-progress-gardener-1f3893.md`).
- Tick 28 at 13:10Z: rebuilt the roster from a fresh journal clone after noticing
  the host's shared journal clone was about fourteen hours stale, a local sync lag
  rather than a fleet outage, and flagged that for operators
  (`entries/2026/09/15/131055Z-progress-gardener-07bd9d.md`).
- Tick 29 at 19:08Z: three clean completions and one fresh fixer claim on the
  #1125 loop, all healthy forward progress
  (`entries/2026/09/15/190811Z-progress-gardener-bd6cfe.md`).
- Tick 30 at 01:09Z: four clean completions plus the one orphaned-claim finding on
  #1125 `review-a74698d6` described above, which drew the window's one maintainer
  message (`entries/2026/09/16/010925Z-progress-gardener-6a92df.md`).

Seven outward-press dispatches (`claude-on-minion-town-press` at `-090505`,
`-120507`, `-150509`, `-182006`, `-212039`, `-003510`, and `-035007`) completed
clean over the window, along with the four completion-press ticks.

### Reminder store and daemon work landed on `main`

A distinct line of minion.town work reached the deployed `main` branch. The
`minion-town-reminder-compat-store` job completed at commit `e7b3b5a`
(`entries/2026/09/16/061648Z-result-gardener-3dff26.md`): it renamed the
experimental shim to a digest-pinned deployment compatibility store, staged plugin
source and provenance under `/opt/endo/reminder-compat`, added conformance
coverage, and exercised a live production @pins revival on a throwaway guest
(message count 3 before restart, 5 after revival, then 8), with `npm test` at 360
passed and 5 skipped. Supporting jobs `minion-town-reminder-daemon-redeploy-20260916`,
`reminder-daemon-revival-failure`, and the weblet audit
`minion-town-dckc-powers-weblet-audit-20260916` also completed, plus a
`fu-minion-town-containment-gateway-endo-sock` follow-up.

## Garden (meta)

The garden spent the day on its own machinery.

### The foreman: quiesced, not broken

The `investigate-malingering-foreman` job returned a definitive root cause: the
fleet foreman is not malfunctioning but was intentionally quiesced fleet-wide by
`GARDEN_FOREMAN_ACTIVE_TARGET=0` set in the unit on 2026-07-14, so every tick reads
as fully subscribed and never pumps
(`entries/2026/09/16/051956Z-result-fixer-8d6528.md`). The fixer ruled out drain,
brake, token back-off, and a stale clone, noted that 124 pre-approved deferred plan
jobs sit unpromoted while the leader host runs near 53 percent usage, and left the
lift-or-not decision to the maintainer. It also delivered instrumentation: a
host-local, self-trimming per-tick `decisions.log` so a did-nothing tick self-
documents its guard branch, plus a regression test pinning both the quiesced
no-pump and the recovered pump. Following that, `foreman-partial-unquiesce-target-2`
completed, partially lifting the quiesce to a target of 2, with a verify job parked
as deferred-urgent.

### Credit-controls orchestration

A new serial, halt-on-failure orchestration `credit-controls-20260916` with four
children began pumping: `credit-controls-fail-closed-pools` (the claim gate now
fails closed on unmetered or uncalibrated pools) and `credit-controls-manual-
gauntlet-trigger` (retiring autonomous gauntlet staging in favor of an explicit
trigger) both completed, while `credit-controls-stale-pr-viability-gate` was
promoted and claimed and `credit-controls-panel-seat-metering-and-tiering` stayed
parked. These match the `main2` tip commits landed over the window.

### Deploy, canaries, and the policy-refusal test

Two deploy candidates were rejected on the oros-studio host by a failing
`policy-refusal-quarantine-test.sh` suite, leaving the deployed tree in place both
times (`entries/2026/09/15/181524Z-error-deploy-garden-2d2016.md`,
`entries/2026/09/15/182729Z-error-deploy-garden-b9f419.md`). That drew a coordinated
response: `diagnose-oros-studio-policy-refusal-test-20260915`,
`verify-policy-refusal-tempdir-oros-20260915`, and
`fix-hardcoded-home-dirname-tempdir-tests` all completed, pointing at a hardcoded
home directory in the temp-dir tests. A rolling deploy was clearly in motion: many
`canary-probe-*` jobs completed across both the endolin-garden2 and oros-studio
hosts, and `fix-canary-drain-self-exclusion-20260916` completed (matching the
`main2` fix for the canary-drain self-exclusion deadlock). One project weave,
`weave-ebfb-1100-pin-merge-base-20260916` (endo-but-for-bots #1100), hit its own
2400-second wall-clock budget and was stamped as a deadline overrun for the reaper
rather than doomed outright (`entries/2026/09/16/063647Z-progress-gardener-31ef05.md`).

### Other garden self-care

A cluster of infrastructure improvements completed: `improve-worker-cgroup-residue-
reaping`, `improve-selected-model-capacity-classification`, `improve-reap-terminal-
handler-failures`, `improve-journal-worktree-staleness`, and `improve-deploy-
candidate-gate-diagnostics`. Reporting and provenance work landed too:
`audit-github-provenance-gaps-20260915`, `add-per-section-provenance-footnotes-
aggregated-reports`, and `fix-comment-provenance-provider-and-automatic-mark`. The
`reconcile-gardener-alias-retirement-gate-20260916` and `triage-halted-gauntlets-
20260916` jobs completed, garden issues `issue-kriscendobot-garden-93` and `-94`
were worked, a `fu-qwen-model-watch` follow-up completed, and the prior day's
`daily-progress-summary-20260915-070506` periodical was produced. One `ses-node26`
gauntlet job (`ses-node26-lockdown-permits-gauntlet-panel-1`) briefly requeued on
an exit-0-unsatisfying handler before recovering
(`entries/2026/09/15/235424Z-progress-gardener-829af9.md`).
