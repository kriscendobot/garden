# Daily progress summary: 2026-09-22 (Pacific)

Window `[2026-09-22T07:00:00Z, 2026-09-23T07:00:00Z)`. Eight journal entries fell
inside it, none carrying a top-level `project:` field, so the project grouping
below is inferred from each entry's content and the garden's own scholar work is
kept in its own section.

## Abstract

A quiet monitoring day with no substantive project merge inside the 24-hour
window. The Claude-on-minion.town arc (issue kriscendobot/garden#89) drew three
completion-press ticks that agree the arc is healthy and advancing but that the
big landings all happened just before this window opened: the endo split stack
for PR #1125 (`#1304`, `#1306`, `#1305`) merged 09-18 and 09-19, and the central
minion.town Claude-agents wiring (`#87`) plus siblings `#98`, `#99`, and `#104`
merged in the early hours of 09-22 before 07:00 Pacific. Inside the window itself
the only project churn was two failed attempts to run the gauntlet on endo PR
`#1329`, both stopped by their own wall-clock budgets rather than by any code
verdict. The garden's scholar ingested one external source on the fresh Opus 5.5
and GPT-6 releases, and one internal automation fix job (the comment-watcher
blockquote-address fix) doomed on a transient exit. Two carried, non-moot doomed
jobs still await a maintainer call: the endo `#1015` refresh-for-review and the
minion.town supply-chain-hardening fix.

## endo-but-for-bots

### Gauntlet attempts on PR #1329

Two separate runs of the gauntlet on PR `#1329` failed inside the window, both on
time rather than on substance. The first, `run-the-gauntlet-endo-pr1329-20260922`,
hit its own handler wall-clock budget at about 2401 seconds against a 2400-second
budget, a deterministic deadline overrun that was stamped for the reaper's bounded
disposition
(`entries/2026/09/22/202436Z-progress-gardener-f7cc9a.md`). A companion run,
`run-the-gauntlet-endo-pr1329-20260922-expanded-window`, exited with the
transient-signature code after its single backed-off retry at about 3117 seconds
and was left in `doin` for the reaper to mark split-eligible
(`entries/2026/09/22/215649Z-progress-gardener-bbbbe9.md`). No gauntlet verdict on
`#1329` was reached in the window; the work is time-starved, not failing review.

### Arc status reported by the completion-press ticks

The completion-press ticks for the arc report that the endo build phase for PR
`#1125` fully landed before this window: the three-slice split stack `#1304`
(merged 09-18T21:05), `#1306` (09-19T05:30), and `#1305` (09-19T15:21) are all
merged (`entries/2026/09/22/104126Z-progress-gardener-822294.md`,
`entries/2026/09/22/164214Z-progress-gardener-e7cf1a.md`). The endo design PRs
`#1226` and `#1227` remain open drafts awaiting merge, `#1228` is closed as
superseded, and the confinement-core PR `#1015` remains a draft whose
refresh-for-review job (`...-pr1015-refresh-for-review-20260919`) doomed on
2026-09-21 with no successor posted, still short of review. The large cluster of
endo-stack conduct, shepherd, weave, and gauntlet-panel jobs that doomed during
the 09-17 to 09-19 fleet outage are all moot because every PR they targeted has
since merged (`entries/2026/09/22/164214Z-progress-gardener-e7cf1a.md`).

## minion.town

The same arc ticks track the minion.town side. The central deliverable, PR `#87`
wiring the Claude-agents capability behind `ENDO_CLAUDE_ENABLED`, merged
09-22T03:13, with `#98` (end-to-end evaluation design) merged 09-22T00:39, `#99`
(pinned Claude harness provisioning) merged 09-18, and `#104` (refresh of the
pinned daemon commit) merged 09-22T05:16
(`entries/2026/09/22/104126Z-progress-gardener-822294.md`). All of these landed
before 07:00 Pacific, so they precede this window. The design PRs `#96` and `#97`
remain open drafts with their gauntlets complete, awaiting merge. The
`fix-minion-town-claude-harness-supply-chain-hardening` job (release signature
re-verification) stays doomed and parked, not superseded by the merges, and needs
a maintainer decision on re-promotion. The last tick notes the earlier `#98`
conduct chain self-healed through a transient test flake and merged cleanly at
commit `c2f0aac` (`entries/2026/09/22/224322Z-progress-gardener-360cc6.md`).

## Garden (meta and internal)

### Scholar library

The scholar ingested one new web source, Simon Willison's link-blog post "Claude
Opus 5.5, GPT-6 Sol, GPT-6 Luna, and a new price war", under the standing scholar
cycle and a maintainer directive from 2026-09-22
(`entries/2026/09/23/001145Z-result-scholar-dbb4fd.md`). The ingest added four
sections, a new concept page `thinking-budget-output-overrun` describing the
failure mode where reasoning tokens exhaust the shared maximum-output-token budget
at the highest effort level, and rows to the `frontier-model-apis` and
`coding-agent-economics` topic pages. The concept is filed as a caution for the
fleet's own Opus high and max runs, drawn from an external report rather than a
measured claim about the garden. All eleven files landed through the normal
library landing path with no follow-on backlog.

### Fleet automation and reliability

One internal fix job, `fix-comment-watcher-blockquote-address-drop`, exited with
the transient-signature code after its single backed-off retry at about 57
seconds and was left in `doin` for the reaper to mark split-eligible or surface
(`entries/2026/09/23/003454Z-progress-gardener-fe6789.md`). Separately, the arc's
own outward press job `claude-on-minion-town-press-20260922-193551` had its
handler fail with a plain error, its output captured and escalated to the gardener
inbox and left for the reaper
(`entries/2026/09/22/194348Z-error-gardener-13cb98.md`). Both are transient
handler failures awaiting reaper disposition rather than diagnosed defects.

### Monitoring cadence

The three completion-press ticks in the window
(`entries/2026/09/22/104126Z-progress-gardener-822294.md`,
`entries/2026/09/22/164214Z-progress-gardener-e7cf1a.md`,
`entries/2026/09/22/224322Z-progress-gardener-360cc6.md`) also corrected a false
alarm about their own schedule. The first two ticks read an apparent multi-day
dispatch gap after 09-18 and worried the press had gone silent through the fleet
outage; the last tick found this was an artifact of the date-subdirectoried
`tada` files and confirmed the press has in fact been dispatching on roughly a
six-hour cadence throughout. No new arc doom appeared in the window, no arc job
left the board without a completion report, and the final tick sent no maintainer
message on anti-fatigue grounds since the two carried non-moot dooms were already
flagged earlier in the day. Every tick left the arc schedule standing.
