# Daily progress summary: 2026-09-13 (Pacific)

Window `[2026-09-13T07:00:00Z, 2026-09-14T07:00:00Z)`.

## Abstract

The endojs/endo-but-for-bots Dependabot cohort dominated the day. Eight bot PRs,
#1267 through #1274, all reached a terminal disposition on the 13th: three merged
(the `actions/deploy-pages` 5.0.0 to 5.0.1 bump, `marked` 17 to 18, and the grouped
19-update all-minor-patch PR) and five closed as reject or supersede (`@vitest/browser`
4 to 5, `lerna` 8 to 10, `eslint-plugin-unicorn` 73 to 74, `@changesets/cli` 2 to 3,
and `electron` 43 to 44, the last three on Node-floor or peer-range incompatibilities).
A recheck sweep early on the 14th then confirmed the whole ledger terminal and the open
bot-PR census empty. Separately, PR #945 (the Endor bytecode precompile and
content-addressed cache design) was approved by kriskowal, un-drafted, rebased onto
`llm`, merged, and receipted; and PR #877 was rebased and woven onto current `llm` with
full Rust and JavaScript execution evidence. On the garden itself, five changes landed
on `main2`: an Ironhorse Map/Set gap-sequencing design, the retirement of the local-qwen
`hermit` lane (pinned inert at zero rather than removed), a dead-letter standing-readdressee
design, and two Dependabotany reliability fixes (a 7200-second recheck handler budget and
a broadened terminal-ledger preflight gate). A gateway authenticated-peer-identity design
for endo-but-for-bots landed on the bot fork without a PR, pending a maintainer disclosure
decision. On minion.town the Claude-on-minion.town completion press ran ticks 20 through
22, all nominal, and a gateway containment drift check on the production host came back
clean with no change. Fourteen entries fell in the window (nine progress, one weaver
result, and four botanist and gardener messages). The board recorded 34 completions, about
40 claims and 40 posts (inflated by a few exit-0-unsatisfying requeues), and zero dooms.

## endojs/endo-but-for-bots

### The Dependabot cohort (#1267 through #1274)

Every open Dependabot PR resolved on the 13th, driven by the dependabotany botanist
lane. Three merged:

- **#1267** `actions/deploy-pages` 5.0.0 to 5.0.1: MERGE-NOW. The single call site moved
  to a tag adding jittered, capped backoff to deployment-status polling, with no
  dependency, permission, or install-behavior change; merged at 20:04Z as `00c3c65c`
  ([ledger](../../../entries/2026/09/13/200500Z-message-botanist-0a373c.md)).
- **#1269** `marked` 17.0.6 to 18.0.11 in `@endo/chat` and `@endo/spaces-util`: MERGE-NOW.
  A genuine forward major on a zero-dependency package with a stable consumed surface,
  OSV clean, maturity floor passed, published under trusted-publishing OIDC; merged at
  21:41Z as `3fb02fde`
  ([ledger](../../../entries/2026/09/13/214304Z-message-botanist-7b6372.md)).
- **#1268** the grouped all-minor-patch PR bumping 19 updates: MERGE-NOW. Diligence
  cleared the full transitive set (the octokit and typescript-eslint chains, a
  `content-type` major, and an `@earendil-works` pi restructure introducing
  `@earendil-works/chord` pinning `esbuild`). Its auto-conduct reached green CI on a
  rebased head, but peer #1269 merging into `llm` mid-flight forced a `yarn.lock`
  conflict, so a one-shot recheck (`dependabotany-recheck-endo-but-for-bots-pr1268`) was
  wired to fire at 23:00Z to re-run diligence on the rebased head
  ([ledger](../../../entries/2026/09/13/220135Z-message-botanist-0dc8df.md)). That recheck
  drove the head to green and merged #1268 at 23:45Z as `e08412fb`.

Five closed without merging:

- **#1270** `@vitest/browser` 4.1.11 to 5.0.0: REJECT (incompatible). `preact-container`
  still pins `vitest ^4.1.11` while the new browser package requires peer `vitest`
  exactly 5.0.0, an empty intersection; closed at 20:09Z
  ([ledger](../../../entries/2026/09/13/200927Z-message-botanist-7d24a3.md)).
- **#1272** `eslint-plugin-unicorn` 73 to 74: REJECT. unicorn 74 requires Node 22 or
  newer, excluding the project's supported 20.17.0 floor; closed at 20:06Z
  ([ledger](../../../entries/2026/09/13/200642Z-message-botanist-49a9ae.md)).
- **#1274** `electron` 43.4.1 to 44.2.0: REJECT. `packages/familiar` advertises Node
  `^22.0.0 || ^24.0.0`, but electron 44.2.0 requires Node 22.12.0 or newer, excluding
  the 22.0.0 through 22.11.x interval; closed at 20:12Z after 7 Familiar tests and lint
  and type checks passed and advisory queries came back empty
  ([ledger](../../../entries/2026/09/13/201545Z-message-botanist-81dde4.md)).
- **#1271** `lerna` 8.2.4 to 10.0.1 and **#1273** `@changesets/cli` 2.31.0 to 3.0.2 were
  both closed in the same sweep (20:06Z and 20:12Z) as part of the terminal set
  ([sweep](../../../entries/2026/09/14/043750Z-message-gardener-ec91bb.md)).

Early on the 14th a recheck sweep re-derived the cumulative ledger and re-fetched live
GitHub state: no now-due embargo verdict was owed, the live open `dependabot[bot]` PR
census was empty, and the entire 1267-through-1274 set was confirmed terminal, with the
last true embargo (#1168) long since merged. No disposition was executed and no schedule
wiring changed; the daily backstop was retained to guard a fully terminal ledger
([sweep](../../../entries/2026/09/14/043750Z-message-gardener-ec91bb.md)).

### PR #945 merged (Endor bytecode precompile)

kriskowal's review carried a single ask, "conduct". The review job verified the PR was
open, draft, mergeable, and green, and routed the ask to a conductor
([review](../../../jobs/tada/endojs-endo-but-for-bots-pr945-review-e4e7a891.md)). The
conductor un-drafted the PR, ran the deterministic merge spine from an isolated worktree,
performed the mandatory pre-merge rebase onto live `llm` (the head was 1122 behind, and
rebased cleanly to exactly the PR's two design commits), reached green CI on the rebased
head (29 checks, code jobs path-filtered to skipped on a docs-only change), re-verified the
standing approval, and merged in the same job as merge commit `a894459f25b` at 14:15Z
([conduct](../../../jobs/tada/endojs-endo-but-for-bots-pr945-conduct.md)). A completion
receipt was then archived and posted
([receipt](../../../jobs/tada/endojs-endo-but-for-bots-pr945-receipt.md)).

### PR #877 rebased onto llm

A weaver rebased #877 from `1199cbe4` onto `llm` at `12a9aa4a` and force-pushed with
lease, new head `61045449`. Conflict resolution retained both the base's CommonJS
reverification and the PR's dual-build evidence, and the fixture-directory rename that
intersected a newer fixture-parity ratchet was taught to the JavaScript oracle, the golden
generator, and the Rust manifest. Real-execution evidence covered a compartment-mapper
suite passing 45 tests under each of two configurations, `cargo test -p endo` at 400
tests, `cargo test -p xsnap` at 142, and clean lint. GitHub reported the head mergeable
against `llm` with CI in progress
([weave](../../../entries/2026/09/13/140757Z-result-weaver-275ca6.md)).

### Gateway authenticated-peer-identity design (landed on the bot fork)

A 309-line design, `designs/gateway-authenticated-peer-identity.md`, was written for
endo-but-for-bots and committed to a branch on the bot fork `kriscendobot/endo-but-for-bots`
as `845f5ac9b`, with no PR opened and `designs/README.md` untouched, per the job's
directive to hold the publish and PR step for further authorization. The design documents,
per transport, that `greeter.hello` takes the peer's node number as a self-asserted
argument with no reciprocal identity check, links that gap to a concrete cross-peer
following and capability-leak sequence, proposes a reciprocal agent-binding proof
generalizing OCapN's, and lays out a staged migration. It carries five maintainer-facing
open questions; the maintainer was notified and a disclosure hold was parked
([design](../../../jobs/tada/design-gateway-authenticated-peer-identity.md)).

## kriscendobot/minion.town

### Completion press (ticks 20 through 22)

The Claude-on-minion.town completion press ran ticks 20, 21, and 22 across the window
(and outward-facing arc presses on their own cadence), each a read-only reconciliation of
the roughly 150-job roster against the prior tick, reporting about two arc completions per
window, all clean, with zero dooms, zero policy refusals, and nothing absent without a
report. The design orchestration `claude-on-minion-town-designs` remains complete with all
seven design children settled. The arc's one artifact-level blocker is unchanged: endo
PR #1125 is still a green draft (head `fb861830`, 34 CI checks pass or skip) carrying a
stale CHANGES_REQUESTED from kriskowal's 2026-09-12 16:41Z review that predates the head,
with a re-review requested but none landed. No qualifying event arose, so no maintainer
message was sent, per the anti-fatigue rule
([tick 20](../../../entries/2026/09/13/122246Z-progress-gardener-e1c1f3.md),
[tick 21](../../../entries/2026/09/13/183913Z-progress-gardener-4a8f88.md),
[tick 22](../../../entries/2026/09/14/003806Z-progress-gardener-3a704d.md)).

### Gateway containment drift check

A recursive drift scan of the live vhost store on the deployed minion.town EC2 host,
reached read-only over SSM, returned a clean no-change pass: all three de-registered
records remain absent from the active store and present in the revoked tree, the
dckc-owned active set holds at 20 (the same 18 content-only weblets plus the two known
benign powers-bearing weblets `counter` and `@none`), exactly four powers-bearing records
remain store-wide, no dangerous escape-shape record is active, and the total record count
is unchanged at 54. The powers plane is deliberately open under kriscendobot/minion.town#58,
which is authorized state rather than drift, so the drop-in was not re-armed. No commit and
no maintainer alert. The one still-open item is the maintainer's pending 09-03/09-04
decision on whether the two benign dckc powers weblets should be revoked or left as
authorized experimentation
([drift check](../../../jobs/tada/fu-minion-town-containment-gateway-endo-sock-1-20260914-043506.md)).

## Garden-meta

### Infrastructure and designs landed on main2

Five garden changes landed:

- **Ironhorse Map/Set gap sequencing** (`6ce1c61ab1`): added
  `designs/ironhorse-js26-map-set-gap-sequencing.md`, recovering and citing all four
  engine-wide gaps, specifying a serial four-increment dependency order with entry, exit,
  regression, and test262 gates, and recording expected coverage deltas and blast radius.
  It notes that all four fixes already landed, so none should be queued unless a fresh
  pinned sweep proves a regression; 47 targeted oracle-backed Ironhorse tests passed
  ([design](../../../jobs/tada/design-ironhorse-js26-map-set-gap-sequencing.md)).
- **Retire the local-qwen `hermit` lane** (`93b5a5a573`): the decision was inert-at-zero,
  keeping the worker kind registered but pinning its count to 0, rather than an outright
  removal that would churn the worker spine and entangle three live neighbors sharing the
  `local` provider and on-box Ollama serving (the qwen3.6 mentor trial, the sysop
  `local-model` provisioning op, and the friar lane). The scaler's `scale()` now clamps a
  hermit count to 0 at the single choke point, `set-hermits.sh` refuses a nonzero count,
  and retirement banners were added to the docs; nine test suites stayed green
  ([retire](../../../jobs/tada/retire-local-qwen-hermit-lane.md)).
- **Dead-letter standing-readdressee design** (`4b3ad8adbb`): added
  `designs/dead-letter-standing-readdressee.md` and indexed it in `designs/README.md`,
  designing a completion-safe unread-to-deadmail handoff, context-carrying continuation
  jobs, and explicit live, staged, and readdressed sender receipts, and rejecting
  permanent per-job addressees, role fan-out, and maintainer fallback. It has no
  unresolved maintainer questions, so it landed bare; the implementation is a future build
  ([design](../../../jobs/tada/design-deadletter-standing-readdressee.md)).
- **Dependabotany recheck handler budget** (`12fcaa50c4`): `set-schedule.sh` now gives the
  `dependabotany-recheck-*` schedule family a default 7200-second handler timeout,
  mirroring their existing preflight default, because the #1268 recheck deterministically
  hit the generic 2400-second wall despite needing botanist review, CI shepherding, and
  merge handling. An explicit value still wins and the budget survives later cadence edits;
  tests and docs were updated
  ([budget](../../../jobs/tada/improve-dependabotany-schedule-budget.md)).
- **Dependabotany terminal-ledger preflight gate** (`4d51ea7f43`): broadened the daily
  backstop's drain matcher in `dependabotany-preflight.sh` to recognize explicit
  terminal-ledger prose ("all rows terminal", "no embargo remains") as a drained set,
  stopping the routine daily re-dispatch of a no-op sweep after every tracked PR had
  merged or closed. The soundness argument holds because a skip still requires zero open
  Dependabot PRs and an active embargo is by construction an open PR; 26 preflight tests
  passed
  ([gate](../../../jobs/tada/improve-dependabotany-terminal-ledger-gate.md)).

At the very start of the window the reaper's `improve-gauntlet-transient-doom`
self-heal completed at 07:00Z, a garden-infra fix for the transient-doom class that had
briefly touched #1125's fix stage the prior day, noted as fleet health rather than an arc
fault
([tick 20](../../../entries/2026/09/13/122246Z-progress-gardener-e1c1f3.md)).

### Operational churn

The two merged Dependabot majors each had a benign transient hiccup before completing:
the #1269 handler exited 0 without the completion signal and was requeued on cycle 1 at
64 seconds
([#1269 requeue](../../../entries/2026/09/13/201624Z-progress-gardener-a520cb.md)), and the
#1268 handler did the same at 32 seconds
([#1268 requeue](../../../entries/2026/09/13/202538Z-progress-gardener-8faea5.md)); both
went on to merge. The #1268 one-shot recheck merged the PR at 23:45Z but its handler also
tripped the generic 2400-second wall (recorded as a deterministic deadline overrun at
2401 seconds and stamped for the reaper), which is exactly the failure the recheck
handler-budget fix above was landed to close
([overrun](../../../entries/2026/09/13/234556Z-progress-gardener-ec7e27.md)).
