# Daily progress summary: 2026-09-23 (Pacific)

Window `[2026-09-23T07:00:00Z, 2026-09-24T07:00:00Z)`. Thirteen journal entries fell
inside it (the two earliest files in `entries/2026/09/23/` predate 07:00Z and belong
to the previous periodical), and 110 job reports landed in `jobs/tada/` during the
window. No entry carries a top-level `project:` field, so the project grouping below
is inferred from each entry and report; garden-internal work has its own section.

## Abstract

A busy, productive day after the quiet 09-22. The garden itself absorbed the most
change: Opus 5.5 became the Anthropic `mentor` default, two new health monitors
(comment-acknowledgment latency and journal contention) were built and then tuned,
the container lost `--privileged` and bot-user sudo, and a GraphQL quota drain was
traced to the worktree sweeper and fixed. On endo-but-for-bots, the design PR
`#1226` merged and its follow-on build opened draft `#1336` the same night, a
five-stage guest-locator federation orchestration launched and produced drafts
`#1332`, `#1333`, and `#1335`, and the `#1100` gauntlet ran six panel and fix rounds
without converging. On minion.town, `#112` (daemon repin) and `#113` (2 MB MCP body
limit) merged, two Codex inference explorations opened drafts `#115` and `#116`, and
the federation town build (`#117`) surfaced a serious account-daemon security
blocker that holds activation. Failures were mostly transient handler exits; three
fix jobs recorded rc=1 errors, and at least two of those had in fact already landed
their fix.

## endo-but-for-bots

### Merges and new drafts

- `#1226` (the endo guest stdio MCP design) was un-drafted, rebased onto live `llm`,
  passed all 30 checks, and merged at 04:16Z as `6726b0f`
  (`jobs/tada/2026/09/24/merge-endojs-endo-but-for-bots-pr1226-20260924.md`). Two
  review jobs on it and a receipt also completed. The unblocked build then produced
  draft `#1336`, implementing the single-tenant MCP adapter shape on pinned base
  `llm-6726b0f`; the confined shape remains a follow-up
  (`jobs/tada/2026/09/24/build-endo-guest-stdio-mcp.md`).
- A fix for corrupt ava snapshots under `@endo/init` opened draft `#1334`. The root
  cause is the immutable-buffer shim repointing `Uint8Array.prototype.constructor`,
  and the fix lands in the test harness because SES forbids restoring the original
  identity (`jobs/tada/2026/09/23/ebfb-lockdown-ava-snapshot-uint8array-corruption.md`).
- A review of `#1248` requested by kumavis was posted, listing 2 high, 9 medium, and
  10 low findings on object-capability discipline and formula durability
  (`jobs/tada/2026/09/24/endojs-endo-but-for-bots-pr1248-151c786d.md`).

### Guest-locator federation orchestration

A planner job launched a five-child serial orchestration,
`endo-minion-town-guest-locator-federation`, covering an endo build, a town build, a
release gate, a deploy, and live acceptance. It also opened draft roadmap PR `#1332`
on frozen base `llm-f9cbcfc`
(`entries/2026/09/23/205635Z-result-ground yourself, decompose the remaining real work into child jobs, park-f197a7.md`;
that entry's `role:` field holds a fragment of prompt text instead of a role name).
Stage 1 opened draft `#1333` with a new `endo adopt-locator` command and a stricter
`adoptFromLocator` (`jobs/tada/2026/09/23/endo-minion-town-federation-endo-build.md`).
A related build bound OCapN gateways and retention queries to authenticated peer
sessions in draft `#1335`
(`jobs/tada/2026/09/23/endo-ocapn-gateway-session-binding-build.md`). The
release-gate child then exited without a completion signal after 53 seconds
(`entries/2026/09/23/215429Z-progress-gardener-d6f845.md`), which fits the
security blocker reported under minion.town below.

### Gauntlet on #1100 and re-export policy validation

The `#1100` exo-stream gauntlet went through a viability check, a doomed first
cleaning run, and a restarted `20260923b` run with six panel rounds and six fix
rounds. It ended at `review-budget-reached`: CI green, but the subjective review did
not converge, so the PR is left for a human decision
(`jobs/tada/2026/09/24/ebfb-exo-stream-pr1100-gauntlet-20260923b.md`). Separately, the
new re-export deprecation policy was validated against a synthetic draft on the
`kriscendobot/endo-but-for-bots` fork (`#7`, never to merge), with every fixture case
passing (`jobs/tada/2026/09/23/validate-reexport-policy-ebfb-20260923.md`).

## minion.town

### Merges and deployment

- `#112` (the endo repin after `#1329`) was conducted and merged, with a receipt, and
  a verify job confirmed the production daemon is healthy on the new `f9cbcfc` pin
  (`entries/2026/09/24/042213Z-progress-gardener-25411b.md`).
- `#113` raised the `/mcp` JSON body limit from 100 KB to 2 MB, leaving every other
  route at the default, and merged as `3062124`
  (`jobs/tada/2026/09/23/minion-town-mcp-body-limit-2mb.md`). A duplicate job for the
  same change found it already merged and did nothing
  (`jobs/tada/2026/09/23/mcp-body-limit-2mb.md`).
- CLIPOMETER (draft `#84`) was rebased, revalidated, and published canonically from
  the real maintainer guest, with the clip live and 27 of 27 tests passing
  (`jobs/tada/2026/09/23/minion-town-clipometer-pr84-canonical-real-guest.md`).

### Designs, explorations, and the federation blocker

- Draft `#114` designs SIWE guest recovery. It supersedes the on-chain
  authorization design and withdrew a stale deploy job
  (`jobs/tada/2026/09/23/design-minion-town-siwe-guest-recovery.md`).
- Two Codex inference explorations opened drafts. `#115` is an OpenAI Responses-API
  backend that has not yet run against a live key. `#116` is a flagged
  Codex-subscription backend with 11 documented gaps
  (`jobs/tada/2026/09/23/build-minion-town-codex-api-key-inference-20260923.md`,
  `jobs/tada/2026/09/23/build-minion-town-codex-subscription-inference-20260923.md`).
- A mentat decision retired the guest formula-id peer-fetch path in favor of the
  guest-to-guest invitation workflow, withdrew the parked verify job, and closed out
  on garden#58 (`jobs/tada/2026/09/23/mentat-guest-peer-fetch-vs-invitation-20260923.md`).
  The parked `#81` fallback fix was later promoted and completed; `#81` is updated
  but still draft, awaiting a gauntlet request.
- Federation stage 2 opened draft `#117` (account locator and deployment wiring) but
  deployed nothing. It reports that installing the OCapN network on the shared
  account daemon, as `#1333` proposes, would let any locator holder enumerate and
  obtain every formula on that daemon, and with signup open that amounts to account
  daemon takeover. This was escalated to the maintainer and holds activation
  (`jobs/tada/2026/09/23/endo-minion-town-federation-town-build.md`).

### Arc press ticks (kriscendobot/garden#89)

Three completion-press ticks reported the Claude-on-minion.town arc as nominal, with
no arc dooms or stalls. The 16:14Z tick noted a roughly 17.5 hour scheduler
quiescence before the window resumed
(`entries/2026/09/23/161407Z-progress-gardener-41ba87.md`). The 22:22Z tick recorded
the `#112` build completing with checks green
(`entries/2026/09/23/222158Z-progress-gardener-58893a.md`). The 04:22Z tick recorded
the `#1226` merge and expected `build-endo-guest-stdio-mcp` to be promoted next, which
happened before the window closed
(`entries/2026/09/24/042213Z-progress-gardener-25411b.md`). That tick also recorded a
roughly 2.5 hour lag between a maintainer's "Please conduct" comment on `#112` and
the watcher acting on it; a fix job for that miss completed in the window.

## Garden (meta and internal)

### Model routing and designs

The combined Opus 5.5 and TypeSafe Jev design was split into two review surfaces,
garden PRs `#108` and `#109`
(`entries/2026/09/23/163521Z-result-gardener-662b7d.md`). `#109` (Jev classification)
merged after a pilot-ready report. Because that merge advanced the frozen base
shared with `#108`, `#108` needed a repin, which then completed
(`jobs/tada/2026/09/23/kriscendobot-garden-pr109-conduct-20260923.md`). The Opus 5.5
build then landed Option B: `claude-opus-5-5` is the Anthropic `mentor` default and
the mentor-to-minion cost downshift is removed
(`jobs/tada/2026/09/23/build-opus55-tier.md`). Design PR `#95` (re-export deprecation
policy) was closed as resolved rather than merged, since its answers and build had
already landed on `main2` (`jobs/tada/2026/09/23/conduct-garden-pr95-20260923.md`).
The policy build itself added the `no-plain-reexport` pre-push probe, the
`reexport-auditor` seat, and a vendored Babel parser
(`jobs/tada/2026/09/23/build-reexport-deprecation-policy-20260923.md`).

### New monitors and hardening

- The comment-acknowledgment latency watch and the journal contention watch were
  each designed and built (`jobs/tada/2026/09/23/build-comment-latency-watch.md`,
  `jobs/tada/2026/09/23/build-journal-contention-watch.md`). Follow-up jobs then
  fixed a false-alarm flood, tuned notice failures and deadlines, made the latency
  watch honor the primary-quota cooldown, and self-healed an empty-sample crash.
- The garden container dropped `--privileged` and bot-user sudo, keeping only
  `SYS_ADMIN` for systemd
  (`jobs/tada/2026/09/23/harden-garden-container-no-privileged-no-sudo.md`). Two
  follow-ups fixed a maintainer-substring probe bug and added gh timeouts.
- The hourly GraphQL quota exhaustion was traced to `garden-worktree-sweeper`
  querying every historical report. It now checks for residue first and reads over
  REST (`jobs/tada/2026/09/23/diagnose-hourly-graphql-quota-exhaustion-20260923.md`).
- Orchestration and deploy fixes landed. `orchestrate.sh` now re-derives a child's
  real disposition instead of latching a false failure from the fleet default
  timeout (`jobs/tada/2026/09/23/fix-orchestrate-latched-child-failure.md`), and
  rolling deploys pin the released sha so a moving `main2` cannot strand a canary
  (`jobs/tada/2026/09/23/fix-rolling-deploy-canary-stranded-on-advanced-target.md`).
  Also completed: fixes for the blocked-gate refusal, the finished-but-not-completed
  requeue, the frozen-base sweep close race, local-verify package manager detection,
  shared-subscription quota checkpoint aggregation, triager pacing churn, and
  budget-pool admission in end-to-end fixtures.
- Seven host-pinned canary probes on `endolin-garden2` completed as rolling-deploy
  validation.

### Failures and transient exits

- Transient rc=75 exits after a single retry: `design-comment-latency-watch`,
  `build-journal-contention-watch`, `kriscendobot-garden-pr109-conduct-20260923`, and
  `build-reexport-deprecation-policy-20260923`
  (`entries/2026/09/23/165427Z-progress-gardener-b74984.md`,
  `entries/2026/09/23/180658Z-progress-gardener-fabe8b.md`,
  `entries/2026/09/23/181825Z-progress-gardener-7b57ce.md`,
  `entries/2026/09/23/183542Z-progress-gardener-e8eea1.md`). All four later reached
  `tada`.
- rc=1 handler failures: `fix-orchestrate-latched-child-failure`,
  `fix-rolling-deploy-canary-stranded-on-advanced-target`, and
  `fix-hardening-probe-blocks-rolling-deploy`
  (`entries/2026/09/23/204352Z-error-gardener-64e331.md`,
  `entries/2026/09/23/233522Z-error-gardener-3218c1.md`,
  `entries/2026/09/24/065043Z-error-gardener-1980cd.md`). The first two were re-run,
  and each re-run found that a prior attempt had already pushed the complete fix. The
  third was still unresolved at window close.

### Bus housekeeping

Four dead-letter messages were promoted and completed as `deadmail-*` jobs, and two
follow-up (`fu-*`) jobs from earlier scheduled work also completed.
