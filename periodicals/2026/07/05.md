---
kind: periodical
cadence: daily-progress-summary
pacific_date: 2026-07-05
window_start: 2026-07-05T07:00:00Z
window_end: 2026-07-06T07:00:00Z
author: journalist
---

# Daily progress summary: 2026-07-05 (Pacific)

## Abstract

The Pacific day split cleanly in two. It opened with the liaison quietly draining
the maintainer inbox and reworking the bulletin in the small hours (roughly 01:36
to 02:01 Pacific), then went dark for most of the working day; no job moved on the
board between about 02:00 and 10:16 Pacific. At 10:16 Pacific (17:16 UTC) the fleet
came back to life, the leader marker was re-designated to `endolinbot`, and the
board ran hot from there into the following UTC morning. Two programs dominated the
active stretch: the XS-to-Rust Endor engine port (endo-but-for-bots PR #600, still
DRAFT) advanced two whole supervisor stages, and a scholar drained the entire
MetaMask/ocap-kernel comment-fragment ingest backlog. Alongside them, a batch of
prosecutor review-retrospectives closed out the day's endo-but-for-bots review
comments (one recorded miss, the rest dismissed), the endoclaw and daemon caplet
work took several steps, and a run of reliability work landed on the garden's own
infrastructure while the reaper churned through transient and deterministic handler
overruns. One item needs a maintainer eye eventually: the recurring
`fable-review-fix-garden-scripts` job that kept getting misclassified as a transient
blip before it finally completed.

## Endor engine port: XS to Rust (endo-but-for-bots PR #600, DRAFT)

The largest single program of the day. It carried through two supervisor stage
transitions and a mid-stage regression catch.

**Results and stage transitions.** Supervisor `s7` accepted the whole of stage 3
(`../../entries/2026/07/06/021036Z-progress-gardener-180e34.md`): its nine stage-3b
children all finished, and a full independent reproduction from a fresh checkout
reran the per-section test262 dual-run numbers with zero divergence across twenty
sections. That reproduction did exactly what it exists to do and surfaced a real
regression: bound functions in callback position dispatching at program start, which
aborted the whole built-ins/Array sweep and silently diverged through `then(bound)`
and `bound.call`/`bound.apply`. A fixer was posted for it and then verified landed by
supervisor `s8` (`../../entries/2026/07/06/034556Z-progress-gardener-3d986f.md`): the
Array tree came back fully covered with no process abort. `s8` also accepted the
CESU-8 to UTF-16 string-representation orchestration (design, build, and test
children all verified), then dispatched stage 4, Hardened JavaScript, as a serial
halt-on-failure orchestration of eight children (accessors, classes, generators,
async-await, modules, compartment, lockdown/harden, ses-conformance). Continuation
`s9` is parked behind stage 4. The PR stays DRAFT by the maintainer's finish-line
directive; the maintainer enters the loop once, at the end.

**Board activity.** The stage-3b orchestration finished and moved to `tada`, the
strings-utf16 orchestration ran its three children and finished, and the stage-4
orchestration was recorded and began promoting its first child. Supporting jobs
completed through the evening: opcode-cost meter instrumentation, meter calibration
stage c1, the test262-and-xst corpus harness, and an expanded phase-3 directory-input
run. Several stage-3b children (promises, xsre-core, xsre-integration) hit the 2400s
handler wall-clock budget and were stamped as deterministic deadline overruns for the
reaper rather than treated as external kills
(`../../entries/2026/07/05/214727Z-progress-gardener-1c59d1.md`,
`../../entries/2026/07/05/225354Z-progress-gardener-2ac369.md`,
`../../entries/2026/07/06/000020Z-progress-gardener-642152.md`).

## endojs/endo-but-for-bots (the fork)

**Review retrospectives.** A wave of prosecutor second-loop retrospectives cleared
the day's review comments across PRs #288, #442, #592, #595, #602, and #604. Almost
all resolved as not-a-miss / new-direction dismissals, recorded durably so the
comments are never re-litigated: #288's cbor-frame asks
(`../../entries/2026/07/05/220700Z-result-gardener-c71c7e.md`), three separate #592
watchDirectory-refactor comments including a garbled bot reply that was root-caused
and fixed on `main2` (`../../entries/2026/07/05/231556Z-result-gardener-f7653e.md`,
`../../entries/2026/07/05/232201Z-result-prosecutor-9490d8.md`, and
`../../entries/2026/07/05/232320Z-result-gardener-8d3d2b.md`), the #595 unredactError
exploratory-design nudges, the #602 freezable-TypedArray proxy proposal
(`../../entries/2026/07/05/223734Z-result-gardener-1dabaa.md`), and kumavis's own
review activity on the #604 privacy-cards caplet, which the garden reviewed rather
than authored. The one exception was a recorded **miss** on PR #442
(`../../entries/2026/07/05/221839Z-result-gardener-c3adb2.md`): a typedef-only `.js`
module that should have been a hand-written `.d.ts`, a convention already encoded from
an identical #58 maintainer directive that failed to bind again. It minted the
`typedef-location-dts` cluster and dispatched an improvement job through the severity
bypass; that improvement job (`review-improve-typedef-location-dts`) completed the
same evening, adding a deterministic pre-push gate probe.

**Endoclaw and daemon caplet work.** Three builds landed or advanced: the
endoclaw-timer daemon formula integration, the endoclaw network-fetch HTTP-client
capability, and a follow-on endoclaw network-fetch build. The daemon Docker self-host
build completed, as did two design jobs, gateway bearer-token auth reconciliation and
daemon agent-tools mount/git capability reconciliation. A lint job cleaned JSDoc
warnings on the endo master branch.

**PR gauntlets and shepherding.** The #608 gauntlet and the #609 endoclaw-timer
gauntlet both completed. PR #288 saw a refresh, a shepherd pass, and its Fable review
chain post through to `tada`. PR #595 got its report-back and a gap-revealing probe
for the unredactError API. PR #442's fix for review 4629047816 completed.

## ocap-kernel library scholarship (read-only reference shelf)

The scholar drained the MetaMask/ocap-kernel comment-fragment ingest backlog end to
end. Six source files were ingested across five cycles, the eleventh through
sixteenth ocap-kernel sources: `VatHandle.ts`
(`../../entries/2026/07/05/221248Z-result-gardener-f8f15f.md`), its in-vat mirror
`VatSupervisor.ts` (`../../entries/2026/07/05/234356Z-result-gardener-814f39.md`),
`KernelRouter.ts` (`../../entries/2026/07/06/000444Z-result-gardener-dfe430.md`),
`KernelServiceManager.ts` (`../../entries/2026/07/06/002603Z-result-gardener-46dae8.md`),
and finally `BaseDuplexStream.ts` plus `exo.ts`, which drained the queue
(`../../entries/2026/07/06/004815Z-result-gardener-680385.md`). Every cluster came
back with no comment-versus-code drift, and each cycle's scoped link-check passed. A
closing cleanup job then cleared the two standing library-hygiene backfill notes
(`../../entries/2026/07/06/021122Z-result-gardener-c4b132.md`): the three
`KernelQueue.ts` leaf sections got their Section rows onto the topic pages, and two
dangling `sources/README.md` wikilinks were rewritten to resolvable topic links. A
whole-repo scan noted roughly two dozen pre-existing advisory danglers on unrelated
pages, left for a future librarian sweep.

## Garden meta (infrastructure, reliability, roles, onboarding)

**Leader and the quiet gap.** The board's long midday silence ended with a leader
re-designation to `endolinbot` at 17:19 UTC, after which the fleet resumed at full
tilt. The pattern reads as a recovery, the fleet coming back after an idle stretch.

**Reliability and self-improvement jobs.** Several mentor-driven improvements landed:
sharper transient-versus-deterministic classification for empty mentor output and for
inner-claude exits, a partial-clone-directory self-heal, a changed-gate fix for
pre-existing danglers in the library link check, gardener memory confinement, a
reaper fix so requeue kills or waits for a live handler, a fix so
`ensure-project-worktree` refuses a silently stale fetch, and a port of the watcher
fail-floor to the mention and issue-inbox watchers. The scheduler gained its
timezone-anchored cadence (`scheduler-timezone-anchored-cadence`), the change that now
pins this very periodical to local midnight. The onboarding orchestration finished,
landing the vocab/tutorial wiring and a slimmed README and CLAUDE.md. A
leader-follower determinism design completed, and an investigation into the Fastmail
masked-email API for bot personas wrapped up.

**Reliability noise worth flagging.** The `fable-review-fix-garden-scripts` job was a
persistent thorn: it cycled through exit-0-without-completion-signal, rc=1, and
rc=143 outcomes, and a gardener flagged twice that its near-constant elapsed time
looked like a deterministic overrun being misclassified as a transient blip
(`../../entries/2026/07/05/175826Z-error-gardener-49c845.md`,
`../../entries/2026/07/05/203103Z-error-gardener-b954ec.md`). It eventually completed
at 20:41 UTC along with its three follow-up jobs. The `port-xs-to-rust ... s7` job and
the `scholar-clear` cleanup both took a failed cycle before succeeding on requeue
(`../../entries/2026/07/06/005415Z-error-gardener-d210af.md`,
`../../entries/2026/07/06/005449Z-error-gardener-554958.md`). Two garden-repo issues
were serviced through the issue inbox (issues #26 and #27) alongside GitHub-wide
@-mentions, and the message bus dead-lettered and re-promoted a steady trickle of
inter-agent messages through the day.

**Scheduled dispatches.** The scheduler fired the prior `daily-progress-summary` runs
(the 05:10 and 23:05 dispatches, both completed) and the `plan-recalibrate` job during
the window.
