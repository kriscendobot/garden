# Daily progress summary: 2026-08-18 (Pacific)

Window `[2026-08-18T07:00:00Z, 2026-08-19T07:00:00Z)`.

## Abstract

A review-heavy, campaign-driven day led by **endo-but-for-bots**, where the PR
**#475** byteArray-narrowing arc dominated: a dozen morning review-thread
directives (a fleet-wide humans-only-resolve policy, an `ArrayBuffer.isView`
sensing fix, a `toThawed` helper benchmark, and a master-pin request proven
infeasible as stated) fed into a five-child serial campaign that advanced the base
onto a fresh `llm` snapshot, consolidated the byte helpers into
`@endo/immutable-arraybuffer`, retconned 39 commits into 13 clean per-package
commits, answered 17 review threads, and ran a gauntlet to a clean, green,
non-draft, mergeable state at head `b28bb1fc3` awaiting human re-review. Two PRs
merged there (the Ironhorse store roadmap **#998** and a 43-package Dependabot
group **#1037**), the hardened262 harness landed as draft **#1040** with its
gauntlet clean stage green, design PR **#987** (libgit2 and Zig) went approved and
green awaiting the merge word, and **#989** (worker-quiescence) halted after six
non-converging rounds. On **minion.town** two long design-doc gauntlets ran out of
road (**#37** ocap mailboxes halted when panel round 6 vanished, **#49**
vitals-weblet burned all six rounds without converging), the maintainer directed
the git-content-substrate reviews **#47**, **#48**, and **#21** closed pending a
fresh `*.ocap.site` redesign, **#29** was shepherded and woven to mergeable, and
**#36** was closed as superseded by the still-unmerged **#20**. The **endor and
xs2rust** parity effort landed a full fixture-parity ratchet-campaign design plus
its Increment 0 oracle scaffold, but the campaign halted at child 2 of 8 when the
CommonJS-require walker stalled, while two of four xs2rust debugger follow-ups
landed as drafts **#1038** and **#1039** and the Ironhorse WeakMap-upsert
follow-up landed on **#970** with a real XS-oracle pass. In garden meta, two
designs (quota auto-throttle and a muse worker kind) and four fixes landed on
`main2`, tightening job-kind eligibility fencing, directive-dedup lifecycle
semantics, standing never-close rules, and retrospective over-minting, all against
a bounded reaper-churn backdrop of about 34 requeue ticks and three
deterministic-overrun escalations.

## endo-but-for-bots

### The PR #475 arc (byteArray narrowed to a plain frozen Uint8Array)

This was the day's center of gravity, spanning review conversations, a base
advance, a build, a retcon, a feedback pass, a gauntlet, and a shepherd, so it is
traced here in one place.

Morning review directives (2026-08-18), against head `feat/narrow-bytearray-to-uint8`:

- Review-thread policy. A directive from erights had the bot unresolve every
  conversation it had resolved: all 47 bot-resolved threads were unresolved, the 8
  human-resolved left intact, and the bot committed to stop resolving on #475
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-e3925eb5.md`). kriskowal then
  made humans-only-resolve fleet-wide policy, confirmed on-thread
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-reply-humans-resolve-policy.md`).
- A mangled-comment investigation traced two historically corrupted comments (#475
  and #486) to shell backtick command substitution stripping the text, restored
  both in place, and added a fail-closed `comment-body-guard.sh` to the fleet gh
  wrapper on `main2` (commit `c7a979c618`)
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-review-f55c1aef.md`).
- Call-site fixes: making `bytesEqual`, `@endo/base64` encode, and ocapn
  `diagnosticEquals` sense genuine against emulated inputs by way of
  `ArrayBuffer.isView` landed in commit `c33a5845c` with tests
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-9885f3d8.md`).
- The shared thaw helper: kriskowal named it `toThawed` and asked for a benchmark
  to decide `view.at(index)` against a defensive copy. A designer job produced a
  Node micro-benchmark (full-scan crossover near 16 to 32 bytes, `.at()` winning at
  every size for partial and early-exit reads) and settled
  `@endo/immutable-arraybuffer` as the home, posted as a design note
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-6bff44d0.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr475-design-tothawed.md`). A sibling
  bare-deferral directive was a verified no-op
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-59c1c020.md`).
- erights annotation questions on `packages/ocapn/src/client/util.js`:
  `decodeSwissnum` narrowed to `SwissNum` (commit `f83e8813d`) and `toHex` narrowed
  to `Uint8Array` (commit `267e98f1d`), each with an in-thread reply
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-review-b4dd5851.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr475-review-662af34e.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr475-util-bytearray-view-type.md`).
- A master-pin request was proven infeasible as literally stated: the head carries
  roughly 2,351 commits not on upstream master by patch-id plus a hard
  `@endo/ascii` dependency, so a mechanical rebase would produce un-shepherdable red
  CI. A factual blocker and a two-path scope decision were routed to the maintainer,
  and no branch was rewritten
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-495be080.md`). A separate
  review directive parked the retcon pending erights follow-up and flagged the
  sequencing conflict with the in-flight pin job
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-review-237b89d7.md`).

The campaign (2026-08-19, serial orchestration, all five children terminal per
`journal/jobs/tada/endojs-endo-but-for-bots-pr475-campaign-20260819.md`):

1. Advance base and rebase. The base moved from `llm-a54c3ad` (183 commits behind)
   to a fresh `llm-c6b70e8`; the 37-commit head was rebased and force-pushed to
   `eb6b270c0`, curating a real ocapn CBOR-codec conflict onto the new
   `@endo/cbor`-backed architecture
   (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-advance-base-20260819.md`).
2. Consolidate bytes. `bytesToImmutable` and `bytesFromImmutable` were renamed to
   `frozenBytes` and `thawedBytes` and moved to `@endo/immutable-arraybuffer` (a
   `./shim.js` kept for compatibility), all importers updated, pushed to `2a1ca09e5`
   (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-consolidate-bytes-20260819.md`).
3. Retcon. 39 sprawling commits were regrouped into 13 clean per-package commits
   plus a separate `chore: Update yarn.lock`, head `554c5e413`, tree byte-identical
   to the reviewed head and still mergeable
   (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-retcon-20260819.md`).
4. Feedback. A solicitor answered 17 review threads against head `b28bb1fc3`,
   adding a commit to gate `concatBytes` solely on `isView` and an XS `Array.from`
   map-fn note, surfaced an `@endo/hex` re-broadening honestly, and posted two
   follow-up jobs
   (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-feedback-20260819.md`).
5. Gauntlet and shepherd. A five-lens panel returned pass with no production
   must-fix; the one CI red leg (`cover (24.18.0)`) was a known socket-teardown
   flake reproduced green locally, and a shepherd re-ran it to confirm all-green,
   open, non-draft, and mergeable at head `b28bb1fc3`
   (`journal/jobs/tada/endojs-endo-but-for-bots-pr475-gauntlet-20260819.md`,
   `journal/jobs/tada/endojs-endo-but-for-bots-pr475-shepherd-20260819.md`).

Open: #475 now sits non-draft, all-green, and mergeable on base `llm-c6b70e8`,
carrying a standing human changes-requested and awaiting maintainer and erights
review of the incremental changes. Loose ends flagged for humans include a
self-contradicting changeset on the removed `bytesToImmutable` and
`bytesFromImmutable` names, a dangling design cross-reference, and optional
read-path coverage for the hex and base64 emulated-wrapper branch.

### Gauntlets, merges, and PR completions

- PR **#998** (Ironhorse store roadmap) merged into `llm` at 2026-08-18T21:47Z
  (merge commit `7a520f13`, head `1de4173e`). Five review-directive jobs were each
  verified as no-ops confirming already-landed resolutions of Copilot findings on
  crank atomicity and rewind-on-checkpoint-failure, a store-backed resume guard,
  durable `collect()`, symbol-key-descriptor GC roots, and deliberate conservative
  marking of weak-collection entries pending ephemerons
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr998-review-619b094b.md` and its four
  siblings).
- PR **#1040** (hardened262 test262 harness mirror) was created as a draft mirroring
  `@endo/hardened262`, judged orthogonal to the existing test262-runner rather than a
  duplicate (`journal/jobs/tada/mirror-hardened262-endo-but-for-bots.md`). A gauntlet
  and shepherd directive drove CI green by excluding `packages/hardened262` from the
  two whole-repo TypeScript programs (commit `53e694691`), and the gauntlet clean
  stage passed with CI 27 of 27 green; panel review and un-draft remain owned by the
  driver (`journal/jobs/tada/endojs-endo-but-for-bots-pr1040-091aec5d.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr1040-gauntlet-clean.md`).
- PR **#989** (worker-quiescence-embargo design doc) gauntlet halted after failing
  to converge in six rounds. Fix round 6 applied every must-fix from the seven-seat
  panel (commit `712c472a28`, CI green), but a panel round 7 would exceed the
  six-iteration cap, so the gauntlet halted
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr989-gauntlet-fix-6.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr989-gauntlet.md`).

### Reviews and review-thread conversations

- PR **#972** (Justin subset spec docs): three review jobs all resolved the same
  wording ask, rewording "dangerous endowments" to "most expressive endowments" at
  `docs/justin.md:372` (commit `5b740e4f3`); two of the three converged as verified
  no-ops after a peer landed the byte-identical change during a race
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr972-review-649a8108.md` and siblings).
- PR **#282** (compartment-mapper fixture parity): a fresh kriskowal
  changes-requested asked for a driven ratchet campaign toward endor and node
  fixture parity, so a designer job was posted grouping the 33 excluded fixtures by
  blocking walker capability, with a PR reply
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr282-review-d4cb53a7.md`).
- PR **#719**: the ferried upstream PR was linked to `endojs/endo#3332` (the
  hardened URL shim) in the #719 body
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr719-cc0b4130.md`).
- PR **#993** (Preact compartment): verified no-op, superseded by and closed in
  favor of the merged PR #1031
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr993-1ffe8014.md`).
- PR **#1031** (preact-container, already merged): a flaked
  `test (24.18.0, macos-15)` leg was re-run to green using the fleet integration,
  no maintainer re-run needed
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr1031-01eac673.md`).

### Rebases, weaves, and shepherds

- PR **#876** (endor webcrypto and npm-run conditions) was rebased onto
  `origin/llm`, resolving textual conflicts and a silent semantic conflict (a
  duplicate `ARCHIVE_ENDOWMENTS_JS` merged to carry both `process` and `crypto`),
  moving from conflicting and dirty to mergeable at head `aa9b0641b` with a clean
  `cargo check`; a fresh shepherd and conduct are warranted since new commits landed
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr876-rebase.md`).
- PR **#987** (endor libgit2 and Zig cross-builds design doc): a shepherd found CI
  could not dispatch because the PR conflicted with base and handed off to a weaver,
  which resolved the `designs/README.md` index conflict (`f265f98c1` to `43dc5bae3`)
  and dispatched CI; a follow-up shepherd then drove all six checks green. The PR is
  approved and awaits the merge word
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr987-weave-20260819.md`,
  `journal/jobs/tada/endojs-endo-but-for-bots-pr987-shepherd-20260819.md`).

### Designs and design PRs

- The `toThawed` benchmark and API design note for #475 (traced in the #475 arc
  above).
- A new designer job for the #282 endor and node fixture-parity ratchet (see the
  endor and xs2rust section below).
- A parked builder budget-hold was queued to implement #987's revised
  libgit2-binding design after the Friday quota reset
  (`budget_resets_at` 2026-08-22T04:00Z), with a note that as parked it would route
  to mentor tier rather than Opus
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr987-2cc814f3.md`).

### Dependabot, CI, and other

- PR **#1037** (grouped `all-minor-patch`, 43 packages) merged onto `llm` at
  2026-08-19T01:07Z (merge commit `e1f0aa7df12c`), CI 26 of 26 green, an autonomous
  botanist disposition. A prettier 3.8.3 to 3.9.6 reformat was pushed to clear lint,
  and the transitive `dompurify` bump was a strict advisory reduction (19 down to 4)
  (`journal/jobs/tada/endojs-endo-but-for-bots-pr1037-dependabot.md`).
- The daily dependabotany backstop sweep recovered PR #1006, whose own botanist job
  had been doomed without a verdict: #923 confirmed reject and closed, #1005 closed
  as superseded by #1037, #1007 re-confirmed embargo to 2026-08-21, and #1006
  (`eslint-plugin-unicorn`) and #1009 (`npm-packlist`) both blocked on a fleet-wide
  node24-runner infra flake (`externals/node24/bin/node: No such file or directory`)
  plus CI-queue backlog, both escalated to the maintainer to merge directly
  (`journal/jobs/tada/dependabotany-recheck-endo-but-for-bots-20260818-170501.md`).
- Three stale-premise follow-up jobs closed as no-ops: a `this.self`
  inference-masking audit found the #713 fix was the daemon's last instance, the Node
  engine floor was already raised to `^20.17.0 || >=22.9.0`, and a `setTimeout`
  global lint directive already survives the genie-to-reminder rename
  (`journal/jobs/tada/fu-endojs-endo-but-for-bots-pr713-review-2b03f8c3-3.md`,
  `journal/jobs/tada/fu-endojs-endo-but-for-bots-pr870-dependabot-3.md`,
  `journal/jobs/tada/fu-endojs-endo-but-for-bots-pr898-shepherd-1.md`).

## minion.town

Two long design-doc gauntlets dominated the day and both ran out of road, the
git-content-substrate build arc was reshaped by a maintainer decision to close and
redesign, and one merge-track PR was shepherded and woven to a ready state.

### Gauntlets and merges

- PR **#37** gauntlet (ocap mailboxes and attenuations for bot accounts, head
  `design/ocap-mailboxes`) halted. Panel round 4 ran all seven seats to a must-fix
  disposition (findings on overclaiming the "receive from" mandate, an unchecked RFC
  5321 local-part limit, mis-cited `mail.js` anchors, `intake.adopt` reproducing mint
  authority, and 37 em-dashes)
  (`journal/jobs/tada/kriscendobot-minion.town-pr37-gauntlet-panel-4.md`). Fix round
  4 applied every item on a fresh worktree (commit `a54f679`, CI green), fix round 5
  addressed a shared-domain-adapter bottleneck and HMAC key domain-separation
  (commit `bd42930`, CI green), and the gauntlet then halted when panel round 6
  vanished from the board, stranding the PR mid-gauntlet
  (`journal/jobs/tada/kriscendobot-minion.town-pr37-gauntlet-fix-5.md`,
  `journal/jobs/tada/kriscendobot-minion.town-pr37-gauntlet.md`).
- PR **#49** gauntlet (reconcile the vitals-weblet delivery substrate onto the git
  remote, two docs) halted after six rounds without converging. Rounds 3 through 6
  reworked the SHA strategy, reframed crash-safety as a write-side reconcile, added a
  partition-scoped index with a growth bound, reset partitions on every publish with
  a self-funding mint-time read and an old-leader revoke, and named the force-push
  dependency with two new open questions (heads `f2917bb`, `5524911`, `e78c406`,
  `a9c9d50`, CI green throughout). The gauntlet halted because a panel round 7 would
  exceed the six-iteration cap
  (`journal/jobs/tada/kriscendobot-minion.town-pr49-gauntlet-fix-6.md`,
  `journal/jobs/tada/kriscendobot-minion.town-pr49-gauntlet.md`).
- PR **#20** merge (retire toy MCP tools for daemon guests, head `749a02d`) was
  still in progress at the window's close, hitting repeated deterministic-overrun
  requeue errors and not landed
  (`journal/entries/2026/08/19/053614Z-error-gardener-7d3965.md`).

### Reviews

- PR **#21** review closed per maintainer. kriskowal's "Please close pending
  redesign" was actioned, closing the `design/weblet-gateway` draft with a pointer at
  the `*.ocap.site` redesign tracked in #47
  (`journal/jobs/tada/kriscendobot-minion.town-pr21-review-cdeb6f79.md`).
- PR **#47** review rewrote the design. Answering the ask to revisit from a fresh
  direction, `designs/weblet-ocap-synthesis.md` was rewritten (+271 and -207) around
  a `@sites`, `register(directory)`, `front` and `back`, and watch model, re-keying
  identity on the registered directory's formula id (commit `27227f1`, mermaid
  validated, reply posted)
  (`journal/jobs/tada/kriscendobot-minion.town-pr47-review-237136a0.md`).
- PR **#48** review closed per maintainer. kriskowal's "Please close, we will
  reconstruct pending #47" closed the `feat/git-content-substrate-serving` PR with an
  explanatory comment, reconstruction gated on #47
  (`journal/jobs/tada/kriscendobot-minion.town-pr48-review-b8fd1e6b.md`).

### Rebases, weaves, and shepherds

- PR **#29** (first-run credential and secret checklist for CD): a shepherd found CI
  already green and approved but merge-blocked by a `DEPLOYMENT.md` conflict, so it
  posted a successor weave rather than push nudges; the weave rebased the single
  commit onto `main` (head `c520613` to `e4ec9ff`), resolved both hunks to main's
  side, re-greened CI, and left the PR clean, mergeable, and approved, ready for a
  conductor merge (`journal/jobs/tada/kriscendobot-minion.town-pr29-shepherd.md`,
  `journal/jobs/tada/kriscendobot-minion.town-pr29-weave.md`).

### Builds and designs (git-content-substrate build arc)

- The `build-minion-town-git-content-substrate-gauntlet` (PR #48, git content
  substrate serving plane) halted. Panel round 4 fanned 34 seats to a must-fix
  disposition on head `12f5eda` (a false-positive success in
  `advanceVhostContentRoot` under concurrent CAS, missing property tests plus a
  `fast-check` dev dependency, fresh abbreviated identifiers, and a missing
  completion-summary comment), and fix round 4 made the CAS re-read after rename and
  downgrade lost writes to a conflict, added `fast-check` property tests, renamed the
  identifiers, and posted the summary (head `5075ba4`, 286 passed and 3 skipped, CI
  green). The gauntlet then halted when panel round 5 vanished from the board, on the
  same PR the maintainer independently closed under the #48 review above
  (`journal/jobs/tada/build-minion-town-git-content-substrate-gauntlet-fix-4.md`,
  `journal/jobs/tada/build-minion-town-git-content-substrate-gauntlet.md`). Two panel
  seats left signal: a scribe flagged an open completion-summary gap on `12f5eda`
  (`journal/entries/2026/08/18/080451Z-result-scribe-c3272d.md`) and a wire-watcher
  approved head `5075ba4` outright with only a non-blocking `qualifyUrl` doubled-path
  note (`journal/entries/2026/08/19/024026Z-result-wire-watcher-f19a2c.md`).

### Close, superseded, and other

- PR **#36** was closed as superseded. #20 (approved, clean, mergeable) does the same
  toy-tool and `mcp/minions:*` scope removal, but diffing against #20's head `749a02d`
  confirmed #20 does not cover #36's independent `whoami` baseline tool, so a blocked
  builder follow-up was parked to auto-promote on the #20 merge before #36 was closed
  with a superseding-comment pointer
  (`journal/jobs/tada/kriscendobot-minion.town-pr36-close-superseded-20260819.md`).
- A containment drift check on the deployed host (`i-0380cd68b90020fad`, us-west-1)
  was clean: a read-only check confirmed the containment drop-in intact byte-for-byte,
  `GATEWAY_ENDO_SOCK` absent from the live process, and both de-registered vhost
  records still absent, with no action taken even though the gateway had restarted
  under a CD redeploy
  (`journal/jobs/tada/fu-minion-town-containment-gateway-endo-sock-1-20260818-235001.md`).

## endor and xs2rust

The endor and node fixture-parity effort produced the day's largest single design
plus its first increment, and two of four xs2rust debugger follow-ups landed as
drafts. The Rust port work lives in the endojs/endo-but-for-bots repo but tracks as
its own arc.

### Fixture-parity ratchet campaign and design

- The design landed and was posted as a full campaign. It read the exact reason
  strings for all 40 fixtures from `rust/endo/tests/compartment_mapper_fixture_parity.rs`
  at the PR head (commit `09e5736da`), grouped the 33 exclusions into eight capability
  groups, laid out seven ratchet increments plus Increment 0 with a rising
  exercised-floor target (7 rising to 32), wrote `designs/endor-fixture-parity-ratchet.md`
  to `main2`, and posted eight orchestrated builder children under a serial
  halt-on-failure orchestration
  (`journal/jobs/tada/design-endor-fixture-parity-ratchet.md`).
- The orchestration completed child 1 (the oracle scaffold), then halted at child 2,
  the CommonJS-require walker, after three requeues hit the stall limit, leaving six
  downstream children parked
  (`journal/jobs/tada/endor-fixture-parity-ratchet-campaign.md`).

### Parity-oracle scaffold (Increment 0)

Increment 0 shipped as commit `253d90dba` on branch `feat/endor-run-entry-point-deps`
(`journal/jobs/tada/endor-parity-oracle-scaffold.md`): a harness-free node reference
oracle (`rust/endo/tools/gen-parity-golden.mjs`) that imports `@endo/compartment-mapper`
directly and serializes a stable structural projection to committed goldens with a
`--check` drift gate, a new test replacing the old compartment-count check with a full
structural diff, a two-tier split of `Exclude` into pending and durable variants
(8 durable and 25 pending across capability groups), and an anti-backslide floor of 7
exercised fixtures. The suite is green at 5 of 5 with no fixtures graduated. Two of the
seven exercised fixtures cannot use a node golden and carry an endor-baseline golden
instead.

### endor walker CommonJS-require

The CommonJS-require walker (campaign child 2 of 8) never produced a completion
report; the handler exited rc=1 across requeue cycles with a near-constant elapsed
that the gardener flagged as a deterministic overrun misclassified as transient and
escalated to the gardener inbox. This stall is what halted the campaign
(`journal/entries/2026/08/19/064415Z-error-gardener-b32e45.md`).

### xs2rust debugger caught-versus-uncaught follow-ups

- Follow-up 1 (recover the debugger row), the blocking prerequisite for the others,
  did not complete: rc=1 exits across requeue cycles with an overrun-suspect
  escalation (`journal/entries/2026/08/19/020507Z-error-gardener-241e4c.md`).
- Follow-up 2 (the `setExceptionBreakMode('uncaught')` silent no-op) landed as draft
  PR **#1038**. With the native fix blocked (the `ironhorse-debug` crate is not yet on
  `llm` and follow-up 1 is unbuilt), the gardener delivered a gate instead of the fix,
  documenting the caveat at every client API site and relabeling the Chat debugger
  panel option, with zero wire or behavior change so the mode goes live automatically
  once Ironhorse implements it (16 daemon tests and the panel tests passed unmodified)
  (`journal/jobs/tada/fu-xs2rust-endor-debugger-caught-vs-uncaught-2.md`).
- Follow-up 3 (engine-raised errors unwind through the jump chain, Phase 4 of the
  design) landed as draft PR **#1039**. It added `Interp::raise` routing engine errors
  through the same unwind path `XS_CODE_THROW` uses (uncaught engine-raises stay
  bit-exact) plus a `settle_halt!` macro, with 9 acceptance tests; the exact
  `test-ironhorse` CI job is green at 101 vm tests
  (`journal/jobs/tada/fu-xs2rust-endor-debugger-caught-vs-uncaught-3.md`).
- Follow-up 4 (BreakpointTable parity nits) did not complete, churning through
  transient-signature and exit-0-unsatisfying requeues on two hosts with an
  overrun-suspect escalation
  (`journal/entries/2026/08/19/020607Z-error-gardener-b3146a.md`).

### Ironhorse js-26 map-methods follow-up

The WeakMap-upsert follow-up landed as one commit `9a8b381bf` on draft PR **#970**
(`journal/jobs/tada/fu-ironhorse-js-26-map-methods-1.md`): it added
`WeakMapGetOrInsert` and `WeakMapGetOrInsertComputed` registered on
`WeakMap.prototype` with correct name and length, generalized the existing Map upsert
handler over receiver kind, and rejected a primitive key with a catchable TypeError
before insert. It added 3 XS-differential regression tests; unit tests pass 101 of 101,
and against the pinned test262 slice through the real XS oracle both new methods pass
with zero failures, every residual skip a symbols-as-weakmap-keys case the XS 8.3.1
oracle predates.

## Garden meta

The garden's self-directed work landed two fresh designs and four fixes directly on
`main2`, all as garden-self commits with no PR per the repo's own convention. The
substantive changes tightened the job system's own plumbing: job-kind eligibility
fencing, directive-dedup lifecycle semantics, standing never-close and never-resolve
rules, and retrospective over-minting. Several follow-up probes closed as no-ops after
finding their premises already resolved.

### Designs written

- Quota auto-throttle (`journal/jobs/tada/design-quota-throttle.md`), committed to
  `main2` as `2616436b34`: a new `designs/quota-throttle.md` specifying a host-level
  reaction to provider quota exhaustion that reuses the existing cap classification
  and captured reset epoch, modeled as a self-expiring per-host-and-provider journal
  marker with a claim-time guard and a scaler reconcile that lifts the marker once the
  reset epoch passes. Ollama and `hermit` are an explicit floor exclusion, and
  manually-funded arms with no parseable reset route to the maintainer inbox.
- Muse worker kind (`journal/jobs/tada/design-muse-worker-kind.md`), committed to
  `main2` as `d2a970ae8b`: a new `designs/muse-worker-kind.md` designing a muse kind
  integrated through the existing Codex harness over the OpenAI-compatible Meta Model
  API rather than the native muse CLI, single-agent, explicit-model-only, count-0 by
  default and provisioning-gated. No build follows pending review.

### Fixes and role or skill changes landed on main2

- Directive-dedup lifecycle conflation
  (`journal/jobs/tada/fix-job-in-lifecycle-tada-conflation.md`), commit `f8c053a02b`:
  `post-job.sh`'s dedup was counting `tada/` as in-lifecycle, wrongly refusing a
  genuinely new directive that shared an identity with an already-completed owner.
  A narrower `job_is_active()` predicate was added and pointed only at the dedup call
  site; regression test 9 reproduces the exact #475 refusal shape and the dedup suite
  passes 24 of 24.
- Never-close and never-resolve standing rules
  (`journal/jobs/tada/revise-standing-instructions-defer-to-submitter.md`), commit
  `b07706492b`: two canonical rules were added to `roles/COMMON.md` (never close a
  GitHub issue, never resolve a PR review thread, both deferring the terminal action
  to the submitter or opener), with the issue-inbox and pr-review-thread-replies
  skills changed to cite the single-sourced rule.
- Hermit claiming role gardener jobs
  (`journal/jobs/tada/improve-hermit-claims-gardener-role-jobs.md`), HEAD
  `7b57fce400`: an unpinned tier-less `role: gardener` job was falling through the
  eligibility check as claimable by any provider, letting non-anthropic kinds claim
  Claude-posture work in a hot loop; a `role_requires_anthropic_posture()` deny-list
  predicate was added and consulted first in `claim-job.sh`, with the spine tests
  passing.
- Retrospective over-minting
  (`journal/jobs/tada/fu-review-retrospective-consolidated-20260804-1.md`): a
  body-only gate-out in `comment-watcher.sh` had been wired only into the review
  branch, so an attention job carrying a bare pipeline directive still minted a retro,
  driving the bulk of a roughly 85% dismissal flood; both verb classes now flow
  through one shared gate-out, deploying on the next deliberate root deploy.

### Fleet operations and follow-up jobs

- An endo npm-CAS registry press found PR #882 already merged with the real XS
  boot-bundle generators present on `llm`, verified all three bundles regenerate as
  real artifacts, and updated a garden memory to mark the stub-bundles gotcha resolved
  (`journal/jobs/tada/fu-endo-npm-cas-registry-press-20260730-203502-1.md`).
- Two qwen-model-watch probes: one re-probed the qwen3.7 Ollama slug and got a
  still-404 with no flip, and the other found its target failure already fixed at HEAD
  (`journal/jobs/tada/fu-qwen-model-watch-20260728-180502-1-20260819-005002.md`,
  `journal/jobs/tada/fu-fu-qwen-model-watch-20260728-180502-1-20260728-223502-2-3.md`).
- A wallclock-cost-proxy follow-up closed effectively as a no-op, carrying only a
  machine-stamped cost block and no authored change
  (`journal/jobs/tada/fu-wallclock-cost-proxy-for-censored-arms-2.md`).

### Reaper and handler churn

The window's meta entry stream was about 34 gardener entries, roughly 31
progress-kind requeue ticks and 3 error escalations, all from one gardener on the two
`endolin` hosts, none reaching the doom threshold of 5. Thrash concentrated on a
handful of jobs, `design-quota-throttle`, `revise-standing-instructions-defer-to-submitter`
(one flagged about-to-escalate at cycle 4), `fu-build-exo-google-sheets-facets-5`,
`fu-guard-worker-self-disqualify-missing-agent-bin-1`, and
`fu-requeue-ps23-stranded-claims-4`. The three error entries are elapsed-constancy
overrun-suspect escalations (near-constant elapsed across requeue cycles, likely
deterministic overruns misclassified as blips), one each for
`fu-build-exo-google-sheets-facets-5`
(`journal/entries/2026/08/19/013022Z-error-gardener-c8e353.md`),
`fu-requeue-ps23-stranded-claims-4`
(`journal/entries/2026/08/19/015602Z-error-gardener-7eef04.md`), and
`design-quota-throttle`
(`journal/entries/2026/08/19/052656Z-error-gardener-6635c1.md`). Three of the
thrashing jobs did not produce in-window completion reports.
