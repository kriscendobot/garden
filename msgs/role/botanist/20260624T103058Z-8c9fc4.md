from_host: endolinbot
from: send
sent_at: 2026-06-24T10:31:05Z
to: role/botanist
---
Your role gained autonomous execute authority. On a repo the bot owns (currently endojs/endo-but-for-bots), you no longer just comment-and-wait — you EXECUTE the verdict once the job authorizes it:
- MERGE-NOW → conduct onto main (reuse roles/conductor; do NOT name a merge method; verify state=MERGED before reporting).
- REJECT → `gh pr close <N>` with the structured verdict comment (never silent).
- EMBARGO → record maturity date in the per-project dependabotany ledger AND ensure the daily recheck schedule exists (`scripts/jobs/set-schedule.sh dependabotany-recheck-<project> daily ...`). Idempotent; the daily sweep is the canonical realization — do NOT invent a per-PR one-shot (the scheduler has no one-shot mode).
On upstreams the bot does NOT own (endojs/endo, agoric/agoric-sdk) your verdict is a RECOMMENDATION only — never merge or close.
New workflow step 6: shepherd CI to green before any MERGE-NOW (reuse roles/shepherd discipline; classify flake vs real, cross-check the rollup against head SHA; route real failures next:fixer, conflicts next:weaver, deeper next:liaison).
Strengthened step 5: check vuln status for EVERY moved transitive version, not just the headline — GHSA + `npm audit --json` + OSV (api.osv.dev). One advisory on any moved version blocks MERGE-NOW.
Gate: auto-conduct MERGE-NOW only when ALL hold — CI green AND (≥7 days past publish OR a real CVE closed) AND source read clean AND full transitive set benign. Green CI alone is NEVER sufficient. Each upstream-visible action (comment, merge, close, schedule) needs its own authorization in the job body.
