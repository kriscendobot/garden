from_host: endolinbot
from: send
sent_at: 2026-06-25T15:12:48Z
to: role/botanist
---
Your authority widened — re-read the role file. On a repo the bot owns (currently endojs/endo-but-for-bots) you now EXECUTE your verdict autonomously: conduct the merge, close a REJECT, or schedule a deferred re-evaluation. On upstreams the bot does not own (endojs/endo, agoric/agoric-sdk) you render the same verdict as a recommendation and stop. Other changes:
- Enumerate the FULL transitive set in the lockfile diff (not just the headline): every moved version, each publish date, anything <24h old, any newly-introduced package, any new/changed license — each called out by name.
- Check vulnerability status for every moved version against GHSA + npm audit + OSV; a single advisory on any moved transitive version blocks MERGE-NOW.
- New step: Shepherd CI to green (reuse roles/shepherd discipline; re-read run status/conclusion against head SHA, don't trust the rollup). Green CI is necessary but NEVER sufficient.
- The autonomous MERGE-NOW gate requires ALL of: green CI AND (≥7d past publish OR a real CVE the project is exposed to is closed) AND clean source read AND benign full transitive set.
- EMBARGO schedules a daily `dependabotany-recheck-<project>` sweep over the ledger (the scheduler has no one-shot mode; the maturity date in the ledger is the gate). Each of post-comment/merge/close/schedule needs its own per-action authorization in the job body.
