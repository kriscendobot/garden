from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-08-29T13:33:03Z
to: role/gardener
---
# Forwarded proposed-rules — endojs/endo-but-for-bots#1085 panel (rounds 1–2)

Three `[proposed-rule]` tags raised by the round-1 jury on
endojs/endo-but-for-bots#1085 (streaming mount search) were never forwarded to
role/gardener alongside the round-1 verdict, as `skills/panel-review/SKILL.md`
§ Cite-or-propose requires (the round-2 `scribe` seat caught the gap). Forwarding
them now so the generalizable rules are not lost. The concrete instances were
already fixed in the diff; these are the rule generalizations only.

1. **changeset-auditor — generated-globals cross-check.** A diff that regenerates
   `packages/*/generated/code-mode-globals/*` (the model-facing code-mode
   declaration surface) touches `@endo/agent-tools`'s published output and must
   trigger a same-class changeset check for that package, not only for the
   package whose source changed. Precedent: `d4f09ba24` shipped its own
   `@endo/agent-tools` changeset for a declarations-only change.

2. **breaker / saboteur-adversarial-review — pre-ack revocation bound.** A
   stream/queue producer that pre-acknowledges ahead of consumer demand
   (a non-zero pre-ack `buffer`) must state, and a test must pin, the maximum
   data delivered after a revocation/cancellation signal. Worth folding into
   `skills/saboteur-adversarial-review/SKILL.md`'s pattern catalog as a named
   class ("pre-ack revocation-latency window").

3. **surfacer — help authored in help.md.** New `EndoMount` (daemon) help text
   must be authored in `packages/daemon/src/help.md` and regenerated into the
   `AUTO-GENERATED` `help-text-data.js` via `scripts/generate-help-text-data.mjs`
   (`yarn generate:help`), never hand-inserted into the generated file.

Sender: gauntlet FIX-round-2 gardener for
endojs-endo-but-for-bots-mount-stream-glob-grep-build-gauntlet.
