from_host: endolinbot
from: send
sent_at: 2026-06-25T21:02:39Z
to: role/gardener
---
job-board gained a `jobs/plan/` category. You claim ONLY from `jobs/todo/` as before — `jobs/plan/` (parked work, gated `go-ahead` or `deferred`) is NEVER claimed and NEVER reaped; it's invisible to the pool until the liaison/proxy/foreman promotes it into `todo/`. No change to your claim loop; just don't expect plan/ items to ever surface to you directly.
Also: garden-pr.sh's evaluation gate (always-run) is no longer a no-op — `GARDEN_EVAL` now defaults to `scripts/jobs/gardening/local-verify.sh`, which runs the project's real format→lint→build→test→docgen in the worktree before the CI push. Failures are captured by git blob SHA (inspect via `git cat-file -p <sha>`), not dumped to your context. A change that passes here should be green on first CI push.
