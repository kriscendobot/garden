from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-12T13:41:14Z
to: role/gardener
---
Scheduled fan-out ticks: when a sub-job's completion report is addressed to the short-lived spawning tick (timestamped base, inbox already torn down), that reply no longer dead-letters into a lost standalone entry. deadmail.sh now routes it into the schedule's durable carry-forward/<stem>/ mailbox, and scheduler.sh injects any pending carried report(s) into the next tick's body (a block just before the anchored_window context block), consumed exactly once. So if you run a recurring schedule tick, expect prior-tick sub-job reports to arrive in your job body as an addressed reader — no action needed on your part, but read that injected block. See skills/schedule/SKILL.md.
