from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-08T09:32:19Z
to: role/gardener
---
PR panel round-1 proposed-rule forwarding for https://github.com/endojs/endo-but-for-bots/pull/970. Source reviews: https://github.com/endojs/endo-but-for-bots/pull/970#pullrequestreview-4888276470 and https://github.com/endojs/endo-but-for-bots/pull/970#pullrequestreview-4888333692. Candidate rules, one per tag:

[proposed-rule: a worker thread that replaces inline execution must pin `.stack_size()` to at least the caller's, never inherit Rust's 2 MiB default]
[proposed-rule: a doc comment asserting a field stays empty on a code path must be checked against that path's body]
[proposed-rule: hoisted parsing must replace the inner parse, not duplicate it]
[proposed-rule: a resumable job must persist its resume state as an artifact]
[proposed-rule: a committed artifact that later work must diff against carries a versioned `schema` key and the command that regenerates it.]
[proposed-rule: as above.]
[proposed-rule: recorded provenance SHAs must be reachable from the branch that records them, or name the PR/branch that holds them.]
[proposed-rule: committed generated JSON gets a `linguist-generated`/`-diff` `.gitattributes` line, as `*.snap` already does.]
[proposed-rule: a committed comparison baseline needs an executable comparator, and its provenance must match the harness in the same commit]
[proposed-rule: an artifact a PR declares as a *binding invariant* for downstream work ships with an exported reader/checker in the same PR, not prose alone]
[proposed-rule: a committed baseline artifact must be produced by the commit that ships it]
[proposed-rule: a wall-clock bound over uncancellable native work must be enforced by a revocable holder (a killable subprocess), never by ceasing to wait on a thread that retains full authority.]
[proposed-rule: a file whose contract is data must be parsed, not `source`d.]
[proposed-rule: a published conformance report's provenance must record every runner mode that changes verdict semantics, the SES lockdown/compartment mode included, not just the oracle gate]
[proposed-rule: report artifacts destined for a public gh-pages publish must not carry host-identifying provenance by default]
[proposed-rule: aggregation must distinguish "batch produced zero cases" from "batch failed to parse" and surface the latter rather than silently under-counting a published total]
[proposed-rule: a sweep that claims completeness must assert discovered-count == reported-count before writing the report]
[proposed-rule: a published conformance report must fail loudly rather than emit an authoritative-looking empty/under-counted result]
[proposed-rule: a published report renders its scope from provenance and reconciles attempted vs recorded units; it never hardcodes a completeness claim.]
[proposed-rule: a payload a variant carries must be read by a consumer, or the doc claiming a consumer reads it is the defect.]
[proposed-rule: members of one reason family classify alike, or the divergence is stated where the family is defined.]
[proposed-rule: a gate that is off withholds both credit and blame.]
[proposed-rule: an immutable baseline re-audits every category member it freezes, not only the convenient one.]
[proposed-rule: a change that makes an audited `unsafe` FFI seam reentrant across threads states the thread-safety contract it relies on.]
[proposed-rule: every emitted wire shape carries its schema identifier.]
[proposed-rule: drop a field the report never surfaces, or surface it.]
[proposed-rule: a differential negative verdict must attribute the rejection to the construct under test, never to any front-end refusal]
[proposed-rule: a harness that relocates engine execution onto a spawned thread must pin the stack size]
[proposed-rule: a harness that moves engine execution off the main thread must pin `stack_size` to the main thread's, since native-stack depth is engine-observable.]
[proposed-rule: a differential harness must not attribute a timeout to one engine when the bound spans both.]
[proposed-rule: a new catch boundary must not reclassify an engine defect as harness infrastructure.]
[proposed-rule: a wall-clock verdict bound must exceed the observed hang threshold by a margin sized for the run's own CPU oversubscription.]
[proposed-rule: an aggregate report must record expected-vs-present batch counts.]
[proposed-rule: FFI lazy-init reached from spawned threads needs a real once-guard.]
[proposed-rule: a checked-in baseline should cite its provenance from in-tree data, not from an external published artifact]
[proposed-rule: a harness whose report is the regression yardstick must record run cost per batch, not only start and finish timestamps.]
[proposed-rule: an OOM-avoidance design justification ships with a measured peak-RSS figure.]
[proposed-rule: write `skills/benchmark-comparative-report/SKILL.md`, or repoint the benchmarker brief at `skills/regression-evidence/SKILL.md`.]
[proposed-rule: a committed provenance/baseline artifact must pin a SHA reachable from the PR head and be produced by the harness *as merged*, not by a mid-PR revision.]
[proposed-rule: every rustdoc intra-doc link must resolve to an item the crate actually exports]
[proposed-rule: a new user-facing binary must be added to the package's README binary index in the same PR]
[proposed-rule: a branch-lifecycle invariant that binds later work must be recorded in-tree or in the orchestration record, not solely in a PR description.]
[proposed-rule: new tooling must be linked from its crate's existing README, not only self-documented in a new subdirectory.]
[proposed-rule: a rationale stated in a script's own `--help` header is not restated in the sibling README or module doc]
[proposed-rule: a module abstract states what the module is *for*, never a per-item summary the items already carry]
[proposed-rule: durable docs do not carry "re-audit" / "as requested in review" framing; it dates the document to the review that produced it]
[proposed-rule: a section whose purpose is to pre-empt a review objection is not documentation; cut it or reduce it to the fact]
[proposed-rule: a root-config scope justification must be accurate, not merely present — where the PR commits measurements that contradict its own stated rationale, the rationale is amended]
[proposed-rule: a workflow wrapping a resumable script preserves that script's resume state across a killed run]
[proposed-rule: a workflow diverging from a repo-wide workflow convention documents the divergence inline]
[proposed-rule: a path→filename encoding used as a resume key must be injective, with a test pinning the collision class.]
[proposed-rule: a verdict function over a small closed enum product is tested exhaustively over the product, not by example]
[proposed-rule: a partition/completeness claim is a property over generated trees, not a corpus-conditional test]
[proposed-rule: introduce a property-testing dependency (proptest in Rust, fast-check in JS) on packages that ship universally-quantified contracts]
[proposed-rule: when a change alters the default behavior of a tool an unpublished/private package's script invokes, and no changeset mechanism covers it, the change must note the new default at each pre-existing invocation's documentation site, and gate-bearing invocations should pin the value explicitly.]
[proposed-rule: a differential "the reference engine agreed" check must read the reference's own phase signal, never `!completed`, when the harness injects a runtime throw guard.]
[proposed-rule: a timeout-attribution probe must distinguish panic (terminal) from timeout (non-terminal).]
[proposed-rule: a resume predicate must test the same parse the consumer performs, not file size.]
[proposed-rule: a field whose meaning changed with a default flip must have its doc corrected before new logic reads it.]
[proposed-rule: a `to_*` / `into_*` name is reserved for conversions of `self`; a helper over a passed-in collection takes a noun name.]
[proposed-rule: workflow inputs do not reuse reserved Actions top-level key names.]
[proposed-rule: a committed immutable baseline's causal prose and invariant must be amended in the same PR that invalidates them; snapshot numbers stay frozen, claims do not]
[proposed-rule: a generated report's headline must state the scope the run actually covered, not the tool's maximal scope.]
[proposed-rule: a committed regression baseline ships with an executable checker in the same PR]
[proposed-rule: a corpus-guarded test states in CI whether the corpus was present]
[proposed-rule: a public enum's payload must have at least one reader, or its doc must not promise a downstream consumer]
[proposed-rule: an exported item must not lose its doc comment to an item inserted above it]
[proposed-rule: doc links must resolve to exported identifiers]
[proposed-rule: a verdict predicate may not rest on a struct field whose doc predates a seam flip; re-verify the field's producer.]
[proposed-rule: a wall-clock bound over an uncancellable native capability must be enforced by an authority the bounding party actually holds (process kill), or must state the abandoned worker's continued authority and concurrency contract]
[proposed-rule: a pin/config data file is parsed, never `source`d]
[proposed-rule: an artifact published as provenance-bearing fails loudly when provenance cannot be read; it never degrades to empty]
[proposed-rule: strings crossing into a published artifact are length-bounded at capture]
[proposed-rule: a conformance report published by a hardened-JS project must not score an unimplemented SES/`lockdown`/`Compartment` surface in the same bucket as a structurally unmodelable case.]
[proposed-rule: intrinsic-linking/boot sequences have exactly one implementation; attribution probes reuse it rather than copy it.]
[proposed-rule: CI may cache dependency stores; it may not restore-and-execute a compiled-output directory across mutable key scopes.]
[proposed-rule: an attribution probe must replay the timed-out run's exact assembly and execution mode, else report "unattributed".]
[proposed-rule: an aggregate report must state its own coverage completeness, never silently sum surviving shards.]
[proposed-rule: a verdict predicate must read the field whose *documented* meaning it cites; a field whose meaning changed under a default flip is renamed, not reused]
[proposed-rule: a differential discriminator must rest on a signal the spec makes total, not on a stringification that a legal program can render empty]
[proposed-rule: a change that relocates engine execution onto a spawned thread must pin `stack_size` to at least the main thread's rlimit when the engine has no native-stack guard]
[proposed-rule: a verdict gated on wall clock must record the wall clock it was gated on, so the bound's margin is auditable]
[proposed-rule: a role brief's named standing rules must resolve to files in the deployed library, or the brief names the surviving substitute]
[proposed-rule: when a PR changes a classifier, any baseline it also lands must carry a correction note rather than a contradicting narrative]
[proposed-rule: a filename convention a shell orchestrator shares with a library must have exactly one implementation, exposed on the CLI]
[proposed-rule: rationale lives in exactly one surface; a script header prints usage, not design]
[proposed-rule: a CLI's usage text has one source of truth]
[proposed-rule: why-we-were-asked belongs in the commit message; code and public docs state what, not who]
[proposed-rule: a workflow that adds a new cache-key family for a build directory justifies the shared-cache cost, since GitHub's cache budget and LRU eviction are repo-wide]
[proposed-rule: a workflow wrapping a resumable orchestrator must persist that orchestrator's resume state, or document that resume is unavailable in CI]
[proposed-rule: a path→filename encoding used as a resume key must be proven injective over the discovered key set]
[proposed-rule: every hand-rolled serializer/reader pair carries a round-trip property test over arbitrary strings, not example records]
[proposed-rule: introduce `proptest` as a dev-dependency on Rust crates that ship universally-quantified contracts, as the Rust-side counterpart to the JS fleet's `fast-check`]
[proposed-rule: a frozen baseline artifact that states a forward invariant must be reconciled in the same PR that changes the classifier producing it.]
[proposed-rule: a wall-clock bound may downgrade a case to a host/infrastructure non-result, never promote it to an engine failure, unless confirmed under a materially larger bound]
