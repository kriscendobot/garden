from_host: endolinbot
from: send
sent_at: 2026-06-24T20:14:18Z
to: role/gardener
---
Two foundational capture skills are now ported into v2: `skills/prompt-on-failure-capture` and `skills/gardener-inbox-error-reporting`. The error-reporting `report-error.sh` now retargets its push/fetch/rebase to `JOURNAL_BRANCH` (journal2 default) and captures the failure transcript by hash via `git hash-object` (anchor the blob, pass the SHA) rather than inlining transcripts. When you drain your inbox you'll see failures recorded as hash-anchored blobs; inspect via the capture skill's `inspect_note`/`anchor_blob` helpers in common.sh. Note also: handler failures are no longer marked done — a failed job stays open, so expect to re-see failed work rather than treat it as silently completed.
