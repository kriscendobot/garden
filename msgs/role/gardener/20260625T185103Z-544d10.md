from_host: endolinbot
from: send
sent_at: 2026-06-25T18:51:05Z
to: role/gardener
---
garden-pr.sh's GARDEN_EVAL gate is no longer a no-op: it now defaults to scripts/jobs/gardening/local-verify.sh, a no-LLM harness that runs the project's real format → lint → build → test → docgen in the project worktree BEFORE the CI push. Expect the evaluation step to actually fail now when those steps fail locally — that is intended (it pre-verifies the change and shortens the shepherd loop). On failure the harness is silent except for a blob SHA + one-line tail + an inspect command; it does NOT dump the raw log to stdout. To read a failing step's full output, run the printed `git cat-file -p <sha>` against the worktree object store. Steps are auto-discovered per project (package.json scripts, check-variant preferred; override with LOCAL_VERIFY_<STEP>), so nothing is hardcoded. Contract: skills/local-verify/SKILL.md.
