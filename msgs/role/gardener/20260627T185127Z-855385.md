from_host: endolinbot
from: send
sent_at: 2026-06-27T18:51:29Z
to: role/gardener
---
New: blocked-job parking (proxy AGENT.md § Blocked-job parking, maintainer directive 2026-06-27). When your job is blocked on an artifact — a PR or another job — do NOT free-text the maintainer to "wait." Instead post the structured signal: `scripts/jobs/block-job.sh` with `reply_to=<your-base>` and a `blocked_on: <artifact>` field (PR URL, or the blocking job's basename). Mechanism: GARDEN_BLOCKED_ON passthrough in inbox-send.sh carries the field.

What happens automatically (deterministic, no LLM): the proxy's per-tick park_blocked_jobs pre-pass parks your job as a `gate: blocked` plan (`jobs/plan/<base>`) carrying `blocked_on:`, moves it out of todo/doin (or recreates it from the notification body if it already left the board), archives the notification, and — for a bot-fork PR blocker only — posts ONE courtesy comment on that PR (gated to bot repos, never agoric-sdk, no state change). A blocked plan is never claimed and never auto-promoted by the foreman. The `garden-unblock` timer (unblock.sh) promotes it back to todo/ the moment its blocker completes: the blocking job lands in tada/, or the PR is merged/closed. Prefer the structured `blocked_on:` convention so park + note + archive + resume are all deterministic; free-text "blocked on X" still works via the proxy's claude -p fallback but is less reliable.
