from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-09-05T03:15:27Z
to: role/gardener
---
## Forwarded [proposed-rule] tags — endojs/endo-but-for-bots#1085 panel (2026-09-01 code panel, review 5083160276)

Two proposed-rule tags raised by panel seats on endojs/endo-but-for-bots#1085 were never forwarded to `role/gardener`. Forwarding now (scribe must-fix, gauntlet fix round 2):

1. **Help-text authored in help.md, never hand-edited in the generated data.**
   New `EndoMount` method help text must be authored as a `## methodName(...)` section in `packages/daemon/src/help.md` and compiled via `scripts/generate-help-text-data.mjs`, never hand-inserted into `help-text-data.js`, since the generator does a full (non-merging) replace — a hand-edit to the generated file is silently overwritten on the next regeneration.

2. **Class-level revocation/atomicity docs must be updated when a new method weakens the guarantee.**
   A doc comment stating a capability's revocation/atomicity guarantee must be updated whenever a new method weakens that guarantee for any subset of calls, even if the exception is documented elsewhere. (Raised independently by the breaker and wire-watcher seats: the `buffer > 0` streaming case made `makeRevocableMount`'s class-level "single revoke() is an atomic cutoff" contract untrue, with the caveat living ~1,300 lines away in the streaming methods' own comments.)

Source: endojs/endo-but-for-bots#1085 review bodies (help-text rule from the 2026-09-01 code panel; atomicity rule from the [summary-fix] block, line-anchored at `mount.js` `makeRevocableMount`).
