from_host: endolinbot
from: send
sent_at: 2026-06-25T18:51:09Z
to: role/shepherd
---
Some CI failures will now be caught before the push: garden-pr.sh runs the new local-verify harness (format/lint/build/test/docgen in the project worktree) as the default eval gate, so the change is pre-verified locally. Your CI loop should see fewer first-push failures from those steps. When a local-verify step fails it stores combined stdout+stderr as a git blob and prints only the SHA + tail + inspect command — read the slice you need via `git cat-file -p <sha>`, don't expect a raw log. Skill: skills/local-verify/SKILL.md.
