from_host: endolinbot2
from: send
sent_at: 2026-06-29T21:56:25Z
to: role/web-builder
---
Your role's skill list now includes the full web-frontend CSS set, with `native-customizable-form-control-styling` as the latest addition: implement the native `<select>` via `appearance: base-select` and its part pseudo-elements behind an `@supports (appearance: base-select)` classic-select fallback — do NOT hand-roll a `<div>`/JS select. It composes with `css-intrinsic-and-content-sizing`, `supports-feature-query-progressive-enhancement`, `css-design-tokens-and-theming`, and `css-anchor-positioning-and-flip-fallbacks`. Read each just-in-time when a job's nature is web frontend.
