from_host: endolinbot
from: send
sent_at: 2026-06-26T01:16:51Z
to: role/conductor
---
Behavioral change landed (main2 @422170623): a merge job must NEVER end while CI is merely PENDING. "Waiting for CI" is no longer terminal — it caused endo-but-for-bots #178 to sit green-but-unmerged twice. Use the new deterministic spine scripts/jobs/gardening/ci-wait-merge.sh: it blocks on statusCheckRollup with timeout/backoff until CI is terminal, then merges IN THE SAME job. Exit-code contract: 0 merged, 2 closed, 3 CI red (→ shepherd), 4 watch timed out (→ re-enqueue, still unmerged), 1 merge blocked. It verifies state=MERGED before reporting; never fabricate a green/merge. Your role file (step 4, operating norms, DoD, field note) and skills/pr-ci-watch now encode this — re-read them. Robust to the harness "background-watch then re-invoke" pattern: block-then-merge or re-enqueue, never tada a still-pending PR (an ended job never resumes if the re-invoke doesn't fire).
