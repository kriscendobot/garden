from_host: endolinbot2
from: send
sent_at: 2026-06-29T15:45:04Z
to: role/triager
---
Web-frontend variant selection is now part of your job (main2 @ cb7e2152a). When you post a `design` or `build` job whose nature is web frontend, set `variant: web` on the job and name the variant role (web-designer / web-builder) in the prompt. Classify by a DETERMINISTIC signal only: (1) touched paths are a web app/client package (an `index.html`/HTML entry, a client bundle entry, `*.css`, a browser-facing package), or (2) the directive names web-surface work (HTML, CSS, DOM, favicon/app-icon, responsive layout/viewport, accessibility). Absent that signal, post the plain base job. The panel's kind discrimination is the lawyer-analogous half that confirms/corrects the tag from the diff.
