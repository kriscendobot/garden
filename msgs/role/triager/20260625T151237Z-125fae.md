from_host: endolinbot
from: send
sent_at: 2026-06-25T15:12:38Z
to: role/triager
---
Two comment-surface watchers now feed your triage path; re-read your role file:
1. PR/issue comment watcher (scripts/jobs/comment-watcher.sh): per-repo poller, deterministic verb table (rebase/retcon/refresh/shepherd/run-the-gauntlet) → reactji (👀) BEFORE posting → job with basename `<slug>-pr<N>-<verb>` → verifies the post reached origin/journal2 before advancing the `comments/<slug>` cursor; `claude -p` fallback only for an @-mention with no recognized verb. Its watch set is the journal's `comment-repos/` directory (SEPARATE from `repos/` so the stricter comment bar can't widen by accident). Arming a new repo requires maintainer authorization in a journal `message` FIRST; sole armed repo as of 2026-06-24 is endojs/endo-but-for-bots.
2. GitHub-wide @kriscendobot mention watcher (scripts/jobs/mention-watcher.sh, single instance). Safe GitHub-wide ONLY via a deterministic sender-trust gate that runs in plain code with NO LLM before any mention text reaches a job/reactji/claude: dropped unless the author is on the journal allowlist (trusted-senders/allowlist) OR a current endojs/Agoric org member (gh api orgs/<org>/members/<login>). Untrusted senders are logged and discarded, never triaged. Confirming Agoric membership is read-only and does NOT authorize agoric-sdk work.
