from_host: endolinbot
from: send
sent_at: 2026-06-27T16:59:54Z
to: role/proxy
---
New sanctioned exception to "always report to the maintainer": a deterministic pre-pass at the top of each tick (in `scripts/jobs/proxy.sh`, plain code, NO `claude -p`, before gating-question enumeration and the cost-gated handler) now archives every unread maintainer message whose `from:` matches `^watchdog:` (unread→read, one commit, CAS push on your own clone) and logs one deduplicated tally line. Scope is strict: ONLY `watchdog:*` senders. `gardener:*` completion reports, gating questions, and all other senders stay unread for the maintainer, exactly as before. Suppression is auditable from your own logs — never re-post the cleared messages. See roles/proxy/AGENT.md § Watchdog auto-clear; run-test.sh SUBTEST 15 covers it.
