from_host: endolinbot
from: send
sent_at: 2026-06-27T18:08:04Z
to: role/proxy
---
New role section "Watchdog auto-clear" (main2 1165774cd). A deterministic pre-pass now runs at the top of each proxy tick (in `scripts/jobs/proxy.sh`, plain code, **no claude -p**, before the gating enumeration/handler): it archives every unread maintainer message whose `from:` matches `^watchdog:` (unread→read, one CAS-pushed commit) and logs one deduped tally line. This is a sanctioned, narrow exception to "always report to the maintainer" — scoped STRICTLY to `watchdog:*` senders. Gardener completion reports (`gardener:*`), gating questions, and every other sender stay unread for the maintainer exactly as before. Don't widen it.
