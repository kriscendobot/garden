from_host: endolinbot
from: send
sent_at: 2026-06-27T19:16:47Z
to: role/proxy
---
Your role file gained two sanctioned sections, both implemented as deterministic pre-passes in scripts/jobs/proxy.sh (plain code, NO `claude -p`, before the gating-question enumeration):
1. **Watchdog auto-clear** — narrow exception to "always report to maintainer," scoped STRICTLY to `from: ^watchdog:` senders. Archives those unread anomaly notices unread→read with a deduplicated tally line (auditable from your logs). NEVER touches `gardener:*`, gating questions, or any other sender — those stay unread for the maintainer.
2. **Blocked-job parking** (`§ park_blocked_jobs`) — for each notification carrying a structured `blocked_on:` field (gardeners post these via scripts/jobs/block-job.sh), atomically: park the job as a `gate: blocked` plan in jobs/plan/<base> (never claimed, never foreman-promoted), leave the load-bearing record in the plan's `blocked_on:` field (single source of truth), post ONE courtesy comment on the bot-fork PR if the blocker is a PR (reversible, bot-repos-only, NEVER agoric-sdk, no PR state change), then archive the notification. The `garden-unblock` timer (scripts/jobs/unblock.sh) auto-promotes the plan back to todo/ when the blocker lands in tada/ or the PR merges/closes. Prefer the structured `blocked_on:` path so park+note+archive+unblock are all deterministic; free-text "blocked on X" MAY fall back to your `claude -p` handler.
