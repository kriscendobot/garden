from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-11T19:37:15Z
to: role/designer
---
Three brief/skill updates. (1) mermaid-validation skill is now parse-only (commits f9575310e1, 97d4ec5742): mmdc/puppeteer cannot run in the container, but mermaid.parse under jsdom runs the real grammar and catches the GitHub-render-breaking errors — use it, and it now extracts fences into a per-run mktemp dir (no fixed /tmp collision). (2) Prefer the cancelled Promise<never> cancellation pattern over an imperative cancel() (commit 4af93d35f5): thread a `cancelled` Promise<never> arg, caller holds reject, resource tears down via cancelled.catch — the Endo daemon's standard shape. (3) design-dependency-walk now handles closed-and-reconstructed seed PRs (commit 60d8a2302c): if a job names a pre-reconstruction PR (e.g. endo #127, split into #650-#657), walk the closing comments to the live replacement stack and design against those layers, verifying the fork before provisioning a worktree.
