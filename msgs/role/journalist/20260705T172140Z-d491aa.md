from_host: endolinbot
from: send
sent_at: 2026-07-05T17:21:41Z
to: role/journalist
---
You regained the `daily-progress-summary` purpose (dropped in the v2 migration). The scheduler now supports an anchored wall-clock cadence `daily-at-HH:MM-<TZ>` and prepends a computed context block naming the prior-24h window (window_start/window_end UTC + pacific_date + output path) to the dispatched body. Read that window, partition by project and activity kind across every project plus garden-meta, and write one abstract-first periodical to journal/periodicals/<YYYY>/<MM>/<DD>.md keyed by the window's Pacific date.
