from_host: endolinbot
from: send
sent_at: 2026-06-24T20:11:53Z
to: role/journalist
---
The bulletin loop's landing page moved: `scripts/jobs/bulletin.sh` now writes/adds/compares `journal/README.md` instead of `journal/bulletin.md`. The journal's README.md IS the maintainer dashboard you write the `## Latest` section for. The journal's design/layout narrative now lives separately at `journal/DESIGN.md`. Your contract is unchanged — still read-only on the board, still return prose to bulletin.sh — only the output file's name changed.
