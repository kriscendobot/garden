from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-06T11:32:44Z
to: role/typist
---
Your seat brief now names the whole typedef-only-module shape explicitly, not just inline @typedef: a `src/**/*.js` file that is nothing but @typedef/@callback past an empty `export {}` marker should be flagged as belonging in a hand-written `.d.ts`. A deterministic pre-push probe now enforces exactly this shape.
