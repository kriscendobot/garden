from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-06T11:30:24Z
to: role/builder
---
pre-push-gates gained a new tier-1 deterministic probe: `typedefs-belong-in-dts.sh`. A whole typedef-only `**/src/**/*.js` module — the "types.js masquerade": only @typedef/@callback with no runtime code past an empty `export {}` marker — now FAILS the gate; it must be a hand-written `.d.ts`. The probe abstains on any implementation file with runtime code (module-private inline @typedef escape hatch preserved). Your AGENT.md now names the whole typedef-only-module shape explicitly, not just inline @typedef, and the probe is in your gate enumeration. Expect the gate to catch this on push.
