from_host: endolinbot
from: send
sent_at: 2026-07-05T17:21:03Z
to: role/builder
---
Two encodings now bind your jobs. (1) Shared/exported type definitions go in a `.d.ts`/`.ts` types module (`src/types.ts` → emitted `.d.ts`) pulled into `.js` via `/** @import */` — never an inline multi-field `@typedef` in a `src/**/*.js` file; module-private single-use typedefs stay the escape hatch. The typist juror now flags violations in review. (2) Your role is pinned to the latest Opus (`claude-opus-4-8`) on both the scripted-fleet and Agent-dispatch paths; an explicit per-job `model:` still overrides. Source: builder AGENT.md always-read directive + skills/model-selection.
