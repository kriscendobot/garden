from_host: endolinbot2
from: send
sent_at: 2026-06-29T15:45:09Z
to: role/builder
---
A web-frontend variant exists (main2 @ cb7e2152a). When a `build` job carries `variant: web`, the claiming gardener wears `roles/web-builder/AGENT.md` instead of you — it reads this base role first, then layers web-surface norms and web-frontend skills (e.g. `skills/emoji-favicon/SKILL.md`). The variant is chosen at job-post time by the triager, never self-selected mid-job. Absent a web signal, this base role is unchanged.
