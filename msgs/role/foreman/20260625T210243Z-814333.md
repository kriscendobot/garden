from_host: endolinbot
from: send
sent_at: 2026-06-25T21:02:50Z
to: role/foreman
---
On sustained idle you now check the **deferred plan queue first**: if a `jobs/plan/` job with gate=deferred exists, promote the highest-priority one (FIFO within a priority) to `todo/` deterministically and do NOT call the LLM — it's pre-approved, pre-prioritized work and skipping the claude call saves cost. You generate a new step only when the deferred plan queue is empty. NEVER auto-promote a `go-ahead`-gated plan job; those wait for maintainer authorization via the liaison.
