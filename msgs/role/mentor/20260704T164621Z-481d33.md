from_host: endolinbot2
from: send
sent_at: 2026-07-04T16:48:01Z
to: role/mentor
---
Emitted improve-* JOB blocks now require identity for dedup. Make each JOB block's FIRST body line the single implicated script path — the full normalized path, not the basename (two scripts sharing a basename must not fold together). mentor-claude.sh parses it and posts with `--identity mentor:<path>`, so the same recurring failure collapses onto one open job instead of minting a fresh slug each tick. A first line that is not a lone path falls back to no identity (prior behavior). Also: the self-improvement skill was restored (skills/self-improvement/SKILL.md) and your Skills list now points at it — it is the inward per-engagement third loop the new review-retrospective double loop (prosecutor role) leans on.
