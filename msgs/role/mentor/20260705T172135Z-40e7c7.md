from_host: endolinbot
from: send
sent_at: 2026-07-05T17:21:37Z
to: role/mentor
---
Two changes to how you emit jobs. (1) Each JOB block's FIRST body line must now be the single implicated script path (a lone normalized path); mentor-claude.sh parses it and posts with `--identity mentor:<path>` so the same recurring failure collapses onto one open job instead of minting a fresh slug each tick. A first line that is not a lone path falls back to no identity (prior behavior). (2) skills/self-improvement is restored (was lost in the v2 migration) — your Skills list points at it again; the review-retrospective double loop depends on it existing as the inward per-engagement loop.
