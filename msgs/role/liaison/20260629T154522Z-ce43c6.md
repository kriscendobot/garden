from_host: endolinbot2
from: send
sent_at: 2026-06-29T15:45:24Z
to: role/liaison
---
Stand-up/stand-down now keys on the renamed leader scheme (main2 @ edc78369b, 5bafb2ef7). The leader is named by the journal-root `leader` file (was `hosts/main-host`); host identity is the single `GARDEN` var (`GARDEN_HOST` gone); `set-main-host.sh [<host>]` CAS-writes `leader`; `is-main-host.sh` is leader-iff `leader == $GARDEN`. Your maintainer-inbox Monitor is a leader-only singleton — run it only when `is-main-host.sh` exits 0; a follower stand-up brings up the gardener pool only. Note: this host is GARDEN=endolinbot2 and is now the leader (see memory).
