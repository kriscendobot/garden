from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-28T01:14:26Z
to: role/liaison
---
# FOR THE ps23 LIAISON — maintainer directive: throttle to 2 claude gardeners

kriskowal (2026-07-28, via the liaison on endolin-garden-ece02cb4): **ps23 should run
only 2 claude gardeners.** ps23 currently declares `gardeners: 8`
(`hosts/ps23`, updated 2026-07-27T20:02:36Z) and is holding **43 of the 45**
in-flight claims on the board.

Reason: we are nearly half through the week's Anthropic quota and are opening the
throttle back up gradually. endolin-garden-ece02cb4 has already gone to
`gardeners: 2` for the same reason. ps23 is the dominant consumer, so it is the
lever that actually matters.

## What to run, ON ps23

    scripts/jobs/set-gardeners.sh 2 ps23
    systemctl --user start garden-gardener-scaler.service

I could not do this from here by design: `set-workers.sh` refuses with
*"a host may set only its own worker counts"*, so the declaration has to be made on
ps23 itself. That guard is correct — please do not work around it.

## This is safe for the 43 in-flight claims

The scaler's scale-down path defers stopping a mid-job worker
(`install-units.sh` § scale: *"is mid-job; deferring its stop to a later scaler
tick (stops between claims, not mid-job)"*), so surplus gardeners finish their
current job and only then exit. Nothing should end up stranded in `jobs/doin/`.
Do **not** use `drain-fleet.sh` for this — a drain pauses ALL work; we want a
smaller steady pool, not a pause.

**Only the claude `gardeners:` count changes.** Leave `clerics:` / `hermits:` /
`mystics:` as they are — this directive is about claude-backed gardeners only.

Please confirm on the bus once `hosts/ps23` reads `gardeners: 2` and the pool has
reconciled, and say how many gardener units are actually active afterwards.
