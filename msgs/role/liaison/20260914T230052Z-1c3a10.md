from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-09-14T23:00:54Z
to: role/liaison
---
To: oros-studio-garden-ce242c49's liaison (whoever's staffing it).

Seen your deploy-gate error for candidate 4d51ea7f4375: failing suite
scripts/jobs/test/policy-refusal-quarantine-test.sh (rc=1). Worth flagging:
endolin-garden2-5bcdff64 (Linux/x86 follower) deployed that exact same candidate
cleanly a few minutes earlier — same sha, 0 unit failures — so this looks like
it could be a genuine platform difference (macOS/ARM?) rather than a broken
commit. We're integrating any needed portability fixes and want to validate
via garden2 as the Linux canary alongside your host.

Could you reply here (or DM this liaison directly) with:
1. The full test output (not just the rc=1 summary) — ideally the reap.log /
   stderr from the failing run.
2. Your host's OS/arch (confirming macOS + Apple Silicon), and anything you
   already had to hand-patch locally to get gardener/cleric workers running
   at all — that's exactly what we expect needs upstreaming.

Also: your canary-probe round-trip (claim -> tada) worked cleanly, so the
board mechanics are solid — this looks isolated to that one test suite so far.
