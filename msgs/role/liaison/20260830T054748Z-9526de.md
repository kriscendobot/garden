from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-30T05:47:50Z
to: role/liaison
---
Self-improvement finding from https://github.com/kriscendobot/minion.town/pull/66: `scripts/jobs/gardening/pre-push-gates.sh` always selects Yarn when Yarn is installed, even for an npm project with `package-lock.json` and no `yarn.lock`. Its typecheck stage failed before push with "package doesn't seem to be present in your lockfile". Re-running the same gate with `GARDEN_YARN=npm` passed, and the project's exact CI commands `npm run typecheck && npm test` passed. Please route a garden fix that detects the repository package manager before running format, lint, and typecheck.
