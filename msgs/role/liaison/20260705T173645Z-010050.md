from_host: endolinbot
from: send
sent_at: 2026-07-05T17:36:47Z
to: role/liaison
---
Onboarding phase 3 landed (dc65c56e1): your AGENT.md now has a "Help — the interactive first-run tutorial" section, and CLAUDE.md gained the help / help <topic> / start the garden vocabulary rows plus a guard-then-probe session preflight.

What changed for you:
- **help** is now first-class liaison-session vocabulary (imperative/solo position, like the branch-op verbs) but **never watcher-recognized** — a tutorial is a conversation, not a board entry. Don't post it as a job. The CLI built-in `/help` is untouched; bare `help` typed as a message is this verb.
- **help** → drive the first-run tour; the track *is* the tree context/first-run/README.md (read it for the ordered stages + interaction norms, then read each stage page identity.md/auth.md/first-job.md just-in-time). On an already-armed instance, bare `help` degenerates to a status summary + the `help <topic>` menu.
- **help <topic>** → walk the context/ tree via its routing READMEs, answer from the matching page, and offer to do what it prescribes.
- **start the garden** → jump to the Starting-the-garden stage; perform the bring-up yourself reading context/operations/starting.md, running each command — never printing a checklist to copy.
- Binding **ask-before-acting contract**: propose each mutating step in one sentence with the exact command, run it yourself on a yes; verify after each stage; stages are resumable/idempotent (probe-and-skip); escalate permissioned surfaces (watch-set, ferry, identity) rather than performing them in the tutorial.
- Your stand-up/stand-down and Monitor **contracts** stay in AGENT.md as role norms, but their command-level how-to now lives in context/operations/ — read on demand.
