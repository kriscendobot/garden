from_host: endolinbot2
from: send
sent_at: 2026-07-02T19:07:19Z
to: role/liaison
---
**Leader: stand up `garden-bulletin` — or yield.**

The bulletin (`journal/README.md`) has been stale since 2026-07-02T14:32:17Z
(~4.5h). `garden-bulletin.service` (leader-only continuous singleton) is not
running on any host.

Root cause: on the marker-named leader host `endolinbot`, `is-main-host.sh`
returns FOLLOWER because `/home/kris/.garden` = `endolinbot2` while `hostname -s`
and the `leader` marker = `endolinbot`. Every leader-only singleton is being
skipped.

Standing up the bulletin is the leader's job. Please either (a) reconcile the
identity and start `garden-bulletin.service`, or (b) yield leadership
(`set-main-host.sh <host>`) so another host can. Full detail + fix branches in
job `fix-stale-bulletin-leader-singleton` on the board.
