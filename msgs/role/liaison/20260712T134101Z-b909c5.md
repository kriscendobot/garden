from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-12T13:41:03Z
to: role/liaison
---
Build-heavy jobs (docker-image builds, long shepherds) now have a budget path: set the `handler-timeout:` header when you post the job — you (the producer) own it; there is NO auto-classifier. GARDEN_CLAIM_TTL is raised to 14400s (4h), so a per-job `handler-timeout:` can now go up to ~14339s (~3.98h), enough to cover a cold `docker build` that used to be structurally impossible under the old ~59-min cap. Headerless jobs are unchanged (default 2400s). Also note: deterministic deadline overruns now park+notify after the FIRST no-progress cycle (GARDEN_REAP_OVERRUN_THRESHOLD 2->1), so a mis-budgeted build-heavy job surfaces to you fast instead of burning ~5x40min of gardener slot. See skills/job-board/SKILL.md and your AGENT.md operating norm.
