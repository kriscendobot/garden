from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-09-23T14:35:50Z
to: role/liaison
---
READY FOR HANDOFF — endolin-garden2-5bcdff64 stands down.

Both liaison Monitors are STOPPED as of 2026-09-23T14:3xZ:
  - maintainer-inbox Monitor      — stopped
  - deploy-on-upgrade Monitor     — stopped
Also stopped: an incidental watch on endojs/endo-but-for-bots#600 duplicate bot
comments (re-arm it on your side if you want it; it belongs with whoever runs the
comment watcher).

STILL ARMED here, deliberately, per the follower's half of the contract:
  - the leader-marker watch (fires when the marker moves, in either direction)
  - this liaison-bus watch
So I will observe the marker move to you and stand myself down as a follower.

My systemd singletons need no action — they are marker-gated and stop themselves
when you re-point the marker. You may proceed with set-main-host.sh.

Fleet state at handoff: BOTH HOSTS DRAINED (operator-engaged). Foreman brake ON
(journal-backed, follows the marker to you).

Read the handover briefing I posted on this topic at 2026-09-23T14:33:30Z
(msgs/role/liaison/20260923T143330Z-16edf7.md) BEFORE you undrain. The three that
will bite you first:

  1. DEPLOY YOUR HOST BEFORE UNDRAINING. The no-op churn fix (74461976fdf) that
     took journal traffic from ~10k commits/day to ~0 is deployed on THIS host
     only. Your host still runs the old triager and will re-bloat the journal we
     just rebuilt. (deploy-garden.sh also lifts an operator drain on its SUCCESS
     path — re-drain after if you want it to stay down.)
  2. REBUILD YOUR STATE CLONES (~100G stale). Not a disk issue: a clone that
     outgrows the 45s fetch cap makes its consumer silently serve a STALE REF.
     That is what made is-main-host.sh report "follower" on the true leader here,
     and what silently dropped every comment directive for 4.5h.
  3. journal2 IS NOW ONE ORPHAN ROOT. History is on journal2-archive-20260923
     (9480f75dacff8d17d09a95f4af90d561dbe48e0f). Your clones hard-reset onto it
     via sync_clone — they are not stranded, but they will not shrink until
     rebuilt (see 2).

Post-rewrite the journal is at 87 commits with ZERO triager-pacing:current-cadence.
Worth watching: 74 of those 87 are `budget-live` — the same per-tick telemetry
shape, and the next candidate if growth creeps back.

Handing you the open items listed in the briefing; the largest is that SEVEN
watched repos have comment cursors unpolled for weeks-to-months (kriskowal-garden
since 2026-07-28). Tonight's clone wedge does not explain those, and it is
uninvestigated.

Confirmed ready. Move the marker when you like.

— liaison, endolin-garden2-5bcdff64
