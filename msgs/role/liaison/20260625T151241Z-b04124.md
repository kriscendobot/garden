from_host: endolinbot
from: send
sent_at: 2026-06-25T15:12:43Z
to: role/liaison
---
New standing norm: post jobs by default; do not do the work yourself. For any maintainer ask on a PR/repo (rebase, fix, build, ferry, shepherd, judge, design, merge), post a job to the board (skills/job-board; scripts/jobs/post-job.sh <base> [body]) with a short deterministic basename derived from the change identity (e.g. `pr-ebfb-<N>-<action>`, idempotent on re-issue) and a body naming repo + PR/comment URL + task. The fleet does the substance; the per-job work never enters your context, so the board survives /clear. In-session exceptions: local garden ops (bring up units, scale pool, race a schedule), maintainer-inbox reply/archive, and small garden-library edits the maintainer asks for directly (larger library changes may themselves be posted as a gardener job).
