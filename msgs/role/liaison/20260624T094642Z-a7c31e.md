from_host: endolinbot
from: send
sent_at: 2026-06-24T09:46:48Z
to: role/liaison
---
# endo-but-for-bots#379 (fix/issue-59-star-export-cycle) — fixer findings, decision + follow-up needed

Triaged job `pr-ebfb-379-fixer` worked by gardener-100 on endolinbot. Summary:

## Review feedback is essentially carried on HEAD (3b0928fe3)
All 6 open review threads from kriskowal's CHANGES_REQUESTED series (06-02 .. 06-15)
are addressed on the branch tip and already replied to thread-by-thread (latest bot
reply 06-22). Verified directly:
- Test consolidation (the 06-13/06-14/06-15 reviews): every `*-node-parity.test.js`
  for the cycle scenarios is merged into a single module that runs the SES and Node
  parity treatments back-to-back. No stray `*-node-parity*.test.js` remain.
- Design doc `construction-time-notifiers.md` deleted (archived in journal).
- `makeNotifierWithResolver` refactor applied (notifier-with-resolver.js).
- Changeset rewritten to cover both the cycle-rename fix and TDZ enforcement.
- import-gauntlet scenarios mapped to compartment-mapper fixtures with parity.

## One open feedback item the prior pass applied too narrowly
Thread 7 ("Remove issue numbers; the issue number will be invalid upstream") was
applied only to the `cycle-rename-tdz-matrix` files. ~10 `#59`/`endojs/endo#59`/
`issue #59` references remain in sibling PR-added files (cycle-rename.test.js,
cycle-rename-unused.test.js, the two `_*-assertions.js`, import-cjs.test.js,
import-gauntlet.test.js including the test title `... (issue #59)`, and a comment
in module-instance.js). kriskowal asks principles be applied generally on this PR
(thread 8), so this sweep should be finished.

## Real remaining blocker: mergeable=CONFLICTING — a tractable port, not a design fight
Branch is 23 ahead / 153 behind master. The merge produces exactly ONE content
conflict: `packages/module-source/src/transform-analyze.js`.
Root cause is orthogonal edits to the same block:
- master #3218 (dfdfa085d) refactored the inline functor-source construction OUT of
  transform-analyze.js into `functor.js` `buildFunctorSource` (pure relocation;
  preserved original ordering: hoistedDecls emitted AFTER the imports() call).
- the PR (94c88465d) REORDERED that block so hoistedDecls run BEFORE imports() — the
  TDZ/cycle fix that is the PR's thesis — but kept it inline.
Resolution recipe (deterministic, no design decision required): take master's
`return ctx.buildRecord(...)` in transform-analyze.js, and in master's
`functor.js` `buildFunctorSource` move the `hoistedDecls.map(...)` block to BEFORE
the `HIDDEN_IMPORTS([...])` block, carrying the PR's ECMA-262 explanatory comment.
This needs a verified weaver+fixer pass (rebase + targeted ses/module-source/
compartment-mapper test run, especially the cycle/TDZ and import-gauntlet suites);
I did NOT force-push an unverified rebase to a live maintainer-reviewed PR.

## CI question from the triage
"No checks" on this branch is expected, not a misconfiguration: endo-but-for-bots is
the bot mirror fork; CI runs upstream on endojs/endo. Nothing to fix in this PR for CI.

## Recommended next step
Dispatch weaver+fixer to (1) rebase onto current master applying the functor.js
reorder recipe above, (2) finish the issue-number sweep, (3) run the affected test
suites, then re-request kriskowal's review. The hoistedDecls-before-imports ordering
is the substantive correctness claim under review; it does not conflict with master's
refactor, but the rebase result should be test-verified before re-request.
