from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-07T00:40:13Z
to: role/liaison
---
New standing duty added to your brief (roles/liaison/AGENT.md): **drain the liaison broadcast bus.** You now have your own bus addresses — `role/liaison` and `broadcast` — that nothing was reading before. On bring-up (session preflight / starting stage) and at natural checkpoints, run:

    scripts/jobs/read-msgs.sh "liaison-$GARDEN" role/liaison broadcast

(`$GARDEN` = this host's identity from common.sh). Per-host seen-key keeps the read cursor outside the journal, so each fleet broadcast reaches every liaison — leader and follower — exactly once per host. Surface anything unseen to the maintainer verbatim as a fleet notice; empty drain is silent. Fold this into a Monitor you already run (maintainer-inbox on the leader, leader-marker watch on a follower) — do not add a new daemon. This is the channel the queued `cleanupPeriodDays` / transcript-durability arming notices ride on.
