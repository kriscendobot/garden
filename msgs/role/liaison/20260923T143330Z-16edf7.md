from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-09-23T14:33:42Z
to: role/liaison
---
HANDOVER BRIEFING — endolin-garden2-5bcdff64 (current leader) to endolin-garden-ece02cb4 (incoming).

I am told you are coming up with typesafe.ai and Opus 5.5 support and will ask me
to resign shortly. I am READY and will stand down my two liaison Monitors and
confirm on this topic the moment you ask. Do not move the marker before that
confirmation (context/operations/leader-follower.md, step 2 before step 3).

STATE YOU ARE INHERITING — read this before you undrain anything.

1. THE JOURNAL WAS REWRITTEN TODAY (2026-09-23 ~05:45Z). journal2 is now a SINGLE
   orphan root commit. Full 146,329-commit history is preserved on the branch
   `journal2-archive-20260923` (sha 9480f75dacff8d17d09a95f4af90d561dbe48e0f).
   The new root's tree is byte-identical to that tip — no state was lost.
   Your clones will hard-reset onto the new root via sync_clone; they are NOT
   stranded. They will NOT shrink until rebuilt (next item).

2. YOUR HOST STILL CARRIES ~100G OF STALE CLONES. On this host 53 state clones
   totaled 96.6G; rebuilt ones are now ~50M each. This is not just disk: a clone
   that outgrows the 45s fetch cap makes its consumer SILENTLY serve a stale ref.
   Two live incidents here today: a 105G leader clone made is-main-host.sh report
   "follower" ON THE TRUE LEADER for hours, and a 6.6G cursors clone latched the
   shared journal-outage cooldown so every comment watcher dropped every directive
   for ~4.5h while systemd recorded each tick as success.
   Remedy used here (safe, ~2 min): move $GARDEN_STATE/*/journal aside, let
   ensure_clone rebuild, rm the retired copies in the background.

3. THE CHURN EMITTER IS FIXED BUT ONLY DEPLOYED HERE. 78% of ALL journal traffic
   was one no-op: `triager-pacing:current-cadence`, 12,125 of 12,267 decision
   commits over 09-20..23, written by every triager tick that found the cadence
   unchanged (~4k commits/day). Fixed in 74461976fdf (records only on a transition,
   plus a 6h heartbeat) and deployed on THIS host. If your host undrains while
   still running the OLD triager, it will re-bloat the fresh journal immediately.
   DEPLOY BEFORE YOU UNDRAIN.

4. FLEET IS DRAINED on both hosts (operator-engaged). NOTE: deploy-garden.sh lifts
   an operator drain on its SUCCESS path — re-drain after deploying if you want it
   to stay down.

5. FOREMAN BRAKE IS ON (config/foreman-brake, journal-backed, set 2026-09-17 for
   quota pressure). It FOLLOWS THE MARKER, so it becomes yours. Maintainer
   instruction 2026-09-22 was to keep it on and promote nothing.

6. STANDING INSTRUCTION (maintainer, 2026-09-23): THE LEADER RUNS THE COMMENT
   WATCHER. 16 repos are armed here. Note `systemctl --user disable` is NOT durable
   — the repo-watcher reconciler re-arms within ~90s; only `mask` holds.

7. garden-mention-watcher is DISABLED here and I did not arm it. It is OMIT-ONLY
   (monitoring-safety constraint): GitHub-wide @-mention surveillance must be a
   deliberate maintainer act. Do not auto-arm it.

OPEN ITEMS I AM HANDING YOU
- BIGGEST: seven watched repos have comment cursors unpolled for WEEKS-TO-MONTHS
  (kriskowal-garden since 2026-07-28; agoric-3-proposals 07-10; agoric-sdk 07-23;
  finbot 07-25; list 08-21; proposal-compartments 08-16; oros-ckm 09-17). Tonight's
  clone wedge does NOT explain those. Directives in them were silently dropped.
  Uninvestigated.
- Maintainer inbox: 43 unread after a muster took it from 227. Survivors are
  classified (6 decision-gated, ~14 doom-and-halt, ~8 informational).
- endojs/endo-but-for-bots#600 carries 34+ duplicate bot acknowledgments from a
  fail-open dedup; the fix landed on main2 (b67de8ea663) but the existing comments
  were deliberately NOT deleted — that cleanup is the maintainer's call.
- endojs/endo-but-for-bots#1329 was merged (f9cbcfc426) after its approval sat
  unseen behind the frozen cursor.

— liaison, endolin-garden2-5bcdff64
