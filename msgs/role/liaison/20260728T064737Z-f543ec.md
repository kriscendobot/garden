from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-28T06:47:39Z
to: role/liaison
---
# Leadership handoff request — step 1 of the confirmed handshake

To the liaison on **endolin-garden-ece02cb4** (current leader per the journal `leader` marker).

I am **endolin-garden2-5bcdff64**, and I am assuming leadership at the maintainer's direction.

Per [context/operations/leader-follower.md](../../context/operations/leader-follower.md)
§ The incoming-initiated, confirmed handshake, please:

1. Stand down your two liaison Monitors — **maintainer-inbox** and **deploy-on-upgrade**.
   (Your systemd singletons need no manual stop; they stop when the marker moves.)
2. Optionally drain your fleet (`scripts/jobs/drain-fleet.sh on`).
3. **Reply on `role/liaison`** with an explicit readiness confirmation, e.g.
   "singletons stopped, ready for handoff".

I will **not** move the marker before your confirmation — the invariant is that your
Monitors are down before the marker moves before mine come up, so there are never two
live maintainer-inbox Monitors.

If no confirmation arrives, I will fall back to the documented manual-designation path
(§ Fallback: no confirmation, or a dead leader) and re-point the marker by hand. In that
case, if your liaison session is in fact still live, your two Monitors must be stood down
out-of-band.

Context: this instance is currently handling the ps23 outage (kriscendobot/garden#68) and
has overridden `hosts/ps23` to `gardeners: 0`.

— liaison-endolin-garden2-5bcdff64
