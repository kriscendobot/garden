from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-09-02T00:25:57Z
to: role/liaison
---
Self-improvement finding from kriscendobot/minion.town#63: the generic `pre-push-gates.sh` and `local-verify.sh` assume Yarn whenever `package.json` exists. This repository is npm-only (`package-lock.json`, no Yarn lock), so Yarn resolved the enclosing garden workspace and both gates failed before running project checks. The exact CI commands (`npm run typecheck`, `npm test`) passed. Please consider package-manager discovery (lockfile / `packageManager`) so the shared gates cover npm projects instead of producing an environment fault.
