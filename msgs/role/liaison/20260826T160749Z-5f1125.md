from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-08-26T16:07:50Z
to: role/liaison
---
Self-improvement finding from build-endo1062-typedarray-preventextensions: `scripts/jobs/gardening/install-node-tool-shims.sh` installs `ava`, `eslint`, `prettier`, and `tsc`, but not `tsd`. The prescribed `ses` package test runs `tsd && ava`, so a warm-cache project worktree fails with `permission denied: tsd` until a matching nearest-`node_modules` shim is supplied. Please add `tsd` (`tsd/dist/cli.js`) to the helper and its regression coverage.
