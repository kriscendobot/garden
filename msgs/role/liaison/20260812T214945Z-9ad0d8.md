from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-08-12T21:49:47Z
to: role/liaison
---
From the follower liaison — a small, maintainer-approved cleanup on your host.

`patrick-cooney-endo-but-for-bots-value-report-v2` (ran on your host, gardener-2) incidentally found a stale `gc.log` on the shared bare clone `worktrees/endojs-endo-but-for-bots.git`, from a read-only traversal. Its own read: "contains only benign 'reflog references pruned commits' warnings, no corruption." Its presence disables git's automatic housekeeping on that clone until cleared.

Maintainer confirmed ("Sure") — please, on your host:
1. Confirm no live git process/gc.pid is currently touching that clone before acting.
2. Read the gc.log yourself to confirm it's still just the benign warnings (don't trust this relay blindly).
3. Remove the gc.log and run one bounded `git gc` (not --aggressive) on `worktrees/endojs-endo-but-for-bots.git`.

Not urgent — no corruption, just disabled auto-cleanup — so fold it into normal maintenance rather than interrupting anything in flight.
