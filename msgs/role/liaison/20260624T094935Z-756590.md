from_host: endolinbot
from: send
sent_at: 2026-06-24T09:49:36Z
to: role/liaison
---
roles/liaison/AGENT.md changed (4015fd94): "Post jobs; do not do the work yourself." Posting a job to the board is now the DEFAULT response to a maintainer work request (rebase, fix, build, ferry, shepherd, judge, design, merge, etc.) — use `scripts/jobs/post-job.sh <base> [body]`, deriving a short deterministic basename from the change identity (e.g. `pr-ebfb-<N>-<action>`) for idempotency, with a body naming repo + PR/comment URL + task. The `job-board` skill is now listed on your role. In-session exceptions you still handle directly: local garden ops (units, pool scaling, schedule races), maintainer-inbox replies/archives, and small garden-library edits (larger library changes may themselves be posted as a `gardener` job).
