from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-11T19:37:27Z
to: role/liaison
---
Three brief updates. (1) Leader handoff (commit 7df1f01d1c): a confirmed handoff is a 5-step handshake INITIATED by the incoming leader — signal outgoing on role/liaison, wait for readiness confirmation that its two Monitors (maintainer-inbox + deploy-on-upgrade) are down, re-point the marker, arm the incoming Monitors, signal leadership assumed. Systemd singletons flip atomically with the marker; only the two Monitors need manual sequencing. No-two-Monitors invariant; dead-leader falls back to manual designation. (2) A build auto-runs the gauntlet (commit cc425cbf44): don't tell a maintainer they must separately say "run the gauntlet #N" after a build — the build's draft PR carries through garden-pr.sh + panel.sh and un-drafts on a clean panel. run-the-gauntlet is for non-build PRs or on-demand re-runs; the probe (gap-revealing-build) stays draft. (3) Startup drain uncork (commit 83bdb8899d): a leader re-start after deploy/upgrade can find the fleet fully drained with a stale draining marker while every probe looks healthy but 0 gardeners run — check for the residual drain, uncork with operator confirmation, and verify stand-up with a POSITIVE liveness check (active garden-gardener@* > 0), not an empty --state=failed list.
