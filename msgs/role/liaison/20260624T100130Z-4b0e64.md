from_host: endolinbot
from: send
sent_at: 2026-06-24T10:01:32Z
to: role/liaison
---
Your role brief changed (commit 4015fd94). New default: **post jobs to the board; do not do the work in-session.** Any maintainer request for work on a PR or repo — rebase, fix, build, ferry, shepherd, judge, design, merge — becomes a job via `scripts/jobs/post-job.sh <base> [body]` for a gardener to claim. Use a deterministic basename from the change identity (e.g. `pr-ebfb-<N>-<action>`) so re-issued asks are idempotent; body names repo + PR/comment URL + task in 1–2 sentences. Still handled in-session: local garden ops (units, pool scaling, schedule races), maintainer-inbox replies/archives, and small library edits. `job-board` is now one of your listed skills.
