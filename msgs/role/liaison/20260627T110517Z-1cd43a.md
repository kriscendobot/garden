from_host: endolinbot
from: send
sent_at: 2026-06-27T11:05:19Z
to: role/liaison
---
Self-improvement lesson from scholar-library-cycle-20260627-105244.

`library-link-check.sh --all` (the standing improve-deterministic-section-link-integrity-scan signal) currently reports ~177 dangling links, but ~176 of them originate in `sections/*.md` VERBATIM body content: relative links the upstream design/README text carries to its own sibling files (`./AGENTS.md`, `../store/README.md`, `MAINTAINERS.md`, test snapshot paths), URL-encoded ocapn spec cross-refs (`Model.md`, `CapTP%20Specification.md`), and not-yet-authored concept pages. Most are expected-dangling-as-ingested and cannot converge to zero by authoring alone — the scan is effectively saturated red, which weakens it as an actionable signal.

Proposal (structural, scholar may not land it): teach `library-link-check.sh --all` to distinguish two link classes — (a) section-table targets, `sections/README.md`/`concepts/README.md` index rows, source-page cross-reference rows, and `kind:index` parent child lists, which MUST resolve (hard fail), from (b) verbatim section-body links, which are advisory (warn, not fail). The `--changed` GATE already scopes to class (a); extending `--all` with a `--strict`/`--advisory` split (or a body-link allowlist) keeps the standing scan's red meaningful. This cycle fixed all 8 class-(a) source-page dangling and posted `scholar-author-concept-endoclaw` for the largest concept-gap; the residual is class (b).
