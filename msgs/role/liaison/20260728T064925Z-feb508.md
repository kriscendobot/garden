from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-28T06:49:29Z
to: role/liaison
---
# Leadership handed off to endolin-garden2-5bcdff64 — you are the leader now

kriskowal directed the handoff (2026-07-28 ~06:48Z). The `leader` marker now names
**endolin-garden2-5bcdff64**. This is the outgoing-initiated form of the handshake in
context/operations/leader-follower.md § Designating and handing off leadership.

## What the outgoing leader (endolin-garden-ece02cb4) already did

- **Stood down BOTH liaison Monitors BEFORE moving the marker** — the maintainer-inbox
  watch and the deploy-on-upgrade watch are stopped. The no-two-live-maintainer-inbox-
  Monitors invariant is satisfied, so you may arm yours immediately.
- Re-pointed the marker with `set-main-host.sh`. Your systemd singletons (foreman,
  scheduler, watchers, bulletin, recovery services) are marker-gated by
  `is-main-host.sh` and start on their next timer tick with no action from you.

## What you need to do

1. **Arm your two liaison Monitors**: the maintainer-inbox watch
   (`scripts/jobs/maintainer-watch.sh`) and the deploy-on-upgrade watch
   (`$GARDEN_STATE/deploy/upgrade-ready`). Nothing is watching the maintainer inbox
   right now.
   - Caution: the inbox holds ~279 unread, about 60 of them `poison-*`.
     `maintainer-watch.sh` re-dumps EVERY unread message on every tick, which will
     flood and get a Monitor auto-stopped. Prefer a delta watch that reports only new
     arrivals and counts poison separately.
2. **Drain your liaison bus**: `scripts/jobs/read-msgs.sh "liaison-$GARDEN" role/liaison broadcast`.
3. Keep watching the `leader` marker, as every host does.

## State you are inheriting

- **endolin-garden-ece02cb4 (this host) is DRAINED and stays drained.** kriskowal is
  performing a MANUAL UPGRADE here. Its board share is clear (0 in-flight claims, 0
  gardeners). **Do not lift its drain**; only kriskowal or its liaison should.
- **Its root is deployed at `bb971c9a`**, three commits behind `main2`
  (`1f3f123186`, `ca328dd15b`, `a046d34ea6`). A deploy here would lift the drain, so
  it is deliberately deferred.
- **ps23 is the live problem.** It holds ~55 in-flight claims and all its gardeners
  fail with "claude not on PATH" — kriscendobot/garden#68. Note your override writing
  `hosts/ps23: gardeners 0` will NOT take effect: the scaler refuses to scale
  gardeners below 1 ("use drain-fleet.sh to pause work"). Stopping ps23 requires a
  HOST-LOCAL drain on ps23 itself. It has also not honored the standing request to
  set `gardeners: 2` (quota throttle, ~01:14Z).
- **Bulletin**: its fine-grained PAT died with the repo transfer (owner-scoped tokens
  do not follow a transfer); kriskowal must re-mint under `kriscendobot`. Job
  `fix-bulletin-after-repo-transfer` is posted and waiting in todo.
- Open jobs posted tonight: `build-sysop-host-operations` (orchestration),
  `fix-identity-drift-guard-test-inbox-leak`, `suffix-github-comments-with-provenance`,
  `fix-bulletin-after-repo-transfer`.

Please confirm "leadership assumed" on role/liaison once your Monitors are armed.
