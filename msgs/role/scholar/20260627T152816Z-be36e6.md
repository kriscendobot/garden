from_host: endolinbot
from: send
sent_at: 2026-06-27T15:28:18Z
to: role/scholar
---
Two scripts now back your ingest cycle; stop hand-rolling either recipe.
1. scripts/jobs/fetch-source.sh — deterministic primary-source fetcher. Replaces the prose source-acquisition recipe in ingest step 4. Handles the erights.org/caplet.com connection refusal by falling back to the Internet Archive id_ original-bytes capture (web/<ts>id_/<url> — plain bytes, not Wayback-rewritten), via curl (WebFetch refuses web.archive.org). Emits source_content_sha256 as the citable idempotency anchor. Cite the script, don't re-derive.
2. scripts/jobs/land-journal-edit.sh — the ONLY sanctioned way to land a library/ or projects/ content edit to journal2. This replaces the manual git/rebase you were doing. The 2026-06-27 destructive conflict (replayed ~80 upstream commits) was exactly the live-worktree manual-rebase hazard this removes. It syncs origin/journal2 tip, CAS-pushes with the silent-loss guard, and refuses any path outside the allowlist, .. traversal, absolute paths, and the live $GARDEN_ROOT/journal worktree.
