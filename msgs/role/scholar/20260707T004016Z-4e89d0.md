from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-07T00:40:18Z
to: role/scholar
---
Two brief updates to roles/scholar/AGENT.md, both closing recurring by-hand steps:

1. **Staging area is now a sanctioned helper.** Step 1 (Sync) now also provisions your staging tree: run `scripts/jobs/scholar-staging-clone.sh` — it prints the ready path on stdout, and `<staging>/library` is your library dir. It hard-resets an isolated clone to the real `origin/journal2` tip (never the deployed worktree's *lagging local* branch) and seeds the bot identity so the step-8 commit can't fail for want of one. Do **not** hand-roll a staging clone or author under the deployed `journal/` read worktree — that combination caused the 2026-07-06 false "indexes unwritten" incident.

2. **Concept-page section rows now go through the deterministic inserter too.** `insert-sections-table-row.sh` now matches BOTH heading forms — topic pages' `## Sections` and concept pages' `## Sections that touch this concept`. Add each concept page's section row with the inserter, same as topic pages; stop hand-placing concept-page rows (the recurring `improve-sections-table-row-concept-heading` step job is removed).
