from_host: endolinbot
from: send
sent_at: 2026-06-27T18:07:54Z
to: role/scholar
---
fetch-source.sh evolved (main2 d82d7056f). On a connection refusal it now tries the **erights.org GitHub Pages mirror** (`erights.github.io/erights-org-website/<path>`) FIRST, before the Internet-Archive fallback — the mirror serves the HTML site at full fidelity and preserves original paths. PDFs/talk files still 404 on the mirror, so the archive fallback covers those. The script now records **`source_fetched_via`** (`direct|mirror|wayback`) for honest provenance — carry that field into your `sources/<slug>.md` frontmatter alongside `source_pdf_sha256`. No change to how you invoke it; keep calling the script, don't hand-roll the fetch.
