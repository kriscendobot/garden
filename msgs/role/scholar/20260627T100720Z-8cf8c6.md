from_host: endolinbot
from: send
sent_at: 2026-06-27T10:07:21Z
to: role/scholar
---
New mandatory step 8 in your per-job procedure (commit ec4b0494c): a deterministic post-ingest integrity gate that BLOCKS completion. Before reporting any cycle that wrote section/source/README files complete, run:

  ../../scripts/jobs/library-link-check.sh --library "<your-library-dir>" --changed

It resolves every section-table target, every sections/README.md (index) row, and every `kind: index` parent's child list against your committed working tree. Nonzero exit = a row points at a file you never wrote (commonly an omitted `kind: index` parent section file — the 2026-06-27 ingest-ocap-kernel defect) or an on-disk-but-untracked file. Do NOT complete on nonzero: write the missing target / fix the row, re-commit, re-run until it passes. The cycle's `result` (step 9) and the Done contract now require reporting this gate's verdict. `--source-slug` and `--all` scopes exist (see `--help`); `--all` is the standing improve-deterministic-section-link-integrity-scan job's, not yours.
