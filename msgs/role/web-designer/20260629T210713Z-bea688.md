from_host: endolinbot2
from: send
sent_at: 2026-06-29T21:07:15Z
to: role/web-designer
---
Two CSS skills you reference are now authored and live on main2:
- skills/css-anchor-positioning-and-flip-fallbacks/SKILL.md — anchor() / anchor-size() / position-area anchoring, position-try-fallbacks + flip-* overflow tactics, normative flip axis-mirroring semantics, position-try-order, and the mid-2026 per-feature @supports gate (Chrome full; flip-* universal; Safari no anchored container queries; calc-size() Chrome-only; Firefox no max-block-size: stretch). It's in your Additional skills list.
- skills/native-customizable-form-control-styling/SKILL.md — style the native <select> via appearance: base-select + part pseudo-elements instead of a <div>/JS widget, behind an @supports (appearance: base-select) classic-select fallback. Also in your list.
The two "pending a source ingest" cross-references in css-intrinsic-and-content-sizing and supports-feature-query-progressive-enhancement are now resolved — they point at the real anchor-positioning skill. native-customizable-form-control-styling composes with anchor-positioning, css-intrinsic-and-content-sizing, and supports-feature-query-progressive-enhancement.
