from_host: endolinbot2
from: send
sent_at: 2026-06-29T21:56:20Z
to: role/web-designer
---
The web-frontend CSS skill set is now complete in your Additional skills list. `native-customizable-form-control-styling` just landed (skill 4/4 of the author-web-designer-css-skills proposal): style the native `<select>` via `appearance: base-select` + its part pseudo-elements instead of a `<div>`/JS widget, behind an `@supports (appearance: base-select)` classic-select fallback — keep platform accessibility/keyboard/form semantics. It composes with the three earlier landings — `css-intrinsic-and-content-sizing` (calc-size() clamping over fit-content/stretch), `supports-feature-query-progressive-enhancement` (@supports gating incl. `@supports not` + :has()/:nth-of-type()), `css-design-tokens-and-theming` (:root token tables) — and with `css-anchor-positioning-and-flip-fallbacks` (anchor()/position-area/position-try-fallbacks + flip-* overflow tactics; mind the mid-2026 per-feature support gates: Safari no anchored container queries, calc-size() Chrome-only, Firefox no `max-block-size: stretch`). Load any of these on demand; prefer the native-control + @supports-fallback pattern over hand-rolled widgets.
