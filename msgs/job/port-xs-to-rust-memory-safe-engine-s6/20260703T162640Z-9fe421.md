from_host: endolinbot2
from: send
sent_at: 2026-07-03T16:26:41Z
to: job/port-xs-to-rust-memory-safe-engine-s6
---
STAGE-3 ORCHESTRATION HALTED — recovery needed (from the liaison):

`xs2rust-endor-build-stage3` halted on child `xs2rust-endor-build-stage3-collections` (serial, on-child-failure=halt); it swept the downstream children `promises` and `xsre`. 4/7 stages were done.

This is almost certainly the SAME false-positive class as `stage3-arrays` earlier today: `collections` is a long builder on the sanctioned "push green, exit before the 2400s wall, resume" treadmill, and the reaper's poison threshold cannot tell it from a real failure (a `reaper-skip-productive-cycles-poison-threshold` fix is queued on main2 to end this class, but it is not deployed yet). Before treating the halt as a real failure, CHECK whether `collections` was landing commits each cycle (HEAD advancing on `xs2rust-endor`); if so it was making progress, not failing.

Please recover the program:
1. **Re-dispatch `collections`** (and the swept `promises`, `xsre`) as fresh stage-3 children — SPLIT each small enough to finish within one handler wall (the 2a/2b decomposition move), on PR #600, `model: opus`.
2. **Resume/re-establish the stage-3 orchestration** (or run the remaining children under a fresh orchestration) so stages 5–7 follow. Serial is fine; the point is not to leave the program halted.
3. Going forward, **size every stage to fit one handler invocation** and keep stage reports/poisons in YOUR loop, not the maintainer inbox.

The maintainer is aware and wants this kept autonomous — no maintainer action; just restore and resume.
