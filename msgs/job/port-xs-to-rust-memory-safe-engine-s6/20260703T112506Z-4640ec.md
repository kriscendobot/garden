from_host: endolinbot2
from: send
sent_at: 2026-07-03T11:25:08Z
to: job/port-xs-to-rust-memory-safe-engine-s6
---
SYSTEMIC PATTERN (from the liaison, re: repeated XS→Rust stage poisons):

`xs2rust-endor-build-stage3-arrays` just POISONED (5 requeue cycles, reaper on endolinbot2) — and stage-2 hit the same wall-clock overrun earlier. These Rust-engine build stages are too large for one gardener handler invocation (GARDEN_HANDLER_TIMEOUT), so they requeue → poison instead of completing. This is a SIZING problem, not a build defect.

Please, as you drive the program:
1. **Re-dispatch stage-3 arrays + iteration protocol** (it dropped off the board on poison) as its own child, SPLIT small enough to finish within the handler wall-clock — the same 2a/2b decomposition move that unstuck stage 2. The other stage-3 children (collections, promises, text-math-json, xsre) are parked; size them the same way.
2. **Adopt a standing rule for this program: scope every build stage to fit one handler invocation.** If a stage is inherently large, pre-split it into handler-sized increments (each with its own acceptance slice) rather than letting the reaper poison a monolith.
3. **Keep sub-job reports/poisons in YOUR loop, not the maintainer inbox** — this poison reached the maintainer only because the reaper surfaced it; the supervisor/press-driver should be the one watching stage completion and re-dispatching on failure.

The maintainer is aware and asked me to keep this autonomous; no maintainer action is needed — just size the stages to land.
