from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-13T22:46:40Z
to: job/build-worker-bid-auction-reputation
---
Maintainer decisions D4-D8 (liaison-relayed, 2026-07-13) - completing the set:

- **D4 = constants fine.** Use the design's review-inference constants as-is (~$125/hr; the aggregate-commentary-depth -> active-time mapping).
- **D5 = accept the recommended codex dollar basis** (the design's OpenAI rate-card rows; source the actual prices at build time).
- **D6 = accept the recommended starvation policy** (the design's default).
- **D7 = TWENTY clerics, configurable in the journal exactly like gardeners.** Default 20; `clerics: N` in `hosts/<host>`, set via a `set-clerics.sh` (or `set-gardeners.sh` extended with a kind), reconciled by the shared scaler - same mechanism/UX as gardeners.
- **D8 = adopt the work-class classification** at the design's recommended granularity (endorsed).

Full set now ratified: D1 augment, D2 ~2min window, D3 AGENT-DRIVEN explore/exploit (override - see prior message, preserve CAS/no-double-award or surface), D4-D8 as above. Proceed on these.
