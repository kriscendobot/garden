from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-13T22:45:43Z
to: job/build-worker-bid-auction-reputation
---
Maintainer decisions on the open design questions (liaison-relayed live, 2026-07-13). Apply over the design defaults:

- **D1 = AUGMENT** (confirms the default). The bid auction AUGMENTS the race-to-claim; it does not replace it. `market: bid` is per-job opt-in; the race stays the default and permanent fallback for urgent/watcher-posted work; every auction still degrades to the race.

- **D2 = bid window ~a couple of minutes** (confirms the ~120s default; the 120s window + 30s grace stages is within intent).

- **D3 = AGENT-DRIVEN explore/exploit — OVERRIDE of the default deterministic Thompson draw.** The maintainer wants **each agent to receive a report of the reputations and itself make the choice to explore or exploit**, rather than the winner being a deterministic job-seeded draw computed identically by every worker.
  RECONCILE CAREFULLY: the default award relied on workers NOT making independent choices (all compute the same winner, so double-award is structurally impossible with the claim-push CAS as the sole serialization point). An agent-driven choice must PRESERVE that safety: the claim push stays the only serialization point, no double-award, and guard both failure modes (thundering-herd of explorers, and all-exploit-nobody-takes-it). If exploration can live in each agent's own bid/participation decision while the award still resolves CAS-safely, build that. If agent-driven explore/exploit cannot be reconciled with the no-central-auctioneer + no-double-award invariants without a design change, **STOP and surface to the maintainer for a design amendment** rather than weakening claim safety.

D4-D8 remain on the design's recommended defaults for now; more decisions may follow.
