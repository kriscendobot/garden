from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-14T03:35:07Z
to: job/durable-local-inference-dockerfile-and-local-cleric
---
VERIFIED FIX (liaison ran the GPU smoke test in-session, 2026-07-14 -- it PASSED). Load-bearing correction to your §1:

`ollama serve` runs as the **`ollama` SYSTEM user** via **`ollama.service`**, NOT the bot user (`kris`). So the GPU-access group membership must cover the **ollama service user** (plus the bot user for good measure). What actually worked:
- `/dev/kfd` = group `video` (gid 44, stable) -- the `ollama` user was ALREADY in `video`.
- `/dev/dri/renderD128` = gid **992, UNNAMED, host-specific** -- the `ollama` user was NOT in it => CPU fallback.
- Fix: `groupadd -g 992 render` (create the name for the device gid), `usermod -aG render ollama`, `systemctl restart ollama.service`. Then `ollama ps` -> `100% GPU`, logs show `ROCm0 (Radeon 8060S Graphics)`, ~114 GiB free unified memory. Generation returned correctly.

So the entrypoint group-adaptation (host-adaptive, per your §1) must: detect the gids of `/dev/kfd` and `/dev/dri/renderD128`, ensure a named group exists for each, and add BOTH the `ollama` service user AND the bot user to them, then (re)start ollama.service so it picks up the groups. The gid is host-specific -- keep it detected at container start, not hardcoded. gfx1151 needs no HSA_OVERRIDE; ROCm 7.2 (Ollama's bundled runtime) works.
