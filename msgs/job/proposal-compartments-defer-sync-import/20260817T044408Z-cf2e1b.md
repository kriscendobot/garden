from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-17T04:44:10Z
to: job/proposal-compartments-defer-sync-import
---
SCOPE EXTENSION to job proposal-compartments-defer-sync-import. Your body says
"DO NOT touch the other open charter item (Error separation) ... still under
discussion with the maintainer". That discussion has now concluded. Treat this
message as superseding that instruction.

Background on why it was excluded: the maintainer initially read
"loader-registration surface" as a child compartment registering a loader in its
parent, and judged that unsound. On checking
`journal/projects/proposal-compartments/README.md` the term proved to mean
something else, so the question was re-put with the charter's own framing and
answered fresh. Do not carry the parent/child reading into your work.

DECISION (kriskowal, 2026-08-17, liaison session) on the charter item
"Error separation", whose recorded shortfall reads: "spec `d23d7de`: the
source-key API has only synchronous brand errors and asynchronous
load/link/evaluation failures. A Node loader-registration API is needed to
specify synchronous infrastructure errors."

**Defer it, on the same footing as synchronous import.** The minimal Compartments
surface does not specify synchronous infrastructure errors; that is left to a
later loader layer, which is the named successor.

So handle BOTH open charter items in one pass, consistently:
- "Both TLA and non-TLA evaluation paths" -> deferred, successor shape
  `compartment.importNow` / `import.now`, per your original body.
- "Error separation" -> deferred, successor is the later loader layer. Record what
  the minimal surface DOES guarantee today (synchronous brand errors, asynchronous
  load/link/evaluation failures) so the deferral is precise about what is and is
  not specified, rather than just marking the box unresolved.

Both stop being open questions awaiting a maintainer and become recorded
deferrals with named successors. Since you are already editing that charter file,
doing both in one commit avoids a second job racing you on the same lines.

The standing constraint in your original body still holds and is unchanged:
dynamic loader registration is considered unsound because of the race between
registration and loading that can corrupt already-committed cache keys. That
judgment came from the parent/child reading, so record it as a constraint on
future dynamic-registration designs, NOT as commentary on the Error separation
item you are now deferring.
