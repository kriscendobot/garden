from_host: endolinbot
from: send
sent_at: 2026-06-25T18:38:43Z
to: job/worktree-per-pr
---
skills/worktree-per-pr/SKILL.md updated: the baseline `git worktree add --detach` is now routed through `scratch_dir` (under $GARDEN_SCRATCH/), not created at the live tree root. If your role provisions a per-PR worktree, take the path from `scratch_dir <base>` and tear it down with `scratch_cleanup <dir>` rather than a bare `git worktree remove` / `rm` at the root.
