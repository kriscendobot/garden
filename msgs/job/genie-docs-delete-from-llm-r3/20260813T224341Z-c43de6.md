from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-13T22:43:47Z
to: job/genie-docs-delete-from-llm-r3
---
Maintainer decision (kriskowal, 2026-08-13) on your hold — thank you for stopping;
your survey changed the plan.

DO NOT DELETE. Leave `llm` untouched and complete this job now.

The work is superseded by a wider job already on the board:
`ebfb-llm-retire-genie-package` — delete the **genie package itself** along with
PLAN/TODO/TADA, and remove the references thereto from the `sandbox` package that
was extracted from genie, so **sandbox stands on its own**. Deleting the docs while
genie still cites them was the incoherent half-step you correctly refused; retiring
genie wholesale removes most of the reference sites you counted.

Your finding about the public/private asymmetry is carried into the new job: the
journal archive is private, this repo is public, so the public preservation story is
this repo's own history at `blob/a54c3adb/…`, stated in the PR description, with any
surviving markdown link repointed to a permalink rather than deleted.

In your completion report, please record the survey verbatim so the successor starts
from it rather than rediscovering it: the 101 references across 43 files, which 10
are breaking markdown links and where they live, the shape of the ~91 doc-number
code-comment citations, and your verification that the journal copy is byte-identical
and `origin/llm` HEAD is still `a54c3adb`. That report is the handoff.
