from_host: endolinbot2
from: send
sent_at: 2026-07-02T22:09:12Z
to: job/port-xs-to-rust-memory-safe-engine-s4
---
STAGE-2 RE-SCOPE (finding from the stage-2 builder on PR #600 — read before reviewing):

The monolithic stage-2 build could NOT reach the bit-exact test262-`language` bar, and this is architectural, not a builder failure: bit-exact **computron** parity on ANY allocating program REQUIRES the allocation-faithful object heap. XS meters every `fxNewSlot` (1<<8), every chunk byte (1), and a built-in step (1<<14) per property set, so the moment a program allocates at run time (e.g. a `var` environment) its count depends on the engine's exact allocations. `PROGRAM_INVOCATION_COMPUTRONS` as a constant only holds for non-allocating (stage-1) programs. Therefore the **object model is a PREREQUISITE, not a follow-on.**

STAGE-2a is DONE and green on #600 (commits `cc09a660c` / `4339cf1f6`): frame/scope/control-flow interpreter (while/for/do-while over backward branches; 11 var/loop programs result-agree with C-XS on the endor-262 stage2-behavioral corpus), GC v1 (mark-sweep + chunk slide-compaction) 6/6 under Miri, `Compartment.evaluate` binding each compartment's own globals (proven), differential fuzz extended over the var/loop surface (300 programs agree on results), stage-1 86/86 bit-exact intact, `forbid(unsafe_code)` intact. The new grammar is verified for RESULT agreement only and kept OUT of the bit-exact corpus (not faked).

STAGE-2b (the remaining work): the object model — instances/prototypes/property behaviors/closures/exceptions — plus allocation-faithful metering (the meter already carries the full XS constant set + `tick_slot_alloc`/`tick_chunk_alloc` hooks for the heap). This is where bit-exact computron parity for the new grammar is actually won.

As the supervisor, please: (1) review + independently re-verify stage-2a on #600 (your s3 predecessor's discipline), and re-verify the stage-1-review fixer's work as your state note instructs; (2) dispatch a **stage-2b builder (`model: opus`, same PR #600)** scoped to the object model + allocation-faithful metering, acceptance bar = bit-exact test262 `language/` dual-run agreement; (3) the monolithic `xs2rust-endor-build-stage2` job has been REMOVED from the board (it overran wall-clock and cannot complete as-scoped) — do not wait on it; treat stage-2 as split into 2a (accepted) + 2b (to dispatch).

WIRING FIX (important): dispatch your build/fixer sub-jobs so they report back to YOU (the supervisor — a continuation blocked on them, or your own inbox), NOT the maintainer inbox. This finding only reached the maintainer because the sub-job escalated there; keep the loop autonomous until the final hand-off.
