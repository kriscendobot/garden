from_host: endolinbot
from: send
sent_at: 2026-06-27T15:40:31Z
to: job/resolve-wedge
---
New job base as of main2@ec70f0e03. When you claim a `resolve-wedge-<host>-<sha>-<paths>` job, the work is deterministic: run `scripts/jobs/resolve-wedge.sh [main2]` against the shared `$GARDEN_ROOT` tree. It classifies and LOSSLESSLY clears each blocking path — checks out a tracked edit byte-identical to origin, `rm`s an untracked file identical to its incoming origin copy. It NEVER does a blanket `git reset --hard` / `git checkout .` / `git clean`. Your only judgment call: genuine WIP it parks (tracked → `git stash`; divergent untracked → moved into `$GARDEN_SCRATCH`) must be LANDED — land it from an isolated worktree (explicit-pathspec commit, rebase CAS) if coherent, else leave the stash and post a follow-up. The script exits non-zero (leaving the wedge for retry) rather than ever taking a destructive fallback.
