from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-12T05:02:12Z
to: job/fix-handler-budget-role-defaults
---
# ADD TO YOUR SCOPE — lower the requeue doom threshold from 5 to 2

**Maintainer directive (kriskowal, 2026-08-12):** "We should also doom anything that
fails twice. Five retries is too much."

Change in `scripts/jobs/reaper.sh:75`:

    : "${GARDEN_REAP_DOOM_THRESHOLD:=5}"   ->   := 2

You are already editing `reaper.sh` for the doom-notice budget fix, so this belongs in
your job rather than a concurrent one — a second job touching the same file is exactly
the collision class the fleet is currently fixing elsewhere.

## Why this is safe to lower, and worth saying in your report

The productive-cycle hint **resets** the counter rather than incrementing it
(`reaper.sh:767`: *"productive: '<base>' advanced a per-job worktree HEAD this cycle;
resetting reap/doom counter"*). So the threshold only ever counts cycles with **no
worktree progress at all**. A long job that keeps advancing never accumulates toward
it, however many times it is requeued. Lowering 5→2 therefore tightens the loop on
genuinely stuck work without endangering slow-but-productive work.

## Related values — check for consistency, do not change without saying so

- `GARDEN_REAP_OVERRUN_THRESHOLD` is already **1** (wall-clock overruns doom after one
  cycle). Leave it.
- `GARDEN_ORCH_STALL_REQUEUE_LIMIT` (`orchestrate.sh:77`) is already **2**, which now
  matches. Note in your report that the two are consistent — but do NOT edit
  `orchestrate.sh`: a peer job (`fix-orchestrate-watcher-progress-and-serial`) is
  live in that file right now and is changing how that limit is applied.

## Verify

Update any test that asserts the 5-cycle threshold, and confirm the doom notice's
wording still reads correctly when it fires after 2 cycles rather than 5. State the
new value and the tests you touched in your report.
