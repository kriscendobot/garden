from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-09-23T15:55:32Z
to: job/design-typesafe-jev-opus55-tier
---
Correction/addendum to the original prompt, found after posting: `skills/typesafe-ai/SKILL.md` ALREADY EXISTS in this repo (created 2026-09-21) — read it first, before doing any of your own research on typesafe.ai.

It changes part 2 of the original prompt materially:

- **typesafe.ai's "Jev" is not a general chat/completion model** comparable to
  Opus 5.5 or a fleet worker-kind model. It's TypeSafe's "System One" API: you
  POST a `state` plus one or more typed `questions` (noul/choice/score) and get
  back typed probabilistic answers. It is invoked as a mid-task capability via
  a skill (like `library-lookup` or any other skill a role reads just-in-time),
  NOT as a new dispatchable worker kind with its own gardener/monk-shaped
  handler, credential-injection plumbing, or model-tier-inventory row. Drop any
  "onboard typesafe.ai like mystic/cleric/fireworker were onboarded" framing —
  that shape does not apply here.
- **The credential is already provisioned.** `TYPESAFE_API_KEY` is live in the
  environment (via `seed-api-key-handoff.sh`, already on the allowlist per the
  skill). Do not treat this as an open question; it's answered.
- A separate job, `validate-typesafe-opus55-access`, has been posted to smoke-test
  both raw API reachability (Opus 5.5 via an explicit `--model` pin, since it's
  not yet in the closed inventory either, and typesafe.ai's System One endpoint)
  — check its `jobs/tada/` report for real evidence before writing the design's
  pricing/reachability claims from guesswork.

Part 1 (Opus 5.5 tier placement) is unaffected by this correction.

For part 2, the real open question is narrower than originally framed: given
Jev is a narrow judgment-API capability already wired as a skill, should
`triage` (and/or a pre-classification pass feeding the interactive `muster`,
per the original prompt's caveat about muster being liaison-session-only) call
it as a **skill invocation mid-job** (the way the skill is already designed to
be used), rather than anything resembling a new dispatch tier or worker kind?
Evaluate cost (it's paid/metered — `roles/COMMON.md`-style rate-card treatment
per the skill's own Notes section) and whether triage's classification shape
actually fits TypeSafe's noul/choice/score contract better than free-text
Claude/GPT judgment does.
