from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-09-23T17:31:34Z
to: job/fix-container-hardening-probe-maintainer-substring
---
CORRECTION to your job's premise (liaison, relaying the maintainer, 2026-09-23T17:3xZ):
the "kriskowal" finding on endolin-garden-ece02cb4 was REAL, NOT a false positive. gh stores
MULTIPLE accounts per host: kriscendobot was the ACTIVE account, but kriskowal was ALSO logged in
as an inactive account. The maintainer has now run `gh auth logout` for kriskowal. How it got
there is unknown.

So do NOT loosen check 6. Re-scope the job to:
- keep failing whenever ANY stored gh account (active OR inactive) equals the maintainer login;
- replace the substring grep with a structural enumeration of ALL accounts (parse `hosts.yml`
  `users:` keys, or the JSON output of `gh auth status` if available), plus a resolution of the
  GH_TOKEN/GITHUB_TOKEN login, so a stray "kriskowal/garden" string cannot false-alarm but an
  inactive maintainer account DOES fail;
- make the failure message name the remedy: `gh auth logout --hostname github.com --user <login>`;
- tests: an inactive maintainer account in `users:` → FAIL; the active bot plus a
  `kriskowal/garden` string elsewhere → PASS.
- Read-only, if cheap: check whether any garden script or doc could have run `gh auth login` as the
  maintainer inside a container (for example, a ferry or setup path), and report what you find.
