from_host: endolinbot2
from: send
sent_at: 2026-07-02T04:09:43Z
to: job/rewrite-garden-readme-usage-tutorial-fable
---
AMENDMENT to job `rewrite-garden-readme-usage-tutorial-fable` (the garden README tutorial):
1. **Reuse the existing getting-started section, don't duplicate it.** There is already a getting-started
   section enumerating many individual steps — and the key simplification to surface is that **the claude
   CLI will GUIDE the maintainer through all of them if they simply say "Please start the garden."** Lead
   the getting-started with that one-liner (the CLI walks you through Docker entry, bot account, SSH key,
   claude auth, the PAT for the bulletin, etc.), then reference/fold the existing step list rather than
   rewriting it from scratch.
2. **Add an APPENDIX: multiple containers + multiple Claude accounts** (the leader/follower fleet).
   Document the liaison vocabulary for running a multi-host fleet:
   - **"You are the leader"** — stand this host up as leader (singletons + maintainer-inbox + deploy Monitors).
   - **"You are the follower"** — run as a follower: gardener pool only, singletons ExecCondition-skip, and
     a standing leader-marker watch that stands you up if leadership is handed to you.
   - **"Please hand off leadership to <hostname>"** — drain + re-point the `leader` marker to another host,
     which raises that host's liaison via its watch. (Note: hand off only to a host that is RUNNING.)
3. **Document the `GARDEN` variable for multiple shards on a single hostname** — export `GARDEN=<name>`
   (persisted via `~/.config/environment.d/`) to run a parallel gardener pool / shard from one host without
   touching the Dockerfile (it overrides the host's logical identity; `GARDEN_HOST` derives from it). Cover
   how each shard needs a UNIQUE `GARDEN` identity (it keys claim metadata / hosts/<host> / the leader marker).
Keep the terse, occasionally-funny voice.
