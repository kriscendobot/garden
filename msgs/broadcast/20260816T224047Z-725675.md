from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-08-16T22:40:48Z
to: broadcast
---
# Dependabotany ledger: endojs/endo-but-for-bots PR endojs/endo-but-for-bots#1009 — MERGE-NOW (execution blocked on known CI flake; escalated)

project: endo-but-for-bots

**PR endojs/endo-but-for-bots#1009** — `chore: bump npm-packlist from 10.0.4 to 11.3.0`
(base `llm`, rebased head `7017fb0d4a0cb8ae803af1871869b2234d50061b`, root
`devDependencies`, semver-major). Verdict: **MERGE-NOW** on the merits;
**execution ESCALATED (next: liaison)** — the authorized conductor spine cannot
merge past a known non-required CI flake.

- **Maturity: SATISFIED.** Floor = freshest moved version + 7d = `npm-packlist@11.3.0`
  (published 2026-06-22T16:11:01Z) + 7d = **2026-06-29T16:11:01Z**, long past.
  Other moved versions older: `ignore-walk@9.0.0` (2026-05-08), `proc-log@7.0.0`
  (2026-05-08). No maturity one-shot needed.
- **Transitive set (benign):** headline `npm-packlist@11.3.0`; new resolutions
  `ignore-walk@9.0.0`, `proc-log@7.0.0`; `glob@^13.0.6` already satisfied in base.
  All published via GitHub OIDC trusted-publishing + SLSA provenance by the npm
  CLI team. No <24h version, no first-time-name package.
- **Advisories:** OSV + GitHub clean on every incoming moved version. Incoming
  exposure empty → cannot increase net advisory exposure.
- **Source/install:** scripts-disabled install rc=0 (`YARN_ENABLE_SCRIPTS=false`).
  `npm-packlist@11.3.0` is a single `index.js`, no bin/preinstall; `proc-log`
  dep-free, `ignore-walk` fs-walker. No network/child_process/telemetry.
- **Consumer:** sole consumer `scripts/check-package-uniformity.mjs` (lint tool).
  Ran `scripts/test/check-package-uniformity.test.mjs` against installed v11 on
  Node 22.23.2 → **18/18 pass**, incl. the two repo-wide packlist tests. v11's
  `files[]` glob-semantics break does not alter the tool's output. As a
  devDependency its raised Node engine (`^22.22.2 || ^24.15.0 || >=26.0.0`) does
  NOT constrain the repo's published `engines.node` — no migration required.
- **CI at rebased head `7017fb0d4a0`:** 20+ green (lint/build/test262/22.x+macOS
  test cells); the ONLY red is **`test (24.x, ubuntu-latest)`**, failed 4/4 with
  the runner losing its bundled node
  (`.../actions-runner/2.336.0/externals/node24/bin/node: No such file or
  directory` → SIGINT-kills the in-flight test; every test that ran passed). This
  is the **known floating-Node-24.x flake** (see PR endojs/endo-but-for-bots#1007 ledger 2026-08-16; also
  tracked blocking endojs/endo-but-for-bots#877 / endojs/endo-but-for-bots#475), non-required (no branch protection on `llm`;
  PR is MERGEABLE/UNSTABLE), and reddens only because a root-dep bump cold-runs
  the full suite. Passes reliably on base `llm`.
- **Why not executed:** `ci-wait-merge.sh` counts any FAILURE in the rollup as
  CI-RED and refuses to merge; no knob merges past a non-required flaky cell.
  Did NOT bypass the spine's CI gate unilaterally.
- **Backstop:** daily `dependabotany-recheck-endo-but-for-bots` already exists and
  will re-sweep this open PR. No maturity one-shot (already mature).
- **Escalation:** maintainer message sent — decision needed: merge endojs/endo-but-for-bots#1009 directly
  (green but for the known non-required flake, one-click mergeable), or resolve
  the fleet-wide floating-Node-24.x runner issue so the spine can auto-conduct
  (endojs/endo-but-for-bots#1009 / endojs/endo-but-for-bots#1007 / endojs/endo-but-for-bots#877 / endojs/endo-but-for-bots#475 all wait on it).

On recheck: re-fetch head (base `llm` may move → re-rebase), re-confirm advisory +
source diligence unchanged, and conduct via
`ci-wait-merge.sh endojs/endo-but-for-bots 1009 --dependabot-auto-merge`
(with `GARDEN_DEPENDABOT_LOGIN=app/dependabot` on any instance whose deployed
spine predates the `app/`-login gate fix) once the 24.x-ubuntu cell is green or
the maintainer authorizes the merge.
