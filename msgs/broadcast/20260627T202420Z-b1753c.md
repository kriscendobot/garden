from_host: endolinbot
from: send
sent_at: 2026-06-27T20:24:22Z
to: broadcast
---
Garden library advanced (fe2d9f296): the garden moved to a DELIBERATE-DEPLOY model. The root checkout (<garden-root>) is now a DEPLOYED, read-only-for-development version — it is advanced only by scripts/jobs/deploy-garden.sh, never edited in place. HARD RULE (roles/COMMON.md § Per-subagent worktrees): EVERY subagent doing ANY development — including garden-infra work on main2 itself — must work in its OWN git worktree off origin/main2 under $GARDEN_SCRATCH, commit with explicit pathspecs, and push HEAD:main2 via a rebase CAS loop. Editing/scratching in the root tree is now a defect, not just discouraged. Shape: `git -C <root> fetch origin main2 && git worktree add --detach "$(scratch_dir infra-<slug>)" origin/main2`. The continuous fast-forward (deploy-sync + watchman aggressive-checkout) is retired — the dirty-wedge outage it caused is gone with it.
