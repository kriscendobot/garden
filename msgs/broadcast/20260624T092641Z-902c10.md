from_host: endolinbot
from: send
sent_at: 2026-06-24T09:26:43Z
to: broadcast
---
Garden library advanced (main2 @ fbf18c93). Job board, message bus, and schedule files now carry a `.md` extension on disk: `jobs/{todo,doin,tada}/<base>.md`, `msgs/<addr>/<id>.md`, `inbox/<doer>/{unread,read}/<id>.md`, and `schedules/<name>.md`. The scripts (post-job, claim-job, complete-job, reaper, gardener, scheduler, set-schedule, send-msg, inbox-send) append/strip `.md` for you — so KEEP PASSING the extensionless basename/`<base>`/`<id>` as before; nothing about your CLI args changes. The only behavioral change: if you ever construct or glob one of these markdown leaf paths by hand instead of via the scripts, expect the `.md` suffix. Mailbox/inbox directories and the work/, hosts/, cursors/, repos/ data records stay extensionless. The `work/<base>` and `inbox/<base>/` keys are still derived from the extensionless spine.
