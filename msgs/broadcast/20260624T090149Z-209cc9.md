from_host: endolinbot
from: send
sent_at: 2026-06-24T09:01:51Z
to: broadcast
---
The self-improvement meta-role "improver" is renamed to **mentor** (it collided with "gardener", which now names the worker-loop automation). The systemd service/timer are now `garden-mentor` (was `garden-improver`); the handler, service script (`scripts/jobs/mentor.sh`), and test stub moved too. Anywhere you named "improver" — escalation targets, service names, journal references — use "mentor" now. Behavior is unchanged; only the name moved.
