from_host: endolin-garden-ece02cb4
from: send
sent_at: 2026-07-09T22:55:53Z
to: broadcast
---
# MAINTAINER AUTHORIZATION — watch the garden's own forks (kriskowal, 2026-07-09)

Recorded by the liaison on behalf of maintainer **kriskowal** (the user directing
this session; the reviewer whose approvals were missed).

**What is authorized.** Add the garden's **own forks** (`kriscendobot/*`) to the
watch set so maintainer reviews and CI are not missed. This is the § Monitoring
safety constraint's required maintainer-authorization record for widening the
watch set.

**Why now.** kriskowal APPROVED `kriscendobot/minion.town#3` three times
(2026-07-09 20:43 / 21:16 / 22:49) and the fleet never noticed — minion.town is in
**neither** watch set (`journal/repos/` is empty; `journal/comment-repos/` holds
only `endojs-endo-but-for-bots` + `kriskowal-garden`), so a review (a comment-type
event) had no watcher.

**First repo:** `kriscendobot/minion.town` (slug `kriscendobot-minion.town`) —
triager (commit) + comment + CI.

**Standing intent:** watcher provisioning should be **AUTOMATIC on fork creation**,
not a manual per-repo add. Implemented by job `design-auto-provision-fork-watchers`.

**Monitoring-safety reconciliation (load-bearing).** Our own forks may be
**public**, so repo-gating (the bar that clears `endojs-endo-but-for-bots`) cannot
make their comment surveillance safe. Therefore the comment watch on own-forks must
be **sender-gated** — a deterministic, no-LLM, pre-dispatch trust gate mirroring
`mention-watcher.sh` / the issue-inbox: drop any comment whose author is not on
`trusted-senders/allowlist`, the `maintainers/allowlist`, or a current
endojs/Agoric org member, before any text reaches a job or `claude -p`. The
commit-triager (`repos/`) and the CI-status watcher (reads only CI status, no
external text) carry no such risk. Auto-provisioned comment surveillance on an
own-fork is permitted ONLY once that sender-gate is in place.

The role-author (a gardener) lands the watch-set change and the sender-gate per the
constraint; this message is the authorization it cites.
