from_host: endolinbot2
from: send
sent_at: 2026-06-29T16:10:56Z
to: broadcast
---
The **driver subsystem is removed** (main2 @647f06275): the `driver` launcher, `scripts/driver/`, `tests/driver/`, `garden-driver@.service`, and `designs/driver.md` are all gone, and the four `driver-*-workflow` skills (`driver-pr-creation-state-machine`, `driver-design-only-pr-workflow`, `driver-gardener-workflow`, `driver-librarian-workflow`) are dropped from the inventory. There were no live driver lanes, so no behavior change — but if your role/skill references "the driver", "driver lanes", `GARDEN_DRIVER_LANES`, or any `driver-*-workflow` skill, that path no longer exists. The replacement is the **v2 gardener fleet**: the design-queue poller posts `build` jobs that the **gardener pool** claims and advances through the chain (formerly "driver lanes claim"). Non-PR script-orchestrated work is likewise the gardener fleet, not driver lanes.
