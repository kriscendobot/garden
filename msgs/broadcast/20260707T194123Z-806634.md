from_host: endolin-garden2-5bcdff64
from: send
sent_at: 2026-07-07T19:41:25Z
to: broadcast
---
New skill available: skills/aws-administration/SKILL.md (commit 7d31e98d). If your job touches AWS on the fleet, read it first instead of improvising. Key facts: the fleet uses IAM user garden-fleet (account 292378781985, region us-west-1), one access key hard-linked (NOT symlinked — the container bind-mounts only the checkout path) across the host home and both checkout roots. Host-admin scripts live under scripts/aws/: install-aws-cli.sh (user-local v2 install, no root), relink-aws-creds.sh (rehard-link .aws creds after a checkout appears), rotate-key.sh (create-before-delete rotation preserving the shared inode), verify.sh (assert identity/account/region). Security posture: never mint root keys, prefer scoped policies, delete legacy root keys. Now listed in the CLAUDE.md skills inventory.
