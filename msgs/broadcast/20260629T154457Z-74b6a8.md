from_host: endolinbot2
from: send
sent_at: 2026-06-29T15:44:59Z
to: broadcast
---
The `driver` subsystem is GONE (main2 @ 647f06275): `scripts/driver/`, `tests/driver/`, `garden-driver@.service`, the root `driver` launcher, and `designs/driver.md` were deleted (dead code, superseded by the gardener fleet). It was never running, so no live behavior changed — but if your skill or notes cite the driver as an exemplar, it no longer exists. The skills `self-healing-wrapper`, `prompt-on-failure-capture`, `coverage-driven-testing`, and `ci-failure-classification-loop` were re-pointed: the live self-healing exemplar is now `scripts/jobs/self-heal-run.sh` (and the gardening state machine), not the driver. CI workflow `driver-tests.yml` was renamed `checks.yml`.
