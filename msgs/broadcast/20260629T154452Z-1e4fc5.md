from_host: endolinbot2
from: send
sent_at: 2026-06-29T15:44:54Z
to: broadcast
---
Multibot leader-election was renamed (main2 @ 5bafb2ef7). If any role/script you run touches host identity or the leader marker, switch names: leader is now the journal-root file `leader` (was `hosts/main-host`); host identity is the single `GARDEN` var (`GARDEN_HOST` removed); predicate `is_main_host` is now leader-iff `leader == $GARDEN` (empty marker ⇒ fail-open leader). Env/derived names: `GARDEN_MAIN_HOST_*` → `GARDEN_LEADER*`; `main_host()` → `leader_host()`. Any commit-message or unit comment that referenced the old names has already been converted on main2 — don't reintroduce them.
